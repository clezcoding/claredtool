---
phase: quick-260830-6hk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src-tauri/Cargo.toml
  - apps/desktop/src-tauri/Cargo.lock
  - apps/desktop/src-tauri/src/lib.rs
  - .github/workflows/desktop-build.yml
  - .github/workflows/backend-image.yml
autonomous: true
requirements:
  - DEP-59
  - DEP-61
  - DEP-62
  - DEP-63
  - DEP-64
  - DEP-65
  - NODE-24
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Desktop crate depends on getrandom 0.4 and SQLCipher key generation compiles against fill (DEP-62, D-03)"
    - "download-artifact major 8 is in desktop-build.yml as a 40-char SHA plus version comment (DEP-65, D-02, D-04)"
    - "backend-image.yml docker cluster is setup-buildx 4, login 4, metadata 6, build-push 7, SHA-pinned twice for build-push (DEP-64, DEP-63, DEP-61, DEP-59, D-04)"
    - "Runtime Node pin stays 24 in .nvmrc, backend Dockerfile, and every workflow node-version (NODE-24, D-01)"
    - "Dependabot floating-tag diffs are not applied; PRs close only after versions exist on the branch (D-04, D-05)"
  artifacts:
    - apps/desktop/src-tauri/Cargo.toml
    - apps/desktop/src-tauri/Cargo.lock
    - apps/desktop/src-tauri/src/lib.rs
    - .github/workflows/desktop-build.yml
    - .github/workflows/backend-image.yml
    - .nvmrc
    - apps/backend/Dockerfile
    - .github/dependabot.yml
  key_links:
    - from: load_or_create_sqlcipher_key_hex
      to: getrandom::fill
      via: both empty-keychain arms fill 32 bytes then hex-encode
      pattern: "grep -n 'getrandom::fill' apps/desktop/src-tauri/src/lib.rs"
    - from: clared Cargo.toml getrandom
      to: Cargo.lock clared package deps
      via: cargo update --precise 0.4.3 then --locked check
      pattern: "grep -A2 'name = \"clared\"' apps/desktop/src-tauri/Cargo.lock | grep -F 'getrandom 0.4'"
    - from: desktop-build publish job
      to: actions/download-artifact v8 SHA
      via: SHA comment # v8
      pattern: "grep -E 'download-artifact@[0-9a-f]{40}.*# v8' .github/workflows/desktop-build.yml"
    - from: .nvmrc / Dockerfile / workflows
      to: Node 24 LTS
      via: identical major; dependabot docker ignore node >=25
      pattern: "grep -E 'node-version: 24|^24$|node:24-bookworm-slim' .nvmrc apps/backend/Dockerfile .github/workflows/ci.yml .github/workflows/desktop-build.yml"
---

<objective>
Land all six open Dependabot upgrades on one branch from origin/main, with the getrandom 0.4 API migration so desktop CI compiles. Keep Node 24 everywhere (D-01). Do not merge red #62 as-is (D-03). Do not cherry-pick dirty action PRs that rewrite SHA pins into floating tags (D-04). After this plan commits, orchestrator pushes, opens one PR, watches CI, fixes, merges; Dependabot PRs then close as superseded (D-05).

Purpose: Apply the upgrades for real so open Dependabot PRs resolve by landing versions, not by dismiss.
Output: getrandom 0.4 + fill on the SQLCipher key path; action majors confirmed SHA-pinned; Node 24 unchanged.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/desktop/src-tauri/Cargo.toml
@apps/desktop/src-tauri/src/lib.rs
@.github/workflows/desktop-build.yml
@.github/workflows/backend-image.yml
@.github/dependabot.yml
@apps/backend/Dockerfile
@.nvmrc

Locked inventory (2026-08-30): open PRs #65 download-artifact 7→8, #64 setup-buildx 3→4, #63 login-action 3→4, #61 metadata-action 5→6, #59 build-push 6→7 (two uses), #62 getrandom 0.2.17→0.4.3. No npm Dependabot PRs. No Node 25/26 PRs.

origin/main already merged #67 (c9b514d): those five GitHub Actions majors are SHA-pinned (download-artifact 3e5f45b2… # v8.0.1; setup-buildx 37fe6310… # v4.3.0; login dbcb8138… # v4.6.0; metadata dc802804… # v6.2.0; build-push 53b7df96… # v7.3.0 twice). Dependabot PRs #59/#61/#63/#64/#65 are dirty against pre-#67 floating tags — cherry-pick would undo F-SEC1.

#62 is based on current main, Cargo.toml+lock only; CI red: cannot find function getrandom in crate getrandom at lib.rs:42 and :50. Context7 getrandom 0.4: fill(&mut bytes) replaces the 0.2-era function. Desktop uses OS CSPRNG only for SQLCipher key hex — no wasm_js, no custom backend cfg.

Node 24 restored by #69 after a brief 26 excursion. Lock: .nvmrc, FROM node:24-bookworm-slim, node-version: 24, dependabot docker ignore node >=25.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end getrandom 0.4 + fill on SQLCipher key path (DEP-62, D-03)</name>
  <precondition>Working tree is a new branch from origin/main (not main). rust-toolchain.toml pins 1.97.1. Cargo.toml still requires getrandom 0.2.</precondition>
  <reversibility rating="costly">Direct crate major 0.2 to 0.4; lockfile retarget. Revert is a Cargo.toml + lockfile restore, not a data migration.</reversibility>
  <files>apps/desktop/src-tauri/Cargo.toml, apps/desktop/src-tauri/Cargo.lock, apps/desktop/src-tauri/src/lib.rs</files>
  <behavior>
    - cargo check of apps/desktop/src-tauri fails until both load_or_create_sqlcipher_key_hex arms use the 0.4 fill API
    - After GREEN: cargo check --locked succeeds; clared lockfile package lists getrandom 0.4.3
    - fill errors still map to String via map_err; no unwrap, no all-zero key on RNG failure
  </behavior>
  <action>
    Branch from origin/main (D-02). Do not merge or cherry-pick github.com/clezcoding/claredtool/pull/62.

    In apps/desktop/src-tauri/Cargo.toml set the getrandom crate requirement to 0.4 (same style as neighboring crate lines). From apps/desktop/src-tauri run: cargo update -p getrandom --precise 0.4.3 so the clared package in Cargo.lock lists getrandom 0.4.3. Transitive 0.2 copies for other crates may remain; do not force a single-version tree.

    In load_or_create_sqlcipher_key_hex (both empty-keychain arms, currently around lines 42 and 50), switch each RNG call to the 0.4 fill API on the 32-byte buffer, then map_err to String. Keep hex encode + keychain set_password. Do not enable wasm_js, js, custom, rdrand, or getrandom_backend cfg (D-03). Do not change SQLCipher PRAGMA / auto_extension wiring.
    <!-- planner-discipline-allow: getrandom::fill -->

    Do not bump Node, Docker node image, or workflow node-version (D-01). Do not edit GitHub Actions uses lines in this task.
  </action>
  <verify>
    <automated>grep -F 'getrandom = "0.4"' apps/desktop/src-tauri/Cargo.toml &amp;&amp; test "$(grep -v '^#' apps/desktop/src-tauri/src/lib.rs | grep -c 'getrandom::fill')" -ge 2 &amp;&amp; test "$(grep -v '^#' apps/desktop/src-tauri/src/lib.rs | grep -c 'getrandom::getrandom')" -eq 0 &amp;&amp; cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml --locked</automated>
  </verify>
  <done>clared depends on getrandom 0.4.3; both SQLCipher key arms use fill; cargo check --locked passes; #62 not merged as-is.</done>
</task>

<task type="auto">
  <name>Confirm SHA-pinned action majors and Node 24 lock (DEP-59–65, NODE-24, D-01, D-04)</name>
  <files>.github/workflows/desktop-build.yml, .github/workflows/backend-image.yml, .nvmrc, apps/backend/Dockerfile, .github/workflows/ci.yml, .github/dependabot.yml</files>
  <action>
    On the same combined branch (D-02), assert origin/main #67 pins already satisfy the five action Dependabot majors. Expected (keep if present; do not replace with @vN tags):

    - desktop-build.yml download-artifact SHA 3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c comment v8 (DEP-65)
    - backend-image.yml setup-buildx SHA 37fe631027851001ddb9b187196cc803df7f5f0e comment v4 (DEP-64)
    - backend-image.yml login-action SHA dbcb813823bdd20940b903addbd779551569679f comment v4 (DEP-63)
    - backend-image.yml metadata-action SHA dc802804100637a589fabce1cb79ff13a1411302 comment v6 (DEP-61)
    - backend-image.yml build-push-action SHA 53b7df96c91f9c12dcc8a07bcb9ccacbed38856a comment v7 on both api and worker steps (DEP-59)

    If any of those majors is missing, resolve the release commit via gh api and pin owner/repo@40-char-sha with a version comment, same pattern as #67. Never apply the dirty Dependabot workflow patches (they rewrite SHA pins to floating tags) (D-04).

    Node 24 lock (D-01, NODE-24): .nvmrc is the single token 24; Dockerfile FROM node:24-bookworm-slim; every setup-node step pins that same major; dependabot.yml docker ignore for dependency-name node versions &gt;=25 stays. Do not raise Node major. Do not add an engines field.
    <!-- planner-discipline-allow: node-version: 24 -->

    Do not gh pr close, dependabot ignore, or dismiss #59–#65/#62. Orchestrator push/PR/CI/merge after SUMMARY; those PRs close as superseded once versions are on main (D-05).
  </action>
  <verify>
    <automated>grep -E 'download-artifact@[0-9a-f]{40}.*# v8' .github/workflows/desktop-build.yml &amp;&amp; grep -E 'setup-buildx-action@[0-9a-f]{40}.*# v4' .github/workflows/backend-image.yml &amp;&amp; grep -E 'login-action@[0-9a-f]{40}.*# v4' .github/workflows/backend-image.yml &amp;&amp; grep -E 'metadata-action@[0-9a-f]{40}.*# v6' .github/workflows/backend-image.yml &amp;&amp; test "$(grep -cE 'build-push-action@[0-9a-f]{40}.*# v7' .github/workflows/backend-image.yml)" -eq 2 &amp;&amp; test "$(grep -v '^#' .github/workflows/*.yml | grep -cE 'uses: .+/[^@]+@v[0-9]')" -eq 0 &amp;&amp; test "$(cat .nvmrc)" = "24" &amp;&amp; grep -F 'FROM node:24-bookworm-slim' apps/backend/Dockerfile &amp;&amp; test "$(grep -vE '^\s*#' .github/workflows/ci.yml .github/workflows/desktop-build.yml | grep -c 'node-version: 24')" -ge 5 &amp;&amp; test "$(grep -vE '^\s*#' .github/workflows/ci.yml .github/workflows/desktop-build.yml | grep -cE 'node-version: 2[56]')" -eq 0 &amp;&amp; grep -A2 'dependency-name: node' .github/dependabot.yml | grep -F '&gt;=25'</automated>
  </verify>
  <done>Five action majors present as SHA plus comments; no floating @vN uses in workflows; Node 24 pins and dependabot ignore intact; Dependabot PRs left open for orchestrator supersede-close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → third-party actions | Mutable @vN tags can be retargeted; SHA pins are the supply-chain control from #67 |
| Desktop Rust → OS CSPRNG | SQLCipher key material must come from getrandom fill; failure must not yield a zero key |
| CI/Docker → Node runtime | Accidental Node 25/26 re-pin breaks the LTS lock just restored by #69 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-6hk-01 | Tampering | .github/workflows/*.yml | high | mitigate | D-04: keep 40-char SHA pins; do not cherry-pick dirty Dependabot floating-tag diffs |
| T-6hk-02 | Information disclosure | load_or_create_sqlcipher_key_hex | high | mitigate | fill() errors map_err to String; no unwrap and no all-zero fallback (D-03) |
| T-6hk-03 | Elevation of privilege | Node runtime pins | medium | mitigate | D-01: .nvmrc, Dockerfile, node-version stay 24; dependabot ignore node >=25 |
| T-6hk-04 | Tampering | getrandom backends | low | accept | Desktop is macOS/Windows OS CSPRNG only; wasm_js/custom not enabled |
| T-6hk-SC | Tampering | cargo update getrandom | medium | accept | Existing crates.io getrandom (rust-random); version bump of a crate already in tree, not a new package |
</threat_model>

<verification>
Task 1 cargo check --locked plus fill grep. Task 2 SHA-comment greps plus Node 24 greps. Orchestrator: push branch, one PR covering all six versions, watch CI, fix, merge; then confirm Dependabot PRs close as superseded (not dismissed).
</verification>

<success_criteria>
- getrandom 0.4.3 + fill compiles on the SQLCipher key path
- Action majors 8/4/4/6/7 present as SHA pins
- Node 24 unchanged
- No Dependabot PR closed without those versions on the branch
</success_criteria>

<output>
Create .planning/quick/260830-6hk-resolve-all-open-dependabot-prs-by-actua/260830-6hk-SUMMARY.md when done
</output>

<!-- gsd-multi-source-audit
GOAL: Apply all six open Dependabot upgrades (not dismiss); Node 24 lock
REQ: DEP-59, DEP-61, DEP-62, DEP-63, DEP-64, DEP-65, NODE-24
RESEARCH: none (quick, no research phase); Context7 getrandom 0.4 fill; #67 already SHA-pinned action majors
CONTEXT: D-01 Node 24, D-02 combined branch, D-03 fill not merge #62, D-04 SHA not floating tags, D-05 supersede-close
COVERED: DEP-62+D-03→task1; DEP-59/61/63/64/65+NODE-24+D-01+D-04+D-05→task2; D-02 both tasks
DEFERRED: none
-->
