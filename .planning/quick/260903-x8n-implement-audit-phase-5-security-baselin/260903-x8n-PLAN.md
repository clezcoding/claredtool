---
phase: quick-260903-x8n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/zizmor.yml
  - .github/workflows/gitleaks.yml
  - .github/workflows/codeql.yml
  - .github/workflows/backend-image.yml
  - .github/workflows/ci.yml
  - .github/workflows/desktop-build.yml
  - docs/claredtool-phasenplan.md
autonomous: true
requirements:
  - G1
  - G2
  - G3
  - G6
  - AUDIT-5
  - AUDIT-5b
  - NODE-24
user_setup:
  - service: github-security-tab
    why: "First-run alert triage for zizmor, gitleaks, Trivy, and CodeQL is GitHub UI only (D-10). YAML cannot mark alerts false-positive."
    dashboard_config:
      - task: "After the first runs, open Security and triage code-scanning / secret-scanning alerts (fix vs accept). Do not turn on CodeQL default setup in Settings — codeql.yml is the setup (D-02)."
        location: "GitHub → Security (code scanning + secret scanning). Not Settings → Code security → default setup."
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "zizmor.yml runs on push to main and on pull_request, with empty workflow permissions, job security-events write plus contents/actions read, checkout persist-credentials false, and the pinned zizmor-action (G1, D-03, D-04, D-05)"
    - "gitleaks.yml runs on push and pull_request, checkout fetch-depth 0 and persist-credentials false, gitleaks-action env is GITHUB_TOKEN only (G3, D-07)"
    - "backend-image build job scans both the API sha tag and the worker -worker tag with Trivy after both docker/build-push-action steps; HIGH,CRITICAL fail the job; unfixed are ignored (G2)"
    - "codeql.yml analyzes javascript-typescript and rust with fail-fast false on push/PR to main plus weekly cron (G6, D-02)"
    - "Harden Runner is the first step of every job in ci.yml, backend-image.yml, and desktop-build.yml with egress-policy audit (5b.2, D-08)"
    - "Every new job has timeout-minutes (D-06). New workflows start with permissions: {} (D-05)"
    - "setup-node node-version stays 24 wherever it already exists; pnpm/action-setup SHA is untouched (D-01)"
    - "docs/claredtool-phasenplan.md is tracked. Product ROADMAP Phase 05 is not edited (D-09)"
  artifacts:
    - .github/workflows/zizmor.yml
    - .github/workflows/gitleaks.yml
    - .github/workflows/codeql.yml
    - .github/workflows/backend-image.yml
    - .github/workflows/ci.yml
    - .github/workflows/desktop-build.yml
    - docs/claredtool-phasenplan.md
  key_links:
    - from: zizmor.yml
      to: .github/workflows/**
      via: zizmor-action after checkout persist-credentials false
      pattern: "grep -F 'zizmorcore/zizmor-action@6fc4b006235f201fdab3722e17240ab420d580e5' .github/workflows/zizmor.yml"
    - from: gitleaks.yml
      to: full git history
      via: fetch-depth 0 plus GITHUB_TOKEN only
      pattern: "grep -F 'fetch-depth: 0' .github/workflows/gitleaks.yml"
    - from: backend-image build
      to: GHCR API and worker images
      via: two trivy-action steps after both build-push steps
      pattern: "grep -c 'aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25' .github/workflows/backend-image.yml"
    - from: codeql.yml matrix
      to: github/codeql-action init autobuild analyze
      via: same SHA cdf488f595d80d6e07e03d4674febd5ab45fa938 and category /language:${{ matrix.language }}
      pattern: "grep -c 'cdf488f595d80d6e07e03d4674febd5ab45fa938' .github/workflows/codeql.yml"
    - from: ci.yml backend-image.yml desktop-build.yml
      to: step-security/harden-runner
      via: first uses of every job, egress-policy audit
      pattern: "grep -c 'step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1' .github/workflows/ci.yml .github/workflows/backend-image.yml .github/workflows/desktop-build.yml"
---

<objective>
Implement audit Phase 5 (G1 zizmor, G3 gitleaks, G2 Trivy) and Phase 5b (G6 CodeQL, Harden-Runner audit) from docs/claredtool-phasenplan.md on the public repo (D-02). SHA-pin every new action (D-03). Keep Node 24 (D-01). Track the currently untracked audit source markdown.

Purpose: Close the free-tool gap (G1–G3, G6, egress audit) while the repo is public. These scans do not exist today.
Output: Three new workflows, Trivy on both GHCR images, Harden-Runner first step on the three listed workflow files, phasenplan.md committed. No ROADMAP product Phase 05 edits. No Phase 6/7 work (D-09).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/claredtool-phasenplan.md
@.github/workflows/ci.yml
@.github/workflows/backend-image.yml
@.github/workflows/desktop-build.yml

This is AUDIT Phase 5, not GSD product Phase 05 (PDF/Offline/Audit). Do not edit .planning/ROADMAP.md. Do not edit .planning/STATE.md current_phase. Do not add Squawk, SBOM, cosign, cargo-check matrix, workflow_run deploy gates, or bundle-id changes (D-09). Do not create a Kaneo task.

Reuse checkout pin actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1 (D-04). Do not change pnpm/action-setup SHA 0977fd99725f1db4007ccb2928dbb4e90d06cc86. Keep every existing setup-node node-version: 24 including backend-image migrate (D-01).

Exact pins (peeled 2026-09-03):
- zizmorcore/zizmor-action@6fc4b006235f201fdab3722e17240ab420d580e5 # v0.6.1
- gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e # v3.0.0
- aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
- step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
- github/codeql-action/init, autobuild, analyze all @cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9

Match existing comment style: 40-char SHA then space hash then version. No emoji in workflow names (existing files are lowercase identifiers).

Harden-Runner jobs (every one, first step): ci.yml changes, lint, desktop-test, backend-unit, backend-e2e, ci; backend-image.yml build, migrate, deploy; desktop-build.yml tauri, publish-faynosync. Do not add Harden-Runner to labeler.yml, sync-labels.yml, or the new scan workflows.

Human leftover (D-10): first-run Security-tab triage. Documented in user_setup. Do not add a blocking checkpoint.
</context>

<tasks>

<task type="tracer">
  <name>End-to-end zizmor workflow — one PR scan path (G1, D-03–D-06)</name>
  <files>.github/workflows/zizmor.yml</files>
  <action>
    Create .github/workflows/zizmor.yml as the single path that scans GitHub Actions YAML on push to main and on pull_request (G1). Follow the official zizmor-action skeleton; drop rainbow emoji from name so it matches existing lowercase workflow names.

    Workflow-level: on.push.branches [main]; on.pull_request (no extra path filters); permissions: {} (D-05).

    One job zizmor: runs-on ubuntu-latest; timeout-minutes: 10 (D-06); job permissions security-events: write, contents: read, actions: read.

    Steps in order: checkout at actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1 with persist-credentials: false (D-04); then zizmorcore/zizmor-action@6fc4b006235f201fdab3722e17240ab420d580e5 # v0.6.1 (D-03). No extra action inputs unless the action requires them to upload results. Do not add Harden-Runner here.

    Do not edit ci.yml, backend-image.yml, or desktop-build.yml in this task. Do not add .zizmor.yml yet (finding fixes belong in task 3 after the other YAML exists).
  </action>
  <verify>
    <automated>test -f .github/workflows/zizmor.yml &amp;&amp; grep -F 'zizmorcore/zizmor-action@6fc4b006235f201fdab3722e17240ab420d580e5 # v0.6.1' .github/workflows/zizmor.yml &amp;&amp; grep -F 'actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1' .github/workflows/zizmor.yml &amp;&amp; grep -F 'persist-credentials: false' .github/workflows/zizmor.yml &amp;&amp; grep -F 'permissions: {}' .github/workflows/zizmor.yml &amp;&amp; grep -F 'security-events: write' .github/workflows/zizmor.yml &amp;&amp; grep -F 'contents: read' .github/workflows/zizmor.yml &amp;&amp; grep -F 'actions: read' .github/workflows/zizmor.yml &amp;&amp; grep -F 'timeout-minutes:' .github/workflows/zizmor.yml &amp;&amp; grep -F 'branches:' .github/workflows/zizmor.yml &amp;&amp; grep -F 'pull_request:' .github/workflows/zizmor.yml &amp;&amp; python3 -c "import yaml; yaml.safe_load(open('.github/workflows/zizmor.yml'))"</automated>
  </verify>
  <done>zizmor.yml exists, SHA-pinned, empty top-level permissions, persist-credentials false, timeout set, parses as YAML. PRs will get a workflow-audit job.</done>
</task>

<task type="auto">
  <name>gitleaks.yml, codeql.yml, track phasenplan (G3, G6, D-02, D-07)</name>
  <files>.github/workflows/gitleaks.yml, .github/workflows/codeql.yml, docs/claredtool-phasenplan.md</files>
  <action>
    Add the two remaining new scan workflows and git-add the untracked audit source. Do not edit existing workflow files (D-02, G3, G6).

    gitleaks.yml (G3): on.push and on.pull_request (no branch filter). permissions: {} (D-05). Job gitleaks: ubuntu-latest, timeout-minutes: 10 (D-06), job permissions contents: read. Steps: checkout same SHA as task 1 with fetch-depth: 0 and persist-credentials: false (D-04); then gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e # v3.0.0 with env GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} only (D-07). Do not add a gitleaks product-license secret. Do not create .gitleaksignore unless a false-positive must ship in this same commit; if created, put the rule id and a one-line reason in a comment in that file.

    codeql.yml (G6, D-02): on.push.branches [main]; on.pull_request.branches [main]; on.schedule cron 18 5 * * 1 (weekly Monday 05:18 UTC). permissions: {} (D-05). Job analyze: ubuntu-latest, timeout-minutes: 45 (D-06), permissions actions: read, contents: read, security-events: write. strategy fail-fast: false, matrix language: ['javascript-typescript', 'rust']. Steps: checkout same SHA with persist-credentials: false; github/codeql-action/init@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9 with languages: ${{ matrix.language }}; github/codeql-action/autobuild@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9; github/codeql-action/analyze@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9 with category: /language:${{ matrix.language }}. Same SHA on init, autobuild, and analyze (D-03). Do not add a separate upload-sarif step. Do not add Harden-Runner. Do not enable GitHub UI default setup.

    docs/claredtool-phasenplan.md: add the existing untracked file as-is (git add). Do not rewrite the analysis. Do not touch .planning/ROADMAP.md (D-09).
  </action>
  <verify>
    <automated>test -f .github/workflows/gitleaks.yml &amp;&amp; test -f .github/workflows/codeql.yml &amp;&amp; test -f docs/claredtool-phasenplan.md &amp;&amp; grep -F 'gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e # v3.0.0' .github/workflows/gitleaks.yml &amp;&amp; grep -F 'fetch-depth: 0' .github/workflows/gitleaks.yml &amp;&amp; grep -F 'persist-credentials: false' .github/workflows/gitleaks.yml &amp;&amp; grep -F 'permissions: {}' .github/workflows/gitleaks.yml &amp;&amp; grep -F 'GITHUB_TOKEN' .github/workflows/gitleaks.yml &amp;&amp; ! grep -F 'GITLEAKS_LICENSE' .github/workflows/gitleaks.yml &amp;&amp; grep -F 'javascript-typescript' .github/workflows/codeql.yml &amp;&amp; grep -F 'rust' .github/workflows/codeql.yml &amp;&amp; grep -F 'fail-fast: false' .github/workflows/codeql.yml &amp;&amp; grep -F 'github/codeql-action/init@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9' .github/workflows/codeql.yml &amp;&amp; grep -F 'github/codeql-action/autobuild@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9' .github/workflows/codeql.yml &amp;&amp; grep -F 'github/codeql-action/analyze@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9' .github/workflows/codeql.yml &amp;&amp; grep -F '/language:${{ matrix.language }}' .github/workflows/codeql.yml &amp;&amp; grep -F 'schedule:' .github/workflows/codeql.yml &amp;&amp; grep -F 'permissions: {}' .github/workflows/codeql.yml &amp;&amp; python3 -c "import yaml; yaml.safe_load(open('.github/workflows/gitleaks.yml')); yaml.safe_load(open('.github/workflows/codeql.yml'))"</automated>
  </verify>
  <done>gitleaks scans full history with GITHUB_TOKEN only; CodeQL matrix covers JS/TS and Rust on main plus weekly cron; phasenplan.md is in the tree. No product ROADMAP edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Trivy both images plus Harden-Runner first step (G2, 5b.2, D-01, D-08)</name>
  <files>.github/workflows/backend-image.yml, .github/workflows/ci.yml, .github/workflows/desktop-build.yml</files>
  <behavior>
    - Trivy runs only after both docker/build-push-action steps in the build job, once for the API sha tag and once for the worker tag with -worker suffix
    - Each of the eleven listed jobs starts with Harden Runner using egress-policy audit and the pinned SHA
    - Existing setup-node node-version: 24 lines and pnpm/action-setup SHA are byte-identical to before this task
  </behavior>
  <action>
    Patch the three existing workflows. Do not add Phase 6/7 jobs (D-09). Keep migrate setup-node at 24 (D-01). Do not retouch pnpm/action-setup.

    Trivy (G2): In backend-image.yml job build, AFTER both docker/build-push-action steps (API image then worker target), add two aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0 steps. Do not insert a scan between the two builds. First scan image-ref ${{ steps.image.outputs.image }}:${{ steps.image.outputs.sha_tag }}. Second scan the same image-ref with -worker appended to the tag. Both steps: scan-type: image, format: table, exit-code: '1', ignore-unfixed: true, severity: HIGH,CRITICAL. Login to GHCR already exists; do not add a second login. Do not set format to sarif. If a HIGH/CRITICAL with a fix exists in the current node:24-bookworm-slim image, either bump only the patch/digest while keeping the node:24-bookworm-slim major or add a .trivyignore listing the CVE plus a one-line reason. Do not change the Node major.

    Harden-Runner (5b.2, D-08): Insert this as the FIRST step of every job listed in context (six in ci.yml, three in backend-image.yml, two in desktop-build.yml). YAML name Harden Runner; uses step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1; with egress-policy: audit only. No other with: keys. No if: on this step (it must run even when later migrate steps are skipped). Do not switch the egress policy to deny-unlisted. Do not add an allowlist this phase. Do not add Harden-Runner to labeler.yml, sync-labels.yml, zizmor.yml, gitleaks.yml, or codeql.yml.

    After the YAML edits: if zizmor or docker is available, scan .github/workflows. For critical injection or overly broad permissions, fix the workflow (prefer persist-credentials false on the flagged checkout, or narrow job permissions). Add .zizmor.yml only for a documented false-positive, with a one-line rationale per ignore. Prefer fix.

    Do not edit .planning/ROADMAP.md. First-run Security-tab triage stays human (D-10); do not pause for it.
  </action>
  <verify>
    <automated>test "$(grep -c 'aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0' .github/workflows/backend-image.yml)" -eq 2 &amp;&amp; grep -F 'scan-type: image' .github/workflows/backend-image.yml &amp;&amp; grep -F "exit-code: '1'" .github/workflows/backend-image.yml &amp;&amp; grep -F 'ignore-unfixed: true' .github/workflows/backend-image.yml &amp;&amp; grep -F 'HIGH,CRITICAL' .github/workflows/backend-image.yml &amp;&amp; grep -F '${{ steps.image.outputs.image }}:${{ steps.image.outputs.sha_tag }}' .github/workflows/backend-image.yml &amp;&amp; grep -F '${{ steps.image.outputs.image }}:${{ steps.image.outputs.sha_tag }}-worker' .github/workflows/backend-image.yml &amp;&amp; python3 -c "
from pathlib import Path
import re, sys
text = Path('.github/workflows/backend-image.yml').read_text()
bp = [m.start() for m in re.finditer(r'docker/build-push-action@', text)]
tv = [m.start() for m in re.finditer(r'aquasecurity/trivy-action@', text)]
sys.exit(0 if len(bp)==2 and len(tv)==2 and max(bp) &lt; min(tv) else 1)
" &amp;&amp; test "$(grep -c 'step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1' .github/workflows/ci.yml)" -eq 6 &amp;&amp; test "$(grep -c 'step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1' .github/workflows/backend-image.yml)" -eq 3 &amp;&amp; test "$(grep -c 'step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1' .github/workflows/desktop-build.yml)" -eq 2 &amp;&amp; test "$(grep -c 'egress-policy: audit' .github/workflows/ci.yml .github/workflows/backend-image.yml .github/workflows/desktop-build.yml)" -eq 11 &amp;&amp; ! grep -F 'egress-policy: block' .github/workflows/ci.yml .github/workflows/backend-image.yml .github/workflows/desktop-build.yml &amp;&amp; python3 -c "
from pathlib import Path
import re, sys
pin = 'step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1'
expect = {'.github/workflows/ci.yml': 6, '.github/workflows/backend-image.yml': 3, '.github/workflows/desktop-build.yml': 2}
ok = True
for f, n in expect.items():
    t = Path(f).read_text()
    blocks = re.findall(r'^    steps:\n((?:.*\n)+?)(?=^  [A-Za-z]|\Z)', t, re.M)
    if len(blocks) != n:
        print(f, 'steps-blocks', len(blocks), 'want', n)
        ok = False
        continue
    for i, b in enumerate(blocks):
        uses = [ln for ln in b.splitlines() if 'uses:' in ln]
        if not uses or pin not in uses[0]:
            print(f, 'job', i, 'first uses is not harden-runner')
            ok = False
sys.exit(0 if ok else 1)
" &amp;&amp; grep -F 'node-version: 24' .github/workflows/ci.yml .github/workflows/backend-image.yml .github/workflows/desktop-build.yml &amp;&amp; ! grep -E 'node-version:[[:space:]]*(20|22|25|26)' .github/workflows/ci.yml .github/workflows/backend-image.yml .github/workflows/desktop-build.yml &amp;&amp; grep -F 'pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86' .github/workflows/ci.yml .github/workflows/backend-image.yml .github/workflows/desktop-build.yml &amp;&amp; python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); yaml.safe_load(open('.github/workflows/backend-image.yml')); yaml.safe_load(open('.github/workflows/desktop-build.yml'))"</automated>
  </verify>
  <done>Both GHCR images are Trivy-scanned after both pushes; eleven jobs start with Harden Runner audit; Node 24 and pnpm/action-setup SHA unchanged. Human Security-tab triage remains leftover, not a gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fork PR → GITHUB_TOKEN / workflow YAML | Untrusted PR can change workflow files; zizmor plus persist-credentials false on new checkouts |
| Git history → gitleaks | Full clone (fetch-depth 0) sees every blob; findings go to logs / Security |
| GHCR image → Trivy | Built API and worker images scanned on the runner after push |
| Runner egress → internet | Harden-Runner records destinations in audit; policy is not deny-unlisted |
| CodeQL → GitHub code scanning | SARIF upload with security-events: write on public repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-x8n-01 | Tampering | new GitHub Actions | high | mitigate | SHA-pin every new uses: line with # version comment (D-03); reuse existing checkout pin |
| T-x8n-02 | Information | checkout credentials | high | mitigate | persist-credentials: false on all NEW workflow checkouts (D-04) |
| T-x8n-03 | Information | git history secrets | high | mitigate | gitleaks with fetch-depth 0; env GITHUB_TOKEN only (G3, D-07) |
| T-x8n-04 | Tampering | GHCR API/worker images | high | mitigate | Trivy image scans after both builds; fail on HIGH,CRITICAL with fixes; ignore-unfixed true (G2) |
| T-x8n-05 | Elevation | GITHUB_TOKEN | high | mitigate | permissions: {} at new workflow top; job-level least privilege (D-05) |
| T-x8n-06 | Injection | existing workflow YAML | high | mitigate | zizmor.yml on PR+main; fix critical findings in task 3 rather than suppress (G1) |
| T-x8n-07 | Denial | Harden-Runner deny-unlisted | medium | mitigate | egress-policy audit only; do not add an allowlist this phase (D-08) |
| T-x8n-08 | Information | unread code-scanning alerts | medium | accept | First-run triage is GitHub UI (D-10); codeql.yml still uploads alerts |
| T-x8n-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager installs; GitHub Actions only, SHA-pinned |
</threat_model>

<verification>
Task automated gates plus: eleven Harden-Runner pins; two Trivy pins after both build-push steps; CodeQL three actions share cdf488f595d80d6e07e03d4674febd5ab45fa938; gitleaks.yml has no GITLEAKS_LICENSE; every setup-node remains 24; pnpm/action-setup SHA unchanged; python3 yaml.safe_load on all touched workflows. Optional: actionlint if installed. Do not treat a red first CI Trivy/CodeQL run as plan failure if YAML matches pins — fix vulns or .trivyignore per task 3. Human Security-tab pass is leftover (D-10).
</verification>

<success_criteria>
- G1 zizmor.yml, G3 gitleaks.yml, G6 codeql.yml present with listed pins, permissions, and timeouts
- G2 Trivy scans API sha tag and worker -worker tag after both image pushes
- Harden-Runner audit is the first uses: of all eleven listed jobs
- docs/claredtool-phasenplan.md is tracked
- Node 24 and pnpm/action-setup SHA unchanged
- ROADMAP product Phase 05 untouched; Phase 6/7 files not added
- D-10 leftover named, not blocking
</success_criteria>

<output>
Create .planning/quick/260903-x8n-implement-audit-phase-5-security-baselin/260903-x8n-SUMMARY.md when done
</output>

<!-- gsd-multi-source-audit
GOAL: Audit Phase 5+5b free security baseline (zizmor, gitleaks, Trivy, CodeQL, Harden-Runner audit); Node 24 lock
REQ: G1 G2 G3 G6 AUDIT-5 AUDIT-5b NODE-24
RESEARCH: pins supplied by orchestrator (zizmor-action v0.6.1, gitleaks-action v3.0.0, trivy-action v0.36.0, harden-runner v2.21.1, codeql-action v4.37.9)
CONTEXT: D-01 Node 24; D-02 public include 5b; D-03 SHA pin; D-04 checkout persist-credentials false on new; D-05 permissions {}; D-06 timeouts; D-07 no gitleaks license env; D-08 harden audit only; D-09 no Phase 6/7/product 05; D-10 human triage leftover
COVERED: G1→task1; G3+G6+phasenplan→task2; G2+Harden-Runner+D-01+D-08→task3; D-02 in task2 codeql; D-10 user_setup not checkpoint
DEFERRED: Phase 6 Squawk/rollback/SBOM; Phase 7 N1-Rest/W2/W1/N4; F-B6/B7/E6/SEC3; GitHub UI CodeQL default setup; first-run alert triage (D-10 human)
-->
