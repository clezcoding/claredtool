---
phase: quick-260902-l7q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/package.json
  - pnpm-lock.yaml
  - apps/backend/jest.config.cjs
  - apps/backend/test/jest-e2e.json
  - .github/workflows/ci.yml
  - .github/workflows/desktop-build.yml
  - .github/workflows/backend-image.yml
  - .nvmrc
  - apps/backend/Dockerfile
  - .github/dependabot.yml
autonomous: true
requirements:
  - DEP-79
  - DEP-80
  - DEP-81
  - DEP-82
  - DEP-83
  - DEP-84
  - DEP-85
  - NODE-24
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All six Dependabot Nest 12 versions are in apps/backend/package.json together and unit tests import @nestjs/core without a CJS SyntaxError (DEP-79–84, D-03)"
    - "@nestjs/terminus is on 12.0.0 so its peer range includes @nestjs/common 12 (D-03 lockstep; 11.1.1 peers stop at 11)"
    - "pnpm/action-setup is pinned to SHA 0977fd99725f1db4007ccb2928dbb4e90d06cc86 with the existing version comment in every workflow use (DEP-85, D-04)"
    - "Runtime Node pin stays 24 in .nvmrc, backend Dockerfile, and every workflow node-version (NODE-24, D-01)"
    - "Dependabot PRs stay open; executor does not merge or dismiss them (D-05)"
  artifacts:
    - apps/backend/package.json
    - pnpm-lock.yaml
    - apps/backend/jest.config.cjs
    - .github/workflows/ci.yml
    - .github/workflows/desktop-build.yml
    - .github/workflows/backend-image.yml
    - .nvmrc
    - apps/backend/Dockerfile
    - .github/dependabot.yml
  key_links:
    - from: apps/backend/jest.config.cjs
      to: "@nestjs/core ESM index.js"
      via: transformIgnorePatterns allowing .pnpm and @nestjs so ts-jest transpiles ESM
      pattern: "grep -F 'transformIgnorePatterns' apps/backend/jest.config.cjs"
    - from: apps/backend/package.json @nestjs/*
      to: pnpm-lock.yaml
      via: pnpm install from repo root after the six Dependabot versions plus terminus 12.0.0
      pattern: "grep -F '\"@nestjs/core\": \"12.0.1\"' apps/backend/package.json"
    - from: ci.yml desktop-build.yml backend-image.yml
      to: pnpm/action-setup SHA
      via: six uses, 40-char pin plus version comment
      pattern: "grep -cE 'pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86' .github/workflows/*.yml"
    - from: .nvmrc / Dockerfile / workflows
      to: Node 24 LTS
      via: identical major; dependabot docker ignore node >=25
      pattern: "grep -E 'node-version: 24|^24$|node:24-bookworm-slim' .nvmrc apps/backend/Dockerfile .github/workflows/ci.yml .github/workflows/desktop-build.yml"
---

<objective>
Land all seven open Dependabot upgrades on the existing combined branch `quick/260902-l7q-dependabot` (D-02). Apply the six Nest 12 version bumps together plus Jest ESM handling so unit/e2e stop failing on mixed 11+12 (D-03). Apply the pnpm/action-setup SHA bump as a 40-char pin (D-04). Keep Node 24 everywhere (D-01). After this plan commits, orchestrator pushes, opens one PR, watches CI, fixes, merges; Dependabot PRs then close as superseded (D-05). Executor does not `gh pr merge`.

Purpose: Apply the upgrades for real so open Dependabot PRs resolve by landing versions, not by dismiss.
Output: Nest 12 lockstep in backend package.json + lockfile; Jest transforms @nestjs ESM; action-setup SHA updated; Node 24 unchanged.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/backend/package.json
@apps/backend/jest.config.cjs
@apps/backend/test/jest-e2e.json
@apps/backend/src/http/catch-all.controller.ts
@.github/workflows/ci.yml
@.github/workflows/desktop-build.yml
@.github/workflows/backend-image.yml
@.github/dependabot.yml
@apps/backend/Dockerfile
@.nvmrc

Locked inventory (2026-09-02): branch `quick/260902-l7q-dependabot` already exists from origin/main @ 62ce4ce (includes merged #86 rust-cache and #78 npm patch-minor). Do not recreate the branch. Do not commit on local main.

Open Dependabot PRs (do not close / ignore / dismiss):

1. #85 pnpm/action-setup SHA ff378ebe… → 0977fd99725f1db4007ccb2928dbb4e90d06cc86. mergeable_state=clean, CI green. Hang: nobody merged. Diff is 3 files, six uses, comment stays v6.0.10. Keep 40-char SHA pin plus version comment.

2. NestJS 11 → 12, all behind, CI red if merged alone:
   - #79 @nestjs/testing 12.0.1
   - #80 @nestjs/cli 12.0.0
   - #81 @nestjs/swagger 12.0.0
   - #82 @nestjs/common 12.0.1
   - #83 @nestjs/core 12.0.1
   - #84 @nestjs/platform-express 12.0.1

Hang root cause: one PR per package. Nest 12 packages are ESM-only. #83 log: `@nestjs/core@12.0.1_@nestjs/common@11.2.3` then `import 'reflect-metadata'` then Jest `SyntaxError: Cannot use import statement outside a module` in `auth/permissions.guard.spec.ts`. Mixed 12+11 cannot work.

Companion (not a Dependabot PR, required for D-03 lockstep): `@nestjs/terminus` 11.1.1 peers `@nestjs/common` ^10 || ^11 only. Set it to 12.0.0 (latest 12). `@nestjs/bullmq` is already 12.0.0.

Context7 Nest 12: ESM packages; CJS apps keep working via Node require(esm). Runtime need 20.19+ or 22.12+. CLI `nest upgrade` wants 22.22.3+ / 24.15+ / 26+ — stay on Node 24 LTS. Do not run `nest upgrade` (it rewrites webpack/tsconfig/ESM). Optional ESM migration is not required.

path-to-regexp v8: unnamed wildcards need named params. Catch-all is already `@All("{*path}")`. Do not convert it to an unnamed splat. Grep controllers for unnamed `*` `+` `(.*)` and name them only if found.

Jest: jest ^30.4.2 + ts-jest ^29.2.5. e2e `test/jest-e2e.json` already has transformIgnorePatterns allowing `.pnpm` plus bullmq. Unit `jest.config.cjs` has none, so default ignores node_modules and Nest 12 ESM blows up. Prefer transformIgnorePatterns allowing @nestjs and the pnpm nested path. Do not migrate tests to Vitest.

Kaneo task w1yf1hjj09dvc4nn19ve20fi (Clared #85) is already in-progress. Do not create another.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end Nest 12 lockstep + Jest ESM transform (DEP-79–84, D-03)</name>
  <precondition>Current branch is quick/260902-l7q-dependabot (not main). apps/backend still pins @nestjs/common 11.2.3.</precondition>
  <reversibility rating="costly">Nest 11 to 12 major across official packages plus lockfile retarget. Revert is package.json plus lockfile restore, not a data migration.</reversibility>
  <files>apps/backend/package.json, pnpm-lock.yaml, apps/backend/jest.config.cjs, apps/backend/test/jest-e2e.json</files>
  <behavior>
    - After package.json versions change and pnpm install, `pnpm --filter ./apps/backend test -- permissions.guard.spec.ts` fails with SyntaxError until unit Jest transforms @nestjs ESM
    - After GREEN: that spec and the full backend unit suite pass; `nest build` succeeds
    - Catch-all route stays the named splat `{*path}`; backend package.json has no ESM package type field
  </behavior>
  <action>
    Stay on existing branch quick/260902-l7q-dependabot (D-02). Do not recreate it. Do not merge or cherry-pick github.com/clezcoding/claredtool/pull/79 through pull/84 (isolated lockfiles and mixed majors are the hang).

    In apps/backend/package.json set these exact versions from the Dependabot PRs (D-03): @nestjs/common 12.0.1, @nestjs/core 12.0.1, @nestjs/platform-express 12.0.1, @nestjs/testing 12.0.1, @nestjs/swagger 12.0.0, @nestjs/cli 12.0.0. Also set @nestjs/terminus to 12.0.0 so its peer range includes common 12 (11.x peers stop at 11). Leave @nestjs/bullmq at 12.0.0. Leave reflect-metadata and rxjs as they are (still peers).

    From repo root run pnpm install so pnpm-lock.yaml resolves the tree. Do not run nest upgrade. Do not add type module, nodenext, or rewrite relative imports. Do not migrate tests to Vitest. Do not bump Node (D-01).

    RED: run `pnpm --filter ./apps/backend test -- permissions.guard.spec.ts` and confirm it fails on Nest ESM (Cannot use import statement outside a module) before changing Jest.

    GREEN: in apps/backend/jest.config.cjs add transformIgnorePatterns matching e2e intent but covering all @nestjs, not only bullmq: ignore node_modules except `.pnpm` and `@nestjs/` (pnpm nested path plus hoisted scope). Copy e2e ts-jest allowJs true onto the unit transform. Optionally add setupFiles reflect-metadata like e2e. In test/jest-e2e.json broaden the existing @nestjs allow from bullmq-only to all `@nestjs/` if e2e still needs it; keep the `.pnpm` exception.

    If pnpm reports an unmeetable peer on some other already-installed package (not a new dependency), bump that package only as far as needed to accept @nestjs/common 12. Do not add new packages.

    Confirm apps/backend/src/http/catch-all.controller.ts still uses the named splat `{*path}`. If any other controller uses an unnamed wildcard, name the param. Do not change OpenAPI or auth behavior.

    Then: pnpm --filter @clared/tax-engine build, prisma generate in backend, nest build, full `pnpm --filter ./apps/backend test`.

    Do not gh pr close, dependabot ignore, or gh pr merge (D-05). Kaneo task w1yf1hjj09dvc4nn19ve20fi already exists; do not create another.
  </action>
  <verify>
    <automated>grep -F '"@nestjs/common": "12.0.1"' apps/backend/package.json &amp;&amp; grep -F '"@nestjs/core": "12.0.1"' apps/backend/package.json &amp;&amp; grep -F '"@nestjs/platform-express": "12.0.1"' apps/backend/package.json &amp;&amp; grep -F '"@nestjs/testing": "12.0.1"' apps/backend/package.json &amp;&amp; grep -F '"@nestjs/swagger": "12.0.0"' apps/backend/package.json &amp;&amp; grep -F '"@nestjs/cli": "12.0.0"' apps/backend/package.json &amp;&amp; grep -F '"@nestjs/terminus": "12.0.0"' apps/backend/package.json &amp;&amp; grep -F 'transformIgnorePatterns' apps/backend/jest.config.cjs &amp;&amp; grep -F '{*path}' apps/backend/src/http/catch-all.controller.ts &amp;&amp; test "$(node -e "console.log(require('./apps/backend/package.json').type||'')")" = "" &amp;&amp; test "$(cat .nvmrc)" = "24" &amp;&amp; pnpm --filter @clared/tax-engine build &amp;&amp; pnpm --filter ./apps/backend exec prisma generate &amp;&amp; pnpm --filter ./apps/backend exec nest build &amp;&amp; pnpm --filter ./apps/backend test -- permissions.guard.spec.ts &amp;&amp; pnpm --filter ./apps/backend test</automated>
  </verify>
  <done>All six Dependabot Nest versions plus terminus 12.0.0 are pinned; unit Jest transforms @nestjs ESM; permissions.guard.spec.ts and the backend unit suite pass; nest build succeeds; backend stays CommonJS; #79–#84 not merged as-is.</done>
</task>

<task type="auto">
  <name>Pin pnpm/action-setup SHA and keep Node 24 (DEP-85, NODE-24, D-01, D-04)</name>
  <files>.github/workflows/ci.yml, .github/workflows/desktop-build.yml, .github/workflows/backend-image.yml, .nvmrc, apps/backend/Dockerfile, .github/dependabot.yml</files>
  <action>
    On the same combined branch (D-02), apply Dependabot #85 as a SHA pin, not a floating tag (D-04). In ci.yml (four uses), desktop-build.yml (one use), and backend-image.yml (one use), set pnpm/action-setup to 0977fd99725f1db4007ccb2928dbb4e90d06cc86 and keep the existing version comment v6.0.10. Six uses total. Do not rewrite other actions uses lines.

    Node 24 lock (D-01, NODE-24): .nvmrc is the single token 24; Dockerfile FROM node:24-bookworm-slim; every setup-node step keeps that same major; dependabot.yml docker ignore for dependency-name node versions &gt;=25 stays. Do not raise Node major. Do not add an engines field.
    <!-- planner-discipline-allow: node-version: 24 -->

    Do not gh pr close, dependabot ignore, or gh pr merge #85 or #79–#84 (D-05). Orchestrator push/PR/CI/merge after SUMMARY; those PRs close as superseded once versions are on main.
  </action>
  <verify>
    <automated>test "$(grep -cE 'pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86' .github/workflows/*.yml)" -eq 6 &amp;&amp; grep -E 'pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86.*# v6.0.10' .github/workflows/ci.yml .github/workflows/desktop-build.yml .github/workflows/backend-image.yml &amp;&amp; test "$(grep -vE '^\s*#' .github/workflows/*.yml | grep -cE 'uses: [^@]+@v[0-9]')" -eq 0 &amp;&amp; test "$(cat .nvmrc)" = "24" &amp;&amp; grep -F 'FROM node:24-bookworm-slim' apps/backend/Dockerfile &amp;&amp; test "$(grep -vE '^\s*#' .github/workflows/ci.yml .github/workflows/desktop-build.yml .github/workflows/backend-image.yml | grep -c 'node-version: 24')" -ge 5 &amp;&amp; test "$(grep -vE '^\s*#' .github/workflows/ci.yml .github/workflows/desktop-build.yml .github/workflows/backend-image.yml | grep -cE 'node-version: 2[56]')" -eq 0 &amp;&amp; grep -A2 'dependency-name: node' .github/dependabot.yml | grep -F '&gt;=25'</automated>
  </verify>
  <done>Six pnpm/action-setup uses pin SHA 0977fd99… with version comment; no floating uses tags in workflows; Node 24 pins and dependabot ignore intact; Dependabot PRs left open for orchestrator supersede-close (D-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → third-party actions | Mutable tags can be retargeted; SHA pins are the supply-chain control |
| Jest CJS runner → @nestjs ESM packages | Untransformed ESM in node_modules executes as CJS and throws SyntaxError; mixed 11+12 trees are unsupportable |
| CI/Docker → Node runtime | Accidental Node 25/26 re-pin breaks the LTS lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-l7q-01 | Tampering | .github/workflows/*.yml | high | mitigate | D-04: keep 40-char SHA pins plus version comment; apply #85 SHA, do not rewrite to floating tags |
| T-l7q-02 | Tampering | apps/backend @nestjs/* | high | mitigate | D-03: bump all six Dependabot Nest packages plus terminus 12.0.0 together; do not merge isolated PRs |
| T-l7q-03 | Denial of service | Jest + @nestjs ESM | medium | mitigate | transformIgnorePatterns in unit (and e2e if needed) so ts-jest transpiles @nestjs and pnpm nested paths |
| T-l7q-04 | Elevation of privilege | Node runtime pins | medium | mitigate | D-01: .nvmrc, Dockerfile, node-version stay 24; dependabot ignore node &gt;=25 |
| T-l7q-SC | Tampering | pnpm install Nest 12 | medium | accept | Version bump of packages already in tree (nestjs official), not a new package; lockfile from pnpm install |
</threat_model>

<verification>
Task 1: package.json version greps, nest build, permissions.guard.spec.ts plus full backend unit suite. Task 2: six action-setup SHA greps plus Node 24 greps. Orchestrator (D-05): push branch, one PR covering Nest 12 lockstep plus action-setup SHA, watch CI (lint/backend-unit/backend-e2e/desktop-test/ci), fix, merge; then confirm Dependabot PRs close as superseded (not dismissed). Executor does not gh pr merge.
</verification>

<success_criteria>
- Nest 12.0.1 / 12.0.0 versions from #79–#84 plus terminus 12.0.0 compile and pass backend unit tests
- pnpm/action-setup SHA 0977fd99… present six times
- Node 24 unchanged
- No Dependabot PR closed without those versions on the branch (D-05)
</success_criteria>

<output>
Create .planning/quick/260902-l7q-es-sind-einige-dependabot-prs-im-repo-of/260902-l7q-SUMMARY.md when done
</output>

<!-- gsd-multi-source-audit
GOAL: Land all 7 open Dependabot PRs by applying upgrades (not dismiss); keep Node 24; diagnose hang (mixed Nest 12+11 ESM); orchestrator push/PR/CI/merge
REQ: DEP-79, DEP-80, DEP-81, DEP-82, DEP-83, DEP-84, DEP-85, NODE-24
RESEARCH: none (quick, no research phase); Context7 Nest 12 ESM + CJS require(esm); Jest transformIgnorePatterns; nest upgrade not run
CONTEXT: D-01 Node 24, D-02 existing branch, D-03 lockstep Nest 12 + Jest ESM not merge isolated PRs, D-04 SHA not floating tags, D-05 executor does not gh pr merge
COVERED: DEP-79–84+D-03→task1; DEP-85+NODE-24+D-01+D-04+D-05→task2; D-02 both tasks
DEFERRED: none (ESM package-type migration explicitly out of scope unless tests cannot go green; first path is transformIgnorePatterns)
-->
