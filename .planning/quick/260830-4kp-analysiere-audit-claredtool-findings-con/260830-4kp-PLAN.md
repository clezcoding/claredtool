---
phase: quick-260830-4kp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/desktop-build.yml
  - .github/workflows/backend-image.yml
  - .github/workflows/ci.yml
  - .github/dependabot.yml
  - .github/labels.yml
  - .github/labeler.yml
autonomous: true
requirements:
  - F-E1
  - F-E2
  - F-B1
  - F-B2
  - F-B3
  - F-B4
  - F-C1
  - F-C2
  - F-C3
  - F-C4
  - F-C5
  - F-C6
  - F-C7
  - F-D2
  - F-D3
  - F-D4
  - F-D6
  - F-D7
  - F-L1
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "UI or pnpm-core changes on main trigger signed desktop-build (F-E1)"
    - "docs/clared-tax-rule-dsl-schema.json changes trigger backend-image build (F-B1)"
    - "Running Coolify deploy is not cancelled by a newer backend-image workflow run (F-B3)"
    - "v* tag and workflow_dispatch desktop builds are never auto-cancelled (F-E2)"
    - "Pure UI changes skip backend CI jobs; tax-engine changes skip desktop lint/test (F-C1, F-C2)"
    - ".npmrc and tax schema route to the correct CI job families (F-C3, F-C4)"
    - "desktop-test uses rust-cache, --frozen-lockfile, --locked, fmt/clippy, full cargo test, Vite prod build (F-C5, F-C6, F-C7)"
    - "backend-image has no redundant changes job; worker build does not double cache-to (F-B2, F-B4)"
    - "Dependabot majors are individual PRs; docker + compose ecosystems active; glib ignore is version-scoped (F-D2, F-D3, F-D4, F-D6, F-D7)"
    - "labels.yml defines backend, tax-engine, docker, infrastructure; labeler applies them (F-L1)"
  artifacts:
    - .github/workflows/desktop-build.yml
    - .github/workflows/backend-image.yml
    - .github/workflows/ci.yml
    - .github/dependabot.yml
    - .github/labels.yml
    - .github/labeler.yml
  key_links:
    - from: desktop-build on.push.paths
      to: packages/ui and pnpm lockfiles
      via: path filter includes UI + workspace roots
      pattern: "grep -F 'packages/ui/**' .github/workflows/desktop-build.yml"
    - from: backend-image on.push.paths
      to: docs/clared-tax-rule-dsl-schema.json
      via: schema copied in Dockerfile
      pattern: "grep -F 'clared-tax-rule-dsl-schema.json' .github/workflows/backend-image.yml"
    - from: ci changes outputs
      to: lint vs backend-unit if conditions
      via: desktop jobs exclude tax-engine-only; backend jobs exclude desktop-only
      pattern: "grep -F 'needs.changes.outputs.tax-engine' .github/workflows/ci.yml"
    - from: labels.yml docker
      to: dependabot docker ecosystems
      via: F-S1 label must exist before docker PRs
      pattern: "grep -F 'name: docker' .github/labels.yml"
---

<objective>
Implement audit findings Phase 1 (release correctness), Phase 2 (CI routing + quality), and Phase 3 subset (Dependabot + labels) from docs/claredtool-findings-consolidated.md.

Purpose: Close real automation gaps — missed signed desktop builds, missed backend image rebuilds on schema change, deploy cancellation races, wasteful CI routing, slow Rust compiles, and incomplete Dependabot coverage.
Output: Six `.github/*` files updated; no app source changes.

**In scope:** F-E1, F-E2, F-B1, F-B2, F-B3, F-B4, F-C1–F-C7, F-D2, F-D3, F-D4, F-D6, F-D7, F-L1.
**Explicitly skipped:** F-SEC1 (SHA pinning), F-SEC2 (branch protection), F-X1 (schema dedup), F-B5 (deploy healthcheck), F-E3 (rust-toolchain.toml), F-D5, F-D8, F-D9, F-P1, F-L2, F-SEC3/4/5, F-E4–E7, F-X2.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@docs/claredtool-findings-consolidated.md
@.github/workflows/ci.yml
@.github/workflows/backend-image.yml
@.github/workflows/desktop-build.yml
@.github/dependabot.yml
@.github/labels.yml
@.github/labeler.yml

Source audit (all in-scope items mapped to tasks):
| ID | Task |
|----|------|
| F-E1, F-E2 | 1 |
| F-B1, F-B3 | 1 |
| F-B2, F-B4, F-C1–F-C7 | 2 |
| F-D2, F-D3, F-D4, F-D6, F-D7, F-L1 | 3 |

Compose files for F-D4: compose.yml, compose.clared.yml at repo root (directory: /).
Backend Dockerfile: apps/backend/Dockerfile (directory: /apps/backend for F-D3).
Schema path for F-B1/F-C4: docs/clared-tax-rule-dsl-schema.json (byte-identical to packages/tax-engine copy; dedup deferred per skip list).
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Release path filters and deploy-safe concurrency</name>
  <files>.github/workflows/desktop-build.yml, .github/workflows/backend-image.yml</files>
  <read_first>docs/claredtool-findings-consolidated.md (F-E1, F-E2, F-B1, F-B3), .github/workflows/desktop-build.yml, .github/workflows/backend-image.yml</read_first>
  <action>
    **desktop-build.yml (F-E1, F-E2):**

    1. Extend `on.push.paths` to include: `packages/ui/**`, `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `.npmrc` (keep existing `apps/desktop/**` and workflow self-path). Do not remove `tags: ['v*']` or `workflow_dispatch`.

    2. Remove workflow-level `concurrency` block (lines 30–32 today).

    3. Add job-level `concurrency` on `tauri`:
       - Group distinguishes prod tags, manual dispatch, and staging main (use `startsWith(github.ref, 'refs/tags/v')`, `github.event_name == 'workflow_dispatch'`, else staging).
       - Set `cancel-in-progress: false` when ref is a `v*` tag OR event is `workflow_dispatch` (F-E2: prod/manual never auto-cancel).
       - Set `cancel-in-progress: true` only for automatic main-branch staging pushes.

    4. Add job-level `concurrency` on `publish-faynosync` with the same cancel rules (tag + dispatch = false; main auto = true).

    5. Change `pnpm install` to `pnpm install --frozen-lockfile` (F-C7 partial — release path).

    **backend-image.yml (F-B1, F-B3):**

    6. Add `docs/clared-tax-rule-dsl-schema.json` to `on.push.paths` list (F-B1). Do not remove other paths.

    7. Remove workflow-level `concurrency` block (lines 40–42 today).

    8. Add job-level `concurrency` on `build` only: `group: backend-image-build-${{ github.ref }}`, `cancel-in-progress: true`. Leave `deploy` job's existing `coolify-nest-deploy` group with `cancel-in-progress: false` untouched (F-B3).

    Do not remove the `changes` job yet (F-B2 is Task 2). Do not touch cache-to (F-B4 is Task 2). Do not edit ci.yml, dependabot, or labels in this task.
  </action>
  <verify>
    <automated>grep -F -q 'packages/ui/**' .github/workflows/desktop-build.yml &amp;&amp; grep -F -q 'pnpm-lock.yaml' .github/workflows/desktop-build.yml &amp;&amp; grep -F -q 'clared-tax-rule-dsl-schema.json' .github/workflows/backend-image.yml &amp;&amp; ! awk '/^jobs:/{exit} /^concurrency:/{exit 1}' .github/workflows/backend-image.yml &amp;&amp; grep -q 'backend-image-build' .github/workflows/backend-image.yml &amp;&amp; grep -q 'cancel-in-progress: false' .github/workflows/desktop-build.yml</automated>
  </verify>
  <done>F-E1 paths live; F-E2 tag/dispatch builds non-cancellable; F-B1 schema in backend-image trigger; F-B3 workflow-level cancel removed from deploy path; frozen-lockfile on desktop-build install.</done>
</task>

<task type="auto">
  <name>Task 2: CI routing, quality gates, backend-image cleanup</name>
  <files>.github/workflows/ci.yml, .github/workflows/backend-image.yml</files>
  <read_first>docs/claredtool-findings-consolidated.md (F-C1–F-C7, F-B2, F-B4), .github/workflows/ci.yml</read_first>
  <action>
    **ci.yml — path filters (F-C1, F-C2, F-C3, F-C4):**

    1. In `changes` job `dorny/paths-filter@v4` filters block:
       - `desktop`: `apps/desktop/**`, `packages/ui/**` only (remove `packages/ui` from `shared`).
       - `backend`: `apps/backend/**`, `docs/clared-tax-rule-dsl-schema.json`.
       - `tax-engine`: `packages/tax-engine/**` unchanged.
       - `shared`: `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `.npmrc`, `.github/workflows/ci.yml` (no `packages/ui/**`).

    2. Update job `if:` conditions:
       - `lint` and `desktop-test`: `main` OR `desktop` OR `shared` — remove `tax-engine` output (F-C2).
       - `backend-unit` and `backend-e2e`: `main` OR `backend` OR `tax-engine` OR `shared` — unchanged family, now benefits from schema in `backend` filter (F-C4).

    **ci.yml — quality and reproducibility (F-C5, F-C6, F-C7):**

    3. All four install steps: `pnpm install --frozen-lockfile`.

    4. `desktop-test` job — after checkout, before pnpm: add `dtolnay/rust-toolchain@stable`, then `swatinem/rust-cache@v2` with `workspaces: apps/desktop/src-tauri` (F-C5).

    5. `desktop-test` — replace single filtered test with, in order after install:
       - `cargo fmt --check --manifest-path apps/desktop/src-tauri/Cargo.toml`
       - `cargo clippy --locked --manifest-path apps/desktop/src-tauri/Cargo.toml -- -D warnings`
       - `cargo test --locked --manifest-path apps/desktop/src-tauri/Cargo.toml`
       - `pnpm --filter ./apps/desktop test`

    6. `lint` job — after tsc, add `pnpm --filter ./apps/desktop build` (Vite prod build, F-C6).

    7. `backend-unit` job — after `prisma generate`, before `nest build`, add `pnpm --filter ./apps/backend exec prisma validate` (F-C6).

    **backend-image.yml (F-B2, F-B4):**

    8. Remove entire `changes` job and its `needs`/`if` on `build`. `build` runs directly on every workflow trigger (push paths already filter; `workflow_dispatch` always builds).

    9. On the **worker** `docker/build-push-action@v6` step (target: worker): remove `cache-to` line; keep `cache-from: type=gha,scope=clared-backend`. API build step keeps both `cache-from` and `cache-to` (F-B4).

    Do not edit dependabot or labels. Do not add SHA pinning or rust-toolchain.toml.
  </action>
  <verify>
    <automated>! grep -A20 'shared:' .github/workflows/ci.yml | grep -q 'packages/ui' &amp;&amp; grep -F -q 'clared-tax-rule-dsl-schema.json' .github/workflows/ci.yml &amp;&amp; grep -F -q '.npmrc' .github/workflows/ci.yml &amp;&amp; grep -q 'swatinem/rust-cache@v2' .github/workflows/ci.yml &amp;&amp; grep -q 'frozen-lockfile' .github/workflows/ci.yml &amp;&amp; grep -q 'cargo clippy' .github/workflows/ci.yml &amp;&amp; grep -q 'prisma validate' .github/workflows/ci.yml &amp;&amp; ! grep -q '^  changes:' .github/workflows/backend-image.yml &amp;&amp; test "$(grep -c 'cache-to:' .github/workflows/backend-image.yml)" -eq 1</automated>
  </verify>
  <done>F-C1–C7 implemented; F-B2 changes job gone; F-B4 single cache-to on API build only; desktop jobs decoupled from tax-engine-only changes.</done>
</task>

<task type="auto">
  <name>Task 3: Dependabot hardening and PR labels</name>
  <files>.github/dependabot.yml, .github/labels.yml, .github/labeler.yml</files>
  <read_first>docs/claredtool-findings-consolidated.md (F-D2, F-D3, F-D4, F-D6, F-D7, F-L1, Anhang A), .github/dependabot.yml, .github/labels.yml</read_first>
  <action>
  **labels.yml (F-L1, F-S1 prerequisite for F-D3/F-D4):**

  1. Append labels (names must match labeler + dependabot):
     - `backend` — color `1d76db`, description `apps/backend (Nest API)`
     - `tax-engine` — color `bfd4f2`, description `packages/tax-engine + tax rule schema`
     - `docker` — color `2496ed`, description `Dockerfile and compose image updates`
     - `infrastructure` — color `ededed`, description `Docker, compose, and deployment config`

  **labeler.yml (F-L1):**

  2. Add path rules:
     - `backend`: `apps/backend/**`
     - `tax-engine`: `packages/tax-engine/**`, `docs/clared-tax-rule-dsl-schema.json`
     - `docker`: `apps/backend/Dockerfile`, `compose.yml`, `compose.clared.yml`, `.dockerignore`
     - `infrastructure`: same globs as `docker` plus `.github/workflows/backend-image.yml`

  **dependabot.yml (F-D2, F-D3, F-D4, F-D6, F-D7) — align with Anhang A:**

  3. npm block: remove `npm-major` group; remove redundant `prefix-development: "chore"` (F-D2, F-D7). Add `time: "06:00"` to schedule.

  4. cargo block: remove `cargo-major` group; replace flat `ignore: glib` with version-scoped ignore `versions: [">=0.19.0"]` plus existing comment (F-D6). Add `time: "06:15"`.

  5. github-actions block: remove `actions-major` group (F-D2). Add `time: "06:30"`.

  6. Add new `docker` ecosystem: `directory: /apps/backend`, weekly Monday `06:45` Europe/Berlin, labels `[dependencies, docker]`, commit prefix chore (F-D3).

  7. Add new `docker-compose` ecosystem: `directory: /`, weekly Monday `07:00` Europe/Berlin, labels `[dependencies, docker]` (F-D4). Authentik variable-interpolated images will be skipped by Dependabot — expected.

  Do not add F-D5 cooldown or F-D9. Do not SHA-pin actions.
  </action>
  <verify>
    <automated>grep -F -q 'name: docker' .github/labels.yml &amp;&amp; grep -F -q 'name: backend' .github/labels.yml &amp;&amp; grep -F -q 'name: tax-engine' .github/labels.yml &amp;&amp; grep -q 'package-ecosystem: docker' .github/dependabot.yml &amp;&amp; grep -q 'docker-compose' .github/dependabot.yml &amp;&amp; ! grep -q 'npm-major:' .github/dependabot.yml &amp;&amp; ! grep -q 'prefix-development' .github/dependabot.yml &amp;&amp; grep -A3 'dependency-name: glib' .github/dependabot.yml | grep -q '>=0.19.0' &amp;&amp; grep -F -q 'apps/backend/**' .github/labeler.yml</automated>
  </verify>
  <done>F-L1 labels + labeler rules live; F-D2 majors ungrouped; F-D3/F-D4 docker ecosystems added; F-D6 glib version-scoped; F-D7 prefix-development removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions workflows | Third-party actions execute with repo read; deploy/publish jobs use secrets |
| Dependabot PRs | Automated dependency bumps enter via labeled PRs |
| Coolify deploy API | backend-image deploy job PATCHes image tag and POSTs deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4kp-01 | Denial of Service | backend-image deploy cancel | high | mitigate | F-B3: cancel-in-progress only on build job; deploy group stays false |
| T-4kp-02 | Tampering | Missed image rebuild on schema change | high | mitigate | F-B1: schema path in on.push.paths |
| T-4kp-03 | Tampering | Stale signed desktop after UI change | high | mitigate | F-E1: UI + lockfile paths trigger desktop-build |
| T-4kp-04 | Denial of Service | Prod release cancelled mid-publish | high | mitigate | F-E2: tag and workflow_dispatch jobs cancel-in-progress false |
| T-4kp-05 | Tampering | Dependabot docker label missing | medium | mitigate | F-L1: create docker label before docker ecosystems (F-S1) |
| T-4kp-06 | Information Disclosure | glib full ignore hides patches | medium | mitigate | F-D6: version-scoped ignore only blocks >=0.19.0 |
| T-4kp-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages; workflow YAML only |
</threat_model>

<verification>
- Grep gates in each task pass
- Optional: `actionlint .github/workflows/*.yml` if available locally
- Confirm skipped items (F-SEC1, F-SEC2, F-X1, F-B5, F-E3) have zero diff hunks
</verification>

<success_criteria>
- Phase 1 findings F-E1, F-E2, F-B1, F-B3 closed
- Phase 2 findings F-C1–F-C7, F-B2, F-B4 closed
- Phase 3 subset F-D2, F-D3, F-D4, F-D6, F-D7, F-L1 closed
- Six files changed; no application source edits
- Skipped findings explicitly untouched
</success_criteria>

<output>
Create `.planning/quick/260830-4kp-analysiere-audit-claredtool-findings-con/260830-4kp-SUMMARY.md` when done
</output>
