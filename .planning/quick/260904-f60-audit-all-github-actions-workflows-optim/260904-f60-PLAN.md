---
phase: quick-260904-f60
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/codeql.yml
  - .github/workflows/labeler.yml
  - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-MANUAL-GITHUB.md
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-09
  - D-13
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "codeql.yml on-map is only push to main (existing paths including rust), weekly cron 18 5 * * 1, and workflow_dispatch. It does not start for PR events (D-01, D-13)."
    - "CodeQL analyze-js-typescript and analyze-rust still run on matching push to main, on the Monday schedule, and on dispatch; detect still forces both languages true for schedule and dispatch (D-01)."
    - "labeler.yml PR types are opened, reopened, ready_for_review only — no mid-PR git-push type (D-02)."
    - "Required merge checks stay aggregator job id ci plus job name gitleaks. CodeQL and zizmor stay not required (D-09, D-13)."
    - "ci.yml, gitleaks.yml, zizmor.yml, desktop-build.yml, backend-image.yml, and sync-labels.yml are byte-unchanged (D-09, 04.4 D-01, D-14)."
    - "04.4-MANUAL-GITHUB.md D-34 and fan-out tables no longer claim CodeQL runs on PRs (D-03)."
  artifacts:
    - .github/workflows/codeql.yml
    - .github/workflows/labeler.yml
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-MANUAL-GITHUB.md
  key_links:
    - "codeql.yml on keys are push + schedule + workflow_dispatch; detect has no third event branch; analyze job if is output-only"
    - "labeler.yml on.pull_request.types exact list opened/reopened/ready_for_review"
    - "SUMMARY copies Pflicht vs skippable matrix; MANUAL D-34 JS/TS and Rust-only rows match"
  prohibitions:
    - statement: "Do not add workflow-level paths to ci.yml or gitleaks.yml (required-check pending trap)."
      status: unverified
    - statement: "Do not fold secret scanning into ci.yml or add path filters to gitleaks.yml (D-09)."
      status: unverified
    - statement: "Do not make CodeQL or zizmor required checks (D-13)."
      status: unverified
    - statement: "Do not add the privileged PR-from-base workflow trigger or workflow_run deploy (04.4 D-02)."
      status: unverified
    - statement: "Do not run signed desktop-build on PRs or untagged main (04.4 D-14)."
      status: unverified
    - statement: "Do not edit Branch Protection, Environments, or Secret Scanning via API (04.4 D-06)."
      status: unverified
    - statement: "Do not add a new npm/pip/cargo package or a new third-party Action."
      status: unverified
    - statement: "Do not revert unrelated 04.4 / quick-cjp work in ci.yml if a local diff appears."
      status: unverified
    - statement: "Do not create a new docs/*.md file; matrix lives in this PLAN, SUMMARY, and the existing MANUAL table."
      status: unverified
---

<objective>
After Phase 04.4: drop the extra CodeQL workflow on every JS/TS PR (D-01), stop labeler on every PR push (D-02), and write the required-vs-skippable matrix so merge rules stay honest (D-03, D-09, D-13).

Purpose: Typical JS/TS PR still fans out ci + gitleaks + codeql + labeler. CodeQL on PR is not a required check and re-scans on merge to main plus weekly. Labeler does not need to re-run on synchronize. Workflow-level paths on required checks would leave those checks Pending — do not do that.

Output: codeql.yml without a PR event; labeler.yml without the mid-PR push type; MANUAL scenario rows + SUMMARY matrix. No new Actions. No GitHub Settings.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.github/workflows/codeql.yml
@.github/workflows/labeler.yml
@.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-MANUAL-GITHUB.md
@.github/workflows/ci.yml
@.github/workflows/gitleaks.yml

04.4 shipped (PR #111). quick-cjp already took Rust CodeQL off PRs; JS/TS CodeQL still starts on PRs. This quick removes the remaining PR CodeQL event.

Working tree was clean at plan time. If executor sees a local ci.yml diff, leave it; do not revert 04.4/cjp.

Keep: ci.yml dorny/paths-filter + aggregator job id exactly `ci`; gitleaks on every PR and main with no path filter; zizmor path-filtered and not required; desktop-build v* + dispatch only; backend-image main paths only; SHA pins; Harden-Runner first steps; CodeQL build-mode none; schedule cron `18 5 * * 1`.

Do not cache node_modules. setup-node/pnpm cache stays in the composite action used by ci.yml (untouched).

No Kaneo from executor. User-facing SUMMARY facts German. Plan English.

## Pflicht vs skippable (copy into SUMMARY, German)

### Pflicht (merge-blocking if Branch Protection set)

- `ci` (Aggregator-Job) — jede PR und jeder main-Push. Area-Jobs intern via paths-filter. Docs-only: nur `changes` + `ci` gruen.
- `gitleaks` — jede PR und jeder main-Push. Nie skippen. Check-Name genau `gitleaks` (D-09).

### Nicht Pflicht

- `codeql` — nach D-01: nur main (Pfadfilter) + weekly Mo 05:18 UTC + dispatch. Nicht auf PRs. Nicht required (D-13).
- `zizmor` — nur Workflow-/Action-YAML. Nicht required (D-13).
- `labeler` — billig; nach D-02 nur opened/reopened/ready_for_review. Kein required check.
- `sync-labels` — labels.yml auf main.
- `desktop-build` — v* / dispatch. Nie PR.
- `backend-image` — main backend/tax-engine/lockfile. Nie PR.

### Nach Aenderungstyp

- Docs-only PR: Pflicht ci (changes+aggregator) + gitleaks. Dazu labeler (open/ready). Skip: codeql, zizmor, rust/web/backend-Jobs, desktop-build, backend-image.
- Draft-PR: Pflicht ci (changes+aggregator) + gitleaks. Heavy ci-Jobs erst ready_for_review. codeql startet nicht (D-01).
- Desktop-Web JS/TS ready PR: ci desktop-web + aggregator, gitleaks, labeler. Skip rust/backend unless shared; kein codeql auf PR; kein desktop-build.
- Rust-only PR: ci desktop-rust + aggregator, gitleaks, labeler. Skip web/backend. Rust-CodeQL nur main-Push.
- Backend/tax-engine PR: ci backend-unit + backend-e2e + squawk + aggregator, gitleaks, labeler. Skip desktop unless shared.
- Workflow-YAML PR: gitleaks; zizmor; ci wenn ci.yml im shared-Filter. labeler. codeql nicht auf PR.
- Shared lockfile/package.json: alle ci-Area-Jobs + gitleaks + labeler. Kein desktop-build.
- main backend-Push: ci-Areas + gitleaks + backend-image (+ codeql wenn js/ts/rs-Pfade). Kein desktop-build.
- v*-Tag: desktop-build signed. PR-Workflows N/A.
</context>

<tasks>

<task type="tracer">
  <name>End-to-end CodeQL off PR events — main and weekly remain</name>
  <files>.github/workflows/codeql.yml</files>
  <read_first>.github/workflows/codeql.yml</read_first>
  <action>
    Per D-01 and D-13: this workflow must not start for PR events. Security scan stays on push to main (existing path list, including rust), weekly cron 18 5 * * 1, and workflow_dispatch.

    Delete the entire on-map event block whose types are currently opened plus mid-PR git-push plus reopened plus ready_for_review and whose paths omit rust globs. After the edit, the on-map keys must be exactly push, schedule, and workflow_dispatch. Leave push.paths, push.branches main, schedule cron, and empty workflow_dispatch unchanged.

    Collapse job detect step set: two branches only. schedule or workflow_dispatch still writes both language outputs true. The else branch (push) still copies FILTER_JS and FILTER_RS. Remove the third branch that forced rust false for the deleted event. Leave the paths-filter step if that skips schedule and dispatch.

    Set analyze-js-typescript if to only needs.detect.outputs.javascript-typescript == true. Set analyze-rust if to only needs.detect.outputs.rust == true. Drop the extra unpublished-PR skip on analyze-js-typescript and the extra event-name guard on analyze-rust.

    Simplify run-name to format codeql · event_name · ref_name only (drop the ternary that includes the pull request id).

    Keep concurrency group github.workflow-github.ref, cancel-in-progress true, permissions, Harden-Runner SHA e14015d583714f6e62063499dc959a02595150a1, checkout SHA 3d3c42e5aac5ba805825da76410c181273ba90b1 persist-credentials false, dorny SHA ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d, codeql-action SHA cdf488f595d80d6e07e03d4674febd5ab45fa938, languages javascript-typescript and rust, build-mode none, timeouts 5/45. Do not add a cargo cache. Do not touch other workflow files. Do not change GitHub Settings (D-09, D-13).
  </action>
  <verify>
    <automated>python3 -c 'import yaml; w=yaml.safe_load(open(".github/workflows/codeql.yml")); on=w.get("on") or w.get(True); assert set(on)=={"push","schedule","workflow_dispatch"}, list(on); assert "main" in on["push"]["branches"]; assert any(str(p).endswith(".rs") for p in on["push"]["paths"]); assert on["schedule"][0]["cron"]=="18 5 * * 1"; run=[s["run"] for s in w["jobs"]["detect"]["steps"] if "run" in s][0]; assert run.count("elif")==0 and "FILTER_RS" in run; aj=" ".join(str(w["jobs"]["analyze-js-typescript"]["if"]).split()); ar=" ".join(str(w["jobs"]["analyze-rust"]["if"]).split()); assert aj.startswith("needs.detect.outputs.javascript-typescript") and aj.count("needs.detect")==1 and "true" in aj; assert ar.startswith("needs.detect.outputs.rust") and ar.count("needs.detect")==1 and "true" in ar; assert "ref_name" in str(w.get("run-name","")); print("codeql ok")'</automated>
  </verify>
  <done>JS/TS and Rust PRs do not queue this workflow. Push main with matching paths, Monday 05:18 UTC, and dispatch still analyze. SHA pins and build-mode none unchanged (D-01, D-13).</done>
</task>

<task type="auto">
  <name>Stop labeler on mid-PR git pushes</name>
  <files>.github/workflows/labeler.yml</files>
  <read_first>.github/workflows/labeler.yml</read_first>
  <action>
    Per D-02: change on.pull_request.types to opened, reopened, ready_for_review only. Do not include the mid-PR git-push event type. Leave permissions contents read and pull-requests write. Leave job label, timeout-minutes 5, ubuntu-latest, actions/labeler SHA bf12e9b00b37c5c0ca2b87b79b2daf7891dbda13, sync-labels false. Keep ready_for_review so converting an unpublished PR still labels. Do not edit sync-labels.yml. Do not add a new Action.
  </action>
  <verify>
    <automated>python3 -c 'import yaml; w=yaml.safe_load(open(".github/workflows/labeler.yml")); on=w.get("on") or w.get(True); t=on["pull_request"]["types"]; assert t==["opened","reopened","ready_for_review"], t; assert w["jobs"]["label"]["steps"][0]["uses"].startswith("actions/labeler@bf12e9b00b37c5c0ca2b87b79b2daf7891dbda13"); print("labeler ok")'</automated>
  </verify>
  <done>Labeler starts on open, reopen, and ready_for_review only. Action SHA and sync-labels false unchanged (D-02).</done>
</task>

<task type="auto">
  <name>Align MANUAL scenario rows and SUMMARY matrix</name>
  <files>.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-MANUAL-GITHUB.md</files>
  <read_first>.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-MANUAL-GITHUB.md</read_first>
  <action>
    Per D-03, D-09, D-13: update existing tables only. Do not add a new docs/*.md file.

    In section D-34 GitHub-Szenariotabelle, rewrite the ready_for_review JS/TS Was-pruefen cell to: Pfadselektive Desktop-Web/Backend-Jobs; CodeQL-Workflow startet nicht auf PRs. Rewrite the Rust-only cell to: desktop-rust plus Aggregator; CodeQL-Workflow startet nicht auf PRs; Rust-Analyse nur auf main-Push / Schedule / Dispatch; kein Backend. Leave other D-34 rows unless they still claim a PR CodeQL run.

    In Vorher/Nachher Fan-out, set the matching Nachher cells for ready_for_review JS/TS and Rust-only to include kein CodeQL-Workflow auf PRs (not merely dropping the rust language on PRs). Vorher column stays historical.

    In Bewusst nicht, if the bullet still claims labeler.yml behavior is untouched this phase, reword so only sync-labels.yml stays deferred; this quick already changed labeler types (D-02). Do not rewrite Settings steps, Dependabot, or SARIF sections.

    When writing 260904-f60-SUMMARY.md, paste the German Pflicht / Nicht Pflicht / Nach Aenderungstyp matrix from this plan context verbatim. English elsewhere in SUMMARY is fine.
  </action>
  <verify>
    <automated>python3 -c 'from pathlib import Path; t=Path(".planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-MANUAL-GITHUB.md").read_text(); assert "CodeQL-Workflow startet nicht auf PRs" in t; assert "kein CodeQL-Workflow auf PRs" in t; assert "kein Rust-CodeQL" not in t; print("manual ok")'</automated>
  </verify>
  <done>MANUAL JS/TS and Rust-only PR rows no longer say CodeQL runs on PRs. SUMMARY contains the German required-vs-skippable matrix (D-03, D-09, D-13).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR fork/same-repo → ci.yml + gitleaks.yml | Untrusted PR code; required checks; read-only GITHUB_TOKEN; no signing secrets |
| PR → codeql.yml (removed) | After D-01 this boundary is closed; CodeQL no longer sees PR heads |
| push main → codeql.yml | Trusted default branch; security-events write |
| schedule → codeql.yml | Trusted weekly scan of default branch |
| workflow YAML PR → zizmor.yml | Untrusted workflow edits; not a required check |
| v* / dispatch → desktop-build.yml | Trusted; signing secrets; not in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-f60-01 | Information disclosure | codeql.yml PR event removed | medium | accept | Locked D-01: PR CodeQL costs ~1.5 min extra and is not a required check (D-13). Residual: JS/TS findings wait until merge to main or Monday 05:18 UTC. PR still has required gitleaks (D-09) and ci tests/clippy. |
| T-f60-02 | Tampering | New workflow triggers | high | mitigate | Do not add privileged PR-from-base trigger or workflow_run deploy (04.4 D-02). Do not run signed desktop-build on PRs (04.4 D-14). Existing SHA pins stay. |
| T-f60-03 | Denial of service | ci.yml / gitleaks.yml on.paths | high | mitigate | Do not add workflow-level paths on required checks. Job-level if plus aggregator already reports Success when areas skip. This plan does not edit those files. |
| T-f60-04 | Tampering | labeler.yml types | low | accept | Dropping the mid-PR git-push type can miss labels for files added in later commits until reopen or ready_for_review. Labels are not a merge gate. |
| T-f60-05 | Elevation of privilege | GitHub Settings API | high | mitigate | Do not change Branch Protection, Environments, or Secret Scanning via API (04.4 D-06). Document only. |
| T-f60-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install tasks. T-f60-SC closed by absence. |
</threat_model>

<verification>
python3 yaml.safe_load on codeql.yml and labeler.yml (handle YAML on-key as True). Task asserts must pass. git diff --name-only must be only the three files_modified paths (plus SUMMARY after wrap-up). git diff --exit-code on ci.yml gitleaks.yml zizmor.yml desktop-build.yml backend-image.yml sync-labels.yml. Do not push. Do not dispatch production desktop. Do not create a v* tag. Do not call GitHub Branch Protection APIs.
</verification>

<success_criteria>
- codeql.yml on-map has no PR event; push/schedule/dispatch unchanged (D-01)
- labeler.yml types are opened, reopened, ready_for_review (D-02)
- MANUAL + SUMMARY state CodeQL is not a PR workflow and not required; ci + gitleaks remain Pflicht (D-03, D-09, D-13)
- Six other workflow files untouched
</success_criteria>

<output>
Create `.planning/quick/260904-f60-audit-all-github-actions-workflows-optim/260904-f60-SUMMARY.md` when done. Include the German Pflicht / Nicht Pflicht / Nach Aenderungstyp matrix from this plan.
</output>
