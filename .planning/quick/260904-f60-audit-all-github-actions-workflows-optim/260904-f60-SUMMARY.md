---
phase: quick-260904-f60
plan: 01
subsystem: infra
tags: [github-actions, codeql, labeler, ci, required-checks]

requires:
  - phase: 04.4
    provides: CI fan-out, aggregator job id ci, gitleaks required-check name, CodeQL SHA pins
provides:
  - CodeQL workflow no longer starts on PR events (push main + weekly + dispatch only)
  - Labeler limited to opened/reopened/ready_for_review
  - Pflicht vs skippable merge-check matrix
affects: [ci, github-actions, merge-gates]

actuals:
  tokens: 1455
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - Workflow-level paths stay off required checks (Pending trap); job-level if skip reports Success
    - CodeQL and zizmor stay not required (D-13)

key-files:
  created: []
  modified:
    - .github/workflows/codeql.yml
    - .github/workflows/labeler.yml
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-MANUAL-GITHUB.md

key-decisions:
  - "D-01: CodeQL off all PR events; residual findings wait until merge to main or Monday 05:18 UTC"
  - "D-02: Labeler skips synchronize; labels are not a merge gate"
  - "D-09/D-13: Required checks remain aggregator ci plus job name gitleaks; CodeQL and zizmor not required"

patterns-established:
  - "Security scans that are not merge-blocking must not use workflow-level on.paths on required workflows"
  - "Cheap labeler: opened/reopened/ready_for_review only"

requirements-completed: [D-01, D-02, D-03, D-09, D-13]

coverage:
  - id: D1
    description: codeql.yml on-map is push/schedule/workflow_dispatch only; detect two branches; analyze ifs output-only
    requirement: D-01
    verification:
      - kind: other
        ref: python3 yaml.safe_load codeql.yml on-keys + detect elif==0 + analyze ifs
        status: pass
    human_judgment: false
  - id: D2
    description: labeler.yml PR types opened, reopened, ready_for_review; Action SHA unchanged
    requirement: D-02
    verification:
      - kind: other
        ref: python3 yaml.safe_load labeler.yml types + actions/labeler SHA
        status: pass
    human_judgment: false
  - id: D3
    description: MANUAL JS/TS and Rust-only rows plus SUMMARY Pflicht matrix; CodeQL not a PR workflow
    requirement: D-03
    verification:
      - kind: other
        ref: python3 assert CodeQL-Workflow startet nicht auf PRs / kein CodeQL-Workflow auf PRs
        status: pass
    human_judgment: false

duration: 2min
completed: 2026-09-04
status: complete
---

# Phase quick-260904-f60 Plan 01: Audit GitHub Actions workflows Summary

**CodeQL läuft nicht mehr auf PRs; Labeler nur noch opened/reopened/ready_for_review; Pflicht-Checks bleiben `ci` + `gitleaks`.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-09-04T09:08:54Z
- **Completed:** 2026-09-04T09:11:48Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments

- `codeql.yml` hat kein PR-Event mehr. Push auf `main` (bestehende Pfade inkl. Rust), weekly cron `18 5 * * 1`, und `workflow_dispatch` bleiben. Detect hat zwei Zweige; Analyze-Jobs hängen nur an Language-Outputs.
- `labeler.yml` startet nicht mehr bei `synchronize`. SHA `bf12e9b00b37c5c0ca2b87b79b2daf7891dbda13`, `sync-labels: false` unverändert.
- `04.4-MANUAL-GITHUB.md` D-34 und Fan-out Nachher: CodeQL-Workflow startet nicht auf PRs. Pflicht-Matrix unten.

## Task Commits

1. **Task 1: End-to-end CodeQL off PR events** - `8c37d74` (feat)
2. **Task 2: Stop labeler on mid-PR git pushes** - `b96a190` (feat)
3. **Task 3: Align MANUAL scenario rows** - `2d67a63` (docs)

**Plan metadata:** skipped (orchestrator Step 8)

## Files Created/Modified

- `.github/workflows/codeql.yml` - PR-Event entfernt; detect/analyze vereinfacht; run-name ohne PR-ID
- `.github/workflows/labeler.yml` - types ohne synchronize
- `.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-MANUAL-GITHUB.md` - D-34 + Fan-out + Bewusst-nicht

Untouched (byte-unchanged): `ci.yml`, `gitleaks.yml`, `zizmor.yml`, `desktop-build.yml`, `backend-image.yml`, `sync-labels.yml`.

## Pflicht vs skippable

### Pflicht (merge-blocking if Branch Protection set)

- `ci` (Aggregator-Job) — jede PR und jeder main-Push. Area-Jobs intern via paths-filter. Docs-only: nur `changes` + `ci` gruen.
- `gitleaks` — jede PR und jeder main-Push. Nie skippen. Check-Name genau `gitleaks` (D-09).

### Nicht Pflicht

- `codeql` — nach D-01: nur main (Pfadfilter) + weekly Mo 05:18 UTC + dispatch. Nicht auf PRs. Nicht required (D-13).
- `zizmor` — nur Workflow-/Action-YAML. Nicht required (D-13).
- `labeler` — billig; nach D-02 nur opened/reopened/ready_for_review. Kein required check.
- `sync-labels` — labels.yml auf main.
- `desktop-build` — v* / dispatch. Nie PR.
- `backend-image` — main backend/tax-engine/lockfile. Nie PR.

### Nach Aenderungstyp

- Docs-only PR: Pflicht ci (changes+aggregator) + gitleaks. Dazu labeler (open/ready). Skip: codeql, zizmor, rust/web/backend-Jobs, desktop-build, backend-image.
- Draft-PR: Pflicht ci (changes+aggregator) + gitleaks. Heavy ci-Jobs erst ready_for_review. codeql startet nicht (D-01).
- Desktop-Web JS/TS ready PR: ci desktop-web + aggregator, gitleaks, labeler. Skip rust/backend unless shared; kein codeql auf PR; kein desktop-build.
- Rust-only PR: ci desktop-rust + aggregator, gitleaks, labeler. Skip web/backend. Rust-CodeQL nur main-Push.
- Backend/tax-engine PR: ci backend-unit + backend-e2e + squawk + aggregator, gitleaks, labeler. Skip desktop unless shared.
- Workflow-YAML PR: gitleaks; zizmor; ci wenn ci.yml im shared-Filter. labeler. codeql nicht auf PR.
- Shared lockfile/package.json: alle ci-Area-Jobs + gitleaks + labeler. Kein desktop-build.
- main backend-Push: ci-Areas + gitleaks + backend-image (+ codeql wenn js/ts/rs-Pfade). Kein desktop-build.
- v*-Tag: desktop-build signed. PR-Workflows N/A.

## Decisions Made

- Followed locked D-01: PR-CodeQL kostet ~1.5 min extra und ist kein required check. Findings warten auf Merge nach `main` oder Montag 05:18 UTC. PR behält required `gitleaks` + `ci`.
- Workflow-level `on.paths` auf required checks (`ci`, `gitleaks`) nicht gesetzt — Pending-Trap.
- GitHub Settings API nicht angerührt (04.4 D-06).

## Deviations from Plan

None - plan executed exactly as written.

Tracer task: `autonomous: true` plus orchestrator "execute all tasks"; tracer `<verify>` re-run after commit passed; continued to expansion (no human-verify pause).

---

**Total deviations:** 0 auto-fixed
**Impact on plan:** None

## Issues Encountered

None

## Authentication Gates

None

## Known Stubs

None

## User Setup Required

None - no external service configuration required. Gründer-Schritte in `04.4-MANUAL-GITHUB.md` (Branch Protection `gitleaks`, Secret Scanning) bleiben manuell.

## Next Phase Readiness

Quick complete. Typical JS/TS PR fan-out: `ci` + `gitleaks` + labeler (open/ready only). CodeQL erst auf `main` / weekly / dispatch. Ready for `/gsd-verify-work` on this quick if desired.

---
*Phase: quick-260904-f60*
*Completed: 2026-09-04*

## Self-Check: PASSED
