---
phase: quick-260904-2ix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/desktop-build.yml
autonomous: true
requirements:
  - G1
  - NODE-24
must_haves:
  truths:
    - "desktop-build.yml tauri job has no setup-node cache and no rust-cache (zizmor cache-poisoning #21 #22)"
    - "setup-node still uses node-version: 24"
    - "ci.yml desktop-test cache is unchanged"
  artifacts:
    - path: .github/workflows/desktop-build.yml
      provides: "Release/publish workflow without Actions cache"
      contains: "node-version: 24"
---

# Fix zizmor cache-poisoning #21 and #22

## Goal

Close the only error-severity Security-tab alerts before audit Phase 6: `zizmor/cache-poisoning` on `desktop-build.yml` (setup-node `cache: pnpm`, `swatinem/rust-cache`).

## Context

`desktop-build` publishes signed Win/macOS bundles and FaynoSync updater artifacts. zizmor docs: remove cache-aware actions from release workflows. `ci.yml` `desktop-test` may keep cache.

## Task

1. Drop `cache: pnpm` from `setup-node`. Keep `node-version: 24`.
2. Remove the `swatinem/rust-cache` step.
3. Do not change Node major anywhere.
