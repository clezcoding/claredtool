---
status: complete
phase: quick-260904-458
plan: 01
subsystem: ci
tags: [github-actions, squawk, sbom, cosign, node24]

requires:
  - phase: main
    provides: SHA-pinned checkout/setup-node/pnpm-action-setup, Node 24 lock, backend-image dual GHCR tags, F-B5 health poll + PREV_TAG rollback
provides:
  - squawk job on Node 24 (squawk-cli 2.64.0) for Prisma migrations plus migrate-diff SQL
  - .squawk.toml excluding already-applied 20260822 baselines
  - SPDX SBOMs and keyless cosign signatures on API and worker GHCR tags after Trivy
affects: [ci, backend-image, github-packages]

actuals:
  tokens: 0
  tasks: 3
  commits: 2

tech-stack:
  added:
    - squawk-cli@2.64.0
    - anchore/sbom-action@3ad7283483fc7af8ff2b4ea19663c2d5ca935e26
    - sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6
  patterns:
    - Do not uses composite actions that hardcode Node 20 when the repo is locked to Node 24
    - buildx provenance/sbom stay false (F-B7); G8 SBOM is Syft + cosign

key-files:
  created:
    - .squawk.toml
  modified:
    - .github/workflows/ci.yml
    - .github/workflows/backend-image.yml

key-decisions:
  - "sbdchd/squawk-action v2.1.0 hardcodes setup-node node-version 20; install squawk-cli@2.64.0 after our Node 24 setup-node instead (D-01)"
  - "Exclude already-applied 20260822 Prisma migrations; new dirs still fail on blocking locks and NOT NULL without default"
  - "Live fail-deploy drill stays human (phasenplan 4b). GET /health/ready was HTTP 200 on 2026-09-04"
  - "Node 24 unchanged"

requirements-completed: [G5, G8, F-B5, AUDIT-6, NODE-24]

coverage:
  - id: D1
    description: squawk-cli 2.64.0 on Node 24 lints Prisma SQL and migrate-diff output; composite squawk action not used
    requirement: G5
    verification:
      - kind: other
        ref: "grep squawk-cli@2.64.0; grep prisma migrate diff; ! grep sbdchd/squawk-action; python3 yaml.safe_load ci.yml"
        status: pass
    human_judgment: false
  - id: D2
    description: SPDX SBOM artifacts plus cosign sign --yes on both GHCR digests after Trivy; buildx sbom/provenance stay false
    requirement: G8
    verification:
      - kind: other
        ref: "grep -c sbom-action SHA == 2; grep cosign-installer SHA; grep id-token write; provenance false count 2"
        status: pass
    human_judgment: false
  - id: D3
    description: /health/ready exists; deploy YAML polls it and rolls back to PREV_TAG; live 200 on 2026-09-04
    requirement: F-B5
    verification:
      - kind: other
        ref: "curl https://clared-api.puzzlessdev.online/health/ready HTTP 200; grep PREV_TAG and phasenplan 4b in backend-image.yml"
        status: pass
    human_judgment: true
    rationale: "Live fail-deploy that proves rollback is GitHub/Coolify only (phasenplan 4b)"

duration: 25min
completed: 2026-09-04
---

# Phase quick-260904-458 Plan 01: Audit Phase 6 Deploy/DB Security

**Squawk-cli on Node 24 for Prisma migrations, live /health/ready 200, Syft SBOM + keyless cosign on both GHCR tags. Node 24 unchanged.**

## Accomplishments

- `ci.yml` squawk job (Harden Runner first, Node 24, postgres 16) lints new Prisma SQL and `prisma migrate diff --script` output
- `.squawk.toml` excludes the three already-applied 20260822 migrations
- Aggregator `ci` job gates squawk (skipped allowed, failure not)
- `backend-image.yml` build job gets `id-token: write`, SPDX artifacts for API and worker, `cosign sign --yes` both digests after Trivy
- buildx `provenance: false` / `sbom: false` stay (F-B7)
- F-B5 rollback comment records live `/health/ready` 200 (2026-09-04); fail-deploy drill stays human

## Task Commits

1. **Task 1: Squawk on Node 24** - `53fe35d` (ci)
2. **Task 2+3: SBOM/cosign + 6.2 comment** - `9f92f6a` (ci)

## Decisions Made

- Do not uses the composite squawk action (Node 20). CLI 2.64.0 after setup-node 24.
- Do not rewrite applied Prisma SQL to silence prefer-robust-stmts / prefer-timestamp-tz.
- Do not add a simulate-fail deploy input.

## User Setup Required

**Rollback drill (phasenplan 4b) — optional, human only.** Agent verified `GET https://clared-api.puzzlessdev.online/health/ready` HTTP 200. To prove PREV_TAG rollback, run a conscious bad deploy via `workflow_dispatch` and confirm the PATCH. Do not do that from an agent session against production.

## Next Phase Readiness

- Audit Phase 6 YAML is on `quick/260904-458-audit-phase-6`
- Deferred: Phase 7 N1-Rest/W2/W1/N4; F-B6/B7/E6/SEC3; live fail-deploy drill
- Product Phase 05 (PDF/Offline/Audit) and product Phase 06 (mockup fidelity) untouched

---

*Phase: quick-260904-458*
*Completed: 2026-09-04*

## Self-Check: PASSED

- FOUND: `.github/workflows/ci.yml` squawk job, squawk-cli@2.64.0, prisma migrate diff
- FOUND: `.squawk.toml`
- FOUND: `.github/workflows/backend-image.yml` sbom-action ×2, cosign-installer, id-token write, provenance false ×2
- FOUND: `53fe35d`
- FOUND: `9f92f6a`
- ABSENT: sbdchd/squawk-action uses
- ABSENT: node-version 20/25/26 in touched workflows
