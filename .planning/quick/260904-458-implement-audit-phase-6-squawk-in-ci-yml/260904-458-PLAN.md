---
phase: quick-260904-458
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/backend-image.yml
  - .squawk.toml
autonomous: true
requirements:
  - G5
  - G8
  - F-B5
  - AUDIT-6
  - NODE-24
user_setup:
  - service: github-coolify-rollback-drill
    why: "Live fail-deploy that proves PREV_TAG rollback is GitHub/Coolify only (phasenplan 4b). YAML already polls /health/ready 30×10s then PATCHes PREV_TAG. Do not add a simulate-fail input that would roll back production."
    dashboard_config:
      - task: "Optional: workflow_dispatch backend-image with a bad tag or a forced health miss, confirm rollback to PREV_TAG, then re-deploy a healthy sha. Agent verified GET https://clared-api.puzzlessdev.online/health/ready HTTP 200 on 2026-09-04."
        location: "GitHub → Actions → backend-image → Run workflow. Not Coolify UI / SSH."
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "ci.yml squawk job lints Prisma migration SQL and prisma migrate diff --script output with squawk-cli 2.64.0 on Node 24 (G5). sbdchd/squawk-action is not used because v2.1.0 hardcodes setup-node node-version 20"
    - "Already-applied 20260822* migrations are excluded in .squawk.toml so baseline CREATE TABLE warnings do not fail CI; new migrations still flag blocking locks and NOT NULL without default"
    - "backend-image build job has id-token write, generates SPDX SBOMs for API and worker tags via anchore/sbom-action (runs node24), then cosign sign --yes both image digests (G8)"
    - "buildx provenance: false and sbom: false stay (F-B7 TECH-DEBT). G8 SBOM is Syft/anchore + cosign, not buildx attestations"
    - "GET /health/ready exists; deploy job already polls it and rolls back to PREV_TAG (F-B5). Live 200 verified 2026-09-04. No production fail-deploy from this task"
    - "Every setup-node node-version stays 24. Dockerfile FROM node:24-bookworm-slim. .nvmrc 24. No Node 20/25/26"
    - "Product ROADMAP Phase 05/06 is not edited. This is audit Phase 6"
  artifacts:
    - .github/workflows/ci.yml
    - .github/workflows/backend-image.yml
    - .squawk.toml
  key_links:
    - from: ci.yml squawk
      to: apps/backend/prisma/migrations
      via: squawk-cli@2.64.0 after setup-node 24
      pattern: "grep -F 'squawk-cli@2.64.0' .github/workflows/ci.yml"
    - from: ci.yml squawk
      to: prisma migrate diff --script
      via: --from-migrations prisma/migrations --to-schema prisma/schema.prisma
      pattern: "grep -F 'prisma migrate diff' .github/workflows/ci.yml"
    - from: backend-image build
      to: GHCR API and worker images
      via: two sbom-action steps then two cosign sign --yes digest
      pattern: "grep -c 'anchore/sbom-action@3ad7283483fc7af8ff2b4ea19663c2d5ca935e26' .github/workflows/backend-image.yml"
    - from: backend-image build permissions
      to: Fulcio/Rekor OIDC
      via: id-token write
      pattern: "grep -F 'id-token: write' .github/workflows/backend-image.yml"
---

<objective>
Implement audit Phase 6 from docs/claredtool-phasenplan.md (G5 Squawk, F-B5 health/rollback verify, G8 SBOM+cosign) after confirming audit Phase 5 is already in the repo. Keep Node 24. Do not edit product ROADMAP Phase 05/06.

Purpose: Lint Prisma migrations for unsafe SQL, keep deploy rollback evidence, attach SBOM + keyless signatures to GHCR images without turning on buildx attestations (F-B7).
Output: squawk job on Node 24, .squawk.toml baseline excludes, SBOM+cosign after Trivy. No ROADMAP product-phase edits. No Phase 7.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/claredtool-phasenplan.md
@.github/workflows/ci.yml
@.github/workflows/backend-image.yml

This is AUDIT Phase 6, not GSD product Phase 6 (mockup fidelity) and not product Phase 05 (PDF/Offline/Audit). Do not edit .planning/ROADMAP.md.

Phase 5 already present (verified this session): zizmor.yml, gitleaks.yml fetch-depth 0, codeql.yml js-ts+rust, Trivy both GHCR tags, Harden-Runner first step on 11 jobs, Node 24, Kaneo #88/#89 done.

Node 24 lock (D-01): .nvmrc 24; Dockerfile FROM node:24-bookworm-slim; every setup-node node-version: 24; dependabot docker ignore node >=25. Do not raise the major. Do not use sbdchd/squawk-action (v2.1.0 composite hardcodes node-version: 20). Install squawk-cli@2.64.0 after our pinned setup-node v7.

Exact pins (peeled 2026-09-04):
- squawk-cli npm 2.64.0 (not an Actions uses)
- anchore/sbom-action@3ad7283483fc7af8ff2b4ea19663c2d5ca935e26 # v0.24.2 (runs using: node24)
- sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
- cosign-release: v3.0.6 (installer default, pin it)

Reuse: checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1; pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10; setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0; harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1

Match existing comment style: 40-char SHA then space hash then version.

Human leftover: live fail-deploy drill (phasenplan 4b). GET /health/ready was HTTP 200 on 2026-09-04. Document in user_setup. Do not add a blocking checkpoint.
</context>

<tasks>

<task type="auto">
  <name>Squawk on Prisma migrations via Node 24 CLI (G5)</name>
  <files>.github/workflows/ci.yml, .squawk.toml</files>
  <action>
    Add .squawk.toml at repo root with pg_version = "16" and excluded_paths for the three already-applied migrations under apps/backend/prisma/migrations/20260822* (init, product_entities_invoices_tax, invoice_dates). Those files already trip prefer-robust-stmts / prefer-timestamp-tz / require-lock-timeout; rewriting applied SQL is out of scope. New migration dirs must still be linted.

    Add job `squawk` to ci.yml: needs changes; same if as backend-unit (main or backend or shared or tax-engine). timeout-minutes 15. Harden Runner first (egress-policy audit). checkout persist-credentials false. pnpm/action-setup + setup-node node-version 24 cache pnpm. pnpm install --frozen-lockfile. npm install -g squawk-cli@2.64.0. Postgres 16-alpine service + DATABASE_URL like backend-e2e (shadow for migrate diff).

    Step 1: squawk --config .squawk.toml --pg-version 16 on every apps/backend/prisma/migrations/**/*.sql.
    Step 2: prisma migrate deploy against the CI postgres, then prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --script to $RUNNER_TEMP/pending.sql (Prisma 7 refuses --from-migrations without shadowDatabaseUrl; do not add shadowDatabaseUrl to prisma.config.ts). If the file has non-comment SQL, squawk it. Empty diff is success.

    Add squawk to the aggregating `ci` job needs list and check() calls (skipped is allowed; failure is not).

    Do not uses: sbdchd/squawk-action. Do not change other jobs' Node version. Do not edit ROADMAP.md.
  </action>
  <verify>
    <automated>test "$(grep -c 'node-version: 24' .github/workflows/ci.yml)" -ge 5 &amp;&amp; ! grep -E 'node-version: 2[056]' .github/workflows/ci.yml &amp;&amp; grep -F 'squawk-cli@2.64.0' .github/workflows/ci.yml &amp;&amp; ! grep -F 'sbdchd/squawk-action' .github/workflows/ci.yml &amp;&amp; grep -F 'prisma migrate diff' .github/workflows/ci.yml &amp;&amp; grep -F 'name: squawk' .github/workflows/ci.yml &amp;&amp; grep -F 'check squawk' .github/workflows/ci.yml &amp;&amp; grep -F 'excluded_paths' .squawk.toml &amp;&amp; python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"</automated>
  </verify>
  <done>ci.yml squawk job runs squawk-cli 2.64.0 on Node 24 against Prisma SQL plus migrate-diff output; baseline 20260822 migrations excluded; aggregator gates squawk.</done>
</task>

<task type="auto">
  <name>SBOM + cosign keyless on both GHCR tags (G8)</name>
  <files>.github/workflows/backend-image.yml</files>
  <action>
    On job build: add permissions id-token: write (keep contents: read, packages: write). Add actions: write so sbom-action can upload artifacts. Give docker/build-push-action ids push-api and push-worker (first API image, second worker target). After both Trivy steps, two anchore/sbom-action@3ad7283483fc7af8ff2b4ea19663c2d5ca935e26 # v0.24.2 steps (API sha tag then -worker tag): format spdx-json, upload-release-assets false, registry-username github.actor, registry-password secrets.GITHUB_TOKEN, distinct artifact-name. Then sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2 with cosign-release v3.0.6. Then cosign sign --yes IMAGE@digest for both push-api and push-worker outputs.digest.

    Keep both provenance: false and sbom: false. Extend the TECH-DEBT comment: G8 SBOM is Syft/anchore + cosign, not buildx attestations.

    Do not bump Node. migrate job setup-node stays 24. Do not enable buildx sbom/provenance.
  </action>
  <verify>
    <automated>test "$(grep -c 'anchore/sbom-action@3ad7283483fc7af8ff2b4ea19663c2d5ca935e26' .github/workflows/backend-image.yml)" -eq 2 &amp;&amp; grep -F 'sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6' .github/workflows/backend-image.yml &amp;&amp; grep -F 'id-token: write' .github/workflows/backend-image.yml &amp;&amp; grep -F 'cosign sign --yes' .github/workflows/backend-image.yml &amp;&amp; test "$(grep -c 'provenance: false' .github/workflows/backend-image.yml)" -eq 2 &amp;&amp; test "$(grep -c 'sbom: false' .github/workflows/backend-image.yml)" -eq 2 &amp;&amp; grep -F 'node-version: 24' .github/workflows/backend-image.yml &amp;&amp; ! grep -E 'node-version: 2[056]' .github/workflows/backend-image.yml &amp;&amp; python3 -c "import yaml; yaml.safe_load(open('.github/workflows/backend-image.yml'))"</automated>
  </verify>
  <done>Both GHCR tags get an SPDX artifact and a keyless cosign signature after Trivy; buildx attestations stay off; Node 24 unchanged.</done>
</task>

<task type="auto">
  <name>Record F-B5 health/rollback verify (6.2)</name>
  <files>.github/workflows/backend-image.yml</files>
  <action>
    Do not simulate a failing production deploy. Confirm in a YAML comment on the existing poll/rollback block: F-B5 polls https://clared-api.puzzlessdev.online/health/ready 30×10s then PATCHes PREV_TAG; live GET /health/ready was HTTP 200 on 2026-09-04; a fail-deploy drill stays human (phasenplan 4b). Do not add workflow_dispatch simulate-fail. Do not call Coolify deploy APIs from this task.
  </action>
  <verify>
    <automated>grep -F 'health/ready' .github/workflows/backend-image.yml &amp;&amp; grep -F 'PREV_TAG' .github/workflows/backend-image.yml &amp;&amp; grep -F 'phasenplan 4b' .github/workflows/backend-image.yml</automated>
  </verify>
  <done>Rollback path remains in YAML with a 6.2 comment; no production blast radius.</done>
</task>

</tasks>
