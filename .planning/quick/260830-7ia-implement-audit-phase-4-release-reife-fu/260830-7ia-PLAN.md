---
phase: quick-260830-7ia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/Dockerfile
  - .github/workflows/backend-image.yml
  - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PDF-OPS.md
  - .github/workflows/desktop-build.yml
  - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-UPDATER-OPS.md
  - .github/workflows/ci.yml
  - .github/workflows/labeler.yml
  - .github/workflows/sync-labels.yml
  - .github/labeler.yml
  - .github/PULL_REQUEST_TEMPLATE.md
autonomous: true
requirements:
  - F-B5
  - F-B6
  - F-B7
  - F-E4
  - F-E5
  - F-E6
  - F-SEC3
  - F-SEC4
  - F-L2
  - F-P1
  - F-SEC5
user_setup:
  - service: github-environment-production
    why: "F-B6 migrate job reads secrets.DATABASE_URL from GitHub Environment production; F-SEC3 reviewers and branch policy cannot be set from YAML"
    env_vars:
      - name: DATABASE_URL
        source: "Repo Settings → Environments → production → Secrets. Same connection string Coolify uses for clared Postgres. Never commit. Job fails closed if unset."
    dashboard_config:
      - task: "Required reviewers; deployment branches main and v* tags. Do not add a wait timer that blocks this private-repo CI."
        location: "GitHub → Settings → Environments → production. YAML only sets environment: production."
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "API container starts with node dist/main.js only; prisma migrate deploy runs once in CI before Coolify deploy (F-B6)"
    - "Worker container still starts with node dist/worker.js (F-B6)"
    - "Deploy job uses GitHub environment production, polls API /health/ready, and rolls back Coolify image tag on failure (F-B5, F-SEC3)"
    - "Worker deploy skips HTTP health (no FQDN) and uses Coolify application status only (F-B5)"
    - "GHCR attestations stay off with an explicit Coolify-upgrade TECH-DEBT comment (F-B7)"
    - "FaynoSync upload aborts unless exactly one matching artifact; basename contains tauri version, platform cue, and arch (F-E4)"
    - "v* publish fails when github.ref_name without leading v disagrees with tauri.conf.json version (F-E5)"
    - "Updater ops doc states Apple Developer ID plus notarization and Windows Authenticode are required for public store/distribution; TAURI_SIGNING_PRIVATE_KEY covers internal FaynoSync (F-E6)"
    - "Every job in ci.yml, backend-image.yml, desktop-build.yml, labeler.yml, and sync-labels.yml has timeout-minutes (F-SEC4)"
    - "documentation labeler does not match .planning markdown; desktop-test runs on ubuntu-24.04 with libwebkit2gtk-4.1-dev (F-L2, F-SEC5)"
    - "PR template test plan covers unit/e2e, prod-build, Prisma migration, desktop/UI, and deploy/infra (F-P1)"
    - "Node major stays 24 in .nvmrc, Dockerfile FROM, and every setup-node step"
  artifacts:
    - apps/backend/Dockerfile
    - .github/workflows/backend-image.yml
    - .github/workflows/desktop-build.yml
    - .github/workflows/ci.yml
    - .github/workflows/labeler.yml
    - .github/workflows/sync-labels.yml
    - .github/labeler.yml
    - .github/PULL_REQUEST_TEMPLATE.md
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-UPDATER-OPS.md
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PDF-OPS.md
  key_links:
    - from: backend-image migrate job
      to: secrets.DATABASE_URL on environment production
      via: pnpm --filter ./apps/backend exec prisma migrate deploy before deploy matrix
      pattern: "grep -F 'prisma migrate deploy' .github/workflows/backend-image.yml"
    - from: backend-image deploy job
      to: https://clared-api.puzzlessdev.online/health/ready
      via: curl poll HTTP 200 after POST /deploy; rollback PATCH previous docker_registry_image_tag
      pattern: "grep -F 'health/ready' .github/workflows/backend-image.yml"
    - from: API Dockerfile CMD
      to: node dist/main.js
      via: JSON-exec form, no shell migration wrapper
      pattern: "grep -F 'CMD [\"node\", \"dist/main.js\"]' apps/backend/Dockerfile"
    - from: publish-faynosync
      to: apps/desktop/src-tauri/tauri.conf.json version
      via: exact-one artifact plus tag/version equality on v* refs
      pattern: "grep -F 'mapfile' .github/workflows/desktop-build.yml"
---

<objective>
Implement audit Phase 4 (Release-Reife) on branch fix/desktop-tauri-matrix-concurrency: Coolify deploy health plus rollback (F-B5), Prisma migrate once in CI not in API CMD (F-B6), keep GHCR attestations off with TECH-DEBT (F-B7), FaynoSync exact-one plus tag/version gate (F-E4, F-E5), OS code-signing docs (F-E6), production environment plus timeouts (F-SEC3, F-SEC4), documentation labeler plus PR template plus ubuntu-24.04 desktop-test (F-L2, F-P1, F-SEC5).

Purpose: Production deploys fail closed, roll back on bad health, and desktop publish cannot upload the wrong binary.
Output: Workflow plus Dockerfile plus two existing ops docs plus PR template and labeler globs. No new markdown files. Do not redo Phase 1–3. Do not create a Kaneo task (ti3dd6tkvihcmqtfwupt5lwh / #84 exists). Node 24 LTS everywhere.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/claredtool-findings-consolidated.md
@apps/backend/Dockerfile
@.github/workflows/backend-image.yml
@.github/workflows/desktop-build.yml
@.github/workflows/ci.yml
@.github/workflows/labeler.yml
@.github/workflows/sync-labels.yml
@.github/labeler.yml
@.github/PULL_REQUEST_TEMPLATE.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-UPDATER-OPS.md
@.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PDF-OPS.md

Stay on fix/desktop-tauri-matrix-concurrency. Phase 1–3 already on this branch (path filters, concurrency, rust-cache, quality gates, frozen-lockfile, Dependabot, SHA pins, rust-toolchain 1.97.1, schema symlink, labels). Do not revert those. F-L1 already in labels.yml plus labeler.yml.

Reuse existing SHA-pinned actions (checkout, setup-node, pnpm/action-setup). New migrate job copies those pins; setup-node with node-version: 24. Do not raise the Node major. Keep .nvmrc at 24 and Dockerfile FROM node:24-bookworm-slim.

Coolify: workflow stays curl against Coolify HTTP API (existing PATCH then POST /deploy pattern). Live GHCR API is vars.COOLIFY_APP_API (operator uuid ucmhsb0srwr7ewt9wxejc4e9, fqdn https://clared-api.puzzlessdev.online). Worker has no FQDN.
</context>

<tasks>

<task type="tracer">
  <name>API migrate-once, production deploy health, rollback (F-B5, F-B6, F-B7, F-SEC3)</name>
  <files>apps/backend/Dockerfile, .github/workflows/backend-image.yml, .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PDF-OPS.md</files>
  <action>
    Wire one release path: image CMD no longer migrates, CI migrates once, Coolify deploy is health-gated with rollback (F-B5, F-B6, F-SEC3). Keep attestations disabled (F-B7).

    Dockerfile (F-B6): On the api stage only, change CMD to JSON-exec form node dist/main.js. Remove the shell wrapper that chained a database migration into process start. Leave the worker stage CMD as node dist/worker.js. Keep apt curl install (Coolify HTTP probe). Do not add a Dockerfile HEALTHCHECK instruction. Do not change FROM image, prisma generate, or nest build. Update the file-header comment in backend-image.yml that still describes the API stage as migrate-plus-main.

    backend-image.yml migrate job (F-B6): Insert job migrate after build, before deploy. needs: build. Same if: condition as deploy (successful build, COOLIFY_IMAGE_DEPLOY true, ref main, workflow_dispatch respects inputs.deploy). environment: production. runs-on ubuntu-latest. timeout-minutes: 15. Steps: checkout (existing SHA pin), pnpm/action-setup (existing pin), setup-node with node-version: 24 and cache pnpm (existing pin), pnpm install --frozen-lockfile, then a run step that fails closed if DATABASE_URL is empty (print a clear error that production environment secret is required; do not skip), then pnpm --filter ./apps/backend exec prisma migrate deploy using secrets.DATABASE_URL. Do not call Coolify MCP from the workflow.

    backend-image.yml deploy job (F-B5, F-SEC3): Add environment: production. needs: [build, migrate]. timeout-minutes: 20. Keep matrix max-parallel 1, fail-fast false, concurrency cancel-in-progress false. YAML comment listing GitHub UI settings that YAML cannot set: required reviewers; deployment branch policy main plus v* tags; no wait timer for this private repo. Do not add a job-level wait-timer.

    Inside the existing Pin sha tag and deploy run script, before PATCH: GET ${COOLIFY_URL}/api/v1/applications/${APP_UUID} with Bearer token, capture previous docker_registry_image_tag via jq (empty string if null). Then PATCH new tag and POST /deploy as today.

    After POST succeeds: if matrix.app.name is clared-api, poll GET https://clared-api.puzzlessdev.online/health/ready until HTTP 200 or about 5 minutes (for example 30 tries, 10s sleep). Treat non-200 including 503 as not ready. If matrix.app.name is clared-worker, do not HTTP-probe any host; poll Coolify GET application until status contains running (case-insensitive) with the same timeout. Never invent a worker FQDN.

    On poll failure: if previous tag is non-empty, PATCH that tag back and POST /deploy again, then exit 1. If previous tag is empty, print that rollback is impossible and exit 1. Success path only when poll succeeds.

    Timeouts (F-SEC4, this file): build job timeout-minutes: 45. migrate 15. deploy 20.

    F-B7: Keep both docker/build-push-action steps at provenance: false and sbom: false. Add an explicit TECH-DEBT comment that Coolify cannot pull attestation indexes; re-enable after a Coolify upgrade that can. Do not set either flag true.

    04.3-PDF-OPS.md: Add a short GHCR attestations note (four lines or fewer) pointing at those flags and the Coolify-upgrade trigger. Do not create a new markdown file.
  </action>
  <verify>
    <automated>grep -F 'CMD ["node", "dist/main.js"]' apps/backend/Dockerfile &amp;&amp; grep -F 'CMD ["node", "dist/worker.js"]' apps/backend/Dockerfile &amp;&amp; ! grep -F 'CMD ["sh"' apps/backend/Dockerfile &amp;&amp; grep -F 'curl' apps/backend/Dockerfile &amp;&amp; grep -F 'FROM node:24-bookworm-slim' apps/backend/Dockerfile &amp;&amp; grep -F 'pnpm --filter ./apps/backend exec prisma migrate deploy' .github/workflows/backend-image.yml &amp;&amp; grep -F 'node-version: 24' .github/workflows/backend-image.yml &amp;&amp; grep -F 'environment: production' .github/workflows/backend-image.yml &amp;&amp; grep -F 'health/ready' .github/workflows/backend-image.yml &amp;&amp; grep -F 'docker_registry_image_tag' .github/workflows/backend-image.yml &amp;&amp; grep -F 'TECH-DEBT' .github/workflows/backend-image.yml &amp;&amp; test "$(grep -c 'provenance: false' .github/workflows/backend-image.yml)" -eq 2 &amp;&amp; test "$(grep -c 'sbom: false' .github/workflows/backend-image.yml)" -eq 2 &amp;&amp; test "$(grep -c 'timeout-minutes:' .github/workflows/backend-image.yml)" -eq 3 &amp;&amp; grep -F 'TECH-DEBT' .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PDF-OPS.md &amp;&amp; test "$(cat .nvmrc)" = "24"</automated>
  </verify>
  <done>API CMD is node-only; migrate job runs once with fail-closed DATABASE_URL before deploy; deploy polls API ready and rolls back; worker has no HTTP health; attestations remain false with TECH-DEBT; Node 24 unchanged.</done>
</task>

<task type="auto">
  <name>FaynoSync exact-one, tag/version gate, signing docs (F-E4, F-E5, F-E6)</name>
  <files>.github/workflows/desktop-build.yml, .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-UPDATER-OPS.md</files>
  <action>
    Harden publish-faynosync then document OS signing in the existing updater ops SSOT (F-E4, F-E5, F-E6).

    F-E4: In Upload macOS updater and Upload Windows updater, stop using first-match find pipelines. Collect paths with bash mapfile into an array (macos: type-f names ending .app.tar.gz excluding .sig; windows: nsis .exe excluding .sig). Abort with a printed listing if array length is not 1. Before curl upload, require basename contains: the version already read from apps/desktop/src-tauri/tauri.conf.json; an arch token matching the FaynoSync payload (aarch64 on macOS, x64 on Windows); a platform cue (.app.tar.gz means darwin, nsis .exe means windows). Do not upload on mismatch.

    F-E5: In the existing Read app version step (or immediately after), when github.ref starts with refs/tags/v, strip the leading v from github.ref_name / GITHUB_REF_NAME and compare to the tauri.conf.json version. On mismatch, fail the publish job. Non-tag refs skip this compare.

    F-SEC4 on this file: tauri job timeout-minutes: 90; publish-faynosync timeout-minutes: 20.

    F-E6: Extend 04.2-UPDATER-OPS.md in place. Replace the line that treats Apple Developer ID, Authenticode, and notarization as deferred. State they are required for public store and Gatekeeper/SmartScreen distribution. State TAURI_SIGNING_PRIVATE_KEY already covers internal FaynoSync updater payloads. Do not add workflow secrets or cert files. Do not invent live certificate values. Keep existing FaynoSync UUID tables.

    Keep setup-node node-version: 24. Do not touch Phase 1–3 concurrency or path filters.
  </action>
  <verify>
    <automated>grep -F 'mapfile' .github/workflows/desktop-build.yml &amp;&amp; ! grep -F 'head -1' .github/workflows/desktop-build.yml &amp;&amp; grep -E 'GITHUB_REF_NAME|github.ref_name' .github/workflows/desktop-build.yml &amp;&amp; grep -F 'tauri.conf.json' .github/workflows/desktop-build.yml &amp;&amp; test "$(grep -c 'timeout-minutes:' .github/workflows/desktop-build.yml)" -eq 2 &amp;&amp; grep -F 'node-version: 24' .github/workflows/desktop-build.yml &amp;&amp; grep -Fi 'notarization' .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-UPDATER-OPS.md &amp;&amp; grep -Fi 'Authenticode' .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-UPDATER-OPS.md &amp;&amp; grep -F 'TAURI_SIGNING_PRIVATE_KEY' .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-UPDATER-OPS.md</automated>
  </verify>
  <done>Publish uploads only an exact-one versioned artifact; v* tags must match tauri version; updater ops doc names Apple notarization and Authenticode as public-distribution requirements.</done>
</task>

<task type="auto">
  <name>Job timeouts, docs labeler, PR template, ubuntu-24.04 (F-SEC4, F-L2, F-P1, F-SEC5)</name>
  <files>.github/workflows/ci.yml, .github/workflows/labeler.yml, .github/workflows/sync-labels.yml, .github/labeler.yml, .github/PULL_REQUEST_TEMPLATE.md</files>
  <action>
    Close remaining Phase 4 hygiene without touching backend-image or desktop-build (timeouts already set there).

    F-SEC4: Add timeout-minutes on every job that still lacks it. ci.yml: changes 5; lint 15; desktop-test 15; backend-unit 15; backend-e2e 20; aggregator job ci 5. labeler.yml label job 5. sync-labels.yml sync job 5. Do not rely on the 360-minute GitHub default.

    F-SEC5: Set desktop-test runs-on to ubuntu-24.04. Keep the apt packages including libwebkit2gtk-4.1-dev, libappindicator3-dev, librsvg2-dev, patchelf, xdg-utils.

    F-L2: In .github/labeler.yml documentation.changed-files, replace the catch-all markdown glob so .planning/** is not also labeled documentation. Keep planning: on .planning/**. documentation any-glob-to-any-file must be only: README.md, docs/**, apps/*/README.md, packages/*/README.md.

    F-P1: Expand .github/PULL_REQUEST_TEMPLATE.md Test plan into checkboxes: unit and e2e green for affected packages; production build of the affected app; Prisma migrate/schema if schema or migrations changed; desktop/UI checks if apps/desktop or packages/ui changed; deploy/infra checks if workflows, Dockerfile, or compose changed. Keep Summary and Merge sections.

    Every setup-node in ci.yml stays node-version: 24. Do not edit labels.yml (F-L1 already done).
  </action>
  <verify>
    <automated>test "$(grep -c 'timeout-minutes:' .github/workflows/ci.yml)" -eq 6 &amp;&amp; test "$(grep -c 'timeout-minutes:' .github/workflows/labeler.yml)" -eq 1 &amp;&amp; test "$(grep -c 'timeout-minutes:' .github/workflows/sync-labels.yml)" -eq 1 &amp;&amp; grep -F 'ubuntu-24.04' .github/workflows/ci.yml &amp;&amp; ! grep -F 'ubuntu-22.04' .github/workflows/ci.yml &amp;&amp; grep -F 'libwebkit2gtk-4.1-dev' .github/workflows/ci.yml &amp;&amp; grep -F 'node-version: 24' .github/workflows/ci.yml &amp;&amp; grep -F 'README.md' .github/labeler.yml &amp;&amp; grep -F 'docs/**' .github/labeler.yml &amp;&amp; grep -F 'apps/*/README.md' .github/labeler.yml &amp;&amp; grep -F 'packages/*/README.md' .github/labeler.yml &amp;&amp; awk '/^documentation:/{d=1} d&amp;&amp;/planning:/{exit} d' .github/labeler.yml | grep -qvF '**/*.md' &amp;&amp; grep -Fi 'prisma' .github/PULL_REQUEST_TEMPLATE.md &amp;&amp; grep -Fi 'e2e' .github/PULL_REQUEST_TEMPLATE.md &amp;&amp; grep -Fi 'Dockerfile' .github/PULL_REQUEST_TEMPLATE.md &amp;&amp; ! grep -E 'node-version:[[:space:]]*(25|26)|node:25|node:26' .github/workflows/*.yml apps/backend/Dockerfile .nvmrc</automated>
  </verify>
  <done>All listed workflow jobs have timeouts; documentation labeler ignores .planning; PR template lists generalized checks; desktop-test is ubuntu-24.04 with webkit 4.1; Node 24 only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → Coolify API | Bearer COOLIFY_TOKEN can PATCH image tags and force deploy |
| GitHub Actions runner → production Postgres | migrate job uses secrets.DATABASE_URL |
| GitHub Actions runner → public /health/ready | Unauthenticated readiness probe of live API |
| GitHub Actions runner → FaynoSync | CI token uploads updater artifacts |
| GitHub Environment production → deploy/migrate jobs | Protection rules (reviewers, branch policy) live in GitHub UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7ia-01 | Elevation | backend-image deploy | high | mitigate | F-SEC3: environment production on migrate and deploy; YAML comment lists required reviewers and main/v* branch policy |
| T-7ia-02 | Tampering | API replicas vs schema | high | mitigate | F-B6: migrate once in CI fail-closed; API CMD is node-only so replicas cannot race migrate |
| T-7ia-03 | Tampering | Coolify bad roll | high | mitigate | F-B5: GET previous tag, poll health/ready, PATCH previous tag and POST /deploy on failure |
| T-7ia-04 | Tampering | FaynoSync upload | high | mitigate | F-E4 exact-one mapfile; basename must contain version, platform cue, arch; F-E5 tag equals tauri version |
| T-7ia-05 | Denial | hung GHA jobs | medium | mitigate | F-SEC4 timeout-minutes on every job in the five workflow files |
| T-7ia-06 | Information | deploy curl logs | low | accept | Token already in env; do not echo COOLIFY_TOKEN or DATABASE_URL |
| T-7ia-07 | Tampering | missing GHCR SBOM | medium | accept | F-B7: Coolify cannot pull attestation indexes; TECH-DEBT plus 04.3-PDF-OPS note; re-enable after Coolify upgrade |
| T-7ia-SC | Tampering | npm/pip/cargo installs | high | accept | No new package-manager installs in this plan |
</threat_model>

<verification>
Task verify blocks plus: grep node-version: 24 in every setup-node workflow; cat .nvmrc equals 24; provenance and sbom still false twice in backend-image.yml. Do not run a live Coolify deploy from this branch as the proof — YAML structure is the gate. Optional: actionlint if installed.
</verification>

<success_criteria>
- F-B5, F-B6, F-B7, F-E4, F-E5, F-E6, F-SEC3, F-SEC4, F-L2, F-P1, F-SEC5 all present in the files above
- Phase 1–3 behavior (path filters, concurrency, SHA pins, rust-toolchain, schema symlink, F-L1 labels) unchanged
- Node 24 only — no 25/26 in Dockerfiles, workflows, or .nvmrc
- No new Kaneo task; no new ops markdown file
</success_criteria>

<output>
Create .planning/quick/260830-7ia-implement-audit-phase-4-release-reife-fu/260830-7ia-SUMMARY.md when done
</output>

<!-- gsd-multi-source-audit
GOAL: Implement audit Phase 4 Release-Reife fully; Node 24 lock
REQ: F-B5 F-B6 F-B7 F-E4 F-E5 F-E6 F-SEC3 F-SEC4 F-L2 F-P1 F-SEC5
RESEARCH: Context7 — environment production, timeout-minutes, prisma migrate in release job, provenance/sbom stay false
CONTEXT: stay on fix/desktop-tauri-matrix-concurrency; Kaneo #84 exists; do not redo Phase 1-3; F-L1 already done
COVERED: F-B5+F-B6+F-B7+F-SEC3→task1; F-E4+F-E5+F-E6→task2; F-SEC4+F-L2+F-P1+F-SEC5→task3
DEFERRED: GitHub UI reviewers/wait timer (documented, YAML cannot set); F-L1 already in repo
-->
