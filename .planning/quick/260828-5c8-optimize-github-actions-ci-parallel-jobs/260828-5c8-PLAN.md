---
phase: quick-260828-5c8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/desktop-build.yml
  - apps/desktop/vitest.config.ts
autonomous: true
requirements:
  - CI-OPT-01
  - CI-OPT-02
  - CI-OPT-03
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "ci.yml runs lint, desktop-test, backend-unit, and backend-e2e as parallel jobs; only backend-e2e starts postgres/redis"
    - "PR runs path-filtered jobs; push to main runs every job regardless of paths"
    - "tax-engine or shared path changes trigger desktop and backend job families on PRs"
    - "Branch protection gate job remains named ci and fails when any required job fails"
    - "desktop-build.yml caches Rust workspace apps/desktop/src-tauri via swatinem/rust-cache@v2"
    - "Desktop vitest runs with default file parallelism; full suite passes locally"
  artifacts:
    - .github/workflows/ci.yml
    - .github/workflows/desktop-build.yml
    - apps/desktop/vitest.config.ts
  key_links:
    - from: changes job
      to: lint/desktop-test/backend-unit/backend-e2e
      via: needs.changes.outputs + main bypass
      pattern: "grep -q 'dorny/paths-filter@v3' .github/workflows/ci.yml"
    - from: ci gate job
      to: lint, desktop-test, backend-unit, backend-e2e
      via: needs + aggregate pass/fail
      pattern: "grep -E '^  ci:' .github/workflows/ci.yml"
    - from: rust-cache step
      to: dtolnay/rust-toolchain
      via: ordered steps before pnpm install / tauri build
      pattern: "grep -q 'swatinem/rust-cache@v2' .github/workflows/desktop-build.yml"
---

<objective>
Split monolithic CI into parallel path-filtered jobs, add Rust build cache to desktop-build, and re-enable Vitest file parallelism.

Purpose: Cut PR CI wall-clock by skipping unrelated jobs and running independent checks concurrently; speed desktop matrix builds with rust-cache; shave desktop test time (~7.7s parallel vs serial).
Output: Updated `ci.yml`, `desktop-build.yml`, `vitest.config.ts` on branch `chore/ci-optimize` from `origin/main`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.github/workflows/ci.yml
@.github/workflows/desktop-build.yml
@apps/desktop/vitest.config.ts

Branch: `chore/ci-optimize` from `origin/main`. Scope: only the three files above — no Phase 04.1 feature work.

Current ci.yml: single `ci` job runs tsc, vitest, tax-engine build, prisma migrate, backend e2e, backend unit, tax-engine tests — all with postgres/redis services.

Monorepo layout (path-filter inputs):
- desktop: `apps/desktop/**`, `packages/ui/**` (desktop depends on `@clared/ui`)
- backend: `apps/backend/**`
- tax-engine: `packages/tax-engine/**`
- shared (triggers all job families on PR): root `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `packages/ui/**`, `.github/workflows/ci.yml`

PR filter rule: desktop jobs run when `desktop || shared || tax-engine`; backend jobs run when `backend || shared || tax-engine`. Push to `main` bypasses filters (always run all jobs).

Vitest: remove `fileParallelism: false` only — 121 tests pass in ~7.7s locally with parallelism enabled.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Parallel path-filtered ci.yml with ci gate</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Replace the single-job workflow with six jobs on branch `chore/ci-optimize`:

    1. **changes** — `ubuntu-latest`, checkout, `dorny/paths-filter@v3` id `filter`, outputs `desktop`, `backend`, `tax-engine`, `shared` using the path globs in context. No services.

    2. **lint** — `needs: changes`. `if`: `github.ref == 'refs/heads/main' || needs.changes.outputs.desktop == 'true' || needs.changes.outputs.shared == 'true' || needs.changes.outputs.tax-engine == 'true'`. Steps: checkout, pnpm/action-setup@v6, setup-node@v7 (node 26, cache pnpm), `pnpm install`, `pnpm --filter ./apps/desktop exec tsc --noEmit`. No postgres/redis.

    3. **desktop-test** — same `needs`/`if` as lint. Steps through install, then `pnpm --filter ./apps/desktop test`. No services.

    4. **backend-unit** — `needs: changes`. `if`: `github.ref == 'refs/heads/main' || needs.changes.outputs.backend == 'true' || needs.changes.outputs.shared == 'true' || needs.changes.outputs.tax-engine == 'true'`. Steps through install, then in order: `pnpm --filter @clared/tax-engine build`, `pnpm --filter ./apps/backend test`, `pnpm --filter @clared/tax-engine test`. No postgres/redis.

    5. **backend-e2e** — same `needs`/`if` as backend-unit. Only job with `services.postgres` (postgres:16-alpine, prisma-test/clared) and `services.redis` (redis:7-alpine). Env `DATABASE_URL` and `REDIS_URL` match current ci.yml. Steps through install, then: `pnpm --filter @clared/tax-engine build`, `pnpm --filter ./apps/backend exec prisma migrate deploy`, `pnpm --filter ./apps/backend test:e2e`.

    6. **ci** (gate, keep this exact job name for branch protection) — `needs: [changes, lint, desktop-test, backend-unit, backend-e2e]`, `if: always()`, `runs-on: ubuntu-latest`. One step that exits non-zero when any needed job result is `failure` or `cancelled`; treat `skipped` as OK (PR path filter). Do not run install or tests in the gate job.

    Preserve top-level: `name: ci`, triggers (`push` main + `pull_request`), `permissions: contents: read`, `concurrency` group unchanged.

    Do not add new workflow files. Do not change desktop-build.yml or vitest.config.ts in this task.
  </action>
  <verify>
    <automated>grep -q 'dorny/paths-filter@v3' .github/workflows/ci.yml &amp;&amp; grep -qE '^  (changes|lint|desktop-test|backend-unit|backend-e2e|ci):' .github/workflows/ci.yml &amp;&amp; grep -c 'services:' .github/workflows/ci.yml | grep -qx '1'</automated>
  </verify>
  <done>Six jobs present; paths-filter drives PR skips; main push runs all; only backend-e2e has services; gate job named `ci` aggregates results.</done>
</task>

<task type="auto">
  <name>Task 2: Rust cache in desktop-build.yml</name>
  <files>.github/workflows/desktop-build.yml</files>
  <action>
  After `dtolnay/rust-toolchain@stable` and before `pnpm install`, add `swatinem/rust-cache@v2` with:
  - `workspaces: apps/desktop/src-tauri`
  - `cache-on-failure: true`

  Keep existing matrix (windows-latest, macos-latest), artifact upload steps, path filters, and concurrency unchanged. Do not modify ci.yml in this task.
  </action>
  <verify>
    <automated>grep -q 'swatinem/rust-cache@v2' .github/workflows/desktop-build.yml &amp;&amp; grep -q 'apps/desktop/src-tauri' .github/workflows/desktop-build.yml &amp;&amp; grep -q 'cache-on-failure' .github/workflows/desktop-build.yml</automated>
  </verify>
  <done>Both matrix OS jobs restore/save Rust cache for the Tauri workspace before build.</done>
</task>

<task type="auto">
  <name>Task 3: Enable Vitest file parallelism</name>
  <files>apps/desktop/vitest.config.ts</files>
  <action>
  Remove the `fileParallelism: false` line from the `test` block in `apps/desktop/vitest.config.ts`. Leave `environment`, `globals`, and `setupFiles` unchanged. Do not add pool/worker overrides unless tests fail — default Vitest parallelism is the goal.

  Run full desktop test suite from repo root to confirm green.
  </action>
  <verify>
    <automated>! grep -q 'fileParallelism' apps/desktop/vitest.config.ts &amp;&amp; pnpm --filter ./apps/desktop test</automated>
  </verify>
  <done>`fileParallelism` removed; `pnpm --filter ./apps/desktop test` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions | Third-party actions (dorny/paths-filter, swatinem/rust-cache) execute in CI with repo read access |
| PR path filters | Incorrect filters could skip required checks on PRs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ci-01 | Tampering | dorny/paths-filter@v3 | medium | mitigate | Pin major version `@v3`; main push bypasses filters so main always runs full suite |
| T-ci-02 | Denial of Service | Path filter false negative | high | mitigate | shared + tax-engine outputs force all job families; gate `ci` job fails on any failure/cancelled |
| T-ci-03 | Tampering | swatinem/rust-cache@v2 | low | mitigate | Pin `@v2`; cache-on-failure only affects build speed not artifact integrity |
| T-ci-04 | Elevation | Skipped jobs on PR | medium | accept | Intentional scope reduction on PR; main branch runs complete matrix |
</threat_model>

<verification>
- `grep` confirms six ci.yml jobs and single `services:` block (backend-e2e only)
- `grep` confirms rust-cache step in desktop-build.yml
- `pnpm --filter ./apps/desktop test` passes after vitest config change
- Optional: push branch and confirm GitHub Actions shows parallel jobs with expected skips on a narrow PR
</verification>

<success_criteria>
- CI parallelized with path filters on PR and full run on main
- Branch protection `ci` check preserved as aggregate gate
- Desktop matrix builds use Rust cache
- Desktop tests run in parallel and pass
- Only three files changed; no Phase 04.1 scope mixed in
</success_criteria>

<output>
Create `.planning/quick/260828-5c8-optimize-github-actions-ci-parallel-jobs/260828-5c8-SUMMARY.md` when done
</output>
