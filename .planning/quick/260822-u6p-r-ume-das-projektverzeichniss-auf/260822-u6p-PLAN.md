---
phase: quick-260822-u6p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements:
  - CLEANUP-01
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Large generated caches (Rust target, GSD tmp) removed from disk"
    - "Build output dirs (dist) and stray .DS_Store files removed"
    - "*.tsbuildinfo is gitignored; no untracked tsbuildinfo in git status"
    - "Source code, .planning/ phase docs, .cursor/rules/, and .gsd/ sentinel untouched"
  artifacts:
    - .gitignore
  key_links:
    - from: "*.tsbuildinfo"
      to: .gitignore
      via: "gitignore pattern"
      pattern: "grep -q 'tsbuildinfo' .gitignore"
---

<objective>
Remove generated/cache artifacts from the Clared monorepo and tighten `.gitignore` so TypeScript incremental build info never surfaces as untracked files again.

Purpose: Reclaim ~5GB disk (mostly `apps/desktop/src-tauri/target`) and keep `git status` clean without touching source or planning docs.
Output: Deleted caches/build outputs; updated `.gitignore`; verification receipt in SUMMARY.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Cleanup scope (confirmed):
- DELETE: `apps/desktop/src-tauri/target` (~4.8GB, gitignored)
- DELETE: `.planning/tmp/` (~57MB, gitignored)
- DELETE: `apps/backend/dist`, `apps/desktop/dist`, `packages/tax-engine/dist` (gitignored)
- DELETE: `.DS_Store` at repo root and under `.cursor/` (gitignored pattern exists)
- DELETE: `apps/backend/tsconfig.build.tsbuildinfo` (untracked, not yet gitignored)
- KEEP: `.gsd/` dispatch sentinel (gitignored, intentional)
- KEEP: all source, `.planning/` phase docs (except `tmp/`), `.cursor/rules/*.mdc`
- ADD to `.gitignore`: `*.tsbuildinfo` (Dockerfile already `rm -f tsconfig.build.tsbuildinfo` at build time; repo-wide ignore prevents recurrence)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove large caches</name>
  <files>apps/desktop/src-tauri/target, .planning/tmp/</files>
  <action>
    From repo root, delete only if path exists:
    - `apps/desktop/src-tauri/target/` (Rust/Tauri build cache)
    - `.planning/tmp/` (GSD agent scratch)
    Do NOT delete `.gsd/`, any `.planning/phases/` tree, or anything under `src/`.
    Record `du -sh` on each path before delete (skip if missing).
  </action>
  <verify>
    <automated>test ! -d apps/desktop/src-tauri/target &amp;&amp; test ! -d .planning/tmp</automated>
  </verify>
  <done>Both cache dirs absent; before-size noted in commit message or SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Remove build outputs and macOS junk</name>
  <files>apps/backend/dist, apps/desktop/dist, packages/tax-engine/dist, apps/backend/tsconfig.build.tsbuildinfo, .DS_Store, .cursor/.DS_Store</files>
  <action>
    Delete only if present (no error when missing):
    - `apps/backend/dist/`
    - `apps/desktop/dist/`
    - `packages/tax-engine/dist/`
    - `apps/backend/tsconfig.build.tsbuildinfo`
    - `.DS_Store` at repo root
    - `.cursor/.DS_Store` (and any other `.DS_Store` under `.cursor/` via `find .cursor -name .DS_Store -delete`)
    Do NOT touch `node_modules/`, `.cursor/rules/`, or any `*.ts` / `*.tsx` / `*.rs` source files.
  </action>
  <verify>
    <automated>test ! -f apps/backend/tsconfig.build.tsbuildinfo &amp;&amp; ! find . -maxdepth 3 -name .DS_Store | grep -q .</automated>
  </verify>
  <done>Dist dirs and tsbuildinfo removed; no .DS_Store within depth 3.</done>
</task>

<task type="auto">
  <name>Task 3: Gitignore tsbuildinfo and verify clean tree</name>
  <files>.gitignore</files>
  <action>
    Append `*.tsbuildinfo` to `.gitignore` under the Nest/Prisma section (after line `apps/backend/src/generated/`), with a one-line comment `# TypeScript incremental build info`.
    Run `git status --short` and confirm:
    - `.gitignore` shows as modified (only expected tracked change)
    - No `*.tsbuildinfo` files appear as untracked
    - Untracked `.cursor/rules/*.mdc` still present (do NOT delete or stage them)
    - No source files deleted or modified
  </action>
  <verify>
    <automated>grep -q 'tsbuildinfo' .gitignore &amp;&amp; ! git status --short | grep -q tsbuildinfo</automated>
  </verify>
  <done>`*.tsbuildinfo` in `.gitignore`; git status free of tsbuildinfo; rules untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem | `rm -rf` on wrong path could delete source |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-01 | Tampering | rm targets | high | mitigate | Delete only listed paths; verify each path with `test -d` before `rm -rf`; never glob `src/` |
| T-quick-02 | Denial of Service | target/ delete | low | accept | Rebuild via `pnpm tauri build` / `cargo build` when needed |
</threat_model>

<verification>
- `du -sh apps/desktop/src-tauri/target` fails (path gone)
- `git status --short` shows only `.gitignore` modified (plus pre-existing untracked rules if any)
- `find . -name '*.tsbuildinfo' -not -path './node_modules/*'` returns empty
</verification>

<success_criteria>
- ~5GB disk reclaimed from removed caches
- Build outputs and macOS junk removed
- `*.tsbuildinfo` gitignored; workspace hygiene improved with zero source changes
</success_criteria>

<output>
Create `.planning/quick/260822-u6p-r-ume-das-projektverzeichniss-auf/260822-u6p-SUMMARY.md` when done
</output>
