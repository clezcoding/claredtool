---
phase: 02-self-hosted-backend-authentik-sso
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/package.json
  - apps/backend/tsconfig.json
  - apps/backend/tsconfig.build.json
  - apps/backend/nest-cli.json
  - apps/backend/jest.config.cjs
  - apps/backend/src/main.ts
  - apps/backend/src/app.module.ts
  - apps/backend/src/auth/rbac.spec.ts
  - apps/backend/test/health.e2e-spec.ts
  - apps/backend/test/auth.e2e-spec.ts
  - apps/backend/test/jest-e2e.json
  - apps/desktop/src/__tests__/auth-gate.test.tsx
  - apps/desktop/src/__tests__/session-chip.test.tsx
  - apps/desktop/src/__tests__/session-banner.test.tsx
  - apps/desktop/src/__tests__/auth-signed-in.ts
  - apps/desktop/src/__tests__/routes.test.tsx
autonomous: true
requirements: [BACK-01, AUTH-01]
user_setup: []

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Nyquist Wave 0: `pnpm --filter ./apps/backend test` is a defined script that runs Jest (02-VALIDATION.md 02-W0-01)"
    - "Failing (RED) e2e specs exist for GET /health, GET /health/ready, unauthenticated GET /me and GET /api/invoices (BACK-01, D-09, D-10)"
    - "Failing (RED) unit spec exists for group-union permissions and primaryRole precedence (AUTH-01, D-22, D-23, D-24, D-25)"
    - "Desktop Vitest files exist for gate, chip, banners; existing routes.test.tsx still passes with a signed-in fixture (UI-01 carry-forward)"
    - "Root LICENSE remains UNLICENSED; workspace glob apps/* already includes apps/backend (D-01, D-04)"
  artifacts:
    - "apps/backend/package.json (name backend, private, script test)"
    - "apps/backend/src/auth/rbac.spec.ts"
    - "apps/backend/test/health.e2e-spec.ts"
    - "apps/backend/test/auth.e2e-spec.ts"
    - "apps/desktop/src/__tests__/auth-gate.test.tsx"
    - "apps/desktop/src/__tests__/session-chip.test.tsx"
    - "apps/desktop/src/__tests__/session-banner.test.tsx"
    - "apps/desktop/src/__tests__/auth-signed-in.ts"
  key_links:
    - "apps/backend is a pnpm workspace package via existing apps/* glob — no pnpm-workspace.yaml edit"
    - "Desktop auth specs are describe.skip('phase02-auth') so Wave 1 CI/desktop suite stays green until 02-04 unsips them"
    - "Backend tests are RED by design; GitHub CI workflow is not taught the backend test script until 02-02 turns them green"
  prohibitions:
    - statement: "MUST NOT present Clared as open source, free, or customer-self-hosted"
      status: flagged-unverified
    - statement: "MUST NOT add a second HTTP framework beside Nest Express"
      status: flagged-unverified
    - statement: "MUST NOT scaffold a tax-engine package or invoice/entity HTTP stubs"
      status: flagged-unverified
  flagged_assumptions:
    - "[EDGE unresolved · BACK-01 unclassified] Probe could not classify BACK-01 shape; treated as HTTPS+OIDC vendor-cluster connectivity covered by later tracer, not silently dropped."
---

<objective>
Nyquist Wave 0: scaffold `apps/backend/` enough to run Jest, and write the RED tests at the public seams (`/health`, `/me`, `rbac.ts`, gate/chip/banners) before any production auth code. Protect Phase 1 route tests with a signed-in fixture.

Purpose: Test files exist first so 02-02 tracer and 02-04 UI have a failing specification to turn green (project TDD skill: public seams only).
Output: Nest package + RED backend specs + skipped desktop auth specs + green routes.test.tsx.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-self-hosted-backend-authentik-sso/02-CONTEXT.md
@.planning/phases/02-self-hosted-backend-authentik-sso/02-RESEARCH.md
@.planning/phases/02-self-hosted-backend-authentik-sso/02-PATTERNS.md
@.planning/phases/02-self-hosted-backend-authentik-sso/02-VALIDATION.md
@.planning/phases/02-self-hosted-backend-authentik-sso/02-UI-SPEC.md
@apps/desktop/package.json
@apps/desktop/src/__tests__/routes.test.tsx
@pnpm-workspace.yaml
</context>

<interfaces>
From 02-CONTEXT.md D-32 /me JSON (contract for rbac.spec and later /me):
- sub: string
- email: string
- name: string
- groups: string[]
- permissions: string[]
- primaryRole: string

From 02-CONTEXT.md D-23 precedence keys (strip clared- prefix):
platform > owner > admin > accountant > tax > clerk > auditor > viewer

From apps/desktop/src/App.tsx:
- export function AppShell()
- export default function App()
- NAV_ITEMS labels: Rechnung, Entities, Kunden, Tax, PDF
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold Nest workspace package and RED health/auth/rbac specs</name>
  <reversibility rating="costly">NestJS in apps/backend/ is the D-04 stack lock; undoing rewrites Coolify deploy and every later API.</reversibility>
  <files>apps/backend/package.json, apps/backend/tsconfig.json, apps/backend/tsconfig.build.json, apps/backend/nest-cli.json, apps/backend/jest.config.cjs, apps/backend/src/main.ts, apps/backend/src/app.module.ts, apps/backend/src/auth/rbac.spec.ts, apps/backend/test/health.e2e-spec.ts, apps/backend/test/auth.e2e-spec.ts, apps/backend/test/jest-e2e.json</files>
  <read_first>
    - .planning/phases/02-self-hosted-backend-authentik-sso/02-RESEARCH.md (Installation block, Validation Architecture, Package Legitimacy Audit — all Approved)
    - .planning/phases/02-self-hosted-backend-authentik-sso/02-VALIDATION.md (Wave 0 Requirements, Per-Task map 02-W0-01 / 02-health / 02-401 / 02-rbac)
    - .planning/phases/02-self-hosted-backend-authentik-sso/02-CONTEXT.md (D-04, D-09, D-10, D-22 through D-25, D-32)
    - .planning/phases/02-self-hosted-backend-authentik-sso/02-PATTERNS.md (Desktop tests analog: invoice.test.tsx → rbac.spec; routes.test.tsx → health.e2e; demo-states.test.tsx → auth.e2e)
    - apps/desktop/src/__tests__/invoice.test.tsx
    - apps/desktop/src/__tests__/routes.test.tsx
    - apps/desktop/src/__tests__/demo-states.test.tsx
    - pnpm-workspace.yaml
    - apps/desktop/package.json (workspace package shape analog)
  </read_first>
  <behavior>
    - GET /health returns 200 with no Postgres I/O (D-09) — RED until 02-02
    - GET /health/ready returns 200 when Postgres answers SELECT 1, or 503 when it does not (D-09) — RED until 02-02
    - GET /me without Authorization returns 401 (D-10, BACK-01)
    - GET /api/invoices without Authorization returns 401 not 404 (D-10)
    - projectRbac(['clared-owner','clared-viewer']) unions permissions and primaryRole is owner (D-22, D-23)
    - projectRbac(['clared-platform']) includes every tenant permission plus platform.tenants.read, platform.tenants.write, platform.billing, platform.support (D-25)
    - projectRbac([]) returns permissions [] and primaryRole "" (D-32 empty groups; do not imply viewer)
    - projectRbac(['clared-accountant']) has invoice.export and not entity.create (D-24, D-25)
    - Ticket replay: second POST /auth/session with the same ticket returns 401 (D-18) — RED until 02-02
    - Ticket SET EX 60 and session SET EX 86400 as integer seconds (AUTH-01 TTL) — RED until 02-02
    - Parallel POST /auth/session with the same ticket: exactly one 200, the rest 401 (D-18 GETDEL) — RED until 02-02
    - POST /auth/logout with Bearer DELs only that session key; a second session for the same user remains (D-30) — RED until 02-02
  </behavior>
  <action>
    Create apps/backend as a private pnpm workspace package (pnpm-workspace.yaml already has apps/*; do not edit it). Per D-04 use NestJS 11.2.1 with @nestjs/platform-express 11.2.1. Pin and install exactly the RESEARCH versions: @nestjs/common@11.2.1 @nestjs/core@11.2.1 @nestjs/platform-express@11.2.1 @nestjs/config@4.0.4 @nestjs/swagger@11.4.7 @nestjs/terminus@11.1.1 @prisma/client@7.9.1 @prisma/adapter-pg@7.9.1 pg@8.23.0 openid-client@6.8.7 ioredis@5.11.1 helmet@8.3.0 class-validator@0.15.1 class-transformer@0.5.1; dev prisma@7.9.1 @nestjs/cli@11.0.24 @nestjs/testing@11.2.1 dotenv@17.4.2 plus jest ts-jest @types/jest supertest @types/supertest @types/express @types/node. Package legitimacy table treats these as Approved — no human install checkpoint. Express adapter only. Pin ioredis@5.11.1.

    package.json name "backend", private true, scripts: "test" runs jest so extra args work (`pnpm --filter ./apps/backend test -- rbac`). Add "test:e2e" using test/jest-e2e.json. jest.config.cjs: rootDir src, testRegex .spec.ts$, ts-jest. test/jest-e2e.json: testRegex .e2e-spec.ts$, rootDir ..

    Minimal AppModule + main.ts so Nest can compile; do not implement /health, /auth, /me, Prisma, or Redis yet (those are 02-02). Do not add a global prefix (routes are /health not /api/health).

    Write apps/backend/src/auth/rbac.spec.ts importing { projectRbac } from ./rbac — file rbac.ts is created in 02-02; this spec must fail to resolve or fail assertions (RED). Analog: apps/desktop/src/__tests__/invoice.test.tsx (pure transform, exact German/domain strings, no HTTP). Cases from the behavior block plus: unknown group names ignored; clared-owner is the only tenant group with entity.create; viewer has audit.read absent; precedence platform beats owner.

    Write test/health.e2e-spec.ts (supertest INestApplication): GET /health 200; GET /health/ready 200 or 503 with body indicating postgres. Analog: apps/desktop/src/__tests__/routes.test.tsx (public HTTP seam, afterEach cleanup). Write test/auth.e2e-spec.ts analog to apps/desktop/src/__tests__/demo-states.test.tsx (exact status + copy/status-code splits, not regex soup): GET /me no header 401; GET /api/invoices no header 401; POST /auth/session with a consumed ticket 401 (replay); ticket EX 60 and session EX 86400 asserted on MemoryStore/ttl helper; two parallel POSTs same ticket yield one 200 and one 401; logout DEL is this-device only. These live under test/*.e2e-spec.ts and run via scripts.test:e2e (jest-e2e.json), not scripts.test. They fail until 02-02 (RED).

    Do not create packages under packages/ for tax. Do not add invoice or entity controllers. Leave GitHub CI workflow untouched (RED backend specs must not enter CI this plan).
  </action>
  <verify>
    <automated>test -f apps/backend/package.json && node -e 'const p=require("./apps/backend/package.json"); if(!p.scripts||!p.scripts.test) process.exit(1)' && test -f apps/backend/src/auth/rbac.spec.ts && test -f apps/backend/test/health.e2e-spec.ts && test -f apps/backend/test/auth.e2e-spec.ts && (pnpm --filter ./apps/backend test -- rbac; test $? -ne 0)</automated>
  </verify>
  <acceptance_criteria>
    - apps/backend/package.json exists, private true, scripts.test is non-empty
    - apps/backend/package.json dependencies include @nestjs/core at 11.2.1 and @nestjs/platform-express at 11.2.1 and ioredis at 5.11.1
    - apps/backend/src/auth/rbac.spec.ts exists and mentions projectRbac, clared-platform, entity.create, primaryRole
    - apps/backend/test/health.e2e-spec.ts exists and mentions /health and /health/ready
    - apps/backend/test/auth.e2e-spec.ts exists and mentions /me, /api/invoices, 401, 86400, and logout
    - `pnpm --filter ./apps/backend test -- rbac` exits non-zero (RED)
    - test ! -d packages/tax-engine
  </acceptance_criteria>
  <done>Nest package exists; Wave 0 backend specs are RED; no backend job on CI yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Desktop RED auth specs plus signed-in fixture so Phase 1 routes stay green</name>
  <files>apps/desktop/src/__tests__/auth-gate.test.tsx, apps/desktop/src/__tests__/session-chip.test.tsx, apps/desktop/src/__tests__/session-banner.test.tsx, apps/desktop/src/__tests__/auth-signed-in.ts, apps/desktop/src/__tests__/routes.test.tsx</files>
  <read_first>
    - .planning/phases/02-self-hosted-backend-authentik-sso/02-UI-SPEC.md (Copywriting Contract, Surfaces, UI Considerations E11/E13/E15)
    - .planning/phases/02-self-hosted-backend-authentik-sso/02-PATTERNS.md (Desktop tests analog, signed-in fixture)
    - .planning/phases/02-self-hosted-backend-authentik-sso/02-CONTEXT.md (D-33, D-34, D-36, D-38, D-31, D-39)
    - apps/desktop/src/__tests__/routes.test.tsx
    - apps/desktop/src/__tests__/setup.ts
    - apps/desktop/src/App.tsx
    - apps/desktop/src/components/error-state.tsx
  </read_first>
  <behavior>
    - Unsigned gate: heading Clared, body Anmelden, um Rechnungen zu stellen., button Anmelden; no navigation role (D-33, D-34)
    - Chip badge map: platform Plattform, owner Inhaber, admin Admin, accountant Buchhaltung, tax Steuer, clerk Sachbearbeitung, auditor Prüfung, viewer Ansicht (D-36)
    - 401 banner text Sitzung abgelaufen. Bitte erneut anmelden. with button Anmelden; not the Phase 1 ErrorState copy (D-38)
    - Cancel banner text Anmeldung abgebrochen with role status (D-20)
    - Signed-in fixture: routes.test still expects nav [Rechnung, Entities, Kunden, Tax, PDF] and RE-2026-001
  </behavior>
  <action>
    Add apps/desktop/src/__tests__/auth-signed-in.ts exporting a helper that future SessionProvider tests will use (a MeResponse fixture: name, email, primaryRole owner, groups [clared-owner]). In this plan, wrap nothing in production App yet.

    Update routes.test.tsx only if App would otherwise break — this plan must keep `pnpm --filter ./apps/desktop test` green. Do not wrap App with a gate in this plan.

    Create three spec files wrapped in describe.skip('phase02-auth') so the desktop suite stays green:
    1. auth-gate.test.tsx — will render LoginGate (import from ../auth/login-gate); asserts h1 Clared, body copy, button Anmelden, no getByRole navigation.
    2. session-chip.test.tsx — German badge labels from UI-SPEC table; empty name falls back to email; never shows sub.
    3. session-banner.test.tsx — 401 copy + role alert; cancel copy + role status; network path still uses ErrorState copy Ein Fehler ist aufgetreten.

    Do not implement LoginGate, SessionChip, or banners here (02-04). Skip is intentional RED-held. Do not add a second test runner on desktop.
  </action>
  <verify>
    <automated>test -f apps/desktop/src/__tests__/auth-gate.test.tsx && test -f apps/desktop/src/__tests__/session-chip.test.tsx && test -f apps/desktop/src/__tests__/session-banner.test.tsx && grep -q "phase02-auth" apps/desktop/src/__tests__/auth-gate.test.tsx && pnpm --filter ./apps/desktop test</automated>
  </verify>
  <acceptance_criteria>
    - The three auth test files exist under apps/desktop/src/__tests__/
    - Each contains describe.skip('phase02-auth') or equivalent skip string phase02-auth
    - auth-gate.test.tsx contains the exact strings Clared and Anmelden, um Rechnungen zu stellen.
    - session-chip.test.tsx contains Inhaber and Buchhaltung
    - session-banner.test.tsx contains Sitzung abgelaufen. Bitte erneut anmelden. and Anmeldung abgebrochen
    - `pnpm --filter ./apps/desktop test` exits 0
    - routes.test.tsx still expects the five nav labels and RE-2026-001
  </acceptance_criteria>
  <done>Desktop Wave 0 auth specs exist (skipped); Phase 1 route tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → apps/backend | First install of Nest/Prisma/openid-client/ioredis into the workspace |
| Test process → future HTTP API | Specs describe authz; no production listener yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | pnpm installs in apps/backend | high | mitigate | Install only RESEARCH-audited versions (Approved). No community keyring plugin. Lock versions in package.json. |
| T-02-01 | Information disclosure | apps/backend/.env | medium | mitigate | Do not add a real SECRET file this plan. No .env with production values. |
| T-02-02 | Elevation of privilege | Default Nest GET / | low | accept | Wave 0 scaffold may expose default hello route until 02-02 global guard; not shipped to Coolify this plan. |
</threat_model>

<assumption_delta_decision>
noun: none yet (specs only)
decision: no-change
rationale: Wave 0 adds tests; Authentik OIDC is promoted in 02-02 when the session path exists.
</assumption_delta_decision>

<verification>
- `pnpm --filter ./apps/backend test -- rbac` is RED
- `pnpm --filter ./apps/desktop test` is GREEN
- Wave 0 files listed in 02-VALIDATION.md exist
</verification>

<success_criteria>
Wave 0 complete: backend test script exists, RED specs for health/auth/rbac, skipped desktop auth specs, Phase 1 desktop tests still pass.
</success_criteria>

## Artifacts this phase produces

This plan:
- `apps/backend` Nest package (package.json, tsconfig, nest-cli, jest)
- `projectRbac` spec only (implementation in 02-02)
- `health.e2e-spec.ts`, `auth.e2e-spec.ts`
- `auth-signed-in.ts` MeResponse fixture
- skipped `auth-gate.test.tsx`, `session-chip.test.tsx`, `session-banner.test.tsx`

<output>
Create `.planning/phases/02-self-hosted-backend-authentik-sso/02-01-SUMMARY.md` when done
</output>
