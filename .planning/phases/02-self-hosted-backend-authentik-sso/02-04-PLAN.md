---
phase: 02-self-hosted-backend-authentik-sso
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - apps/desktop/src/App.tsx
  - apps/desktop/src/auth/login-gate.tsx
  - apps/desktop/src/auth/session-provider.tsx
  - apps/desktop/src/auth/api.ts
  - apps/desktop/src/components/session-chip.tsx
  - apps/desktop/src/components/session-banner.tsx
  - apps/desktop/src/__tests__/auth-gate.test.tsx
  - apps/desktop/src/__tests__/session-chip.test.tsx
  - apps/desktop/src/__tests__/session-banner.test.tsx
  - apps/desktop/src/__tests__/routes.test.tsx
  - apps/desktop/src/__tests__/auth-signed-in.ts
  - packages/ui/src/components/badge.tsx
  - packages/ui/src/components/dropdown-menu.tsx
  - packages/ui/src/index.ts
  - apps/desktop/public/login-gate-hero.png
autonomous: true
requirements: [AUTH-01]
user_setup:
  - service: higgsfield
    why: "Gate hero is Higgsfield GPT Image 2 (D-35); CLI session required"
    dashboard_config:
      - task: "higgsfield account status / higgsfield auth login"
        location: "local machine — higgsfield CLI 1.1.23"

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Signed-out full-screen dark gate: h1 Clared, body Anmelden, um Rechnungen zu stellen., one Button Anmelden, no AppShell sidebar (D-33, D-34)"
    - "Gate hero is /login-gate-hero.png not /empty-state-hero.png; missing file still shows title, body, Anmelden (UI E11 empty)"
    - "Opening login does not replace the gate with a spinner (UI E11 loading). E12 login-window spinner is 02-03 login-init.html, not a React gate swap."
    - "401 banner: Sitzung abgelaufen. Bitte erneut anmelden. + Anmelden, role=alert, text-destructive (D-38, UI E15)"
    - "Cancel/timeout/window-close: Anmeldung abgebrochen + Anmelden, role=status, no auto-loop (D-20, UI E15)"
    - "Network/5xx uses Phase 1 ErrorState copy and Erneut versuchen — does not open the login window (D-31)"
    - "Silent boot: keychain token or valid clared:// ticket shows full-viewport Spinner + sr-only Wird geladen; no gate flash (D-37, D-18, UI E17)"
    - "prefers-reduced-motion reduce: spinner is a static 16×16 ring (UI E17 loading)"
    - "Silent boot /me 200 lands AppShell on sample invoice RE-2026-001 (Phase 1 D-22, D-37)"
    - "Silent boot /me 401 → gate with no banner unless user just cancelled (UI E17 error)"
    - "Normal cold start without clared:// → gate, no banner (UI E17)"
    - "Expired/invalid ticket after clared:// → gate + cancel banner (D-18, UI E17)"
    - "Chip in sidebar mt-auto: name from /me.name or email fallback; never sub, groups, or permissions (D-36, UI E13)"
    - "primaryRole badge German labels: Plattform Inhaber Admin Buchhaltung Steuer Sachbearbeitung Prüfung Ansicht; unknown shows raw value variant secondary (D-36, UI E16)"
    - "Mini-menu: Rolle: {label} then Abmelden; no profile/billing; Escape closes (D-36, UI E14)"
    - "Abmelden destroys this-device session and opens login window for end_session; then main shows gate (D-21, D-30)"
    - "Keyboard: gate Anmelden autofocus + Enter; chip and Abmelden reachable; no global login shortcut (D-39)"
    - "401 keeps shell, retries last request after successful login (D-31)"
    - "NAV_ITEMS order unchanged; routes.test green with signed-in fixture"
    - "Gate column max-w-xl gap-4 padding 3xl; body wraps; column scrolls if viewport shorter than hero+copy+CTA (UI E11 overflow/long-text)"
    - "Login window native titlebar only; Authentik owns content scroll (UI E12 populated)"
    - "Allowlist/network failure in WebView: native close → cancel banner (UI E12 error)"
    - "Name truncates with title tooltip; badge stays Label 12px; chip pinned footer (UI E13 overflow/long-text)"
    - "Menu always exactly Rolle row + Abmelden; unknown primaryRole wraps in Rolle row (UI E14)"
    - "Banner Anmelden may show Spinner inside the button while the window opens; message wraps; never overlays sidebar (UI E15)"
    - "Paid SaaS copy never uses OSS/free/self-host language (D-01, D-34)"
  artifacts:
    - "apps/desktop/src/auth/login-gate.tsx"
    - "apps/desktop/src/auth/session-provider.tsx"
    - "apps/desktop/src/components/session-chip.tsx"
    - "apps/desktop/src/components/session-banner.tsx"
    - "packages/ui badge + dropdown-menu barrel exports"
    - "apps/desktop/public/login-gate-hero.png"
  key_links:
    - "SessionProvider wraps App: unsigned LoginGate; boot Spinner; signed RouterProvider (gate is not a hash route)"
    - "Anmelden invokes open_login_window; ticket-received → redeemTicket → keychain_set_session → fetchMe"
    - "apiFetch 401 → banner + login window + replay; network → ErrorState"
    - "E12 login spinner is apps/desktop/src-tauri/login-init.html from 02-03; SessionProvider/LoginGate stay on screen while that window loads"
  prohibitions:
    - statement: "MUST NOT show product screens (invoice mock, sidebar) while signed out"
      status: flagged-unverified
    - statement: "MUST NOT ship OSS, kostenlos, free, or self-host copy"
      status: flagged-unverified
    - statement: "MUST NOT add profile, billing, Avatar, theme toggle, toasts, or Überall abmelden"
      status: flagged-unverified
    - statement: "MUST NOT add a Button CVA variant API this phase"
      status: flagged-unverified
    - statement: "MUST NOT reuse empty-state-hero.png as the gate hero"
      status: flagged-unverified
    - statement: "MUST NOT use English Logout or a light theme"
      status: flagged-unverified
  flagged_assumptions: []
---

<objective>
Signed-out gate, silent boot, session chip, 401/cancel banners, Higgsfield hero — implement 02-UI-SPEC against the 02-02 API and 02-03 Tauri commands. Unskip Wave 0 desktop auth tests and keep Phase 1 routes green.

Purpose: User-visible AUTH-01. Unsigned users cannot see the product; signed-in users see identity + logout.
Output: Gate + provider + chip + banners + login-gate-hero.png; desktop tests green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-self-hosted-backend-authentik-sso/02-UI-SPEC.md
@.planning/phases/02-self-hosted-backend-authentik-sso/02-CONTEXT.md
@.planning/phases/02-self-hosted-backend-authentik-sso/02-PATTERNS.md
@.planning/phases/02-self-hosted-backend-authentik-sso/02-RESEARCH.md
@apps/desktop/src/App.tsx
@apps/desktop/src/auth/api.ts
@apps/desktop/src/components/error-state.tsx
@apps/desktop/src/components/spinner.tsx
@apps/desktop/src/__tests__/routes.test.tsx
@packages/ui/src/index.ts
@packages/ui/components.json
</context>

<interfaces>
From 02-02: redeemTicket, fetchMe, logoutSession, apiFetch, MeResponse
From 02-03 invoke: open_login_window, keychain_set_session, keychain_get_session, keychain_delete_session
Event: ticket-received
From App.tsx: AppShell, createHashRouter, NAV_ITEMS — do not reorder
From @clared/ui Button: no variant prop; override className per UI-SPEC tables
German badge map (locked):
platform Plattform, owner Inhaber, admin Admin, accountant Buchhaltung, tax Steuer, clerk Sachbearbeitung, auditor Prüfung, viewer Ansicht
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LoginGate + SessionProvider wrap (boot, gate, signed-in shell)</name>
  <files>apps/desktop/src/auth/login-gate.tsx, apps/desktop/src/auth/session-provider.tsx, apps/desktop/src/App.tsx, apps/desktop/src/auth/api.ts, apps/desktop/src/__tests__/auth-gate.test.tsx, apps/desktop/src/__tests__/routes.test.tsx, apps/desktop/src/__tests__/auth-signed-in.ts</files>
  <read_first>
    - .planning/phases/02-self-hosted-backend-authentik-sso/02-UI-SPEC.md (Signed-out gate, Silent boot, Copywriting, Color, Typography, UI Considerations E11 E12 E17)
    - .planning/phases/02-self-hosted-backend-authentik-sso/02-PATTERNS.md (Gate wraps hash router, invoice-empty-state analog, spinner)
    - apps/desktop/src/App.tsx
    - apps/desktop/src/auth/api.ts
    - apps/desktop/src/components/spinner.tsx
    - apps/desktop/src/components/invoice-empty-state.tsx
    - apps/desktop/src/__tests__/auth-gate.test.tsx
    - apps/desktop/src/__tests__/routes.test.tsx
  </read_first>
  <behavior>
    - Unsigned: LoginGate heading Clared, body Anmelden, um Rechnungen zu stellen., button Anmelden autofocus; no navigation
    - Anmelden invokes open_login_window; Enter activates the button
    - Boot with keychain token: Spinner full viewport sr-only Wird geladen; /me 200 shows shell on RE-2026-001
    - /me 401 → gate; network → ErrorState with Erneut versuchen retrying /me
    - routes.test with signed-in mock still lists the five nav labels and RE-2026-001
  </behavior>
  <action>
    LoginGate: full viewport bg-background, centered column max-w-xl gap-4 py-16 (3xl 64px), img src /login-gate-hero.png alt="" w-full max-w-xl rounded-md, h1 Display classes text-[28px] font-semibold leading-[1.2] Clared, p muted body copy, @clared/ui Button min-h-11 font-semibold Anmelden autoFocus. Do not render AppShell. Do not add a second CTA.

    SessionProvider outside the router. States: boot | unsigned | signed | boot-error. Cold start: invoke keychain_get_session; if token, silent fetchMe; listen plugin-deep-link getCurrent and onOpenUrl plus ticket-received; if ticket valid, redeemTicket with hostname from plugin-os, keychain_set_session, fetchMe. Prefer viewer primaryRole only when that group exists — empty groups already handled on server.

    App default export: SessionProvider wrapping current App. Unsigned → LoginGate. Boot → centered Spinner (reuse spinner.tsx; reduced-motion: drop animate-spin). Signed → existing createHashRouter / AppShell. Insert nothing into NAV_ITEMS. Opening Anmelden must keep LoginGate visible; the E12 spinner is the 02-03 login WebView (login-init.html), not a gate replacement.

    Wire apiFetch 401 callback for task 2; keep a lastRequest slot.

    Remove describe.skip('phase02-auth') from auth-gate.test.tsx. Provide signed-in fixture so routes.test does not see the gate (mock SessionProvider or mock keychain + /me 200). Keep afterEach cleanup and hash #/.
  </action>
  <verify>
    <automated>pnpm --filter ./apps/desktop test -- auth-gate && pnpm --filter ./apps/desktop test -- routes</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./apps/desktop test -- auth-gate` exits 0
    - `pnpm --filter ./apps/desktop test -- routes` exits 0
    - login-gate.tsx contains Clared, Anmelden, um Rechnungen zu stellen., /login-gate-hero.png, and Anmelden
    - login-gate.tsx does not import AppShell
    - session-provider.tsx uses keychain_get_session and fetchMe
    - App.tsx still uses createHashRouter and the five NAV_ITEMS labels in the same order
  </acceptance_criteria>
  <done>Unsigned users see only the gate; signed-in tests still land on the sample invoice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Session chip, mini-menu, 401/cancel banners, 401 retry</name>
  <files>apps/desktop/src/components/session-chip.tsx, apps/desktop/src/components/session-banner.tsx, apps/desktop/src/App.tsx, packages/ui/src/components/badge.tsx, packages/ui/src/components/dropdown-menu.tsx, packages/ui/src/index.ts, apps/desktop/src/auth/api.ts, apps/desktop/src/auth/session-provider.tsx, apps/desktop/src/__tests__/session-chip.test.tsx, apps/desktop/src/__tests__/session-banner.test.tsx</files>
  <read_first>
    - .planning/phases/02-self-hosted-backend-authentik-sso/02-UI-SPEC.md (chip, mini-menu, banners, Button className table, UI Considerations E13 E14 E15 E16)
    - .planning/phases/02-self-hosted-backend-authentik-sso/02-PATTERNS.md (badge/dropdown shadcn add, error-state analog)
    - packages/ui/components.json
    - packages/ui/src/index.ts
    - packages/ui/src/components/button.tsx
    - apps/desktop/src/components/error-state.tsx
    - apps/desktop/src/App.tsx
  </read_first>
  <behavior>
    - Chip shows name then German primaryRole badge; empty name uses email
    - Menu: Rolle: {German label} separator Abmelden; Abmelden calls logoutSession, keychain_delete_session, open_login_window for endSessionUrl
    - 401 banner exact copy + role alert; cancel banner exact copy + role status
    - Network errors still render ErrorState, not the 401 banner
  </behavior>
  <action>
    From packages/ui (not apps/desktop) add shadcn official registry components badge and dropdown-menu (preset radix-nova already in components.json). Export Badge and dropdown primitives from packages/ui/src/index.ts like Button. Do not add Avatar. Do not introduce Button variants.

    SessionChip in sidebar nav as last child with mt-auto. Trigger: Button className w-full min-h-11 bg-card text-foreground font-normal hover:bg-muted. Badge variant secondary Label 12px. DropdownMenuContent side top align start. Item Abmelden default, not destructive. Accessible name = display name. aria-haspopup menu.

    SessionBanner: Card + Button min-h-11 font-semibold. Sticky top of main when shell visible; top of gate column when unsigned. Never overlay nav. 401: role alert, destructive message. Cancel: role status, muted message. Button Anmelden opens login window. Optional Spinner inside the button while opening.

    D-31: 401 keeps shell, banner, open_login_window, replay last apiFetch after new token. Network: ErrorState only.

    Unskip session-chip and session-banner tests (remove phase02-auth skip).
  </action>
  <verify>
    <automated>pnpm --filter ./apps/desktop test -- session-chip && pnpm --filter ./apps/desktop test -- session-banner && pnpm --filter ./apps/desktop test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./apps/desktop test` exits 0
    - packages/ui/src/index.ts exports Badge
    - session-chip.tsx contains Inhaber or the badge map object with owner: Inhaber
    - session-chip.tsx contains Abmelden and Rolle:
    - session-banner.tsx contains Sitzung abgelaufen. Bitte erneut anmelden. and Anmeldung abgebrochen
    - AppShell nav still has five links plus the chip footer
    - error-state.tsx copy is unchanged
  </acceptance_criteria>
  <done>Chip and banners match UI-SPEC; full desktop suite green including Phase 1 tests.</done>
</task>

<task type="auto">
  <name>Task 3: Higgsfield gate hero PNG</name>
  <precondition>higgsfield account status reports a signed-in session. If not, halt — UI-SPEC forbids shipping a 1×1 placeholder for the gate hero.</precondition>
  <files>apps/desktop/public/login-gate-hero.png</files>
  <read_first>
    - /Users/puzzless/.claude/skills/higgsfield-generate/SKILL.md
    - .planning/phases/02-self-hosted-backend-authentik-sso/02-UI-SPEC.md (Higgsfield section, locked prompt)
    - apps/desktop/src/components/invoice-empty-state.tsx (path analog only)
    - apps/desktop/src/auth/login-gate.tsx
  </read_first>
  <action>
    Follow higgsfield-generate. Check higgsfield account status. Generate one image with gpt_image_2 --aspect_ratio 16:9 --resolution 2k --quality high --wait (D-35). Prompt exactly: Dark navy B2B login-gate illustration: a sealed invoice folio and a geometric keyhole glow on deep navy, clean shapes, subtle cool light, professional calm dense, no people, no letters, no logo, no UI chrome. Save to apps/desktop/public/login-gate-hero.png targeting 2688×1520. Do not copy the invoice empty-state PNG. Do not use Cursor image generation. Decorative alt stays empty on the img tag (already in LoginGate).
  </action>
  <verify>
    <automated>test -f apps/desktop/public/login-gate-hero.png && file apps/desktop/public/login-gate-hero.png | grep -qi png && python3 -c "import os; assert os.path.getsize('apps/desktop/public/login-gate-hero.png')>10000"</automated>
  </verify>
  <acceptance_criteria>
    - apps/desktop/public/login-gate-hero.png exists and is a PNG larger than 10 KB
    - login-gate.tsx references /login-gate-hero.png
    - The gate hero file is not a byte-identical copy of apps/desktop/public/empty-state-hero.png (`cmp` exits non-zero)
  </acceptance_criteria>
  <done>Gate hero is a real Higgsfield asset at the consumed path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → Tauri commands | Session token only through keychain IPC on main |
| User → gate CTA | Opens login WebView; no secret in JS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-16 | Information disclosure | Session token in JS memory | medium | mitigate | Persist only via keychain commands; do not write to localStorage or document.cookie |
| T-02-17 | Spoofing | 401 vs network confusion | medium | mitigate | Network uses ErrorState; 401 uses session banner + login window (D-31) |
| T-02-18 | Elevation of privilege | Unsigned product UI | high | mitigate | Gate replaces AppShell; no invoice mock unsigned (D-33) |
| T-02-19 | Information disclosure | Chip leaking groups/permissions | low | mitigate | Chip shows name + primaryRole only (D-36) |
</threat_model>

<assumption_delta_decision>
noun: Authentik OIDC session
decision: no-change
rationale: UI consumes the promoted session; no second identity provider.
</assumption_delta_decision>

<verification>
pnpm --filter ./apps/desktop test
file login-gate-hero.png is PNG > 10KB
<human-check>
Launch Tauri: signed-out gate (dark, one Anmelden, no sidebar). Click Anmelden: second window title Anmelden 480×640. Cancel: banner Anmeldung abgebrochen. After mock/live login: chip + sample invoice. Keyboard: Tab/Enter on gate, chip menu Abmelden.
</human-check>
</verification>

<success_criteria>
UI-SPEC gate/chip/banners/boot hold; desktop tests green; hero PNG present.
</success_criteria>

## Artifacts this phase produces

This plan:
- `LoginGate`
- `SessionProvider`
- `SessionChip`
- `SessionBanner`
- `Badge`, `DropdownMenu*` from @clared/ui
- `apps/desktop/public/login-gate-hero.png`

<output>
Create `.planning/phases/02-self-hosted-backend-authentik-sso/02-04-SUMMARY.md` when done
</output>
