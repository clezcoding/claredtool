---
phase: 02-self-hosted-backend-authentik-sso
plan: 06
type: execute
wave: 5
depends_on: ["02-03"]
files_modified:
  - apps/desktop/src-tauri/Cargo.toml
  - apps/desktop/src-tauri/Cargo.lock
autonomous: true
requirements: [AUTH-01]
gap_closure: true
gap_ids: [G-02-1]
user_setup: []

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Anmelden opens a second native window titled Anmelden at 480×640 (D-19, D-33, G-02-1)"
    - "Login WebView still starts from login-init.html bundled in src-tauri (02-03 include_str path); Tauri 2 accepts that document (D-16)"
    - "Cancel/close of that window remains reachable so Anmeldung abgebrochen can show (D-31, G-02-1)"
  artifacts:
    - "apps/desktop/src-tauri/Cargo.toml (tauri feature webview-data-url enabled)"
  key_links:
    - "open_login_window → WebviewUrl::External(login_init_url) → login WebView paints → navigate GET /auth/login (D-16)"
  prohibitions:
    - statement: "MUST NOT move login-init.html into Vite public/ (02-03: src-tauri HTML must not depend on frontend dist)"
      status: flagged-unverified
    - statement: "MUST NOT add keychain IPC to the login capability"
      status: flagged-unverified
  flagged_assumptions:
    - "[G-02-1] Enabling the Tauri 2 webview-data-url feature is sufficient; login_init_url() stays a data:text/html URL. Asset protocol is the fallback only if cargo check or a local Anmelden click still rejects data URLs after the feature is on."
---

<objective>
Close G-02-1: Tauri 2 rejects the login-init data URL, so Anmelden never opens the 480×640 window. Enable the crate feature Tauri requires for that URL.

Purpose: AUTH-01 login window (D-16, D-19, D-33) is unreachable until the WebView can load login-init.html.
Output: `webview-data-url` on `tauri` in Cargo.toml; cargo check green; Anmelden can create the login window.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-self-hosted-backend-authentik-sso/02-UAT.md
@.planning/phases/02-self-hosted-backend-authentik-sso/02-03-SUMMARY.md
@apps/desktop/src-tauri/Cargo.toml
@apps/desktop/src-tauri/src/lib.rs
</context>

<interfaces>
Login window: label login, title Anmelden, 480×640, native decorations
Init document: apps/desktop/src-tauri/login-init.html via login_init_url() data:text/html
Then: GET {BACKEND_URL}/auth/login (D-16)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Enable Tauri 2 webview-data-url on the desktop crate</name>
  <files>apps/desktop/src-tauri/Cargo.toml, apps/desktop/src-tauri/Cargo.lock</files>
  <read_first>
    - apps/desktop/src-tauri/Cargo.toml
    - apps/desktop/src-tauri/src/lib.rs (login_init_url, open_login_window)
    - .planning/phases/02-self-hosted-backend-authentik-sso/02-UAT.md (G-02-1)
    - .planning/phases/02-self-hosted-backend-authentik-sso/02-03-SUMMARY.md (include_str data URL decision)
  </read_first>
  <action>
    G-02-1: Vite reports invalid window url because data URLs need the Tauri 2 crate feature webview-data-url, and Cargo.toml currently has tauri features empty.

    On the tauri dependency in apps/desktop/src-tauri/Cargo.toml, set features to the single feature webview-data-url. Do not add unrelated tauri features. Do not change tauri-build features. Run cargo check from apps/desktop/src-tauri so Cargo.lock records the feature. Keep login_init_url() and WebviewUrl::External(login_init_url()) as they are (per D-16 / 02-03: login-init.html stays include_str in src-tauri, not Vite public/). Only if cargo check or a subsequent Anmelden click still rejects data URLs after this feature is on, load login-init.html through Tauri custom-protocol/asset instead — that is the UAT fallback, not the first move.
  </action>
  <verify>
    <automated>grep -q 'webview-data-url' apps/desktop/src-tauri/Cargo.toml && (cd apps/desktop/src-tauri && cargo check)</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml tauri dependency lists webview-data-url
    - cargo check in apps/desktop/src-tauri exits 0
    - lib.rs still contains login_init_url and WebviewUrl::External(login_init_url())
  </acceptance_criteria>
  <done>Tauri crate accepts the login-init data URL; cargo check is green (G-02-1, D-16, D-19).</done>
</task>

<task type="auto">
  <name>Task 2: Keep desktop auth suite green after the crate feature</name>
  <files>apps/desktop/src-tauri/Cargo.toml</files>
  <read_first>
    - apps/desktop/package.json
    - apps/desktop/src-tauri/src/lib.rs (allow_navigation still allows data for the init document)
  </read_first>
  <action>
    Re-run the desktop Vitest suite so the Cargo.toml feature does not regress gate/chip/banner specs (AUTH-01 UI from 02-04). Do not widen login.json capabilities. Do not register keychain commands on the login window. allow_navigation may keep allowing the data scheme for the init document only; do not loosen http/https to hosts other than BACKEND_URL and AUTHENTIK_URL (CR-02).
  </action>
  <verify>
    <automated>pnpm --filter ./apps/desktop test</automated>
    <human-check>
      Signed-out Tauri: click Anmelden. Second window titled Anmelden, 480×640, no Vite data-URL error. Close it: banner Anmeldung abgebrochen (G-02-1, D-31, D-33, D-39).
    </human-check>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./apps/desktop test` exits 0
    - Cargo.toml still lists webview-data-url
  </acceptance_criteria>
  <done>Desktop tests green; Anmelden window is unblocked for UAT retest (G-02-1).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main window → login WebView | Anmelden creates a second WebView that will load Authentik (D-16) |
| data: document → later https navigation | Init HTML is local; next URL must be BACKEND_URL / AUTHENTIK_URL hosts only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-25 | Spoofing | login WebView data URL | medium | mitigate | Enable only webview-data-url; keep on_navigation host allowlist and login.json without keychain (T-02-10 from 02-03) |
| T-02-26 | Elevation of privilege | login capability | high | mitigate | Do not add keychain or os:allow-hostname to login.json |
| T-02-SC | Tampering | no new crates | low | accept | Feature flag on existing tauri 2; no new package name |
</threat_model>

<verification>
grep webview-data-url in Cargo.toml
cargo check in apps/desktop/src-tauri
pnpm --filter ./apps/desktop test
<human-check>
Anmelden opens Anmelden 480×640; closing it shows Anmeldung abgebrochen.
</human-check>
</verification>

<success_criteria>
G-02-1 closed in code: Tauri 2 can load the login-init data URL. Human UAT confirms the second window.
</success_criteria>

## Artifacts this phase produces

This plan:
- tauri feature webview-data-url in apps/desktop/src-tauri/Cargo.toml

<output>
Create `.planning/phases/02-self-hosted-backend-authentik-sso/02-06-SUMMARY.md` when done
</output>
