---
phase: 02-self-hosted-backend-authentik-sso
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/backend/src/main.ts
  - apps/backend/src/app.module.ts
  - apps/backend/src/prisma/prisma.service.ts
  - apps/backend/src/redis/redis.service.ts
  - apps/backend/src/redis/memory-store.ts
  - apps/backend/src/health/health.controller.ts
  - apps/backend/src/auth/public.decorator.ts
  - apps/backend/src/auth/auth.guard.ts
  - apps/backend/src/auth/auth.controller.ts
  - apps/backend/src/auth/oidc.ts
  - apps/backend/src/auth/rbac.ts
  - apps/backend/src/auth/rbac.spec.ts
  - apps/backend/src/me/me.controller.ts
  - apps/backend/prisma/schema.prisma
  - apps/backend/prisma.config.ts
  - apps/backend/prisma/migrations
  - apps/backend/Dockerfile
  - apps/backend/.env.example
  - apps/backend/test/health.e2e-spec.ts
  - apps/backend/test/auth.e2e-spec.ts
  - apps/desktop/src/auth/api.ts
  - apps/desktop/src/auth/types.ts
  - compose.clared.yml
  - .github/workflows/ci.yml
autonomous: true
requirements: [BACK-01, AUTH-01]
user_setup:
  - service: orbstack-postgres-redis
    why: "D-03 local Postgres + Redis for /health/ready 200 and SCHEMA_PUSH; Nest stays native pnpm"
    dashboard_config:
      - task: "OrbStack running (docker compose -f compose.clared.yml up -d)"
        location: "local machine — OrbStack 2.x"

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "GET /health returns 200 without touching Postgres (BACK-01, D-09)"
    - "GET /health/ready runs SELECT 1 via PrismaHealthIndicator; 200 if Postgres up, 503 if down (BACK-01, D-09)"
    - "Unauthenticated GET /me and GET /api/invoices return 401 (BACK-01, D-10)"
    - "POST /auth/session with a valid one-time ticket returns 200 { token }; the same ticket a second time returns 401 (AUTH-01, D-18)"
    - "Bearer session lives in Redis EX 86400 seconds absolute from login; no refresh grant (D-26, D-28, D-29)"
    - "GET /me with a valid Bearer returns sub, email, name, groups[], permissions[], primaryRole and no plan/subscription fields (D-32)"
    - "Ticket TTL is 60 integer seconds; session TTL is 86400 integer seconds; no sub-second rounding (AUTH-01 precision)"
    - "At ticket t=0 (key present) redeem succeeds; missing/expired key returns 401 (AUTH-01 boundary, D-18)"
    - "Parallel POST /auth/session with the same ticket: exactly one 200, the rest 401 via atomic GETDEL (AUTH-01 concurrency, D-18)"
    - "Many sessions per user, no cap; logout DEL this device key only (D-30)"
    - "Nest is the confidential OAuth2 client; SECRET is only an env var on the server (D-16)"
    - "Prisma 7 provider prisma-client with required output and PrismaPg adapter; no User or Role models (D-06, D-22)"
    - "Image CMD runs prisma migrate deploy then node dist/main.js (D-07, D-08)"
    - "Desktop typed fetch client posts the ticket and sends Authorization Bearer (BACK-01)"
    - "Empty Authentik groups yield permissions [] and primaryRole empty string (D-22 discretion)"
  artifacts:
    - "apps/backend/src/health/health.controller.ts"
    - "apps/backend/src/auth/auth.controller.ts"
    - "apps/backend/src/auth/auth.guard.ts"
    - "apps/backend/src/auth/rbac.ts (projectRbac)"
    - "apps/backend/src/me/me.controller.ts"
    - "apps/backend/prisma/schema.prisma"
    - "apps/backend/Dockerfile"
    - "compose.clared.yml"
    - "apps/desktop/src/auth/api.ts"
    - "apps/desktop/src/auth/types.ts"
  key_links:
    - "Global AuthGuard reads Authorization Bearer from Redis session:{token}; @Public() on /health, /health/ready, GET /auth/login, GET /auth/callback, POST /auth/session"
    - "Catch-all @All('*') controller is NOT public so unmatched paths 401 (D-10)"
    - "Ticket GETDEL then SET session:{token} EX 86400 with { sub, email, name, groups, permissions, primaryRole, iat, hostname }"
    - "apps/desktop/src/auth/api.ts redeemTicket → POST /auth/session; fetchMe → GET /me"
    - "Dockerfile build context is repo root; prisma generate before nest build (D-08)"
  prohibitions:
    - statement: "MUST NOT put a JWT in the desktop client or store the session token in WebView storage"
      status: flagged-unverified
    - statement: "MUST NOT add a second role table in Postgres this phase"
      status: flagged-unverified
    - statement: "MUST NOT run migrate dev inside the production image"
      status: flagged-unverified
    - statement: "MUST NOT use Nixpacks for Coolify"
      status: flagged-unverified
    - statement: "MUST NOT add invoice or entity HTTP handlers"
      status: flagged-unverified
  flagged_assumptions:
    - "[EDGE unresolved · BACK-01 unclassified] Vendor Coolify FQDNs are not in-repo; tracer uses OrbStack local URLs from .env.example. Prod hostnames stay Coolify env (RESEARCH Open Question 1)."
    - "[A2] Coolify Dockerfile pack runs image CMD as-is; migrate && node is in Dockerfile not an undocumented UI start field."
---

<objective>
Production-quality tracer: one login ticket redeems into a Redis Bearer and GET /me succeeds. Wire Nest Express, Prisma 7 (empty schema), Redis, global 401 guard, confidential /auth/*, desktop typed fetch, Clared compose Postgres+Redis, and Dockerfile. Then fill the full RBAC catalog and push the schema.

Purpose: Prove BACK-01 + AUTH-01 architecture end-to-end before Tauri window chrome and live Authentik.
Output: Green health/auth/rbac tests; migrate-deployable schema; Coolify Dockerfile; desktop api.ts.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-self-hosted-backend-authentik-sso/02-CONTEXT.md
@.planning/phases/02-self-hosted-backend-authentik-sso/02-RESEARCH.md
@.planning/phases/02-self-hosted-backend-authentik-sso/02-PATTERNS.md
@.planning/phases/02-self-hosted-backend-authentik-sso/02-VALIDATION.md
@apps/backend/package.json
@apps/backend/test/health.e2e-spec.ts
@apps/backend/test/auth.e2e-spec.ts
@apps/backend/src/auth/rbac.spec.ts
</context>

<interfaces>
MeResponse (D-32) — export from apps/desktop/src/auth/types.ts AND apps/backend (shared shape, no shared package per D-05):
- sub: string
- email: string
- name: string
- groups: string[]
- permissions: string[]
- primaryRole: string

export function projectRbac(groups: string[]): { permissions: string[]; primaryRole: string }

KeyValueStore (apps/backend/src/redis/memory-store.ts for tests; RedisService wraps ioredis in prod):
- set(key, val, 'EX', seconds, 'NX'?): Promise
- get(key): Promise string | null
- del(key): Promise number
- getdel(key): Promise string | null  // atomic

Redis keys (D-18, D-28, D-29):
- oauth:{state} EX 600 → { code_verifier }
- ticket:{id} EX 60 NX → { sub, email, name, groups }
- session:{token} EX 86400 → MeResponse + iat + hostname

POST /auth/session body: { ticket: string, hostname: string }
POST /auth/session 200: { token: string }
POST /auth/logout 200: { endSessionUrl: string } (stub URL until 02-05)

Desktop apps/desktop/src/auth/api.ts:
- redeemTicket(ticket: string, hostname: string): Promise<{ token: string }>
- fetchMe(token: string): Promise<MeResponse>
- logoutSession(token: string): Promise<{ endSessionUrl: string }>
- apiFetch(path: string, init?: RequestInit): Promise<Response>

CORS_ORIGINS must include tauri://localhost, https://tauri.localhost, http://localhost:5174
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end "ticket redeem → Bearer → /me" — one path only</name>
  <reversibility rating="costly">Opaque Redis Bearer (D-26) and Prisma 7 (D-06) are the session and ORM contracts later phases inherit.</reversibility>
  <precondition>02-01 Nest package and RED specs exist (apps/backend/package.json scripts.test, test/auth.e2e-spec.ts).</precondition>
  <files>apps/backend/src/main.ts, apps/backend/src/app.module.ts, apps/backend/src/prisma/prisma.service.ts, apps/backend/src/redis/redis.service.ts, apps/backend/src/redis/memory-store.ts, apps/backend/src/health/health.controller.ts, apps/backend/src/auth/public.decorator.ts, apps/backend/src/auth/auth.guard.ts, apps/backend/src/auth/auth.controller.ts, apps/backend/src/auth/oidc.ts, apps/backend/src/auth/rbac.ts, apps/backend/src/me/me.controller.ts, apps/backend/prisma/schema.prisma, apps/backend/prisma.config.ts, apps/backend/Dockerfile, apps/backend/.env.example, compose.clared.yml, apps/desktop/src/auth/api.ts, apps/desktop/src/auth/types.ts, apps/backend/test/health.e2e-spec.ts, apps/backend/test/auth.e2e-spec.ts</files>
  <read_first>
    - .planning/phases/02-self-hosted-backend-authentik-sso/02-RESEARCH.md (Patterns 1–3, Pitfalls 1/2/5/8, Swagger/Terminus/CORS snippets, Redis key table)
    - .planning/phases/02-self-hosted-backend-authentik-sso/02-CONTEXT.md (D-04 through D-10, D-16, D-18, D-22, D-26 through D-32)
    - .planning/phases/02-self-hosted-backend-authentik-sso/02-PATTERNS.md (Nest main.ts, Prisma 7, auth/*, Dockerfile, compose.clared.yml)
    - apps/backend/test/health.e2e-spec.ts
    - apps/backend/test/auth.e2e-spec.ts
    - apps/desktop/vite.config.ts (port 5174)
  </read_first>
  <behavior>
    - GET /health 200 without Prisma I/O
    - GET /health/ready 200 with Postgres, 503 without
    - GET /me missing Bearer → 401
    - GET /api/invoices missing Bearer → 401
    - Seed ticket in MemoryStore; POST /auth/session { ticket, hostname } → 200 { token }; GET /me with Authorization Bearer that token → 200 MeResponse
    - Second POST /auth/session same ticket → 401
    - Ticket SET EX is the integer 60; session SET EX is the integer 86400 (no sub-second rounding)
    - At t=0 a present ticket redeems 200; missing or expired ticket → 401
    - Two parallel POST /auth/session with the same ticket: exactly one 200, the other 401 (atomic GETDEL)
    - POST /auth/logout with Bearer DELs only that session:{token}; a second session for the same sub still GET /me 200
    - redeemTicket in api.ts POSTs {BACKEND_URL}/auth/session and fetchMe GETs /me with Bearer
  </behavior>
  <action>
    Implement the tracer through every layer this phase will keep: Nest HTTP, Prisma ping, Redis ticket/session, desktop fetch. Live Authentik is stubbed behind oidc.ts (02-05 fills discovery); that is a fillable stub, not an architecture change.

    Prisma 7 per D-06/RESEARCH Pattern 3: schema.prisma datasource postgresql, generator provider prisma-client, output ../src/generated/prisma, moduleFormat cjs. URL only in prisma.config.ts (dotenv + defineConfig). No User or Role models (D-22). PrismaService uses PrismaPg({ connectionString: process.env.DATABASE_URL }).

    compose.clared.yml per D-03/D-12: Postgres 16-alpine db name clared user clared_app, Redis 7-alpine; no Authentik here. .env.example local: DATABASE_URL postgresql://clared_app:clared_app_dev@127.0.0.1:5432/clared, REDIS_URL redis://127.0.0.1:6379, AUTHENTIK_URL http://localhost:9000, CLIENT_ID clared, SECRET local-only, BACKEND_URL http://localhost:3000, CORS_ORIGINS the three Tauri origins. Laptop never documents a production SECRET (D-13).

    RedisService: ioredis 5.11.1 in prod; MemoryStore Map with atomic getdel for tests. Keys: oauth:{state} EX 600, ticket:{id} EX 60 NX, session:{token} EX 86400. Ticket id and Bearer from crypto.randomBytes(32).toString('base64url'). Do not keep Authentik access tokens after callback.

    Health per D-09: GET /health @Public() 200 no DB. GET /health/ready @Public() Terminus PrismaHealthIndicator.pingCheck('postgres', prisma).

    Auth per D-16/D-18: GET /auth/login @Public() stores PKCE verifier under oauth:{state}, 302 to oidc.buildAuthorizationUrl (stub may 302 to /auth/callback?code=&state= when AUTH_TEST_MODE=1 for e2e). GET /auth/callback @Public() oidc.authorizationCodeGrant (mockable), projectRbac on groups, SET ticket EX 60 NX, 302 Location clared://auth?ticket={id}. POST /auth/session @Public() ValidationPipe whitelist DTO { ticket, hostname }; GETDEL ticket; SET session EX 86400; 200 { token }. POST /auth/logout (guarded) DEL this session key; 200 { endSessionUrl } stub string until 02-05.

    Global AuthGuard + @Public(). Unmatched routes: a catch-all handler that is not public so GET /api/invoices is 401 (D-10). GET /me guarded, returns D-32 fields only.

    main.ts: helmet(), enableCors origin from CORS_ORIGINS split, ValidationPipe transform+whitelist, Swagger DocumentBuilder title Clared version 0.1.0 addBearerAuth, setup api/docs with jsonDocumentUrl openapi.json (D-05). No global prefix. Express adapter only.

    Dockerfile in apps/backend/, build context repo root, pnpm install → prisma generate → nest build, CMD prisma migrate deploy && node dist/main.js (D-07, D-08). Not Nixpacks.

    Desktop: apps/desktop/src/auth/types.ts MeResponse. api.ts uses import.meta.env.VITE_BACKEND_URL ?? http://localhost:3000; redeemTicket, fetchMe, logoutSession, apiFetch with Authorization Bearer. No token in localStorage. 401 retry hook can be a callback placeholder for 02-04.

    e2e uses MemoryStore + mocked oidc grant (TTL integers 60/86400 on the store, GETDEL for parallel redeem, logout DEL this key only). Turn 02-01 health and auth specs GREEN via scripts.test:e2e (test/*.e2e-spec.ts). rbac.spec.ts stays under scripts.test (jest rootDir src). Add CI steps: pnpm --filter ./apps/backend test:e2e and pnpm --filter ./apps/backend test -- rbac, plus tsc if present; keep Node 26; do not merge into desktop-build.yml.

    Executor-note: nest files are one vertical slice, not 20 independent features. Implement the single redeem path first, then the 401 catch-all.
  </action>
  <verify>
    <automated>pnpm --filter ./apps/backend test:e2e -- health && pnpm --filter ./apps/backend test:e2e -- auth && pnpm --filter ./apps/desktop exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./apps/backend test:e2e -- health` exits 0
    - `pnpm --filter ./apps/backend test:e2e -- auth` exits 0
    - GET /health handler does not call prisma (source: health.controller.ts has a /health method with no prisma identifier)
    - auth.guard.ts reads the Authorization header and looks up session:{token} in Redis
    - me.controller.ts GET /me returns JSON keys sub, email, name, groups, permissions, primaryRole
    - Ticket redeem uses getdel (or GET then DEL in one critical section) in redis.service.ts or auth.controller.ts
    - schema.prisma generator provider is prisma-client and contains no model User
    - Dockerfile CMD contains prisma migrate deploy
    - apps/desktop/src/auth/api.ts exports redeemTicket and fetchMe
    - compose.clared.yml defines a postgres service and a redis service
    - .github/workflows/ci.yml runs pnpm --filter ./apps/backend test:e2e and pnpm --filter ./apps/backend test -- rbac
  </acceptance_criteria>
  <done>One path works: ticket → Bearer in Redis → GET /me 200; unauthenticated /me and /api/invoices are 401; /health is 200.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full RBAC catalog union and primaryRole precedence</name>
  <files>apps/backend/src/auth/rbac.ts, apps/backend/src/auth/rbac.spec.ts</files>
  <read_first>
    - .planning/phases/02-self-hosted-backend-authentik-sso/02-CONTEXT.md (D-22, D-23, D-24, D-25 tables)
    - .planning/phases/02-self-hosted-backend-authentik-sso/02-RESEARCH.md (RBAC pure function notes)
    - apps/backend/src/auth/rbac.spec.ts
    - apps/backend/src/auth/rbac.ts
  </read_first>
  <behavior>
    - Precedence order platform > owner > admin > accountant > tax > clerk > auditor > viewer (D-23)
    - Strip clared- prefix; ignore unknown groups
    - Union permissions across groups
    - clared-platform: every tenant checkmark plus platform.tenants.read, platform.tenants.write, platform.billing, platform.support (D-25)
    - clared-owner has entity.create; clared-admin does not (D-24, AUTH-01)
    - clared-accountant has invoice.export; clared-clerk does not
    - clared-viewer does not have audit.read; clared-auditor does
    - Empty groups: permissions [] primaryRole ""
    - Multiple groups: primaryRole is the highest precedence present
  </behavior>
  <action>
    Keep projectRbac a pure function in rbac.ts with no Nest imports (D-22). Encode the D-25 permission matrix as a const object keyed by group suffix. clared-platform copies all tenant permissions then adds the four platform.* keys. Do not add platform.impersonate. Turn rbac.spec.ts GREEN. Wire /me to call projectRbac(groups) so e2e still passes.
  </action>
  <verify>
    <automated>pnpm --filter ./apps/backend test -- rbac</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ./apps/backend test -- rbac` exits 0
    - rbac.ts exports projectRbac
    - rbac.ts lists all eight group names from D-24
    - rbac.ts includes entity.create only on owner (and platform via copy), and includes the four platform.* strings
  </acceptance_criteria>
  <done>AUTH-01 catalog is unit-tested; /me uses the same function.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Prisma schema push after all schema edits</name>
  <precondition>OrbStack Docker is up; compose.clared.yml postgres is reachable at DATABASE_URL from apps/backend/.env.example (D-03).</precondition>
  <files>apps/backend/prisma/schema.prisma, apps/backend/prisma/migrations, apps/backend/prisma.config.ts</files>
  <read_first>
    - apps/backend/prisma/schema.prisma
    - apps/backend/prisma.config.ts
    - compose.clared.yml
    - .planning/phases/02-self-hosted-backend-authentik-sso/02-CONTEXT.md (D-07)
  </read_first>
  <action>
    After schema.prisma is final for this phase (init only, no business models), start compose.clared.yml postgres if needed. From apps/backend run prisma migrate deploy against local DATABASE_URL. If no migration exists yet, create the init migration with prisma migrate dev --name init locally (developer machine only), then deploy. Do not put migrate dev in the Dockerfile. SCHEMA_PUSH_REQUIRED: verification of this phase cannot pass without this command succeeding. Non-TTY destructive prisma db push --accept-data-loss is not allowed unless the schema is empty and CONTEXT allows it — prefer migrate deploy. Confirm GET /health/ready 200 against that database (start Nest briefly or use a script).
  </action>
  <verify>
    <automated>cd apps/backend && npx prisma migrate deploy && pnpm --filter ./apps/backend test:e2e -- health</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma migrate deploy` in apps/backend exits 0 against local DATABASE_URL
    - prisma/migrations contains at least one migration directory
    - `pnpm --filter ./apps/backend test:e2e -- health` still exits 0
  </acceptance_criteria>
  <done>Database schema is applied; ready check can ping Postgres.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Desktop fetch → Nest | Untrusted Origin and missing/forged Bearer |
| Nest → Authentik (stub this plan) | Code+state; secret only on Nest |
| Nest → Redis | Tickets and sessions |
| Nest → Postgres | Health ping only |
| Coolify env → Nest process | SECRET, DATABASE_URL, REDIS_URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Spoofing | GET /auth/login state | high | mitigate | Random state in Redis oauth:{state} EX 600; authorizationCodeGrant expectedState |
| T-02-04 | Spoofing | POST /auth/session ticket replay | high | mitigate | GETDEL 60s NX; second redeem 401; e2e in auth.e2e-spec.ts |
| T-02-05 | Information disclosure | CLIENT SECRET | high | mitigate | Env on Nest/Coolify only; never in apps/desktop or Tauri bundle |
| T-02-06 | Elevation of privilege | Unmatched HTTP routes | high | mitigate | Global AuthGuard + non-public catch-all; GET /api/invoices 401 |
| T-02-07 | Tampering | Session fixation | medium | mitigate | New random Bearer at redeem; ticket dead after GETDEL |
| T-02-08 | Information disclosure | Redis session payload | medium | mitigate | Opaque random token; claims only; no Authentik access token stored |
| T-02-09 | Denial of service | Session count | low | accept | D-30 no cap this phase; founder cluster scale |
| T-02-SC | Tampering | already-installed pins | high | mitigate | Stay on RESEARCH versions; no extra registries |
</threat_model>

<assumption_delta_decision>
noun: Authentik OIDC session (opaque Redis Bearer)
decision: promote
rationale: First auth method in the product; there is no prior login to keep alongside. Desktop session is this Bearer, not cookies and not a client JWT.
</assumption_delta_decision>

<verification>
pnpm --filter ./apps/backend test:e2e -- health
pnpm --filter ./apps/backend test:e2e -- auth
pnpm --filter ./apps/backend test -- rbac
npx prisma migrate deploy (apps/backend)
pnpm --filter ./apps/desktop exec tsc --noEmit
</verification>

<success_criteria>
Tracer green: health, 401 catch-all, one-time ticket, /me shape, RBAC catalog, schema applied, Dockerfile migrate CMD, desktop api.ts compiles.
</success_criteria>

## Artifacts this phase produces

This plan:
- `HealthController.getHealth`, `HealthController.ready`
- `Public` decorator, `AuthGuard`, `AuthController` (login, callback, session, logout)
- `projectRbac`
- `MeController.me`
- `PrismaService`, `RedisService`, `MemoryStore`
- `oidc.ts` stub (`buildAuthorizationUrl`, `authorizationCodeGrant`)
- `redeemTicket`, `fetchMe`, `logoutSession`, `apiFetch`, `MeResponse`
- `compose.clared.yml` postgres + redis
- `apps/backend/Dockerfile`
- Prisma init migration

<output>
Create `.planning/phases/02-self-hosted-backend-authentik-sso/02-02-SUMMARY.md` when done
</output>
