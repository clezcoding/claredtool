---
phase: 01-tauri-desktop-mockup-first-ui
plan: 05
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - apps/desktop/src/components/skeleton.tsx
  - apps/desktop/src/components/spinner.tsx
  - apps/desktop/src/components/error-state.tsx
  - apps/desktop/src/routes/rechnung.tsx
  - apps/desktop/src/__tests__/demo-states.test.tsx
autonomous: true
requirements: [UI-01]
gap_closure: true

estimate:
  tokens: 42000
  raw_tokens: 27000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Loading/error are demo surfaces only (skeleton/spinner; error copy „Ein Fehler ist aufgetreten…“) — refines D-11, no real network"
    - "[UI-SPEC UI-Considerations loading] In-flight demo state shows a skeleton on the invoice form/line-item list/media and a spinner on the CTA; it is a mock demo state with no real network (D-11 refinement)"
    - "[UI-SPEC UI-Considerations error] A failed load/submit demo state shows the Copywriting error row „Ein Fehler ist aufgetreten. Bitte versuchen Sie es erneut oder kontaktieren Sie den Support.“ with a retry path back to the ready surface"
    - "Demo loading and error states are reachable through a local demo-state control on the Rechnung screen — no fetch, no timers hitting the network, no persist"
  artifacts:
    - "apps/desktop/src/components/skeleton.tsx (Skeleton pulse block)"
    - "apps/desktop/src/components/spinner.tsx (Spinner)"
    - "apps/desktop/src/components/error-state.tsx (ErrorState — exact German error copy + retry)"
    - "apps/desktop/src/routes/rechnung.tsx (demo-state machine + switcher + loading/error branches)"
    - "apps/desktop/src/__tests__/demo-states.test.tsx (runnable check that fails if loading/error demo missing)"
  key_links:
    - "rechnung.tsx renders Skeleton/Spinner during the loading demo state and ErrorState during the error demo state, switching between ready/loading/error via local useState only (D-11 refinement, no network)"
    - "ErrorState retry control calls back into rechnung.tsx to restore the ready surface (the UI-SPEC retry path)"
  prohibitions:
    - "[flagged-unverified] MUST NOT introduce real network / fetch / async I/O — loading and error are local-state demo surfaces only (D-11 refinement, Phase 1 has no network)"
    - "[flagged-unverified] MUST NOT persist demo state (no localStorage / sessionStorage / store) — local React state only"
    - "[flagged-unverified] MUST NOT bundle or fall back to Electron"
    - "[flagged-unverified] MUST NOT present staged tax or the mock PDF as real/computed output"
    - "[flagged-unverified] MUST NOT use PDF.js or an embedded PDF binary renderer"
  flagged_assumptions:
    - "[EDGE unresolved · UI-01 adjacency] Whether two equal/adjacent line items merge, collide, or separate is unspecified — this gap plan touches only demo loading/error surfaces and does not add merge/collision logic. Carried as assumption; not solved here."
    - "[EDGE unresolved · UI-01 empty] Behavior for empty/single/null line-item input is unspecified beyond the existing empty-state toggle — this gap plan does not change zero/one/many handling. Carried as assumption; not solved here."
    - "[EDGE unresolved · UI-01 ordering] Stable output order for equal-comparing line items is unspecified — unchanged by this gap plan (SAMPLE_INVOICE array order). Carried as assumption; not solved here."
    - "[EDGE unresolved · DESK-01 unclassified] Windows Tauri launch remains behavior-unverified (CI-only, no git remote) — out of scope for this gap plan per gap source. Carried as assumption; not solved here."
---

<objective>
Close the one code gap from 01-VERIFICATION.md: the invoice screen has no demo loading or error surface. Add local-state demo loading (skeleton + spinner) and demo error (the UI-SPEC error copy with a retry path) to the Rechnung centerpiece. All state is local React state — no network, no fetch, no persist. This refines CONTEXT D-11: the mock exposes these as demo surfaces, not live network states.

Purpose: Make UI-01 truth #16 („Loading/error are demo surfaces…“) verifiable in code so Phase 1 can seal.
Output: Skeleton/Spinner/ErrorState components and a Rechnung demo-state machine, covered by a Vitest check that fails if either surface is missing.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-tauri-desktop-mockup-first-ui/01-UI-SPEC.md
@.planning/phases/01-tauri-desktop-mockup-first-ui/01-PATTERNS.md
@apps/desktop/src/routes/rechnung.tsx
@apps/desktop/src/components/invoice-empty-state.tsx
@apps/desktop/src/__tests__/invoice.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Demo surface primitives — Skeleton, Spinner, ErrorState (exact German error copy + retry)</name>
  <files>apps/desktop/src/components/skeleton.tsx, apps/desktop/src/components/spinner.tsx, apps/desktop/src/components/error-state.tsx</files>
  <read_first>
    - apps/desktop/src/components/invoice-empty-state.tsx (existing presentational component pattern: named export, Tailwind classes, callback prop)
    - apps/desktop/src/components/line-item-card.tsx (card/border/testid conventions)
    - .planning/phases/01-tauri-desktop-mockup-first-ui/01-UI-SPEC.md (Copywriting Contract error row; Color: Accent hsl(217 91% 60%), Destructive hsl(0 84% 60%); Spacing scale)
  </read_first>
  <action>Create three presentational components as named exports, matching the existing invoice-empty-state.tsx style (function component, Tailwind classes, no persist, no network). skeleton.tsx exports `Skeleton` — a muted pulsing block (Tailwind `animate-pulse`, rounded, `bg-muted`/`bg-secondary`) accepting a `className` prop for width/height, with `data-testid="skeleton"` on the root. spinner.tsx exports `Spinner` — a small rotating indicator (Tailwind `animate-spin`, a bordered circle or an SVG), `role="status"` with an accessible label „Wird geladen“, and `data-testid="spinner"` on the root. error-state.tsx exports `ErrorState` taking an `onRetry: () => void` prop; it renders the Copywriting Contract error row VERBATIM as its message text — the exact string from the UI-SPEC error row — using the destructive color for emphasis, plus a retry `<button>` labelled „Erneut versuchen“ that calls `onRetry`, with `data-testid="error-state"` on the root. Use @clared/ui primitives (Card/Button) where they fit; German copy verbatim; no fenced tax/PDF content. These are pure demo surfaces (D-11 refinement) — no fetch, no timers, no I/O.</action>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - error-state.tsx source contains the exact string „Ein Fehler ist aufgetreten. Bitte versuchen Sie es erneut oder kontaktieren Sie den Support.“ (matches 01-UI-SPEC Copywriting Contract error row verbatim)
    - error-state.tsx renders a retry button labelled „Erneut versuchen“ wired to the `onRetry` prop
    - skeleton.tsx exports `Skeleton` with `data-testid="skeleton"`; spinner.tsx exports `Spinner` with `role="status"` and `data-testid="spinner"`
    - No fetch/XMLHttpRequest/import of async data in any of the three files (grep -v '^\s*//' then grep for `fetch(` returns nothing)
    - `tsc --noEmit` for apps/desktop exits 0
  </acceptance_criteria>
  <done>Skeleton, Spinner, and ErrorState components exist as reusable named exports; ErrorState carries the exact UI-SPEC error copy and a working retry callback; TypeScript compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire demo-state machine into Rechnung (loading/error/ready) + runnable Vitest check</name>
  <files>apps/desktop/src/routes/rechnung.tsx, apps/desktop/src/__tests__/demo-states.test.tsx</files>
  <read_first>
    - apps/desktop/src/routes/rechnung.tsx (current ready-only render: form, line-item list, „+ Position“, empty toggle, TaxRail)
    - apps/desktop/src/components/skeleton.tsx, apps/desktop/src/components/spinner.tsx, apps/desktop/src/components/error-state.tsx (Task 1 exports)
    - apps/desktop/src/__tests__/invoice.test.tsx (render(<App/>) + hash "#/" harness, testid/role query conventions, cleanup)
    - .planning/phases/01-tauri-desktop-mockup-first-ui/01-UI-SPEC.md (UI Considerations loading/error rows)
  </read_first>
  <behavior>
    - Activating the loading demo shows the invoice skeleton surface and a spinner, and hides the real line-item cards
    - Activating the error demo shows the exact error copy „Ein Fehler ist aufgetreten…“ and a „Erneut versuchen“ retry control
    - Clicking „Erneut versuchen“ restores the ready surface (sample line-item cards visible again)
  </behavior>
  <action>Add a `type DemoState = "ready" | "loading" | "error"` and `const [demo, setDemo] = useState<DemoState>("ready")` to RechnungScreen. Add a small always-visible demo-state switcher in the invoice header (next to „Neue Rechnung“) with three buttons — „Demo: Bereit“ (setDemo "ready"), „Demo: Laden“ (setDemo "loading"), „Demo: Fehler“ (setDemo "error") — a discreet demo affordance, local state only. Branch the screen body: when `demo === "loading"`, render a loading surface (wrap it in a container with `data-testid="invoice-skeleton"`) built from Skeleton blocks standing in for the form fields, the line-item list, and the media/empty-hero area, plus a Spinner on the header CTA area (the „form/lists/media“ + „spinner on CTA“ from the UI-SPEC loading row); do NOT render the real line-item cards while loading. When `demo === "error"`, render `<ErrorState onRetry={() => setDemo("ready")} />` in place of the canvas. When `demo === "ready"`, keep the existing ready render (form + cards + „+ Position“ + empty toggle) unchanged. Hide the TaxRail during loading and error (only the ready canvas shows the rail). No fetch, no network timer, no persist — the switcher only flips local state (D-11 refinement). Then create __tests__/demo-states.test.tsx following the invoice.test.tsx harness: render(<App/>) at hash "#/"; test 1 clicks „Demo: Laden“ and asserts `getByTestId("invoice-skeleton")` and `getByTestId("spinner")` are present and `queryAllByTestId("line-item-card")` has length 0; test 2 clicks „Demo: Fehler“, asserts the exact error string is shown and a „Erneut versuchen“ button exists, clicks it, and asserts the sample line-item cards return (length === SAMPLE_INVOICE.lineItems.length).</action>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec vitest run --reporter=verbose apps/desktop/src/__tests__/demo-states.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - demo-states.test.tsx asserts loading demo renders `data-testid="invoice-skeleton"` + `data-testid="spinner"` and zero `line-item-card` nodes
    - demo-states.test.tsx asserts error demo renders the exact UI-SPEC error copy and a „Erneut versuchen“ button, and that clicking it restores SAMPLE_INVOICE.lineItems.length cards
    - rechnung.tsx renders Skeleton/Spinner only in the loading branch and ErrorState only in the error branch; ready branch is unchanged
    - rechnung.tsx demo switcher flips local state only — no fetch/network call added (grep for `fetch(` in rechnung.tsx returns nothing)
    - `vitest run` for demo-states.test.tsx exits 0
  </acceptance_criteria>
  <done>The Rechnung screen exposes reachable local-state demo loading (skeleton + spinner) and demo error (exact copy + retry) surfaces; the ready surface is untouched; demo-states.test.tsx passes and fails if either surface is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo-state switcher → React state | Local UI toggle only; no network, no persist, no eval, no external input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Tampering | Demo-state switcher (ready/loading/error) | low | accept | Switcher flips a local `useState` union only; no persist, no network, no injection sink |
| T-01-08 | Info Disclosure | ErrorState message | low | mitigate | Error copy is the fixed UI-SPEC string; no real error object, stack, or user/financial data surfaced |
| T-01-09 | Denial of Service | Loading demo surface | low | accept | Pure render branch; no timers/loops hitting network — cannot spin or exhaust I/O |
</threat_model>

<verification>
- `pnpm --filter ./apps/desktop exec vitest run` green including demo-states.test.tsx (loading skeleton+spinner, error copy+retry, retry restores ready)
- `pnpm --filter ./apps/desktop exec tsc --noEmit` exits 0
- grep confirms no `fetch(` / network call added by rechnung.tsx or the new components
- Manual (folds into phase verify): on Rechnung, click „Demo: Laden“ (skeleton + spinner, no cards), „Demo: Fehler“ (error copy + „Erneut versuchen“), retry returns to the sample canvas
</verification>

<success_criteria>
UI-01 truth #16 is now real in code: the Rechnung screen has local-state demo loading (skeleton/spinner on form/lists/media + CTA) and demo error surfaces (exact „Ein Fehler ist aufgetreten…“ copy with a working retry path), no network, no persist — and a Vitest check fails if either is missing.
</success_criteria>

## Artifacts this phase produces (Plan 01-05)

- `apps/desktop/src/components/skeleton.tsx` — `Skeleton`
- `apps/desktop/src/components/spinner.tsx` — `Spinner`
- `apps/desktop/src/components/error-state.tsx` — `ErrorState` (exact UI-SPEC error copy + retry)
- `apps/desktop/src/routes/rechnung.tsx` — demo-state machine + switcher + loading/error/ready branches
- `apps/desktop/src/__tests__/demo-states.test.tsx` — runnable check for the demo loading/error surfaces

<output>
Create `.planning/phases/01-tauri-desktop-mockup-first-ui/01-05-SUMMARY.md` when done.
</output>
