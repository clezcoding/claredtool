---
phase: 01-tauri-desktop-mockup-first-ui
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/desktop/src/routes/entities.tsx
  - apps/desktop/src/routes/kunden.tsx
  - apps/desktop/src/routes/tax.tsx
  - apps/desktop/src/components/create-disabled-button.tsx
  - apps/desktop/src/__tests__/screens.test.tsx
autonomous: true
requirements: [UI-01]

estimate:
  tokens: 60000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Entities is a list with exactly one EU-GmbH row (the sample-invoice seller); clicking it opens a read-only fake detail — not a split inspector, not list-only (D-29, D-30)"
    - "Kunden is a list with exactly one US customer row (the sample-invoice buyer); clicking opens a read-only fake detail (D-29, D-30)"
    - "„Anlegen“ buttons on Entities and Kunden are visible but disabled with the hint „Wird in Phase 3 aktiviert“ — not omitted, not opening a fake create form (D-31)"
    - "The /tax sidebar screen shows the staged document-level TaxDecision (Live Steuerberechnung view) using canonical fields — tax is present on both the invoice rail and the Tax screen (D-15)"
    - "Entities/Kunden are never empty in Phase 1 (one sample row each) and one row does not overflow (UI-SPEC UI-Considerations empty/overflow E7,E8 covered)"
    - "[UI-SPEC UI-Considerations] populated E7/E8 = one row, click opens read-only fake detail; zero-one-many E7/E8 = Phase 1 shows exactly one row (zero/many layouts not built) — covered"
  artifacts:
    - "apps/desktop/src/routes/entities.tsx"
    - "apps/desktop/src/routes/kunden.tsx"
    - "apps/desktop/src/routes/tax.tsx"
    - "apps/desktop/src/components/create-disabled-button.tsx"
    - "apps/desktop/src/__tests__/screens.test.tsx"
  key_links:
    - "entities.tsx and kunden.tsx read the seller/buyer objects from SAMPLE_INVOICE (01-01) so the entity/customer are the SAME parties as the sample invoice (D-30)"
    - "tax.tsx reads SAMPLE_INVOICE.taxDecision (01-01) — same staged decision as the invoice rail (D-15)"
  prohibitions:
    - "MUST NOT omit or hide the „Anlegen“ buttons, and MUST NOT open a fake create form — they are visible, disabled, hinted mocks (D-31)"
    - "MUST NOT invent owner-only entity-create logic — that is Phase 3; Phase 1 shows only the disabled mock button (CONTEXT deferred)"
    - "MUST NOT persist — clicking a row toggles local selection state only, no store (D-13)"
  flagged_assumptions:
    - "[EDGE unresolved · DESK-01 unclassified] The Tax/Entities/Kunden screens are mock-only; their real data flow (Phase 3) is unspecified here and intentionally not built. Carried as assumption."
---

<objective>
Build the three remaining sidebar destinations as clickable mocks: Entities and Kunden as one-row lists with read-only fake detail and a visible-disabled „Anlegen“, and the Tax screen as a staged document-level TaxDecision view. All data comes from SAMPLE_INVOICE; nothing persists; create actions are intentional disabled mocks.

Purpose: Complete the navigable five-screen shell (UI-01, success criterion 3).
Output: entities.tsx, kunden.tsx, tax.tsx and a shared disabled-create button.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-tauri-desktop-mockup-first-ui/01-CONTEXT.md
@.planning/phases/01-tauri-desktop-mockup-first-ui/01-UI-SPEC.md
@.planning/phases/01-tauri-desktop-mockup-first-ui/01-PATTERNS.md
@apps/desktop/src/data/sample-invoice.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Entities screen — one EU-GmbH row, read-only detail, disabled „Anlegen“</name>
  <files>apps/desktop/src/routes/entities.tsx, apps/desktop/src/components/create-disabled-button.tsx, apps/desktop/src/__tests__/screens.test.tsx</files>
  <read_first>
    - apps/desktop/src/data/sample-invoice.ts (SAMPLE_INVOICE.seller)
    - .planning/phases/01-tauri-desktop-mockup-first-ui/01-PATTERNS.md (entities/kunden list + read-only detail; disabled-button copy)
    - .planning/phases/01-tauri-desktop-mockup-first-ui/01-UI-SPEC.md (Copywriting Contract: disabled create hint)
  </read_first>
  <behavior>
    - Entities list shows exactly one row (the EU-GmbH seller name from SAMPLE_INVOICE)
    - Clicking the row shows a read-only detail panel with the entity fields
    - „Anlegen“ button is present, disabled, and shows „Wird in Phase 3 aktiviert“
  </behavior>
  <action>Create routes/entities.tsx: a list rendering exactly one row from SAMPLE_INVOICE.seller (EU-GmbH — D-30). Clicking a row sets local selectedId state and shows a read-only fake detail panel (name, address, ustid) — no navigation, no dialog, not a permanent split inspector (D-29). Create components/create-disabled-button.tsx: a visible disabled @clared/ui Button labelled „Anlegen“ with the visible hint „Wird in Phase 3 aktiviert“ (D-31; reused by kunden.tsx). Do not open a create form; do not add owner-role logic. Dense-but-calm (D-08); German copy verbatim.</action>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec vitest run --reporter=verbose apps/desktop/src/__tests__/screens.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - screens.test.tsx asserts entities list renders one row with the SAMPLE_INVOICE.seller name
    - Clicking the row reveals a read-only detail containing the seller ustid
    - „Anlegen“ button is present, has the disabled attribute, and the hint text „Wird in Phase 3 aktiviert“ is queryable
    - `vitest run` exits 0
  </acceptance_criteria>
  <done>Entities shows one EU-GmbH row with read-only detail on click and a visible-disabled „Anlegen“ mock; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Kunden screen — one US customer row, read-only detail, disabled „Anlegen“</name>
  <files>apps/desktop/src/routes/kunden.tsx, apps/desktop/src/__tests__/screens.test.tsx</files>
  <read_first>
    - apps/desktop/src/data/sample-invoice.ts (SAMPLE_INVOICE.buyer)
    - apps/desktop/src/components/create-disabled-button.tsx (from Task 1)
    - .planning/phases/01-tauri-desktop-mockup-first-ui/01-PATTERNS.md (entities/kunden pattern)
  </read_first>
  <behavior>
    - Kunden list shows exactly one row (the US buyer name from SAMPLE_INVOICE)
    - Clicking the row shows a read-only detail panel
    - Reuses the disabled „Anlegen“ button with the Phase-3 hint
  </behavior>
  <action>Create routes/kunden.tsx mirroring entities.tsx: one row from SAMPLE_INVOICE.buyer (US customer — D-30), click opens a read-only fake detail (name, address, country) via local selectedId state (D-29), and reuse components/create-disabled-button.tsx for the disabled „Anlegen“ + „Wird in Phase 3 aktiviert“ hint (D-31). No create form, no persist.</action>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec vitest run --reporter=verbose apps/desktop/src/__tests__/screens.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - screens.test.tsx asserts kunden list renders one row with the SAMPLE_INVOICE.buyer name
    - Clicking the row reveals a read-only detail containing the buyer country
    - Disabled „Anlegen“ + „Wird in Phase 3 aktiviert“ present
    - `vitest run` exits 0
  </acceptance_criteria>
  <done>Kunden shows one US-customer row with read-only detail and the shared disabled-create mock; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Tax screen — staged document-level TaxDecision view</name>
  <files>apps/desktop/src/routes/tax.tsx, apps/desktop/src/__tests__/screens.test.tsx</files>
  <read_first>
    - apps/desktop/src/data/sample-invoice.ts (SAMPLE_INVOICE.taxDecision, StagedTaxDecision)
    - docs/clared-tax-engine-architecture.md (lines 39-47 canonical fields)
    - .planning/phases/01-tauri-desktop-mockup-first-ui/01-UI-SPEC.md (Copywriting: Tax rail heading)
  </read_first>
  <action>Replace the /tax placeholder with routes/tax.tsx rendering the staged document-level TaxDecision from SAMPLE_INVOICE.taxDecision (D-15) using canonical field names (place_of_supply_country, tax_liability_party, invoice_tax_rate, invoice_tax_shown, reverse_charge_flag, legal_reference, invoice_text_block_id, applied_rule_id, applied_rule_version). Read-only, hardcoded — no evaluate(), no computed math, no collision logic (D-13; do not invent). This screen makes tax reachable from the sidebar in addition to the invoice rail. Dense-but-calm.</action>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec vitest run --reporter=verbose apps/desktop/src/__tests__/screens.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tax.tsx renders SAMPLE_INVOICE.taxDecision.legal_reference and applied_rule_id
    - tax.tsx uses canonical field names only (no `rate`/`reverse_charge`/`legal_text`)
    - screens.test.tsx asserts the tax screen renders the applied_rule_id; `vitest run` exits 0
  </acceptance_criteria>
  <done>/tax renders the staged canonical TaxDecision read-only; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static sample data → React render | Entities/Kunden/Tax read hardcoded SAMPLE_INVOICE; no input sink, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Elevation of Privilege | Disabled „Anlegen“ mock buttons | low | accept | Buttons are disabled UI mocks; no create path, no role logic wired — real RBAC is Phase 3 (D-31, CONTEXT deferred) |
| T-01-08 | Info Disclosure | Read-only fake detail panels | low | accept | Only hardcoded sample seller/buyer shown; no real customer data |
| T-01-09 | Tampering | Tauri IPC / commands | low | accept | No Tauri commands added; IPC surface remains zero |
</threat_model>

<verification>
- `pnpm --filter ./apps/desktop exec vitest run` green (screens.test.tsx: entities row+detail+disabled, kunden row+detail, tax canonical fields)
- Folds into phase verify: navigate Entities/Kunden/Tax from sidebar; click rows; confirm „Anlegen“ disabled with hint
</verification>

<success_criteria>
The five-screen shell is fully navigable: Entities and Kunden are one-row mock lists with read-only detail and visible-disabled create, and /tax shows the staged canonical TaxDecision. All data from SAMPLE_INVOICE, nothing persists, tests green.
</success_criteria>

## Artifacts this phase produces (Plan 01-03)

- `apps/desktop/src/routes/entities.tsx` — `EntitiesScreen`
- `apps/desktop/src/routes/kunden.tsx` — `KundenScreen`
- `apps/desktop/src/routes/tax.tsx` — `TaxScreen`
- `apps/desktop/src/components/create-disabled-button.tsx` — `CreateDisabledButton`
- `apps/desktop/src/__tests__/screens.test.tsx`

<output>
Create `.planning/phases/01-tauri-desktop-mockup-first-ui/01-03-SUMMARY.md` when done.
</output>
