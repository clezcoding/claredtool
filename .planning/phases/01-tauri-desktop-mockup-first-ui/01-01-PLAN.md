---
phase: 01-tauri-desktop-mockup-first-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - apps/desktop/package.json
  - apps/desktop/vite.config.ts
  - apps/desktop/tsconfig.json
  - apps/desktop/index.html
  - apps/desktop/vitest.config.ts
  - apps/desktop/src/main.tsx
  - apps/desktop/src/App.tsx
  - apps/desktop/src/data/sample-invoice.ts
  - apps/desktop/src/routes/rechnung.tsx
  - apps/desktop/src/styles/globals.css
  - apps/desktop/src/__tests__/routes.test.tsx
  - apps/desktop/public/empty-state-hero.png
  - apps/desktop/src-tauri/Cargo.toml
  - apps/desktop/src-tauri/tauri.conf.json
  - apps/desktop/src-tauri/src/lib.rs
  - packages/ui/package.json
  - packages/ui/tsconfig.json
  - packages/ui/components.json
  - packages/ui/src/index.ts
  - packages/ui/src/styles/globals.css
autonomous: false
requirements: [UI-01, DESK-01]
user_setup:
  - service: rust
    why: "Tauri core is Rust; tauri dev/build cannot run without rustc/cargo (RESEARCH Pitfall 1 — not installed on this machine)"
    dashboard_config:
      - task: "Verify Rust toolchain after rustup install (source $HOME/.cargo/env in a new shell)"
        location: "local machine — rustup"

estimate:
  tokens: 78000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Clared launches as a Tauri window (native OS titlebar, decorations ON) on macOS — not Electron (DESK-01, D-07)"
    - "Left sidebar lists exactly Rechnung · Entities · Kunden · Tax · PDF in that order and all five routes are navigable (D-05, D-06, UI-01)"
    - "App is React/TypeScript with shadcn/ui + Radix components imported from packages/ui, styled with Tailwind CSS v4, dark theme applied at boot (D-01, D-02, D-03, D-09)"
    - "Repo layout is apps/desktop + packages/ui only — no apps/backend, no packages/tax-engine scaffolded (D-04)"
    - "Launch lands on the filled sample-invoice Rechnung route as the index route (D-22, UI-01)"
    - "sample-invoice.ts carries one EU-GmbH seller + US customer B2B invoice with canonical TaxDecision fields (D-12)"
    - "Deliverable is a clickable dark UI running inside the Tauri window plus the approved UI-SPEC (D-10)"
    - "Fixed 5-item nav does not overflow (UI-SPEC UI-Considerations: overflow/E3 nav covered, D-06)"
  artifacts:
    - "pnpm-workspace.yaml (apps/*, packages/*)"
    - "apps/desktop/src-tauri/tauri.conf.json (Tauri v2 app.windows[], decorations true, title Clared)"
    - "apps/desktop/src/App.tsx (createHashRouter + AppShell + Outlet)"
    - "apps/desktop/src/main.tsx (dark-first classList.add('dark'))"
    - "apps/desktop/src/data/sample-invoice.ts (StagedTaxDecision + LineItem + SampleInvoice, canonical fields)"
    - "packages/ui (shadcn/Radix design system, @clared/ui barrel export)"
    - "apps/desktop/vitest.config.ts + apps/desktop/src/__tests__/routes.test.tsx"
    - "apps/desktop/public/empty-state-hero.png (placeholder, replaced in 01-04)"
  key_links:
    - "packages/ui exports ./src/index.ts (TypeScript source) → apps/desktop imports @clared/ui via workspace:* — Vite resolves at dev time, no build step (RESEARCH Pitfall 5)"
    - "createHashRouter (NOT BrowserRouter) so sub-routes survive Tauri custom protocol tauri://localhost in production build (RESEARCH Pitfall + Pattern 3)"
    - "tauri.conf.json build.devUrl (http://localhost:5173) must match the Vite dev port"
  prohibitions:
    - "MUST NOT bundle or fall back to Electron — the desktop runtime is Tauri (DESK-01 product lock; RESEARCH Out of Scope)"
    - "MUST NOT present the app as persisting data — Phase 1 has no persist; no localStorage, no store wiring beyond hardcoded sample data"
  flagged_assumptions:
    - "[EDGE unresolved · UI-01 empty] Result for empty/single/null invoice input is not fully specified — Phase 1 resolves empty invoice to the E4 empty state (D-23) and single line item to one card; null is out of scope. Carried as assumption, not silently dropped."
    - "[EDGE unresolved · DESK-01 unclassified] Windows launch parity cannot be verified on this macOS host; verification is CI-only (windows-latest, see 01-04). Carried as assumption."
---

<objective>
Install the Rust toolchain, scaffold the Clared Tauri 2 monorepo (apps/desktop + packages/ui), stand up the shadcn/ui + Tailwind v4 dark-first design system, and wire ONE end-to-end vertical slice: a Tauri window that boots dark, renders the left sidebar shell, and shows the sample-invoice Rechnung route via a @clared/ui component. This is the tracer — it proves the whole Phase-1 architecture (Rust → Tauri → Vite/React → HashRouter → packages/ui import) end-to-end before any screen is expanded.

Purpose: De-risk the architecture on the first commit. Every later plan expands out from this proven slice.
Output: A launchable dark Tauri desktop shell with five navigable routes, the sample invoice data module, and the Vitest harness.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tauri-desktop-mockup-first-ui/01-CONTEXT.md
@.planning/phases/01-tauri-desktop-mockup-first-ui/01-RESEARCH.md
@.planning/phases/01-tauri-desktop-mockup-first-ui/01-PATTERNS.md
@.planning/phases/01-tauri-desktop-mockup-first-ui/01-UI-SPEC.md
@docs/clared-tax-engine-architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Rust + scaffold Tauri 2 monorepo with shadcn/ui dark-first design system</name>
  <reversibility rating="costly">Repo layout apps/desktop + packages/ui and the @clared/ui import graph lock in the module structure every later phase inherits (D-04); undo means re-pathing all imports.</reversibility>
  <files>pnpm-workspace.yaml, package.json, apps/desktop/package.json, apps/desktop/vite.config.ts, apps/desktop/tsconfig.json, apps/desktop/index.html, apps/desktop/vitest.config.ts, apps/desktop/src/styles/globals.css, apps/desktop/public/empty-state-hero.png, apps/desktop/src-tauri/Cargo.toml, apps/desktop/src-tauri/tauri.conf.json, apps/desktop/src-tauri/src/lib.rs, packages/ui/package.json, packages/ui/tsconfig.json, packages/ui/components.json, packages/ui/src/index.ts, packages/ui/src/styles/globals.css</files>
  <read_first>
    - .planning/phases/01-tauri-desktop-mockup-first-ui/01-RESEARCH.md (Installation block, Architecture Patterns 1–5, Pitfalls 1/3/5/6, Environment Availability)
    - .planning/phases/01-tauri-desktop-mockup-first-ui/01-PATTERNS.md (pnpm-workspace.yaml, tauri.conf.json, globals.css, packages/ui export patterns)
    - .planning/phases/01-tauri-desktop-mockup-first-ui/01-UI-SPEC.md (Design System, Spacing Scale, Typography, Color)
  </read_first>
  <action>Install Rust via rustup (curl https://sh.rustup.rs) then source $HOME/.cargo/env; confirm rustc and cargo resolve (RESEARCH Pitfall 1, blocking). Scaffold at repo root with `pnpm create tauri-app@latest apps/desktop -- --template react-ts` (Tauri 2.11.4, @tauri-apps/api 2.11.1, react 19, vite 8, typescript). Create pnpm-workspace.yaml with globs apps/* and packages/* and a root package.json. Set tauri.conf.json to Tauri v2 format: productName "Clared", identifier "com.clared.app", app.windows[0] label "main" title "Clared" width 1280 height 800 resizable true decorations true (D-07 native titlebar), security.csp null, build.devUrl http://localhost:5173, build.frontendDist ../dist. Use app.windows[] NOT tauri.windows[] (v2, RESEARCH Pitfall 2). Create packages/ui and run `npx shadcn@latest init -t vite --monorepo -y` from inside packages/ui ONLY (D-02, RESEARCH Pitfall 3) — never inside apps/desktop; set packages/ui/package.json name @clared/ui with exports "." → ./src/index.ts and a barrel src/index.ts; add react-router 8.3.0 and lucide-react to apps/desktop; add @clared/ui as workspace:* dependency of apps/desktop. Author both globals.css files with Tailwind v4 CSS-first imports (@import "tailwindcss"; tw-animate-css; shadcn/tailwind.css; @custom-variant dark) — do NOT create tailwind.config.js (D-03, RESEARCH Pitfall 6). Encode the UI-SPEC dark palette as CSS tokens (dominant hsl(222 47% 11%), secondary hsl(217 33% 17%), accent hsl(217 91% 60%), destructive hsl(0 84% 60%)), the 4px-multiple spacing scale, and Inter/14px body typography, holding dense-but-calm (D-08). Install the Vitest harness in apps/desktop (`pnpm add -D vitest @testing-library/react jsdom @vitejs/plugin-react`) and write vitest.config.ts with jsdom environment and globals true. Drop a 1×1 placeholder empty-state-hero.png into apps/desktop/public/ (replaced by Higgsfield in 01-04) so the empty-state image path always resolves. Do NOT scaffold apps/backend or packages/tax-engine (D-04, deferred).</action>
  <executor-note>The 22 files in this task's files_modified are a single scaffold batch, not 22 independent hand-authored files. `pnpm create tauri-app` and `npx shadcn init` CLI-generate the bulk (apps/desktop scaffold, src-tauri/*, packages/ui/*); you hand-edit only tauri.conf.json, both globals.css, the two package.json workspace links, and vitest.config.ts. Treat file count as scaffold output, not authoring load.</executor-note>
  <verify>
    <automated>rustc --version && cargo --version && test -f apps/desktop/src-tauri/tauri.conf.json && test -f pnpm-workspace.yaml && test -f packages/ui/package.json && test ! -d apps/backend && test ! -d packages/tax-engine && pnpm install && pnpm --filter ./apps/desktop exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rustc --version` and `cargo --version` both exit 0
    - tauri.conf.json contains key `app.windows` (v2) and does NOT contain top-level `tauri.windows` (v1)
    - tauri.conf.json app.windows[0].decorations is true and title is "Clared"
    - packages/ui/package.json name is "@clared/ui" and exports "." points to "./src/index.ts"
    - apps/desktop/package.json lists "@clared/ui": "workspace:*"
    - No tailwind.config.js exists anywhere in the repo
    - apps/backend and packages/tax-engine directories do NOT exist
    - `pnpm install` and `tsc --noEmit` for apps/desktop both exit 0
  </acceptance_criteria>
  <done>Rust installed; Tauri v2 monorepo scaffolded as apps/desktop + packages/ui with shadcn/Radix/Tailwind-v4 dark tokens, @clared/ui workspace link resolving, and Vitest configured. No backend/tax-engine dirs, no tailwind.config.js.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Tracer — dark boot → HashRouter → AppShell sidebar → sample-invoice Rechnung route, end-to-end in the Tauri window</name>
  <files>apps/desktop/src/main.tsx, apps/desktop/src/App.tsx, apps/desktop/src/data/sample-invoice.ts, apps/desktop/src/routes/rechnung.tsx, apps/desktop/src/__tests__/routes.test.tsx</files>
  <read_first>
    - .planning/phases/01-tauri-desktop-mockup-first-ui/01-PATTERNS.md (App.tsx HashRouter, main.tsx dark boot, AppShell sidebar, sample-invoice.ts data module + resolved field-name table)
    - .planning/phases/01-tauri-desktop-mockup-first-ui/01-RESEARCH.md (Pattern 3 HashRouter, sidebar layout code example, Validation Architecture)
    - docs/clared-tax-engine-architecture.md (lines 39-47 — canonical TaxDecision field names)
    - apps/desktop/src/main.tsx, apps/desktop/src/App.tsx (scaffold output from Task 1)
  </read_first>
  <action>Create apps/desktop/src/data/sample-invoice.ts exporting a StagedTaxDecision interface using the CANONICAL fields from clared-tax-engine-architecture.md — place_of_supply_country, tax_liability_party ("supplier" | "customer"), invoice_tax_rate, invoice_tax_shown, reverse_charge_flag, legal_reference, invoice_text_block_id, applied_rule_id, applied_rule_version — NOT the simplified RESEARCH aliases (rate/reverse_charge/legal_text); a LineItem interface (bezeichnung, menge, einzelpreis, netto); and a SampleInvoice interface plus one SAMPLE_INVOICE constant with an EU-GmbH seller (name, address, ustid) and a US buyer (name, address, country), a small line-item list, and a reverse-charge TaxDecision (reverse_charge_flag true, invoice_tax_rate 0, legal_reference the §13b UStG text, applied_rule_id "eu-b2b-reverse-charge") — D-12, D-15. In main.tsx add document.documentElement.classList.add("dark") before render (D-09; no localStorage). In App.tsx build createHashRouter (import from "react-router", NOT react-router-dom) with a root AppShell element and child routes: index → RechnungScreen, "entities"/"kunden"/"tax"/"pdf" → placeholder screens; render via RouterProvider. Build AppShell with a left NavLink sidebar whose NAV_ITEMS are exactly Rechnung (/) · Entities · Kunden · Tax · PDF in that order with lucide-react icons and an Outlet main region (D-05, D-06). Create routes/rechnung.tsx as the tracer's far end: render SAMPLE_INVOICE header fields and at least one value inside a Card imported from "@clared/ui", proving the cross-package import path works end-to-end. Write src/__tests__/routes.test.tsx (React Testing Library) asserting all five sidebar destinations render/navigate and the index route shows the sample invoice number.</action>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - sample-invoice.ts StagedTaxDecision uses fields invoice_tax_rate, reverse_charge_flag, legal_reference, applied_rule_id (canonical) and does NOT declare a field named `rate`, `reverse_charge`, or `legal_text`
    - App.tsx imports createHashRouter from "react-router" (no "react-router-dom" import anywhere)
    - AppShell NAV order is Rechnung, Entities, Kunden, Tax, PDF
    - routes/rechnung.tsx imports at least one component from "@clared/ui"
    - routes.test.tsx asserts 5 navigable routes and passes: `vitest run` exits 0
  </acceptance_criteria>
  <done>The single happy path works end-to-end and is committed: the dark Tauri window boots, the sidebar shows the five routes in order, the index Rechnung route renders sample-invoice data through a @clared/ui component, and the route test is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — Tauri window launches on macOS</name>
  <what-built>A Tauri 2 desktop window that launches dark, shows the left sidebar (Rechnung · Entities · Kunden · Tax · PDF), and renders the sample-invoice Rechnung index route — the tracer slice proving the macOS runtime.</what-built>
  <how-to-verify>
    From apps/desktop run `pnpm tauri dev`. Confirm: (1) a native window titled "Clared" opens with the OS titlebar/decorations (NOT an Electron chrome, NOT a browser tab); (2) the app is dark; (3) the left sidebar lists Rechnung, Entities, Kunden, Tax, PDF in that order and clicking each navigates without a blank/404; (4) the app opens on the Rechnung sample invoice (DESK-01, D-07, D-22).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what rendered wrong (window chrome, theme, nav order, or launch route).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm/cargo registry → build | Third-party packages pulled during scaffold/install |
| WebView → OS | Tauri WebView renders local bundle; no IPC commands defined this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | Tauri IPC / commands | low | accept | No Tauri commands defined in Phase 1 — IPC attack surface is zero (RESEARCH Security Domain) |
| T-01-02 | Info Disclosure | WebView filesystem access | low | accept | No filesystem/allowlist capabilities enabled; Tauri default is deny-all |
| T-01-03 | Tampering | Tauri CSP (security.csp null) | low | accept | Permissive CSP acceptable for local mockup-only app; tighten before Phase 2 network calls (RESEARCH) |
| T-01-SC | Tampering | npm/cargo installs (create-tauri-app, shadcn, react, vite) | high | mitigate | Package Legitimacy Audit in RESEARCH.md — all packages verified against registry 2026-08-19; SUS "too-new" flags confirmed false-positive canonical packages; no SLOP; no [ASSUMED]/[SUS] install requiring a human-verify checkpoint |
</threat_model>

<verification>
- `rustc --version` / `cargo --version` succeed
- `pnpm install` + `tsc --noEmit` (apps/desktop) succeed
- `pnpm --filter ./apps/desktop exec vitest run` green (5 routes navigable)
- Human checkpoint: macOS Tauri window launches (native titlebar, dark, correct nav, lands on Rechnung)
</verification>

<success_criteria>
Dark Tauri window launches on macOS with the five-route sidebar shell, the sample-invoice index route renders through @clared/ui, monorepo is apps/desktop + packages/ui only, and the route test suite is green.
</success_criteria>

## Artifacts this phase produces (Plan 01-01)

Symbols/files created:
- `pnpm-workspace.yaml`, root `package.json`
- `apps/desktop/` Tauri app: `src-tauri/{Cargo.toml,tauri.conf.json,src/lib.rs}`, `vite.config.ts`, `vitest.config.ts`, `tsconfig.json`, `index.html`, `public/empty-state-hero.png` (placeholder)
- `apps/desktop/src/main.tsx` — dark boot
- `apps/desktop/src/App.tsx` — `createHashRouter`, `AppShell`, `NAV_ITEMS`
- `apps/desktop/src/data/sample-invoice.ts` — `StagedTaxDecision`, `LineItem`, `SampleInvoice`, `SAMPLE_INVOICE`
- `apps/desktop/src/routes/rechnung.tsx` — tracer render
- `apps/desktop/src/__tests__/routes.test.tsx`
- `packages/ui/` — `@clared/ui` barrel `src/index.ts`, `components.json`, `src/styles/globals.css`
- `apps/desktop/src/styles/globals.css` — Tailwind v4 dark tokens

<output>
Create `.planning/phases/01-tauri-desktop-mockup-first-ui/01-01-SUMMARY.md` when done.
</output>
