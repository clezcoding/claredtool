---
phase: 01-tauri-desktop-mockup-first-ui
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/desktop/public/empty-state-hero.png
  - .github/workflows/desktop-build.yml
autonomous: true
requirements: [UI-01, DESK-01]
user_setup:
  - service: higgsfield
    why: "Empty-state hero illustration is generated by the Higgsfield CLI (D-25); generation requires an authenticated session"
    dashboard_config:
      - task: "Authenticate the Higgsfield CLI (higgsfield account status / login) before running generation"
        location: "local machine — /opt/homebrew/bin/higgsfield"

estimate:
  tokens: 40000
  raw_tokens: 27000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The empty-state hero illustration at apps/desktop/public/empty-state-hero.png is generated with the higgsfield CLI (GPT Image 2 default), not a stock photo and not Cursor GenerateImage (D-25)"
    - "Higgsfield is NOT used for the invoice paper — the staged PDF stays HTML/CSS (D-26)"
    - "A GitHub Actions windows-latest job builds the Tauri app so DESK-01 Windows launch parity is verifiable without a local Windows machine (DESK-01, RESEARCH Pitfall 4)"
  artifacts:
    - "apps/desktop/public/empty-state-hero.png (real Higgsfield asset, replacing the 01-01 placeholder)"
    - ".github/workflows/desktop-build.yml (windows-latest + macos build job)"
  key_links:
    - "empty-state-hero.png is referenced by invoice-empty-state.tsx (01-02) — replacing the placeholder upgrades the empty state with no code change (D-24)"
    - "The Windows CI job runs pnpm install + Rust toolchain + tauri build on windows-latest — the only DESK-01 Windows evidence available on a macOS host"
  prohibitions:
    - "MUST NOT use stock photos or Cursor GenerateImage for the empty-state art — Higgsfield CLI only (D-25)"
    - "MUST NOT use Higgsfield for the invoice paper visual (D-26)"
  flagged_assumptions:
    - "[EDGE unresolved · DESK-01 unclassified] Windows launch is verified via CI build artifact only (no interactive Windows session on this host); a green windows-latest build is the DESK-01 Windows evidence. Carried as explicit assumption."
    - "[Higgsfield auth] If the Higgsfield session is unauthenticated at run time, the 01-01 placeholder PNG remains and UI work is unblocked; the real asset is generated once auth is resolved (RESEARCH Open Question 3)."
---

<objective>
Produce the two cross-cutting deliverables that stand apart from the screen code: the Higgsfield-generated empty-state hero illustration (D-25) and a GitHub Actions windows-latest build job that gives DESK-01 its Windows launch evidence on a macOS-only host.

Purpose: Close D-25 (real illustration) and the DESK-01 Windows gate.
Output: apps/desktop/public/empty-state-hero.png and .github/workflows/desktop-build.yml.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-tauri-desktop-mockup-first-ui/01-CONTEXT.md
@.planning/phases/01-tauri-desktop-mockup-first-ui/01-RESEARCH.md
@.planning/phases/01-tauri-desktop-mockup-first-ui/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the empty-state hero illustration with the Higgsfield CLI</name>
  <files>apps/desktop/public/empty-state-hero.png</files>
  <precondition>Higgsfield CLI is authenticated (`higgsfield account status` reports a signed-in session). If unauthenticated, keep the 01-01 placeholder PNG and flag the asset as pending — do not block the phase.</precondition>
  <read_first>
    - .planning/phases/01-tauri-desktop-mockup-first-ui/01-PATTERNS.md (Higgsfield empty-state pattern, auth check)
    - .planning/phases/01-tauri-desktop-mockup-first-ui/01-RESEARCH.md (Pattern 6, Open Question 3)
    - /Users/puzzless/.claude/skills/higgsfield-generate/SKILL.md (CLI workflow, GPT Image 2)
    - apps/desktop/src/components/invoice-empty-state.tsx (consumer — image path, from 01-02)
  </read_first>
  <action>Read and follow the higgsfield-generate skill. Check `higgsfield account status`; if authenticated, generate one dark-UI illustration with GPT Image 2 (a crisp B2B invoice document floating on deep navy, clean geometric shapes, subtle glow, professional/calm/dense, no people, no text — D-25) and save the returned image to apps/desktop/public/empty-state-hero.png, replacing the placeholder. Do NOT use stock photos or Cursor GenerateImage (D-25). Do NOT generate the invoice paper visual with Higgsfield (D-26). If unauthenticated, leave the placeholder in place and record the asset as pending in the SUMMARY (RESEARCH Open Question 3).</action>
  <verify>
    <automated>test -f apps/desktop/public/empty-state-hero.png && file apps/desktop/public/empty-state-hero.png | grep -qi png</automated>
  </verify>
  <acceptance_criteria>
    - apps/desktop/public/empty-state-hero.png exists and is a valid PNG
    - If Higgsfield auth succeeded, the file is the generated illustration (not the 1×1 placeholder); if it failed, the placeholder remains and the SUMMARY records the asset as pending
    - No stock-photo download and no Cursor GenerateImage output used for this asset
  </acceptance_criteria>
  <done>The empty-state hero PNG is present at the consumed path — the real Higgsfield illustration when authenticated, otherwise the placeholder with a pending note; invoice-empty-state renders it unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions windows-latest Tauri build (DESK-01 Windows gate)</name>
  <files>.github/workflows/desktop-build.yml</files>
  <read_first>
    - .planning/phases/01-tauri-desktop-mockup-first-ui/01-RESEARCH.md (Pitfall 4, Environment Availability — no Windows machine)
    - apps/desktop/src-tauri/tauri.conf.json (build config, from 01-01)
    - apps/desktop/package.json (scripts, from 01-01)
  </read_first>
  <action>Create .github/workflows/desktop-build.yml: on push/PR, a matrix (or two jobs) building the Tauri app on windows-latest and macos-latest. Each job: checkout, setup pnpm 11 + Node 26, install the Rust toolchain (dtolnay/rust-toolchain stable), `pnpm install`, then `pnpm --filter ./apps/desktop tauri build`. Upload the build artifact (Windows .msi/.exe under src-tauri/target/release/bundle) so the windows-latest job is the DESK-01 Windows launch evidence (RESEARCH Pitfall 4). Keep it minimal — one workflow file, no signing/secrets this phase.</action>
  <verify>
    <automated>test -f .github/workflows/desktop-build.yml && grep -q 'windows-latest' .github/workflows/desktop-build.yml && grep -q 'tauri build' .github/workflows/desktop-build.yml</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/desktop-build.yml exists
    - Workflow references runner `windows-latest` and runs a Rust toolchain setup + `tauri build`
    - Workflow uploads the Windows bundle as an artifact
    - Workflow is valid YAML (parses)
  </acceptance_criteria>
  <done>A windows-latest (and macos-latest) Tauri build workflow exists and uploads bundles — DESK-01 Windows parity is CI-verifiable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Higgsfield CLI → local filesystem | External CLI writes a PNG into public/; no app runtime integration |
| CI runner → build | GitHub Actions builds the Tauri bundle; no secrets/signing this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Tampering | Higgsfield-generated asset | low | accept | Asset is a static build-time illustration written to public/; no code execution, reviewed visually before commit |
| T-01-11 | Info Disclosure | GitHub Actions workflow | low | accept | No secrets or signing keys used this phase; public build steps only. Add signing secrets in a later phase |
| T-01-12 | Tampering | CI dependency install (pnpm/cargo on runners) | high | mitigate | Same Package Legitimacy Audit as 01-01 (RESEARCH) governs installed packages; pin toolchain action to a released tag |
</threat_model>

<verification>
- `test -f apps/desktop/public/empty-state-hero.png` (valid PNG)
- `.github/workflows/desktop-build.yml` present, references windows-latest + tauri build + artifact upload
- (CI) windows-latest job builds the Tauri bundle green — DESK-01 Windows evidence
</verification>

<success_criteria>
The empty-state hero is a real Higgsfield illustration (or documented-pending placeholder) at the consumed path, and a windows-latest CI build makes DESK-01 Windows launch parity verifiable without a local Windows machine.
</success_criteria>

## Artifacts this phase produces (Plan 01-04)

- `apps/desktop/public/empty-state-hero.png` — Higgsfield GPT Image 2 illustration (replaces 01-01 placeholder)
- `.github/workflows/desktop-build.yml` — windows-latest + macos-latest Tauri build + artifact upload

<output>
Create `.planning/phases/01-tauri-desktop-mockup-first-ui/01-04-SUMMARY.md` when done.
</output>
