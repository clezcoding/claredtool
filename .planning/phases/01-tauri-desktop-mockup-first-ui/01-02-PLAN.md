---
phase: 01-tauri-desktop-mockup-first-ui
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/desktop/src/routes/rechnung.tsx
  - apps/desktop/src/routes/pdf.tsx
  - apps/desktop/src/components/line-item-card.tsx
  - apps/desktop/src/components/tax-rail.tsx
  - apps/desktop/src/components/invoice-empty-state.tsx
  - apps/desktop/src/components/pdf-paper.tsx
  - apps/desktop/src/__tests__/invoice.test.tsx
autonomous: true
requirements: [UI-01]

estimate:
  tokens: 72000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Invoice workspace is a split: invoice form + line-item cards on the left, staged live-tax rail on the right (D-14)"
    - "Line items render as one compact card per line showing Bezeichnung · Menge · Einzelpreis · Netto, all fields always visible, no per-line tax rate (D-17, D-18, D-19)"
    - "„+ Position“ control sits at the bottom of the line-item list and a small X reveals on card hover to delete (D-20, D-21)"
    - "Right rail „Live Steuerberechnung“ shows staged TaxDecision-shaped fields — rate, reverse charge, legal text, applied_rule_id (D-15)"
    - "A mini PDF paper peek („Vorschau“) sits under the tax rail; clicking it navigates to /pdf (no modal) (D-16)"
    - "„Neue Rechnung“ in the invoice header toggles locally to the empty state; a further action returns to the sample; no persist (D-22, D-23)"
    - "Empty state is a large Higgsfield illustration + marketing copy (heading „Noch keine Rechnung erstellt“, the 2-minute body copy), not a blank form or short hint (D-24)"
    - "The /pdf route shows one centered light paper on a dark stage (frame + shadow), rendered HTML/CSS from SAMPLE_INVOICE so numbers stay consistent; content is not inverted and the app theme stays dark (D-26, D-27, D-28)"
    - "Tax and PDF are staged only — no tax engine, no real PDF file, no invented tax math (D-13)"
    - "[UI-SPEC UI-Considerations] populated E2 = compact 4-field cards on the one sample invoice; overflow E2 = left panel scrolls when cards exceed the pane; overflow E5 = tax rail scrolls if legal text overflows; long-text = Bezeichnung/values/legal text wrap (no ellipsis); zero-one-many E2 = zero uses empty state, one/many use cards + „+ Position“ (all covered)"
    - "[UI-SPEC UI-Considerations] loading/error are demo surfaces only (skeleton/spinner; error copy „Ein Fehler ist aufgetreten…“) — refines D-11, no real network"
  artifacts:
    - "apps/desktop/src/routes/rechnung.tsx (split layout host)"
    - "apps/desktop/src/components/line-item-card.tsx"
    - "apps/desktop/src/components/tax-rail.tsx (+ PDF peek link)"
    - "apps/desktop/src/components/invoice-empty-state.tsx"
    - "apps/desktop/src/routes/pdf.tsx + apps/desktop/src/components/pdf-paper.tsx"
    - "apps/desktop/src/__tests__/invoice.test.tsx"
  key_links:
    - "rechnung.tsx and pdf.tsx both read SAMPLE_INVOICE from data/sample-invoice.ts (01-01) — the peek and full page render the SAME numbers (D-16, D-26)"
    - "PDF peek → react-router navigation to /pdf route registered in App.tsx (01-01), NOT a modal (D-16)"
    - "invoice-empty-state references /empty-state-hero.png (placeholder from 01-01, real asset from 01-04) — path resolves regardless of asset source"
  prohibitions:
    - "MUST NOT present staged tax or the mock PDF as real output — the tax rail shows hardcoded TaxDecision-shaped data and the PDF is HTML/CSS, never a generated file or computed math (transparency; D-13, D-26)"
    - "MUST NOT invert PDF paper content or switch the /pdf screen to a light app theme (D-27)"
    - "MUST NOT use PDF.js or an embedded PDF binary renderer — the paper is HTML/CSS from sample data (D-26)"
    - "MUST NOT persist invoice edits — „+ Position“, delete, and „Neue Rechnung“ toggle are local React state only (D-13, D-23)"
  flagged_assumptions:
    - "[EDGE unresolved · UI-01 adjacency] Whether two equal/adjacent line items merge, collide, or separate is unspecified — Phase 1 renders each line item independently with no merge/collision logic (collision logic is CONTEXT-deferred, do not invent). Carried as assumption."
    - "[EDGE unresolved · UI-01 ordering] Stable output order for equal-comparing elements is unspecified — line items render in SAMPLE_INVOICE array order with no sorting. Carried as assumption."
---

<objective>
Expand the tracer's Rechnung route into the full invoice canvas — the two-minute-loop centerpiece: split form/rail layout, compact line-item cards, staged live-tax rail, PDF peek, the „Neue Rechnung“ empty-state toggle — and build the full /pdf paper route. All data flows from SAMPLE_INVOICE; nothing persists; tax and PDF stay staged.

Purpose: Deliver the invoice → live-tax → PDF loop as a clickable mockup (UI-01, the core value).
Output: A fully interactive (local-state) Rechnung screen and a light-paper-on-dark /pdf screen.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-tauri-desktop-mockup-first-ui/01-CONTEXT.md
@.planning/phases/01-tauri-desktop-mockup-first-ui/01-UI-SPEC.md
@.planning/phases/01-tauri-desktop-mockup-first-ui/01-PATTERNS.md
@apps/desktop/src/data/sample-invoice.ts
@apps/desktop/src/routes/rechnung.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Invoice form left panel — line-item cards, „+ Position“, hover-delete, „Neue Rechnung“ empty-state toggle</name>
  <files>apps/desktop/src/routes/rechnung.tsx, apps/desktop/src/components/line-item-card.tsx, apps/desktop/src/components/invoice-empty-state.tsx, apps/desktop/src/__tests__/invoice.test.tsx</files>
  <read_first>
    - apps/desktop/src/data/sample-invoice.ts (LineItem, SAMPLE_INVOICE)
    - apps/desktop/src/routes/rechnung.tsx (tracer render from 01-01)
    - .planning/phases/01-tauri-desktop-mockup-first-ui/01-PATTERNS.md (LineItemCard component, rechnung.tsx split-layout rule, entities/kunden disabled-button copy)
    - .planning/phases/01-tauri-desktop-mockup-first-ui/01-UI-SPEC.md (Copywriting Contract, UI Considerations)
  </read_first>
  <behavior>
    - Rendering SAMPLE_INVOICE shows one line-item card per line with Bezeichnung, Menge, Einzelpreis, Netto all visible
    - Clicking „Neue Rechnung“ replaces the form with the empty state (empty heading text present); a return action restores the sample cards
    - „+ Position“ control is present at the bottom of the list; hover reveals a delete X on a card
  </behavior>
  <action>Restructure routes/rechnung.tsx into the left panel of a split layout: an invoice header with form labels Rechnungsnummer · Datum · Fällig (editable inputs bound to local React state, no persist — D-13) and a „Neue Rechnung“ button (accent CTA) that toggles a local boolean between the filled sample and the empty state. Create components/line-item-card.tsx rendering the four LineItem fields in a compact card, all fields always visible (no accordion, no dialog — D-19), with a hover-revealed X delete control labelled „Position löschen“ (D-21) and no per-line tax rate (D-18). Drive the list from useState(SAMPLE_INVOICE.lineItems); render a „+ Position“ control at the list bottom that appends a blank/local visual card (D-20; may be a visible no-op per Claude discretion). Create components/invoice-empty-state.tsx: a large image referencing /empty-state-hero.png plus the UI-SPEC empty heading „Noch keine Rechnung erstellt“ and the 2-minute marketing body copy (D-24). Keep density dense-but-calm (D-08); left panel scrolls on overflow (UI-SPEC overflow E2). Use @clared/ui components; German copy verbatim from the Copywriting Contract.</action>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec vitest run --reporter=verbose apps/desktop/src/__tests__/invoice.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - invoice.test.tsx asserts N line-item cards for SAMPLE_INVOICE.lineItems.length and each shows all four fields
    - Test asserts clicking „Neue Rechnung“ renders the empty-state heading „Noch keine Rechnung erstellt“ and hides the sample cards
    - line-item-card.tsx renders no tax-rate field (grep: card component has no per-line rate)
    - „+ Position“ control present at list bottom (queryable by its „+ Position“ label)
    - `vitest run` for invoice.test.tsx exits 0
  </acceptance_criteria>
  <done>Left panel renders compact line-item cards from SAMPLE_INVOICE, „+ Position“ adds a local card, hover-X deletes, and „Neue Rechnung“ toggles the Higgsfield-hero empty state — all local state, tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Staged live-tax rail (right) + PDF peek under the rail</name>
  <files>apps/desktop/src/components/tax-rail.tsx, apps/desktop/src/routes/rechnung.tsx, apps/desktop/src/__tests__/invoice.test.tsx</files>
  <read_first>
    - apps/desktop/src/data/sample-invoice.ts (StagedTaxDecision, SAMPLE_INVOICE.taxDecision)
    - apps/desktop/src/routes/rechnung.tsx (Task 1 left panel)
    - .planning/phases/01-tauri-desktop-mockup-first-ui/01-UI-SPEC.md (Copywriting Contract: Tax rail heading, PDF peek label; Color)
    - docs/clared-tax-engine-architecture.md (lines 39-47 canonical fields)
  </read_first>
  <behavior>
    - Right rail „Live Steuerberechnung“ shows the staged TaxDecision fields (rate, reverse charge, legal reference, applied_rule_id) from SAMPLE_INVOICE.taxDecision
    - A PDF peek labelled „Vorschau“ sits under the rail; activating it navigates to /pdf
  </behavior>
  <action>Create components/tax-rail.tsx as the right column of rechnung.tsx's split layout (D-14): heading „Live Steuerberechnung“ (D-15), rendering the staged fields from SAMPLE_INVOICE.taxDecision — invoice_tax_rate, reverse_charge_flag, legal_reference, applied_rule_id (canonical names). Data is document-level, read-only, hardcoded — no evaluate(), no computed math (D-13). The rail scrolls if legal text overflows (UI-SPEC overflow E5); legal text wraps, no ellipsis (long-text). Under the rail add a mini PDF paper peek labelled „Vorschau“ (D-16) that is a react-router Link/navigate to /pdf — NOT a modal, NOT a real file. Wire tax-rail into rechnung.tsx so the screen is form-left / rail-right. Use accent color only per the UI-SPEC reservation.</action>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec vitest run --reporter=verbose apps/desktop/src/__tests__/invoice.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tax-rail.tsx renders the heading „Live Steuerberechnung“ and the four staged fields sourced from SAMPLE_INVOICE.taxDecision
    - tax-rail reads canonical field names (invoice_tax_rate, reverse_charge_flag, legal_reference, applied_rule_id); no field named `rate`/`legal_text`
    - PDF peek is labelled „Vorschau“ and is a route navigation to /pdf, not a dialog/modal component
    - invoice.test.tsx asserts rail heading + a staged field render; `vitest run` exits 0
  </acceptance_criteria>
  <done>Rechnung is a form-left / staged-tax-rail-right split with the „Vorschau“ PDF peek linking to /pdf; tax data is hardcoded TaxDecision-shaped, tests green.</done>
</task>

<task type="auto">
  <name>Task 3: /pdf route — light paper on dark stage, HTML/CSS from SAMPLE_INVOICE</name>
  <files>apps/desktop/src/routes/pdf.tsx, apps/desktop/src/components/pdf-paper.tsx, apps/desktop/src/__tests__/invoice.test.tsx</files>
  <read_first>
    - apps/desktop/src/data/sample-invoice.ts (SAMPLE_INVOICE)
    - apps/desktop/src/App.tsx (pdf route registration from 01-01)
    - .planning/phases/01-tauri-desktop-mockup-first-ui/01-PATTERNS.md (pdf.tsx paper mock pattern)
    - .planning/phases/01-tauri-desktop-mockup-first-ui/01-CONTEXT.md (D-26, D-27, D-28)
  </read_first>
  <action>Replace the /pdf placeholder with routes/pdf.tsx + components/pdf-paper.tsx: one centered light page (white background, A4-ish proportions, frame + shadow) on the dark app stage (D-27, D-28), rendering SAMPLE_INVOICE fields (seller, buyer, line items, netto/brutto totals, TaxDecision legal_reference text block) as HTML/CSS so numbers match the invoice screen exactly (D-26). No two-page spread, no full-bleed. Optional decorative-only zoom controls below the paper (Claude discretion). Do NOT use PDF.js, do NOT invert content, do NOT switch the app theme to light (D-26, D-27).</action>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec vitest run --reporter=verbose apps/desktop/src/__tests__/invoice.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - pdf-paper.tsx renders SAMPLE_INVOICE seller name, buyer name, and the same totals shown on the invoice screen
    - No import of pdfjs/pdf.js anywhere in pdf.tsx or pdf-paper.tsx
    - Paper element uses a light background inside a dark stage container (app root keeps the `dark` class)
    - invoice.test.tsx asserts the /pdf paper renders the sample invoice number; `vitest run` exits 0
  </acceptance_criteria>
  <done>/pdf shows one centered light HTML/CSS paper on the dark stage with numbers consistent with the invoice screen; no PDF binary, no theme switch; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (mock form fields) → React state | Editable invoice fields; no persist, no network, no eval |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | Mock form inputs (rechnung form, „+ Position“) | low | accept | Local React state only, no persist and no injection sink; number inputs validated client-side for UX only (RESEARCH V5 minimal) |
| T-01-05 | Info Disclosure | Staged tax/PDF surfaces | low | mitigate | Staged data is hardcoded sample only; no real user/financial data present; surfaces read as mocks (transparency prohibition) |
| T-01-06 | Tampering | Tauri IPC / commands | low | accept | No Tauri commands added; IPC surface remains zero this phase |
</threat_model>

<verification>
- `pnpm --filter ./apps/desktop exec vitest run` green (invoice.test.tsx: cards, empty toggle, tax rail, PDF paper)
- Manual (folds into 01-01 macOS checkpoint / phase verify): navigate Rechnung → „Vorschau“ → /pdf; toggle „Neue Rechnung“ → empty state → back
</verification>

<success_criteria>
The invoice → live-tax → PDF loop is a clickable mockup: split form/rail Rechnung with compact cards, staged „Live Steuerberechnung“ rail, „Vorschau“ peek to a light-paper /pdf, and a Higgsfield-hero empty state — all local state, all numbers consistent, tests green.
</success_criteria>

## Artifacts this phase produces (Plan 01-02)

- `apps/desktop/src/routes/rechnung.tsx` — split-layout host (form + rail)
- `apps/desktop/src/components/line-item-card.tsx` — `LineItemCard`
- `apps/desktop/src/components/tax-rail.tsx` — `TaxRail` + PDF peek
- `apps/desktop/src/components/invoice-empty-state.tsx` — `InvoiceEmptyState`
- `apps/desktop/src/routes/pdf.tsx` + `apps/desktop/src/components/pdf-paper.tsx` — `PdfPaper`
- `apps/desktop/src/__tests__/invoice.test.tsx`

<output>
Create `.planning/phases/01-tauri-desktop-mockup-first-ui/01-02-SUMMARY.md` when done.
</output>
