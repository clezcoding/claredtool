---
phase: 04.6-takumi-pdfcn-pdf-engine-cutover
plan: 04
type: execute
wave: 4
depends_on:
  - 04.6-03
files_modified:
  - apps/backend/Dockerfile
  - .github/workflows/backend-image.yml
  - .github/workflows/ci.yml
  - .github/scripts/coolify-deploy-nest.sh
  - .github/scripts/coolify-deploy-nest.test.sh
autonomous: true
requirements:
  - PDF-01
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "API image COPY+builds @clared/pdf-templates; takumi-pdf wasm remains in node_modules (not pruned, not esbuild-inlined) (D-09, SC-6)"
    - "Dockerfile has no worker stage; CMD stays prisma migrate + node dist/main.js (D-09)"
    - "GHA backend-image and coolify-deploy-nest.sh are API-only: no worker tag, no worker Trivy/SBOM/cosign, no COOLIFY_APP_WORKER (D-09; 04.5 D-03 worker half superseded)"
    - "ci.yml installs/builds/tests @clared/pdf-templates; path filter includes packages/pdf-templates/** (SC-6, SC-8)"
  artifacts:
    - apps/backend/Dockerfile
    - .github/workflows/backend-image.yml
    - .github/workflows/ci.yml
    - .github/scripts/coolify-deploy-nest.sh
    - .github/scripts/coolify-deploy-nest.test.sh
  key_links:
    - "Dockerfile COPY packages/pdf-templates analog tax-engine COPY block (lines 17-26)"
    - "coolify-deploy-nest.sh pins API UUID only; /health/ready poll stays (D-25)"
    - "backend-image.yml paths include packages/pdf-templates/** next to packages/tax-engine/**"
  assumptions:
    - "WASM path is node_modules/takumi-pdf/pkg/*.wasm inside the API image; Nest tsc does not copy wasm into dist/"
  prohibitions:
    - statement: "Do not reintroduce a worker image tag or COOLIFY_APP_WORKER (D-09)."
      status: unverified
    - statement: "Do not esbuild-flatten Nest with takumi-pdf inlined (ENOENT wasm)."
      status: unverified
    - statement: "Do not prune node_modules/takumi-pdf from the API image."
      status: unverified
    - statement: "Do not delete Coolify apps this plan (04.6-05)."
      status: unverified
    - statement: "Do not add a public generate HTTP route (D-04)."
      status: unverified
---

<objective>
API-only image + CI: COPY pdf-templates, wasm in the API image, drop worker stage and GHA worker deploy (D-09).

Purpose: Coolify API can run Takumi without a worker tag; 04.5 API-then-worker order is superseded.

Output: Dockerfile, ci.yml, backend-image.yml, coolify-deploy-nest.sh + mock tests API-only.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.6-takumi-pdfcn-pdf-engine-cutover/04.6-RESEARCH.md
@.planning/phases/04.6-takumi-pdfcn-pdf-engine-cutover/04.6-PATTERNS.md
@.planning/phases/04.6-takumi-pdfcn-pdf-engine-cutover/04.6-03-SUMMARY.md
@apps/backend/Dockerfile
@.github/workflows/backend-image.yml
@.github/workflows/ci.yml
@.github/scripts/coolify-deploy-nest.sh
@.github/scripts/coolify-deploy-nest.test.sh

<interfaces>
Dockerfile tax-engine analog (apps/backend/Dockerfile 17-26): COPY package.json, pnpm install --filter, COPY sources, pnpm --filter build. Duplicate for @clared/pdf-templates before nest build. Keep FROM build AS api CMD migrate + node dist/main.js (lines 56-60). Delete FROM build AS worker stage (lines 51-54). Plan 03 already dropped the dist/worker.js assert.

ci.yml backend path group (lines 72-76) plus install lines 211-213, 275-277: add packages/pdf-templates/** and --filter @clared/pdf-templates... plus build and test. Jest: exec jest not test -- --testPathPattern (ci.yml comment 224-226).

backend-image.yml paths 21-23: add packages/pdf-templates/**. Delete push-worker job (~148-215), needs push-worker, worker Trivy/SBOM, WORKER_UUID / COOLIFY_APP_WORKER (~347-356). Rewrite header comment to API-only.

coolify-deploy-nest.sh: drop test -n WORKER_UUID and every coolify_pin of SHA_TAG-worker. Keep API pin + /health/ready poll (D-25).
coolify-deploy-nest.test.sh: rewrite API-only; fail if script pins a worker tag.
</interfaces>
</context>

<!-- planner-discipline-allow: COOLIFY_APP_WORKER -->
<!-- planner-discipline-allow: WORKER_UUID -->
<!-- planner-discipline-allow: push-worker -->
<!-- planner-discipline-allow: FROM build AS worker -->
<!-- planner-discipline-allow: dist/worker.js -->

<tasks>

<task type="auto">
  <name>Dockerfile COPY pdf-templates; drop worker stage (D-09)</name>
  <files>apps/backend/Dockerfile</files>
  <read_first>
    - apps/backend/Dockerfile (tax-engine COPY 17-26; worker stage 51-54; api stage 56-60)
    - packages/pdf-templates/package.json (must exist from 04.6-01)
  </read_first>
  <precondition>packages/pdf-templates/package.json exists from 04.6-01; apps/backend/src/worker.ts is already deleted from 04.6-03</precondition>
  <reversibility rating="costly">D-09 image layout: wasm lives in API node_modules; undoing means a new worker stage and a second tag.</reversibility>
  <action>
    Duplicate the tax-engine COPY/install/build block for packages/pdf-templates: COPY packages/pdf-templates/package.json before frozen install; add --filter @clared/pdf-templates... to the install line alongside tax-engine and backend; COPY packages/pdf-templates; pnpm --filter @clared/pdf-templates build before nest build. Do not prune takumi-pdf from node_modules. Delete the entire FROM build AS worker stage. Keep FROM build AS api CMD prisma migrate deploy && node dist/main.js. Do not esbuild-bundle Nest. Optional: if docker is available, docker build --target api and confirm node_modules/takumi-pdf/pkg has a wasm file; if docker is missing, skip image smoke and rely on the COPY grep.
  </action>
  <acceptance_criteria>
    - grep pdf-templates apps/backend/Dockerfile
    - grep -F '@clared/pdf-templates' apps/backend/Dockerfile
    - ! grep -F 'FROM build AS worker' apps/backend/Dockerfile
    - grep -F 'FROM build AS api' apps/backend/Dockerfile
  </acceptance_criteria>
  <verify>
    <automated>grep -F 'pdf-templates' apps/backend/Dockerfile && grep -F '@clared/pdf-templates' apps/backend/Dockerfile && ! grep -F 'FROM build AS worker' apps/backend/Dockerfile && grep -F 'FROM build AS api' apps/backend/Dockerfile && ! grep -F 'dist/worker.js' apps/backend/Dockerfile</automated>
    <fails_when>non-zero exit, or worker stage still in Dockerfile, or pdf-templates COPY/filter missing</fails_when>
  </verify>
  <done>API image builds pdf-templates; worker stage gone; wasm package left in node_modules (D-09).</done>
</task>

<task type="auto">
  <name>ci.yml and backend-image.yml API-only + pdf-templates filter (D-09)</name>
  <files>.github/workflows/ci.yml, .github/workflows/backend-image.yml</files>
  <read_first>
    - .github/workflows/ci.yml (backend/tax-engine path groups 72-76; install 211-213, 275-277; jest exec comment 224-226; backend-e2e 275)
    - .github/workflows/backend-image.yml (paths 21-23; push-worker ~148; COOLIFY_APP_WORKER ~347-356; coolify-deploy-nest.test.sh step ~75)
  </read_first>
  <reversibility rating="costly">D-09 GHA contract: dropping push-worker is the published-tag change; restoring needs a new job and GHCR tag.</reversibility>
  <action>
    Add packages/pdf-templates/** to ci.yml backend and/or a pdf-templates path group; include that output in backend-unit and backend-e2e if conditions analog tax-engine. Add --filter @clared/pdf-templates... to frozen installs; pnpm --filter @clared/pdf-templates build (and test) before or beside tax-engine. Keep exec jest (not test -- --testPathPattern).

    backend-image.yml: add packages/pdf-templates/** to on.push paths. Delete push-worker job, needs push-worker, worker digest/Trivy/SBOM/cosign, WORKER_UUID and COOLIFY_APP_WORKER env. Rewrite the file header comment from API-then-worker to API-only. Keep deploy-script-test running bash .github/scripts/coolify-deploy-nest.test.sh (rewritten in the next task — order within this plan: this task may still see the old test until task 3; if the workflow would fail mid-plan, do task 3 first or keep the workflow job but this task only drops worker jobs). Prefer completing task 3's script rewrite in the same working tree before pushing.
  </action>
  <acceptance_criteria>
    - .github/workflows/ci.yml mentions packages/pdf-templates
    - .github/workflows/ci.yml mentions @clared/pdf-templates
    - .github/workflows/backend-image.yml mentions packages/pdf-templates
    - .github/workflows/backend-image.yml has no push-worker job id
    - .github/workflows/backend-image.yml has no COOLIFY_APP_WORKER
  </acceptance_criteria>
  <verify>
    <automated>grep -F 'packages/pdf-templates' .github/workflows/ci.yml && grep -F '@clared/pdf-templates' .github/workflows/ci.yml && grep -F 'packages/pdf-templates' .github/workflows/backend-image.yml && ! grep -F 'push-worker' .github/workflows/backend-image.yml && ! grep -F 'COOLIFY_APP_WORKER' .github/workflows/backend-image.yml</automated>
    <fails_when>non-zero exit, or push-worker / COOLIFY_APP_WORKER still in backend-image.yml, or pdf-templates path/filter missing in ci.yml</fails_when>
  </verify>
  <done>CI builds pdf-templates; GHCR/deploy no longer publish a worker tag (D-09).</done>
</task>

<task type="auto">
  <name>coolify-deploy-nest.sh API-only + rewrite mock tests (D-09)</name>
  <files>.github/scripts/coolify-deploy-nest.sh, .github/scripts/coolify-deploy-nest.test.sh</files>
  <read_first>
    - .github/scripts/coolify-deploy-nest.sh (lines 1-13 require WORKER_UUID; worker pin ~190)
    - .github/scripts/coolify-deploy-nest.test.sh (WORKER_UUID export line 19; worker pin assertions 82-91, 311-318)
  </read_first>
  <reversibility rating="costly">D-09 deploy script: 04.5 API-then-worker order removed; restoring needs the worker UUID pin again.</reversibility>
  <action>
    Drop WORKER_UUID requirement and every coolify_pin of ${SHA_TAG}-worker. Keep API pin and /health/ready poll (valid without a Chromium probe, D-25). Rewrite coolify-deploy-nest.test.sh API-only: do not export WORKER_UUID; assert the script does not pin a worker tag; fail the mock test if COOLIFY_APP_WORKER or SHA_TAG-worker pin appears in the script under test. Keep bash .github/scripts/coolify-deploy-nest.test.sh as the proven command shape from 04.5.
  </action>
  <acceptance_criteria>
    - bash .github/scripts/coolify-deploy-nest.test.sh exits 0
    - ! grep -F WORKER_UUID .github/scripts/coolify-deploy-nest.sh
    - ! grep -F COOLIFY_APP_WORKER .github/scripts/coolify-deploy-nest.sh
  </acceptance_criteria>
  <verify>
    <automated>bash .github/scripts/coolify-deploy-nest.test.sh && ! grep -F WORKER_UUID .github/scripts/coolify-deploy-nest.sh && ! grep -F 'COOLIFY_APP_WORKER' .github/scripts/coolify-deploy-nest.sh && ! grep -F -- '-worker' .github/scripts/coolify-deploy-nest.sh</automated>
    <fails_when>non-zero exit from the mock test, or WORKER_UUID / COOLIFY_APP_WORKER / -worker still in coolify-deploy-nest.sh</fails_when>
  </verify>
  <done>Deploy script and mocks are API-only; 04.5 worker half superseded (D-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dockerfile → GHCR API image | wasm + templates must land or Coolify render ENOENT |
| GHA secrets → Coolify | API UUID only; worker UUID unused |
| ci.yml path filters → tests | template-only PRs must still run pdf-templates tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.6-13 | Tampering | missing wasm in image | high | mitigate | COPY pdf-templates; do not prune takumi-pdf; optional docker --target api smoke |
| T-04.6-14 | Elevation of Privilege | leftover worker deploy | high | mitigate | Delete push-worker and COOLIFY_APP_WORKER; mock test fails on worker pin |
| T-04.6-SC | Tampering | npm/pip/cargo installs | high | accept | No new registry packages this plan |
</threat_model>

<verification>
Dockerfile copies pdf-templates, no worker stage. ci.yml + backend-image.yml API-only with pdf-templates paths. bash .github/scripts/coolify-deploy-nest.test.sh passes.
</verification>

<success_criteria>
API image and GHA deploy Takumi without a worker tag (D-09, SC-6, SC-7).
</success_criteria>

## Artifacts this phase produces

- `apps/backend/Dockerfile` — pdf-templates COPY/build; no worker stage
- `.github/workflows/ci.yml` — pdf-templates path + filter + test
- `.github/workflows/backend-image.yml` — API-only
- `.github/scripts/coolify-deploy-nest.sh` — API pin + ready poll
- `.github/scripts/coolify-deploy-nest.test.sh` — API-only mocks

<output>
Create `.planning/phases/04.6-takumi-pdfcn-pdf-engine-cutover/04.6-04-SUMMARY.md` when done
</output>
