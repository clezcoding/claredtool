---
phase: 04.6-takumi-pdfcn-pdf-engine-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/pdf-templates/package.json
  - packages/pdf-templates/tsconfig.json
  - packages/pdf-templates/jest.config.cjs
  - packages/pdf-templates/components.json
  - packages/pdf-templates/src/index.ts
  - packages/pdf-templates/src/invoice-minimal.tsx
  - packages/pdf-templates/src/render-invoice.ts
  - packages/pdf-templates/src/render-invoice.spec.ts
  - packages/pdf-templates/fonts
  - pnpm-lock.yaml
autonomous: false
requirements:
  - PDF-01
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Human confirmed npmjs identity for takumi-pdf 0.14.2 and @takumi-rs/helpers 2.13.6 before install (D-08, Package Legitimacy SUS too-new)"
    - "renderInvoice(InvoiceModel + TaxDecision, locale de, vatLine omit) returns PdfBytes whose first four bytes are %PDF- for D-23 fixture 1 DE B2B 19% tax shown (D-01, PDF-01)"
    - "Page size is a4; fonts are local WOFF2 Inter + Instrument Serif bytes; no remote font fetch (D-01, D-07)"
    - "Workspace package @clared/pdf-templates emits CJS dist with jsx react-jsx; Nest tsconfig stays without jsx (D-01)"
    - "No public generate HTTP route added (D-04)"
  artifacts:
    - packages/pdf-templates/package.json
    - packages/pdf-templates/src/render-invoice.ts
    - packages/pdf-templates/src/invoice-minimal.tsx
    - packages/pdf-templates/src/render-invoice.spec.ts
    - packages/pdf-templates/fonts
  key_links:
    - "Fixture 1 facts → invoice-minimal JSX → takumi-pdf render() → Uint8Array magic %PDF-"
    - "fonts/ WOFF2 files → render options fonts[{ name, data }] via path.join(__dirname, '../fonts')"
  assumptions:
    - "empty/null input: fail-closed throw (D-26); never return empty PdfBytes — asserted on expansion in 04.6-03"
    - "PDF identity is the byte prefix 25 50 44 46 2D, not Unicode/NFC equality"
    - "concurrency: in-process sync call; one module-level PdfRenderer; no queue; event-loop serializes overlapping calls"
  prohibitions:
    - statement: "Do not add npm packages named in RESEARCH Do not install table (D-08)."
      status: unverified
    - statement: "Do not set archival-profile, file-embed, or UA-tag render keys (D-05, D-06)."
      status: unverified
    - statement: "Do not add a public generate HTTP route (D-04)."
      status: unverified
    - statement: "Do not add jsx to apps/backend/tsconfig.json."
      status: unverified
    - statement: "Do not edit apps/backend/prisma/schema.prisma (D-20)."
      status: unverified
    - statement: "Do not rewrite 04.3 PLAN or SUMMARY files (D-10)."
      status: unverified
    - statement: "Do not add desktop download/view, AUDT-01, OFFL-01, Factur-X/XRechnung embed, Forme registry, or a mail-with-PDF queue."
      status: unverified
---

<objective>
Wave 1: supply-chain pin then tracer — InvoiceModel + TaxDecision → %PDF- bytes via Takumi + pdfcn Invoice Minimal for D-23 fixture 1 (DE B2B 19%).

Purpose: Prove the in-process WASM path before Nest cutover or Gotenberg deletion (D-01, D-08).

Output: @clared/pdf-templates package, vendored fonts, one green fixture render.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.6-takumi-pdfcn-pdf-engine-cutover/04.6-CONTEXT.md
@.planning/phases/04.6-takumi-pdfcn-pdf-engine-cutover/04.6-RESEARCH.md
@.planning/phases/04.6-takumi-pdfcn-pdf-engine-cutover/04.6-PATTERNS.md
@.planning/phases/04.6-takumi-pdfcn-pdf-engine-cutover/04.6-VALIDATION.md
@.planning/phases/04.6-takumi-pdfcn-pdf-engine-cutover/04.6-COVERAGE.md
@packages/tax-engine/package.json
@packages/tax-engine/tsconfig.json
@packages/tax-engine/jest.config.cjs
@packages/ui/tsconfig.json
@packages/ui/components.json
@apps/backend/src/pdf/pdf.contract.ts
@apps/backend/src/pdf/gotenberg.client.ts
@packages/tax-engine/src/schema.ts
@apps/backend/prisma/schema.prisma
@apps/desktop/src/styles/globals.css

<interfaces>
PdfBytes (keep; from apps/backend/src/pdf/pdf.contract.ts): bytes Uint8Array, contentType "application/pdf".

TaxDecision (packages/tax-engine/src/schema.ts): invoice_tax_rate, invoice_tax_shown, reverse_charge_flag, legal_reference, plus audit fields that must not print (D-12).

Prisma fields available (do not add columns, D-20): Entity.name/address/vatId/country/legalForm; Customer.name/address/vatId/country; Invoice.number/date/dueDate; InvoiceItem.bezeichnung/menge/einzelpreis/netto.

renderInvoice (this plan creates): async (props) => Uint8Array. Options: size "a4", fonts local bytes, footer PageNumber/TotalPages. Module-level PdfRenderer (discretion).

D-23 fixture 1: locale de, DE seller+buyer B2B, invoice_tax_shown true, rate 19, issued-looking number/date/dueDate.
</interfaces>
</context>

<!-- planner-discipline-allow: "takumi" -->
<!-- planner-discipline-allow: "pdfcn" -->
<!-- planner-discipline-allow: "forme" -->
<!-- planner-discipline-allow: "@takumi-rs/core" -->
<!-- planner-discipline-allow: "takumi": -->
<!-- planner-discipline-allow: "pdfcn": -->
<!-- planner-discipline-allow: "forme": -->
<!-- planner-discipline-allow: "@takumi-rs/core": -->
<!-- planner-discipline-allow: googleFonts -->
<!-- planner-discipline-allow: factur-x.xml -->

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Verify SUS renderer pin on npmjs (D-08)</name>
  <read_first>
    - .planning/phases/04.6-takumi-pdfcn-pdf-engine-cutover/04.6-RESEARCH.md (Package Legitimacy Audit — takumi-pdf and @takumi-rs/helpers SUS too-new)
    - https://www.npmjs.com/package/takumi-pdf
    - https://www.npmjs.com/package/@takumi-rs/helpers
  </read_first>
  <reversibility rating="costly">D-25-adjacent: pinning 0.14.2 is the runtime WASM contract; bumping later is coordinated but not a Coolify delete.</reversibility>
  <what-built>Supply-chain gate before D-01 renderer enters the workspace. No install yet.</what-built>
  <how-to-verify>
    Open npmjs.com/package/takumi-pdf and npmjs.com/package/@takumi-rs/helpers. Confirm version 0.14.2 / 2.13.6, repo github.com/kane50613/takumi, no postinstall. Do not reopen Gotenberg vs Takumi (D-01 locked). React 19.1.0 is OK (mature).
  </how-to-verify>
  <action>
    Human verifies registry identity for the RESEARCH-pinned versions. Do not run pnpm add until resume signal. Do not install names from the RESEARCH Do not install table.
  </action>
  <acceptance_criteria>
    - npm view takumi-pdf@0.14.2 version prints 0.14.2
    - npm view @takumi-rs/helpers@2.13.6 version prints 2.13.6
    - packages/pdf-templates/package.json either absent or has no takumi-pdf dependency yet
  </acceptance_criteria>
  <verify>
    <automated>npm view takumi-pdf@0.14.2 version && npm view @takumi-rs/helpers@2.13.6 version && (test ! -f packages/pdf-templates/package.json || node -e "const p=require('./packages/pdf-templates/package.json'); if (p.dependencies && p.dependencies['takumi-pdf']) process.exit(1)")</automated>
    <fails_when>non-zero exit, or printed versions are not 0.14.2 and 2.13.6, or takumi-pdf already listed in packages/pdf-templates/package.json</fails_when>
    <human-check>Confirm npmjs publisher/repo and no postinstall on both SUS packages; type packages verified.</human-check>
  </verify>
  <resume-signal>Type "packages verified" to install 0.14.2 / 2.13.6, or name a blocked package with evidence</resume-signal>
  <done>Human accepted the RESEARCH pin; tracer may install (D-08).</done>
</task>

<task type="tracer">
  <name>End-to-end fixture 1 — InvoiceModel+TaxDecision to %PDF- via Takumi Invoice Minimal</name>
  <files>packages/pdf-templates/package.json, packages/pdf-templates/tsconfig.json, packages/pdf-templates/jest.config.cjs, packages/pdf-templates/components.json, packages/pdf-templates/src/index.ts, packages/pdf-templates/src/invoice-minimal.tsx, packages/pdf-templates/src/render-invoice.ts, packages/pdf-templates/src/render-invoice.spec.ts, packages/pdf-templates/fonts, pnpm-lock.yaml</files>
  <read_first>
    - packages/tax-engine/package.json (workspace skeleton: name, private, main dist/index.js, test jest)
    - packages/tax-engine/tsconfig.json (CJS emit; add only jsx react-jsx from packages/ui/tsconfig.json line 7)
    - packages/tax-engine/jest.config.cjs (extend moduleFileExtensions with tsx; transform .tsx)
    - packages/ui/components.json (retarget aliases into packages/pdf-templates/src)
    - apps/backend/src/pdf/gotenberg.client.ts (magic-byte check lines 52-59; reuse after render, German generic throw)
    - packages/tax-engine/src/schema.ts (TaxDecision)
    - apps/backend/prisma/schema.prisma (Entity/Customer/Invoice/InvoiceItem fields)
    - apps/desktop/src/styles/globals.css (Inter / Instrument Serif token names)
    - https://takumi.kane.tw/docs/pdf
    - https://www.pdfcn.dev/docs/installation.md
  </read_first>
  <reversibility rating="costly">D-01 renderer pin plus Invoice Minimal copy: swapping engine later is a new phase; template JSX is local to this package.</reversibility>
  <behavior>
    - RED first: packages/pdf-templates/src/render-invoice.spec.ts describes D-23 fixture 1 (DE locale, DE B2B 19%, tax shown, issued number/date/due) and expects bytes starting with %PDF- and contentType application/pdf.
    - After GREEN: renderInvoice returns Uint8Array with first four bytes 37 80 68 70 45 (%PDF-), byteLength &gt;= 5.
    - Render options omit archival-profile, file-embed, and UA-tag keys (D-05, D-06).
    - Header uses Entity.name as text; no logo image (D-13).
  </behavior>
  <action>
    After "packages verified": create packages/pdf-templates analog tax-engine (name @clared/pdf-templates, private, main/types dist, build tsc, test jest). tsconfig copies tax-engine CJS + jsx react-jsx only — do not copy ui ESNext/bundler/noEmit; do not add jsx to apps/backend/tsconfig.json.

    pnpm --filter @clared/pdf-templates add takumi-pdf@0.14.2 @takumi-rs/helpers@2.13.6 react@19.1.0. Pin jest/ts-jest/typescript/@types/node/@types/jest like tax-engine; add @types/react matching 19. From packages/pdf-templates, npx shadcn@latest add @pdfcn/takumi/invoice-minimal only (Takumi registry, not the second renderer namespace). Then grep package.json against RESEARCH Do not install table and strip any pulled names. Restyle copied JSX to Crafted tokens (Oatmeal/Charcoal/Sage; flat flex/stack/table; no CSS filter blur).

    Vendor Inter + Instrument Serif WOFF2 plus OFL text under packages/pdf-templates/fonts/ (D-07). Load via path.join(__dirname, "../fonts/...") after tsc (Nest CJS). One module-level PdfRenderer. Footer primitives PageNumber and TotalPages plus Entity.name · legalForm · country (D-16). Legal block under totals with legal_reference only for fixture 1 (D-11, D-12). No Zahlungsblock (D-14). No draft watermark (D-17).

    Export renderInvoice from src/index.ts. Map fixture 1 knobs locale de and vatLine omit explicitly (D-21). Magic-byte check after render: throw German Render fehlgeschlagen if empty or not %PDF-; never return empty PdfBytes (D-26). Do not interpolate seller/line text into Error.message.

    If ts-jest cannot load WASM (RESEARCH A1), add packages/pdf-templates/scripts/assert-pdf-magic.ts and point the test script at node running that file; keep a Jest spec that at least imports types. Do not esbuild-flatten. Do not add a Nest HTTP route (D-04). Do not touch Prisma, compose, Dockerfile, or Coolify this plan.
  </action>
  <acceptance_criteria>
    - packages/pdf-templates/package.json name is @clared/pdf-templates and lists takumi-pdf 0.14.2
    - packages/pdf-templates/package.json has none of the RESEARCH deny-list quoted names
    - test -f packages/pdf-templates/src/render-invoice.ts and at least one WOFF2 under packages/pdf-templates/fonts
    - pnpm --filter @clared/pdf-templates exec jest --testPathPattern=render-invoice exits 0 with at least one passed test
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @clared/pdf-templates exec jest --testPathPattern=render-invoice && test -f packages/pdf-templates/package.json && grep -F '"takumi-pdf"' packages/pdf-templates/package.json && ! grep -E '"takumi":|"pdfcn":|"forme":|"@takumi-rs/core":' packages/pdf-templates/package.json && test "$(find packages/pdf-templates/fonts -name '*.woff2' | wc -l | tr -d ' ')" -ge 1</automated>
    <fails_when>non-zero exit, or "0 passed" in the Jest summary, or deny-list quoted names present in packages/pdf-templates/package.json, or no WOFF2 under packages/pdf-templates/fonts</fails_when>
    <human-check>Open the fixture 1 PDF; header is Entity.name text; legal block under totals; no logo placeholder (D-01 restyle).</human-check>
  </verify>
  <done>D-23 fixture 1 produces %PDF- in-process with vendored fonts; package is the Nest consume analog (D-01, D-07, D-08, PDF-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → packages/pdf-templates | New WASM/runtime deps enter the API image later |
| Fixture InvoiceModel → PDF bytes | Structured props, not HTML strings |
| Font files → renderer | Local bytes only; no user-supplied paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.6-SC | Tampering | takumi-pdf / @takumi-rs/helpers install | high | mitigate | blocking-human legitimacy checkpoint; pin 0.14.2 / 2.13.6; no postinstall; deny-list grep |
| T-04.6-01 | Tampering | renderInvoice output | high | mitigate | Fail-closed %PDF- magic; never empty PdfBytes (D-26) |
| T-04.6-02 | Information Disclosure | font loading | high | mitigate | Vendored WOFF2 only (D-07); no remote font helper |
| T-04.6-03 | Information Disclosure | Error.message | medium | mitigate | Generic German Render fehlgeschlagen; no names/lines (D-26) |
| T-04.6-04 | Elevation of Privilege | HTTP surface | low | accept | No generate route this plan (D-04); Phase 5 authorizes download |
</threat_model>

<verification>
pnpm --filter @clared/pdf-templates exec jest --testPathPattern=render-invoice passes. Deny-list absent from package.json. At least one WOFF2 vendored. No Nest HTTP route added.
</verification>

<success_criteria>
D-23 fixture 1 (DE B2B 19%) yields %PDF- bytes from InvoiceModel + TaxDecision via Takumi Invoice Minimal with local fonts (PDF-01 prep tracer).
</success_criteria>

## Artifacts this phase produces

- `packages/pdf-templates/` — CJS JSX workspace package `@clared/pdf-templates`
- `packages/pdf-templates/src/render-invoice.ts` — `renderInvoice` + PdfRenderer + magic-byte check
- `packages/pdf-templates/src/invoice-minimal.tsx` — pdfcn Invoice Minimal copy, Crafted tokens
- `packages/pdf-templates/src/render-invoice.spec.ts` — D-23 fixture 1
- `packages/pdf-templates/fonts/` — Inter + Instrument Serif WOFF2 + OFL
- `pnpm-lock.yaml` — pinned takumi-pdf@0.14.2, helpers@2.13.6, react@19.1.0

<output>
Create `.planning/phases/04.6-takumi-pdfcn-pdf-engine-cutover/04.6-01-SUMMARY.md` when done
</output>
