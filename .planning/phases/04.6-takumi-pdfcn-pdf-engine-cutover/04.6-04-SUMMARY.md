---
phase: 04.6-takumi-pdfcn-pdf-engine-cutover
plan: 04
subsystem: infra
tags: [docker, github-actions, coolify, pdf-templates, takumi, api-only]

requires:
  - phase: 04.6-03
    provides: Nest InvoicePdfService sync; worker.ts deleted; Ready D-25
provides:
  - API image COPY+builds @clared/pdf-templates; no worker stage (D-09)
  - ci.yml path/filter/build/test for pdf-templates
  - backend-image.yml API-only (no push-worker / COOLIFY_APP_WORKER)
  - coolify-deploy-nest.sh API pin + /health/ready poll; mocks API-only
affects:
  - 04.6-05 (Coolify app deletes)
  - Phase 5 (invoice PDF download/view)

actuals:
  tokens: 7482
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - Dockerfile tax-engine COPY/install/build duplicated for pdf-templates
    - GHA API-only image: one push-api, one Trivy/SBOM/cosign, one Coolify UUID
    - Deploy health poll uses /health/ready details.build.sha (D-25)

key-files:
  created: []
  modified:
    - apps/backend/Dockerfile
    - .github/workflows/ci.yml
    - .github/workflows/backend-image.yml
    - .github/scripts/coolify-deploy-nest.sh
    - .github/scripts/coolify-deploy-nest.test.sh

key-decisions:
  - "API image owns Takumi wasm via node_modules; no worker stage/tag (D-09)"
  - "Deploy polls /health/ready build sha after API pin (D-25; Chromium gone)"
  - "Mock tests fail closed if WORKER_UUID / COOLIFY_APP_WORKER / -worker reappear"

patterns-established:
  - "pdf-templates path group gates backend-unit/e2e like tax-engine"
  - "Coolify nest deploy is single-app pin + Ready poll"

requirements-completed: [PDF-01]

coverage:
  - id: D1
    description: Dockerfile COPY/builds pdf-templates; no FROM build AS worker; api stage kept
    requirement: PDF-01
    verification:
      - kind: other
        ref: grep -F pdf-templates/@clared/pdf-templates + ! FROM build AS worker apps/backend/Dockerfile
        status: pass
    human_judgment: false
  - id: D2
    description: ci.yml + backend-image.yml API-only with packages/pdf-templates paths; no push-worker / COOLIFY_APP_WORKER
    requirement: PDF-01
    verification:
      - kind: other
        ref: grep packages/pdf-templates + @clared/pdf-templates ci.yml; ! push-worker/COOLIFY_APP_WORKER backend-image.yml
        status: pass
    human_judgment: false
  - id: D3
    description: coolify-deploy-nest.sh API-only; mock tests pass; no WORKER_UUID / -worker
    requirement: PDF-01
    verification:
      - kind: other
        ref: bash .github/scripts/coolify-deploy-nest.test.sh
        status: pass
    human_judgment: false

duration: 4min
completed: 2026-09-05
status: complete
---

# Phase 04.6 Plan 04: API-only image + CI cutover Summary

**API image and GHA deploy Takumi via `@clared/pdf-templates` with no worker stage or worker Coolify pin (D-09).**

## Performance

- **Duration:** 4 min
- **Started:** 2026-09-05T17:11:18Z
- **Completed:** 2026-09-05T17:15:03Z
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments

- Dockerfile installs/builds `@clared/pdf-templates` next to tax-engine; worker stage and `dist/worker.js` check removed
- `ci.yml` path-filters and builds/tests pdf-templates; `backend-image.yml` is API-only (no push-worker / worker Trivy/SBOM/cosign / `COOLIFY_APP_WORKER`)
- `coolify-deploy-nest.sh` pins API only and polls `/health/ready` build sha; mocks fail if worker pin returns

## Task Commits

Each task was committed atomically:

1. **Task 1: Dockerfile COPY pdf-templates; drop worker stage (D-09)** - `1bc04c6` (feat)
2. **Task 2: ci.yml and backend-image.yml API-only + pdf-templates filter (D-09)** - `3fb1755` (feat)
3. **Task 3: coolify-deploy-nest.sh API-only + rewrite mock tests (D-09)** - `e14e219` (feat)

**Plan metadata:** (pending docs commit)

## Files Created/Modified

- `apps/backend/Dockerfile` — pdf-templates COPY/install/build; api-only stage
- `.github/workflows/ci.yml` — pdf-templates path group, filter, build, test
- `.github/workflows/backend-image.yml` — API-only push/scan/sign/deploy
- `.github/scripts/coolify-deploy-nest.sh` — API pin + `/health/ready` poll
- `.github/scripts/coolify-deploy-nest.test.sh` — API-only mocks + worker-pin guard

## Decisions Made

- Kept wasm in `node_modules/takumi-pdf` (no prune, no esbuild flatten) per D-09
- Switched deploy health poll from `/health/build` to `/health/ready` now that Ready is postgres+redis+build only (D-25)
- Coolify worker app delete deferred to 04.6-05

## Deviations from Plan

None - plan executed exactly as written.

## Threat Flags

None — no new trust-boundary surface beyond plan T-04.6-13/14 mitigations (COPY pdf-templates; drop worker deploy; mock fails on worker pin).

## Known Stubs

None.

## Verification Results

| Check | Result |
|-------|--------|
| Dockerfile pdf-templates / no worker stage | pass |
| ci.yml + backend-image.yml API-only + pdf-templates | pass |
| `bash .github/scripts/coolify-deploy-nest.test.sh` | pass (shellcheck + actionlint + zizmor) |

Optional `docker build --target api` wasm smoke skipped (grep acceptance used).

## Self-Check: PASSED

- FOUND: `.planning/phases/04.6-takumi-pdfcn-pdf-engine-cutover/04.6-04-SUMMARY.md`
- FOUND: `1bc04c6`, `3fb1755`, `e14e219`
