---
phase: 04.6-takumi-pdfcn-pdf-engine-cutover
plan: 03
type: execute
wave: 3
depends_on:
  - 04.6-02
files_modified:
  - apps/backend/package.json
  - apps/backend/src/pdf/pdf.contract.ts
  - apps/backend/src/pdf/pdf.contract.spec.ts
  - apps/backend/src/pdf/invoice-pdf.service.ts
  - apps/backend/src/pdf/invoice-pdf.service.spec.ts
  - apps/backend/src/pdf/pdf.module.ts
  - apps/backend/src/health/health.controller.ts
  - apps/backend/test/health.e2e-spec.ts
  - apps/backend/src/pdf/gotenberg.client.ts
  - apps/backend/src/pdf/gotenberg.client.spec.ts
  - apps/backend/src/pdf/pdf.processor.ts
  - apps/backend/src/pdf/pdf.service.ts
  - apps/backend/src/pdf/empty-html.error.ts
  - apps/backend/src/worker.ts
  - apps/backend/src/worker.module.ts
  - apps/backend/src/worker-health.ts
  - apps/backend/src/worker-health.spec.ts
  - apps/backend/Dockerfile
  - pnpm-lock.yaml
autonomous: true
requirements:
  - PDF-01
estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "InvoicePdfService maps Entity+Customer+Invoice+InvoiceItem+TaxDecision plus knobs to renderInvoice and returns PdfBytes synchronously (D-02, D-03, D-15, D-21)"
    - "Render failure throws; never empty PdfBytes; Error.message has no fixture names or line text (D-26)"
    - "PdfModule has no BullModule; PdfBytes remains; HtmlInvoiceFacts and queue exports are gone (D-02, D-03)"
    - "GET /health/ready JSON includes postgres and redis and does not include the Chromium renderer key; Ready is postgres+redis+build-sha with no WASM ping (D-25)"
    - "invoices.controller.ts still has only CRUD Post/Get/Patch — no generate route (D-04)"
    - "apps/backend/prisma/schema.prisma is unchanged (D-20)"
  artifacts:
    - apps/backend/src/pdf/invoice-pdf.service.ts
    - apps/backend/src/pdf/invoice-pdf.service.spec.ts
    - apps/backend/src/pdf/pdf.module.ts
    - apps/backend/src/pdf/pdf.contract.ts
  key_links:
    - "PdfModule providers InvoicePdfService → @clared/pdf-templates renderInvoice"
    - "HealthController.ready indicators: postgres, redis, build only (D-25)"
    - "AppModule still imports PdfModule (apps/backend/src/app.module.ts lines 20, 38)"
  assumptions:
    - "empty/null service input throws fail-closed (D-26); encoding is %PDF- bytes"
    - "concurrency: sync await renderInvoice on the Nest event loop; Redis remains for session KV only (D-02)"
  prohibitions:
    - statement: "Do not add a public generate HTTP route (D-04)."
      status: unverified
    - statement: "Do not add Prisma locale/vatLine/iban/logo columns (D-20)."
      status: unverified
    - statement: "Do not keep a feature toggle or Chromium fallback (D-03)."
      status: unverified
    - statement: "Do not add a WASM ping or engine stub on Ready (D-25)."
      status: unverified
    - statement: "Do not edit Dockerfile or GHA this plan (04.6-04)."
      status: unverified
    - statement: "Do not delete Coolify apps this plan (04.6-05, D-24)."
      status: unverified
---

<objective>
Nest sync InvoicePdfService + drop queue/Chromium client/worker bootstrap in the API process (D-02, D-03, D-25, D-26).

Purpose: PDF-01 bytes contract lives in Nest; old HTML-queue path cannot stay GREEN.

Output: invoice-pdf.service, rewritten contract/module/health, deleted queue/worker/Chromium source.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.6-takumi-pdfcn-pdf-engine-cutover/04.6-CONTEXT.md
@.planning/phases/04.6-takumi-pdfcn-pdf-engine-cutover/04.6-RESEARCH.md
@.planning/phases/04.6-takumi-pdfcn-pdf-engine-cutover/04.6-PATTERNS.md
@.planning/phases/04.6-takumi-pdfcn-pdf-engine-cutover/04.6-01-SUMMARY.md
@.planning/phases/04.6-takumi-pdfcn-pdf-engine-cutover/04.6-02-SUMMARY.md
@apps/backend/src/pdf/pdf.contract.ts
@apps/backend/src/pdf/pdf.module.ts
@apps/backend/src/pdf/pdf.service.ts
@apps/backend/src/invoices/invoices.module.ts
@apps/backend/src/tax/facts-mapper.ts
@apps/backend/src/health/health.controller.ts
@apps/backend/test/health.e2e-spec.ts
@apps/backend/src/invoices/invoices.controller.ts
@apps/backend/src/app.module.ts
@apps/backend/package.json

<interfaces>
InvoicePdfService.render(input): Promise&lt;PdfBytes&gt;
  input.entity: name, address, vatId, country, legalForm
  input.customer: name, address, vatId, country
  input.invoice: number, date, dueDate
  input.items: bezeichnung, menge, einzelpreis, netto
  input.tax: TaxDecision
  input.knobs: { locale: "de" | "en"; vatLine: "omit" | "zero" }
  Calls renderInvoice from @clared/pdf-templates. Throws on empty/invalid; no Result type (D-26).

Keep: export interface PdfBytes { bytes: Uint8Array; contentType: "application/pdf" }

Delete exports: HtmlInvoiceFacts, PDF_QUEUE, DEFAULT_GOTENBERG_URL, PDF_PAPER_A4 inch strings (Takumi size a4).

PdfModule: no BullModule, no controllers (D-04). providers/exports: InvoicePdfService.
AppModule already imports PdfModule — keep that import.
invoices.controller.ts: @Post @Get @Get(:id) @Patch(:id) only — do not add generate.
</interfaces>
</context>

<!-- planner-discipline-allow: gotenberg -->
<!-- planner-discipline-allow: PDF_QUEUE -->
<!-- planner-discipline-allow: bullmq -->
<!-- planner-discipline-allow: HtmlInvoiceFacts -->
<!-- planner-discipline-allow: dist/worker.js -->

<tasks>

<task type="auto" tdd="true">
  <name>InvoicePdfService + PdfBytes contract (D-02, D-03, D-26)</name>
  <files>apps/backend/package.json, apps/backend/src/pdf/pdf.contract.ts, apps/backend/src/pdf/pdf.contract.spec.ts, apps/backend/src/pdf/invoice-pdf.service.ts, apps/backend/src/pdf/invoice-pdf.service.spec.ts, pnpm-lock.yaml</files>
  <read_first>
    - apps/backend/src/pdf/pdf.contract.ts (keep PdfBytes only)
    - apps/backend/src/pdf/pdf.contract.spec.ts (rewrite; pitfall 5 — old queue assertions must not stay GREEN)
    - apps/backend/src/tax/facts-mapper.ts (Prisma/DTO → engine mapping analog)
    - apps/backend/src/pdf/gotenberg.client.spec.ts (empty throws; non-PDF rejected — rewrite against InvoicePdfService)
    - apps/backend/src/pdf/pdf.service.ts (fail-closed empty guard analog; do not copy InjectQueue)
    - packages/pdf-templates/src/index.ts (renderInvoice, defaultsFromCountry)
    - apps/backend/package.json (add @clared/pdf-templates workspace:* analog tax-engine line 15)
  </read_first>
  <reversibility rating="costly">D-02 sync Nest API: callers after this plan expect PdfBytes not a queue job id; undoing needs a new worker phase.</reversibility>
  <behavior>
    - render(valid DE B2B 19% input) returns contentType application/pdf and bytes starting %PDF-.
    - render with missing required fields throws; does not return { bytes: new Uint8Array(0), contentType: "application/pdf" }.
    - When renderInvoice is mocked to return non-PDF bytes, service throws and message does not contain seller name or line bezeichnung from the fixture.
    - pdf.contract.spec.ts asserts PdfBytes contentType and does not import queue or Chromium URL constants.
  </behavior>
  <action>
    Add "@clared/pdf-templates": "workspace:*" to apps/backend/package.json. Keep PdfBytes; delete HtmlInvoiceFacts, PDF_QUEUE, DEFAULT_GOTENBERG_URL, PDF_PAPER_A4 from pdf.contract.ts. Rewrite pdf.contract.spec.ts to match (pitfall 5).

    Create InvoicePdfService @Injectable analog invoices.service.ts: map Entity/Customer/Invoice/InvoiceItem + TaxDecision + knobs into renderInvoice; await sync (D-02). Optional: call defaultsFromCountry only if knobs omitted in tests — fixtures pass knobs explicitly (D-21). Throw named error with message Render fehlgeschlagen; never interpolate names, IBAN, or line text (D-26 / 04.3 D-23). Magic-byte check if not already inside renderInvoice. No HTTP decorator on this class (D-04). Do not add Prisma columns (D-20). Do not set archival-profile flags (D-06).
  </action>
  <acceptance_criteria>
    - apps/backend/package.json lists @clared/pdf-templates workspace:*
    - pdf.contract.ts exports PdfBytes and does not export PDF_QUEUE
    - pnpm --filter ./apps/backend exec jest --testPathPattern='invoice-pdf.service|pdf.contract' exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./apps/backend exec jest --testPathPattern='invoice-pdf.service|pdf.contract' && ! grep -F 'PDF_QUEUE' apps/backend/src/pdf/pdf.contract.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the Jest summary, or PDF_QUEUE still exported from apps/backend/src/pdf/pdf.contract.ts</fails_when>
  </verify>
  <done>Nest service returns PdfBytes fail-closed; old queue contract tests rewritten (D-02, D-03, D-26, PDF-01).</done>
</task>

<task type="auto">
  <name>PdfModule without queue; delete Chromium client, processor, worker bootstrap</name>
  <files>apps/backend/src/pdf/pdf.module.ts, apps/backend/package.json, apps/backend/src/pdf/gotenberg.client.ts, apps/backend/src/pdf/gotenberg.client.spec.ts, apps/backend/src/pdf/pdf.processor.ts, apps/backend/src/pdf/pdf.service.ts, apps/backend/src/pdf/empty-html.error.ts, apps/backend/src/worker.ts, apps/backend/src/worker.module.ts, apps/backend/src/worker-health.ts, apps/backend/src/worker-health.spec.ts, apps/backend/Dockerfile, pnpm-lock.yaml</files>
  <read_first>
    - apps/backend/src/invoices/invoices.module.ts (module without Bull analog)
    - apps/backend/src/pdf/pdf.module.ts (current BullModule clared-bull — stop copying)
    - apps/backend/src/app.module.ts (keep PdfModule import)
    - apps/backend/package.json (drop @nestjs/bullmq and bullmq; keep ioredis/RedisService)
  </read_first>
  <reversibility rating="costly">D-02 git: removing worker.ts and the queue producer; git revert restores sources until Coolify apps are deleted in 04.6-05.</reversibility>
  <action>
    Rewrite pdf.module.ts analog invoices.module.ts: providers InvoicePdfService, exports same, no BullModule, no controllers. Keep AppModule PdfModule import.

    Delete Chromium client + spec, pdf.processor.ts, pdf.service.ts, empty-html.error.ts, worker.ts, worker.module.ts, worker-health.ts, worker-health.spec.ts. Drop @nestjs/bullmq and bullmq from apps/backend/package.json; keep ioredis and REDIS_URL consumers. Do not delete Redis session KV (D-02). In apps/backend/Dockerfile drop only the `test -f dist/worker.js` conjunct so nest build without worker.ts still images (04.6-04 owns COPY pdf-templates and removing the worker stage). Confirm invoices.controller.ts has no generate Post.
  </action>
  <acceptance_criteria>
    - test ! -f apps/backend/src/pdf/gotenberg.client.ts
    - test ! -f apps/backend/src/worker.ts
    - apps/backend/package.json has no @nestjs/bullmq and no bullmq dependency keys
    - PdfModule source has no BullModule import
    - apps/backend/Dockerfile no longer asserts dist/worker.js
  </acceptance_criteria>
  <verify>
    <automated>test ! -f apps/backend/src/pdf/gotenberg.client.ts && test ! -f apps/backend/src/worker.ts && test ! -f apps/backend/src/pdf/pdf.processor.ts && ! grep -F '@nestjs/bullmq' apps/backend/package.json && ! grep -F '"bullmq"' apps/backend/package.json && ! grep -F BullModule apps/backend/src/pdf/pdf.module.ts && ! grep -F 'dist/worker.js' apps/backend/Dockerfile</automated>
    <fails_when>non-zero exit, or Chromium client / worker.ts / pdf.processor.ts still on disk, or BullModule still imported in pdf.module.ts</fails_when>
  </verify>
  <done>Queue/Chromium/worker bootstrap gone from Nest sources; Redis session KV stays (D-02, D-03, D-04).</done>
</task>

<task type="auto" tdd="true">
  <name>Ready without Chromium indicator (D-25)</name>
  <files>apps/backend/src/health/health.controller.ts, apps/backend/test/health.e2e-spec.ts</files>
  <read_first>
    - apps/backend/src/health/health.controller.ts (keep postgres/redis/build lines 34-59; delete callback lines 60-75 and gotenbergAuthHeader import)
    - apps/backend/test/health.e2e-spec.ts (keep postgres/redis match; add not.toMatch for the Chromium key; keep PII_KEYS)
  </read_first>
  <reversibility rating="costly">D-25: Coolify deploy poll and Kuma currently include the Chromium ready check; after this, Ready is postgres+redis+build-sha only.</reversibility>
  <behavior>
    - GET /health/ready body lowercased matches postgres and redis and does not match gotenberg.
    - No WASM ping and no pdf engine stub object on Ready (D-25).
  </behavior>
  <action>
    Delete the Chromium ready indicator entirely (D-25). Keep postgres, redis, build-sha. Do not add a Takumi wasm ping. Do not add pdf: { engine: "takumi" }. Extend health.e2e-spec.ts: expect(body).not.toMatch(/gotenberg/) while keeping 200/503 and postgres/redis assertions.
  </action>
  <acceptance_criteria>
    - health.controller.ts has no gotenbergAuthHeader import
    - health.e2e-spec.ts asserts body does not match gotenberg
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./apps/backend exec jest --testPathPattern='pdf|health' && ! grep -F gotenberg apps/backend/src/health/health.controller.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the Jest summary, or gotenberg still appears in health.controller.ts</fails_when>
  </verify>
  <done>Ready is postgres+redis+build-sha; deploy poll remains valid without a PDF probe (D-25).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| InvoicePdfService → renderer | Structured models only; no HTML string |
| /health/ready → Coolify/Kuma | Public-ish probe; must not leak PII or require Chromium |
| invoices.controller → clients | CRUD only this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.6-08 | Information Disclosure | InvoicePdfService errors | medium | mitigate | Generic German message; no names/lines (D-26) |
| T-04.6-09 | Information Disclosure | leftover Chromium fetch | high | mitigate | Delete gotenberg.client.ts (SSRF path gone) |
| T-04.6-10 | Tampering | empty PdfBytes | high | mitigate | Throw; magic %PDF- (D-26) |
| T-04.6-11 | Elevation of Privilege | generate HTTP | high | mitigate | No new Post; grep invoices.controller (D-04) |
| T-04.6-12 | Denial of Service | sync render on event loop | low | accept | v1 invoice volume; Takumi page cap; no new queue |
| T-04.6-SC | Tampering | npm installs | high | accept | Workspace dep only; no new registry packages |
</threat_model>

<verification>
pnpm --filter ./apps/backend exec jest --testPathPattern='pdf|health'. Chromium client, worker.ts, pdf.processor.ts absent. Ready body has no gotenberg key.
</verification>

<success_criteria>
Nest produces PdfBytes in-process; queue/Chromium/worker sources deleted; Ready no longer keys Chromium (PDF-01, D-02, D-03, D-25, D-26).
</success_criteria>

## Artifacts this phase produces

- `apps/backend/src/pdf/invoice-pdf.service.ts` — sync mapper + fail-closed render
- `apps/backend/src/pdf/pdf.module.ts` — no BullModule
- `apps/backend/src/pdf/pdf.contract.ts` — PdfBytes only
- Deleted: gotenberg.client.ts, pdf.processor.ts, pdf.service.ts, worker.ts, worker.module.ts, worker-health.ts

<output>
Create `.planning/phases/04.6-takumi-pdfcn-pdf-engine-cutover/04.6-03-SUMMARY.md` when done
</output>
