# Phase 04.6 — API coverage

Takumi is **not** a REST PDF API. Nest calls `takumi-pdf` `render()` in-process (WASM). No public generate HTTP route (D-04). Phase 5 owns download/view.

| Detector | Result |
|----------|--------|
| External HTTP PDF API | None — library `render()` only |
| Public generate route | Forbidden this phase |
| Prisma/schema push | None — D-20 no new columns |
