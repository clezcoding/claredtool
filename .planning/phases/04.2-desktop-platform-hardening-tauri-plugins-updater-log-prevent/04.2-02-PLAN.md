---
phase: 04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent
plan: 02
type: execute
wave: 2
depends_on:
  - 04.2-01
files_modified:
  - apps/desktop/package.json
  - apps/desktop/src-tauri/Cargo.toml
  - apps/desktop/src-tauri/src/lib.rs
  - apps/desktop/src-tauri/tauri.conf.json
  - apps/desktop/src-tauri/capabilities/default.json
  - apps/desktop/src/lib/desktop-store.ts
  - apps/desktop/src/lib/theme.ts
  - apps/desktop/src/lib/theme-menu.ts
  - apps/desktop/src/main.tsx
  - apps/desktop/src/styles/globals.css
  - apps/desktop/src/lib/desktop-store.test.ts
  - apps/desktop/src/lib/theme.test.ts
  - apps/desktop/src/__tests__/capabilities-desk.test.ts
autonomous: true
requirements:
  - DESK-01
  - DESK-02
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Theme preference reads/writes through tauri-plugin-store in Tauri and localStorage fallback outside Tauri (D-13, D-15)."
    - "One-shot migration moves clared-theme into store while dual-writing localStorage so index.html boot IIFE still paints (D-14, D-36, RESEARCH Pitfall 1)."
    - "tauri-plugin-prevent-default is registered; debug builds keep DevTools/reload usable per RESEARCH Flags pattern (D-31, D-50)."
    - "tauri.conf.json security.csp is a non-null object allowing Vite localhost:5174, backend, Authentik, and documented exceptions (D-51)."
    - "Capabilities grant store/prevent-default permissions; mcp-bridge:default removed from release default capability set (D-49, D-51)."
    - "globals.css disables overscroll rubber-band and sets selection defaults per D-32/D-33."
  artifacts:
    - apps/desktop/src/lib/desktop-store.ts
    - apps/desktop/src-tauri/src/lib.rs
    - apps/desktop/src-tauri/tauri.conf.json
    - apps/desktop/src-tauri/capabilities/default.json
    - apps/desktop/src/styles/globals.css
  key_links:
    - "theme.ts / theme-menu.ts call desktop-store set/get; applyTheme dual-writes THEME_KEY localStorage."
    - "lib.rs registers store + prevent_default(); Builder order documented."
    - "CSP connect-src includes auth + Vite origins so DESK-01 OIDC still works."
  prohibitions:
    - statement: "Session tokens must not move into tauri-plugin-store (D-13)."
      status: forbidden
      verification: "desktop-store keys limited to theme/desktop prefs; no session/token keys."
    - statement: "Window geometry must not be duplicated into store (D-16)."
      status: forbidden
      verification: "No window position/size keys written by desktop-store."
    - statement: "Do not redesign splash or change native titlebar (D-36)."
      status: forbidden
      verification: "App.tsx splash path and decorations:true unchanged."
---

<objective>
Tracer slice: prove plugin-add + store theme migration + prevent-default + CSP hardening end-to-end so later waves expand from a working host/frontend contract (DESK-01, DESK-02).

Purpose: Catch ACL/CSP/boot-flash architecture failures early with one production-quality path (D-13…D-16, D-31…D-33, D-36, D-51).
Output: store + prevent-default wired; desktop-store + theme dual-write; CSP object; selection/overscroll CSS; green desktop-store/theme tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-CONTEXT.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-RESEARCH.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-PATTERNS.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-01-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end theme store + prevent-default + CSP — one path</name>
  <reversibility rating="costly">D-13 store pref schema and D-51 CSP lock-in: wrong store shape or CSP breaks many call sites / auth runtime.</reversibility>
  <files>apps/desktop/package.json, apps/desktop/src-tauri/Cargo.toml, apps/desktop/src-tauri/src/lib.rs, apps/desktop/src-tauri/tauri.conf.json, apps/desktop/src-tauri/capabilities/default.json, apps/desktop/src/lib/desktop-store.ts, apps/desktop/src/lib/theme.ts, apps/desktop/src/lib/theme-menu.ts, apps/desktop/src/main.tsx, apps/desktop/src/lib/desktop-store.test.ts, apps/desktop/src/__tests__/capabilities-desk.test.ts</files>
  <read_first>
    - apps/desktop/src-tauri/src/lib.rs
    - apps/desktop/src-tauri/capabilities/default.json
    - apps/desktop/src-tauri/tauri.conf.json
    - apps/desktop/src/lib/theme.ts
    - apps/desktop/src/lib/theme-menu.ts
    - apps/desktop/index.html
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-RESEARCH.md (Pattern 1, Pitfall 1/4, Package Legitimacy — official OK plugins)
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-PATTERNS.md (desktop-store, lib.rs, capabilities)
  </read_first>
  <action>
    Prefer `pnpm --filter ./apps/desktop tauri add store` (and Rust-only `tauri-plugin-prevent-default = "5"`) so Cargo + stubs land together — official packages already OK in RESEARCH legitimacy audit. Register `tauri_plugin_store` and `prevent_default()` in `lib.rs` without changing keychain commands, `open_login_window`, deep-link, or mcp `#[cfg(debug_assertions)]` binding (D-49). Implement `desktop-store.ts` with STORE_FILE `clared-desktop.json`, `getThemePref`/`setThemePref`, one-shot `migrateThemeToStore` reading `THEME_KEY` (D-14), and localStorage fallback when not Tauri (D-15). After every store theme write, dual-write localStorage `clared-theme` so boot IIFE keeps working (D-36 / Pitfall 1) — do not strip localStorage. Wire theme.ts/theme-menu/main to use store helpers. Replace `csp: null` with a CSP object that keeps Vite `http://localhost:5174`, backend, and Authentik connect/script needs (D-51 discretion for exact string). Grant store permissions in `default.json`; remove `mcp-bridge:default` from release default permissions (D-49/D-51). Make `desktop-store.test.ts` and capabilities-desk assertions pass for store write path and mcp absence + no clipboard-read yet. Do not add updater/Sentry/clipboard/dragout in this tracer.
  </action>
  <acceptance_criteria>
    - Cargo.toml lists tauri-plugin-store and tauri-plugin-prevent-default; Cargo.toml does not list sentry-tauri.
    - lib.rs calls store plugin init and prevent_default helper; mcp-bridge remains behind debug_assertions only.
    - desktop-store.ts exports getThemePref, setThemePref, migrateThemeToStore; dual-writes THEME_KEY.
    - tauri.conf.json security.csp is an object (not null) including localhost:5174 for dev.
    - default.json lacks mcp-bridge:default; includes store permission entries.
    - `pnpm --filter ./apps/desktop exec vitest run src/lib/desktop-store.test.ts src/__tests__/capabilities-desk.test.ts` exits 0 for store/mcp assertions that this plan owns (clipboard assertions may still pending until Plan 08 — mark those skipped or scoped).
    - `cd apps/desktop/src-tauri && cargo check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec vitest run src/lib/desktop-store.test.ts && cd apps/desktop/src-tauri && cargo check</automated>
  </verify>
  <done>Theme store path + prevent-default + CSP + tightened capabilities work as one end-to-end tracer.</done>
</task>

<task type="auto">
  <name>Task 2: Selection + overscroll CSS (D-32/D-33)</name>
  <files>apps/desktop/src/styles/globals.css, apps/desktop/src/lib/theme.test.ts</files>
  <read_first>
    - apps/desktop/src/styles/globals.css
    - apps/desktop/src/lib/theme.test.ts
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-PATTERNS.md (globals.css section)
  </read_first>
  <action>
    In globals.css set `overscroll-behavior: none` on html/body (D-33). Set chrome `user-select: none` with restores for `input, textarea, [contenteditable], .tabular-nums` and a documented copy-target class (e.g. `.copyable`) per D-32. Keep PAINT_* / `--background` hex sync with theme.ts. Extend theme.test.ts or a small css assertion if practical without brittle snapshots.
  </action>
  <acceptance_criteria>
    - globals.css contains overscroll-behavior: none and user-select rules matching D-32/D-33.
    - PAINT_LIGHT/PAINT_DARK tests still pass.
    - `pnpm --filter ./apps/desktop exec vitest run src/lib/theme.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec vitest run src/lib/theme.test.ts && grep -q 'overscroll-behavior' apps/desktop/src/styles/globals.css && grep -q 'user-select' apps/desktop/src/styles/globals.css</automated>
  </verify>
  <done>Selection and overscroll desktop feel defaults applied without breaking theme paint contract.</done>
</task>

<task type="auto">
  <name>Task 3: Prevent-default flag matrix documentation in Rust</name>
  <files>apps/desktop/src-tauri/src/lib.rs</files>
  <read_first>
    - apps/desktop/src-tauri/src/lib.rs
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-RESEARCH.md (Prevent-default debug vs release)
  </read_first>
  <action>
    Finalize prevent-default builder: debug builds use Flags::all().difference(DEV_TOOLS | RELOAD) so F12/devtools remain (D-50); release uses init()/full battery blocking reload (F5/Ctrl-R), back/forward, disruptive zoom (D-31). Map Cmd↔Ctrl via plugin defaults. Allow context menu in inputs per D-31. Comment cites D-31/D-50. No Specta/TauRPC (D-48).
  </action>
  <acceptance_criteria>
    - lib.rs has cfg(debug_assertions) vs release prevent_default functions.
    - Debug path excludes DEV_TOOLS and RELOAD flags.
    - cargo check still passes.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/desktop/src-tauri && cargo check && grep -q 'prevent_default' src/lib.rs && grep -q 'debug_assertions' src/lib.rs</automated>
  </verify>
  <done>Prevent-default debug/release matrix matches D-31/D-50.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Kind | Notes |
|--------|------|-------|
| `getThemePref` | fn | desktop-store.ts |
| `setThemePref` | fn | desktop-store.ts |
| `migrateThemeToStore` | fn | desktop-store.ts |
| `STORE_FILE` | const | `clared-desktop.json` |
| `prevent_default` | fn | lib.rs |
| CSP object | config | tauri.conf.json security.csp |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView → Rust plugins | Capability allowlist gates IPC |
| WebView document → navigation | CSP + future link-guard |
| localStorage / store file | Prefs only — no tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-02 | Elevation of Privilege | capabilities/default.json | high | mitigate | Remove mcp-bridge from release defaults; least-privilege store grants (task 1) |
| T-04.2-03 | Tampering | CSP | high | mitigate | Replace csp null with allowlist preserving auth origins (task 1) |
| T-04.2-04 | Information Disclosure | desktop-store | medium | mitigate | Session stays Keychain; store theme-only (D-13) |
| T-04.2-SC | Tampering | npm/cargo | high | mitigate | Official OK packages only; no SUS installs this plan |
</threat_model>

<verification>
cargo check + desktop-store + theme vitest green; CSP non-null; mcp removed from default capabilities.
</verification>

<success_criteria>
User can change theme via existing menu and preference persists through store with dual-write boot path; browser chrome shortcuts blocked in release; CSP no longer null.
</success_criteria>

<output>
Create `.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-02-SUMMARY.md` when done
</output>
