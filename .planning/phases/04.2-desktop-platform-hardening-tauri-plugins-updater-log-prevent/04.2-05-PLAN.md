---
phase: 04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent
plan: 05
type: execute
wave: 5
depends_on:
  - 04.2-03
  - 04.2-04
files_modified:
  - apps/desktop/src-tauri/tauri.conf.json
  - apps/desktop/src-tauri/Cargo.toml
  - apps/desktop/src-tauri/src/lib.rs
  - apps/desktop/src-tauri/capabilities/default.json
  - apps/desktop/package.json
  - .github/workflows/desktop-build.yml
  - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-UPDATER-OPS.md
autonomous: false
requirements:
  - DESK-04
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
user_setup:
  - service: faynosync
    why: "Self-hosted update server on Coolify (D-05/D-09); not deployed yet"
    env_vars:
      - name: FAYNOSYNC_ADMIN_CREDENTIALS
        source: "Coolify FaynoSync service secrets after deploy"
    dashboard_config:
      - task: "Approve Coolify FaynoSync service + public HTTPS updates subdomain (D-09)"
        location: "via user-coolify MCP / coolify CLI — not Coolify UI"
  - service: tauri-updater-signing
    why: "Private key must live only in CI/Coolify secrets (D-06)"
    env_vars:
      - name: TAURI_SIGNING_PRIVATE_KEY
        source: "Generated locally with tauri signer generate; store in GitHub Actions secrets + Coolify"
      - name: TAURI_SIGNING_PRIVATE_KEY_PASSWORD
        source: "If key encrypted; CI secret only"
must_haves:
  truths:
    - "Human confirmed shipping updater pubkey into clients (D-06 one-way) before pubkey lands in tauri.conf."
    - "FaynoSync prod HTTPS endpoint and separate staging endpoint exist or are documented blocked with resume path (D-05, D-09, D-12)."
    - "tauri.conf plugins.updater has pubkey content (not path) and stable-channel endpoint template; no user-editable URL UI (D-06, D-07, D-08)."
    - "bundle.createUpdaterArtifacts true; Windows updater primary target NSIS; macOS updater archive path documented (D-27, D-28)."
    - "CI can sign with TAURI_SIGNING_PRIVATE_KEY from secrets; private key never committed (D-06)."
  artifacts:
    - apps/desktop/src-tauri/tauri.conf.json
    - .github/workflows/desktop-build.yml
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-UPDATER-OPS.md
  key_links:
    - "updater endpoints point at FaynoSync checkVersion with channel=stable and updater=tauri (D-07, D-11)."
    - "Staging endpoint selected only via build env — prod builds never use staging (D-12)."
  prohibitions:
    - statement: "Private updater key must never be committed to git (D-06)."
      status: forbidden
      verification: "git grep / secret scan finds no private key PEM in repo."
    - statement: "Do not deploy beta/nightly channels in 4.2 (D-07)."
      status: forbidden
      verification: "tauri.conf endpoints use channel=stable only."
    - statement: "Do not perform OS code-signing/notarization in 4.2 (D-06 deferred)."
      status: forbidden
      verification: "No Apple/Windows cert steps added as required gates."
---

<objective>
Ship ops foundation: human gate for D-06 pubkey, FaynoSync Coolify deploy (prod+staging), updater plugin config + CI signing secrets wiring (DESK-04).

Purpose: Make signed update channel real before client UX (D-05…D-12, D-27…D-29).
Output: 04.2-UPDATER-OPS.md, tauri.conf updater block, updater/process plugins registered, CI signing env.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-CONTEXT.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-RESEARCH.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/COVERAGE.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-03-SUMMARY.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Confirm D-06 updater pubkey one-way door</name>
  <read_first>
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-CONTEXT.md (D-06)
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-RESEARCH.md (Pitfall 6)
  </read_first>
  <decision>
    Ship generated updater public key into `tauri.conf.json` now (clients pin pubkey). Private key only in CI/Coolify secrets. OS code-signing remains deferred after 4.2.
  </decision>
  <context>
    This is a one-way door: rotating the pubkey later requires a coordinated release to every installed client. Undo cost is a migration/reinstall wave.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with pubkey</name>
      <pros>Unblocks DESK-04 signed channel in 4.2; matches locked D-06</pros>
      <cons>Pubkey rotation later needs coordinated client release</cons>
    </option>
    <option id="defer">
      <name>Defer updater pubkey</name>
      <pros>Avoids embedding key until ops ready</pros>
      <cons>Blocks DESK-04 ship goal for this phase</cons>
    </option>
  </options>
  <action>
    On "proceed with pubkey", executor runs tauri signer generate, places public key PEM content into tauri.conf plugins.updater.pubkey, documents private-key secret placement in UPDATER-OPS.md — never write private key into the repo. On defer, stop plan and mark DESK-04 blocked in SUMMARY.
  </action>
  <acceptance_criteria>
    - Human chooses proceed or defer explicitly.
    - If proceed: pubkey string present in tauri.conf; private key absent from git-tracked files.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const j=require('./apps/desktop/src-tauri/tauri.conf.json'); if(!j.plugins?.updater?.pubkey) { console.error('AWAITING_PUBKEY'); process.exit(1);} if(String(j.plugins.updater.pubkey).includes('PRIVATE')) process.exit(1); console.log('PUBKEY_PRESENT')"</automated>
  </verify>
  <resume-signal>Type "proceed with pubkey" or "defer updater pubkey"</resume-signal>
  <done>D-06 door confirmed or phase ship explicitly deferred.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Approve FaynoSync domain + secrets for Coolify deploy</name>
  <precondition>Coolify MCP/CLI authenticated; Docker available for compose pull</precondition>
  <read_first>
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-RESEARCH.md (FaynoSync ops, Environment Availability)
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/COVERAGE.md
    - .cursor/rules/coolify-mcp-cli.mdc
  </read_first>
  <action>
    Provide the public HTTPS updates subdomain slug (within D-09) and any FaynoSync admin secrets the executor cannot mint, so Coolify MCP/CLI deploy can proceed for prod + staging (D-05, D-12).
  </action>
  <instructions>
    Executor will deploy FaynoSync via user-coolify MCP + coolify CLI (no Coolify UI). Human must: (1) confirm domain slug e.g. updates.*; (2) paste or confirm Coolify-side secrets for FaynoSync admin; (3) confirm staging twin hostname. After resume, executor writes 04.2-UPDATER-OPS.md and completes deploy+DNS checks.
  </instructions>
  <verification>
    Coolify list_services shows FaynoSync prod (+ staging); 04.2-UPDATER-OPS.md lists prod/staging URLs with channel=stable; HTTPS checkVersion reachable.
  </verification>
  <acceptance_criteria>
    - Human provided domain slug + secret confirmation.
    - After executor continues: UPDATER-OPS.md exists with stable channel URLs; no private updater key in ops doc.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-UPDATER-OPS.md && grep -q 'stable' .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-UPDATER-OPS.md</automated>
    <human-check>Confirm HTTPS updates host resolves and staging is distinct from prod.</human-check>
  </verify>
  <resume-signal>Type "faynosync secrets ready" with prod+staging hostnames, or describe blocker</resume-signal>
  <done>Human unblocked FaynoSync Coolify deploy inputs.</done>
</task>

<task type="auto">
  <name>Task 3: Wire updater+process plugins, endpoints, CI signing</name>
  <reversibility rating="one-way">D-06 pubkey embedded in clients after Task 1 approval.</reversibility>
  <files>apps/desktop/package.json, apps/desktop/src-tauri/Cargo.toml, apps/desktop/src-tauri/src/lib.rs, apps/desktop/src-tauri/tauri.conf.json, apps/desktop/src-tauri/capabilities/default.json, .github/workflows/desktop-build.yml, .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-UPDATER-OPS.md</files>
  <read_first>
    - apps/desktop/src-tauri/tauri.conf.json
    - .github/workflows/desktop-build.yml
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-UPDATER-OPS.md
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-RESEARCH.md (updater config skeleton)
  </read_first>
  <action>
    After pubkey + human domain/secrets ready: deploy FaynoSync via user-coolify MCP + coolify CLI (D-05/D-09/D-12) and write 04.2-UPDATER-OPS.md with checkVersion URL template channel=stable updater=tauri (D-07/D-11), CI+manual upload notes (D-10), optional GH mirror (D-29/D-30). Then add official updater + process plugins (OK audit). Set plugins.updater.endpoints from UPDATER-OPS prod URL with {{current_version}}/{{target}}/{{arch}} and channel=stable (D-07/D-08). Staging URL via env-gated build config only (D-12). Enable createUpdaterArtifacts; prefer NSIS for Windows updater artifacts; macOS .app.tar.gz for updates, DMG for first install OK (D-27/D-28). Register plugins in lib.rs + capabilities. Update desktop-build.yml to pass TAURI_SIGNING_PRIVATE_KEY from GitHub secrets and upload signed artifacts + notes to FaynoSync; optional GH Release mirror. Never commit private key. Do not add Settings URL UI (D-08).
  </action>
  <acceptance_criteria>
    - tauri.conf has plugins.updater.pubkey and endpoints with channel=stable.
    - createUpdaterArtifacts is true; Windows updater path documents NSIS primary.
    - Cargo/package include updater + process.
    - desktop-build.yml references TAURI_SIGNING_PRIVATE_KEY without inlining secret values.
    - cargo check exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const j=require('./apps/desktop/src-tauri/tauri.conf.json'); if(!j.plugins?.updater?.pubkey) process.exit(1); if(!j.bundle?.createUpdaterArtifacts) process.exit(1); const e=(j.plugins.updater.endpoints||[]).join(' '); if(!e.includes('stable')) process.exit(1); console.log('UPDATER_CONF_OK')" && (cd apps/desktop/src-tauri && cargo check) && grep -q 'TAURI_SIGNING_PRIVATE_KEY' .github/workflows/desktop-build.yml</automated>
  </verify>
  <done>Signed updater channel configured for DESK-04 ops half.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol / Artifact | Kind |
|-------------------|------|
| `plugins.updater` | tauri.conf block |
| `04.2-UPDATER-OPS.md` | ops SSOT |
| CI signing env wiring | workflow |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → FaynoSync HTTPS | Public metadata/artifacts; signature verifies integrity |
| CI secrets → signer | Private key never in repo |
| Prod vs staging endpoints | Build-time selection only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-10 | Tampering | update binary | critical | mitigate | Mandatory signatures + pubkey pin (tasks 1/3) |
| T-04.2-11 | Spoofing | update server | high | mitigate | HTTPS FaynoSync; no user-editable URL (D-08/D-11) |
| T-04.2-12 | Information Disclosure | private key | critical | mitigate | Secrets only; ops doc without PEM (tasks 1–3) |
| T-04.2-SC | Tampering | plugin installs | high | mitigate | Official updater/process only |
</threat_model>

<verification>
Pubkey present; FaynoSync ops doc; CI signing wired; cargo check.
</verification>

<success_criteria>
Ship path can produce and host signed updater artifacts for stable channel (DESK-04 ops).
</success_criteria>

<output>
Create `.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-05-SUMMARY.md` when done
</output>
