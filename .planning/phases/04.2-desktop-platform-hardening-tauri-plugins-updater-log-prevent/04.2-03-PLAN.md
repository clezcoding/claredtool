---
phase: 04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent
plan: 03
type: execute
wave: 3
depends_on:
  - 04.2-02
files_modified:
  - apps/desktop/package.json
  - apps/desktop/src-tauri/Cargo.toml
  - apps/desktop/src-tauri/src/lib.rs
  - apps/desktop/src-tauri/capabilities/default.json
  - apps/desktop/src/lib/link-guard.ts
  - apps/desktop/src/lib/link-guard.test.ts
  - apps/desktop/src/main.tsx
  - apps/desktop/src/App.tsx
autonomous: false
requirements:
  - DESK-01
  - DESK-02
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "External https links open in system browser via opener; file/unknown blocked and logged; clared:// stays in-app; http limited to localhost/dev (D-34)."
    - "OIDC login WebView allowlist and deep-link handling in lib.rs remain unchanged (DESK-01, D-34)."
    - "tauri-plugin-window-state persists window geometry without writing geometry into store (D-16)."
    - "tauri-plugin-notification is registered; permission is not requested on cold start (D-20, D-21)."
    - "tauri-plugin-dragout is cfg(macos) only so Windows cargo check still succeeds (D-35)."
  artifacts:
    - apps/desktop/src/lib/link-guard.ts
    - apps/desktop/src-tauri/src/lib.rs
    - apps/desktop/src-tauri/Cargo.toml
  key_links:
    - "main.tsx or App.tsx installs link-guard on main WebView only."
    - "link-guard uses @tauri-apps/plugin-opener for https."
    - "dragout dependency + plugin init behind target_os=macos."
  prohibitions:
    - statement: "Do not drive arbitrary external sites inside the main WebView (D-34)."
      status: forbidden
      verification: "link-guard never returns navigate-in-webview for https external."
    - statement: "Do not enable dragout on Windows/Linux builds (D-35)."
      status: forbidden
      verification: "Cargo.toml/lib.rs use cfg(target_os = \"macos\") for dragout."
    - statement: "Do not request notification permission at app cold start (D-21)."
      status: forbidden
      verification: "No requestPermission in main/App boot path."
---

<objective>
Expand desktop feel: link-guard, window-state, notification plugin (lazy policy), and macOS-only dragout smoke (DESK-02) without breaking OIDC (DESK-01).

Purpose: Complete Wave 1 ROADMAP feel items after tracer store/CSP foundation.
Output: link-guard module + tests green; window-state + notification plugins; dragout cfg-gated after human SUS verify.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-CONTEXT.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-RESEARCH.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-PATTERNS.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Link-guard scheme matrix + install</name>
  <files>apps/desktop/src/lib/link-guard.ts, apps/desktop/src/lib/link-guard.test.ts, apps/desktop/src/main.tsx, apps/desktop/src/App.tsx</files>
  <read_first>
    - apps/desktop/src-tauri/src/lib.rs (allow_navigation, clared deep-link)
    - apps/desktop/src/auth/session-provider.tsx
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-PATTERNS.md (link-guard)
    - apps/desktop/src/lib/link-guard.test.ts
  </read_first>
  <behavior>
    - https → action open-external (opener)
    - http://localhost* and http://127.0.0.1* allowed in dev; other http blocked
    - clared: → in-app (no opener)
    - file: and unknown schemes → block + German log message (D-54)
  </behavior>
  <action>
    Implement `decideLinkAction(url: string)` and `installLinkGuard()` that intercepts click/auxclick on anchors and window open attempts in the main WebView only (D-34). Use existing opener plugin for https. Do not alter Rust login-window navigation allowlist. Make link-guard.test.ts pass. Wire install from main.tsx or App.tsx after paint (D-36 — do not block boot). Log blocked navigations in German (D-54).
  </action>
  <acceptance_criteria>
    - link-guard.ts exports decideLinkAction and installLinkGuard.
    - vitest link-guard.test.ts exits 0 covering https/http-localhost/clared/file/unknown.
    - Login/OIDC Rust paths untouched (grep open_login_window / allow_navigation still present unchanged in intent).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec vitest run src/lib/link-guard.test.ts</automated>
  </verify>
  <done>Link-guard enforces D-34 scheme matrix on main WebView.</done>
</task>

<task type="auto">
  <name>Task 2: window-state + notification plugins (lazy policy)</name>
  <files>apps/desktop/package.json, apps/desktop/src-tauri/Cargo.toml, apps/desktop/src-tauri/src/lib.rs, apps/desktop/src-tauri/capabilities/default.json</files>
  <read_first>
    - apps/desktop/src-tauri/src/lib.rs
    - apps/desktop/src-tauri/capabilities/default.json
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-RESEARCH.md (notification lazy permission)
  </read_first>
  <action>
    Add official `tauri-plugin-window-state` and `tauri-plugin-notification` via `pnpm tauri add` when possible (OK legitimacy). Register in lib.rs. Grant least-privilege capabilities. Do not write window geometry into desktop-store (D-16). Do not call requestPermission on startup (D-21) — registration only; update-ready notify lands in Plan 06. Keep boot free of network waits (D-36).
  </action>
  <acceptance_criteria>
    - Cargo.toml and package.json include window-state and notification plugins.
    - lib.rs initializes both plugins.
    - default.json grants their permissions.
    - No requestPermission in main.tsx/App.tsx boot path.
    - cargo check exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/desktop/src-tauri && cargo check && grep -E 'window.state|notification' Cargo.toml</automated>
    <human-check>After later update path exists: confirm notification permission sheet appears only on first update-ready, not cold start (D-21).</human-check>
  </verify>
  <done>Window-state and notification plugins registered with lazy permission policy preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Verify SUS dragout then cfg(macos) smoke install</name>
  <read_first>
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-RESEARCH.md (Package Legitimacy Audit — tauri-plugin-dragout SUS)
    - https://crates.io/crates/tauri-plugin-dragout (human opens)
  </read_first>
  <what-built>Supply-chain gate before community dragout crate; after approval executor adds cfg(macos) dependency + plugin init smoke only (D-35).</what-built>
  <how-to-verify>Open crates.io/tauri-plugin-dragout; confirm repo alexqqqqqq777/tauri-plugin-dragout, review recent downloads/risk; approve smoke-only macOS use or block.</how-to-verify>
  <action>
    Human verifies tauri-plugin-dragout legitimacy (SUS per RESEARCH). On resume signal "dragout verified", executor adds optional/macos-cfg dependency and `#[cfg(target_os = "macos")]` plugin init in lib.rs for smoke only — no PDF drag-out UX (deferred). Windows/Linux must still `cargo check`. If human blocks, document skip in SUMMARY and leave Windows no-op; do not force install.
  </action>
  <acceptance_criteria>
    - Human records dragout verified or blocked with evidence.
    - If verified: Cargo.toml/lib.rs gate dragout with target_os=macos; cargo check on host succeeds.
    - If blocked: SUMMARY notes skip; no dragout dependency.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/desktop/src-tauri && cargo check</automated>
    <human-check>Confirm crates.io identity for tauri-plugin-dragout before install.</human-check>
  </verify>
  <resume-signal>Type "dragout verified" to install cfg(macos) smoke, or "dragout blocked" to skip with SUMMARY note</resume-signal>
  <done>Dragout either cfg-gated smoke-installed or explicitly skipped after human gate.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Kind |
|--------|------|
| `decideLinkAction` | fn |
| `installLinkGuard` | fn |
| `LinkAction` | type (open-external \| in-app \| block) |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anchor click → OS browser | opener for https only |
| Main WebView navigation | link-guard scheme allowlist |
| Login WebView | separate Rust allowlist — must stay |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-05 | Tampering | link-guard.ts | high | mitigate | Scheme allowlist; block file/unknown (task 1) |
| T-04.2-06 | Spoofing | open redirect | high | mitigate | No in-webview external drive-by; https → opener (task 1) |
| T-04.2-07 | Tampering | dragout crate | medium | mitigate | blocking-human legitimacy + macos cfg only (task 3) |
| T-04.2-SC | Tampering | npm/cargo | high | mitigate | Official plugins OK; dragout gated |
</threat_model>

<verification>
link-guard tests green; window-state+notification in Cargo; cargo check; dragout SUS gate completed.
</verification>

<success_criteria>
External links safe; window geometry via official plugin; notification plugin ready without cold-start prompt; dragout smoke policy honored.
</success_criteria>

<output>
Create `.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-03-SUMMARY.md` when done
</output>
