---
phase: 04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/__tests__/setup.ts
  - apps/desktop/src/lib/link-guard.test.ts
  - apps/desktop/src/lib/desktop-store.test.ts
  - apps/desktop/src/lib/clipboard.test.ts
  - apps/desktop/src/lib/updater.test.ts
  - apps/desktop/src/lib/sentry.test.ts
  - apps/desktop/src/lib/log.test.ts
  - apps/desktop/src/__tests__/capabilities-desk.test.ts
  - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-VALIDATION.md
autonomous: true
requirements:
  - DESK-02
  - DESK-03
  - DESK-04
  - DESK-05
  - DESK-06
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Vitest mocks exist for store, log, updater, process, clipboard-manager, notification, and window-state plugins so later plans can RED→GREEN without harness churn (D-15)."
    - "Wave 0 test files exist for link-guard, desktop-store, clipboard, updater, sentry scrub, and log helper and currently fail or skip-assert until implementations land."
    - "Capabilities fixture test asserts clipboard write-only permission token and documents mcp-bridge release exclusion expectation (D-04, D-49, D-51)."
  artifacts:
    - apps/desktop/src/__tests__/setup.ts
    - apps/desktop/src/lib/link-guard.test.ts
    - apps/desktop/src/lib/desktop-store.test.ts
    - apps/desktop/src/lib/clipboard.test.ts
    - apps/desktop/src/lib/updater.test.ts
    - apps/desktop/src/lib/sentry.test.ts
    - apps/desktop/src/lib/log.test.ts
    - apps/desktop/src/__tests__/capabilities-desk.test.ts
  key_links:
    - "setup.ts vi.mock entries cover every @tauri-apps/plugin-* import path used by 04.2 libs."
    - "04.2-VALIDATION.md Per-Task Verification Map Task IDs match this plan set."
  prohibitions:
    - statement: "Do not install production plugin packages in Wave 0 — mocks only."
      status: forbidden
      verification: "apps/desktop/package.json has no new @tauri-apps/plugin-{store,log,updater,process,clipboard-manager,notification,window-state} deps after this plan."
    - statement: "Do not implement production helpers in Wave 0 beyond empty stubs required for import."
      status: forbidden
      verification: "No desktop-store.ts / clipboard.ts / link-guard.ts / updater.ts / sentry.ts / log.ts production bodies beyond minimal export stubs if required for test import."
---

<objective>
Wave 0 Nyquist scaffolding for Phase 04.2: extend Vitest plugin mocks and create failing/stub test files so expansion plans have automated RED targets before plugin wiring.

Purpose: Satisfy VALIDATION.md Wave 0 gaps before tracer/expansion; prevent mock churn mid-phase (DESK-02…06).
Output: Extended setup.ts mocks, six lib test files, capabilities fixture test, VALIDATION task-ID fill.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-CONTEXT.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-RESEARCH.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-PATTERNS.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Vitest setup mocks for 04.2 plugins</name>
  <files>apps/desktop/src/__tests__/setup.ts</files>
  <read_first>
    - apps/desktop/src/__tests__/setup.ts
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-PATTERNS.md (section setup.ts)
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <action>
    Extend `apps/desktop/src/__tests__/setup.ts` with `vi.mock` stubs for `@tauri-apps/plugin-store` (`load` returns in-memory get/set/save), `@tauri-apps/plugin-log`, `@tauri-apps/plugin-updater` (`check` resolves null), `@tauri-apps/plugin-process` (`relaunch` no-op), `@tauri-apps/plugin-clipboard-manager` (`writeText` resolves), `@tauri-apps/plugin-notification` (`isPermissionGranted`/`requestPermission`/`sendNotification`), and `@tauri-apps/plugin-window-state`. Match existing deep-link/os mock style from PATTERNS. Do not add npm packages. Do not implement production helpers here.
  </action>
  <acceptance_criteria>
    - setup.ts contains vi.mock for each of: plugin-store, plugin-log, plugin-updater, plugin-process, plugin-clipboard-manager, plugin-notification, plugin-window-state.
    - Existing auth/deep-link/os mocks remain intact.
    - `pnpm --filter ./apps/desktop test` still exits 0 on current suite (no new imports yet).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./apps/desktop test</automated>
  </verify>
  <done>All 04.2 official plugin import paths mocked in setup.ts; full desktop vitest green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Wave 0 lib test stubs (RED targets)</name>
  <files>apps/desktop/src/lib/link-guard.test.ts, apps/desktop/src/lib/desktop-store.test.ts, apps/desktop/src/lib/clipboard.test.ts, apps/desktop/src/lib/updater.test.ts, apps/desktop/src/lib/sentry.test.ts, apps/desktop/src/lib/log.test.ts</files>
  <read_first>
    - apps/desktop/src/lib/theme.test.ts
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-VALIDATION.md
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-RESEARCH.md (Validation Architecture)
  </read_first>
  <behavior>
    - link-guard: https opens via opener path; http only localhost/dev; clared in-app; file/unknown blocked (D-34).
    - desktop-store: Tauri path uses store; non-Tauri falls back to localStorage; migration reads clared-theme once (D-14/D-15).
    - clipboard: copyText success true; failure returns false and surfaces clipboard.failed key path (D-03/D-04).
    - updater: startup+24h cadence constants; dialog states available/later/install/relaunch; silent fail does not toast (D-17/D-23).
    - sentry: beforeSend strips Authorization, token-like strings, email, invoice customer names (D-40).
    - log: helper exposes debug vs info level selection for env (D-43).
  </behavior>
  <action>
    Create the six test files listed in VALIDATION Wave 0. Each file imports the future module path under `apps/desktop/src/lib/` and asserts the behaviors above. If production modules do not exist yet, add minimal empty export stubs (`export {}` or named stubs that intentionally fail assertions) so vitest can load files — stubs must not contain real plugin logic. Prefer tests that fail until later plans implement. Do not install plugins. Cite D-03, D-04, D-14, D-15, D-17, D-23, D-34, D-40, D-43 in test titles/comments only as needed for traceability.
  </action>
  <acceptance_criteria>
    - All six test files exist at the paths in files.
    - Running `pnpm --filter ./apps/desktop exec vitest run src/lib/link-guard.test.ts src/lib/desktop-store.test.ts src/lib/clipboard.test.ts src/lib/updater.test.ts src/lib/sentry.test.ts src/lib/log.test.ts` exits non-zero OR reports failing assertions until implementations land (RED is expected and OK for Wave 0).
    - No new production plugin dependencies in package.json.
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/desktop/src/lib/link-guard.test.ts && test -f apps/desktop/src/lib/desktop-store.test.ts && test -f apps/desktop/src/lib/clipboard.test.ts && test -f apps/desktop/src/lib/updater.test.ts && test -f apps/desktop/src/lib/sentry.test.ts && test -f apps/desktop/src/lib/log.test.ts && if pnpm --filter ./apps/desktop exec vitest run src/lib/link-guard.test.ts src/lib/desktop-store.test.ts src/lib/clipboard.test.ts src/lib/updater.test.ts src/lib/sentry.test.ts src/lib/log.test.ts; then echo EXPECTED_RED_GOT_GREEN; exit 1; else echo W0_RED_OK; fi</automated>
  </verify>
  <done>Wave 0 RED targets present for DESK-02…06 lib behaviors.</done>
</task>

<task type="auto">
  <name>Task 3: Capabilities fixture + VALIDATION map IDs</name>
  <files>apps/desktop/src/__tests__/capabilities-desk.test.ts, .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-VALIDATION.md</files>
  <read_first>
    - apps/desktop/src-tauri/capabilities/default.json
    - apps/desktop/src-tauri/capabilities/login.json
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-VALIDATION.md
  </read_first>
  <action>
    Add `capabilities-desk.test.ts` that reads `default.json` as text/JSON and currently documents expected end-state assertions for D-04/D-49/D-51: must eventually include `clipboard-manager:allow-write-text`, must not include `clipboard-manager:allow-read-text`, and release default capability set must not grant `mcp-bridge:default` (assert as TODO/failing until Plan 02+ tightens). Update 04.2-VALIDATION.md Per-Task Verification Map replacing TBD rows with concrete Task IDs matching plans 01–08 (W0 = 04.2-01-T1…T3). Do not modify capabilities JSON in this plan.
  </action>
  <acceptance_criteria>
    - capabilities-desk.test.ts exists and imports/reads default.json.
    - VALIDATION.md map lists Task IDs for plans 01–08 (no TBD-W0 leftover in the map body).
    - default.json unchanged in this plan.
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/desktop/src/__tests__/capabilities-desk.test.ts && grep -q '04.2-01' .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-VALIDATION.md && echo VAL_MAP_OK</automated>
  </verify>
  <done>Capabilities fixture + VALIDATION IDs ready for Nyquist sampling.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols introduced or owned across Phase 04.2 (this plan scaffolds tests only; production symbols land in later plans):

| Symbol | Module | Plan |
|--------|--------|------|
| (mocks) `load` / `writeText` / `check` / `relaunch` | setup.ts mocks | 01 |
| Wave 0 test suites | `*.test.ts` above | 01 |
| `getThemePref` / `setThemePref` / `migrateThemeToStore` | desktop-store.ts | 02 |
| `prevent_default` | lib.rs | 02 |
| `decideLinkAction` / `installLinkGuard` | link-guard.ts | 03 |
| `copyText` | clipboard.ts | 08 |
| `checkForUpdates` / `installUpdateFlow` | updater.ts | 06 |
| `scrubSentryEvent` / `initDesktopSentry` | sentry.ts | 04 |
| `desktopLog` | log.ts | 04 |
| `UpdateDialog` | update-dialog.tsx | 06 |
| `DebugPanel` | debug-panel.tsx | 07 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vitest harness → mocked plugins | Tests must not call real OS clipboard/network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-01 | Information Disclosure | setup mocks | low | accept | Mocks stay in-process; no DSN/key material in fixtures |
| T-04.2-SC | Tampering | npm/cargo installs | high | mitigate | No installs in this plan; later plans gate SUS packages |
</threat_model>

<verification>
Wave 0 files exist; setup mocks present; VALIDATION map updated; production package.json unchanged for new plugins.
</verification>

<success_criteria>
Nyquist Wave 0 checklist items for lib tests + setup mocks + capabilities fixture are on disk.
</success_criteria>

<output>
Create `.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-01-SUMMARY.md` when done
</output>
