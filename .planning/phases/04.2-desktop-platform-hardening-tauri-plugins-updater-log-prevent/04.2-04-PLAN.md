---
phase: 04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent
plan: 04
type: execute
wave: 4
depends_on:
  - 04.2-03
files_modified:
  - apps/desktop/package.json
  - apps/desktop/src-tauri/Cargo.toml
  - apps/desktop/src-tauri/src/lib.rs
  - apps/desktop/src-tauri/capabilities/default.json
  - apps/desktop/src/lib/log.ts
  - apps/desktop/src/lib/log.test.ts
  - apps/desktop/src/lib/sentry.ts
  - apps/desktop/src/lib/sentry.test.ts
  - apps/desktop/src/main.tsx
autonomous: false
requirements:
  - DESK-03
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "tauri-plugin-log writes to app data LogDir with plugin rotation defaults; Dev=debug Prod=info (D-42, D-43, D-44)."
    - "Sentry uses tauri-plugin-sentry 0.6 (not sentry-tauri); EU DSN env-gated; empty DSN no-ops in local dev (D-37, D-38)."
    - "sendDefaultPii false; beforeSend scrubs tokens, Authorization, keychain values, email/name, invoice/customer names (D-39, D-40)."
    - "release and environment tags set; boot does not await Sentry/network (D-41, D-36)."
    - "Internal log and Sentry messages are German (D-54)."
  artifacts:
    - apps/desktop/src/lib/log.ts
    - apps/desktop/src/lib/sentry.ts
    - apps/desktop/src-tauri/src/lib.rs
  key_links:
    - "lib.rs initializes sentry before other plugins when DSN present."
    - "main.tsx calls initDesktopSentry fire-and-forget after first paint path."
    - "log helper maps level by import.meta.env.DEV vs production."
  prohibitions:
    - statement: "Do not install crate sentry-tauri 0.3.1 or GlitchTip (D-37)."
      status: forbidden
      verification: "Cargo.toml depends on tauri-plugin-sentry; no sentry-tauri; no GlitchTip URLs."
    - statement: "Do not enable performance tracing above 0.1 or on prod by default (D-39)."
      status: forbidden
      verification: "sentry.ts tracesSampleRate is 0 or ≤0.1 staging-only."
    - statement: "Open log folder must not appear in prod Help menu (D-45)."
      status: forbidden
      verification: "No prod menu item for open log folder; debug panel only later."
---

<objective>
Wire observability: official log plugin + Sentry EU Free via tauri-plugin-sentry 0.6 with scrubbing and env gates (DESK-03, D-37…D-45, D-54).

Purpose: Crash/error pipeline without PII leaks; local rotating logs.
Output: log.ts + sentry.ts green tests; Rust/JS init; optional sentry-api after SUS gate.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-CONTEXT.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-RESEARCH.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-PATTERNS.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-02-SUMMARY.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/COVERAGE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tauri-plugin-log + desktopLog helper</name>
  <reversibility rating="costly">D-37 Sentry project coupling is separate; log plugin choice is low-cost but levels become operational habit.</reversibility>
  <files>apps/desktop/package.json, apps/desktop/src-tauri/Cargo.toml, apps/desktop/src-tauri/src/lib.rs, apps/desktop/src-tauri/capabilities/default.json, apps/desktop/src/lib/log.ts, apps/desktop/src/lib/log.test.ts</files>
  <read_first>
    - apps/desktop/src-tauri/src/lib.rs
    - apps/desktop/src/lib/money.ts
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-PATTERNS.md (log.ts analog: money.ts)
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-RESEARCH.md (log plugin, D-42…D-44)
    - apps/desktop/src/lib/log.test.ts
  </read_first>
  <behavior>
    - desktopLog.debug|info|error callable; level filter debug in DEV else info
    - messages accept German strings
  </behavior>
  <action>
    Add official tauri-plugin-log (OK audit). Register Builder with LogDir target and LevelFilter debug in debug_assertions / info in release (D-42/D-43). Use plugin rotation defaults only (D-44). Implement `desktopLog` wrapper in log.ts as a thin helper shaped like `money.ts` (PATTERNS role-match). Pass log.test.ts. Grant log capabilities. German message strings in helper examples (D-54).
  </action>
  <acceptance_criteria>
    - Cargo.toml has tauri-plugin-log; package.json has @tauri-apps/plugin-log.
    - lib.rs initializes log plugin with level split debug/release.
    - log.test.ts exits 0.
    - cargo check exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec vitest run src/lib/log.test.ts && (cd apps/desktop/src-tauri && cargo check)</automated>
  </verify>
  <done>File logging with correct levels available to the app.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify SUS tauri-plugin-sentry-api (if needed)</name>
  <read_first>
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-RESEARCH.md (Package Legitimacy — tauri-plugin-sentry-api SUS; prefer crates plugin + GitHub docs)
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/COVERAGE.md
  </read_first>
  <what-built>Gate before optional npm tauri-plugin-sentry-api. Preferred path: crates tauri-plugin-sentry 0.6 + @sentry/browser with init_with_no_injection for beforeSend control (A6).</what-built>
  <how-to-verify>Confirm whether npm tauri-plugin-sentry-api is required. If using @sentry/browser only + Rust plugin, choose "sentry-api skipped". If installing sentry-api, verify npm identity first.</how-to-verify>
  <action>
    Human chooses: (A) skip tauri-plugin-sentry-api and use @sentry/browser + Rust plugin only, or (B) install after verifying npm package. Do not install sentry-tauri. Create Sentry EU project on de.sentry.io if missing (user_setup). Resume with choice string.
  </action>
  <acceptance_criteria>
    - Resume signal is either "sentry-api skipped" or "sentry-api verified".
    - No sentry-tauri dependency ever.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('apps/desktop/src-tauri/Cargo.toml','utf8'); if(t.includes('sentry-tauri')) process.exit(1); console.log('NO_SENTRY_TAURI')"</automated>
    <human-check>Confirm Sentry EU project exists or will be created; decide sentry-api skip vs verify.</human-check>
  </verify>
  <resume-signal>Type "sentry-api skipped" (recommended) or "sentry-api verified"</resume-signal>
  <done>Sentry npm companion decision recorded; wrong crate name blocked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sentry EU init + beforeSend scrub</name>
  <reversibility rating="costly">D-37 DSN/project coupling — switching later needs coordinated release.</reversibility>
  <files>apps/desktop/src-tauri/Cargo.toml, apps/desktop/src-tauri/src/lib.rs, apps/desktop/package.json, apps/desktop/src/lib/sentry.ts, apps/desktop/src/lib/sentry.test.ts, apps/desktop/src/main.tsx</files>
  <read_first>
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-RESEARCH.md (Sentry correct package, A6)
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-PATTERNS.md (sentry.ts analog: auth/api.ts + RESEARCH sketch)
    - apps/desktop/src/auth/api.ts
    - apps/desktop/src/lib/sentry.test.ts
    - apps/desktop/src-tauri/src/lib.rs
  </read_first>
  <behavior>
    - scrubSentryEvent removes Authorization headers, bearer/token-like strings, emails, customer/invoice name fields from breadcrumbs/extras
    - init no-ops when DSN empty
    - sendDefaultPii false; sample rate 1.0 errors; traces off or ≤0.1 staging
  </behavior>
  <action>
    Add `tauri-plugin-sentry = "0.6"` and `sentry = "0.49"` to Cargo.toml. Init Rust client with empty-DSN no-op before other plugins (D-38). Frontend `initDesktopSentry` with sendDefaultPii false, beforeSend → scrubSentryEvent (D-40), release=app version, environment=dev|staging|prod (D-41). Prefer init_with_no_injection + @sentry/browser if needed for scrub (A6; PATTERNS sentry.ts / auth/api.ts env-gate style). Fire-and-forget after paint — never await before first paint (D-36). German event messages (D-54). Pass sentry.test.ts. Update CSP connect-src for de.sentry.io if needed (D-51). Document env var `SENTRY_DSN` / `VITE_SENTRY_DSN` in SUMMARY — secrets not in git.
  </action>
  <acceptance_criteria>
    - Cargo.toml has tauri-plugin-sentry; does not have sentry-tauri.
    - sentry.ts exports scrubSentryEvent and initDesktopSentry.
    - sentry.test.ts proves scrub strips token/email fixtures.
    - main does not block render on Sentry init.
    - cargo check + vitest sentry.test.ts exit 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec vitest run src/lib/sentry.test.ts && (cd apps/desktop/src-tauri && cargo check) && ! grep -q 'sentry-tauri' apps/desktop/src-tauri/Cargo.toml</automated>
  </verify>
  <done>Env-gated scrubbed Sentry EU + log pipeline live for DESK-03.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Kind |
|--------|------|
| `desktopLog` | object/fn |
| `initDesktopSentry` | fn |
| `scrubSentryEvent` | fn |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → de.sentry.io | Telemetry egress; DSN secret |
| Log files on disk | Local only; may contain operational msgs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-08 | Information Disclosure | sentry.ts beforeSend | high | mitigate | scrub tokens/PII; sendDefaultPii false (task 3) |
| T-04.2-09 | Information Disclosure | log files | medium | mitigate | No tokens in log helper; German ops msgs without payloads |
| T-04.2-SC | Tampering | sentry-api npm | high | mitigate | blocking-human gate (task 2); forbid sentry-tauri |
</threat_model>

<verification>
log + sentry unit tests green; cargo check; no sentry-tauri.
</verification>

<success_criteria>
Observability stack env-gated, scrubbed, non-blocking at boot (DESK-03).
</success_criteria>

<output>
Create `.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-04-SUMMARY.md` when done
</output>
