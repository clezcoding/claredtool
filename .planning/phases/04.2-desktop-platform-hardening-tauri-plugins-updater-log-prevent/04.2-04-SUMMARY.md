---
phase: 04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent
plan: 04
subsystem: desktop
tags: [tauri, observability, sentry, logging, desk-03]

requires:
  - phase: 04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent
    provides: CSP, link-guard, plugin foundation (Plans 02–03)
provides:
  - tauri-plugin-log with LogDir rotation and dev/prod level split
  - desktopLog helper for German ops messages
  - tauri-plugin-sentry 0.6 + @sentry/browser with init_with_no_injection
  - scrubSentryEvent beforeSend scrubbing and env-gated initDesktopSentry
affects: [04.2-06, 04.2-07, DESK-03]

actuals:
  tokens: 52000
  tasks: 3
  commits: 4

tech-stack:
  added:
    - "@tauri-apps/plugin-log@~2.9.0"
    - "tauri-plugin-log"
    - "@sentry/browser@~10.71.0"
    - "tauri-plugin-sentry@0.6"
    - "sentry@0.49"
  patterns:
    - "Sentry Rust init before other plugins; empty DSN no-op (D-38)"
    - "init_with_no_injection + @sentry/browser beforeSend scrub (A6)"
    - "initDesktopSentry via requestAnimationFrame after React render (D-36)"

key-files:
  created: []
  modified:
    - apps/desktop/src/lib/log.ts
    - apps/desktop/src/lib/sentry.ts
    - apps/desktop/src/main.tsx
    - apps/desktop/src-tauri/src/lib.rs
    - apps/desktop/src-tauri/Cargo.toml
    - apps/desktop/src-tauri/capabilities/default.json
    - apps/desktop/package.json
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-VALIDATION.md

key-decisions:
  - "Human chose sentry-api skipped: tauri-plugin-sentry 0.6 + @sentry/browser only; no tauri-plugin-sentry-api npm"
  - "Log plugin uses LogDir target with rotation defaults; debug in dev / info in release (D-42/D-43/D-44)"
  - "Sentry tracesSampleRate 0 except staging 0.1; sendDefaultPii false (D-39)"

patterns-established:
  - "desktopLog thin wrapper over @tauri-apps/plugin-log (money.ts role-match)"
  - "scrubSentryEvent redacts Authorization, tokens, emails, customer/invoice names before egress"

requirements-completed: [DESK-03]

coverage:
  - id: D1
    description: File logging via tauri-plugin-log with dev/prod level split and desktopLog helper
    requirement: DESK-03
    verification:
      - kind: unit
        ref: "pnpm --filter ./apps/desktop exec vitest run src/lib/log.test.ts"
        status: pass
      - kind: unit
        ref: "cd apps/desktop/src-tauri && cargo check"
        status: pass
    human_judgment: false
  - id: D2
    description: Sentry npm companion gate — no sentry-tauri; sentry-api skipped (Option A)
    requirement: DESK-03
    verification:
      - kind: other
        ref: "04.2-VALIDATION.md Package Legitimacy Gates + Cargo.toml assert"
        status: pass
    human_judgment: true
    rationale: Human confirmed sentry-api skipped at blocking-human checkpoint
  - id: D3
    description: Env-gated Sentry EU with beforeSend scrub; non-blocking boot init
    requirement: DESK-03
    verification:
      - kind: unit
        ref: "pnpm --filter ./apps/desktop exec vitest run src/lib/sentry.test.ts"
        status: pass
      - kind: unit
        ref: "cd apps/desktop/src-tauri && cargo check"
        status: pass
    human_judgment: false

duration: 18min
completed: 2026-08-28
status: complete
---

# Phase 04.2 Plan 04: Observability (Log + Sentry EU) Summary

**Official log plugin with rotating LogDir plus env-gated Sentry EU via tauri-plugin-sentry 0.6 and scrubbed @sentry/browser beforeSend**

## Performance

- **Duration:** ~18 min (continuation after Task 1 checkpoint)
- **Tasks:** 3/3
- **Commits:** 4 (edfbb1d, 74e7a14, 8a87643, docs)

## Accomplishments

- **Logging (DESK-03 / D-42–D-44):** `tauri-plugin-log` writes to app LogDir with plugin rotation defaults; Rust level `debug` in dev / `info` in release; `desktopLog` wrapper in `log.ts`.
- **Sentry gate (T-04.2-SC):** Human chose **sentry-api skipped** — `tauri-plugin-sentry` 0.6 + `@sentry/browser` with `init_with_no_injection`; no `tauri-plugin-sentry-api`; `sentry-tauri` forbidden and absent.
- **Sentry pipeline (D-37–D-41, D-54):** Rust client env-gated via `SENTRY_DSN` / `VITE_SENTRY_DSN` (empty = no-op); `initDesktopSentry` runs fire-and-forget after first paint; `scrubSentryEvent` strips Authorization, tokens, emails, customer/invoice names; `sendDefaultPii: false`; traces off except staging `0.1`.
- **CSP:** `connect-src` already allows `https://*.sentry.io` (covers `de.sentry.io`).

## Environment (secrets not in git)

| Variable | Where | Purpose |
|----------|-------|---------|
| `SENTRY_DSN` | Rust runtime / CI / Coolify | EU DSN for tauri-plugin-sentry |
| `VITE_SENTRY_DSN` | Vite build / dev | Browser SDK DSN for @sentry/browser |
| `SENTRY_ENVIRONMENT` / `VITE_SENTRY_ENVIRONMENT` | Optional | Override Rust `environment` tag (defaults dev/prod) |

Create project on [de.sentry.io](https://de.sentry.io) if missing; paste DSN into env only.

## Deviations from Plan

None — plan executed as written after human sentry-api skip.

## Self-Check: PASSED

- FOUND: apps/desktop/src/lib/log.ts
- FOUND: apps/desktop/src/lib/sentry.ts
- FOUND: apps/desktop/src-tauri/src/lib.rs
- FOUND: edfbb1d
- FOUND: 74e7a14
- FOUND: 8a87643
