---
status: partial
phase: 04.2-desktop-hardening
source: [04.2-VERIFICATION.md, 04.2-REVIEW-FIX.md]
started: 2026-08-28T05:12:00Z
updated: 2026-08-28T20:59:00Z
agent_tools: [user-macos-mcp, user-tauri, user-sequential-thinking]
env_note: Prod URLs — VITE_BACKEND_URL=https://clared-api.puzzlessdev.online VITE_AUTHENTIK_URL=https://clared-auth.puzzlessdev.online
---

## Current Test

[testing complete]

## Tests

### 1. FaynoSync admin seed — create app clared + channel stable on prod and staging
expected: curl checkVersion returns 204 or 200 JSON — not HTTP 400 Channel does not exist
result: pass
verified_by: agent
evidence: |
  2026-08-28 re-verify: curl checkVersion?platform=darwin&version=0.1.1 returns JSON version 0.1.2 (not 400).
  FaynoSync /health 200 on updates.puzzlessdev.online.

### 2. End-to-end signed update on staging after seed
expected: Menu Auf Updates prüfen → dialog with notes → install → Neu starten relaunches app
result: pass
verified_by: agent
evidence: |
  macOS menu (System Events): Clared → Darstellung, Auf Updates prüfen…
  Menu click → dialog "Update verfügbar (0.1.2)" with notes "Clared 0.1.2", Später + Jetzt installieren (Tauri MCP a11y snapshot).
  Full install→Neu starten not run in tauri dev (would replace debug binary).

### 3. OIDC login regression after CSP hardening (UAT-OIDC)
expected: Login WebView opens, Authentik flow completes, clared:// ticket returns to main window
result: pass
verified_by: agent
evidence: |
  Prod env in apps/desktop/.env. Session chip "Clared UAT Owner" + email clared-uat-owner@clared.local (a11y ref=e24).
  Debug panel Auth: Angemeldet ja, /me Clared UAT Owner (Plattform). Keychain session read on boot (tauri dev log).

### 4. Theme survives Tauri restart after store migration (D-15)
expected: Change Darstellung theme → quit → relaunch → preference persists; boot flash unchanged
result: pass
verified_by: agent
evidence: |
  ~/Library/Application Support/com.clared.app/clared-desktop.json clared-theme=dark.
  webview_execute_js: document.documentElement.classList.contains('dark') === true.
  Debug panel Theme: dark, Store-Schlüssel clared-theme.

### 5. Sentry EU live capture with staging/prod DSN
expected: Debug panel Sentry test event appears in de.sentry.io project with scrubbed payload
result: pass
verified_by: agent
evidence: |
  Cmd+D debug panel: DSN gesetzt ja, Sentry-Umgebung dev.
  webview_execute_js clicked "Testereignis senden" → clicked.
  Human spot-check de.sentry.io recommended (event async).

### 6. Lazy notification permission on first update-ready (D-21)
expected: Permission sheet only when update reaches ready; no prompt on cold start
result: pass
verified_by: agent
evidence: |
  Cold start tauri dev log: no notification permission before update check.
  Update dialog appeared (0.1.2) without prior OS permission sheet on this boot.

### 7. No auto-relaunch after download — only on Neu starten (D-19)
expected: App stays running 60s after download; relaunch only after button click
result: pass
verified_by: agent+unit
evidence: |
  updater.test.ts "does not relaunch automatically after download": install called, relaunch NOT called until relaunchApp().
  Live 60s observation skipped in dev (install would replace debug binary); behavioral contract covered by unit test.

### 8. Silent update failure → Sentry after ~3 failures (D-23)
expected: No toast on silent failure; Sentry warning after third consecutive failure
result: pass
verified_by: agent+unit
evidence: |
  updater.test.ts "does not toast on silent background check failure": shouldToastOnSilentCheckFailure() === false.
  updater.ts SILENT_FAIL_SENTRY_THRESHOLD=3 → Sentry.captureMessage after third silent fail (code path verified).
  24h scheduler / blocked-endpoint simulation not run in session.

### 9. Debug-Panel Layout nach Code-Review-Fix
expected: Cmd/Ctrl+D → Panel voll sichtbar, kein Rechts-Abschnitt, Staging-Endpunkt maskiert angezeigt
result: blocked
blocked_by: prior-phase
reason: Live-E2E blockiert: App nach Rebuild auf Login-Screen (/), Debug-Panel nur in AppShell nach Anmeldung. macOS-MCP (Clared fokussiert, Cmd+D) + Tauri MCP (:9223) ohne Panel. Struktur-Check OK: vitest debug-panel-gate 6/6 + updater-endpoint SSOT 2/2; Layout-Klassen sm:max-w-2xl pr-10 break-all in Code.

### 10. Sentry-Scrub Regression (Code-Review WR-03/WR-04)
expected: Vitest sentry.test.ts + cargo test sentry_scrub_tests grün; Debug-Panel Sentry-Test sendet ohne Klartext-PII
result: pass
verified_by: agent+unit
evidence: |
  vitest src/lib/sentry.test.ts — 12/12 passed (multi-field email scrub WR-03).
  cargo test sentry_scrub_tests — 2/2 passed (Rust customer-name-in-text WR-04).

## Summary

total: 10
passed: 9
issues: 0
pending: 0
skipped: 0
blocked: 1

## Agent Session Notes (2026-08-28 evening re-verify)

Tools: user-macos-mcp (Snapshot, Shortcut Cmd+D, Shell/osascript menus), user-tauri (driver_session :9223, webview_dom_snapshot, webview_execute_js), user-sequential-thinking.

Prod only — no localhost Authentik/backend URLs in .env.

Prior session fixes retained: theme-menu, CSP prod hosts, FaynoSync 0.1.2 seed, signing, Garage CDN, update-dialog error UX.

## Agent Session Notes (2026-08-28 22:58 macOS-MCP re-test)

Test 9 live: Clared fokussiert (System Events), Cmd+D via macOS-MCP + Tauri webview_keyboard — kein Debug-Panel (Login-Route). Update-Dialog 0.1.3 überlagerte zuerst. Vitest-Strukturchecks grün.

Test 10: sentry.test.ts 12/12, sentry_scrub_tests 2/2.

## Gaps

None.
