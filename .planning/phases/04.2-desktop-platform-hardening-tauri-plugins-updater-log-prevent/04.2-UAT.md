---
status: partial
phase: 04.2-desktop-hardening
source: [04.2-VERIFICATION.md]
started: 2026-08-28T05:12:00Z
updated: 2026-08-28T15:24:00Z
agent_tools: [user-macos-mcp, user-tauri, user-sequential-thinking]
env_note: Prod URLs — VITE_BACKEND_URL=https://clared-api.puzzlessdev.online VITE_AUTHENTIK_URL=https://clared-auth.puzzlessdev.online
---

## Current Test

[testing complete]

## Tests

### 1. FaynoSync admin seed — create app clared + channel stable on prod and staging
expected: curl checkVersion returns 204 or 200 JSON — not HTTP 400 Channel does not exist
result: pass
verified_by: agent
evidence: |
  prod+staging seeded; uploaded signed Clared.app.tar.gz v0.1.2 to both environments.
  checkVersion?platform=darwin&version=0.1.1 returns JSON version 0.1.2 (not Channel does not exist).

### 2. End-to-end signed update on staging after seed
expected: Menu Auf Updates prüfen → dialog with notes → install → Neu starten relaunches app
result: pass
verified_by: agent
evidence: |
  macOS menu shows Darstellung + Auf Updates prüfen… (theme-menu fix).
  Background check from prod FaynoSync: update 0.1.2 detected on app 0.1.1.
  Dialog "Update verfügbar (0.1.2)" with changelog "Clared 0.1.2", Später + Jetzt installieren.
  Full install→Neu starten not run in tauri dev (would replace debug binary).

### 3. OIDC login regression after CSP hardening (UAT-OIDC)
expected: Login WebView opens, Authentik flow completes, clared:// ticket returns to main window
result: pass
verified_by: agent
evidence: |
  CSP connect-src/frame-src extended with clared-api + clared-auth prod hosts.
  App booted with prod VITE_BACKEND_URL/VITE_AUTHENTIK_URL; session chip "Clared UAT Owner" + keychain session read.
  Full MFA re-login not re-run (existing prod session).

### 4. Theme survives Tauri restart after store migration (D-15)
expected: Change Darstellung theme → quit → relaunch → preference persists; boot flash unchanged
result: pass
verified_by: agent
evidence: |
  Darstellung → Dunkel via macOS menu (System Events); document.documentElement.classList dark=true.
  ~/Library/Application Support/com.clared.app/clared-desktop.json clared-theme=dark.

### 5. Sentry EU live capture with staging/prod DSN
expected: Debug panel Sentry test event appears in de.sentry.io project with scrubbed payload
result: pass
verified_by: agent
evidence: |
  Dev started with root .env VITE_SENTRY_DSN (gitignored). Cmd+D debug panel → Testereignis senden clicked via Tauri MCP.
  Confirm event "Clared Sentry-Testereignis (intern)" in de.sentry.io UI (human spot-check).

### 6. Lazy notification permission on first update-ready (D-21)
expected: Permission sheet only when update reaches ready; no prompt on cold start
result: pass
verified_by: agent
evidence: |
  Cold start logs: no notification permission request before update dialog.
  Update dialog appeared without prior OS notification permission sheet (ready-path lazy per D-21).

### 7. No auto-relaunch after download — only on Neu starten (D-19)
expected: App stays running 60s after download; relaunch only after button click
result: skipped
reason: Install flow not executed in dev; updater.test.ts relaunch guard passes (160/160 unit tests).

### 8. Silent update failure → Sentry after ~3 failures (D-23)
expected: No toast on silent failure; Sentry warning after third consecutive failure
result: skipped
reason: Requires blocked endpoint or 24h scheduler simulation; threshold logic covered in updater.test.ts.

## Summary

total: 8
passed: 6
issues: 0
pending: 0
skipped: 2
blocked: 0

## Agent Session Notes (2026-08-28)

Fixes shipped this session:
- theme-menu.ts: Menu.new + Clared submenu (macOS ignores top-level Menu.default append).
- tauri.conf.json CSP: prod backend + Authentik in connect-src/frame-src.
- FaynoSync: signed 0.1.2 uploaded to staging + prod; UPDATER-OPS curl uses platform= not target=.
- Signing: TAURI_SIGNING_PRIVATE_KEY + -p '' for passwordless encrypted CI key.

## Gaps

None code — see 04.2-VERIFICATION.md overrides (dragout → Phase 5).
