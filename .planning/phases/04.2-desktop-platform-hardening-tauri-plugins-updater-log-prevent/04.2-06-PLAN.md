---
phase: 04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent
plan: 06
type: execute
wave: 6
depends_on:
  - 04.2-05
files_modified:
  - apps/desktop/src/lib/updater.ts
  - apps/desktop/src/lib/updater.test.ts
  - apps/desktop/src/components/update-dialog.tsx
  - apps/desktop/src/lib/theme-menu.ts
  - apps/desktop/src/main.tsx
  - apps/desktop/src/components/app-shell.tsx
  - apps/desktop/src/i18n/locales/de.json
  - apps/desktop/src/i18n/locales/en.json
  - apps/desktop/src/__tests__/i18n.test.ts
autonomous: true
requirements:
  - DESK-04
  - DESK-01
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Update check runs on startup and about every 24h in background (D-17)."
    - "Update available shows in-app dialog with FaynoSync notes (or version-only if empty) and Jetzt installieren / Später (D-18, D-30)."
    - "After download user must click Neu starten via process relaunch — no forced mid-invoice restart, no 60s auto-restart (D-19)."
    - "Native menu Auf Updates prüfen on macOS and Windows parity (D-25); no Settings route (D-08)."
    - "Silent check failures log only; Sentry after repeated failures (N≈3); manual fail/Aktuell toasts (D-23)."
    - "OS notification only for update-ready (+ later debug test); permission requested lazily on first ready (D-20–D-22)."
    - "All user strings via i18next DE/EN fallbackLng de (D-52, D-53)."
  artifacts:
    - apps/desktop/src/lib/updater.ts
    - apps/desktop/src/components/update-dialog.tsx
    - apps/desktop/src/i18n/locales/de.json
    - apps/desktop/src/i18n/locales/en.json
  key_links:
    - "theme-menu or sibling installs update menu item calling checkForUpdates."
    - "update-dialog triggers downloadAndInstall then awaits user relaunch."
    - "Failed download aborts/cleanup — no corrupt apply (D-23)."
  prohibitions:
    - statement: "No auto-rollback UX in app (D-24)."
      status: forbidden
      verification: "updater.ts has no rollback/reinstall-previous flow."
    - statement: "No Settings screen for update URL (D-08)."
      status: forbidden
      verification: "No settings route or user endpoint field added."
---

<objective>
Client updater UX: cadence, native menu, dialog, process relaunch, lazy notification, i18n toasts (DESK-04 completion).

Purpose: User-controllable install path on top of Plan 05 signed channel (D-17…D-30, D-52…D-54).
Output: updater.ts + UpdateDialog + menu + i18n keys; tests green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-CONTEXT.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-RESEARCH.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-PATTERNS.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-UPDATER-OPS.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: updater state machine + cadence</name>
  <files>apps/desktop/src/lib/updater.ts, apps/desktop/src/lib/updater.test.ts</files>
  <read_first>
    - apps/desktop/src/lib/updater.test.ts
    - apps/desktop/src/lib/theme-menu.ts
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-RESEARCH.md (Pattern 2, D-17/D-23)
  </read_first>
  <behavior>
    - CHECK_INTERVAL_MS ≈ 24h; run check on startUpdateScheduler
    - silent failure increments counter; Sentry only after N=3 (discretion)
    - states: idle | available | downloading | ready | error
    - download failure cleans up and does not apply
  </behavior>
  <action>
    Implement updater.ts with checkForUpdates(manual: boolean), startUpdateScheduler(), and state callbacks. Use @tauri-apps/plugin-updater check/downloadAndInstall and @tauri-apps/plugin-process relaunch only when caller requests (D-19). Silent failures: desktopLog only; repeated → Sentry German message (D-23/D-54). Manual no-update / fail surfaces toast keys. Prefer FaynoSync body notes else version-only (D-30). Pass updater.test.ts with mocked plugins.
  </action>
  <acceptance_criteria>
    - updater.ts exports checkForUpdates, startUpdateScheduler, and typed state.
    - updater.test.ts exits 0 covering cadence constant, silent vs manual failure, no auto-relaunch.
    - No Settings URL configuration API.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec vitest run src/lib/updater.test.ts</automated>
  </verify>
  <done>Updater state machine and cadence tested.</done>
</task>

<task type="auto">
  <name>Task 2: UpdateDialog + i18n keys</name>
  <files>apps/desktop/src/components/update-dialog.tsx, apps/desktop/src/i18n/locales/de.json, apps/desktop/src/i18n/locales/en.json, apps/desktop/src/__tests__/i18n.test.ts, apps/desktop/src/components/app-shell.tsx</files>
  <read_first>
    - packages/ui/src/components/dialog.tsx
    - apps/desktop/src/__tests__/dialog-smoke.test.tsx
    - apps/desktop/src/i18n/locales/de.json
    - apps/desktop/src/components/app-shell.tsx
  </read_first>
  <action>
    Build UpdateDialog with @clared/ui Dialog: changelog/notes area, buttons for Jetzt installieren / Später / Neu starten after download (D-18/D-19). Wire AppShell feedback toast for update.manualFail / update.current / update.downloadFail keys (D-23). Add DE+EN keys under update.* and ensure i18n.test covers German strings (D-52). Version shown in dialog chrome (D-26). On update-ready, lazily request notification permission then send update-ready notify; if denied keep dialog and optional one-shot hint — never re-prompt every session (D-20–D-22).
  </action>
  <acceptance_criteria>
    - update-dialog.tsx exists and uses i18n keys for all visible strings.
    - de.json/en.json contain update.install, update.later, update.relaunch, update.current, update.manualFail (or equivalent names documented in SUMMARY).
    - i18n.test asserts at least German update.install / clipboard-adjacent not required here.
    - Dialog never auto-relaunch without button.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec vitest run src/__tests__/i18n.test.ts && test -f apps/desktop/src/components/update-dialog.tsx</automated>
  </verify>
  <done>Update dialog and i18n toast keys ready.</done>
</task>

<task type="auto">
  <name>Task 3: Native menu Prüfen + scheduler boot wire</name>
  <files>apps/desktop/src/lib/theme-menu.ts, apps/desktop/src/main.tsx</files>
  <read_first>
    - apps/desktop/src/lib/theme-menu.ts
    - apps/desktop/src/main.tsx
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-PATTERNS.md (updater menu)
  </read_first>
  <action>
    Extend theme-menu (or sibling installUpdateMenu) to append Auf Updates prüfen… using app i18n language (D-25/D-53). Windows parity via same Menu API. Soft-fail outside Tauri. From main.tsx startUpdateScheduler after paint (D-36) — do not block boot on network. Keep Authentik/session flows untouched (DESK-01).
  </action>
  <acceptance_criteria>
    - Menu install path includes update check action calling checkForUpdates(true).
    - startUpdateScheduler invoked from main without awaiting before render.
    - `pnpm --filter ./apps/desktop test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./apps/desktop test</automated>
    <human-check>Staging UAT: menu Prüfen → dialog → install → Neu starten; theme/OIDC still work.</human-check>
  </verify>
  <done>User can check/install updates via menu and background cadence.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Kind |
|--------|------|
| `checkForUpdates` | fn |
| `startUpdateScheduler` | fn |
| `UpdateDialog` | component |
| `update.*` i18n keys | locales |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dialog → downloadAndInstall | Signature verified by plugin before apply |
| Notification permission | OS prompt; in-app dialog remains |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-13 | Tampering | updater apply | critical | mitigate | Rely on signed artifacts from Plan 05; abort on download fail (task 1) |
| T-04.2-14 | Denial of Service | check spam | low | accept | 24h cadence; no exponential retry storms (D-23) |
| T-04.2-15 | Information Disclosure | toast/dialog | low | mitigate | i18n strings only; no endpoint secrets in UI (mask in debug later) |
</threat_model>

<verification>
updater + i18n tests green; full desktop vitest green; menu/scheduler wired.
</verification>

<success_criteria>
Stable-channel update UX complete per D-17…D-30 without Settings or forced restart.
</success_criteria>

<output>
Create `.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-06-SUMMARY.md` when done
</output>
