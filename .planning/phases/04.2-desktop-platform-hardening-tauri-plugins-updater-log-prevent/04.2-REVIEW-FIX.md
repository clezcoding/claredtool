---
phase: 04.2
fixed_at: 2026-08-28T20:20:00Z
review_path: .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-REVIEW.md
iteration: 3
findings_in_scope: 5
fixed: 5
skipped: 0
status: all_fixed
---

# Phase 04.2: Code Review Fix Report

**Fixed at:** 2026-08-28T20:20:00Z  
**Source review:** `.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-REVIEW.md`  
**Iteration:** 3

**Summary:**
- Findings in scope: 5
- Fixed: 5
- Skipped: 0

**Verification:** `pnpm exec vitest run src/lib/sentry.test.ts src/lib/updater.test.ts` — 20/20 passed in **main checkout** (after worktree fast-forward teardown). `cargo check` in `apps/desktop/src-tauri` passed in isolated worktree before merge.

## Fixed Issues

### WR-01: `scrubSentryEvent` leaves top-level message and exception values unscrubbed

**Files modified:** `apps/desktop/src/lib/sentry.ts`, `apps/desktop/src/lib/sentry.test.ts`  
**Commit:** `bbacad4`  
**Applied fix:** Extended `SentryEvent` type; scrub top-level `message` and `exception.values[].value` via `redactMessage`; added regression tests for both paths.

### WR-02: Ready-state update dialog close button is a no-op

**Files modified:** `apps/desktop/src/components/update-dialog.tsx`  
**Commit:** `36d7eda`  
**Applied fix:** `handleOpenChange` now calls `dismissUpdate()` when closing from `ready` state (same as `available`/`error`).

### WR-03: Rust Sentry plugin has no PII scrub hook

**Files modified:** `apps/desktop/src-tauri/src/lib.rs`  
**Commit:** `e1aaf50`  
**Applied fix:** Added `sentry_scrub_event` + `before_send` on Rust `sentry::init` — mirrors JS D-40 rules (Authorization drop, sensitive-key redact, email/bearer redaction in message/exception/extra/user/tags/breadcrumbs).

### IN-01: `dismissUpdate` from `ready` resets state and clears `pluginUpdate`

**Files modified:** `apps/desktop/src/lib/updater.ts`  
**Commit:** `bb117c2`  
**Applied fix:** Track `dismissedReadyVersion`; on ready dismiss skip re-promoting same version to `available` on subsequent `check()`.

### IN-02: No automated test for `ready`-state check guard

**Files modified:** `apps/desktop/src/lib/updater.test.ts`  
**Commit:** `f929a3f`  
**Applied fix:** Added tests for ready-state `checkForUpdates` short-circuit and dismissed-ready re-prompt prevention.

---

_Fixed: 2026-08-28T20:20:00Z_  
_Fixer: Claude (gsd-code-fixer)_  
_Iteration: 3_
