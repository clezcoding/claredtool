---
phase: 04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent
verified: 2026-08-28T05:07:00Z
status: human_needed
score: 20/21 must-haves verified
behavior_unverified: 3
overrides_applied: 1
overrides:
  - must_have: "Community dragout plugin installed and cfg(macos) smoke for PDF drag-out readiness"
    reason: "Human blocked tauri-plugin-dragout at SUS gate (2026-08-28); Phase 5 will use CrabNebula tauri-plugin-drag cross-platform with temp-file PDF path instead"
    accepted_by: "Clemens"
    accepted_at: "2026-08-28T03:54:59Z"
behavior_unverified_items:
  - truth: "OS notification permission is requested lazily on first update-ready, not on cold start (D-21)"
    test: "Publish a signed update to FaynoSync staging, launch desktop app, complete download to ready state"
    expected: "Permission sheet appears only when update reaches ready; cold start shows no permission prompt"
    why_human: "requestPermission only in update-dialog notify path; no automated test exercises OS permission sheet timing"
  - truth: "After download, relaunch happens only on explicit Neu starten click — no auto-restart (D-19)"
    test: "Complete update download in Tauri app; observe for 60s without clicking relaunch"
    expected: "App stays running; relaunch occurs only after button click"
    why_human: "Unit test proves relaunchApp is not called from downloadUpdate; full install/restart path needs signed artifact + FaynoSync"
  - truth: "Silent background update check failures log only and reach Sentry after ~3 failures (D-23)"
    test: "Block updates host or seed invalid endpoint; run app >24h or trigger scheduler; inspect logs/Sentry"
    expected: "No toast on silent failure; Sentry warning after third consecutive failure"
    why_human: "Threshold logic present in updater.ts; no test simulates multi-failure session against live plugin"
deferred:
  - truth: "Community dragout smoke / PDF drag-out readiness"
    addressed_in: "Phase 5 (PDF product flows)"
    evidence: "04.2-03-SUMMARY.md and WINDOWS.md #33 — CrabNebula tauri-plugin-drag replaces blocked tauri-plugin-dragout"
human_verification:
  - test: "FaynoSync admin seed — create app clared + channel stable on prod and staging"
    expected: "curl checkVersion returns 204 (no update) or 200 with JSON body — not HTTP 400 Channel does not exist"
    why_human: "Services deployed (health 200); verified prod_check:400 staging_check:400 until admin UI seed per 04.2-UPDATER-OPS.md"
  - test: "End-to-end signed update on staging after seed"
    expected: "Menu Auf Updates prüfen → dialog with notes → install → Neu starten relaunches app"
    why_human: "Requires FaynoSync artifact upload + TAURI_SIGNING_PRIVATE_KEY CI secret; no probe in repo"
  - test: "OIDC login regression after CSP hardening (UAT-OIDC)"
    expected: "Login WebView opens, Authentik flow completes, clared:// ticket returns to main window"
    why_human: "Rust allowlist preserved in lib.rs; full WebView auth flow not automatable in Vitest"
  - test: "Theme survives Tauri restart after store migration (D-15)"
    expected: "Change Darstellung theme → quit → relaunch → preference persists; boot flash unchanged"
    why_human: "desktop-store.test.ts covers migration logic; restart needs real Tauri + store file on disk"
  - test: "Sentry EU live capture with staging/prod DSN"
    expected: "Debug panel Sentry test event appears in de.sentry.io project with scrubbed payload"
    why_human: "DSN env-gated; empty DSN no-ops by design in dev"
decision_coverage:
  honored: 54
  total: 54
  not_honored: []
---

# Phase 04.2: Desktop Platform Hardening Verification Report

**Phase Goal:** Harden the Tauri desktop shell for ship-readiness and premium macOS feel — observability, auto-update, native polish, and agent-friendly dev tooling

**Verified:** 2026-08-28T05:07:00Z  
**Status:** human_needed  
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Wave 1: external link-guard (https→opener, dev http, clared in-app, block file/unknown) | ✓ VERIFIED | `link-guard.ts` + 6 unit tests; `installLinkGuard()` in `main.tsx` |
| 2 | Wave 1: overscroll off + selection defaults (D-32/D-33) | ✓ VERIFIED | `globals.css` `overscroll-behavior: none`; selective `user-select` |
| 3 | Wave 1: prevent-default registered; debug keeps DevTools/reload (D-31/D-50) | ✓ VERIFIED | `lib.rs` `prevent_default()` cfg debug vs release |
| 4 | Wave 1: store theme + localStorage dual-write migration (D-13–D-15) | ✓ VERIFIED | `desktop-store.ts` `migrateThemeToStore`; `desktop-store.test.ts` green |
| 5 | Wave 1: window-state persists geometry, not in store (D-16) | ✓ VERIFIED | `lib.rs` `tauri_plugin_window_state`; no window keys in store |
| 6 | Wave 1: notification plugin registered; no cold-start permission (D-21) | ✓ VERIFIED | `lib.rs` plugin init; `requestPermission` only in `update-dialog.tsx` / debug panel |
| 7 | Wave 1: community dragout smoke / PDF drag readiness | PASSED (override) | Override: human blocked dragout; Phase 5 CrabNebula drag — accepted Clemens 2026-08-28 |
| 8 | Wave 2: tauri-plugin-log LogDir + dev debug / prod info (D-42–D-44) | ✓ VERIFIED | `log_plugin()` in `lib.rs`; `log.test.ts` |
| 9 | Wave 2: tauri-plugin-sentry 0.6 EU, env-gated, scrubbed (D-37–D-40) | ✓ VERIFIED | `Cargo.toml` 0.6; `init_with_no_injection`; `sentry.test.ts` scrub cases |
| 10 | Wave 2: boot does not await Sentry/network (D-36/D-41) | ✓ VERIFIED | `main.tsx` rAF dynamic import `initDesktopSentry` |
| 11 | Wave 2: internal log/Sentry messages German (D-54) | ✓ VERIFIED | `updater.ts` / `sentry.ts` German strings |
| 12 | Wave 3: updater pubkey + createUpdaterArtifacts + stable endpoint (D-06/D-07) | ✓ VERIFIED | `tauri.conf.json` pubkey, endpoints, `createUpdaterArtifacts: true` |
| 13 | Wave 3: FaynoSync prod+staging HTTPS on Coolify (D-05/D-09/D-12) | ✓ VERIFIED | `04.2-UPDATER-OPS.md`; curl health 200 both hosts |
| 14 | Wave 3: updater/process plugins + CI signing env (D-06) | ✓ VERIFIED | `lib.rs` plugins; `desktop-build.yml` `TAURI_SIGNING_PRIVATE_KEY`; no private key in git |
| 15 | Wave 3: update cadence, menu, dialog, manual relaunch, i18n (D-17–D-25) | ✓ VERIFIED | `updater.ts`, `update-dialog.tsx`, `theme-menu.ts`; `updater.test.ts` relaunch test passes |
| 16 | Wave 4: Cmd/Ctrl+D rich debug panel DEV-only (D-46/D-47) | ✓ VERIFIED | `app-shell.tsx` dynamic import; `debug-panel-gate.test.ts` 6/6 |
| 17 | Wave 4: MCP bridge debug_assertions only unchanged (D-49) | ✓ VERIFIED | `lib.rs` `#[cfg(debug_assertions)]`; no `mcp-bridge:default` in capabilities |
| 18 | Wave 4: no Specta/TauRPC (D-48) | ✓ VERIFIED | `grep` clean in `Cargo.toml` / `package.json` |
| 19 | Wave 5: clipboard write-only + invoice/tax copy + i18n toast (D-01–D-04) | ✓ VERIFIED | `clipboard.ts`; `rechnung.tsx`/`tax.tsx`; `capabilities-desk.test.ts`; DE `Kopieren fehlgeschlagen` |
| 20 | Constraints: CSP non-null; no Settings update URL; no CrossCopy/GlitchTip | ✓ VERIFIED | `tauri.conf.json` CSP object; no settings route; dependency audit clean |
| 21 | DESK-01: OIDC login WebView allowlist + deep-link handlers preserved | ✓ VERIFIED | `lib.rs` `open_login_window`, `allow_navigation`, `clared://` handler unchanged |

**Score:** 20/21 truths verified (1 override; 3 present, behavior-unverified)

### Decision Coverage

All 54 trackable `04.2-CONTEXT.md` decisions honored by shipped artifacts (gsd-tools `check.decision-coverage-verify`).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/desktop/src/lib/desktop-store.ts` | Theme store + migration | ✓ VERIFIED | Substantive; wired from `theme.ts`, `main.tsx` |
| `apps/desktop/src/lib/link-guard.ts` | D-34 scheme policy | ✓ VERIFIED | Wired via `main.tsx` rAF |
| `apps/desktop/src/lib/updater.ts` | Check cadence + state machine | ✓ VERIFIED | Wired from `main.tsx`, menu, dialog |
| `apps/desktop/src/lib/sentry.ts` | Scrub + env gate | ✓ VERIFIED | Wired from `main.tsx` rAF |
| `apps/desktop/src/lib/log.ts` | Log helper | ✓ VERIFIED | Used by updater |
| `apps/desktop/src/lib/clipboard.ts` | writeText helper | ✓ VERIFIED | Routes + debug panel |
| `apps/desktop/src/components/update-dialog.tsx` | Install/later/relaunch UX | ✓ VERIFIED | Mounted in `app-shell.tsx` |
| `apps/desktop/src/components/debug-panel.tsx` | Rich DEV panel | ✓ VERIFIED | Dynamic import DEV-only |
| `apps/desktop/src-tauri/src/lib.rs` | Plugin registration order | ✓ VERIFIED | Sentry→log→notification→window-state→store→prevent-default→updater→process |
| `apps/desktop/src-tauri/tauri.conf.json` | Updater + CSP | ✓ VERIFIED | Pubkey, endpoints, `createUpdaterArtifacts` |
| `apps/desktop/src-tauri/capabilities/default.json` | ACL tighten | ✓ VERIFIED | write-only clipboard; no mcp-bridge |
| `.planning/phases/.../04.2-UPDATER-OPS.md` | FaynoSync SSOT | ✓ VERIFIED | Prod/staging UUIDs, URLs, verification cmds |
| `.github/workflows/desktop-build.yml` | Signed artifacts CI | ✓ VERIFIED | `TAURI_SIGNING_PRIVATE_KEY`; NSIS + `.app.tar.gz.sig` upload paths |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `main.tsx` | `desktop-store` | `migrateThemeToStore` + `getThemePref` before paint | WIRED | Async IIFE before React render |
| `main.tsx` | `link-guard` | `installLinkGuard` rAF | WIRED | Main WebView only |
| `main.tsx` | `sentry` / `updater` | rAF dynamic import | WIRED | Non-blocking boot |
| `theme-menu.ts` | `updater` | MenuItem → `checkForUpdates(true)` | WIRED | i18n `update.menuCheck` |
| `update-dialog.tsx` | `updater` | subscribe + `downloadUpdate` / `relaunchApp` | WIRED | User-triggered relaunch |
| `rechnung.tsx` / `tax.tsx` | `clipboard.ts` | `copyText` + `setFeedback(t('clipboard.failed'))` | WIRED | D-02 copy actions |
| `lib.rs` | Sentry plugin | `init_with_no_injection` before log | WIRED | DSN empty → skip |
| `tauri.conf.json` | FaynoSync | `plugins.updater.endpoints` prod URL | WIRED | Staging URL in CSP only; build env gate documented not wired |
| `capabilities/default.json` | clipboard plugin | `allow-write-text` only | WIRED | Test enforced |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `desktop-store.ts` | `ThemePref` | Tauri store file / localStorage fallback | ✓ | ✓ FLOWING |
| `updater.ts` | `UpdateInfo` | `@tauri-apps/plugin-updater` `check()` | ✓ (when server seeded) | ✓ FLOWING |
| `update-dialog.tsx` | `notes` | FaynoSync release body via plugin | ✓ | ✓ FLOWING |
| `debug-panel.tsx` | auth label | `useSession().me` | ✓ | ✓ FLOWING (no token) |
| `sentry.ts` | scrubbed event | `beforeSend` on browser SDK | ✓ | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full desktop unit suite | `pnpm --filter ./apps/desktop test` | 31 files, 160 tests pass | ✓ PASS |
| Relaunch only on explicit call (D-19) | `vitest run src/lib/updater.test.ts -t "does not relaunch automatically"` | 1 passed | ✓ PASS |
| Debug panel DEV gate | `vitest run src/__tests__/debug-panel-gate.test.ts` | 6 passed | ✓ PASS |
| Pubkey present, no PRIVATE | node check on `tauri.conf.json` | PUBKEY_PRESENT | ✓ PASS |
| FaynoSync health | `curl …/health` prod + staging | HTTP 200 both | ✓ PASS |
| FaynoSync checkVersion pre-seed | `curl …/checkVersion?...` | HTTP 400 (channel missing) | ? SKIP | Expected until admin seed — not a code defect |

### Probe Execution

Step 7c: SKIPPED — no `scripts/*/tests/probe-*.sh` declared for this phase.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| DESK-01 | 02, 03, 06 | Tauri shell; OIDC preserved | ✓ SATISFIED | `lib.rs` login paths; CSP allows auth origins |
| DESK-02 | 02, 03 | Desktop feel plugins + polish | ✓ SATISFIED | store, prevent-default, link-guard, window-state, notification; dragout override |
| DESK-03 | 04 | Log + Sentry EU scrubbed | ✓ SATISFIED | Plugins + tests |
| DESK-04 | 05, 06 | Signed updater + FaynoSync + UX | ⚠️ NEEDS HUMAN | Code + deploy complete; FaynoSync channel seed + e2e install pending |
| DESK-05 | 07 | Debug panel + MCP unchanged | ✓ SATISFIED | Gate tests + cfg |
| DESK-06 | 08 | Clipboard write-only + copy UX | ✓ SATISFIED | Helper + routes + ACL |

### Test Quality Audit

| Test File | Linked Req | Active | Skipped | Circular | Assertion Level | Verdict |
|-----------|-----------|--------|---------|----------|-----------------|---------|
| `updater.test.ts` | DESK-04 | 7 | 0 | No | Behavioral (relaunch) | OK |
| `sentry.test.ts` | DESK-03 | 4 | 0 | No | Value (scrub) | OK |
| `link-guard.test.ts` | DESK-02 | 6 | 0 | No | Value | OK |
| `desktop-store.test.ts` | DESK-02 | multiple | 0 | No | Value | OK |
| `clipboard.test.ts` | DESK-06 | 2 | 0 | No | Status | OK |
| `debug-panel-gate.test.ts` | DESK-05 | 6 | 0 | No | Behavioral (DEV gate) | OK |
| `capabilities-desk.test.ts` | DESK-04/06 | 4 | 0 | No | Value | OK |

**Disabled tests on requirements:** 0  
**Circular patterns detected:** 0  
**Insufficient assertions:** 0 (clipboard failure path asserts boolean only — acceptable for thin helper)

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | No TBD/FIXME/PLACEHOLDER in `apps/desktop` phase files | — | — |

### Human Verification Required

1. **FaynoSync admin seed** — Create app `clared` + channel `stable` on prod and staging FaynoSync admin. Expected: `checkVersion` no longer returns 400. Why: services live; channel missing is ops step documented in `04.2-UPDATER-OPS.md`.

2. **E2E signed update (staging)** — Upload signed `.app.tar.gz.sig` / NSIS to staging; run full menu → dialog → install → Neu starten flow. Why: requires CI secret + seeded server.

3. **OIDC login regression (UAT-OIDC)** — Full Authentik login after CSP change. Why: WebView flow not covered by unit tests.

4. **Theme restart smoke (D-15)** — Quit/relaunch after theme change in real Tauri. Why: store logic tested; disk persistence needs desktop runtime.

5. **Sentry live DSN** — Trigger debug panel test event with `VITE_SENTRY_DSN` set. Why: env-gated; code path verified statically.

6. **Behavior-unverified invariants** — See frontmatter `behavior_unverified_items` (notification lazy permission, silent-failure Sentry threshold, live no-auto-restart observation).

### Gaps Summary

No code blockers found. Implementation matches phase plans with two documented human decisions (dragout blocked, sentry-api skipped). Remaining work is operational and manual UAT:

- FaynoSync `checkVersion` returns **400** until admin creates `clared` + `stable` (health **200** confirms deploy).
- Staging updater endpoint selection is **documented** (`TAURI_UPDATER_ENDPOINT` comment in workflow) but **not wired** in `tauri.conf.json` build substitution — prod builds correctly use prod only (D-12 satisfied for prod).
- CI FaynoSync artifact **upload** is commented, not automated — allowed per D-10 (manual founder upload).

---

_Verified: 2026-08-28T05:07:00Z_  
_Verifier: Claude (gsd-verifier)_
