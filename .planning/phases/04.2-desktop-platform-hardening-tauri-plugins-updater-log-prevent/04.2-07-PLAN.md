---
phase: 04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent
plan: 07
type: execute
wave: 8
depends_on:
  - 04.2-04
  - 04.2-06
  - 04.2-08
files_modified:
  - apps/desktop/src/components/debug-panel.tsx
  - apps/desktop/src/components/app-shell.tsx
  - apps/desktop/src/main.tsx
  - apps/desktop/src-tauri/tauri.conf.json
  - apps/desktop/src/__tests__/debug-panel-gate.test.ts
autonomous: true
requirements:
  - DESK-05
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Cmd+D / Ctrl+D opens rich debug panel only in debug/dev builds; dead in release (D-46)."
    - "Panel shows env, version, build time, auth logged-in yes/no + /me label without token, theme/store keys, window-state info, updater check-now + last check + masked endpoint, log level + open log path, Sentry test event, clipboard test copy, notification test ping, link-guard/prevent-default flags, plugin health checklist (D-47)."
    - "MCP bridge remains #[cfg(debug_assertions)] only — unchanged policy (D-49)."
    - "WebView DevTools allowed in debug builds only; never production (D-50)."
    - "No Specta/TauRPC added (D-48)."
  artifacts:
    - apps/desktop/src/components/debug-panel.tsx
    - apps/desktop/src/__tests__/debug-panel-gate.test.ts
  key_links:
    - "AppShell keydown Cmd/Ctrl+D gated by import.meta.env.DEV (or equivalent)."
    - "Release build tree-shakes or excludes DebugPanel import."
    - "D-47 clipboard test copy calls copyText from Plan 08 clipboard.ts (plan 08 must complete first)."
  prohibitions:
    - statement: "Debug panel must not ship in production bundles (D-46)."
      status: forbidden
      verification: "debug-panel-gate.test asserts DEV gate; release build lacks panel chunk or import."
    - statement: "Panel must not display session tokens or keychain secrets (D-47)."
      status: forbidden
      verification: "Component reads me label/state only — no token fields rendered."
    - statement: "Do not add Specta or TauRPC (D-48)."
      status: forbidden
      verification: "Cargo.toml/package.json lack specta/taurpc."
    - statement: "Do not change MCP bridge release policy (D-49)."
      status: forbidden
      verification: "mcp-bridge still behind debug_assertions; not in release capabilities."
---

<objective>
Rich debug-only Cmd/Ctrl+D panel + DevTools policy (DESK-05, D-46…D-50).

Purpose: Agent-friendly DX without leaking into release.
Output: debug-panel.tsx, gate test, AppShell chord, tauri.devTools debug-only.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-CONTEXT.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-PATTERNS.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-04-SUMMARY.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-06-SUMMARY.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-08-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DebugPanel component + DEV gate test</name>
  <files>apps/desktop/src/components/debug-panel.tsx, apps/desktop/src/__tests__/debug-panel-gate.test.ts</files>
  <read_first>
    - apps/desktop/src/components/app-shell.tsx
    - apps/desktop/src/components/session-chip.tsx
    - apps/desktop/src/lib/clipboard.ts
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-CONTEXT.md (D-47 checklist)
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-PATTERNS.md (debug-panel)
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-08-SUMMARY.md
  </read_first>
  <behavior>
    - DebugPanel renders sections listed in D-47
    - No token string rendered from session
    - Module export is behind DEV conditional import pattern testable in vitest
    - Clipboard test control calls copyText (from Plan 08) — not a stub
  </behavior>
  <action>
    Precondition: Plan 08 shipped `copyText` + write-only clipboard plugin. Implement rich DebugPanel covering D-47 checklist using existing helpers (desktop-store, updater check-now, desktopLog open path via plugin API if available, initDesktopSentry test event, copyText clipboard test copy per D-47, notification test ping, link-guard/prevent-default flag booleans, plugin health checklist). Auth: logged-in boolean + /me display name/role only — never token (D-47). Updater endpoint shown masked. Open log folder control only here (D-45). No Redux dump / network proxy / SQL/PDF tools. Write debug-panel-gate.test.ts proving DEV-only mount path.
  </action>
  <acceptance_criteria>
    - debug-panel.tsx exports DebugPanel with D-47 sections present (labels/ids).
    - Clipboard test control imports and calls copyText from lib/clipboard.ts.
    - No `token` / `Bearer` / keychain secret render paths in component source.
    - debug-panel-gate.test.ts exits 0.
    - No specta/taurpc deps added.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec vitest run src/__tests__/debug-panel-gate.test.ts && grep -q 'copyText' apps/desktop/src/components/debug-panel.tsx && ! grep -iE 'specta|taurpc' apps/desktop/package.json apps/desktop/src-tauri/Cargo.toml</automated>
  </verify>
  <done>Rich debug panel exists and is test-gated for DEV-only; D-47 clipboard uses Plan 08 copyText.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Cmd/Ctrl+D + DevTools debug-only</name>
  <files>apps/desktop/src/components/app-shell.tsx, apps/desktop/src/main.tsx, apps/desktop/src-tauri/tauri.conf.json</files>
  <read_first>
    - apps/desktop/src/components/app-shell.tsx
    - apps/desktop/src-tauri/src/lib.rs (mcp debug_assertions)
    - apps/desktop/src-tauri/tauri.conf.json
  </read_first>
  <action>
    Wire Cmd+D / Ctrl+D in AppShell like Cmd+K pattern, only when import.meta.env.DEV (D-46). Lazy-import DebugPanel so production builds drop it. Configure Tauri/WebView devtools for debug builds only (D-50) — never production. Confirm mcp-bridge still debug_assertions and absent from release capabilities (D-49). Do not steal chord from inputs carelessly (ignore when target is input/textarea/contenteditable).
  </action>
  <acceptance_criteria>
    - AppShell registers meta/ctrl+D handler gated by DEV.
    - Production path does not statically import DebugPanel (dynamic/conditional).
    - tauri.conf or window config documents/enforces devtools debug-only.
    - mcp-bridge:default still absent from default.json; lib.rs mcp still cfg debug.
    - `pnpm --filter ./apps/desktop test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./apps/desktop test && grep -q 'debug_assertions' apps/desktop/src-tauri/src/lib.rs && ! grep -q 'mcp-bridge:default' apps/desktop/src-tauri/capabilities/default.json</automated>
  </verify>
  <done>Debug DX live in dev builds only; release stays clean (DESK-05).</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Kind |
|--------|------|
| `DebugPanel` | component |
| `debug-panel-gate` | test |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Debug panel → plugins | DEV-only IPC surface |
| Release binary | Must not include panel/MCP |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-16 | Information Disclosure | debug panel | high | mitigate | DEV gate; no tokens; masked updater URL (tasks 1–2) |
| T-04.2-17 | Information Disclosure | MCP in release | high | mitigate | Keep debug_assertions; no capability in default.json (task 2) |
| T-04.2-18 | Elevation of Privilege | DevTools in prod | high | mitigate | DevTools debug-only (D-50) |
</threat_model>

<verification>
debug-panel-gate + full vitest green; mcp absent from release capabilities; copyText wired in panel.
</verification>

<success_criteria>
Rich Cmd/Ctrl+D panel works in debug builds and cannot appear in release (DESK-05).
</success_criteria>

<output>
Create `.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-07-SUMMARY.md` when done
</output>
