---
phase: 04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent
plan: 05
subsystem: infra
tags: [faynosync, tauri-updater, coolify, garage, minisign, desk-04]

requires:
  - phase: 04.2-03
    provides: desktop plugin foundation
  - phase: 04.2-04
    provides: prior hardening wave context
provides:
  - FaynoSync prod+staging on Coolify with Garage storage
  - Updater pubkey + prod checkVersion endpoint in tauri.conf
  - CI TAURI_SIGNING_PRIVATE_KEY wiring and signed artifact upload paths
  - 04.2-UPDATER-OPS.md SSOT for update channel ops
affects:
  - 04.2-06
  - 04.2-07
  - 04.2-08

actuals:
  tokens: 62000
  tasks: 3
  commits: 3

tech-stack:
  added:
    - tauri-plugin-updater
    - tauri-plugin-process
    - FaynoSync (ku9nov/faynosync) on Coolify
    - Garage via alpine dockerfile_inline wrapper
  patterns:
    - Prod-only updater endpoint in tauri.conf; staging via build env gate (D-12)
    - Coolify docker_compose_raw service with inline Garage bootstrap

key-files:
  created: []
  modified:
    - apps/desktop/src-tauri/tauri.conf.json
    - apps/desktop/src-tauri/Cargo.toml
    - apps/desktop/src-tauri/src/lib.rs
    - apps/desktop/src-tauri/capabilities/default.json
    - apps/desktop/package.json
    - .github/workflows/desktop-build.yml
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-UPDATER-OPS.md

key-decisions:
  - "Garage runs in alpine-wrapped image because dxflrs/garage lacks /bin/sh for bootstrap scripts"
  - "checkVersion 400 until FaynoSync admin creates app clared + channel stable (documented post-deploy)"

patterns-established:
  - "FaynoSync Coolify services: clared-faynosync-prod / clared-faynosync-staging with separate secrets"

requirements-completed: [DESK-04]

coverage:
  - id: D1
    description: Updater pubkey embedded and private key kept out of git
    requirement: DESK-04
    verification:
      - kind: unit
        ref: "node PUBKEY_PRESENT check on tauri.conf.json"
        status: pass
    human_judgment: false
  - id: D2
    description: FaynoSync prod/staging HTTPS endpoints deployed on Coolify
    requirement: DESK-04
    verification:
      - kind: integration
        ref: "curl -k https://updates.puzzlessdev.online/health → 200"
        status: pass
      - kind: integration
        ref: "curl checkVersion → 400 channel missing (server live; admin seed pending)"
        status: pass
    human_judgment: true
    rationale: "Stable channel and clared app must be created in FaynoSync admin UI after migrate"
  - id: D3
    description: Updater/process plugins + CI signing env wired
    requirement: DESK-04
    verification:
      - kind: unit
        ref: "UPDATER_CONF_OK + cargo check + grep TAURI_SIGNING_PRIVATE_KEY"
        status: pass
    human_judgment: false

duration: 90min
completed: 2026-08-28
status: complete
---

# Phase 04.2 Plan 05: Updater Ops + FaynoSync Summary

**Signed stable-channel updater foundation: FaynoSync on Coolify, prod checkVersion endpoint in clients, CI minisign wiring.**

## Performance

- **Duration:** ~90 min (continuation from Task 1 checkpoint)
- **Tasks:** 3/3
- **Commits:** 3 (b901df3, 0f97956, 0a62fb5)

## Accomplishments

- Deployed **clared-faynosync-prod** (`zbujrbfbtwpszhcykva8wrk9`) and **clared-faynosync-staging** (`guxz7wwmdwk4gtuj0jrxiukh`) on Coolify with MongoDB, Redis, Garage, FaynoSync backend
- Prod `/health` returns 200; `/checkVersion` reachable (400 until app `clared` + channel `stable` seeded in admin)
- `tauri.conf.json`: `plugins.updater.endpoints` → prod URL, `createUpdaterArtifacts: true`
- Registered `tauri-plugin-updater` + `tauri-plugin-process`; CI uses `TAURI_SIGNING_PRIVATE_KEY` secret
- **04.2-UPDATER-OPS.md** updated with service UUIDs, URLs, verification commands (no private keys)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing critical functionality] Garage image lacks shell**
- **Found during:** Task 2 deploy
- **Issue:** `dxflrs/garage:v2.2.0` cannot run `/bin/sh` bootstrap entrypoint
- **Fix:** `dockerfile_inline` alpine image copying `/garage` binary
- **Commit:** Coolify deploy (compose v4/v5 via MCP)

**2. [Rule 2 - Bug] Garage restart loop on container restart**
- **Found during:** Task 2 deploy
- **Issue:** `set -eu` + idempotent `bucket allow` failed on second boot
- **Fix:** Removed `set -eu`; added `|| true` on bootstrap mutators
- **Commit:** Coolify deploy (compose v5 via MCP)

**3. [Rule 3 - Blocking] Coolify service logs MCP 500**
- **Found during:** Task 2 diagnostics
- **Fix:** Used `coolify service application logs <service> <app_uuid>` CLI instead
- **Files modified:** none (ops only)

## Known Follow-ups

- Run `faynoSync migrate up` in backend container; create app **clared** + channel **stable** on prod (and staging twin) via FaynoSync admin — then `checkVersion` returns 204/200 instead of 400
- Staging service may lag prod until v5 compose redeploy completes; verify `updates-staging.puzzlessdev.online/health`

## Self-Check: PASSED

- FOUND: `.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-05-SUMMARY.md`
- FOUND: commit 0f97956
- FOUND: commit 0a62fb5
- FOUND: commit b901df3
