---
phase: 04.1
slug: stitch-react-5-route-conversion
status: verified
threats_open: 0
asvs_level: 1
created: 2026-08-28
---

# Phase 04.1 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| npm registry → desktop package.json | i18next / Fontsource packages enter the bundle | JS modules, font binaries |
| Stitch HTML → React tree | Approved Stitch mockups converted to TSX components | Static UI structure (no raw HTML injection) |
| session `/me` → shell chrome | Persona card reads authenticated user | Name, email, org (existing Phase 2 session) |
| permission gates → CRUD affordances | `me.permissions` gates invoice/entity/customer actions | Permission string array |
| stub CTAs → in-app feedback | Upgrade/export/download stubs | Local Bald toast only; no navigation |
| mockups/approved → pixel QA | Golden PNG refs compared at verify time | Local PNG bytes |

---

## Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Status |
|-----------|----------|-----------|----------|-------------|------------|--------|
| T-04.1-SC | Tampering | i18next / react-i18next / Fontsource installs | high | mitigate | Plan 04.1-01 Task 1 blocking-human legitimacy gate before install; pinned versions in `apps/desktop/package.json` | closed |
| T-04.1-01 | Tampering | Stitch HTML port (all five routes) | high | mitigate | React elements only; zero `dangerouslySetInnerHTML` in `apps/desktop/src`; enforced by `rechnung-stitch.test.tsx`, `pdf-stitch.test.tsx`, `routes.test.tsx` | closed |
| T-04.1-02 | Information | Material ligature a11y | medium | mitigate | `MaterialIcon` sets `aria-hidden` vs `aria-label` (`material-icon.tsx`); covered by `material-icon.test.tsx` | closed |
| T-04.1-03 | Elevation | Upgrade/export/download stubs | medium | mitigate | `app-shell.tsx` + `export-panel.tsx` use local `showBald` toast; no `window.open` / external `href` in `apps/desktop/src` | closed |
| T-04.1-04 | Information | Persona email display | low | accept | Same session data already shown in SessionChip (Phase 2) | closed |
| T-04.1-05 | Tampering | Invoice field input | medium | mitigate | Controlled inputs + `canRead`/`canWrite` gates in `rechnung.tsx` | closed |
| T-04.1-06 | Spoofing | Empty-state CTA | low | accept | CTA focuses form only; no privilege escalation | closed |
| T-04.1-07 | Elevation | Create entity/customer | medium | mitigate | `canCreate` from `entity.create` / `kunde.write` wired through `registry-list-panel.tsx` | closed |
| T-04.1-08 | Tampering | Tax rule reorder UI | low | accept | Stubbed mutations; no privileged backend write invented | closed |
| T-04.1-09 | Information | Audit timeline copy | low | accept | Same tax decision data already in-app | closed |
| T-04.1-10 | Tampering | Golden PNG swap | medium | mitigate | Primary refs in `.planning/phases/04-premium-ui-brand-redesign/mockups/approved/`; capture host + `sw_vers` documented in `04.1-QA.md` | closed |

*Status: open · closed · open — below high threshold (non-blocking)*
*Severity: critical > high > medium > low — only open threats at or above `workflow.security_block_on` count toward `threats_open`*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

> **Note:** `T-04.1-SC` and `T-04.1-01` recur across plans 04.1-01–07. Recorded once. `T-04.1-03` severity escalated to medium in plan 04.1-06 (export stubs); disposition `mitigate` takes precedence over earlier `accept`.

SUMMARY threat flags: none across 04.1-01–07 SUMMARY files (last: 04.1-06 — export stubs in-app toast, React elements only).

L1 grep evidence (2026-08-28):
- T-04.1-SC: `i18next@26.4.0`, `react-i18next@17.0.12`, Fontsource packages in `package.json`; human legitimacy gate in plan 04.1-01 Task 1.
- T-04.1-01: `rg dangerouslySetInnerHTML apps/desktop/src` → no matches; tests assert absence.
- T-04.1-02: `material-icon.tsx:32-33` aria branching; `material-icon.test.tsx`.
- T-04.1-03: `rg 'window\.open|href=["\x27]https?://' apps/desktop/src` → no matches; `showBald` in `app-shell.tsx`, `export-panel.tsx`.
- T-04.1-05: `rechnung.tsx` permission gates on read/write/autosave paths.
- T-04.1-07: `entities.tsx` / `kunden.tsx` `canCreate` → `registry-list-panel.tsx` disabled affordance.
- T-04.1-10: `verify-pixel-qa.mjs` + `04.1-QA.md` host row (macOS 26.5.2 arm64).

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-04.1-01 | T-04.1-04 | Persona card shows existing session fields; no new data exposure beyond SessionChip | gsd-secure-phase | 2026-08-28 |
| AR-04.1-02 | T-04.1-06 | Empty-state CTA only focuses invoice form; no auth or permission change | gsd-secure-phase | 2026-08-28 |
| AR-04.1-03 | T-04.1-08 | Tax rule reorder is UI stub; no backend mutation surface in 4.1 | gsd-secure-phase | 2026-08-28 |
| AR-04.1-04 | T-04.1-09 | Audit timeline displays in-app tax decision copy only | gsd-secure-phase | 2026-08-28 |

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-08-28 | 11 | 11 | 0 | gsd-secure-phase (ASVS L1 grep; register at plan time, threats_open 0 — auditor skipped per L1 short-circuit) |

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-08-28
