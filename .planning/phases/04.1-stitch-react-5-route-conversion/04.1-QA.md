# Phase 04.1 capture SOP

Mac-pinned pixel gate (D-20–D-26, A1, A3). Use this before every route-wave golden capture.

## Host

- **OS:** macOS arm64 (developer machine). Record `sw_vers` in the per-route QA row.
- **Product window** may stay 1280×800 (tauri.conf). Capture viewport is independent (A3).

## Viewport

Force content size **1536×1024** before capture so goldens match `mockups/approved/`.

## Capture

Prefer Tauri MCP `webview_screenshot` with `format: "png"` (window `main`). Do not OS-screencapture the full native chrome unless cropping the titlebar first — titlebar breaks D-21.

## Theme

Capture **light and dark** where approved pairs exist (D-25). Drive theme via existing `applyTheme('light' | 'dark')`. Disable motion (`prefers-reduced-motion` or `--dur: 0`) so animations cannot flake bytes.

## Persona

Force **R-02** fallback for every golden (D-34): Alexander Wagner / aw@clared.de / Greenfield Studio GmbH. Never capture a live `/me` name in CI goldens.

## Golden paths

| Priority | Path | When |
|----------|------|------|
| Primary | `mockups/approved/` | Default compare (D-24) |
| Secondary | `.stitch/designs/` | Disputes only |

## Diff

`node apps/desktop/scripts/pixel-diff.mjs <expected.png> <actual.png> [diff.png]`

- Equal dimensions + `pixelmatch` **threshold 0** → exit 0 (A1 operational byte-equal).
- Dimension mismatch → non-zero (no pixel hash).
- Pixel mismatch → non-zero + writes a diff PNG.

Self-check: `node apps/desktop/scripts/pixel-diff.mjs --self-test equal,dimension-different,pixel-different`

## Layout constants (not spacing tokens)

These are named layout geometry. Do **not** declare them as spacing tokens or spacing-review rows (D-11/D-13, UI-SPEC).

| Name | px |
|------|----|
| `LAYOUT_SIDEBAR_WIDTH` | 260 |
| `LAYOUT_HEADER_HEIGHT` | 72 |
| `LAYOUT_TAX_RAIL_WIDTH` | 360 |
| `LAYOUT_CONTENT_MAX` | 1000 |

Hairline separators stay when layout edges touch (sidebar|main, tax-rail|canvas). No panel merge.

## Threat notes for later waves

- No `dangerouslySetInnerHTML` of Stitch HTML — React elements only (T-04.1-01).
- Material Symbols load from Fontsource, never Google Fonts CDN (D-44).
