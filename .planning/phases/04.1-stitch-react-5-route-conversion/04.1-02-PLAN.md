---
phase: 04.1-stitch-react-5-route-conversion
plan: 02
type: execute
wave: 2
depends_on:
  - 04.1-01
files_modified:
  - apps/desktop/src/components/app-shell.tsx
  - apps/desktop/src/App.tsx
  - apps/desktop/src/__tests__/app-shell.test.tsx
  - apps/desktop/src/i18n/locales/de.json
  - apps/desktop/src/i18n/locales/en.json
  - packages/ui/src/styles/globals.css
  - apps/desktop/src/styles/globals.css
  - .stitch/qa/STITCH-APPROVAL-BACKLOG.md
autonomous: false
requirements:
  - BRAND-01
  - D-11/D-13 scope lock
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Shared AppShell uses LAYOUT_SIDEBAR_WIDTH 260px measured from 02-rechnung-light; nav is exactly five items (D-18, D-11/D-13)."
    - "Nav active state uses NavLink isActive callback with sage --nav-active-bar (D-17, BRAND-01)."
    - "Shell persona uses /me when signed in else Alexander Wagner / aw@clared.de / Greenfield Studio GmbH (D-32–D-34)."
    - "⌘K chrome is visible 1:1; shortcut and click produce Bald feedback, never silent (D-51/D-52, F-01)."
    - "Logo mark + upgrade card chrome exist on shell; upgrade CTA may stub (D-53, F-01)."
    - "Stitch shell hex values that diverge from Crafted Minimal are synced into globals.css CSS vars then consumed as semantic classes (D-05/D-06)."
    - "R-01 is documented superseded by 260px Stitch nav SSOT in STITCH-APPROVAL-BACKLOG (D-19)."
  artifacts:
    - apps/desktop/src/components/app-shell.tsx
    - apps/desktop/src/__tests__/app-shell.test.tsx
    - .stitch/qa/STITCH-APPROVAL-BACKLOG.md
  key_links:
    - "App.tsx createHashRouter mounts AppShell from components/app-shell.tsx with unchanged five child routes."
    - "MaterialIcon ligatures replace Lucide on touched shell nav icons (D-44/D-47)."
  prohibitions:
    - "Do not add sixth nav item or out-of-nav routes (login, dashboard, settings, full palette)."
    - "Do not fold 260/72 into the spacing-token scale — use named layout constants / arbitrary Tailwind widths."
    - "Do not leave #[hex] in className strings at wave-end validate (sync CSS vars first)."
  assumptions:
    - "[ASSUMED A2] Org line Greenfield Studio GmbH is shell-canon hardcode when MeResponse lacks org."
    - "[UNRESOLVED EDGE D-11-D-13 adjacency] Sidebar|main touch uses Stitch hairline border — panels stay separate."
---

<objective>
Tracer slice: extract shared AppShell from Stitch 02 nav chrome with 260px sidebar, five-item NavLink nav, Material icons, R-02 persona, ⌘K Bald, logo + upgrade card (F-01), then sync Stitch-winning tokens into globals.css and close R-01 as superseded.

Purpose: Prove stitch-build → Tauri shell path end-to-end before densest Rechnung conversion (D-01).
Output: app-shell.tsx, thin App.tsx router host, app-shell tests, backlog R-01 note, token sync for shell surfaces.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-stitch-react-5-route-conversion/04.1-CONTEXT.md
@.planning/phases/04.1-stitch-react-5-route-conversion/04.1-RESEARCH.md
@.planning/phases/04.1-stitch-react-5-route-conversion/04.1-PATTERNS.md
@.planning/phases/04.1-stitch-react-5-route-conversion/04.1-UI-SPEC.md
@.stitch/designs/02-rechnung-light.html
@.stitch/qa/STITCH-APPROVAL-BACKLOG.md
@apps/desktop/src/App.tsx
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Confirm D-06 Stitch-wins token sync</name>
  <decision>When Stitch values diverge from Crafted Minimal CSS vars, which wins?</decision>
  <context>
    Costly: token SSOT for converted surfaces shifts toward Stitch; later routes inherit synced vars. validate.js rejects hex in className — sync is required for D-15 gates. Single component tree + dark: variants remain (D-07/D-08).
  </context>
  <options>
    <option id="stitch-wins">
      <name>Stitch wins; sync CSS vars afterward (recommended)</name>
      <pros>Matches D-05/D-06; pixel fidelity; validate can pass after sync</pros>
      <cons>Crafted Minimal vars shift toward Stitch for converted surfaces</cons>
    </option>
    <option id="remap-first">
      <name>Remap Stitch classes to existing Phase 4 CSS vars first</name>
      <pros>Keeps Phase 4 token names stable</pros>
      <cons>Contradicts D-05/D-06; visual drift from Stitch SSOT</cons>
    </option>
  </options>
  <resume-signal>Select: stitch-wins (recommended) or remap-first</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End-to-end AppShell tracer — 260px five-nav Stitch chrome</name>
  <files>apps/desktop/src/components/app-shell.tsx, apps/desktop/src/App.tsx, apps/desktop/src/__tests__/app-shell.test.tsx, apps/desktop/src/i18n/locales/de.json, apps/desktop/src/i18n/locales/en.json</files>
  <read_first>
    - apps/desktop/src/App.tsx (AppShell, NAV_ITEMS, createHashRouter — extract pattern)
    - .stitch/designs/02-rechnung-light.html (nav w-[260px], h-[72px], chrome)
    - .planning/phases/04.1-stitch-react-5-route-conversion/04.1-PATTERNS.md (app-shell assignment)
    - .planning/phases/04.1-stitch-react-5-route-conversion/04.1-CONTEXT.md (D-01, D-17–D-19, D-32–D-34, D-51–D-53)
    - .planning/phases/06-mockup-1-1-fidelity-closure-all-5-routes-pixel-match-approve/06-MOCKUP-GAPS.md (F-01)
    - apps/desktop/src/components/material-icon.tsx
    - apps/desktop/src/components/create-disabled-button.tsx (Bald/disabled pattern)
    - apps/desktop/src/auth/session-provider.tsx (me for persona)
    - stitch-build react-components SKILL.md (Props interfaces, NavLink, validate)
  </read_first>
  <behavior>
    - Signed-in shell renders navigation with exactly five links: Rechnung, Entities, Kunden, Tax, PDF.
    - Active route applies sage active styles via NavLink isActive.
    - Sidebar element uses width class w-[260px] (LAYOUT_SIDEBAR_WIDTH).
    - ⌘K control is present; invoking Bald path shows Bald copy (toast or tooltip text).
    - When me is null in test fixture, persona shows Alexander Wagner and aw@clared.de.
  </behavior>
  <action>
    Follow stitch-build react-components for shell chrome from local .stitch/designs/02-rechnung-light.html (D-03 reuse; no MCP refresh unless broken). Extract AppShell into apps/desktop/src/components/app-shell.tsx with exported AppShellProps interface. Set left nav to w-[260px] (LAYOUT_SIDEBAR_WIDTH); top bar h-[72px] (LAYOUT_HEADER_HEIGHT) — never map these into spacing xs–3xl. Keep NAV_ITEMS length 5 only (D-11/D-13). Wire NavLink + isActive className callback (D-17). Replace touched Lucide nav icons with MaterialIcon ligatures from Stitch (D-44); if a ligature is missing use nearest Material Symbol and note in QA (D-45). Persona card: useSession me when present; else R-02 canon Alexander Wagner / aw@clared.de / Greenfield Studio GmbH (D-32/D-33, A2). Single component tree + dark: variants for shell chrome (D-07/D-08). Render ⌘K hint chrome 1:1; disable open; on click and on ⌘/Ctrl+K intercept show Bald feedback (D-51/D-52). Build logo mark + upgrade card; upgrade CTA stub with local toast only — no external navigation (D-53, T-04.1-03). Convert chrome strings via t(). Thin App.tsx to import AppShell and keep createHashRouter children unchanged. RED→GREEN app-shell.test.tsx extending routes.test signed-in render pattern. Map every Stitch px font/spacing on shell to nearest typography/spacing role in SUMMARY consistency note (planner hint) without collapsing values. Real React elements only — no Stitch HTML string injection.
  </action>
  <verify>
    <automated>pnpm --filter desktop exec vitest run src/__tests__/app-shell.test.tsx && pnpm --filter desktop exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - apps/desktop/src/components/app-shell.tsx exports AppShell and AppShellProps.
    - Sidebar uses w-[260px]; App.tsx no longer defines inline AppShell body (import only).
    - NAV_ITEMS or equivalent has exactly 5 entries.
    - app-shell.test.tsx exits 0 asserting five nav labels, w-[260px], and Bald feedback path.
    - grep for dangerouslySetInnerHTML in app-shell.tsx returns no matches.
  </acceptance_criteria>
  <done>AppShell tracer works end-to-end: 260px five-nav Stitch chrome, persona, ⌘K Bald, Material icons, tests green.</done>
  <reversibility rating="costly">D-09/D-27 scope already locked in Plan 01; shell chrome is the F-01 carrier for in-phase pixel.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Sync shell Stitch tokens + close R-01 superseded + validate</name>
  <files>packages/ui/src/styles/globals.css, apps/desktop/src/styles/globals.css, .stitch/qa/STITCH-APPROVAL-BACKLOG.md, apps/desktop/src/components/app-shell.tsx</files>
  <read_first>
    - packages/ui/src/styles/globals.css
    - apps/desktop/src/styles/globals.css
    - .stitch/qa/STITCH-APPROVAL-BACKLOG.md (R-01 section)
    - .planning/phases/04.1-stitch-react-5-route-conversion/04.1-RESEARCH.md (Pitfall 2 validate hex)
    - apps/desktop/src/components/app-shell.tsx
  </read_first>
  <action>
    Per D-06 (Task 1 A): promote any remaining Stitch hex used by shell into :root/.dark CSS variables in both globals.css copies, then replace className hex with semantic tokens so stitch-build validate passes. Document R-01 closed as superseded: measured nav width 260px from 02-rechnung-light; backlog 360px was tax rail LAYOUT_TAX_RAIL_WIDTH (D-19). Run package stitch:validate on app-shell.tsx and tsc. Do not convert Rechnung body in this plan.
  </action>
  <verify>
    <automated>pnpm --filter desktop exec tsc --noEmit && pnpm --filter desktop run stitch:validate -- apps/desktop/src/components/app-shell.tsx && grep -q 'R-01' .stitch/qa/STITCH-APPROVAL-BACKLOG.md</automated>
  </verify>
  <acceptance_criteria>
    - STITCH-APPROVAL-BACKLOG.md states R-01 superseded / 260px nav SSOT.
    - stitch:validate on app-shell.tsx exits 0 (no className hex; Props interface present).
    - Both globals.css copies remain in sync for any new shell token vars.
    - tsc --noEmit exits 0.
  </acceptance_criteria>
  <done>Shell tokens synced; R-01 superseded documented; D-15 validate+tsc green for AppShell.</done>
  <reversibility rating="costly">D-06 token SSOT shift toward Stitch for converted shell surfaces.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session /me → persona card | Untrusted display strings from API |
| Keyboard shortcut → shell | Global ⌘K intercept |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-01 | Tampering | app-shell.tsx | high | mitigate | React elements only; acceptance forbids dangerouslySetInnerHTML |
| T-04.1-03 | Elevation | upgrade CTA stub | low | mitigate | Local toast only; no window.open / external href |
| T-04.1-04 | Information | persona email display | low | accept | Existing session data already shown in SessionChip |
| T-04.1-SC | Tampering | npm installs | low | accept | No new packages this plan |

No new high threats beyond T-04.1-01 (mitigated).
</threat_model>

<verification>
- app-shell tests + tsc + stitch:validate green.
- Five-item nav only; 260px sidebar; R-01 superseded note.
</verification>

<success_criteria>
Shared AppShell is the single Stitch-faithful shell for all five routes; F-01 chrome present; wave gate D-15 passes.
</success_criteria>

## Artifacts this phase produces (Plan 04.1-02)

- **New:** `apps/desktop/src/components/app-shell.tsx`, `apps/desktop/src/__tests__/app-shell.test.tsx`
- **Modified:** `App.tsx` (thin router host), dual `globals.css`, `STITCH-APPROVAL-BACKLOG.md` R-01
- **Layout constants in use:** LAYOUT_SIDEBAR_WIDTH=260, LAYOUT_HEADER_HEIGHT=72

<output>
Create `.planning/phases/04.1-stitch-react-5-route-conversion/04.1-02-SUMMARY.md` when done
</output>
