---
phase: 04.1
fixed_at: 2026-08-28T02:30:00Z
review_path: .planning/phases/04.1-stitch-react-5-route-conversion/04.1-REVIEW.md
iteration: 1
findings_in_scope: 11
fixed: 11
skipped: 0
status: all_fixed
---

# Phase 04.1: Code Review Fix Report

**Fixed at:** 2026-08-28T02:30:00Z  
**Source review:** `.planning/phases/04.1-stitch-react-5-route-conversion/04.1-REVIEW.md`  
**Iteration:** 1

**Summary:**
- Findings in scope: 11
- Fixed: 11
- Skipped: 0

**Verification:** `pnpm --filter desktop test` — 121 passed (main checkout after fast-forward merge).

## Fixed Issues

### CR-01: `stitch:validate` hardcodes a developer-local absolute path

**Files modified:** `apps/desktop/scripts/stitch-validate.mjs`  
**Commit:** `dba13d8`  
**Applied fix:** Resolve `validate.js` via `STITCH_VALIDATE` env or discovery under `~/.cursor` Stitch plugin caches; exit with actionable message when missing.

### CR-02: Detail panel close is immediately undone by auto-select `useEffect`

**Files modified:** `apps/desktop/src/routes/entities.tsx`, `apps/desktop/src/routes/kunden.tsx`  
**Commit:** `4bbe8a4`  
**Applied fix:** `userClosedRef` guards auto-select after explicit close; reset ref when user selects a row again.

### CR-03: Invoice date helpers use UTC `toISOString()` for local calendar fields

**Files modified:** `apps/desktop/src/routes/rechnung.tsx`  
**Commit:** `578d55c`  
**Applied fix:** Added `formatIsoLocal()` and replaced `toISOString().slice(0, 10)` in date helpers.

### WR-01: Delete-invoice confirm does not delete

**Files modified:** `apps/desktop/src/routes/rechnung.tsx`  
**Commit:** `578d55c`  
**Applied fix:** `handleDeleteInvoice()` DELETEs via API, updates drafts, navigates to next draft or empty hero.

### WR-02: Duplicated tax-percent logic in Rechnung vs shared `money.ts`

**Files modified:** `apps/desktop/src/routes/rechnung.tsx`, `apps/desktop/src/lib/money.ts`, `apps/desktop/src/components/tax-rail.tsx`  
**Commit:** `578d55c`, `a294b2a`  
**Applied fix:** Added `taxPercent` / `taxFraction` to `lib/money`; Rechnung and TaxRail import shared helpers.

### WR-03: Permission-denied and PDF error retries are no-ops

**Files modified:** `apps/desktop/src/routes/rechnung.tsx`, `apps/desktop/src/routes/pdf.tsx`  
**Commit:** `578d55c` (rechnung), `99deab6` (pdf)  
**Applied fix:** `ErrorState` retry optional; omit for permission-denied; PDF error retry resets demo state to `populated`.

### WR-04: Kunden create form can POST without `entityId`

**Files modified:** `apps/desktop/src/routes/kunden.tsx`  
**Commit:** `4bbe8a4`  
**Applied fix:** Guard `handleCreate` when `entityId` empty; disable submit until entity chosen.

### WR-05: Signed-in shell footer always shows R-02 company string

**Files modified:** `apps/desktop/src/components/app-shell.tsx`  
**Commit:** `be82276`  
**Applied fix:** Hide demo company line when `me` is present.

### WR-06: Tax rule modal save discards all edits

**Files modified:** `apps/desktop/src/routes/tax.tsx`  
**Commit:** `4f9554b`  
**Applied fix:** `saveRule()` merges draft into `rules` on create/edit before closing modal.

### IN-01: `ErrorState` body copy bypasses i18n

**Files modified:** `apps/desktop/src/components/error-state.tsx`, `apps/desktop/src/i18n/locales/de.json`, `apps/desktop/src/i18n/locales/en.json`  
**Commit:** `5f97bdb`  
**Applied fix:** Body string moved to `error.retry.body` in locale files.

### IN-02: Dead `status` variable in pixel-diff manifest loop

**Files modified:** `apps/desktop/scripts/pixel-diff.mjs`  
**Commit:** `adce7c2`  
**Applied fix:** Use computed `status` for `rows[].result`; removed `void status`.

---

_Fixed: 2026-08-28T02:30:00Z_  
_Fixer: Claude (gsd-code-fixer)_  
_Iteration: 1_
