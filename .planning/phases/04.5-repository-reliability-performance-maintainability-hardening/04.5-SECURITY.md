---
phase: "04.5"
slug: "repository-reliability-performance-maintainability-hardening"
status: verified
threats_open: 0
asvs_level: 1
created: "2026-09-05"
---

# Phase 04.5 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| Planning docs → operator | Addendum must not invent GitHub Settings state; manual tasks labeled human-only | Planning metadata, run IDs |
| gh CLI → run metadata | Baseline run IDs must match real Actions runs | CI telemetry |
| Rule JSON → Ajv compile | Schema change must accept all existing rule files | Tax rule DSL JSON |
| store.ts → production validation | Config must stay strict; no option bypass | Validated rule payloads |
| Vite dist → Tauri bundle | Asset paths must remain valid for Tauri packaging | Frontend bundles |
| GHA secrets → deploy script | COOLIFY_TOKEN must not log to stdout or GITHUB_STEP_SUMMARY | Deploy credentials |
| Coolify API → production | Wrong order or missing rollback deploys bad worker | Container image tags |
| CI YAML → runner | Workflow changes must not drop security scanners | Workflow definitions |
| Parallel builds → cosign | Both digests required before sign step | Image digests |
| Export deletion → importers | Zero-caller proof must include dynamic import strings | Source modules |
| Graph false negative → production break | rg backstop required before delete | Dead-code candidates |

---

## Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Status |
|-----------|----------|-----------|----------|-------------|------------|--------|
| T-04.5-01 | Tampering | Immutable 04.4 PLAN/SUMMARY | medium | mitigate | Addendum-only edits per D-16; `04.4-POST-MERGE-ADDENDUM.md` created without mutating `04.4-*-SUMMARY.md` bodies | closed |
| T-04.5-02 | Information disclosure | POST-MERGE addendum | low | accept | No secrets in docs; Coolify tokens stay in GHA secrets | closed |
| T-04.5-03 | Repudiation | Baseline table | low | mitigate | GitHub run IDs pinned in `04.5-VALIDATION.md` with `gh run view` re-verify note | closed |
| T-04.5-04 | Tampering | store.ts Ajv options | high | mitigate | `packages/tax-engine/src/store.ts:7` — `new Ajv2020({ allErrors: true })` only; no loosening | closed |
| T-04.5-05 | Tampering | Schema SSOT drift | medium | mitigate | `cmp` gate passes between `docs/` and `packages/tax-engine/src/schema/` copies | closed |
| T-04.5-06 | Elevation | allowUnionTypes shortcut | high | mitigate | No `allowUnionTypes` in codebase; anyOf refactor per D-09 documented in VALIDATION rejected ideas | closed |
| T-04.5-07 | Tampering | Tax semantics via schema | critical | mitigate | `scripts/assert-ajv-zero-warnings.sh` exit 0; wired in `ci.yml` backend-unit (lines 215–216); evaluate.spec.ts matrix green | closed |
| T-04.5-08 | Tampering | vite.config manualChunks | medium | mitigate | `vite.config.ts` untouched; main gzip 240.27 kB < 500 kB gate (VALIDATION Desktop bundle) | closed |
| T-04.5-09 | Denial of service | Bundle +5% regression | low | accept | Split not applied; D-07 N/A when rejected per VALIDATION | closed |
| T-04.5-10 | Information disclosure | coolify-deploy-nest.sh | critical | mitigate | No `set -x`; `set -euo pipefail` only; `assert_no_token_leak` in `.github/scripts/coolify-deploy-nest.test.sh` | closed |
| T-04.5-11 | Tampering | Deploy order | critical | mitigate | `trap rollback_changed ERR` + `trap rollback_on_exit EXIT`; mock tests assert worker-before-API rollback order | closed |
| T-04.5-12 | Denial of service | Health poll skip | high | mitigate | `/health/build` SHA poll before worker pin; `health_fail` test asserts rollback on poll failure | closed |
| T-04.5-13 | Tampering | backend-image.yml | high | mitigate | Workflow calls versioned script; SHA-pinned actions; actionlint clean per VERIFICATION | closed |
| T-04.5-14 | Elevation | Trivy/CodeQL removal | critical | mitigate | 2× `trivy-action` in `backend-image.yml`; CodeQL `build-mode: none` retained in `codeql.yml` | closed |
| T-04.5-15 | Tampering | backend-image parallel | high | mitigate | Parallel `push-api`/`push-worker`; `DIGEST_API`/`DIGEST_WORKER` env vars required before cosign sign | closed |
| T-04.5-16 | Repudiation | Walltime claims | medium | mitigate | Before/after run IDs in `04.5-VALIDATION.md` CI experiments table | closed |
| T-04.5-17 | Denial of service | Dead code deletion | high | mitigate | Graph-first + rg backstop; `create-disabled-button.tsx` deleted with zero-caller proof + boundary test | closed |
| T-04.5-18 | Tampering | Graph-only grep skip | medium | mitigate | D-14 graph-first then rg literal backstop documented in VALIDATION Repo hygiene | closed |
| T-04.5-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages across all six plans; AJV already pinned | closed |

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-04.5-01 | T-04.5-02 | POST-MERGE addendum contains only public workflow facts; no credential material | gsd-secure-phase L1 audit | 2026-09-05 |
| AR-04.5-02 | T-04.5-09 | Bundle split rejected — gzip 240 kB well under 500 kB gate; +5% cap irrelevant | gsd-secure-phase L1 audit | 2026-09-05 |
| AR-04.5-03 | T-04.5-SC | Phase scope explicitly forbids new package installs; supply-chain risk unchanged from baseline | gsd-secure-phase L1 audit | 2026-09-05 |

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-09-05 | 19 | 19 | 0 | gsd-secure-phase (orchestrator L1 grep; ASVS L1; auditor skipped per short-circuit) |

### Security Audit 2026-09-05

| Metric | Count |
|--------|-------|
| Threats found | 19 |
| Closed | 19 |
| Open | 0 |

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-09-05
