---
phase: 04.5-repository-reliability-performance-maintainability-hardening
fixed_at: 2026-09-05T01:02:00Z
review_path: .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-REVIEW.md
iteration: 2
findings_in_scope: 2
fixed: 2
skipped: 0
status: all_fixed
---

# Phase 04.5: Code Review Fix Report

**Fixed at:** 2026-09-05T01:02:00Z  
**Source review:** `.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-REVIEW.md`  
**Iteration:** 2

**Summary:**
- Findings in scope: 2
- Fixed: 2
- Skipped: 0

## Fixed Issues

### CR-01: Workflow cancel-in-progress aborts in-flight Coolify deploy

**Files modified:** `.github/workflows/backend-image.yml`  
**Commit:** `2473a24`  
**Status:** fixed: requires human verification  
**Applied fix:** Removed workflow-level `concurrency` (`cancel-in-progress: true`). Added distinct job-level groups:
- `push-api`: `backend-image-${{ github.ref }}-push-api` with `cancel-in-progress: true`
- `push-worker`: `backend-image-${{ github.ref }}-push-worker` with `cancel-in-progress: true`
- `deploy`: unchanged `coolify-nest-deploy` with `cancel-in-progress: false`

Stale builds can still cancel independently without cancelling an in-flight Coolify deploy. Parallel push siblings no longer share a cancel group (REL-04 safe). Residual risk: a slower older build could still reach deploy after a newer one finishes (no SHA freshness guard yet).

### WR-01: Deploy rollback trap is ERR-only — cancel/timeout skips rollback

**Files modified:** `.github/scripts/coolify-deploy-nest.sh`  
**Commit:** `6a3c550`  
**Status:** fixed: requires human verification  
**Applied fix:** Added `deploy_succeeded` / `rollback_started` guards, `rollback_on_exit` on `EXIT`, kept `ERR` trap, and explicit `INT`/`TERM` handlers that exit so `EXIT` runs. Cleared all traps only after successful deploy. Idempotent `rollback_changed` prevents double-pin when ERR + EXIT both fire.

## Verification

- **Where:** isolated worktree `.claude/worktrees/rf-04.5-97940-*` (not main checkout `node_modules`)
- `bash -n .github/scripts/coolify-deploy-nest.sh` — OK
- `bash .github/scripts/coolify-deploy-nest.test.sh` — OK (health rollback, worker rollback, happy path, zizmor)

## Skipped Issues

None.

---

_Fixed: 2026-09-05T01:02:00Z_  
_Fixer: Claude (gsd-code-fixer)_  
_Iteration: 2_
