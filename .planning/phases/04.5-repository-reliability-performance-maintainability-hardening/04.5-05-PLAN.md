---
phase: 04.5-repository-reliability-performance-maintainability-hardening
plan: 05
type: execute
wave: 4
depends_on:
  - 04.5-01
  - 04.5-04
  - 04.5-06
files_modified:
  - .github/workflows/backend-image.yml
  - .github/workflows/codeql.yml
  - .github/workflows/ci.yml
  - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md
autonomous: true
requirements:
  - REL-04
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "backend-image build job experiments recorded with before/after walltime (D-13)"
    - "desktop-rust and analyze-rust baselines investigated with documented outcome per equal priority (D-13)"
    - "No Trivy, CodeQL, clippy, or coverage steps removed (D-13 security tradeoff rule)"
    - "Each experiment has rollback: revert YAML commit"
  artifacts:
    - .github/workflows/backend-image.yml
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md
  key_links:
    - "VALIDATION CI experiments table links baseline run IDs to post-change run IDs"
    - "Parallel docker push steps keep cosign, Trivy, SBOM unchanged"
    - "codeql.yml build-mode none and path filters unchanged if no safe win"
---

<objective>
Measured CI walltime experiments on all three baselines with before/after evidence (REL-04, D-13).

Purpose: Pursue equal-priority perf wins without weakening security gates.

Output: VALIDATION experiment log; optional backend-image parallel build if ≥30s saving on PR merge.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-RESEARCH.md
@.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-PATTERNS.md
@.github/workflows/backend-image.yml
@.github/workflows/ci.yml
@.github/workflows/codeql.yml
@.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end backend-image parallel build experiment</name>
  <files>.github/workflows/backend-image.yml, .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md</files>
  <read_first>
    - .github/workflows/backend-image.yml (sequential push-api/push-worker lines 101-131 per PATTERNS)
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-RESEARCH.md (A1 parallel saves ≥30s)
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md (baseline ~5m02s run 33922528337)
  </read_first>
  <action>
    Refactor build job so push-api and push-worker docker/build-push-action steps run in parallel via a job matrix or parallel steps pattern that preserves both digests for cosign sign step (D-13). Keep cache-from/cache-to scope clared-backend, platforms linux/amd64, Trivy scans, cosign, SBOM — do not remove scanners. Run actionlint locally. After merge to planning branch, record new gh run view build+deploy walltime in VALIDATION **CI experiments** table with before 33922528337. If parallel layout cannot preserve DIGEST_API/DIGEST_WORKER wiring, revert and document blocked with evidence. Rollback path: restore sequential steps from PATTERNS.md.
  </action>
  <acceptance_criteria>
    - backend-image.yml still contains both push-api and push-worker build-push-action SHA pins
    - grep -c 'aquasecurity/trivy-action@' .github/workflows/backend-image.yml is at least 2
    - VALIDATION CI experiments section exists with backend-image row containing before run ID 33922528337 and either after run ID or explicit no-change verdict with rationale (D-13)
  </acceptance_criteria>
  <verify>
    <automated>grep -F 'docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a' .github/workflows/backend-image.yml && test "$(grep -c 'aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25' .github/workflows/backend-image.yml || true)" -ge 2 && grep -F 'CI experiments' .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md && grep -F '33922528337' .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md && grep -Ei 'after run|no-change' .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md && docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667 && uvx zizmor@1.30.0 .github/workflows/backend-image.yml</automated>
    <fails_when>Trivy removed, build-push-action missing, VALIDATION backend-image row lacks before run ID or after/no-change verdict, actionlint fails, or zizmor exits non-zero</fails_when>
  </verify>
  <done>backend-image baseline experiment implemented or documented blocked with rollback (REL-04, D-13).</done>
</task>

<task type="auto">
  <name>desktop-rust baseline investigation</name>
  <files>.github/workflows/ci.yml, .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md</files>
  <read_first>
    - .github/workflows/ci.yml desktop-rust job (rust-cache, fmt, clippy, test)
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-RESEARCH.md (7m29s baseline run 33921528007)
  </read_first>
  <action>
    Per D-13 equal priority: audit desktop-rust for one safe walltime experiment — e.g. run cargo test and cargo clippy in parallel jobs only if actionlint-valid and needs graph keeps ci aggregator green; OR document no safe change after review (swatinem/rust-cache already present; fmt must stay before clippy). Record decision in VALIDATION desktop-rust row: implemented change with expected delta OR no-change with rationale. Do not remove clippy -D warnings or cargo test. Do not add Actions cache to signed desktop-build.yml (phase boundary).
  </action>
  <acceptance_criteria>
    - VALIDATION desktop-rust experiment row filled with outcome
    - ci job id remains ci; desktop-rust job still runs fmt, clippy, test
  </acceptance_criteria>
  <verify>
    <automated>grep -E '^  desktop-rust:' .github/workflows/ci.yml && grep -F 'cargo clippy' .github/workflows/ci.yml && grep -F 'desktop-rust' .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md && uvx zizmor@1.30.0 .github/workflows/ci.yml</automated>
    <fails_when>desktop-rust job removed, clippy dropped, VALIDATION row missing, or zizmor exits non-zero</fails_when>
  </verify>
  <done>desktop-rust baseline addressed with evidence (D-13).</done>
</task>

<task type="auto">
  <name>CodeQL Rust baseline investigation</name>
  <files>.github/workflows/codeql.yml, .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md</files>
  <read_first>
    - .github/workflows/codeql.yml analyze-rust job
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-RESEARCH.md (8m58s baseline run 33922528386)
  </read_first>
  <action>
    Per D-13: investigate analyze-rust for safe win without dropping paths or build-mode none. Acceptable: concurrency already set; document if no further change. Not acceptable: remove rust-toolchain.toml from paths, skip schedule, weaken SARIF. Add VALIDATION analyze-rust row with before run 33922528386 and after run id placeholder or no-change verdict. Run actionlint on codeql.yml if edited.
  </action>
  <acceptance_criteria>
    - grep -F 'build-mode: none' .github/workflows/codeql.yml
    - grep -F 'rust-toolchain.toml' .github/workflows/codeql.yml
    - VALIDATION analyze-rust row present
  </acceptance_criteria>
  <verify>
    <automated>grep -F 'build-mode: none' .github/workflows/codeql.yml && grep -F 'rust-toolchain.toml' .github/workflows/codeql.yml && grep -F 'analyze-rust' .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md && uvx zizmor@1.30.0 .github/workflows/codeql.yml</automated>
    <fails_when>CodeQL rust paths weakened, VALIDATION row missing, or zizmor exits non-zero</fails_when>
  </verify>
  <done>All three D-13 baselines have documented experiment outcomes (REL-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI YAML → runner | Workflow changes must not drop security scanners |
| Parallel builds → cosign | Both digests required before sign step |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.5-14 | Elevation | Trivy/CodeQL removal | critical | mitigate | Verify trivy-action count ≥2; build-mode none retained (D-13) |
| T-04.5-15 | Tampering | backend-image parallel | high | mitigate | actionlint + preserve digest env vars for cosign |
| T-04.5-16 | Repudiation | Walltime claims | medium | mitigate | gh run view IDs in VALIDATION before/after table |
| T-04.5-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages |
</threat_model>

<verification>
actionlint on touched workflows. Trivy and CodeQL gates intact. VALIDATION CI experiments table covers all three baselines.
</verification>

<success_criteria>
REL-04 satisfied: baselines from Plan 01 plus measured or documented-no-change outcomes for each baseline job.
</success_criteria>

## Artifacts this phase produces

- `.github/workflows/backend-image.yml` — optional parallel build layout (REL-04 experiment)
- `.github/workflows/ci.yml` — optional desktop-rust tweak if safe win found
- `.github/workflows/codeql.yml` — unchanged or documented no-change
- `.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md` — CI experiments table with run IDs

<output>
Create `.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-05-SUMMARY.md` when done.
</output>
