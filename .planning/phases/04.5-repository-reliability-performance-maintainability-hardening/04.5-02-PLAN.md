---
phase: 04.5-repository-reliability-performance-maintainability-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/clared-tax-rule-dsl-schema.json
  - packages/tax-engine/src/schema/clared-tax-rule-dsl-schema.json
  - scripts/assert-ajv-zero-warnings.sh
autonomous: false
requirements:
  - REL-03
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Seven union type arrays refactored to anyOf in schema SSOT (D-09, D-11)"
    - "docs/ and packages/tax-engine/src/schema/ copies byte-identical after sync"
    - "pnpm --filter @clared/tax-engine test emits zero strict mode: warnings (D-12, REL-03)"
    - "store.ts Ajv options unchanged (D-10)"
  artifacts:
    - docs/clared-tax-rule-dsl-schema.json
    - packages/tax-engine/src/schema/clared-tax-rule-dsl-schema.json
    - scripts/assert-ajv-zero-warnings.sh
  key_links:
    - "Schema SSOT edited in docs/ first then copied to tax-engine package"
    - "evaluate.spec.ts matrix still passes all rule IDs after anyOf refactor"
    - "No allowUnionTypes or strictTypes loosening in store.ts"
---

<objective>
Eliminate all AJV strictTypes warnings via anyOf schema refactor — fixtures and SSOT only (REL-03, D-09 through D-12).

Purpose: Clean test output without weakening production validation or tax semantics.

Output: Zero-warning tax-engine test runs locally and in CI.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-CONTEXT.md
@.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-RESEARCH.md
@.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-PATTERNS.md
@docs/clared-tax-rule-dsl-schema.json
@packages/tax-engine/src/store.ts
@packages/tax-engine/src/evaluate.spec.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm schema anyOf refactor preserves tax semantics</name>
  <files>docs/clared-tax-rule-dsl-schema.json</files>
  <read_first>
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-CONTEXT.md (D-11 one-way door)
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-RESEARCH.md (Pattern 2 anyOf, fields list)
  </read_first>
  <decision>Proceed with structural anyOf schema refactor without changing tax semantics (D-11)</decision>
  <context>Locked D-11: evaluate(), priorities, and collision semantics must stay unchanged. D-10 forbids store.ts Ajv option edits.</context>
  <options>
    <option id="proceed-anyof">
      <name>Proceed with anyOf refactor in schema SSOT only</name>
      <pros>Fixes 7 strictTypes warnings per D-09/D-12; evaluate.spec.ts is acceptance gate</pros>
      <cons>One-way schema shape change; abort if any rule fixture fails</cons>
    </option>
    <option id="abort">
      <name>Abort REL-03 implementation</name>
      <pros>No schema churn</pros>
      <cons>Phase goal unmet; warnings remain</cons>
    </option>
  </options>
  <resume-signal>Type proceed-anyof to continue or abort to halt</resume-signal>
  <action>
    Default expectation: proceed-anyof. Executor must run full evaluate.spec.ts after refactor and revert if behavior changes.
  </action>
  <acceptance_criteria>
    - Decision recorded before schema edits
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/clared-tax-rule-dsl-schema.json</automated>
    <fails_when>checkpoint not recorded before schema edits begin</fails_when>
  </verify>
  <done>D-11 scope acknowledged; REL-03 implementation may proceed.</done>
</task>

<task type="tracer">
  <name>End-to-end anyOf tracer — one field through sync and test</name>
  <files>docs/clared-tax-rule-dsl-schema.json, packages/tax-engine/src/schema/clared-tax-rule-dsl-schema.json</files>
  <read_first>
    - docs/clared-tax-rule-dsl-schema.json (lines 16-23 supplier_country)
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-PATTERNS.md (anyOf target pattern)
    - packages/tax-engine/src/store.ts (confirm no edits)
  </read_first>
  <action>
    Refactor `supplier_country` from `"type": ["string", "array"]` to anyOf with string branch and array-of-strings branch per PATTERNS.md (D-09). Edit docs/clared-tax-rule-dsl-schema.json first. Copy to packages/tax-engine/src/schema/clared-tax-rule-dsl-schema.json. Run cmp to confirm byte identity. Run pnpm --filter @clared/tax-engine test — warning count must drop versus baseline 7. Do not touch store.ts or jest.config.cjs (D-10).
  </action>
  <acceptance_criteria>
    - cmp docs/clared-tax-rule-dsl-schema.json packages/tax-engine/src/schema/clared-tax-rule-dsl-schema.json exits 0
    - grep -F 'anyOf' docs/clared-tax-rule-dsl-schema.json matches for supplier_country
    - pnpm --filter @clared/tax-engine test exits 0
  </acceptance_criteria>
  <verify>
    <automated>cmp docs/clared-tax-rule-dsl-schema.json packages/tax-engine/src/schema/clared-tax-rule-dsl-schema.json && grep -A6 '"supplier_country"' docs/clared-tax-rule-dsl-schema.json | grep -F 'anyOf' && pnpm --filter @clared/tax-engine test</automated>
    <fails_when>schema copies diverge, anyOf missing on supplier_country, or tests fail</fails_when>
  </verify>
  <done>Tracer proves anyOf path works end-to-end for one field (D-09).</done>
</task>

<task type="auto">
  <name>Complete seven-field anyOf refactor and zero-warning gate</name>
  <files>docs/clared-tax-rule-dsl-schema.json, packages/tax-engine/src/schema/clared-tax-rule-dsl-schema.json, scripts/assert-ajv-zero-warnings.sh</files>
  <read_first>
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-RESEARCH.md (seven fields list, zero-warning gate command)
    - packages/tax-engine/src/evaluate.spec.ts (23 rule ID matrix)
  </read_first>
  <action>
    Refactor remaining six fields in docs SSOT: customer_country, supplier_vat_registered, customer_vat_registered, supply_type, channel (include null branch), invoice_tax_rate (number|string anyOf). Sync copy to packages/tax-engine/src/schema/. Add scripts/assert-ajv-zero-warnings.sh with shebang #!/usr/bin/env bash, set -euo pipefail, set -o pipefail. Script runs `pnpm --filter @clared/tax-engine test` first and exits non-zero on test failure before counting warnings. Then fails if combined output contains `strict mode:` (use `rg -c`; no `|| true` or default-to-zero). Fix test fixtures only if anyOf still warns — never loosen Ajv (D-09). All evaluate.spec.ts cases must pass unchanged (D-11).
  </action>
  <acceptance_criteria>
    - bash scripts/assert-ajv-zero-warnings.sh exits 0
    - cmp docs and packages schema copies exits 0
    - grep -cE '"type": \[' docs/clared-tax-rule-dsl-schema.json for the seven condition fields returns 0
  </acceptance_criteria>
  <verify>
    <automated>cmp docs/clared-tax-rule-dsl-schema.json packages/tax-engine/src/schema/clared-tax-rule-dsl-schema.json && bash scripts/assert-ajv-zero-warnings.sh</automated>
    <fails_when>schema copies differ, pnpm test exits non-zero, assert script exits non-zero, or any strict mode warning remains in test output</fails_when>
  </verify>
  <done>REL-03 complete: zero AJV warnings in local and CI tax-engine runs (D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rule JSON → Ajv compile | Schema change must accept all existing rule files |
| store.ts → production validation | Config must stay strict; no option bypass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.5-04 | Tampering | store.ts Ajv options | high | mitigate | Do not edit store.ts; D-10 enforced in task read_first |
| T-04.5-05 | Tampering | Schema SSOT drift | medium | mitigate | cmp gate between docs/ and packages/ copies after every edit |
| T-04.5-06 | Elevation | allowUnionTypes shortcut | high | mitigate | Reject allowUnionTypes; anyOf only per D-09 |
| T-04.5-07 | Tampering | Tax semantics via schema | critical | mitigate | D-11 checkpoint; evaluate.spec.ts full matrix must pass |
| T-04.5-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages; AJV already pinned |
</threat_model>

<verification>
assert-ajv-zero-warnings.sh passes. cmp schema copies. Full tax-engine test suite green. store.ts unchanged.
</verification>

<success_criteria>
Zero strict mode warnings in packages/tax-engine test output. Schema SSOT synchronized. Tax evaluate behavior unchanged.
</success_criteria>

## Artifacts this phase produces

- `docs/clared-tax-rule-dsl-schema.json` — anyOf refactor SSOT (REL-03, D-09)
- `packages/tax-engine/src/schema/clared-tax-rule-dsl-schema.json` — synced mirror
- `scripts/assert-ajv-zero-warnings.sh` — CI/local zero-warning gate

<output>
Create `.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-02-SUMMARY.md` when done.
</output>
