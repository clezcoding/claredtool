---
phase: 04.5-repository-reliability-performance-maintainability-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/scripts/coolify-deploy-nest.sh
  - .github/scripts/coolify-deploy-nest.test.sh
  - .github/workflows/backend-image.yml
autonomous: false
requirements:
  - REL-01
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "205-line deploy bash extracted verbatim to versioned script (D-01, REL-01)"
    - "Workflow calls script with same env secret boundaries as inline block (D-01, D-04)"
    - "ShellCheck clean via actionlint image; mock tests cover rollback order (D-02, D-03)"
    - "Fail-closed API then worker order and health poll preserved (D-03, D-04)"
  artifacts:
    - .github/scripts/coolify-deploy-nest.sh
    - .github/scripts/coolify-deploy-nest.test.sh
    - .github/workflows/backend-image.yml
  key_links:
    - "backend-image.yml deploy step run: bash .github/scripts/coolify-deploy-nest.sh"
    - "Script trap ERR rollback_changed order worker then API when both changed"
    - "Health poll hits clared-api.puzzlessdev.online/health/ready before worker pin"
---

<objective>
Extract Coolify deploy bash to locally testable script with identical runtime behavior (REL-01, D-01 through D-04).

Purpose: ShellCheck + mock coverage without changing production deploy safety from Phase 04.4.

Output: Versioned deploy script wired in backend-image.yml with passing mock tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-CONTEXT.md
@.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-RESEARCH.md
@.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-PATTERNS.md
@.github/workflows/backend-image.yml
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm deploy extraction preserves fail-closed order</name>
  <files>.github/workflows/backend-image.yml</files>
  <read_first>
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-CONTEXT.md (D-03 costly, D-04)
    - .github/workflows/backend-image.yml (deploy run block lines 235-439)
  </read_first>
  <decision>Extract inline deploy bash to versioned script without simplifying fail-closed flow (D-03, D-04)</decision>
  <context>Phase 04.4 D-23/D-25 deploy safety is costly to reverse. Extraction must be verbatim move with same env boundaries.</context>
  <options>
    <option id="extract-verbatim">
      <name>Extract verbatim to .github/scripts/coolify-deploy-nest.sh</name>
      <pros>ShellCheck + mock tests per D-02; readability gain on 205 lines per D-01</pros>
      <cons>Production deploy path changes file location only; must pass mock rollback tests</cons>
    </option>
    <option id="keep-inline">
      <name>Keep inline bash in backend-image.yml</name>
      <pros>Zero deploy wiring change</pros>
      <cons>REL-01 unmet; no local test surface</cons>
    </option>
  </options>
  <resume-signal>Type extract-verbatim to continue or keep-inline to halt</resume-signal>
  <action>
    Default: extract-verbatim. Preserve trap ERR rollback, API-before-worker, health/ready poll before worker pin.
  </action>
  <acceptance_criteria>
    - Checkpoint recorded before script extraction
  </acceptance_criteria>
  <verify>
    <automated>grep -F 'coolify_pin' .github/workflows/backend-image.yml</automated>
    <fails_when>inline deploy block missing or checkpoint skipped</fails_when>
  </verify>
  <done>D-03 deploy safety scope locked for extraction (REL-01).</done>
</task>

<task type="tracer">
  <name>End-to-end extract script and wire workflow</name>
  <files>.github/scripts/coolify-deploy-nest.sh, .github/workflows/backend-image.yml</files>
  <read_first>
    - .github/workflows/backend-image.yml (lines 235-439)
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-PATTERNS.md (extraction contract)
  </read_first>
  <action>
    Create .github/scripts/coolify-deploy-nest.sh with #!/usr/bin/env bash and set -euo pipefail. Move deploy run block verbatim: coolify_get, coolify_pin, wait_for_deployment, rollback_changed, changed_api/changed_worker, trap ERR, API deploy, health poll loop to clared-api.puzzlessdev.online/health/ready, worker deploy. No set -x. No new env vars beyond COOLIFY_TOKEN, COOLIFY_URL, API_UUID, WORKER_UUID, SHA_TAG, IMAGE (D-01). Replace workflow deploy step run: | with run: bash .github/scripts/coolify-deploy-nest.sh keeping identical env: block (D-04). Keep job environment production, concurrency coolify-nest-deploy, cancel-in-progress false.
  </action>
  <acceptance_criteria>
    - test -f .github/scripts/coolify-deploy-nest.sh
    - grep -F 'bash .github/scripts/coolify-deploy-nest.sh' .github/workflows/backend-image.yml
    - grep -F 'health/ready' .github/scripts/coolify-deploy-nest.sh
    - grep -F 'trap rollback_changed ERR' .github/scripts/coolify-deploy-nest.sh
  </acceptance_criteria>
  <verify>
    <automated>test -f .github/scripts/coolify-deploy-nest.sh && grep -F 'bash .github/scripts/coolify-deploy-nest.sh' .github/workflows/backend-image.yml && grep -F 'health/ready' .github/scripts/coolify-deploy-nest.sh && grep -F 'trap rollback_changed ERR' .github/scripts/coolify-deploy-nest.sh && docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667 && uvx zizmor@1.30.0 .github/workflows/backend-image.yml</automated>
    <fails_when>script missing, workflow not wired, health poll or ERR trap absent, actionlint fails, or zizmor exits non-zero</fails_when>
  </verify>
  <done>Deploy path runs through versioned script; workflow parses (D-01, D-04).</done>
</task>

<task type="auto">
  <name>Mock deploy tests and ShellCheck gate</name>
  <files>.github/scripts/coolify-deploy-nest.test.sh, .github/scripts/coolify-deploy-nest.sh</files>
  <read_first>
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-PATTERNS.md (test harness, D-02 assertions)
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-RESEARCH.md (mock skeleton)
  </read_first>
  <action>
    Add .github/scripts/coolify-deploy-nest.test.sh per D-02: stub curl/jq via PATH fixtures; assert rollback_changed runs worker rollback before API when both changed; assert failed API health poll triggers rollback before worker pin; assert no COOLIFY_TOKEN in stdout/stderr. Run ShellCheck through actionlint docker image on both scripts (04.4 precedent — no koalaman image). Script must exceed ~150 lines proving readability gain (D-01). Do not run live Coolify deploy.
  </action>
  <acceptance_criteria>
    - bash .github/scripts/coolify-deploy-nest.test.sh exits 0
    - wc -l .github/scripts/coolify-deploy-nest.sh reports at least 150 lines
    - actionlint container passes on backend-image.yml
  </acceptance_criteria>
  <verify>
    <automated>bash .github/scripts/coolify-deploy-nest.test.sh && test "$(wc -l < .github/scripts/coolify-deploy-nest.sh | tr -d ' ')" -ge 150 && docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667 && uvx zizmor@1.30.0 .github/workflows/backend-image.yml</automated>
    <fails_when>mock tests fail, script under 150 lines, actionlint fails, or zizmor exits non-zero</fails_when>
  </verify>
  <done>REL-01 complete: locally testable deploy script with identical behavior contract (D-02, D-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GHA secrets → deploy script | COOLIFY_TOKEN must not log to stdout or GITHUB_STEP_SUMMARY |
| Coolify API → production | Wrong order or missing rollback deploys bad worker |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.5-10 | Information disclosure | coolify-deploy-nest.sh | critical | mitigate | No set -x; mock test asserts token absent from output |
| T-04.5-11 | Tampering | Deploy order | critical | mitigate | Preserve trap ERR and API-before-worker; mock tests assert rollback order (D-03) |
| T-04.5-12 | Denial of service | Health poll skip | high | mitigate | Keep health/ready poll before worker pin (D-04) |
| T-04.5-13 | Tampering | backend-image.yml | high | mitigate | actionlint on workflow after edit; SHA-pinned actions unchanged |
| T-04.5-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs |
</threat_model>

<verification>
Mock tests pass. Script ShellCheck clean via actionlint image. backend-image.yml actionlint clean. Deploy helpers and health poll present in script.
</verification>

<success_criteria>
Coolify deploy extracted with test coverage and unchanged fail-closed semantics.
</success_criteria>

## Artifacts this phase produces

- `.github/scripts/coolify-deploy-nest.sh` — versioned deploy orchestration (REL-01)
- `.github/scripts/coolify-deploy-nest.test.sh` — mock curl/jq tests (D-02)
- `.github/workflows/backend-image.yml` — calls script instead of inline bash

<output>
Create `.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-04-SUMMARY.md` when done.
</output>
