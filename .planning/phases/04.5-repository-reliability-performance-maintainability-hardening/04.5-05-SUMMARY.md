---
phase: 04.5-repository-reliability-performance-maintainability-hardening
plan: 05
subsystem: infra
tags: [github-actions, ci, docker, codeql, rust, performance]

requires:
  - phase: 04.5-01
    provides: CI baseline walltimes with gh run IDs in VALIDATION
  - phase: 04.5-04
    provides: backend-image deploy script extraction (unchanged by this plan)
  - phase: 04.5-06
    provides: hygiene wave complete before final CI experiments wave
provides:
  - Parallel push-api / push-worker jobs in backend-image.yml
  - CI experiments table in 04.5-VALIDATION.md for all three D-13 baselines
  - Documented no-change verdicts for desktop-rust and analyze-rust
affects:
  - backend-image CI walltime on main
  - REL-04 closure

actuals:
  tokens: 9000
  tasks: 3
  commits: 4

tech-stack:
  added: []
  patterns:
    - "Parallel docker push jobs with aggregate scan/sign job preserving cosign digest wiring"
    - "CI experiment log with before run IDs and implemented/no-change verdicts per baseline"

key-files:
  created: []
  modified:
    - .github/workflows/backend-image.yml
    - .github/workflows/codeql.yml
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md

key-decisions:
  - "Parallelize API/worker docker pushes into sibling jobs; Trivy/SBOM/cosign stay in downstream build job (A1)"
  - "desktop-rust: reject parallel clippy/test split — setup duplication outweighs savings"
  - "analyze-rust: no-change — concurrency, build-mode none, and path filters already optimal"

patterns-established:
  - "REL-04 experiments table in VALIDATION links before run IDs to verdicts; after run IDs filled post-merge"

requirements-completed: [REL-04]

coverage:
  - id: D1
    description: "backend-image parallel push-api/push-worker with Trivy, SBOM, cosign preserved"
    requirement: REL-04
    verification:
      - kind: other
        ref: "actionlint + zizmor on .github/workflows/backend-image.yml"
        status: pass
      - kind: other
        ref: "grep trivy-action count >= 2 in backend-image.yml"
        status: pass
    human_judgment: true
    rationale: "After-run walltime on main requires post-merge gh run view; before run 33922528337 recorded"
  - id: D2
    description: "desktop-rust baseline investigated with documented no-change outcome"
    requirement: REL-04
    verification:
      - kind: other
        ref: "grep desktop-rust + cargo clippy in ci.yml; VALIDATION no-change row"
        status: pass
    human_judgment: false
  - id: D3
    description: "analyze-rust CodeQL baseline investigated with documented no-change outcome"
    requirement: REL-04
    verification:
      - kind: other
        ref: "grep build-mode none + rust-toolchain.toml in codeql.yml; VALIDATION no-change row"
        status: pass
    human_judgment: false

duration: 5min
completed: 2026-09-05
status: complete
---

# Phase 04.5 Plan 05: CI Walltime Experiments Summary

**Parallel backend-image API/worker docker pushes with scan/sign aggregate job; desktop-rust and CodeQL baselines documented no-change per D-13**

## Performance

- **Duration:** 5 min
- **Started:** 2026-09-04T23:58:00Z
- **Completed:** 2026-09-05T00:03:00Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments

- Split `push-api` and `push-worker` into parallel jobs; `build` job runs Trivy (×2), SBOM (×2), cosign with `needs.push-*` digests
- Added **CI experiments** table to `04.5-VALIDATION.md` with before run IDs for all three baselines
- desktop-rust: no-change — fmt→clippy→test order and rust-cache retained; parallel split would duplicate apt/toolchain setup
- analyze-rust: no-change — concurrency, `build-mode: none`, and `rust-toolchain.toml` paths already in place

## Task Commits

Each task was committed atomically:

1. **Task 1: End-to-end backend-image parallel build experiment** - `aecc64b` (perf)
2. **Task 2: desktop-rust baseline investigation** - `8ed6699` (docs)
3. **Task 3: CodeQL Rust baseline investigation** - `a4f32f8` (docs)

**Plan metadata:** `e5a5019` (docs: complete plan)

## Files Created/Modified

- `.github/workflows/backend-image.yml` — parallel `push-api` / `push-worker` jobs; aggregate `build` for scanners and cosign
- `.github/workflows/codeql.yml` — D-13 audit comment only; behavior unchanged
- `.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md` — CI experiments table with verdicts

## Decisions Made

- Implemented parallel docker pushes (A1 hypothesis: ≥30s build savings) rather than blocking on measured after-run
- Rejected desktop-rust job split: duplicated Linux deps + toolchain install (~2m) exceeds clippy/test overlap savings
- Rejected CodeQL tuning: existing concurrency and `build-mode: none` are the safe ceiling

## Deviations from Plan

### Auto-fixed Issues

None - plan executed as written.

**Note:** Task 2 commit includes both desktop-rust and analyze-rust VALIDATION rows (single file edit); task 3 commit adds only the `codeql.yml` audit comment.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

**Post-merge follow-up:** Record backend-image after run ID and walltime in VALIDATION via `gh run view` once parallel layout runs on `main`.

## Next Phase Readiness

- REL-04 satisfied for documented outcomes; backend-image after-run measurement pending main merge
- All three baselines have experiment rows; security gates (Trivy ≥2, CodeQL build-mode none) verified intact

## Self-Check: PASSED

- FOUND: `.github/workflows/backend-image.yml`
- FOUND: `.github/workflows/codeql.yml`
- FOUND: `.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md`
- FOUND: `.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-05-SUMMARY.md`
- FOUND: `aecc64b`
- FOUND: `8ed6699`
- FOUND: `a4f32f8`

---
*Phase: 04.5-repository-reliability-performance-maintainability-hardening*
*Completed: 2026-09-05*
