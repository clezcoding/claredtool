---
phase: 04.5-repository-reliability-performance-maintainability-hardening
plan: 06
type: execute
wave: 3
depends_on:
  - 04.5-01
  - 04.5-03
files_modified:
  - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md
  - apps/desktop/src/__tests__/hygiene-boundary.test.ts
autonomous: true
requirements:
  - REL-05
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Dead-code candidates discovered via graph-first query then grep confirmation (D-14)"
    - "Only modules with proven zero callers removed (D-15)"
    - "Exported/public deletions include boundary test before removal (D-15)"
    - "Hygiene outcomes documented in 04.5-VALIDATION.md even if zero deletions"
  artifacts:
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md
  key_links:
    - "Graph query precedes rg caller confirmation for each deletion candidate"
    - "Boundary test pattern follows debug-panel-gate.test.ts for any export removed"
    - "No deletion when caller count > 0"
---

<objective>
Repo hygiene via knowledge graph discovery; remove dead code only with zero-caller proof (REL-05, D-14, D-15).

Purpose: Reduce maintainability debt without breaking imports.

Output: VALIDATION hygiene log; optional minimal deletions with tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-CONTEXT.md
@.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-RESEARCH.md
@.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-PATTERNS.md
@apps/desktop/src/__tests__/debug-panel-gate.test.ts
@.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end graph discovery to caller proof</name>
  <files>.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md</files>
  <read_first>
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md (graph tooling note from Plan 01)
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-CONTEXT.md (D-14, D-15)
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-RESEARCH.md (large modules list — not auto-delete)
  </read_first>
  <action>
    Using graphify or codebase-memory index from Plan 01, run graph-first query for unused exports or orphan modules (D-14). Produce candidate list (minimum 3 items reviewed). For top candidate with lowest risk, confirm zero callers with rg across repo excluding definition file. Record in VALIDATION **Repo hygiene** section: candidate path, graph signal, rg caller count, disposition keep|delete. Do not delete in this task — discovery only. Grep allowed for config literals and string paths per D-14.
  </action>
  <acceptance_criteria>
    - VALIDATION Repo hygiene section lists at least 3 reviewed candidates
    - Each candidate row includes graph source and rg caller count
  </acceptance_criteria>
  <verify>
    <automated>grep -F 'Repo hygiene' .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md && test "$(awk '/^## Repo hygiene/,/^## /' .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md | grep -c '|')" -ge 4 && grep -Ei 'caller|graph' .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md</automated>
    <fails_when>VALIDATION lacks Repo hygiene section, fewer than 3 candidate data rows (header + separator + 3 rows), or rows missing caller/graph columns</fails_when>
  </verify>
  <done>Graph-first discovery tracer complete (D-14, REL-05).</done>
</task>

<task type="auto">
  <name>Delete zero-caller module with boundary test if exported</name>
  <files>apps/desktop/src/__tests__/hygiene-boundary.test.ts</files>
  <read_first>
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-PATTERNS.md (boundary test pattern)
    - apps/desktop/src/__tests__/debug-panel-gate.test.ts
    - VALIDATION Repo hygiene candidate with delete disposition
  </read_first>
  <action>
    If at least one candidate has rg caller count 0 and is not a public API surface required by Tauri IPC: add boundary test at apps/desktop/src/__tests__/hygiene-boundary.test.ts when module is exported (D-15), then delete module and fix imports — list deleted path in SUMMARY files_modified. If module is non-exported internal with zero callers, delete without test. If no zero-caller candidate exists, skip deletion, document scoped-down hygiene in VALIDATION (A3), and leave hygiene-boundary.test.ts absent or as skipped placeholder. Run tests only for the touched package (desktop if desktop file deleted; do not fall back to tax-engine).
  </action>
  <acceptance_criteria>
    - Any deletion has prior rg caller count 0 documented in VALIDATION
    - If export deleted, new or extended boundary test exists
    - Relevant package tests pass
  </acceptance_criteria>
  <verify>
    <automated>set -o pipefail; if rg -q 'delete disposition' .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md; then pnpm --filter ./apps/desktop test; else grep -Fi 'no deletion' .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md; fi</automated>
    <fails_when>desktop tests exit non-zero after deletion, deletion lacks zero-caller evidence in VALIDATION, or no-deletion path lacks explicit no-deletion verdict</fails_when>
  </verify>
  <done>At most one safe deletion shipped or explicit no-deletion verdict (D-15, REL-05).</done>
</task>

<task type="auto">
  <name>Hygiene sign-off and duplicate config scan</name>
  <files>.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md</files>
  <read_first>
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-RESEARCH.md (duplicate config, oversized modules)
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md
  </read_first>
  <action>
    Grep-only scan for duplicate config literals (YAML/env duplicates, twin pnpm install blocks) per D-14 grep exception. Record findings in VALIDATION: fixed|documented|none. Mark REL-05 complete. Set nyquist_compliant true in VALIDATION frontmatter if all automated verifies from phase plans have file-exists paths green. Do not refactor rechnung.tsx/tax.tsx size in this phase — note as future optional, out of 04.5 scope unless zero-caller sub-export found.
  </action>
  <acceptance_criteria>
    - VALIDATION contains Hygiene sign-off subsection
    - REL-05 marked complete in VALIDATION or SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>grep -Fi 'hygiene sign-off' .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md || grep -Fi 'REL-05' .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md</automated>
    <fails_when>VALIDATION lacks hygiene sign-off or REL-05 completion note</fails_when>
  </verify>
  <done>REL-05 repo hygiene wave complete with graph-first evidence (D-14, D-15).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Export deletion → importers | Zero-caller proof must include dynamic import strings |
| Graph false negative → production break | rg backstop required before delete |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.5-17 | Denial of service | Dead code deletion | high | mitigate | rg caller count 0 + graph query; boundary test for exports (D-15) |
| T-04.5-18 | Tampering | Graph-only grep skip | medium | mitigate | D-14 requires graph first; grep confirms literals |
| T-04.5-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages |
</threat_model>

<verification>
VALIDATION Repo hygiene section complete. Any deletion has zero-caller proof. Package tests pass.
</verification>

<success_criteria>
Graph-first hygiene executed; deletions only with proof; outcomes documented.
</success_criteria>

## Artifacts this phase produces

- `.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md` — Repo hygiene candidate table and sign-off (REL-05)
- Optional deleted source files — only with zero callers and documented evidence

<output>
Create `.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-06-SUMMARY.md` when done.
</output>
