---
phase: 04.5-repository-reliability-performance-maintainability-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 04.5-01
files_modified:
  - apps/desktop/vite.config.ts
  - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md
autonomous: true
requirements:
  - REL-02
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Main index chunk gzip ≤500 kB measured and recorded (D-05, REL-02)"
    - "sentry dynamic import produces separate chunk; updater ineffective import documented (D-06)"
    - "No route/manualChunks split shipped — gzip gate passes (D-05, D-07, D-08)"
    - "Total gzip bundle within baseline; split rejected with evidence in VALIDATION"
  artifacts:
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md
  key_links:
    - "vite build output lists sentry-*.js separate from index-*.js"
    - "VALIDATION documents static updater imports in app-shell, update-dialog, theme-menu"
    - "vite.config.ts unchanged unless gzip gate fails (expected no edit)"
---

<objective>
Verify desktop Vite bundle metrics and dynamic-import effectiveness; reject code split by default (REL-02, D-05 through D-08).

Purpose: Close handoff chunk warning without UI changes or bundle regression.

Output: VALIDATION bundle section with gzip evidence and explicit split rejection.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-RESEARCH.md
@.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-PATTERNS.md
@apps/desktop/vite.config.ts
@apps/desktop/src/main.tsx
@apps/desktop/src/components/app-shell.tsx
</context>

<tasks>

<task type="tracer">
  <name>End-to-end Vite build metric capture</name>
  <files>.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md</files>
  <read_first>
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-RESEARCH.md (measured baseline table, D-05)
    - apps/desktop/vite.config.ts
  </read_first>
  <action>
    Run pnpm --filter ./apps/desktop exec vite build. Capture index chunk minified and gzip sizes from build output. Record in 04.5-VALIDATION.md **Desktop bundle** section: index minified kB, index gzip kB, sentry chunk sizes, total JS gzip. Assert main gzip ≤500000 bytes equivalent (240.27 kB baseline). If gzip exceeds 500 kB, stop and escalate — do not implement split in this plan without orchestrator replan (D-05). Expected outcome: gate passes, no vite.config.ts edit.
  </action>
  <acceptance_criteria>
    - VALIDATION.md contains index gzip figure
    - pnpm --filter ./apps/desktop exec vite build exits 0
    - Build log shows gzip under 500 kB for index chunk
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec vite build 2>&1 | tee /tmp/vite-045.log && rg 'index-.*gzip' /tmp/vite-045.log && grep -F 'Desktop bundle' .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md</automated>
    <fails_when>vite build fails, index gzip line missing from log, or VALIDATION lacks Desktop bundle section</fails_when>
  </verify>
  <done>REL-02 baseline metrics captured; gzip gate evaluated (D-05).</done>
</task>

<task type="auto">
  <name>Verify dynamic imports and document split rejection</name>
  <files>.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md</files>
  <read_first>
    - apps/desktop/src/main.tsx (dynamic sentry/updater)
    - apps/desktop/src/components/app-shell.tsx (debug-panel DEV guard)
    - .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-RESEARCH.md (updater static import grep)
  </read_first>
  <action>
    Confirm build output contains separate sentry-*.js asset (D-06 effective split). Grep apps/desktop/src for static imports of lib/updater from app-shell.tsx, update-dialog.tsx, theme-menu.ts — document updater dynamic import ineffective. Note debug-panel DEV-only dynamic import has no prod chunk requirement. Add VALIDATION **Split decision** paragraph: route/manualChunks rejected because index gzip 240 kB < 500 kB; +5% cap irrelevant; minified 787 kB warning accepted. Do not edit vite.config.ts or routing (D-08). If grep finds no sentry-*.js in dist listing, fail task — split verification broken.
  </action>
  <acceptance_criteria>
    - rg 'sentry-' /tmp/vite-045.log or fresh build log matches
    - VALIDATION mentions updater static imports and split rejected
    - git diff --name-only apps/desktop/vite.config.ts is empty
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec vite build 2>&1 | rg 'sentry-.*\.js' && grep -Fi 'split rejected' .planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md && test "$(git diff --name-only -- apps/desktop/vite.config.ts | wc -l | tr -d ' ')" -eq 0</automated>
    <fails_when>sentry chunk missing, VALIDATION lacks rejection rationale, or vite.config.ts was modified</fails_when>
  </verify>
  <done>REL-02 verify-only complete; no bundle split shipped (D-05, D-06, D-07, D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vite dist → Tauri bundle | Asset paths must remain valid for Tauri packaging |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.5-08 | Tampering | vite.config manualChunks | medium | mitigate | No config edit unless gzip >500 kB; default reject per D-05 |
| T-04.5-09 | Denial of service | Bundle +5% regression | low | accept | Split not applied; D-07 N/A when rejected |
| T-04.5-SC | Tampering | npm/pip/cargo installs | high | accept | No new deps |
</threat_model>

<verification>
Vite build succeeds. sentry chunk present. VALIDATION documents metrics and split rejection. vite.config.ts untouched.
</verification>

<success_criteria>
Desktop bundle debt closed as verify-only with evidence; gzip under 500 kB gate; rejected ideas documented.
</success_criteria>

## Artifacts this phase produces

- `.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md` — Desktop bundle metrics, dynamic import audit, split rejection (REL-02)

<output>
Create `.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-03-SUMMARY.md` when done.
</output>
