# Phase 04.5: Repository Reliability, Performance & Maintainability Hardening — Pattern Map

**Mapped:** 2026-09-05
**Baseline:** `origin/main` @ `2831456950e5a2ec7f13d3a9be103bc1efa375fd`
**Files analyzed:** 18
**Analogs found:** 15 / 18

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `.github/scripts/coolify-deploy-nest.sh` | utility | request-response | `.github/workflows/backend-image.yml` deploy `run:` (lines 235–439) | exact (extract source) |
| `.github/scripts/coolify-deploy-nest.test.sh` | test | batch | `.agents/skills/diagnosing-bugs/scripts/hitl-loop.template.sh` | partial (bash harness only) |
| `.github/workflows/backend-image.yml` | config | request-response | itself (deploy job + build job) | exact |
| `docs/clared-tax-rule-dsl-schema.json` | config | transform | itself (`docs/clared-tax-rule-dsl-schema.json`) | exact |
| `packages/tax-engine/src/schema/clared-tax-rule-dsl-schema.json` | config | transform | `docs/clared-tax-rule-dsl-schema.json` (byte-identical sync) | exact |
| `scripts/assert-ajv-zero-warnings.sh` | utility | batch | `.github/workflows/zizmor.yml` actionlint step | partial (shell gate pattern) |
| `packages/tax-engine/jest.config.cjs` | config | batch | itself — **do not change** (D-10) | exact (no-op) |
| `packages/tax-engine/src/store.ts` | service | transform | itself — **do not change** Ajv options (D-10) | exact (no-op) |
| `apps/desktop/vite.config.ts` | config | transform | itself — optional; split rejected by default (gzip 240 kB) | exact |
| `apps/desktop/src/main.tsx` | hook | transform | itself — verify dynamic imports only | exact |
| `apps/desktop/src/components/app-shell.tsx` | component | transform | itself — verify `debug-panel` dynamic import | exact |
| `apps/desktop/src/lib/updater.ts` | service | transform | itself — optional thin-module experiment only if gzip gate fails | exact |
| `.github/workflows/ci.yml` | config | batch | itself (`desktop-rust` job) | exact |
| `.github/workflows/codeql.yml` | config | batch | itself (`analyze-rust` job) | exact |
| `.planning/ROADMAP.md` | config | transform | itself — addendum pointer, not rewrite 04.4 history | partial |
| `.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-POST-MERGE-ADDENDUM.md` | config | transform | `04.4-D34-EVIDENCE.md` | role-match |
| `.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md` | config | batch | `04.4-VALIDATION.md` | exact |
| `.planning/graphs/graph.json` (via graphify) | config | batch | — | none (tooling gap) |

**Conditional / discovery-phase (REL-05):** dead-code target files TBD after graphify index or `codebase-memory-mcp index_repository`. Boundary test analog: `apps/desktop/src/__tests__/debug-panel-gate.test.ts`.

## Pattern Assignments

### `.github/scripts/coolify-deploy-nest.sh` (utility, request-response)

**Analog:** `.github/workflows/backend-image.yml` deploy step (lines 235–439)

**Extraction contract:** Move bash verbatim from workflow `run:` block. Workflow keeps `env:` secret/var boundaries; script reads same env vars. No `set -x`. No new env vars.

**Shebang / strict mode** (from workflow lines 236–242):

```bash
set -euo pipefail
test -n "$COOLIFY_TOKEN"
test -n "$COOLIFY_URL"
test -n "$API_UUID"
test -n "$WORKER_UUID"
test -n "$SHA_TAG"
test -n "$IMAGE"
```

**Core helper pattern** (lines 249–271):

```bash
coolify_get() {
  curl -fsS -X GET "${COOLIFY_URL}/api/v1/applications/${1}" \
    -H "Authorization: Bearer ${COOLIFY_TOKEN}" \
    -H "Accept: application/json"
}

coolify_pin() {
  local uuid="$1" tag="$2" deploy_json
  curl -fsS -X PATCH "${COOLIFY_URL}/api/v1/applications/${uuid}" \
    -H "Authorization: Bearer ${COOLIFY_TOKEN}" \
    -H "Accept: application/json" \
    -H "Content-Type: application/json" \
    -d "{\"docker_registry_image_name\":\"${IMAGE}\",\"docker_registry_image_tag\":\"${tag}\"}" \
    -o /dev/null
  deploy_json=$(curl -fsS -X POST "${COOLIFY_URL}/api/v1/deploy" \
    -H "Authorization: Bearer ${COOLIFY_TOKEN}" \
    -H "Accept: application/json" \
    -H "Content-Type: application/json" \
    -d "{\"uuid\":\"${uuid}\",\"force\":true}")
  printf '%s' "$deploy_json" \
    | jq -er '.deployments[0].deployment_uuid | strings | select(length > 0)'
}
```

**Fail-closed deploy order** (lines 303–352, 354–439):

```bash
changed_api=0
changed_worker=0

rollback_changed() {
  echo "Rolling back already-changed apps"
  # worker rollback first, then API — preserve order from inline script
  ...
}

trap rollback_changed ERR
# API deploy → wait → health poll → worker deploy → worker status poll
trap - ERR
```

**Health poll before worker** (lines 371–387) — keep unchanged:

```bash
for _ in $(seq 1 24); do
  API_POLL_JSON=$(coolify_get "$API_UUID" || true)
  LIVE_TAG=$(printf '%s' "${API_POLL_JSON:-}" | jq -r '.docker_registry_image_tag // empty' 2>/dev/null || true)
  code=$(curl -sS -o /dev/null -w "%{http_code}" \
    "https://clared-api.puzzlessdev.online/health/ready" || true)
  if [ "$LIVE_TAG" = "$SHA_TAG" ] && [ "$code" = "200" ]; then
    poll_ok=1
    break
  fi
  sleep 5
done
```

**Workflow invocation pattern** (target after extraction):

```yaml
      - name: Deploy API then worker
        env:
          COOLIFY_TOKEN: ${{ secrets.COOLIFY_TOKEN }}
          COOLIFY_URL: ${{ vars.COOLIFY_URL }}
          API_UUID: ${{ vars.COOLIFY_APP_API }}
          WORKER_UUID: ${{ vars.COOLIFY_APP_WORKER }}
          SHA_TAG: ${{ needs.build.outputs.sha_tag }}
          IMAGE: ${{ needs.build.outputs.image }}
        run: bash .github/scripts/coolify-deploy-nest.sh
```

---

### `.github/scripts/coolify-deploy-nest.test.sh` (test, batch)

**Analog:** `.agents/skills/diagnosing-bugs/scripts/hitl-loop.template.sh` (bash harness); mock stubs — no repo analog

**Harness pattern** (lines 1–18):

```bash
#!/usr/bin/env bash
set -euo pipefail
```

**Test skeleton** (from RESEARCH.md — planner implements):

```bash
export COOLIFY_URL="http://mock" COOLIFY_TOKEN="t" API_UUID="a" WORKER_UUID="w"
export SHA_TAG="sha-deadbeef" IMAGE="ghcr.io/org/clared"
# PATH stubs: curl/jq emit fixtures; assert rollback order worker→API
# source script functions or run script with stubbed externals
```

**Assertions required (D-02/D-03):**
- `trap ERR` fires → `rollback_changed` runs worker before API when both changed
- Failed API health poll → rollback before worker pin
- No token in stdout/stderr/`GITHUB_STEP_SUMMARY`

---

### `.github/workflows/backend-image.yml` (config, request-response + batch)

**Analog:** itself

**Deploy step — replace inline `run: |` with script call** (see above). Keep job `environment: production`, `concurrency: coolify-nest-deploy`, `cancel-in-progress: false` (lines 214–220).

**Build job — sequential Docker pattern to preserve** (lines 101–131):

```yaml
      - id: push-api
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          file: apps/backend/Dockerfile
          platforms: linux/amd64
          push: true
          cache-from: type=gha,scope=clared-backend
          cache-to: type=gha,scope=clared-backend,mode=max

      - id: push-worker
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          target: worker
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.image.outputs.sha_tag }}-worker
          cache-from: type=gha,scope=clared-backend
```

**CI perf experiment (REL-04):** parallelize `push-api`/`push-worker` and/or dual Trivy scans only with before/after `gh run view` evidence. Keep scanners, cosign, SBOM steps. Rollback: revert to sequential steps above.

**Sign step bash pattern** (lines 186–191) — same `set -euo pipefail` style for any new inline bash:

```bash
          set -euo pipefail
          test -n "$DIGEST_API"
          test -n "$DIGEST_WORKER"
          cosign sign --yes "${IMAGE}@${DIGEST_API}"
```

---

### `docs/clared-tax-rule-dsl-schema.json` (config, transform)

**Analog:** itself; TypeScript union types in `packages/tax-engine/src/schema.ts` (lines 51–59) confirm runtime accepts both shapes

**Fields to refactor** (lines 16–23, 36) — replace union `type` arrays with `anyOf`:

```json
"supplier_country": { "type": ["string", "array"] },
"customer_country": { "type": ["string", "array"] },
"supplier_vat_registered": { "type": ["boolean", "string"] },
"customer_vat_registered": { "type": ["boolean", "string"] },
"supply_type": { "type": ["string", "array"] },
"channel": { "type": ["string", "array", "null"] },
"invoice_tax_rate": { "type": ["number", "string"] }
```

**Target pattern** (one field — apply to all seven):

```json
"supplier_country": {
  "anyOf": [
    { "type": "string" },
    { "type": "array", "items": { "type": "string" } }
  ]
}
```

**Nullable pattern** for `channel`:

```json
"channel": {
  "anyOf": [
    { "type": "string" },
    { "type": "array", "items": { "type": "string" } },
    { "type": "null" }
  ]
}
```

**Sync rule:** Edit `docs/` first; copy to `packages/tax-engine/src/schema/`; verify `cmp docs/clared-tax-rule-dsl-schema.json packages/tax-engine/src/schema/clared-tax-rule-dsl-schema.json`.

---

### `packages/tax-engine/src/schema/clared-tax-rule-dsl-schema.json` (config, transform)

**Analog:** `docs/clared-tax-rule-dsl-schema.json` (must stay byte-identical)

No independent edits. Mirror docs SSOT after every schema change.

---

### `packages/tax-engine/src/store.ts` + `jest.config.cjs` (service/config — no-op)

**Analog:** themselves

**Production Ajv — do not change** (store.ts lines 7–8):

```typescript
const ajv = new Ajv2020({ allErrors: true });
addFormats(ajv);
```

**Jest config — unchanged** (`jest.config.cjs` lines 1–17):

```javascript
module.exports = {
  moduleFileExtensions: ["js", "json", "ts"],
  rootDir: "src",
  testRegex: ".spec.ts$",
  testEnvironment: "node",
};
```

---

### `scripts/assert-ajv-zero-warnings.sh` (utility, batch)

**Analog:** `.github/workflows/zizmor.yml` actionlint docker gate (lines 73–76)

**Lint gate pattern:**

```yaml
      - name: actionlint
        run: |
          docker run --rm -v "$PWD:/repo" -w /repo \
            rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667
```

**Assert script target** (from RESEARCH):

```bash
#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/../packages/tax-engine"
WARN_COUNT=$(pnpm test 2>&1 | rg -c 'strict mode:' || true)
test "${WARN_COUNT:-0}" -eq 0
```

Alternative: add to plan verify only — no new file if wave uses inline grep in PLAN `<verify>`.

---

### `apps/desktop/vite.config.ts` (config, transform)

**Analog:** itself — **default: no edit** (gzip 240 kB < 500 kB per D-05)

**Current pattern** (lines 16–42) — no `manualChunks` / `rolldownOptions`:

```typescript
export default defineConfig({
  plugins: [react(), tailwindcss()],
  define: {
    "import.meta.env.VITE_APP_VERSION": JSON.stringify(appVersion),
  },
  resolve: {
    alias: {
      "@": path.resolve(root, "./src"),
    },
  },
  // ...
});
```

If split ever approved: measure total gzip before/after; reject if >+5% (D-07). Do not add route-level splits without failing gzip gate.

---

### `apps/desktop/src/main.tsx` (hook, transform)

**Analog:** itself

**Effective dynamic import** (lines 35–41):

```typescript
requestAnimationFrame(() => {
  void import("./lib/sentry").then(({ initDesktopSentry }) => initDesktopSentry());
  void import("./lib/updater").then(({ startUpdateScheduler }) =>
    startUpdateScheduler(),
  );
});
```

**Verify-only default:** `sentry` chunk splits (~57 kB). `updater` ineffective — static imports in `app-shell.tsx`, `update-dialog.tsx`, `theme-menu.ts`. Document in validation notes; no code change unless gzip gate fails.

---

### `apps/desktop/src/components/app-shell.tsx` (component, transform)

**Analog:** itself

**DEV-gated dynamic import** (lines 60–65):

```typescript
  useEffect(() => {
    if (!import.meta.env.DEV || import.meta.env.MODE === "test") return;
    void import("./debug-panel").then((mod) => {
      setDebugPanel(() => mod.DebugPanel);
    });
  }, []);
```

Stripped from prod behavior by guard. Verify-only for REL-02.

---

### `.github/workflows/ci.yml` (config, batch)

**Analog:** itself — `desktop-rust` job (REL-04 baseline ~7m29s)

**Job shell pattern** (lines 157–184):

```yaml
  desktop-rust:
    timeout-minutes: 15
    needs: changes
    if: |
      (github.event_name != 'pull_request' || github.event.pull_request.draft == false) &&
      (needs.changes.outputs.desktop-rust == 'true' || needs.changes.outputs.shared == 'true')
    runs-on: ubuntu-latest
    steps:
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # v1
        with:
          toolchain: "1.97.1"
          components: rustfmt, clippy
      - uses: swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          workspaces: apps/desktop/src-tauri
      - run: cargo fmt --check --manifest-path apps/desktop/src-tauri/Cargo.toml
      - run: cargo clippy --locked --manifest-path apps/desktop/src-tauri/Cargo.toml -- -D warnings
      - run: cargo test --locked --manifest-path apps/desktop/src-tauri/Cargo.toml
```

**Backend split** (lines 186–226) — live jobs are `backend-unit` + `backend-e2e`, not `backend-test`. Do not re-consolidate.

**CI experiment rule:** one PR per baseline change; record `gh run view <id> --json jobs` before/after.

---

### `.github/workflows/codeql.yml` (config, batch)

**Analog:** itself — `analyze-rust` job (REL-04 baseline ~8m58s)

**Detect + filter pattern** (lines 49–68):

```yaml
      - uses: dorny/paths-filter@ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d # v4.0.3
        id: filter
        with:
          filters: |
            rust:
              - '**/*.rs'
              - '**/Cargo.toml'
              - '**/rust-toolchain.toml'
```

Keep `build-mode: none` for Rust. No path-filter removal to save minutes.

---

### `.planning/ROADMAP.md` + `04.4-POST-MERGE-ADDENDUM.md` (config, transform)

**Analog:** `04.4-D34-EVIDENCE.md` (evidence table format); `04.4-MANUAL-GITHUB.md` (founder manual tasks)

**Drift to fix** (ROADMAP line 180): "consolidated backend-test" → `backend-unit` + `backend-e2e`. Do not edit immutable `04.4-*-SUMMARY.md` / `PLAN.md` bodies (D-16).

**Addendum table pattern** (from `04.4-D34-EVIDENCE.md` lines 1–18):

```markdown
# D-34 scenario evidence

| Scenario | Run | Jobs | Result |
|----------|-----|------|--------|
| Docs-only PR | PR #104 `33846513846` | `changes`+`ci` success; ... | pass |
```

**POST-MERGE addendum should cover:**
- `backend-unit` + `backend-e2e` (not `backend-test`)
- `windows-2025` + `macos-26` (not `windows-2022` / `macos-15`)
- Required checks: `gitleaks` + `ci`; Secret Scanning / Push Protection = manual founder tasks
- Link from ROADMAP Phase 04.4 success criteria — addendum pointer, not rewrite history

---

### `.planning/phases/04.5-repository-reliability-performance-maintainability-hardening/04.5-VALIDATION.md` (config, batch)

**Analog:** `04.4-VALIDATION.md`

**Frontmatter pattern** (lines 1–8):

```yaml
---
phase: "04.5"
slug: "repository-reliability-performance-maintainability-hardening"
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-05"
---
```

**Baseline recording section to add** (REL-04):

| Baseline | Job | Walltime | Run ID |
|----------|-----|----------|--------|
| PR desktop-rust | `desktop-rust` | 7m 29s | 33921528007 |
| main Rust CodeQL | `analyze-rust` | 8m 58s | 33922528386 |
| backend-image+deploy | `build`+`deploy` | ~5m 02s | 33922528337 |

---

## Shared Patterns

### Bash strict mode (all new shell scripts)

**Source:** `.github/workflows/backend-image.yml` (line 236), `.github/workflows/desktop-build.yml` (line 239)

**Apply to:** `coolify-deploy-nest.sh`, `coolify-deploy-nest.test.sh`, `assert-ajv-zero-warnings.sh`

```bash
set -euo pipefail
```

### ShellCheck gate

**Source:** `.github/workflows/zizmor.yml` (lines 73–76)

**Apply to:** any new `.github/scripts/*.sh`; add path to zizmor `on.push.paths` if new script dir created

```bash
docker run --rm -v "$PWD:/repo" -w /repo \
  rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667
```

Actionlint image includes ShellCheck — do not brew-install a second copy (04.4 Wave 0 precedent).

### Tax-engine test gate

**Source:** `packages/tax-engine/src/evaluate.spec.ts` (matrix fixtures lines 10–34)

**Apply to:** post-schema-change verification — all 23 rule IDs must still pass

```bash
pnpm --filter @clared/tax-engine test
```

### Boundary test before export deletion

**Source:** `apps/desktop/src/__tests__/debug-panel-gate.test.ts` (lines 60–78)

**Apply to:** REL-05 dead-code removal of public/exported modules (D-15)

```typescript
describe("debug-panel gate (D-46/D-47)", () => {
  it("returns null when DEBUG_PANEL_ENABLED is false", async () => {
    vi.stubEnv("DEV", false);
    vi.resetModules();
    const { DebugPanel } = await import("../components/debug-panel");
    const { container } = renderPanel(DebugPanel);
    expect(container.firstChild).toBeNull();
  });
});
```

### Script shebang (repo scripts)

**Source:** `packages/tax-engine/scripts/generate-rules.mjs` (lines 1–8)

**Apply to:** new `scripts/assert-ajv-zero-warnings.sh` if created at repo root

```javascript
#!/usr/bin/env node
```

For bash: `#!/usr/bin/env bash` per `hitl-loop.template.sh` line 1.

### Harden Runner + SHA-pinned actions

**Source:** `.github/workflows/ci.yml` (lines 28–31)

**Apply to:** any workflow YAML touch

```yaml
- name: Harden Runner
  uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
  with:
    egress-policy: audit
```

## No Analog Found

| File | Role | Data Flow | Reason |
|------|------|-----------|--------|
| `.github/scripts/coolify-deploy-nest.test.sh` (mock stubs) | test | batch | No curl/jq mock harness in repo; use RESEARCH skeleton + bash `PATH` stubs |
| `.planning/graphs/graph.json` | config | batch | graphify disabled; Wave 0 enables via `gsd-tools config-set graphify.enabled true` or codebase-memory index |
| `anyOf` in JSON Schema | config | transform | No existing `anyOf` in repo schemas; follow AJV strict-mode docs (RESEARCH Pattern 2) |

## Metadata

**Analog search scope:** `.github/workflows/`, `.github/scripts/` (empty), `packages/tax-engine/`, `apps/desktop/`, `.planning/phases/04.4*/`, `.agents/skills/**/scripts/`
**Files scanned:** ~45
**Git-tracked analogs verified:** all named paths checked via `git ls-files`
**Pattern extraction date:** 2026-09-05
