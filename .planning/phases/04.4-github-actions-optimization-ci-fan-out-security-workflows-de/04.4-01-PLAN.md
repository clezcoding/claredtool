---
phase: 04.4-github-actions-optimization-ci-fan-out-security-workflows-de
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/gitleaks.yml
  - .github/workflows/zizmor.yml
  - .github/workflows/codeql.yml
  - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-VALIDATION.md
autonomous: true
requirements:
  - BRIEF-1.1
  - BRIEF-1.2
  - BRIEF-1.3
  - BRIEF-1.4
  - D-01
  - D-28
  - D-29
  - D-31
  - D-33
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Working tree contains origin/main security workflows gitleaks.yml, zizmor.yml, and codeql.yml after rebase or fast-forward onto origin/main, before any remainder YAML invent (Wave 0). D-33 commit 1 is trigger/concurrency hunks only."
    - "Aggregator job id in ci.yml is exactly ci (D-01). Workflow-level paths key is absent on ci.yml on:."
    - "ci.yml pull_request types include ready_for_review (D-29, BRIEF-1.2)."
    - "Heavy ci.yml jobs no longer OR-true on every main push; changes plus aggregator still always run (D-28, BRIEF-1.1)."
    - "ci, gitleaks, zizmor, and codeql concurrency.group is ${{ github.workflow }}-${{ github.ref }} with cancel-in-progress true (D-31, BRIEF-1.3)."
    - "ci.yml run-name uses event/ref or PR number and head_ref only — never github.event.pull_request.title (D-31, BRIEF-1.4)."
    - statement: "A second identical merge of origin/main after this plan is a no-op on the trigger/concurrency hunks (D-28 idempotency)."
      verification: backstop
    - statement: "Skipped heavy children still let aggregator job ci conclude; failure or cancelled children fail it (D-28 concurrency)."
      verification: backstop
  artifacts:
    - .github/workflows/ci.yml
    - .github/workflows/gitleaks.yml
    - .github/workflows/zizmor.yml
    - .github/workflows/codeql.yml
  key_links:
    - "ci.yml on.pull_request.types contains ready_for_review and job if: uses github.event.pull_request.draft"
    - "concurrency.group on ci/gitleaks/zizmor/codeql matches github.workflow-github.ref without a ci- prefix"
    - "job id ci unchanged so Branch Protection required check still matches (D-01)"
  prohibitions:
    - statement: "Do not add the privileged PR-from-base workflow trigger for PR code (D-02)."
      status: unverified
    - statement: "Do not add workflow_run for desktop or backend deploy (D-02)."
      status: unverified
    - statement: "Do not put workflow-level paths on the ci workflow (D-01 pending-check trap)."
      status: unverified
    - statement: "Do not change GitHub Branch Protection, Environments, or Secret Scanning via API (D-06)."
      status: unverified
    - statement: "Do not trigger production Coolify deploy or create a v* tag during this phase (D-06, D-34)."
      status: unverified
    - statement: "Do not add a GitHub-hosted Prisma migrate job (D-22)."
      status: unverified
    - statement: "Do not add a new reporting SaaS or lockfile reporter package (D-35)."
      status: unverified
    - statement: "Do not recreate gitleaks.yml, zizmor.yml, or codeql.yml; rebase or fast-forward origin then edit (Pitfall 8)."
      status: unverified
---

<objective>
Wave 0 rebase or fast-forward onto origin/main plus D-33 logical commit 1: trigger/concurrency remainder (BRIEF-1.1, BRIEF-1.2, BRIEF-1.3, BRIEF-1.4, D-01, D-28, D-29, D-31).

Purpose: Planning branch lacks origin #92–#98. Rebase or fast-forward first so later plans edit existing security workflows. Then make docs-only and draft PRs cheap without dropping the required check named ci.

Output: Linear history on origin/main; ci.yml draft/path-on-main behavior; unified concurrency and run-names on analysis workflows.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-CONTEXT.md
@.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-RESEARCH.md
@.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-PATTERNS.md
@.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-VALIDATION.md
@docs/claredtool-github-actions-optimierungsauftrag.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end origin rebase/ff + ci.yml trigger slice</name>
  <files>.github/workflows/ci.yml, .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-VALIDATION.md</files>
  <read_first>
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-RESEARCH.md (Wave 0, Pattern 1–3, Pitfall 1–2, Pitfall 8)
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-PATTERNS.md (ci.yml Keep, Trigger remainder, Concurrency remainder, Main-exception to delete, Aggregator pattern)
    - .github/workflows/ci.yml
    - .github/workflows/labeler.yml (copy pull_request types list only; do not edit labeler)
  </read_first>
  <action>
    Fetch origin. Fast-forward or rebase this branch onto origin/main (commit 1419b31c029ef68b2c1f64402264b3a77d2ad1b1 or newer origin/main tip). Do not create a merge commit (D-33: a merge commit would be an eighth commit or would mix origin #92–#98 into logical commit 1). Wave 0 history alignment is not D-33 commit 1.

    If conflicts under .github/workflows/, keep origin versions of gitleaks.yml, zizmor.yml, codeql.yml and origin steps that add Harden-Runner, Trivy, squawk, and package-manager-cache false. Do not recreate those files from scratch.

    After the rebase or fast-forward, edit only ci.yml for this tracer (D-28, D-29, D-31, D-01). D-33 commit 1 must contain only trigger/concurrency workflow hunks (ci.yml plus Plan 01 task 2 analysis files), not the origin security-baseline commits:
    - Keep workflow and job id ci. Do not add paths under on.push or on.pull_request.
    - Set on.pull_request.types to opened, synchronize, reopened, ready_for_review (copy the list from labeler.yml; D-29, BRIEF-1.2).
    - Set concurrency.group to ${{ github.workflow }}-${{ github.ref }} and cancel-in-progress true. Remove the extra ci- prefix currently on origin (D-31, BRIEF-1.3, PATTERNS Concurrency remainder).
    - Add run-name using github.event_name, github.ref_name, and for pull_request also github.event.pull_request.number plus github.head_ref. Do not interpolate github.event.pull_request.title anywhere, including run: steps (D-31, BRIEF-1.4).
    - On every expensive job if: (lint, desktop-test, backend-unit, backend-e2e, squawk after merge — names still origin until Plan 02), delete the main-ref OR prefix documented in PATTERNS.md "Main-exception to delete". Replace with the draft-skip AND path pattern from RESEARCH Pattern 2, still using current output names (desktop, backend, shared, tax-engine) until Plan 02 splits filters (D-28, BRIEF-1.1).
    - Keep job ci with if: always(), needs including changes plus those expensive jobs, and the check() function that fails on failure or cancelled. Skipped children stay allowed (D-01).
    - Keep contents: read. Do not add workflow-level paths. Do not change labeler.yml or sync-labels.yml (D-36).
    - Do not gh workflow run, do not tag, do not push.

    Mark 04.4-VALIDATION.md Wave 0 rebase/ff checkbox done and map task 04.4-01-T1.
  </action>
  <acceptance_criteria>
    - test -f .github/workflows/gitleaks.yml and test -f .github/workflows/zizmor.yml and test -f .github/workflows/codeql.yml
    - grep -E '^  ci:' .github/workflows/ci.yml matches
    - grep -F 'ready_for_review' .github/workflows/ci.yml matches
    - ci.yml on: block has no paths: key
    - Heavy job if: blocks no longer contain the PATTERNS "Main-exception to delete" OR-prefix
  </acceptance_criteria>
  <verify>
    <automated>test -f .github/workflows/gitleaks.yml && test -f .github/workflows/zizmor.yml && test -f .github/workflows/codeql.yml && grep -E '^  ci:' .github/workflows/ci.yml && grep -F 'ready_for_review' .github/workflows/ci.yml && python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); on=d.get('on') or d.get(True); assert 'paths' not in (on.get('push') or {}); pr=on.get('pull_request') or {}; assert not (isinstance(pr, dict) and 'paths' in pr)" && test "$(grep -vE '^[[:space:]]*#' .github/workflows/ci.yml | grep -c \"github.ref == 'refs/heads/main' ||\" || true)" -eq 0</automated>
  </verify>
  <done>Origin security workflows present; required job still named ci; main-push exception gone; ready_for_review listed (D-01, D-28, D-29).</done>
</task>

<task type="auto">
  <name>Unify analysis concurrency and run-names</name>
  <files>.github/workflows/gitleaks.yml, .github/workflows/zizmor.yml, .github/workflows/codeql.yml</files>
  <read_first>
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-PATTERNS.md (gitleaks Target trigger + concurrency; zizmor Current trigger; codeql Keep + Target concurrency)
    - .github/workflows/gitleaks.yml
    - .github/workflows/zizmor.yml
    - .github/workflows/codeql.yml
  </read_first>
  <action>
    Edit origin files only (do not recreate). This is still D-33 commit 1 (same plan commit as the tracer).

    gitleaks.yml (D-08 trigger tightening starts here; SARIF waits for Plan 03): set on.push.branches to [main]; keep pull_request (drafts included by default); add workflow_dispatch; add concurrency group ${{ github.workflow }}-${{ github.ref }} with cancel-in-progress true; add short run-name without PR titles. Do not add a schedule. Keep fetch-depth 0, persist-credentials false, existing gitleaks-action SHA e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e, job name gitleaks.

    zizmor.yml: add the same concurrency + run-name. Do not add Actionlint or path filters yet (Plan 03). Keep zizmor-action SHA 6fc4b006235f201fdab3722e17240ab420d580e5.

    codeql.yml: add the same concurrency + run-name. Keep weekly cron "18 5 * * 1" and existing analyze matrix for this commit. Draft-skip and language split are Plan 03.

    Never set cancel-in-progress true on desktop-build or coolify-nest-deploy (do not edit those files in this plan).
  </action>
  <acceptance_criteria>
    - Each of gitleaks.yml, zizmor.yml, codeql.yml contains cancel-in-progress: true
    - Each contains github.workflow }}-${{ github.ref
    - gitleaks.yml on.push has branches: [main]
    - gitleaks.yml has no schedule: key
  </acceptance_criteria>
  <verify>
    <automated>for f in gitleaks zizmor codeql; do grep -q 'cancel-in-progress: true' .github/workflows/$f.yml && grep -F 'github.workflow }}-${{ github.ref' .github/workflows/$f.yml; done && grep -A2 'push:' .github/workflows/gitleaks.yml | grep -q 'main' && test "$(grep -c '^  schedule:' .github/workflows/gitleaks.yml || true)" -eq 0</automated>
  </verify>
  <done>Analysis workflows share D-31 concurrency; Gitleaks push limited to main (D-08 start, D-31).</done>
</task>

<task type="auto">
  <name>Local Actionlint container smoke + VALIDATION Wave 0</name>
  <files>.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-VALIDATION.md</files>
  <read_first>
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-VALIDATION.md
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-RESEARCH.md (Actionlint image digest, Environment Availability)
  </read_first>
  <action>
    Re-inspect the Actionlint image digest at execute time: docker buildx imagetools inspect rhysd/actionlint:1.7.12 — if the index digest differs from RESEARCH sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667, pin the live digest and note it in VALIDATION.md (A2). Run Actionlint against .github/workflows via that digest-pinned image (no curl pipe bash). That image includes ShellCheck; do not brew-install a second ShellCheck (D-34). Run uvx zizmor@1.30.0 . and record the output in VALIDATION.md Wave 0 — the tool must run; pre-existing origin findings are documented, not a Plan 01 fail (Plan 07 close is the zizmor gate). Record Wave 0 checkboxes: rebase/ff done, Actionlint available, zizmor ran, ShellCheck via Actionlint image. Map Per-Task Verification Map rows 04.4-W0-merge and BRIEF-1.1 / BRIEF-1.2 / D-01 to this plan. Do not brew-install a second Actionlint if Docker works. Do not push.
  </action>
  <acceptance_criteria>
    - Actionlint container run exits 0 on .github/workflows
    - uvx zizmor@1.30.0 ran (VALIDATION.md records zizmor)
    - VALIDATION.md records ShellCheck via Actionlint image
    - VALIDATION.md Wave 0 rebase/ff checkbox is marked done
    - VALIDATION.md mentions 04.4-01
  </acceptance_criteria>
  <verify>
    <automated>docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667 && uvx zizmor@1.30.0 --version && (uvx zizmor@1.30.0 .; echo zizmor_wave0_exit:$?) && grep -qi zizmor .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-VALIDATION.md && grep -qi shellcheck .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-VALIDATION.md && grep -q '04.4-01' .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-VALIDATION.md</automated>
  </verify>
  <done>Merged workflows parse; Wave 0 recorded Actionlint, Zizmor, ShellCheck-via-Actionlint-image (D-34).</done>
</task>

</tasks>

<!-- planner-discipline-allow: github.ref == 'refs/heads/main' || -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub PR fork → ci.yml | Untrusted head SHA; pull_request token is read-only |
| GitHub expressions in run: | Attacker-controlled PR title/body must not reach bash |
| Required check name ci | Skip-the-workflow paths would leave Branch Protection pending |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-01 | Elevation of privilege | ci.yml / all workflows | high | mitigate | Never add the privileged PR-from-base trigger; stay on pull_request (D-02). Block-on high. |
| T-04.4-02 | Tampering | ci.yml run-name and run: | high | mitigate | PR number and head_ref only; never PR title in run: (D-31). |
| T-04.4-03 | Tampering | ci.yml caches | medium | accept | This plan does not touch signed desktop; rust-cache stays off signed path (origin). |
| T-04.4-04 | Tampering | uses: pins | high | mitigate | Keep origin 40-char SHAs; add no moving tags (D-03). |
| T-04.4-05 | Information disclosure | run-name | low | mitigate | No secrets in run-name. |
| T-04.4-06 | Elevation of privilege | deploy triggers | high | mitigate | Do not add workflow_run (D-02). |
| T-04.4-07 | Tampering | GHCR latest | medium | accept | Not this plan (Plan 06, D-24). |
| T-04.4-08 | Tampering | dual migrators | medium | accept | Not this plan (Plan 06, D-22). |
| T-04.4-SC | Tampering | npm/pip/cargo installs | high | accept | No registry packages this plan. |
</threat_model>

<verification>
Actionlint on .github/workflows (ShellCheck included in that image). uvx zizmor@1.30.0. Grep job ci, ready_for_review, no workflow-level paths on ci.yml, no main-ref OR-prefix on heavy jobs. Files gitleaks.yml zizmor.yml codeql.yml exist.
</verification>

<success_criteria>
Origin security workflows are in the working tree via rebase or fast-forward (no merge commit). Required aggregator still named ci. Draft and path-on-main remainder for current job names is live. Analysis concurrency matches D-31. One git commit for D-33 item 1 covering only trigger/concurrency YAML (orchestrator/executor commit; planner does not commit).
</success_criteria>

## Artifacts this phase produces

- `.github/workflows/ci.yml` — trigger, concurrency, run-name, draft skip, no main-exception (job names still origin until 04.4-02)
- `.github/workflows/gitleaks.yml` — main-only push, dispatch, D-31 concurrency
- `.github/workflows/zizmor.yml` / `codeql.yml` — D-31 concurrency only
- `04.4-VALIDATION.md` — Wave 0 rebase/ff + Actionlint/Zizmor/ShellCheck + 04.4-01 task IDs

<output>
Create `.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-01-SUMMARY.md` when done.
D-33 commit 1 of 7: trigger/concurrency only. Wave 0 rebase/ff is separate and must not be a merge commit. Single commit. Do not squash with later plans. Do not push.
</output>

## Flagged assumptions

- A1: `clezcoding` may need `GITLEAKS_LICENSE` if it is an org — Plan 07 checklist, no secret invented in YAML.
- A2: Actionlint 1.7.12 digest re-inspected at execute.
- A3: Postgres/Redis digests re-inspected at Plan 07 execute.
- A4: empty `components:` on dtolnay vs rust-toolchain.toml — staging log after merge (D-17).
- A5: Bake abort is the expected default (Plan 06).
- D-30 walltime +25 percent cannot be measured until GitHub; consolidate now (backstop in Plan 02).
- D-34 GitHub scenario table waits for explicit push OK (backstop in Plan 07).

