---
phase: 04.4-github-actions-optimization-ci-fan-out-security-workflows-de
verified: 2026-09-04T05:20:00Z
status: human_needed
score: 14/17 must-haves verified
behavior_unverified: 3
overrides_applied: 0
decision_coverage:
  honored: 36
  total: 36
  not_honored: []
behavior_unverified_items:
  - truth: "Skipped heavy children still let aggregator job ci conclude; failure or cancelled children fail it (D-28 concurrency)."
    test: "Open a docs-only or path-filtered PR so desktop-web/desktop-rust/backend-test/squawk skip; then a PR that fails one heavy job."
    expected: "Job `ci` is success when children are skipped; job `ci` is failure when any needed child is failure or cancelled."
    why_human: "check() encodes the invariant in YAML; GitHub skipped/failure semantics are only observed on a real Actions run (D-34)."
  - truth: "backend-test walltime vs the slower of origin backend-unit/backend-e2e cannot be measured until GitHub; split again only if a later measured run exceeds +25 percent (D-30 boundary)."
    test: "After merge, compare one backend-test walltime to origin backend-unit vs backend-e2e (slower of the two)."
    expected: "Keep the consolidated job unless walltime exceeds +25 percent of that slower origin job."
    why_human: "No GitHub Actions timing exists in-repo; presence of backend-test does not prove the cost bound."
  - truth: "Validation table from the brief covered (docs-only, draft, JS/TS, Rust, backend, workflow-YAML, v* tag, staging dispatch) (D-34)."
    test: "After explicit push OK, run 04.4-MANUAL-GITHUB.md scenario table. No production Coolify dispatch and no v* tag in this phase. Staging dispatch (D-17) only after merge."
    expected: "Each row matches the Nachher column: docs-only/draft stay cheap; path-selective heavy jobs; no signed desktop on main; CodeQL language-split."
    why_human: "Live GitHub event matrix. D-34 forbids executing it until the user says push OK."
human_verification:
  - test: "After explicit push OK, run the D-34 GitHub scenario table in 04.4-MANUAL-GITHUB.md (docs-only PR, draft PR, ready_for_review JS/TS, Rust-only, backend-only, workflow-YAML, docs-only main). No production dispatch. No v* tag."
    expected: "changes + aggregator `ci` always; heavy jobs skip on draft/docs; path-selective desktop-web / desktop-rust / backend-test / squawk; CodeQL/Zizmor only when their paths match; no signed desktop-build on main."
    why_human: "GitHub-hosted event routing. D-34 forbids this until push OK."
  - test: "After merge: Branch Protection on main — add required status check named exactly `gitleaks` (job name). Keep `ci`. Do not require CodeQL or Zizmor. No admin bypass (D-09)."
    expected: "Merge blocked on gitleaks failure. Path-skipped CodeQL/Zizmor do not pend-block (D-13)."
    why_human: "D-06 forbids changing Branch Protection via API. Founder GitHub UI."
  - test: "After merge: enable GitHub Secret Scanning and Push Protection on the public repo (D-12). Check GITLEAKS_LICENSE if clezcoding is an org."
    expected: "Secret Scanning + Push Protection on. Gitleaks remains the fail-closed merge gate."
    why_human: "D-06 forbids Secret Scanning via API. Founder GitHub UI."
  - test: "After merge: workflow_dispatch staging desktop-build on both windows-2022 and macos-15 (D-17). Production dispatch and v* tag stay off."
    expected: "Both pinned runner generations complete a staging signed build. Swap labels only if that run proves the generation wrong."
    why_human: "D-34 / D-17: staging runner proof is after merge, not inside implementation."
---

# Phase 04.4: GitHub Actions Optimization Verification Report

**Phase Goal:** Implement `docs/claredtool-github-actions-optimierungsauftrag.md` in full — cut runner fan-out and cost without weakening the `ci` aggregator, signing, or deploy safety
**Verified:** 2026-09-04T05:20:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

Infrastructure/CI phase. Code must-haves are in the tree. Live GitHub scenario matrix, Branch Protection, Secret Scanning, and staging runner proof are founder steps by D-06 / D-34 — not automated gaps.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | ------- | ---------- | -------------- |
| 1 | Brief Phases 1–2: `ci.yml` path filters on `main`, draft-PR gating, desktop-web vs desktop-rust split, consolidated `backend-test` | ✓ VERIFIED | `on:` has no workflow-level `paths`. Heavy jobs `if:` draft skip plus `needs.changes.outputs.*` — no `github.ref == 'refs/heads/main' \|\|`. Jobs `desktop-web` / `desktop-rust` / `backend-test`. desktop-web filter is file globs under `apps/desktop/src/**` etc., not `apps/desktop/**`. `backend-unit` / `backend-e2e` gone. squawk job id unchanged. |
| 2 | Skipped heavy children still let aggregator `ci` conclude; failure/cancelled fail it (D-28) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `ci.yml` job id `ci`, `if: always()`, `check()` exits 1 only on `failure`/`cancelled`. Encoded, not exercised on GitHub. |
| 3 | Brief Phases 3–4: Gitleaks / Zizmor / Actionlint / CodeQL path+language as specified | ✓ VERIFIED | `gitleaks.yml` job name `gitleaks`, no `continue-on-error`, SARIF upload category `gitleaks`, no weekly schedule. `.gitleaks.toml` defaults-only. `zizmor.yml` workflow `paths` include `.github/workflows/**`, `.github/actions/**`, `.github/dependabot.yml`, `action.yml`, `action.yaml`; Actionlint via `rhysd/actionlint:1.7.12@sha256:b1934ee5…`; not a third PR-visible workflow. `codeql.yml` push/PR paths per brief 3.4; schedule/dispatch have no paths; `detect` + `analyze-js-typescript` / `analyze-rust`; draft skip; schedule/dispatch force both languages; `build-mode: none`. |
| 4 | Signed `desktop-build` only on `v*` tags and `workflow_dispatch` (no automatic signed `main` publish) | ✓ VERIFIED | `on.push.tags: ['v*']`, no `branches: [main]`. `publish-faynosync` if: dispatch+`inputs.publish` or `startsWith` tags `v`. `github-release` if: `startsWith(github.ref, 'refs/tags/v')` only. Matrix `windows-2022` / `macos-15` with D-17 comment. `fail-fast` true only for non-tag dispatch. Artifact `compression-level: 0`, `if-no-files-found: error`, retention 14 on `v*` else 3. Signed job `package-manager-cache: false`, `components: ""`, no rust-cache. FaynoSync `channel: "stable"`, NSIS exe+sig and `.app.tar.gz`+sig. `cancel-in-progress: false` on tauri/publish. |
| 5 | Dependabot lockstep/limits (BRIEF-5 / D-32) | ✓ VERIFIED | npm weekly Monday 06:00 Europe/Berlin, limit 5; `nestjs-major` before `npm-patch-minor`. cargo limit 3, glib ignore `>=0.19.0`. github-actions / docker / docker-compose monthly 06:30 / 06:45 / 07:00, limit 3, no `schedule.day`. Node ignore `>=25` on docker. Limits are not 0. Single `.github/dependabot.yml` version 2. |
| 6 | backend-image/deploy cleanup (BRIEF-6 / D-20–D-27) | ✓ VERIFIED | No `type=raw,value=latest`; `flavor: latest=false`; identity `sha-<7>` / `sha-<7>-worker`. No job `migrate`; `deploy.needs: [build]`. API Dockerfile CMD still `prisma migrate deploy && node dist/main.js`; worker CMD `node dist/worker.js`. One deploy job, `concurrency.group: coolify-nest-deploy`, `cancel-in-progress: false`, PREV_TAG capture, `/health/ready` poll, rollback trap. `vars.COOLIFY_IMAGE_DEPLOY` kill switch; no PR trigger. Path list dropped redundant Dockerfile/tax-schema; lockfile remains. Both Trivy HIGH/CRITICAL; `provenance: false` / `sbom: false`; SBOM + cosign. Bake aborted in YAML comment (A5); two build-push steps keep `DIGEST_API` / `DIGEST_WORKER`. |
| 7 | Checkout/digest/permissions hardening, step summaries, no new reporting SaaS (BRIEF-7–8) | ✓ VERIFIED | Every `actions/checkout` under owned 04.4 workflows has `persist-credentials: false`. `sync-labels.yml` omits it — D-36 / `.github/zizmor.yml` artipacked ignore; file left byte-unchanged. Postgres 16 and Redis 7 service images are `tag@sha256:…` with readable tag in comment. Aggregator / build / deploy / desktop-build write `$GITHUB_STEP_SUMMARY` without secrets or PR titles. desktop-web Vitest junit upload `if: failure()`, `retention-days: 5`. backend-test Jest `github-actions` reporter only; no junit.xml artifact. No `jest-junit` in package.json. Harden-Runner `egress-policy: audit` (not block). |
| 8 | Required check still named exactly `ci`; no `pull_request_target`; no `workflow_run` for desktop/backend deploy; SHA-pinned actions; `package-manager-cache: false` on signed desktop jobs | ✓ VERIFIED | `^  ci:` in `ci.yml`. Repo grep of `.github/workflows` for `pull_request_target` and `workflow_run`: empty. All `uses:` 40-char SHAs except local `./.github/actions/setup-pnpm-ci`. Signed job `package-manager-cache: false`; composite not referenced from `desktop-build.yml`. |
| 9 | Validation table from the brief (docs-only, draft, JS/TS, Rust, backend, workflow-YAML, `v*` tag, staging dispatch) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Table documented in `04.4-MANUAL-GITHUB.md`. D-34: not executed until push OK. `v*` / production dispatch forbidden this phase. |
| 10 | `uvx zizmor@1.30.0 .` exits 0; Wave 0 origin findings fixed, not carried as exceptions (D-34) | ✓ VERIFIED | Verifier ran `uvx zizmor@1.30.0 .` → exit 0, “No findings to report. Good job! (6 ignored, 35 suppressed)”. Actionlint digest image exit 0. |
| 11 | Founder checklist exists: gitleaks required check, Secret Scanning, quarterly service-digest bump, D-34 table, D-17 staging; Vorher/Nachher fan-out table (BRIEF deliverable 3) | ✓ VERIFIED | `04.4-MANUAL-GITHUB.md` lists D-09, D-12, GITLEAKS_LICENSE, D-17, D-36 quarterly re-pin, D-34 table, fan-out table. Execution of those steps is human (below). |
| 12 | Gitleaks fail-closed + SARIF (D-08, D-10, BRIEF-3.1) | ✓ VERIFIED | Job `name: gitleaks`. No `continue-on-error`. `security-events: write` on the job. `upload-sarif` category `gitleaks`. `fetch-depth: 0`. Concurrency `${{ github.workflow }}-${{ github.ref }}` cancel true. |
| 13 | GH Release tag-only MSI+DMG; FaynoSync updater artifacts stable-only (D-16, D-07) | ✓ VERIFIED | `softprops/action-gh-release@efb35369…`, `fail_on_unmatched_files: true`, `contents: write` on that job only. download-artifact SHA `3e5f45b2…`. FaynoSync JSON `channel: "stable"`. |
| 14 | No GitHub-hosted Prisma migrate job; API CMD is the only migrator (D-22) | ✓ VERIFIED | No `^  migrate:` in workflows. Dockerfile comment names API CMD as only migrator. CI `prisma migrate deploy` hits job service Postgres only. |
| 15 | `setup-pnpm-ci` pins pnpm/setup-node SHAs with `cache: pnpm`; signed desktop does not use it (D-04, D-30, BRIEF-2.4) | ✓ VERIFIED | Composite pins `pnpm/action-setup@0977fd99…` and `actions/setup-node@82076278…`. Used from `ci.yml` only. |
| 16 | desktop-rust has no pnpm/Node setup (D-30) | ✓ VERIFIED | `desktop-rust` is apt + `dtolnay/rust-toolchain` + `swatinem/rust-cache` + cargo fmt/clippy/test. No pnpm. |
| 17 | backend-test walltime vs origin split cannot be measured until GitHub (D-30 boundary) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Job consolidated. +25 percent split rule is policy, not a measured GitHub run. |

**Score:** 14/17 truths verified (3 present, behavior-unverified)

Roadmap SC 1–4 map to truths 1, 3–8. Roadmap SC 5 is truth 9 (human, not a code gap).

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | ----------- | ------ | ------- |
| `.github/workflows/ci.yml` | Aggregator `ci`, path/draft split, backend-test, summaries | ✓ VERIFIED | Exists, substantive, wired. `gsd-tools verify.artifacts` returned empty (PLAN artifacts are path strings, not `{path, provides}` objects) — verified by read/grep. |
| `.github/workflows/gitleaks.yml` | Fail-closed Gitleaks + SARIF | ✓ VERIFIED | Origin file edited, not recreated. |
| `.github/workflows/zizmor.yml` | Path-filtered zizmor + Actionlint digest | ✓ VERIFIED | Actionlint in this workflow. |
| `.github/workflows/codeql.yml` | detect + language-split + draft skip | ✓ VERIFIED | Schedule/dispatch force both languages. |
| `.github/actions/setup-pnpm-ci/action.yml` | Unsigned CI Node/pnpm cache | ✓ VERIFIED | SHA-pinned; not used by desktop-build. |
| `.gitleaks.toml` | Defaults-only, no fixture allowlist | ✓ VERIFIED | Comment-only config. |
| `.github/workflows/desktop-build.yml` | Tags + dispatch signed build | ✓ VERIFIED | No main-push trigger. |
| `.github/dependabot.yml` | Caps, monthly Actions/Docker, Nest major | ✓ VERIFIED | Single version-2 file. |
| `.github/workflows/backend-image.yml` | SHA tags, no migrate job, ordered deploy | ✓ VERIFIED | Bake abort documented in-file. |
| `apps/backend/Dockerfile` | API CMD only migrator | ✓ VERIFIED | Comment-only change; CMD strings unchanged. |
| `04.4-MANUAL-GITHUB.md` | Founder checklist + fan-out table | ✓ VERIFIED | D-09/D-12/D-17/D-34/D-36. |
| `.github/workflows/labeler.yml` / `sync-labels.yml` | Byte-unchanged (D-36) | ✓ VERIFIED | D-36 carve-out: sync-labels checkout still lacks `persist-credentials: false`. |

**Artifacts:** 12/12 verified (D-36 residual on sync-labels is required, not a stub)

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `ci.yml` `on.pull_request.types` | Heavy job `if:` | `ready_for_review` + `github.event.pull_request.draft` | ✓ WIRED | Types include `ready_for_review`. Heavy jobs skip when `draft == true`. |
| `dorny/paths-filter` | `desktop-web` / `desktop-rust` / `backend-test` / `squawk` | `needs.changes.outputs.*` | ✓ WIRED | Outputs declared; job `if:` consumes them plus `shared` / `tax-engine`. |
| Aggregator job `ci` | Child job ids | `needs: [changes, desktop-web, desktop-rust, backend-test, squawk]` + `check()` | ✓ WIRED | Job ids match `check()` names in the same file. |
| `gitleaks` job | Code scanning | `upload-sarif` category `gitleaks` | ✓ WIRED | `security-events: write` on that job. |
| Actionlint | `zizmor.yml` | docker digest step | ✓ WIRED | `rhysd/actionlint:1.7.12@sha256:b1934ee5…` |
| CodeQL `detect` | `analyze-js-typescript` / `analyze-rust` | outputs + `if:` | ✓ WIRED | Draft skip on analyze; schedule/dispatch both true. |
| `github-release` | `tauri` artifacts | `needs: tauri` + `startsWith` tags `v` | ✓ WIRED | MSI+DMG only. |
| `publish-faynosync` | FaynoSync | dispatch publish or `v*` tag | ✓ WIRED | No origin main-push publish branch. |
| `deploy` | `build` | `needs: [build]` | ✓ WIRED | Health URL `https://clared-api.puzzlessdev.online/health/ready`. |
| Aggregator / deploy / desktop-build | `$GITHUB_STEP_SUMMARY` | `echo … >> "$GITHUB_STEP_SUMMARY"` | ✓ WIRED | Areas/results/tags; no `secrets.*` interpolated into the summary. |
| desktop-web | junit.xml | `if: failure()` upload | ✓ WIRED | backend-test has no junit.xml artifact. |
| Harden-Runner | signing/deploy/ci jobs | `egress-policy: audit` | ✓ WIRED | Analysis workflows omit it (REVIEW IN-02, advisory). |

**Wiring:** 12/12 key links wired

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `ci.yml` changes | `desktop-web` / `desktop-rust` / `backend` / … | `dorny/paths-filter` git diff | Yes | ✓ FLOWING |
| aggregator summary | `AREA_*` / `RES_*` | `needs.*.outputs` / `needs.*.result` | Yes (GitHub job results) | ✓ FLOWING |
| `backend-image` tags | `sha_tag` | `GITHUB_SHA::7` | Yes | ✓ FLOWING |
| deploy | `PREV_API` / `PREV_WORKER` | Coolify GET `docker_registry_image_tag` | Yes when job runs | ✓ FLOWING |
| desktop-build TARGET | staging/production | dispatch input or `v*` tag | Yes | ✓ FLOWING |

No hardcoded empty message lists or static API stubs. WR-03 leftover `type=ref,event=branch` can still emit a branch-named GHCR tag on non-main dispatch (advisory; does not restore `type=raw,value=latest`).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| zizmor 1.30.0 clean | `uvx zizmor@1.30.0 .` | exit 0, 6 ignored / 35 suppressed | ✓ PASS |
| actionlint | `docker run … rhysd/actionlint:1.7.12@sha256:b1934ee5…` | exit 0 | ✓ PASS |
| SHA-pinned `uses:` | grep `uses:` minus local composite minus `@[0-9a-f]{40}` | empty | ✓ PASS |
| no `pull_request_target` / `workflow_run` | grep `.github/workflows` | empty | ✓ PASS |
| aggregator job id `ci` | `grep -E '^  ci:' .github/workflows/ci.yml` | match line 285 | ✓ PASS |
| no main-OR on heavy jobs | grep `github.ref == 'refs/heads/main' \|\|` in `ci.yml` | empty | ✓ PASS |
| no `migrate` / `backend-unit` / `backend-e2e` jobs | grep job ids | empty | ✓ PASS |
| signed cache off | `package-manager-cache: false` in `desktop-build.yml` | match | ✓ PASS |

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| — | — | No `scripts/**/tests/probe-*.sh`; PLAN/SUMMARY do not declare probe scripts. zizmor/actionlint ran as spot-checks. | SKIPPED |

### Requirements Coverage

BRIEF-* live in `docs/claredtool-github-actions-optimierungsauftrag.md`, not `.planning/REQUIREMENTS.md`. That is not a blocker (planning note). CI-OPT 01–06 shipped in quicks `260828-5c8` / `260828-mn3`; this phase is the remainder. No REQUIREMENTS.md row maps to Phase 04.4 — no orphaned REQUIREMENTS.md IDs.

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| BRIEF-1.1–1.4 | 01 | Path-on-main, draft, concurrency, run-name | ✓ SATISFIED | `ci.yml` / analysis `concurrency` / `run-name` without PR title |
| D-01, D-28, D-29, D-31 | 01 | Aggregator name, no workflow paths, draft types, concurrency | ✓ SATISFIED | YAML; aggregator live path is PBU (truth 2) |
| BRIEF-2.1–2.4 / D-30 | 02 | desktop-web/rust split, backend-test, filtered pnpm | ✓ SATISFIED | YAML; walltime bound is PBU (truth 17) |
| BRIEF-3.1–3.4 / D-08, D-10, D-11, D-13 | 03 | Gitleaks SARIF, Actionlint-in-zizmor, CodeQL split | ✓ SATISFIED | workflows + `.gitleaks.toml` |
| D-09, D-12 | 03 / 07 | gitleaks required check; Secret Scanning | ? NEEDS HUMAN | Documented in MANUAL; D-06 no API |
| BRIEF-4 / D-04, D-07, D-14–D-19 | 04 | Signed tags+dispatch, GH Release, runner pins | ✓ SATISFIED | `desktop-build.yml`; D-17 live staging is human |
| BRIEF-5 / D-32 | 05 | Dependabot caps/cadence/Nest group | ✓ SATISFIED | `dependabot.yml` |
| BRIEF-6 / D-20–D-27 | 06 | latest drop, migrate gone, deploy collapse | ✓ SATISFIED | `backend-image.yml` + Dockerfile comment |
| BRIEF-7–8 / D-02, D-03, D-05, D-06, D-33–D-36 | 07 | pins, summaries, JUnit, founder checklist | ✓ SATISFIED | YAML + MANUAL; D-34 table is PBU |
| CI-OPT remainder | ROADMAP | SSOT is the brief | ✓ SATISFIED | Remainder implemented; 01–06 prior quicks |
| D-17 | 04 / 07 | Staging dispatch both runners after merge | ? NEEDS HUMAN | Labels pinned in YAML; live proof after merge |
| D-34 | 07 | GitHub scenario table after push OK | ? NEEDS HUMAN | MANUAL table; not executed |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `.github/workflows/backend-image.yml` | 325–330 | `case … *running*` treats `running: unhealthy` as success (REVIEW WR-01) | ⚠️ Warning | Worker poll can skip rollback. Does not remove the poll or the migrate prohibition. Not a must_have contradiction. |
| `.github/workflows/backend-image.yml` | 300–313 | `/health/ready` 200 may be previous revision (REVIEW WR-02) | ⚠️ Warning | API overlap can look healthy. Poll exists (D-25). |
| `.github/workflows/backend-image.yml` | 82–84 | `type=ref,event=branch` leftover (REVIEW WR-03) | ⚠️ Warning | Dispatch from a branch named `latest` could publish a `:latest`-shaped tag. `type=raw,value=latest` is gone; `flavor: latest=false` remains. |
| `.github/workflows/desktop-build.yml` | 274, 321 | FaynoSync HTTP ≥400 + body `already exists` (REVIEW WR-04) | ⚠️ Warning | Duplicate handling is substring, not 409. Upload path exists. |
| `.github/workflows/desktop-build.yml` | 69, 198 | `${{ }}` inside `run:` (REVIEW IN-01) | ℹ️ Info | Not PR-triggered; ternary is staging/production only. |
| `gitleaks.yml` / `zizmor.yml` / `codeql.yml` | — | No Harden-Runner (REVIEW IN-02) | ℹ️ Info | No extra secrets on those jobs. |
| `backend-image.yml` | 172–174 | cosign-release `v3.0.6` blob (REVIEW IN-03) | ℹ️ Info | Installer action is SHA-pinned. |
| `codeql.yml` | 18 vs 74–78 | `rust-toolchain.toml` starts workflow, not rust detect (REVIEW IN-04) | ℹ️ Info | Analyze jobs skip when both outputs false. |
| `desktop-build.yml` | 273, 320 | FaynoSync body echoed (REVIEW IN-05) | ℹ️ Info | Token is Authorization header. |
| `.github/workflows/backend-image.yml` | 11–12 | `TECH-DEBT` provenance/sbom Coolify | ℹ️ Info | Named follow-up, not `TBD`/`FIXME`/`XXX`. |

No `TBD` / `FIXME` / `XXX` in phase-owned workflow files. Review status `issues` (4 warnings) — advisory; none flipped a must_have to FAILED.

### Test Quality Audit

N/A — phase ships GitHub Actions YAML, not requirement-linked app tests. No skipped `it.skip` / `test.skip` proving a BRIEF/D-* ID. Spot-checks are linters (actionlint, zizmor), not circular oracles.

**Disabled tests on requirements:** 0
**Circular patterns detected:** 0
**Insufficient assertions:** 0

### Decision Coverage

All trackable CONTEXT.md decisions are honored by shipped artifacts. `honored: 36 / total: 36`. Gate is non-blocking.

### Human Verification Required

Infrastructure/CI phase — no end-user UI. Founder GitHub settings and the D-34 event matrix are in the roadmap success criteria and PLAN 07 `<human-check>`. Those items keep status `human_needed`.

### 1. D-34 GitHub scenario table (after explicit push OK)

**Test:** User says push OK. Then run the table in `04.4-MANUAL-GITHUB.md` (docs-only PR, draft, ready_for_review JS/TS, Rust-only, backend-only, workflow-YAML, docs-only `main`). No production Coolify dispatch. No `v*` tag.
**Expected:** Nachher column: `changes` + aggregator `ci` always; Gitleaks; heavy jobs skip on draft/docs; path-selective desktop/backend; CodeQL language-split; no signed desktop on `main`. Also observe aggregator `ci` green when children are `skipped`, red when a child is `failure`/`cancelled`. After a backend-test run, compare walltime to origin split (+25 percent rule).
**Why human:** GitHub-hosted events. D-34 forbids executing this during implementation.

### 2. D-09 required check `gitleaks` (after merge)

**Test:** Branch Protection on `main`: add required check named exactly `gitleaks`. Keep `ci`. Do not require CodeQL or Zizmor. No admin bypass.
**Expected:** Failed gitleaks blocks merge. Path-skipped CodeQL/Zizmor do not pend-block.
**Why human:** D-06 — no Branch Protection API.

### 3. D-12 Secret Scanning + Push Protection (after merge)

**Test:** Enable Secret Scanning and Push Protection. If `clezcoding` is an org, set `GITLEAKS_LICENSE`.
**Expected:** Both GitHub features on. Gitleaks remains fail-closed.
**Why human:** D-06 — no Secret Scanning API.

### 4. D-17 staging desktop dispatch (after merge)

**Test:** `workflow_dispatch` target staging, `publish` as needed, on **both** `windows-2022` and `macos-15`. Production dispatch and `v*` stay off.
**Expected:** Both pinned generations complete. Change labels only if the run proves them wrong.
**Why human:** D-34 / D-17 — runner proof is post-merge, not this PR.

### Gaps Summary

**No code gaps blocking the phase goal.** YAML implements brief remainder Phases 1–8. Aggregator name, draft/path split, security workflows, signed-desktop trigger cut, Dependabot caps, deploy cleanup, SHA pins, and zizmor/actionlint are in the tree.

Remaining work is founder GitHub (D-09, D-12, D-17) and the D-34 scenario table after **push OK**. Review WR-01–WR-04 stay advisory.

### Prohibitions (negative checks)

Grep/read evidence, not GitHub UI:

- No `pull_request_target` in `.github/workflows`
- No `workflow_run` in `.github/workflows`
- No workflow-level `paths` on `ci.yml`
- No GitHub-hosted `migrate` job
- No `jest-junit` lockfile package / no new reporting SaaS
- No Harden-Runner `egress-policy: block`
- `labeler.yml` / `sync-labels.yml` not behavior-changed (D-36)
- Step summaries do not echo `secrets.COOLIFY_TOKEN` / `FAYNOSYNC_CI_TOKEN` / `TAURI_SIGNING_PRIVATE_KEY`
- Verifier did not push, create a `v*` tag, or dispatch production

---

_Verified: 2026-09-04T05:20:00Z_
_Verifier: Claude (gsd-verifier)_
