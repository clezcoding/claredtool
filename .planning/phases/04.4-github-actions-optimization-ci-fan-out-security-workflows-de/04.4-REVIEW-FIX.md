---
phase: 04.4-github-actions-optimization-ci-fan-out-security-workflows-de
fixed_at: 2026-09-04T05:44:00Z
review_path: .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-REVIEW.md
iteration: 1
findings_in_scope: 10
fixed: 10
skipped: 0
status: all_fixed
verification_environment: isolated worktree (`.claude/worktrees/rf-04.4-*`); YAML `safe_load` only — no full CI gates
---

# Phase 04.4: Code Review Fix Report

**Fixed at:** 2026-09-04T05:44:00Z
**Source review:** `.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-REVIEW.md`
**Iteration:** 1

**Summary:**
- Findings in scope: 10
- Fixed: 10
- Skipped: 0

## Fixed Issues

### WR-01: Worker Coolify status glob treats `running: unhealthy` as success

**Files modified:** `.github/workflows/backend-image.yml`
**Commit:** `77b50d9`
**Status:** fixed: requires human verification
**Applied fix:** Worker poll matches only `running` or `running:healthy` after normalizing case and stripping spaces.

### WR-02: API `/health/ready` poll can succeed on the previous revision

**Files modified:** `.github/workflows/backend-image.yml`
**Commit:** `da87da9`
**Status:** fixed: requires human verification
**Applied fix:** API poll requires Coolify `docker_registry_image_tag` equals `SHA_TAG` and `/health/ready` HTTP 200.

### WR-03: Leftover `type=ref,event=branch` can publish a `:latest`-shaped GHCR tag

**Files modified:** `.github/workflows/backend-image.yml`
**Commit:** `b4743bd`
**Status:** fixed
**Applied fix:** Removed `type=ref,event=branch` metadata tag line; only `type=sha,prefix=sha-,format=short` remains.

### WR-04: FaynoSync upload treats HTTP ≥400 as success if the body contains `already exists`

**Files modified:** `.github/workflows/desktop-build.yml`
**Commit:** `2cac3c4`
**Status:** fixed: requires human verification
**Applied fix:** macOS and Windows uploads accept success on HTTP &lt; 400 or HTTP 409 with `already exists` body; otherwise fail.

### WR-05: Deploy rollback swallows `coolify_pin` failures

**Files modified:** `.github/workflows/backend-image.yml`
**Commit:** `fff1f21`
**Status:** fixed: requires human verification
**Applied fix:** Removed `|| true` on rollback pins; per-app rollback status written to step summary; job exits 1 on rollback failure.

### IN-01: `${{ }}` still interpolated inside `run:` on signed desktop

**Files modified:** `.github/workflows/desktop-build.yml`
**Commit:** `2ffc3df`
**Status:** fixed
**Applied fix:** Moved target ternaries to `env:` (`TARGET`, `FS_ENV`) and referenced shell variables in scripts.

### IN-02: Analysis workflows omit Harden-Runner

**Files modified:** `.github/workflows/gitleaks.yml`, `.github/workflows/zizmor.yml`, `.github/workflows/codeql.yml`
**Commit:** `d22fef3`
**Status:** fixed
**Applied fix:** Added SHA-pinned `step-security/harden-runner` with `egress-policy: audit` as first step on gitleaks, zizmor, and all three CodeQL jobs.

### IN-03: `cosign-installer` downloads Cosign `v3.0.6` at runtime

**Files modified:** `.github/workflows/backend-image.yml`
**Commit:** `065c8fb`
**Status:** fixed
**Applied fix:** Added `ponytail:` comment documenting that `cosign-installer` has no checksum input; the SHA-pinned action verifies `v3.0.6` internally via `bootstrap_linux_amd64_sha`. No separate checksum pin supported by the action inputs.

### IN-04: `rust-toolchain.toml` triggers CodeQL but does not enable Rust analyze

**Files modified:** `.github/workflows/codeql.yml`
**Commit:** `d22fef3`
**Status:** fixed
**Applied fix:** Added `rust-toolchain.toml` to the `rust` paths-filter block (committed with IN-02 because both touch `codeql.yml`).

### IN-05: FaynoSync response bodies are echoed to workflow logs

**Files modified:** `.github/workflows/desktop-build.yml`
**Commit:** `9bff66a`
**Status:** fixed
**Applied fix:** Replaced raw `cat` with HTTP code line plus `jq -c '{version, id}'` projection for macOS and Windows uploads.

---

_Fixed: 2026-09-04T05:44:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
