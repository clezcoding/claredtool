# Phase 04.4 — Post-merge addendum (PR #115)

**Recorded:** 2026-09-05  
**Purpose:** Live facts on `origin/main` after PR #115 squash merge. Per D-16 — do not edit immutable `04.4-*-PLAN.md` / `04.4-*-SUMMARY.md` bodies.

## Live workflow facts

| Area | Planned / historical claim | Live state post-PR #115 | Evidence |
|------|---------------------------|-------------------------|----------|
| Backend CI jobs | `backend-test` (consolidated) | **`backend-unit`** + **`backend-e2e`** (split jobs; both wired into aggregator `ci`) | `.github/workflows/ci.yml` jobs `backend-unit`, `backend-e2e` |
| Desktop signing matrix | `windows-2022` + `macos-15` (D-34 staging evidence) | **`windows-2025`** + **`macos-26`** (pinned runner labels) | `.github/workflows/desktop-build.yml` matrix `os: [windows-2025, macos-26]` |
| Gitleaks | Fail-closed merge gate | Job name **`gitleaks`**; full-history scan; explicit **`.gitleaks.toml`**; SARIF upload retained | `.github/workflows/gitleaks.yml`, `.gitleaks.toml` |
| Backend deploy | Coolify pin + health | Waits on Coolify **deployment UUID**, tag/status poll, **`trap ERR`** rollback path | `.github/workflows/backend-image.yml` deploy job |
| Worker health | — | `running:unknown` accepted only when healthcheck disabled | `backend-image.yml` deploy logic |
| CodeQL Rust | — | Detects **`**/rust-toolchain.toml`** for language routing | `.github/workflows/codeql.yml` |
| Squawk | — | Dedicated **DB path filter** (not full-repo) | `.github/workflows/ci.yml` `squawk` job paths |
| Dependabot | — | **Docker + Compose** minor/patch grouped | `.github/dependabot.yml` |
| Labeler / sync-labels | — | **Unchanged** by PR #115 | No diff on `labeler.yml` / `sync-labels.yml` in #115 |

## Founder manual tasks (human-only)

GitHub Settings — Cursor must not configure via API (D-06). See also `04.4-MANUAL-GITHUB.md`.

| Task | Requirement | Expected outcome |
|------|-------------|------------------|
| Required checks | Add status check named exactly **`gitleaks`** on `main`; keep **`ci`** | Merge blocked on gitleaks failure; path-skipped CodeQL/Zizmor do not pend-block |
| Secret Scanning | Enable on public repo | Complements Gitleaks; does not replace fail-closed scan |
| Push Protection | Enable on public repo | Blocks secret pushes at origin |
| `GITLEAKS_LICENSE` | If `clezcoding` is an org | Free Starter license for `gitleaks-action` on org repos |

## Immutable history

- Phase **04.4 stays complete** — no re-execution of executed plans.
- Drift in `04.4-VERIFICATION.md` / `ROADMAP.md` success criteria is corrected in **active guidance** (`ROADMAP.md` Phase 04.4 item 1) and this addendum — not by rewriting plan execution logs.
