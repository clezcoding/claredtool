---
phase: 04.4-github-actions-optimization-ci-fan-out-security-workflows-de
plan: 02
subsystem: infra
tags: [github-actions, ci, path-filter, pnpm, rust-cache, composite-action]

requires:
  - phase: 04.4-01
    provides: "ci.yml draft-skip + path-on-main; aggregator job still named ci; origin job names lint/desktop-test/backend-unit/backend-e2e"
provides:
  - "dorny filters desktop-web and desktop-rust; desktop-web does not use apps/desktop/**"
  - "Jobs desktop-web, desktop-rust, backend-test; aggregator ci needs them plus changes and squawk"
  - "Filtered frozen pnpm installs; local setup-pnpm-ci composite for unsigned CI twins"
affects: [04.4-03, 04.4-07, branch-protection-ci]

actuals:
  tokens: 1295
  tasks: 3
  commits: 4

tech-stack:
  added:
    - ".github/actions/setup-pnpm-ci composite (pnpm/action-setup + setup-node cache pnpm)"
  patterns:
    - "desktop-web vs desktop-rust path filters; never apps/desktop/** as the web glob"
    - "Unsigned CI Node/pnpm via local composite; filtered install stays in the caller"
    - "One backend-test job: unit then e2e on origin Postgres 16 / Redis 7"

key-files:
  created:
    - .github/actions/setup-pnpm-ci/action.yml
  modified:
    - .github/workflows/ci.yml

key-decisions:
  - "Create setup-pnpm-ci because three unsigned twins remain (desktop-web, backend-test, squawk)"
  - "desktop-rust has no pnpm/Node; build.rs is serde_json only"
  - "backend-test timeout-minutes 30 so sequential unit+e2e cannot inherit the old 15/20 split budgets"
  - "Postgres 16 / Redis 7 tags kept; digest pin waits for Plan 07"

patterns-established:
  - "Pattern: path-selective desktop-web (src/vite/vitest/ui) vs desktop-rust (src-tauri)"
  - "Pattern: uses: ./.github/actions/setup-pnpm-ci after checkout; signed desktop-build must not call it"

requirements-completed:
  - BRIEF-2.1
  - BRIEF-2.2
  - BRIEF-2.3
  - BRIEF-2.4
  - D-30
  - D-33

coverage:
  - id: D1
    description: dorny outputs desktop-web and desktop-rust exist; desktop-web filter list does not include apps/desktop/**
    requirement: BRIEF-2.1
    verification:
      - kind: other
        ref: "python3 yaml assert desktop-web glob region has no apps/desktop/**; grep '^  desktop-web:' and '^  desktop-rust:'"
        status: pass
    human_judgment: false
  - id: D2
    description: Jobs desktop-web and desktop-rust exist; aggregator job ci needs them plus changes, backend-test, and squawk
    requirement: D-01
    verification:
      - kind: other
        ref: "python3 yaml jobs['ci']['needs'] == changes, desktop-web, desktop-rust, backend-test, squawk"
        status: pass
    human_judgment: false
  - id: D3
    description: backend-unit and backend-e2e gone; one backend-test runs unit then e2e with origin Postgres 16 and Redis 7; squawk stays separate
    requirement: BRIEF-2.2
    verification:
      - kind: other
        ref: "grep '^  backend-test:' and '^  squawk:'; grep job ids backend-unit/backend-e2e count 0; services postgres:16-alpine redis:7-alpine"
        status: pass
    human_judgment: false
  - id: D4
    description: desktop-web and backend-test use pnpm install --frozen-lockfile with RESEARCH filter flags; desktop-rust has no pnpm/Node
    requirement: BRIEF-2.3
    verification:
      - kind: other
        ref: "grep frozen-lockfile --filter ./apps/desktop... and --filter @clared/tax-engine...; desktop-rust block has no pnpm/setup-node"
        status: pass
    human_judgment: false
  - id: D5
    description: setup-pnpm-ci pins pnpm/action-setup and setup-node SHAs with cache pnpm; desktop-build.yml does not reference the composite and keeps package-manager-cache false
    requirement: BRIEF-2.4
    verification:
      - kind: other
        ref: "grep SHAs in action.yml; grep setup-pnpm-ci in ci.yml (3 calls); ! grep setup-pnpm-ci desktop-build.yml; grep package-manager-cache: false"
        status: pass
    human_judgment: false
  - id: D6
    description: Actionlint 1.7.12 on ci.yml exits 0; full-tree still SC2129 on origin desktop-build.yml
    requirement: D-34
    verification:
      - kind: other
        ref: "docker run rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667 .github/workflows/ci.yml (exit 0)"
        status: pass
    human_judgment: false

duration: 5min
completed: 2026-09-04
status: complete
---

# Phase 04.4 Plan 02: CI job split and filtered installs Summary

**Path-selective desktop-web/desktop-rust jobs, one backend-test with filtered frozen pnpm, and a local setup-pnpm-ci composite for unsigned CI**

## Performance

- **Duration:** 5 min
- **Started:** 2026-09-04T03:56:07Z
- **Completed:** 2026-09-04T04:01:08Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments

- Split `desktop` into `desktop-web` (src/vite/vitest/`packages/ui`, filtered `pnpm install --filter ./apps/desktop...`) and `desktop-rust` (src-tauri cargo fmt/clippy/test, rust-cache, no Node).
- Replaced `backend-unit` + `backend-e2e` with `backend-test` (unit then e2e) on origin Postgres 16 and Redis 7. `squawk` stays its own job with `squawk-cli@2.64.0` global install.
- Extracted `.github/actions/setup-pnpm-ci` (SHA-pinned pnpm + Node 24 `cache: pnpm`) for the three unsigned twins. Signed `desktop-build.yml` does not use it.
- Aggregator job id remains `ci`; `needs`/`check()` updated in the same edits as the job ids.

## Task Commits

Each task was committed atomically:

1. **Task 1: Split path filters and desktop jobs** - `08cb492` (feat)
2. **Task 2: Consolidate backend-test keep squawk** - `d7e7d66` (feat)
3. **Task 3: Extract setup-pnpm-ci if twins remain** - `34ecaae` (feat)

**Plan metadata:** (this commit)

## Files Created/Modified

- `.github/workflows/ci.yml` — desktop-web/desktop-rust filters and jobs; backend-test; filtered installs; composite callers; aggregator `ci`
- `.github/actions/setup-pnpm-ci/action.yml` — unsigned CI Node 24 + pnpm cache composite

## Decisions Made

- Created the composite (3 identical unsigned setups ≥ 2).
- `desktop-rust` skips pnpm/Node; `build.rs` only reads `tauri.conf.json` via `serde_json`.
- `backend-test` `timeout-minutes: 30` so sequential unit+e2e is not capped at the old 15/20 split.
- Left Postgres/Redis on tags `postgres:16-alpine` / `redis:7-alpine` (digest pin is Plan 07).
- Did not add `apps/desktop/**` to the web filter; did not point `desktop-build.yml` at the composite.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] backend-test timeout 30 not 15/20**
- **Found during:** Task 2
- **Issue:** Plan said keep timeout; origin unit was 15 and e2e 20. One sequential job can exceed either budget.
- **Fix:** `timeout-minutes: 30` on `backend-test`. Service names `postgres`/`redis` unchanged.
- **Files modified:** `.github/workflows/ci.yml`
- **Verification:** YAML parse; job id `backend-test` present
- **Committed in:** `d7e7d66`

---

**Total deviations:** 1 auto-fixed (missing critical timeout).
**Impact on plan:** Prevents a false timeout fail after consolidating. No scope creep. D-33 still three task commits plus this docs commit (same as Plan 01); the plan output line “Single commit” would have violated the executor per-task protocol.

## Issues Encountered

- Full-tree Actionlint 1.7.12 still exits 1 on origin `desktop-build.yml` SC2129. `ci.yml` Actionlint exits 0. `-ignore 'SC2129'` makes the tree exit 0. Out of this plan (do not edit `desktop-build.yml`).

## Authentication Gates

None.

## Known Stubs

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Ready for `04.4-03-PLAN.md` (Gitleaks SARIF, Actionlint in zizmor, CodeQL). Do not push until user OK (D-34). Do not treat origin SC2129 as a Plan 02 failure.

---
*Phase: 04.4-github-actions-optimization-ci-fan-out-security-workflows-de*
*Completed: 2026-09-04*

## Self-Check: PASSED

- FOUND: `.github/workflows/ci.yml`
- FOUND: `.github/actions/setup-pnpm-ci/action.yml`
- FOUND: `08cb492`, `d7e7d66`, `34ecaae`
