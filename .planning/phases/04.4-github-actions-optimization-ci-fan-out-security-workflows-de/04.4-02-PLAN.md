---
phase: 04.4-github-actions-optimization-ci-fan-out-security-workflows-de
plan: 02
type: execute
wave: 2
depends_on:
  - 04.4-01
files_modified:
  - .github/workflows/ci.yml
  - .github/actions/setup-pnpm-ci/action.yml
autonomous: true
requirements:
  - BRIEF-2.1
  - BRIEF-2.2
  - BRIEF-2.3
  - BRIEF-2.4
  - D-30
  - D-33
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "dorny/paths-filter outputs desktop-web and desktop-rust exist; desktop-web filter lists do not include apps/desktop/** (D-30, BRIEF-2.1)."
    - "Jobs named desktop-web and desktop-rust exist; aggregator job ci needs them plus changes, backend-test, and squawk (D-01, D-30)."
    - "backend-unit and backend-e2e job ids are gone; one backend-test job runs unit then e2e with origin Postgres 16 and Redis 7 services; squawk stays its own job (D-30, BRIEF-2.2)."
    - "desktop-web and backend-test use pnpm install --frozen-lockfile with the RESEARCH filter flags (BRIEF-2.3)."
    - "desktop-rust has no pnpm/Node setup unless a local cargo test proves build.rs needs Node (D-30; build.rs is serde_json only)."
    - "If setup-pnpm-ci exists, it pins pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 and actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 with cache pnpm; desktop-build.yml does not reference that composite (D-04, D-30, BRIEF-2.4)."
    - statement: "backend-test walltime vs the slower of origin backend-unit/backend-e2e cannot be measured until GitHub; consolidate now; split again only if a later measured run exceeds +25 percent (D-30 boundary)."
      verification: backstop
  artifacts:
    - .github/workflows/ci.yml
    - .github/actions/setup-pnpm-ci/action.yml
  key_links:
    - "needs.changes.outputs.desktop-web and desktop-rust drive job if: together with draft skip from Plan 01"
    - "aggregator check() names match the new job ids in the same commit"
    - "squawk job id unchanged (origin G5)"
  prohibitions:
    - statement: "Do not use apps/desktop/** as the desktop-web filter (D-30)."
      status: unverified
    - statement: "Do not point signed desktop-build.yml at setup-pnpm-ci (D-04)."
      status: unverified
    - statement: "Do not add squawk-cli to the lockfile; keep npm install -g squawk-cli@2.64.0 (D-30)."
      status: unverified
    - statement: "Do not add the privileged PR-from-base workflow trigger (D-02)."
      status: unverified
    - statement: "Do not trigger production deploy or a v* tag (D-34)."
      status: unverified
    - statement: "Do not add workflow-level paths on ci.yml (D-01)."
      status: unverified
    - statement: "Do not change GitHub settings via API (D-06)."
      status: unverified
    - statement: "Do not add a new reporting SaaS (D-35)."
      status: unverified
---

<objective>
D-33 logical commit 2: CI job split and filtered installs (BRIEF-2.1–2.4, D-30).

Purpose: Stop Rust-only desktop diffs from starting the web Vitest/tsc job; stop dual backend jobs unless measured walltime forces a split; install only the workspace slices each job needs.

Output: desktop-web / desktop-rust / backend-test on ci.yml; optional local composite; aggregator check() updated in the same commit.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-RESEARCH.md
@.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-PATTERNS.md
@.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-01-PLAN.md
@apps/backend/Dockerfile
@apps/desktop/src-tauri/build.rs
</context>

<tasks>

<task type="auto">
  <name>Split path filters and desktop jobs</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-PATTERNS.md (Core path-filter pattern, Target filters, Rust CI cache, Aggregator pattern)
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-RESEARCH.md (desktop-web vs desktop-rust filters, Filtered pnpm)
    - apps/desktop/src-tauri/build.rs
  </read_first>
  <action>
    In the changes job dorny/paths-filter filters block (SHA ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d), replace the desktop: list that uses apps/desktop/** with two filters copied from PATTERNS Target filters: desktop-web (src, index.html, package.json, vite/vitest/tsconfig files, scripts, packages/ui) and desktop-rust (src-tauri/** and rust-toolchain.toml). Keep backend, tax-engine, shared as origin. Do not put apps/desktop/** on desktop-web (D-30, BRIEF-2.1).

    Rename origin lint + desktop-test into jobs desktop-web and desktop-rust:
    - desktop-web: ubuntu-latest; Harden-Runner e14015d583714f6e62063499dc959a02595150a1 egress-policy audit; checkout SHA 3d3c42e5aac5ba805825da76410c181273ba90b1; Node 24 via setup-node 820762786026740c76f36085b0efc47a31fe5020 with cache pnpm; pnpm/action-setup 0977fd99725f1db4007ccb2928dbb4e90d06cc86; install with pnpm install --frozen-lockfile --filter ./apps/desktop... then the origin lint/typecheck/vitest/build commands that lived on lint/desktop-test web half (BRIEF-2.3). if: draft skip from Plan 01 AND (outputs.desktop-web or outputs.shared).
    - desktop-rust: ubuntu-24.04 with origin Tauri Linux apt deps; dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 toolchain 1.97.1 components rustfmt, clippy; swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 workspaces apps/desktop/src-tauri; cargo fmt/clippy/test --locked --manifest-path apps/desktop/src-tauri/Cargo.toml. No pnpm and no setup-node. if: draft skip AND (outputs.desktop-rust or outputs.shared).

    Update aggregator job ci needs and check() in this same edit so names cannot drift (Pitfall 3). Keep squawk in needs. Job id ci unchanged (D-01).
  </action>
  <acceptance_criteria>
    - grep -E '^  desktop-web:' and '^  desktop-rust:' both match in ci.yml
    - grep -E '^  lint:' and '^  desktop-test:' do not match as job ids
    - The desktop-web filter list does not contain the glob apps/desktop/**
    - Aggregator needs includes desktop-web, desktop-rust, and squawk
  </acceptance_criteria>
  <verify>
    <automated>grep -E '^  desktop-web:' .github/workflows/ci.yml && grep -E '^  desktop-rust:' .github/workflows/ci.yml && test "$(grep -E '^  lint:' .github/workflows/ci.yml | wc -l | tr -d ' ')" -eq 0 && test "$(grep -E '^  desktop-test:' .github/workflows/ci.yml | wc -l | tr -d ' ')" -eq 0 && python3 -c "
import yaml
d=yaml.safe_load(open('.github/workflows/ci.yml'))
# dorny filters are a YAML string; assert desktop-web glob is not the whole desktop tree
text=open('.github/workflows/ci.yml').read().split('desktop-web:')[1].split('desktop-rust:')[0]
assert \"apps/desktop/**\" not in text
needs=d['jobs']['ci']['needs']
assert 'desktop-web' in needs and 'desktop-rust' in needs and 'squawk' in needs
print('SPLIT_OK')
"</automated>
  </verify>
  <done>desktop-web vs desktop-rust filters and jobs exist; aggregator lists them (BRIEF-2.1, D-30).</done>
</task>

<task type="auto">
  <name>Consolidate backend-test keep squawk</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-PATTERNS.md (Services analog, Filtered pnpm)
    - apps/backend/Dockerfile
    - apps/backend/package.json
  </read_first>
  <action>
    Replace origin jobs backend-unit and backend-e2e with one job id backend-test (D-30, BRIEF-2.2). Default is consolidate (walltime +25 percent is a later GitHub measurement; do not keep the split speculatively). Copy origin services postgres:16-alpine and redis:7-alpine with the same POSTGRES_* env, ports, and health options. Do not bump to compose 18/8. Digest pin is Plan 07.

    Steps: Harden-Runner audit SHA as origin; checkout; pnpm + Node 24 cache; pnpm install --frozen-lockfile --filter @clared/tax-engine... --filter ./apps/backend... (Dockerfile analog); then origin unit test command then origin e2e command in that job. if: draft skip AND (outputs.backend or outputs.tax-engine or outputs.shared).

    Keep squawk as a separate job with origin squawk-cli@2.64.0 global install; do not add it to the lockfile. Keep persist-credentials false on squawk checkout (already origin).

    Update aggregator needs and check() to backend-test (drop backend-unit and backend-e2e). Keep timeout and postgres/redis service names.
  </action>
  <acceptance_criteria>
    - grep -E '^  backend-test:' matches
    - grep -E '^  backend-unit:' and '^  backend-e2e:' do not match as job ids
    - grep -E '^  squawk:' matches
    - ci.yml contains --filter @clared/tax-engine... and --filter ./apps/backend... and --filter ./apps/desktop...
  </acceptance_criteria>
  <verify>
    <automated>grep -E '^  backend-test:' .github/workflows/ci.yml && grep -E '^  squawk:' .github/workflows/ci.yml && test "$(grep -E '^  backend-unit:' .github/workflows/ci.yml | wc -l | tr -d ' ')" -eq 0 && test "$(grep -E '^  backend-e2e:' .github/workflows/ci.yml | wc -l | tr -d ' ')" -eq 0 && grep -F 'pnpm install --frozen-lockfile --filter @clared/tax-engine...' .github/workflows/ci.yml && grep -F 'pnpm install --frozen-lockfile --filter ./apps/desktop...' .github/workflows/ci.yml</automated>
  </verify>
  <done>One backend-test job; squawk separate; frozen filtered installs (BRIEF-2.2, BRIEF-2.3).</done>
</task>

<task type="auto">
  <name>Extract setup-pnpm-ci if twins remain</name>
  <files>.github/actions/setup-pnpm-ci/action.yml, .github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-PATTERNS.md (setup-pnpm-ci composite skeleton)
    - .github/workflows/desktop-build.yml
  </read_first>
  <action>
    Count remaining unsigned CI jobs that share identical Node 24 + pnpm/action-setup + setup-node cache pnpm (expect desktop-web, backend-test, squawk ≥2) (D-30, BRIEF-2.4). If ≥2, create .github/actions/setup-pnpm-ci/action.yml as a composite: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 then actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 with node-version 24 and cache pnpm. No free-form inputs executed as shell. Point those unsigned jobs at uses: ./.github/actions/setup-pnpm-ci. Filtered pnpm install stays in each calling job.

    If fewer than 2 twins remain, do not create the composite.

    Confirm desktop-build.yml still has no reference to setup-pnpm-ci and still has package-manager-cache false (D-04). Do not edit desktop-build.yml in this plan.
  </action>
  <acceptance_criteria>
    - Either action.yml exists with both SHAs above and ci.yml jobs call ./.github/actions/setup-pnpm-ci, or action.yml is absent and ci.yml still inlines the two uses with those SHAs
    - grep setup-pnpm-ci .github/workflows/desktop-build.yml exits 1
    - grep package-manager-cache .github/workflows/desktop-build.yml matches
  </acceptance_criteria>
  <verify>
    <automated>grep -F 'package-manager-cache: false' .github/workflows/desktop-build.yml && ! grep -F 'setup-pnpm-ci' .github/workflows/desktop-build.yml && if [ -f .github/actions/setup-pnpm-ci/action.yml ]; then grep -F '0977fd99725f1db4007ccb2928dbb4e90d06cc86' .github/actions/setup-pnpm-ci/action.yml && grep -F '820762786026740c76f36085b0efc47a31fe5020' .github/actions/setup-pnpm-ci/action.yml && grep -F './.github/actions/setup-pnpm-ci' .github/workflows/ci.yml; else grep -F '0977fd99725f1db4007ccb2928dbb4e90d06cc86' .github/workflows/ci.yml; fi && docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667</automated>
  </verify>
  <done>Composite only if ≥2 CI twins; signed path unused (BRIEF-2.4, D-04, D-30).</done>
</task>

</tasks>

<!-- planner-discipline-allow: apps/desktop/** -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR head → pnpm install / cargo test | Untrusted code on GitHub-hosted ubuntu; read-only token |
| Local composite uses: | Composite is same-repo; pwn-request only if privileged PR-from-base trigger (forbidden) |
| Shared pnpm cache on unsigned CI | Cache poisoning matters on signed artifacts, not this CI path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-01 | Elevation of privilege | ci.yml | high | mitigate | Stay on pull_request; composite is not loaded via privileged base-ref trigger (D-02). |
| T-04.4-02 | Tampering | ci.yml run: | high | mitigate | No PR title in run:; filters are static YAML. |
| T-04.4-03 | Tampering | rust-cache / pnpm cache | medium | mitigate | rust-cache only on desktop-rust; signed desktop-build must not call setup-pnpm-ci (D-04). |
| T-04.4-04 | Tampering | composite uses: | high | mitigate | SHA-pin actions inside the composite (D-03). |
| T-04.4-05 | Information disclosure | ci.yml | low | accept | No new secrets. |
| T-04.4-06 | Elevation of privilege | deploy | high | mitigate | ci.yml does not deploy. |
| T-04.4-07 | Tampering | GHCR latest | low | accept | Not this plan. |
| T-04.4-08 | Tampering | dual migrators | low | accept | Not this plan. |
| T-04.4-SC | Tampering | npm installs | high | accept | No lockfile packages; squawk stays global CLI pin. |
</threat_model>

<verification>
Actionlint. Job ids desktop-web, desktop-rust, backend-test, squawk, ci. No apps/desktop/** inside the desktop-web filter region. Filtered frozen installs present. desktop-build.yml still package-manager-cache false and not using the composite.
</verification>

<success_criteria>
Path-selective desktop jobs; one backend-test; aggregator still named ci; D-33 commit 2 of 7.
</success_criteria>

## Artifacts this phase produces

- `.github/workflows/ci.yml` — split jobs, filters, backend-test, filtered pnpm
- `.github/actions/setup-pnpm-ci/action.yml` — only if ≥2 identical unsigned setups remain

<output>
Create `.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-02-SUMMARY.md` when done.
D-33 commit 2 of 7: CI job split + filtered installs. Single commit. Do not squash. Do not push.
</output>
