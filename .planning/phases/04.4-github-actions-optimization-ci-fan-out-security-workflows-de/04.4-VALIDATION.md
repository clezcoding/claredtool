---
phase: 04.4
slug: github-actions-optimization-ci-fan-out-security-workflows-de
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-09-04
---

# Phase 04.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Actionlint 1.7.12 + zizmor + grep gates + existing Jest/Vitest/Cargo |
| **Config file** | none for GHA; `apps/backend/jest.config.cjs`; `apps/desktop/vitest.config.ts` |
| **Quick run command** | `docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667` |
| **Full suite command** | actionlint + `uvx zizmor .` + `pnpm --filter ./apps/desktop test` + `pnpm --filter ./apps/backend test` + `cargo test --locked --manifest-path apps/desktop/src-tauri/Cargo.toml` |
| **Estimated runtime** | ~30–90 seconds (YAML lint); app suites longer |

---

## Sampling Rate

- **After every task commit:** actionlint on changed YAML + grep gates below
- **After every plan wave:** actionlint + zizmor + relevant `pnpm`/`cargo` tests
- **Before `/gsd-verify-work`:** Full lint + no unpinned `uses:` + aggregator still named `ci`
- **Max feedback latency:** 90 seconds (YAML lint)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 04.4-W0-merge | 01 | 0 | Wave 0 | — | origin/main workflows present | other | `test -f .github/workflows/gitleaks.yml` | ✅ after rebase | ✅ green |
| 04.4-01-T1 | 01 | 1 | BRIEF-1.1 / D-28 / D-29 / D-31 / D-01 | T-04.4-01 | trigger/concurrency remainder on origin `ci.yml` | unit | Plan 01 `<verify>` (job `ci`, `ready_for_review`, no `on.paths`, no main-OR) | ✅ | ✅ green |
| BRIEF-1.1 | 01 | 1 | BRIEF-1.1 | T-pending-check | no `github.ref == 'refs/heads/main' \|\|` on heavy `ci.yml` jobs | unit | `! grep -n "github.ref == 'refs/heads/main' ||" .github/workflows/ci.yml` | ✅ | ✅ green |
| BRIEF-1.2 | 01 | 1 | BRIEF-1.2 / D-29 | T-pending-check | `ready_for_review` in `pull_request` types | unit | `grep -F 'ready_for_review' .github/workflows/ci.yml` | ✅ | ✅ green |
| D-01 | 01 | 1 | D-01 | T-pending-check | job named exactly `ci` | unit | `grep -E '^  ci:' .github/workflows/ci.yml` | ✅ origin | ✅ green |
| D-02 | * | * | D-02 | T-pwn-request | no `pull_request_target`; no deploy `workflow_run` | unit | `! grep -R pull_request_target .github/workflows` | ✅ | ✅ green |
| D-03 | * | * | D-03 | T-unpinned | SHA-pinned `uses:` | unit | `! grep -rh 'uses:' .github/workflows \| grep -vE '@[0-9a-f]{40}'` | ✅ origin | ⬜ pending |
| BRIEF-2.1 | 02 | 2 | BRIEF-2.1 / D-30 | — | no `apps/desktop/**` as desktop-web filter; jobs `desktop-web` + `desktop-rust` | unit | filters in `ci.yml` | ❌ | ⬜ pending |
| BRIEF-2.2 | 02 | 2 | D-30 | — | `backend-test` (or documented walltime split) | unit | `grep -E '^  backend-test:' .github/workflows/ci.yml` | ❌ | ⬜ pending |
| BRIEF-3.3 | 03 | 3 | BRIEF-3.3 / D-13 | T-injection | Actionlint in zizmor workflow | unit | `grep actionlint .github/workflows/zizmor.yml` | ❌ | ⬜ pending |
| D-04 | 04 | 4 | D-04 | T-cache-poison | `package-manager-cache: false` on signed job | unit | `grep package-manager-cache .github/workflows/desktop-build.yml` | ✅ origin | ⬜ pending |
| BRIEF-4 | 04 | 4 | D-14 | T-secret-surface | no `branches: [main]` signed trigger | unit | desktop-build `on.push` | ❌ | ⬜ pending |
| BRIEF-5 | 05 | 5 | D-32 | — | Dependabot npm `open-pull-requests-limit: 5` | unit | `grep -A20 npm .github/dependabot.yml` | ❌ | ⬜ pending |
| BRIEF-6-latest | 06 | 6 | D-24 | T-latest | no GHCR `latest` tag | unit | `! grep -E 'value=latest|type=raw,value=latest' .github/workflows/backend-image.yml` | ❌ | ⬜ pending |
| BRIEF-6-migrate | 06 | 6 | D-22 | T-dual-migrator | no skip-only `migrate` job | unit | `! grep -E '^  migrate:' .github/workflows/backend-image.yml` | ❌ job exists on origin | ⬜ pending |
| D-34 | 07 | 7 | D-34 | — | GitHub scenario table after user push OK | manual | docs/draft/JS/Rust/backend/workflow | — | ⬜ manual-after-push-OK (04.4-07) |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] Merge/rebase `origin/main` (`1419b31`) onto the planning branch so gitleaks/zizmor/codeql/squawk/Trivy/Harden-Runner exist — **04.4-01** rebase (no merge commit); `origin/main` is ancestor of `gsd/phase-04.4-…`
- [x] Confirm actionlint container digest, zizmor, shellcheck available locally — Actionlint image includes ShellCheck; do not brew-install a second ShellCheck (D-34)
- [x] Re-inspect Postgres/Redis/Actionlint digests immediately before pin — Actionlint 04.4-01 (A2); Postgres/Redis **04.4-07** (A3, live index digests match RESEARCH; pinned `tag@sha256`)
- [x] Do **not** add a new GHA test framework; prefer `scripts/` grep in plan verify steps

Existing app Jest/Vitest/Cargo cover product code; this phase is YAML/CI.

### Wave 0 execution log (04.4-01)

**A2 Actionlint digest (re-inspected at execute):** `docker buildx imagetools inspect rhysd/actionlint:1.7.12` index digest `sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667` — **matches RESEARCH**. No pin change.

**Actionlint:** `docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667` on Plan 01 files (`ci.yml`, `gitleaks.yml`, `zizmor.yml`, `codeql.yml`) exits **0**. Full-tree run exits **1** on origin-only `desktop-build.yml:74` ShellCheck **SC2129** (style; not this plan's files). Full tree with `-ignore 'SC2129'` exits **0**. ShellCheck ran via the Actionlint image — no second brew ShellCheck.

**zizmor:** `uvx zizmor@1.30.0 --version` → `zizmor 1.30.0`. `uvx zizmor@1.30.0 .` ran; `zizmor_wave0_exit:13`. 66 findings (43 suppressed): 4 informational, 12 low, 7 medium, **0 high**. Pre-existing origin findings (dependabot-cooldown, artipacked, dtolnay ref-version-mismatch, adhoc squawk-cli, template-injection on backend-image/desktop-build). Plan 01 fail-closed only on new highs; Plan 07 is the zizmor gate.

### A3 service-image digests (04.4-07)

Re-inspected at execute 2026-09-04 via `docker buildx imagetools inspect`:

| Image | Index digest | Match RESEARCH |
|-------|--------------|----------------|
| `postgres:16-alpine` | `sha256:cf78e76683b9ca8c5733cbbdce6c9262b45b6767934dd0a95e671f9a0fc20685` | yes |
| `redis:7-alpine` | `sha256:ff02b58f971e7d7d156a1267e283fcbbeee91773b6aa36c49dac28ecfe28eadf` | yes |

Pinned in `ci.yml` `backend-test` and `squawk` as `tag@sha256:…` with the floating tag in a comment. Versions stay 16 and 7. Dependabot cannot bump `jobs.*.services.*.image`; quarterly human re-pin is the D-36 substitute (Pitfall 6).

### Plan 07 close (04.4-07)

D-34 GitHub scenario table stays **manual-after-push-OK** — not executed in this plan. Checklist: `04.4-MANUAL-GITHUB.md`.

**zizmor gate:** `uvx zizmor@1.30.0 .` must exit 0. Wave 0 origin findings mitigated (persist-credentials, env-wrap, SHA comments, Dependabot `cooldown.default-days: 7`). `sync-labels.yml` artipacked ignored via `.github/zizmor.yml` (D-36 byte-unchanged). ShellCheck covered by the Actionlint image — no second brew ShellCheck.

**Actionlint:** same digest-pinned `rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667` as Plan 01 (A2).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| GitHub scenario table (docs-only, draft, ready-for-review JS/TS, Rust-only, backend-only, workflow-YAML, docs-only `main`) | D-34 | Needs live GitHub after explicit user push OK | After merge approval: run table from brief; no prod dispatch; no `v*` tag |
| Staging desktop dispatch + runner pin (`windows-2022` / `macos-15`) | D-17 | Needs GitHub-hosted runners + secrets | After merge; both platforms |
| Branch Protection required check `gitleaks`; Secret Scanning + Push Protection | D-09 / D-12 | GitHub settings — Cursor must not edit via API | Founder checklist in `04.4-MANUAL-GITHUB.md` (04.4-07) |
| Bake vs two-step | D-20 | Needs measured spike | Attempt Bake; abort if cosign digests worse |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s for YAML lint
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
