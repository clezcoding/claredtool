---
phase: 04.4-github-actions-optimization-ci-fan-out-security-workflows-de
plan: 03
type: execute
wave: 3
depends_on:
  - 04.4-02
files_modified:
  - .github/workflows/gitleaks.yml
  - .github/workflows/zizmor.yml
  - .github/workflows/codeql.yml
  - .gitleaks.toml
autonomous: true
requirements:
  - BRIEF-3.1
  - BRIEF-3.2
  - BRIEF-3.3
  - BRIEF-3.4
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-33
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "gitleaks job still named gitleaks; findings fail the job (no continue-on-error); SARIF uploads via github/codeql-action/upload-sarif@cdf488f595d80d6e07e03d4674febd5ab45fa938 category gitleaks (D-08, D-10, BRIEF-3.1)."
    - ".gitleaks.toml exists empty or defaults-only with no blanket fixture-path allowlist (D-11)."
    - "zizmor.yml has workflow-level paths on .github/workflows/**, .github/actions/**, .github/dependabot.yml, action.yml, action.yaml; Actionlint runs in this workflow via rhysd/actionlint digest image; not a third PR-visible workflow (D-13, BRIEF-3.2, BRIEF-3.3)."
    - "codeql.yml on.push.paths and on.pull_request.paths include brief 3.4 globs (**/*.js/jsx/ts/tsx, **/*.rs, package.json, pnpm-lock.yaml, pnpm-workspace.yaml, **/Cargo.toml, **/Cargo.lock, .github/workflows/codeql.yml); schedule and workflow_dispatch have no paths; docs-only does not start the workflow. Job detect (dorny/paths-filter outputs javascript-typescript and rust) plus analyze-js-typescript and analyze-rust remain; analyze jobs need detect and if on those outputs plus draft skip; schedule and workflow_dispatch force both outputs true; push/PR JS-only does not start analyze-rust and reverse; weekly full both languages; JS/TS and Rust use build-mode none; CodeQL is not a required check (D-13, BRIEF-3.4)."
    - "D-09 and D-12 are documented as founder-after-merge only; this plan does not call the GitHub settings API."
  artifacts:
    - .github/workflows/gitleaks.yml
    - .github/workflows/zizmor.yml
    - .github/workflows/codeql.yml
    - .gitleaks.toml
  key_links:
    - "gitleaks job permissions security-events write only because SARIF uploads (D-10)"
    - "Actionlint docker step uses digest not a floating latest tag"
    - "codeql on.push / on.pull_request paths per brief 3.4; schedule has no paths; detect outputs drive analyze job if:; draft skip on analyze; schedule/dispatch both languages true (D-13, BRIEF-3.4)"
  prohibitions:
    - statement: "Do not mark CodeQL or Zizmor as required checks while they are path-skipped (D-13)."
      status: unverified
    - statement: "Do not add a third PR-visible Actionlint workflow (D-13)."
      status: unverified
    - statement: "Do not install Actionlint via curl pipe bash (BRIEF-3.3)."
      status: unverified
    - statement: "Do not add a weekly Gitleaks schedule (D-08)."
      status: unverified
    - statement: "Do not edit Branch Protection via API; gitleaks required check is founder-after-merge (D-09, D-06)."
      status: unverified
    - statement: "Do not add the privileged PR-from-base trigger (D-02)."
      status: unverified
    - statement: "Do not trigger production deploy or a v* tag (D-34)."
      status: unverified
    - statement: "Do not add a new reporting SaaS (D-35)."
      status: unverified
---

<objective>
D-33 logical commit 3: security workflow remainder plus Actionlint (BRIEF-3.1–3.4, D-08–D-13).

Purpose: Tighten origin gitleaks/zizmor/codeql to the brief. Do not recreate files. Do not make path-skipped workflows required.

Output: SARIF Gitleaks, Actionlint inside zizmor.yml, CodeQL detect plus language-split with draft skip, empty .gitleaks.toml.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-RESEARCH.md
@.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-PATTERNS.md
@.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-01-PLAN.md
@docs/claredtool-github-actions-optimierungsauftrag.md
</context>

<tasks>

<task type="auto">
  <name>Gitleaks SARIF plus defaults toml</name>
  <files>.github/workflows/gitleaks.yml, .gitleaks.toml</files>
  <read_first>
    - .github/workflows/gitleaks.yml
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-PATTERNS.md (gitleaks.yml analog)
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-RESEARCH.md (Gitleaks remainder, Pitfall 7, A1)
  </read_first>
  <action>
    Modify origin gitleaks.yml. Keep job name gitleaks, checkout fetch-depth 0 persist-credentials false, action SHA e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e, no continue-on-error (D-08, D-10, BRIEF-3.1). Plan 01 already set main-only push, dispatch, concurrency.

    Job permissions: contents read and security-events write on this job because it uploads SARIF (D-10). Workflow-level permissions stay {}.

    After the gitleaks-action step, upload gitleaks.sarif with github/codeql-action/upload-sarif@cdf488f595d80d6e07e03d4674febd5ab45fa938 and category gitleaks. If the action does not write that file, add a digest-pinned gitleaks/gitleaks CLI step with --report-format sarif --report-path gitleaks.sarif in addition, still failing the job on leaks. Do not invent GITLEAKS_LICENSE in YAML (A1 checklist in Plan 07).

    Create .gitleaks.toml empty or with a comment-only header. Allowlist entries only with a proven false positive plus comment. No blanket fixture-path excludes (D-11).
  </action>
  <acceptance_criteria>
    - gitleaks.yml contains upload-sarif@cdf488f595d80d6e07e03d4674febd5ab45fa938
    - gitleaks.yml contains category: gitleaks
    - gitleaks.yml has no continue-on-error
    - .gitleaks.toml exists and has no [[allowlists]] path that matches fixtures or test
  </acceptance_criteria>
  <verify>
    <automated>grep -F 'github/codeql-action/upload-sarif@cdf488f595d80d6e07e03d4674febd5ab45fa938' .github/workflows/gitleaks.yml && grep -F 'category: gitleaks' .github/workflows/gitleaks.yml && test "$(grep -c 'continue-on-error' .github/workflows/gitleaks.yml || true)" -eq 0 && test -f .gitleaks.toml && ! grep -E 'fixtures|__tests__|test/' .gitleaks.toml</automated>
  </verify>
  <done>Gitleaks fails closed and uploads SARIF; toml defaults-only (D-08, D-10, D-11, BRIEF-3.1).</done>
</task>

<task type="auto">
  <name>Zizmor paths plus Actionlint in same workflow</name>
  <files>.github/workflows/zizmor.yml</files>
  <read_first>
    - .github/workflows/zizmor.yml
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-PATTERNS.md (zizmor Target paths, Actionlint docker step)
  </read_first>
  <action>
    Modify origin zizmor.yml. Add workflow-level paths on push and pull_request: .github/workflows/**, .github/actions/**, .github/dependabot.yml, action.yml, action.yaml (D-13, BRIEF-3.2). Path filter is allowed because this workflow is not a required check. Keep workflow_dispatch. Add schedule cron "42 4 * * 3" (Wednesday 04:42 UTC) so it does not collide with CodeQL "18 5 * * 1" or Dependabot Monday 06:00 Europe/Berlin.

    Keep zizmor-action@6fc4b006235f201fdab3722e17240ab420d580e5, checkout persist-credentials false, job permissions security-events write + contents read + actions read.

    Add an actionlint step that docker-runs rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667 with -v "$PWD:/repo" -w /repo (re-inspect digest at execute; A2). No curl pipe bash. Do not create a separate actionlint.yml (BRIEF-3.3, D-13).
  </action>
  <acceptance_criteria>
    - zizmor.yml on.push.paths and on.pull_request.paths include .github/workflows/**
    - zizmor.yml contains rhysd/actionlint:1.7.12@sha256:
    - No file .github/workflows/actionlint.yml exists
  </acceptance_criteria>
  <verify>
    <automated>grep -F '.github/workflows/**' .github/workflows/zizmor.yml && grep -F 'rhysd/actionlint:1.7.12@sha256:' .github/workflows/zizmor.yml && test ! -f .github/workflows/actionlint.yml && docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667</automated>
  </verify>
  <done>Path-filtered zizmor workflow also runs Actionlint (BRIEF-3.2, BRIEF-3.3, D-13).</done>
</task>

<task type="auto">
  <name>CodeQL detect, language split, draft skip</name>
  <files>.github/workflows/codeql.yml</files>
  <read_first>
    - .github/workflows/codeql.yml
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-PATTERNS.md (codeql Current analyze matrix, Target jobs)
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-RESEARCH.md (CodeQL language split)
  </read_first>
  <action>
    Modify origin codeql.yml (D-13, BRIEF-3.4). Origin has no workflow-level paths. Add on.push.paths and on.pull_request.paths from brief 3.4 (CodeQL is not a required check, so workflow-level paths are allowed). Required globs: **/*.js, **/*.jsx, **/*.ts, **/*.tsx, **/*.rs, package.json, pnpm-lock.yaml, pnpm-workspace.yaml, **/Cargo.toml, **/Cargo.lock, rust-toolchain.toml, .github/workflows/codeql.yml. Quote globs that start with *. Do not put paths on schedule or workflow_dispatch — weekly and manual must still start the workflow with both languages. Docs-only must not start CodeQL. These paths do not replace in-workflow language fan-out.

    Add job id detect that always runs when the workflow starts. detect checks out with persist-credentials false and uses dorny/paths-filter@ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d. Outputs must be named javascript-typescript and rust. Paths-filter filters: javascript-typescript covers js/jsx/ts/tsx plus package.json, pnpm-lock.yaml, pnpm-workspace.yaml, and this workflow file; rust covers rs plus Cargo.toml, Cargo.lock, and this workflow file. The javascript-typescript filter must not include rust sources; the rust filter must not include js/jsx/ts/tsx sources.

    When github.event_name is schedule or workflow_dispatch, detect sets both outputs to true (weekly and manual full JS/TS+Rust). On push and pull_request, outputs come only from paths-filter.

    Replace the single analyze matrix with jobs analyze-js-typescript and analyze-rust, fail-fast independence preserved (no job-level fail-fast true across languages). Both analyze jobs needs: detect. SHA cdf488f595d80d6e07e03d4674febd5ab45fa938 for init and analyze. Drop autobuild when using build-mode none. Set build-mode: none on both language jobs. If comments note scheduled-run emptiness, revert only analyze-rust to autobuild later — not in this phase unless init fails locally to parse.

    Each analyze job if: requires its matching detect output equal to true AND (event is not pull_request OR draft is false). Add ready_for_review to pull_request types so converting a draft re-runs detect plus analyze (Pitfall 2). Weekly cron stays "18 5 * * 1". Keep persist-credentials false and permissions actions read, contents read, security-events write on analyze jobs. detect needs contents read only.

    Do not add CodeQL as a required check (D-13). D-09 required name gitleaks and D-12 Secret Scanning stay founder checklist (Plan 07).
  </action>
  <acceptance_criteria>
    - grep -E '^  detect:' and '^  analyze-js-typescript:' and '^  analyze-rust:' match
    - detect uses dorny/paths-filter@ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d
    - analyze jobs needs include detect; if: uses detect outputs plus draft skip
    - schedule and workflow_dispatch force both detect outputs true
    - JS-only filter paths do not enable the rust output
    - on.push.paths and on.pull_request.paths include **/*.ts, **/*.rs, package.json, pnpm-lock.yaml, **/Cargo.toml, .github/workflows/codeql.yml
    - on.schedule and on.workflow_dispatch have no paths key
    - codeql.yml contains ready_for_review
    - codeql.yml contains build-mode: none
    - No leftover job id that is only the old single-language matrix
  </acceptance_criteria>
  <verify>
    <automated>grep -E '^  detect:' .github/workflows/codeql.yml && grep -E '^  analyze-js-typescript:' .github/workflows/codeql.yml && grep -E '^  analyze-rust:' .github/workflows/codeql.yml && grep -F 'dorny/paths-filter@ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d' .github/workflows/codeql.yml && grep -F 'ready_for_review' .github/workflows/codeql.yml && grep -F 'build-mode: none' .github/workflows/codeql.yml && python3 -c "
import yaml
d=yaml.safe_load(open('.github/workflows/codeql.yml'))
on=d.get('on') or d.get(True)
push=on['push']
pr=on['pull_request']
need=['**/*.js','**/*.jsx','**/*.ts','**/*.tsx','**/*.rs','package.json','pnpm-lock.yaml','pnpm-workspace.yaml','**/Cargo.toml','**/Cargo.lock','.github/workflows/codeql.yml']
for label, block in (('push', push), ('pull_request', pr)):
    paths=block.get('paths')
    assert paths, label+' missing paths'
    blob='\n'.join(paths) if isinstance(paths, list) else str(paths)
    for g in need:
        assert g in blob, label+' missing '+g
sched=on.get('schedule')
assert not isinstance(sched, dict) or 'paths' not in sched
if isinstance(sched, list):
    for item in sched:
        assert not isinstance(item, dict) or 'paths' not in item
wd=on.get('workflow_dispatch')
if isinstance(wd, dict):
    assert 'paths' not in wd
jobs=d['jobs']
assert 'detect' in jobs and 'analyze-js-typescript' in jobs and 'analyze-rust' in jobs
det=jobs['detect']
outs=det.get('outputs') or {}
assert 'javascript-typescript' in outs and 'rust' in outs
filters=None
det_blob=yaml.dump(det)
assert 'schedule' in det_blob and 'workflow_dispatch' in det_blob
for step in det.get('steps') or []:
    uses=str(step.get('uses') or '')
    if 'paths-filter' in uses:
        filters=step.get('with',{}).get('filters')
assert filters, 'detect missing paths-filter filters'
f=yaml.safe_load(filters)
js=f.get('javascript-typescript')
rs=f.get('rust')
assert js and rs
js_s='\n'.join(js) if isinstance(js,list) else str(js)
rs_s='\n'.join(rs) if isinstance(rs,list) else str(rs)
assert '.ts' in js_s and '.rs' in rs_s
assert '.rs' not in js_s
assert '.ts' not in rs_s and '.js' not in rs_s
for name, key in (('analyze-js-typescript','javascript-typescript'),('analyze-rust','rust')):
    n=jobs[name].get('needs')
    assert n=='detect' or n==['detect'] or (isinstance(n,list) and 'detect' in n)
    ifs=str(jobs[name].get('if') or '')
    assert 'draft' in ifs and key in ifs
assert 'analyze' not in jobs
print('DETECT_OK')
" && docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667</automated>
  </verify>
  <done>CodeQL detect fans out languages; drafts skip analyze; schedule/dispatch both; not required (BRIEF-3.4, D-13).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR + Gitleaks history clone | fetch-depth 0 with persist-credentials false |
| SARIF upload → GitHub code scanning | security-events write on gitleaks/codeql/zizmor jobs only |
| Path-skipped zizmor/codeql | Must not be required checks (pending trap) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-01 | Elevation of privilege | security workflows | high | mitigate | pull_request only; persist-credentials false (D-02, D-08). |
| T-04.4-02 | Tampering | zizmor Actionlint run: | medium | mitigate | Digest-pinned image; no untrusted interpolation in the docker run. |
| T-04.4-03 | Tampering | caches | low | accept | These workflows do not cache build outputs. |
| T-04.4-04 | Tampering | action SHAs | high | mitigate | Keep origin SHAs; Actionlint by digest (D-03). |
| T-04.4-05 | Information disclosure | Gitleaks SARIF | medium | mitigate | Job fail is the merge gate; SARIF may mark later-removed secrets resolved — note in Plan 07 checklist (Pitfall 7, D-10 locked). |
| T-04.4-06 | Elevation of privilege | workflow_run | high | mitigate | Do not add (D-02). |
| T-04.4-07 | Tampering | GHCR latest | low | accept | Not this plan. |
| T-04.4-08 | Tampering | dual migrators | low | accept | Not this plan. |
| T-04.4-SC | Tampering | installs | high | accept | No npm/pip/cargo packages. |
</threat_model>

<verification>
Actionlint. Gitleaks SARIF upload SHA present. zizmor has Actionlint digest and workflow paths. codeql has brief 3.4 on.push/on.pull_request paths (schedule has none), detect plus two analyze jobs, language outputs, draft skip types, build-mode none.
</verification>

<success_criteria>
Origin security workflows edited not replaced. Gitleaks fail-closed with SARIF. Actionlint lives in zizmor.yml. CodeQL split. D-33 commit 3 of 7.
</success_criteria>

## Artifacts this phase produces

- `.github/workflows/gitleaks.yml` — SARIF, fail-closed
- `.github/workflows/zizmor.yml` — paths + Actionlint
- `.github/workflows/codeql.yml` — detect + language-split + draft skip
- `.gitleaks.toml` — defaults-only

<output>
Create `.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-03-SUMMARY.md` when done.
D-33 commit 3 of 7: security workflows + Actionlint. Single commit. Do not squash. Do not push.
</output>
