---
phase: 04.4-github-actions-optimization-ci-fan-out-security-workflows-de
plan: 01
subsystem: infra
tags: [github-actions, ci, concurrency, gitleaks, zizmor, codeql, actionlint]

requires:
  - phase: origin/main
    provides: "Security baseline workflows gitleaks.yml, zizmor.yml, codeql.yml plus Harden-Runner, Trivy, squawk (#92–#98)"
provides:
  - "Linear rebase onto origin/main (no merge commit); security workflows in working tree"
  - "ci.yml draft skip + path-on-main remainder; aggregator job still named ci"
  - "D-31 concurrency and run-names on ci, gitleaks, zizmor, codeql"
  - "Gitleaks push limited to main + workflow_dispatch (D-08 start)"
affects: [04.4-02, 04.4-03, branch-protection-ci]

actuals:
  tokens: 2724
  tasks: 3
  commits: 4

tech-stack:
  added: []
  patterns:
    - "Job-level if: draft-skip AND paths-filter; never workflow-level paths on ci"
    - "concurrency.group: ${{ github.workflow }}-${{ github.ref }} + cancel-in-progress: true"
    - "run-name uses event/ref or PR number + head_ref; never # inside format() strings (expression comment)"

key-files:
  created: []
  modified:
    - .github/workflows/ci.yml
    - .github/workflows/gitleaks.yml
    - .github/workflows/zizmor.yml
    - .github/workflows/codeql.yml
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-VALIDATION.md

key-decisions:
  - "Wave 0 is a rebase onto origin/main, not a merge commit (D-33)"
  - "Plan 01 keeps origin job names (lint, desktop-test, backend-unit, backend-e2e, squawk); split waits for 04.4-02"
  - "run-name format strings omit # because GitHub expressions treat # as a comment (Actionlint)"
  - "A2 Actionlint 1.7.12 index digest unchanged from RESEARCH"
  - "Full-tree Actionlint still fails origin desktop-build.yml SC2129; Plan 01 YAML files exit 0"

patterns-established:
  - "Pattern 2 draft skip: (event_name != pull_request || draft == false) && path outputs"
  - "Pattern 3 concurrency without extra ci- prefix"

requirements-completed:
  - BRIEF-1.1
  - BRIEF-1.2
  - BRIEF-1.3
  - BRIEF-1.4
  - D-01
  - D-28
  - D-29
  - D-31
  - D-33

coverage:
  - id: D1
    description: Working tree contains origin/main security workflows after rebase onto origin/main (no merge commit)
    requirement: D-33
    verification:
      - kind: other
        ref: "test -f .github/workflows/gitleaks.yml && test -f .github/workflows/zizmor.yml && test -f .github/workflows/codeql.yml; git merge-base --is-ancestor origin/main HEAD"
        status: pass
    human_judgment: false
  - id: D2
    description: Aggregator job id stays ci; no workflow-level paths; ready_for_review listed; heavy jobs no longer OR-true on every main push
    requirement: D-01
    verification:
      - kind: other
        ref: "grep -E '^  ci:' .github/workflows/ci.yml; grep -F ready_for_review; python3 yaml assert no on.paths; grep -c main-OR == 0"
        status: pass
    human_judgment: false
  - id: D3
    description: ci, gitleaks, zizmor, codeql share D-31 concurrency; gitleaks push limited to main; no gitleaks schedule
    requirement: D-31
    verification:
      - kind: other
        ref: "grep cancel-in-progress + github.workflow }}-${{ github.ref on gitleaks/zizmor/codeql; gitleaks push branches main; no schedule"
        status: pass
    human_judgment: false
  - id: D4
    description: Wave 0 Actionlint (ShellCheck via image) and uvx zizmor@1.30.0 recorded in VALIDATION.md
    requirement: D-34
    verification:
      - kind: other
        ref: "docker run rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667 on Plan 01 YAML (exit 0); uvx zizmor@1.30.0 . (exit 13, 0 high)"
        status: pass
    human_judgment: false

duration: 8min
completed: 2026-09-04
status: complete
---

# Phase 04.4 Plan 01: Trigger/concurrency remainder Summary

**Rebase onto origin/main plus D-33 commit-1 remainder: ci.yml draft/path-on-main, unified workflow-ref concurrency, run-names without PR titles**

## Performance

- **Duration:** 8 min
- **Started:** 2026-09-04T03:42:04Z
- **Completed:** 2026-09-04T03:50:39Z
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments

- Planning branch rebased onto `origin/main` `1419b31` (linear; no merge commit). `gitleaks.yml`, `zizmor.yml`, `codeql.yml`, squawk, Harden-Runner present.
- `ci.yml`: `ready_for_review` types; concurrency `github.workflow-github.ref`; heavy jobs use draft-skip AND current path outputs (`desktop`/`backend`/`shared`/`tax-engine`); aggregator job still `ci`.
- Analysis workflows share D-31 concurrency + short run-names. Gitleaks push is `main` only + `workflow_dispatch`; no Gitleaks schedule.
- Wave 0 Actionlint digest re-inspected (unchanged). Plan 01 YAML Actionlint exit 0. `uvx zizmor@1.30.0` ran (0 high; origin findings documented).

## Task Commits

Each task was committed atomically (D-33 YAML split by Actionlint fix):

1. **Task 1+2: End-to-end origin rebase/ff + ci.yml trigger slice / Unify analysis concurrency** - `ae7fd66` (chore)
2. **Rule 1 fix: drop `#` in run-name format strings** - `d32f08d` (fix)
3. **Task 3: Local Actionlint container smoke + VALIDATION Wave 0** - `c4661eb` (docs)

**Plan metadata:** (this commit)

_Note: Wave 0 rebase replayed existing docs commits; it is not D-33 commit 1._

## Files Created/Modified

- `.github/workflows/ci.yml` — run-name, `ready_for_review`, drop `ci-` concurrency prefix, draft+path `if:` on lint/desktop-test/backend-unit/backend-e2e/squawk; job `ci` unchanged
- `.github/workflows/gitleaks.yml` — `push.branches: [main]`, `workflow_dispatch`, D-31 concurrency, run-name
- `.github/workflows/zizmor.yml` — D-31 concurrency + run-name only (no Actionlint/paths yet)
- `.github/workflows/codeql.yml` — D-31 concurrency + run-name; weekly cron and analyze matrix kept
- `.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-VALIDATION.md` — Wave 0 checkboxes + 04.4-01 task map

## Decisions Made

- Rebase (not merge) so D-33 stays seven linear commits.
- Keep origin job/filter names until Plan 02 (plan action).
- `run-name` uses `format('… PR {n} …')` without `#` so Actionlint/GitHub expression lexers do not treat `#` as a comment.
- A2: Actionlint 1.7.12 index digest still `sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667`.
- Do not edit `desktop-build.yml` to clear origin SC2129 in this plan.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `PR #{n}` in `run-name` format strings**
- **Found during:** Task 3 (Actionlint)
- **Issue:** GitHub expression `#` comments out the rest of `format()`, Actionlint EOF on string literal
- **Fix:** `PR {n}` without hash
- **Files modified:** `.github/workflows/ci.yml`, `gitleaks.yml`, `zizmor.yml`, `codeql.yml`
- **Verification:** Actionlint on those four files exits 0
- **Committed in:** `d32f08d`

**2. [Rule 3 - Blocking] OrbStack Docker daemon was down**
- **Found during:** Task 3
- **Issue:** `docker run` failed (`docker.sock` missing)
- **Fix:** `orb start`; inspect had already succeeded against the registry
- **Files modified:** none
- **Verification:** Actionlint container ran
- **Committed in:** n/a (environment)

---

**Total deviations:** 2 auto-fixed (1 bug, 1 blocking env). D-33 “single YAML commit” became two (`ae7fd66` + `d32f08d`) because amend is forbidden after a successful commit. Full-tree Actionlint still exits 1 on origin `desktop-build.yml` SC2129 — out of Plan 01 files; Plan 01 YAML exits 0; `-ignore 'SC2129'` makes the full tree exit 0.
**Impact on plan:** Correctness of `run-name` required the extra commit. No scope creep into Plan 02/03/04 files.

## Issues Encountered

- Interactive tracer human-verify gate not surfaced: D-33 requires Task 1+2 in one YAML commit and the orchestrator asked for all tasks + SUMMARY.
- Rebase conflicted only on `.planning/STATE.md` (kept 04.4 planning intent + origin Phase 6 history). Dependabot-plan commit `2a032b7` replay produced no unique tree vs origin after keeping HEAD frontmatter.

## Authentication Gates

None.

## Known Stubs

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Ready for `04.4-02-PLAN.md` (desktop-web / desktop-rust split, `backend-test`, filtered pnpm). Do not push until user OK (D-34). Do not treat origin SC2129 or zizmor exit 13 as Plan 01 failures.

---
*Phase: 04.4-github-actions-optimization-ci-fan-out-security-workflows-de*
*Completed: 2026-09-04*

## Self-Check: PASSED

- FOUND: `.github/workflows/ci.yml`
- FOUND: `.github/workflows/gitleaks.yml`
- FOUND: `.github/workflows/zizmor.yml`
- FOUND: `.github/workflows/codeql.yml`
- FOUND: `.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-VALIDATION.md`
- FOUND: `ae7fd66`, `d32f08d`, `c4661eb`
