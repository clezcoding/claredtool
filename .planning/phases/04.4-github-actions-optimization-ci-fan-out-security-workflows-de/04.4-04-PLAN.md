---
phase: 04.4-github-actions-optimization-ci-fan-out-security-workflows-de
plan: 04
type: execute
wave: 4
depends_on:
  - 04.4-03
files_modified:
  - .github/workflows/desktop-build.yml
autonomous: false
requirements:
  - BRIEF-4
  - D-04
  - D-07
  - D-14
  - D-15
  - D-16
  - D-17
  - D-18
  - D-19
  - D-33
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "desktop-build.yml on.push has tags v* and has no branches main; workflow_dispatch remains (D-14, D-15, BRIEF-4)."
    - "publish-faynosync if: is dispatch with inputs.publish or startsWith tags v; the origin main-push publish branch is gone (D-14, D-15, D-07)."
    - "A job using softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64 runs only on v* tags, uploads MSI and DMG, fail_on_unmatched_files true, contents write on that job only; production dispatch does not create a GitHub Release (D-16)."
    - "Matrix os is windows-2022 and macos-15 with a comment naming D-17; fail-fast is true only for non-tag workflow_dispatch; tag matrix fail-fast false (D-17, D-19)."
    - "upload-artifact uses compression-level 0, if-no-files-found error, retention-days 14 on v* else 3 (D-18)."
    - "Signed tauri job keeps package-manager-cache false, no rust-cache, dtolnay components empty string, no rustfmt/clippy install on the release toolchain (D-04, D-19)."
    - "FaynoSync still receives NSIS exe+sig and macOS app.tar.gz+sig; channel stable only (D-07)."
    - statement: "Re-running the same v* tag publish is FaynoSync/GitHub Release overwrite behavior; concurrency cancel-in-progress stays false for tags and dispatch (D-14 idempotency/concurrency)."
      verification: backstop
  artifacts:
    - .github/workflows/desktop-build.yml
  key_links:
    - "GH Release job needs tauri and if: startsWith(github.ref, 'refs/tags/v')"
    - "download-artifact SHA 3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c reused from origin publish job"
    - "Harden-Runner audit remains on signing jobs (D-36)"
  prohibitions:
    - statement: "Do not add Nightly desktop builds (D-07, 04.2 D-07 deferred)."
      status: unverified
    - statement: "Do not enable pnpm or rust-cache on the signed tauri job (D-04)."
      status: unverified
    - statement: "Do not add workflow_run for desktop deploy (D-02)."
      status: unverified
    - statement: "Do not dispatch production or create a v* tag during this phase (D-06, D-34)."
      status: unverified
    - statement: "Do not add the privileged PR-from-base trigger (D-02)."
      status: unverified
    - statement: "Do not change GitHub settings via API (D-06)."
      status: unverified
    - statement: "Do not add a new reporting SaaS (D-35)."
      status: unverified
    - statement: "Do not edit rust-toolchain.toml to drop clippy for CI convenience (Pitfall 4)."
      status: unverified
---

<objective>
D-33 logical commit 4: signed desktop only on v* and workflow_dispatch; GitHub Release on tags; pin hosted runner generations (BRIEF-4, D-14–D-19, D-04, D-07).

Purpose: Stop automatic signed Windows/macOS + FaynoSync on every main UI push. Keep FaynoSync as updater SSOT. GH Release is tag-only.

Output: desktop-build.yml remainder. Staging runner proof is after merge (D-34), not this PR.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-CONTEXT.md
@.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-RESEARCH.md
@.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-PATTERNS.md
@.planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-UPDATER-OPS.md
@docs/claredtool-github-actions-optimierungsauftrag.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Confirm dropping signed builds from main</name>
  <decision>Remove automatic signed Windows/macOS Tauri build and FaynoSync publish from pushes to main (locked D-14).</decision>
  <context>
    Today origin desktop-build.yml signs both platforms on path-filtered main. After this plan, only v* tags (production) and workflow_dispatch remain. Undo means restoring the main trigger and re-opening runner cost plus signing-secret surface on every UI push. Staging dispatch plus runner-pin test stays after merge (D-17, D-34). Default option is the locked decision.
  </context>
  <options>
    <option id="proceed-drop-main">
      <name>Proceed — tags and dispatch only (D-14)</name>
      <pros>Cuts the largest desktop runner cost; shrinks secret exposure on routine main pushes; matches the brief.</pros>
      <cons>main UI commits no longer produce signed artifacts until a tag or manual dispatch.</cons>
    </option>
    <option id="keep-main-signing">
      <name>Keep signed builds on main</name>
      <pros>Every main UI push still produces updater artifacts.</pros>
      <cons>Violates locked D-14; keeps the cost/secret surface the brief exists to remove.</cons>
    </option>
  </options>
  <resume-signal>Select proceed-drop-main (required to continue) or keep-main-signing (halts this plan; do not implement BRIEF-4).</resume-signal>
</task>

<task type="auto">
  <name>Drop main trigger; pin runners; FaynoSync if</name>
  <reversibility rating="one-way">Signed updater artifacts stop appearing on ordinary main pushes; restoring that cadence re-opens two-platform signing on every UI commit (D-14).</reversibility>
  <files>.github/workflows/desktop-build.yml</files>
  <read_first>
    - .github/workflows/desktop-build.yml
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-PATTERNS.md (desktop-build Trigger remainder, Concurrency keep, Matrix remainder, Publish if remainder)
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-RESEARCH.md (Desktop-release remainder)
  </read_first>
  <action>
    Only after proceed-drop-main. Modify origin desktop-build.yml (BRIEF-4, D-14, D-15, D-17, D-19).

    on.push: keep tags: ['v*']; delete branches: [main] and the paths block that existed only for main. Keep workflow_dispatch inputs target (staging default) and publish (D-15).

    Concurrency: cancel-in-progress must be false for tags and workflow_dispatch (origin expression already encodes this; after main removal set cancel-in-progress: false explicitly if simpler) (D-31).

    Matrix os: [windows-2022, macos-15] with a YAML comment that D-17 staging test after merge may swap to windows-2025 or macos-14 (discretion; RESEARCH recommendation). fail-fast: ${{ github.event_name == 'workflow_dispatch' && !startsWith(github.ref, 'refs/tags/v') }} so staging dispatch fail-fast true; v* fail-fast false (D-19).

    Publish job if: keep (workflow_dispatch && inputs.publish) OR startsWith tags v. Delete the origin branch that published on main push (PATTERNS Publish if remainder). Keep FaynoSync curl/jq, NSIS exe+sig, macOS app.tar.gz+sig, stable channel (D-07). Production dispatch still uploads FaynoSync only (D-16).

    Signed tauri job: keep package-manager-cache false; do not add rust-cache or setup-pnpm-ci (D-04). Set dtolnay/rust-toolchain components to an empty string so rust-toolchain.toml rustfmt/clippy are not installed on the release job (D-19, A4). Do not edit rust-toolchain.toml.

    persist-credentials false on every checkout in this file. Harden-Runner audit stays. Do not gh workflow dispatch. Do not create tags.
  </action>
  <acceptance_criteria>
    - on.push has tags v*
    - on.push has no branches: [main]
    - matrix os lists windows-2022 and macos-15
    - package-manager-cache: false still present
    - publish job if: has no refs/heads/main push clause
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import yaml
d=yaml.safe_load(open('.github/workflows/desktop-build.yml'))
on=d.get('on') or d.get(True)
push=on['push']
assert 'v*' in push.get('tags', []) or any('v*' in str(t) for t in push.get('tags', []))
assert 'main' not in (push.get('branches') or [])
jobs=d['jobs']
tauri=jobs.get('tauri') or jobs[list(jobs)[0]]
oslist=tauri['strategy']['matrix']['os']
assert 'windows-2022' in oslist and 'macos-15' in oslist
print('DESKTOP_TRIGGERS_OK')
" && grep -F 'package-manager-cache: false' .github/workflows/desktop-build.yml && test "$(grep -vE '^[[:space:]]*#' .github/workflows/desktop-build.yml | grep -c \"github.ref == 'refs/heads/main' && github.event_name == 'push'\" || true)" -eq 0</automated>
  </verify>
  <done>Signed desktop no longer follows main; runners pinned; FaynoSync on tag/dispatch only (D-14, D-15, D-17, D-19, D-04, D-07).</done>
</task>

<task type="auto">
  <name>Tag-only GitHub Release and artifact retention</name>
  <files>.github/workflows/desktop-build.yml</files>
  <read_first>
    - .github/workflows/desktop-build.yml
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-PATTERNS.md (Artifact upload analog, GH Release job)
  </read_first>
  <action>
    On origin upload-artifact steps (SHA 043fb46d1a93c77aae656e7c1c64a875d1fc6a0a), add compression-level: 0, if-no-files-found: error (already origin), and retention-days: ${{ startsWith(github.ref, 'refs/tags/v') && 14 || 3 }} (D-18). Keep Windows NSIS/MSI globs and macOS DMG plus app.tar.gz plus sig globs.

    Add a job after tauri, if: startsWith(github.ref, 'refs/tags/v') only (D-16). Job permissions contents: write on this job only. Checkout persist-credentials false. Download artifacts with actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c pattern *-bundle path artifacts merge-multiple true. Then softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64 with files artifacts/**/*.msi and artifacts/**/*.dmg and fail_on_unmatched_files: true. Do not attach NSIS exe to the GitHub Release (installers are MSI + DMG; FaynoSync still gets updater binaries).

    Production workflow_dispatch (no tag) must not satisfy this job if:. Do not run a release during implementation.
  </action>
  <acceptance_criteria>
    - desktop-build.yml contains softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64
    - GH Release job if: contains startsWith(github.ref, 'refs/tags/v') and does not mention workflow_dispatch
    - GH Release step has fail_on_unmatched_files: true
    - compression-level: 0 present on upload-artifact
    - retention-days expression contains 14 and 3
  </acceptance_criteria>
  <verify>
    <automated>grep -F 'softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64' .github/workflows/desktop-build.yml && grep -F 'fail_on_unmatched_files: true' .github/workflows/desktop-build.yml && grep -F 'compression-level: 0' .github/workflows/desktop-build.yml && grep -F 'retention-days:' .github/workflows/desktop-build.yml && python3 -c "
import yaml
d=yaml.safe_load(open('.github/workflows/desktop-build.yml'))
jobs=d['jobs']
rel=None
for job in jobs.values():
    for step in job.get('steps') or []:
        if 'action-gh-release' in str(step.get('uses') or ''):
            rel=job
            break
    if rel is not None:
        break
assert rel is not None, 'missing GH Release job'
ifs=str(rel.get('if') or '')
assert 'startsWith' in ifs and 'refs/tags/v' in ifs
assert 'workflow_dispatch' not in ifs
print('GH_RELEASE_TAG_ONLY_OK')
" && docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667</automated>
  </verify>
  <done>Tag-only GH Release for MSI/DMG; artifact retention 3/14 (D-16, D-18).</done>
</task>

</tasks>

<!-- planner-discipline-allow: github.ref == 'refs/heads/main' && github.event_name == 'push' -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tag v* → signing secrets | Protected tag should be founder-only; contents write only on GH Release job |
| workflow_dispatch production | Write access required; outside fork threat model but still least-privilege |
| Artifact upload | Signed binaries; no cache on this path (D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-01 | Elevation of privilege | desktop-build.yml | high | mitigate | No pull_request signed path (already); no privileged PR-from-base trigger (D-02). |
| T-04.4-02 | Tampering | FaynoSync run: | high | mitigate | Keep origin env-wrapping of tokens; no PR title in run:. |
| T-04.4-03 | Tampering | signed artifacts | high | mitigate | package-manager-cache false; no rust-cache (D-04). Block-on high. |
| T-04.4-04 | Tampering | action-gh-release SHA | high | mitigate | Pin efb35369e0ad2afab669f228072c1b0d510eae64 (D-03). |
| T-04.4-05 | Information disclosure | summaries/logs | high | mitigate | Do not echo TAURI_SIGNING_PRIVATE_KEY or FAYNOSYNC_CI_TOKEN (D-05). |
| T-04.4-06 | Elevation of privilege | workflow_run | high | mitigate | Do not add (D-02). |
| T-04.4-07 | Tampering | GHCR latest | low | accept | Not this plan. |
| T-04.4-08 | Tampering | dual migrators | low | accept | Not this plan. |
| T-04.4-SC | Tampering | installs | high | accept | No registry packages. |
</threat_model>

<verification>
Actionlint. Python assert no main branches; os pins; package-manager-cache false; GH Release SHA; job if: startsWith refs/tags/v and no workflow_dispatch; compression-level 0. Do not dispatch.
</verification>

<success_criteria>
Main no longer signs desktop. Tags and dispatch remain. GH Release on tags only. D-33 commit 4 of 7 after human confirm D-14.
</success_criteria>

## Artifacts this phase produces

- `.github/workflows/desktop-build.yml` — no main; pinned runners; GH Release; retention/compression

<output>
Create `.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-04-SUMMARY.md` when done.
D-33 commit 4 of 7: desktop-release. Single commit. Do not squash. Do not push. Do not tag v*.
</output>
