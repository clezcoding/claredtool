# Phase 04.4: GitHub Actions Optimization - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-04
**Phase:** 04.4-GitHub Actions Optimization
**Areas discussed:** Pflicht-Checks, Desktop-Release-Trigger, Backend-Image, Lieferung & Ops
**Mode:** `--batch` (plain-text numbered questions; recommended option marked each time)

---

## Pflicht-Checks

| Option | Description | Selected |
|--------|-------------|----------|
| Gitleaks required after merge | Manual Branch Protection check `gitleaks` | ✓ |
| Gitleaks advisory only | Visible, not required | |
| Drafts still scanned | Gitleaks on draft PRs | ✓ |
| Drafts skip Gitleaks | Scan at ready_for_review | |
| Fail on finding | Job red | ✓ |
| continue-on-error | Warn, merge allowed | |
| Full scan workflow_dispatch only | No weekly Gitleaks | ✓ |
| Weekly Gitleaks schedule | Extra schedule | |
| Native Secret Scanning + Push Protection on checklist | Public repo, free | ✓ |
| Gitleaks only | Skip GitHub native | |
| Separate required check `gitleaks` | Own workflow | ✓ |
| Fold into `ci` aggregator | One required check | |
| No admin bypass | Even admins cannot merge red Gitleaks | ✓ |
| Admins may merge | Bypass allowed | |
| Empty `.gitleaks.toml`; FP+comment only | Tight allowlist | ✓ |
| Blanket fixture excludes | | |
| No config file | Defaults only | |
| Job fail + SARIF to Security tab | User override vs “log only” rec | ✓ |
| Job log + fail only | Recommended, not chosen | |

**User's choice:** 1a, 2a, 3a, 4a, 5a, 7a, 8a, 9a, 10b
**Notes:** User stated the repo is public (corrects private-repo assumption). Chose SARIF (10b) against the log-only recommendation. CodeQL/Zizmor remain not required (path-skip pending trap) — carried from brief, not re-opened.

---

## Desktop-Release-Trigger

| Option | Description | Selected |
|--------|-------------|----------|
| Remove signed builds from `main`; only `v*` + dispatch | | ✓ |
| Keep `main` as staging-only auto publish | | |
| Keep current `main` signed + FaynoSync | | |
| `v*` = prod; dispatch default staging, prod dispatch allowed | | ✓ |
| Production only via `v*` tag | | |
| GH Release (MSI/DMG) on `v*`; FaynoSync updater SSOT | | ✓ |
| FaynoSync only, no GH Release | | |
| Pin runner generations after dual-platform staging test | | ✓ |
| Keep `*-latest` | | |
| Artifact retention staging 3d / release 14d | | ✓ |
| Staging 3d / release 30d | | |
| GH Release only on tags; prod dispatch = FaynoSync only | | ✓ |
| Prod dispatch also writes GH Release | | |
| Staging fail-fast true; tags fail-fast false | | ✓ |
| fail-fast false everywhere | | |

**User's choice:** 1a, 2a, 3a, 4a, 5a, 6a, 7a
**Notes:** Nightly left out (04.2 D-07). Signed path stays cache-free.

---

## Backend-Image

| Option | Description | Selected |
|--------|-------------|----------|
| Try BuildKit Bake; fallback two steps if tags/cache unclear | | ✓ |
| Keep two build-push steps | | |
| Trivy: one scan iff RootFS equality proven; else both / fail | | ✓ |
| Always scan both; no invariant | | |
| Do not create GitHub migrate job; API CMD migrator; Coolify deploy stays | | ✓ |
| New GitHub migrate job against prod DB | | |
| One ordered deploy job; rollback previous tags on fail | | ✓ |
| Fail stops; manual rollback | | |
| Remove GHCR `latest`; SHA tags only | | ✓ |
| Keep `latest` pointer | | |
| HTTP `/health` or `/health/ready`; fail → rollback | | ✓ |
| Coolify API success only | | |
| Log health, do not fail | | |
| Keep `COOLIFY_IMAGE_DEPLOY` kill switch | | ✓ |
| Always deploy on main | | |
| No PR images to GHCR | | ✓ |
| PR build without push | | |

**User's choice:** 1a, 2a, 3a (after clarification), 4a, 6a, 7a, 8a, 9a
**Notes:** User asked whether Q3 was the Coolify deploy job. Clarified: no — current `backend-image.yml` has no GitHub migrate job; `deploy` is Coolify PATCH+POST and must stay. Trivy is **new** (not in Ist-Stand).

---

## Lieferung & Ops

| Option | Description | Selected |
|--------|-------------|----------|
| One PR, seven logical commits (brief split) | | ✓ |
| Seven PRs | | |
| One squash commit | | |
| Dependabot brief table + Nest major group; keep glib/Node-25 | | ✓ |
| Keep current higher weekly caps | | |
| Everything monthly | | |
| Local static validation first; GitHub scenarios after explicit push OK; no prod dispatch / no v* in phase | | ✓ |
| Observe on main after merge only | | |
| Include staging desktop dispatch in this phase | | |
| Native JUnit if already supported; fail-only artifacts 3–7d; no reporting SaaS | | ✓ |
| Add JUnit reporter packages now | | |
| No test artifacts, summaries only | | |

**User's choice:** 1a, 2a, 3a, 4a
**Notes:** Staging desktop dispatch + runner-pin test scheduled after merge/approval, not in the implementation PR.

---

## Claude's Discretion

- Exact runner generation labels after docs + staging test
- Bake vs two-step after attempting Bake
- Trivy Action SHA and RootFS compare implementation
- Actionlint pin; CodeQL Rust `build-mode` only if official docs support it
- `/health` vs `/health/ready`
- Whether to add `.github/actions/setup-pnpm-ci`
- Nest `@nestjs/*` major-group glob
- Postgres/Redis digests (fetch, do not invent)
- Step-summary copy; JUnit retention inside 3–7 days

## Deferred Ideas

- Nightly signed desktop builds (04.2 D-07)
- GitHub-hosted Prisma migrate when DB is reachable (brief 6.6)
- Harden-Runner `egress-policy: block`
- CodeQL/Zizmor as required checks while path-selective
- Multi-replica external migration runner (document only)
