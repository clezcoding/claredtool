---
phase: 04.4-github-actions-optimization-ci-fan-out-security-workflows-de
plan: 04
subsystem: infra
tags: [github-actions, desktop-build, tauri, faynosync, github-release, runner-pin]

requires:
  - phase: 04.4-03
    provides: "Security workflows + Actionlint fail-closed; desktop-build.yml still origin-shaped (main trigger intact)"
provides:
  - "desktop-build.yml on.push is v* tags only; workflow_dispatch target/publish remain (D-14, D-15)"
  - "Matrix windows-2022 + macos-15 with D-17 comment; staging dispatch fail-fast; tags fail-fast false (D-17, D-19)"
  - "github-release job on v* only uploads MSI+DMG via pinned action-gh-release; production dispatch stays FaynoSync-only (D-16)"
  - "upload-artifact compression-level 0 and retention 14 on v* else 3 (D-18)"
affects: [04.4-05, 04.4-07, faynosync-ops, desktop-release]

actuals:
  tokens: 1315
  tasks: 3
  commits: 3

tech-stack:
  added:
    - "softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64 (v3.0.3) tag-only MSI/DMG"
  patterns:
    - "Signed desktop triggers: v* tags + workflow_dispatch only; never main push"
    - "cancel-in-progress: false explicit on remaining desktop jobs (tags and dispatch)"
    - "GH Release contents: write on that job only; workflow stays contents: read"
    - "dtolnay components empty string so rust-toolchain.toml rustfmt/clippy stay off the signed job"

key-files:
  created: []
  modified:
    - .github/workflows/desktop-build.yml

key-decisions:
  - "Human selected proceed-drop-main; locked D-14 implemented; keep-main-signing not taken"
  - "cancel-in-progress set to false explicitly after main removal (D-31); expression no longer needed"
  - "Matrix pinned windows-2022 + macos-15 now; D-17 staging swap to windows-2025/macos-14 stays after merge (D-34)"
  - "dtolnay components: \"\" overrides rust-toolchain.toml clippy/rustfmt on the signed job (A4); rust-toolchain.toml untouched"
  - "Two atomic feat commits (Task 2 then Task 3) instead of the plan output's single D-33 commit; linear, no squash"

patterns-established:
  - "Pattern: signed desktop = tags v* + dispatch; FaynoSync on those; GitHub Release on tags only"
  - "Pattern: persist-credentials false on every checkout in desktop-build.yml"
  - "Pattern: artifact retention 14 on v* else 3; compression-level 0 for already-compressed bundles"

requirements-completed:
  - BRIEF-4
  - D-04
  - D-07
  - D-14
  - D-15
  - D-16
  - D-17
  - D-18
  - D-19
  - D-33

coverage:
  - id: D1
    description: on.push has tags v* and no branches main; workflow_dispatch inputs target (staging default) and publish remain
    requirement: D-14
    verification:
      - kind: other
        ref: "python3 yaml DESKTOP_TRIGGERS_OK; grep branches: count 0 in desktop-build.yml"
        status: pass
    human_judgment: false
  - id: D2
    description: Matrix os is windows-2022 and macos-15 with D-17 comment; fail-fast true only for non-tag workflow_dispatch
    requirement: D-17
    verification:
      - kind: other
        ref: "python3 yaml assert windows-2022 and macos-15; grep fail-fast expression"
        status: pass
    human_judgment: false
  - id: D3
    description: publish-faynosync if is dispatch with inputs.publish or startsWith tags v; origin main-push publish branch gone; NSIS exe+sig and macOS app.tar.gz+sig still uploaded to FaynoSync
    requirement: D-07
    verification:
      - kind: other
        ref: "grep refs/heads/main count 0; grep nsis/*.exe and app.tar.gz in FaynoSync steps"
        status: pass
    human_judgment: false
  - id: D4
    description: github-release job if startsWith refs/tags/v only; action-gh-release SHA efb35369; MSI+DMG; fail_on_unmatched_files true; contents write on that job only
    requirement: D-16
    verification:
      - kind: other
        ref: "python3 yaml GH_RELEASE_TAG_ONLY_OK; grep action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64"
        status: pass
    human_judgment: false
  - id: D5
    description: upload-artifact compression-level 0, if-no-files-found error, retention-days 14 on v* else 3; signed job package-manager-cache false and empty rust components
    requirement: D-18
    verification:
      - kind: other
        ref: "grep compression-level: 0, retention-days 14/3, package-manager-cache: false, components: \"\"; docker actionlint 1.7.12 exit 0"
        status: pass
    human_judgment: false
  - id: D6
    description: Staging signed-build proof on pinned runner generations after merge (D-17, D-34); not executed in this plan
    requirement: D-17
    verification: []
    human_judgment: true
    rationale: "D-34 forbids workflow_dispatch and v* tags during this phase; runner-generation proof is founder staging after merge"

duration: 4min
completed: 2026-09-04
status: complete
---

# Phase 04.4 Plan 04: Desktop-release remainder Summary

**Signed desktop-build.yml only on v* tags and workflow_dispatch; GH Release (MSI+DMG) tag-only; runners pinned windows-2022/macos-15**

## Performance

- **Duration:** 4 min
- **Started:** 2026-09-04T04:20:52Z
- **Completed:** 2026-09-04T04:24:30Z
- **Tasks:** 3 (1 decision checkpoint + 2 auto)
- **Files modified:** 1

## Accomplishments

- Dropped automatic signed Windows/macOS + FaynoSync from `main` pushes (D-14)
- Kept `workflow_dispatch` `target`/`publish` and FaynoSync NSIS exe+sig plus macOS app.tar.gz+sig on stable (D-15, D-07)
- Pinned hosted runners to `windows-2022` and `macos-15`; staging dispatch fail-fast true; v* fail-fast false (D-17, D-19)
- Added tag-only `github-release` job with pinned `action-gh-release`, MSI+DMG, `fail_on_unmatched_files: true` (D-16)
- Artifact retention 14 days on v* else 3; `compression-level: 0` (D-18)
- Signed tauri job still `package-manager-cache: false`, no rust-cache, dtolnay `components: ""` (D-04, D-19)

## Task Commits

Each task was committed atomically:

1. **Task 1: Confirm dropping signed builds from main** — no commit (checkpoint:decision; human selected `proceed-drop-main`)
2. **Task 2: Drop main trigger; pin runners; FaynoSync if** - `57b867f` (feat)
3. **Task 3: Tag-only GitHub Release and artifact retention** - `3a316f4` (feat)

**Plan metadata:** `docs(04.4-04): complete desktop-release remainder plan` (this close-out commit)

_Note: Plan output asked for a single D-33 commit 4 of 7; executor protocol requires per-task commits. Keep both linear. Do not squash. Do not push._

## Files Created/Modified

- `.github/workflows/desktop-build.yml` - tags+dispatch only; pinned OS; empty rust components; persist-credentials false; GH Release job; artifact compression/retention

## Decisions Made

- Human confirmed `proceed-drop-main` (locked D-14). `keep-main-signing` not implemented.
- After removing `main`, `cancel-in-progress: false` is explicit on tauri and publish-faynosync.
- Runner pins are RESEARCH recommendation now; D-17 staging test after merge may swap generations.
- `components: ""` on dtolnay so `rust-toolchain.toml` rustfmt/clippy are not installed on the signed job. toml unchanged.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Harden-Runner on github-release**
- **Found during:** Task 3 (Tag-only GitHub Release)
- **Issue:** Plan listed Harden-Runner only as remaining on signing jobs; the new GH Release job downloads signed binaries and talks to the GitHub API with `contents: write`.
- **Fix:** Same Harden-Runner audit step as tauri and publish-faynosync.
- **Files modified:** `.github/workflows/desktop-build.yml`
- **Verification:** Actionlint exit 0; job still tag-only
- **Committed in:** `3a316f4` (Task 3)

**2. [Process] Two feat commits instead of one D-33 logical commit**
- **Found during:** Task 2/3 commit protocol vs plan `<output>` "Single commit"
- **Issue:** Plan output asked for one desktop-release commit; executor requires atomic per-task commits.
- **Fix:** Two feat commits, linear, no squash — orchestrator instruction wins.
- **Files modified:** same workflow, split across commits
- **Verification:** `git log -2` shows Task 2 then Task 3
- **Committed in:** `57b867f`, `3a316f4`

---

**Total deviations:** 2 (1 auto-fixed Rule 2, 1 process)
**Impact on plan:** Extra Harden-Runner is least-privilege hygiene. Split commits stay D-33 commit 4 of 7 as two linear feats. No scope creep.

## Issues Encountered

None. Actionlint 1.7.12 digest image exit 0 on the full tree (including this workflow).

## Authentication Gates

None.

## User Setup Required

None - no external service configuration required. D-17 staging dispatch on both pinned runners stays founder-after-merge (D-34). Do not create a v* tag. Do not `gh workflow dispatch`. Do not trigger production Coolify.

## Next Phase Readiness

Ready for 04.4-05 (Dependabot). Signed desktop no longer follows `main`. Keep commits linear. Do not squash. Do not push.

## Self-Check: PASSED

- FOUND: `.github/workflows/desktop-build.yml`
- FOUND: `.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-04-SUMMARY.md`
- FOUND: `57b867f`, `3a316f4`

---
*Phase: 04.4-github-actions-optimization-ci-fan-out-security-workflows-de*
*Completed: 2026-09-04*
