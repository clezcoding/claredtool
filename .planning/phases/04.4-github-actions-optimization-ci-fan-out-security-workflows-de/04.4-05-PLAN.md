---
phase: 04.4-github-actions-optimization-ci-fan-out-security-workflows-de
plan: 05
type: execute
wave: 5
depends_on:
  - 04.4-04
files_modified:
  - .github/dependabot.yml
autonomous: true
requirements:
  - BRIEF-5
  - D-32
  - D-33
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "npm ecosystem open-pull-requests-limit is 5, weekly Monday 06:00 Europe/Berlin (D-32, BRIEF-5)."
    - "cargo open-pull-requests-limit is 3; glib ignore >=0.19.0 remains (D-32)."
    - "github-actions, docker, and docker-compose use interval monthly, open-pull-requests-limit 3, staggered times 06:30 / 06:45 / 07:00 Europe/Berlin, and no schedule.day key (D-32, Pitfall 5)."
    - "npm groups nestjs-major with applies-to version-updates, patterns @nestjs/*, update-types major appears before npm-patch-minor so first-match wins (D-32 adjacency)."
    - "Node ignore >=25 remains on docker; security updates stay enabled (limit is not 0) (D-32)."
    - statement: "Dependabot does not bump GHA services.*.image digests; quarterly human bump is Plan 07 checklist (Pitfall 6, D-36)."
      verification: backstop
  artifacts:
    - .github/dependabot.yml
  key_links:
    - "Single file .github/dependabot.yml version 2 (dependabot skill: no second file)"
    - "nestjs-major listed before the catch-all patch-minor group"
  prohibitions:
    - statement: "Do not set schedule.day on monthly ecosystems (D-32, Pitfall 5)."
      status: unverified
    - statement: "Do not drop glib ignore or Node >=25 ignore unless those breaks are proven fixed (D-32)."
      status: unverified
    - statement: "Do not add a second dependabot.yml (dependabot skill)."
      status: unverified
    - statement: "Do not add the privileged PR-from-base trigger (D-02)."
      status: unverified
    - statement: "Do not trigger production deploy or a v* tag (D-34)."
      status: unverified
    - statement: "Do not change GitHub settings via API (D-06)."
      status: unverified
    - statement: "Do not add a new reporting SaaS (D-35)."
      status: unverified
    - statement: "Do not add a GitHub-hosted Prisma migrate job (D-22)."
      status: unverified
---

<objective>
D-33 logical commit 5: Dependabot caps, monthly Actions/Docker cadence, Nest major lockstep group (BRIEF-5, D-32).

Purpose: Cut Dependabot-driven CI fan-out (brief: ~22.5 percent of sampled runner time). Keep security updates on.

Output: .github/dependabot.yml only.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-RESEARCH.md
@.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-PATTERNS.md
@apps/backend/package.json
</context>

<tasks>

<task type="auto">
  <name>Lower caps and switch Actions/Docker to monthly</name>
  <files>.github/dependabot.yml</files>
  <read_first>
    - .github/dependabot.yml
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-PATTERNS.md (Dependabot Limits / cadence remainder)
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-RESEARCH.md (Dependabot remainder, Pitfall 5)
  </read_first>
  <action>
    Edit the single origin .github/dependabot.yml (D-32, BRIEF-5). Keep version: 2, labels, commit-message prefix chore, existing patch-minor groups, cargo glib ignore, docker node ignore >=25.

    npm: open-pull-requests-limit 5 (was 10); keep weekly monday 06:00 Europe/Berlin (day is valid on weekly).
    cargo: open-pull-requests-limit 3 (was 5); keep weekly 06:15 and glib ignore.
    github-actions: interval monthly, time "06:30", timezone Europe/Berlin, open-pull-requests-limit 3. Omit day.
    docker: interval monthly, time "06:45", timezone Europe/Berlin, open-pull-requests-limit 3, keep node >=25 ignore. Omit day.
    docker-compose: interval monthly, time "07:00", timezone Europe/Berlin, open-pull-requests-limit 3. Omit day.

    Do not set open-pull-requests-limit 0 (that would disable version updates). Do not add cooldown unless already present.
  </action>
  <acceptance_criteria>
    - npm block has open-pull-requests-limit: 5
    - cargo block has open-pull-requests-limit: 3
    - github-actions, docker, docker-compose each have interval: monthly and open-pull-requests-limit: 3
    - Those three monthly blocks have no day: key
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import yaml
d=yaml.safe_load(open('.github/dependabot.yml'))
by={}
for u in d['updates']:
    by[u['package-ecosystem']]=u
assert by['npm']['open-pull-requests-limit']==5
assert by['cargo']['open-pull-requests-limit']==3
for eco in ('github-actions','docker','docker-compose'):
    assert by[eco]['schedule']['interval']=='monthly'
    assert 'day' not in by[eco]['schedule']
    assert by[eco]['open-pull-requests-limit']==3
print('DEPENDABOT_CAPS_OK')
"</automated>
  </verify>
  <done>Caps and monthly cadence match the brief table (D-32, BRIEF-5).</done>
</task>

<task type="auto">
  <name>Add nestjs-major group before patch-minor</name>
  <files>.github/dependabot.yml</files>
  <read_first>
    - .github/dependabot.yml
    - apps/backend/package.json
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-PATTERNS.md (nestjs-major group)
  </read_first>
  <action>
    Under the npm updates.groups map, insert nestjs-major before npm-patch-minor (Dependabot first-match; D-32 adjacency). Fields: applies-to: version-updates, patterns: ["@nestjs/*"], update-types: ["major"]. Glob covers origin @nestjs/common, core, platform-express, swagger, terminus, cli, testing, bullmq.

    Keep existing npm-patch-minor catch-all after it. Keep security updates on (no applies-to that disables them). Keep glib and node ignores.
  </action>
  <acceptance_criteria>
    - npm groups contain nestjs-major with @nestjs/* and major
    - In the file, the nestjs-major key appears above npm-patch-minor
    - cargo still ignores glib
    - docker still ignores node >=25
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import yaml
d=yaml.safe_load(open('.github/dependabot.yml'))
npm=next(u for u in d['updates'] if u['package-ecosystem']=='npm')
keys=list(npm['groups'])
assert keys.index('nestjs-major') < keys.index('npm-patch-minor')
g=npm['groups']['nestjs-major']
assert '@nestjs/*' in g['patterns']
assert 'major' in g['update-types']
cargo=next(u for u in d['updates'] if u['package-ecosystem']=='cargo')
assert any(i.get('dependency-name')=='glib' for i in cargo.get('ignore',[]))
docker=next(u for u in d['updates'] if u['package-ecosystem']=='docker')
assert any(i.get('dependency-name')=='node' for i in docker.get('ignore',[]))
print('NEST_GROUP_OK')
"</automated>
  </verify>
  <done>Nest majors lockstep; ignores kept; first-match order correct (D-32).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dependabot PRs → ci.yml | Untrusted version bumps; still go through required check ci |
| Monthly Actions ecosystem | Fewer SHA-bump PRs; still must remain SHA-pinned in workflows (D-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-01 | Elevation of privilege | dependabot.yml | low | accept | Config only; no workflow trigger change. |
| T-04.4-02 | Tampering | Dependabot PRs | medium | mitigate | Required check ci still gates merge (D-01). |
| T-04.4-03 | Tampering | caches | low | accept | N/A. |
| T-04.4-04 | Tampering | action tags | high | mitigate | github-actions ecosystem still opens PRs that must keep SHA pins in YAML (D-03); humans review. |
| T-04.4-05 | Information disclosure | dependabot.yml | low | accept | No secrets. |
| T-04.4-06 | Elevation of privilege | workflow_run | high | mitigate | File does not add workflows. |
| T-04.4-07 | Tampering | GHCR latest | low | accept | Not this plan. |
| T-04.4-08 | Tampering | dual migrators | low | accept | Not this plan. |
| T-04.4-SC | Tampering | installs | high | accept | YAML config only; no npm install in this plan. |
</threat_model>

<verification>
Python asserts on dependabot.yml ecosystems, limits, monthly without day, nestjs-major before patch-minor, glib and node ignores.
</verification>

<success_criteria>
Dependabot matches brief table. D-33 commit 5 of 7.
</success_criteria>

## Artifacts this phase produces

- `.github/dependabot.yml` — caps, monthly Actions/Docker/Compose, nestjs-major

<output>
Create `.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-05-SUMMARY.md` when done.
D-33 commit 5 of 7: Dependabot. Single commit. Do not squash. Do not push.
</output>
