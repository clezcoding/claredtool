# Phase 04.4: GitHub Actions Optimization - Research

**Researched:** 2026-09-04
**Domain:** GitHub Actions CI/CD (fan-out, security workflows, desktop-release, Dependabot, GHCR/Coolify)
**Confidence:** HIGH (Ist-Stand + locked D-01–D-36); MEDIUM (runner-pin choice until staging test; Bake vs two-step)

<user_constraints>
## User Constraints (from CONTEXT.md)

### Locked Decisions
- **D-01:** Required aggregator check stays named exactly `ci`. Do not skip the whole `ci` workflow with workflow-level `paths` (pending-check trap).
- **D-02:** No `pull_request_target` for PR code. No `workflow_run` for desktop or backend deploy.
- **D-03:** Pin every external Action to a full commit SHA. No moving tags (`@v4`, `@main`, `@latest`).
- **D-04:** Signed desktop jobs keep `package-manager-cache: false`. No pnpm/Rust/`node_modules`/build-output caches on the signed path.
- **D-05:** `GITHUB_TOKEN` least privilege; unmentioned scopes fall to `none`. No secrets in logs, step summaries, or artifacts.
- **D-06:** Do not auto-change GitHub settings, Branch Protection, or Environments. End with a manual checklist. Do not trigger production deploys or signed releases during implementation.
- **D-07:** Carry forward 04.2: FaynoSync is updater SSOT (D-05/D-29 there); channel `stable` only — no Nightly in 4.4 (04.2 D-07); Windows NSIS + macOS `.app.tar.gz` updater artifacts (04.2 D-27/D-28).
- **D-08:** Add `.github/workflows/gitleaks.yml`: `push` only `main`, `pull_request` including drafts, optional `workflow_dispatch` for full-history scans. No weekly Gitleaks schedule. `fetch-depth: 0`, `persist-credentials: false`, concurrency with `cancel-in-progress: true`.
- **D-09:** After merge, founder adds **required** status check named `gitleaks` (separate from `ci`). No admin bypass. Cursor does not edit Branch Protection.
- **D-10:** Gitleaks findings **fail** the job (not `continue-on-error`). Upload SARIF to the Security tab; `security-events: write` only on that upload job/step.
- **D-11:** Add `.gitleaks.toml` empty (or defaults-only). Allowlist entries only with a proven false positive plus comment. No blanket fixture-path excludes.
- **D-12:** Manual checklist: enable GitHub Secret Scanning **and** Push Protection (repo is public; free). Does not replace Gitleaks.
- **D-13:** CodeQL and Zizmor/Actionlint stay **not** required while they are path-selective (skipped workflow = pending required check). Zizmor workflow is path-filtered; Actionlint runs in the same workflow, not a third PR-visible workflow. CodeQL: weekly full JS/TS+Rust; push/PR language-split jobs; draft PRs skip analyze jobs.
- **D-14:** Remove automatic signed Windows/macOS build + FaynoSync publish from `main`. Triggers: `v*` tags (production) and `workflow_dispatch` only. — **Reversibility:** costly — every `main` UI push currently signs two platforms; putting it back re-opens the cost/secret surface.
- **D-15:** `v*` tag = production publish. `workflow_dispatch` defaults to staging; production dispatch remains an explicit override (rebuild without a new tag). Tags and manual release runs are not cancelled by branch concurrency.
- **D-16:** On `v*` tags: create a GitHub Release with installers (MSI + DMG). FaynoSync still gets updater artifacts (NSIS `.exe`+`.sig`, macOS `.app.tar.gz`+`.sig`). Production **dispatch** (no tag) uploads FaynoSync only — no GH Release.
- **D-17:** Replace `windows-latest` / `macos-latest` with pinned GitHub-hosted generations after a manual staging test on **both** platforms. Exact labels from current GitHub docs (research). Comment the chosen generations in the workflow.
- **D-18:** Actions artifact retention: staging 3 days, release 14 days. GH Release assets stay until the release is deleted. `compression-level: 0` for already-compressed binaries; `if-no-files-found: error`.
- **D-19:** Staging matrix `fail-fast: true`. `v*` tag matrix `fail-fast: false`. Do not install `rustfmt`/`clippy` on the release toolchain.
- **D-20:** Prefer one BuildKit Bake for API + worker (shared graph/cache). If Bake makes tags, labels, or cache less clear or less safe: keep two `build-push` steps and document why. Identity stays immutable `sha-<7>` (API) and `sha-<7>-worker`. `linux/amd64`. `provenance: false` and `sbom: false` until Coolify attestation-index issues are gone.
- **D-21:** Introduce Trivy (not present today). One HIGH/CRITICAL scan only if the workflow **proves** API and worker RootFS layers match; otherwise fail or scan both. If the comparison costs more than the second scan, keep both scans. Never drop coverage for a theoretical saving.
- **D-22:** Do **not** add a GitHub-hosted Prisma migrate job. API container `CMD` (`prisma migrate deploy && node dist/main.js`) stays the only migrator. Worker does not migrate. Coolify `deploy` job stays. Document that multi-replica later needs exactly one external migrator (brief 6.6) — do not activate it now.
- **D-23:** Collapse the serial deploy matrix into one ordered job: API then worker. Capture previous tags; log both deploys; on failure roll back already-changed targets to previous tags. Keep `concurrency.group: coolify-nest-deploy` with `cancel-in-progress: false`.
- **D-24:** Stop pushing GHCR `latest`. Deploy and humans use SHA tags only. — **Reversibility:** costly — anything still pulling `:latest` will break; Coolify already pins SHA.
- **D-25:** After Coolify deploy, HTTP-check API `/health` or `/health/ready`; failure triggers D-23 rollback. Worker has no public host — do not invent a public worker health URL. Kuma remains the standing monitor.
- **D-26:** Keep `vars.COOLIFY_IMAGE_DEPLOY` kill switch. Image builds stay `main` + `workflow_dispatch` only — no PR images to GHCR.
- **D-27:** Clean redundant `backend-image.yml` path entries (`apps/backend/Dockerfile`, explicit tax-schema path) per brief 6.1. Keep the root lockfile trigger until an importer-accurate filter is proven.
- **D-28:** Remove the `github.ref == 'refs/heads/main' ||` exception on expensive `ci.yml` jobs. `changes` + aggregator `ci` always run; skipped children are OK; `failure`/`cancelled` fail the aggregator.
- **D-29:** Draft PRs: `pull_request` types include `ready_for_review`. Heavy jobs skip on drafts; `changes` + aggregator still succeed. `ready_for_review` starts path-selective heavy jobs.
- **D-30:** Split `desktop` filter into `desktop-web` vs `desktop-rust` (do not use `apps/desktop/**` as the web filter). New jobs `desktop-web` and `desktop-rust`. Consolidate `backend-unit` + `backend-e2e` into `backend-test` unless walltime rises >25% vs the slower of the two (then keep split, share setup via local composite). Filtered `pnpm install --frozen-lockfile`. Local `.github/actions/setup-pnpm-ci` only if ≥2 identical Node/pnpm setups remain; signed desktop must not use that cache-on composite.
- **D-31:** Unify PR/analysis concurrency to `group: ${{ github.workflow }}-${{ github.ref }}` + `cancel-in-progress: true` on `ci`, `codeql`, `gitleaks`, `zizmor` (labeler optional). Never cancel production deploys, tags, or manual releases. Add short `run-name`s (purpose + ref/PR or deploy target). No untrusted PR titles in shells.
- **D-32:** Dependabot per brief table: npm weekly open-PR cap 5; Cargo weekly cap 3; GitHub Actions / Docker / Docker Compose monthly cap 3; stagger times. Add Nest major lockstep group for co-updated `@nestjs/*`. Keep security updates on. Keep `glib` ignore and Node-25 block unless the underlying break is proven fixed.
- **D-33:** Ship as **one PR with seven logical commits**: (1) trigger/concurrency, (2) CI job split + filtered installs, (3) security workflows + Actionlint, (4) desktop-release, (5) Dependabot, (6) backend-image/deploy, (7) hardening/summaries/test reports. Linear history; no squash of those seven.
- **D-34:** Validate locally first (YAML parse, Actionlint, Zizmor, ShellCheck, relevant tests). GitHub scenario table (docs-only, draft, ready-for-review JS/TS, Rust-only, backend-only, workflow-YAML, docs-only `main`, etc.) only after an **explicit user push OK**. No production dispatch and no `v*` tag during this phase. Staging desktop dispatch + runner-pin test is **after** merge/approval, not inside the implementation PR.
- **D-35:** Step summaries via `$GITHUB_STEP_SUMMARY` (changed areas, skipped jobs, image tags, deploy/rollback) with no secrets. Test reports: native JUnit/coverage if the existing framework already supports it; upload verbose reports only on `failure()`; retain 3–7 days; no new reporting SaaS; extra reporter dependency only with a separate justification.
- **D-36:** Leave `labeler.yml` / `sync-labels.yml` behavior as-is (brief phase 9). Harden-Runner stays on signing/deploy jobs; `egress-policy: block` is out of this phase. Pin CI service images (Postgres/Redis) by digest once research has verified current official digests; keep the readable tag in a comment; Dependabot must still be able to bump them.

### Claude's Discretion
- Exact GitHub-hosted runner generation labels (`windows-2022` vs `2025`, `macos-14` vs `15`) after docs + staging test (D-17).
- Bake vs two-step after a real Bake attempt (D-20).
- Trivy Action SHA, RootFS-compare method, and whether one vs two scans wins on measured cost (D-21).
- Actionlint version/image digest; CodeQL `build-mode` for Rust only if current official docs support it.
- `/health` vs `/health/ready` for D-25 (use whichever the API already exposes as the deploy gate).
- Whether `.github/actions/setup-pnpm-ci` is warranted after the split (D-30).
- Nest major-group package glob within `@nestjs/*`.
- Step-summary wording and JUnit retention inside the 3–7 day window.
- Postgres/Redis digest values (must be fetched, never invented).

### Deferred Ideas (OUT OF SCOPE)
- Nightly signed desktop builds — out of 4.4; 04.2 D-07 stable-only.
- GitHub-hosted `prisma migrate deploy` when prod DB is reachable from GHA — brief 6.6, later.
- Harden-Runner `egress-policy: block` after 1–2 weeks of egress reports — brief explicit non-goal.
- CodeQL / Zizmor as required checks — only if they stop being path-skipped (otherwise pending trap).
- Multi-replica external migration runner — document only (D-22).
</user_constraints>

<phase_requirements>
## Phase Requirements

CI-OPT-01–06 are already shipped (quicks `260828-5c8`, `260828-mn3`). Do not re-plan them. SSOT is `docs/claredtool-github-actions-optimierungsauftrag.md`. REQUIREMENTS.md has no CI-OPT IDs.

| ID | Description | Research Support |
|----|-------------|------------------|
| CI-OPT-01 | Parallel path-filtered `ci.yml` + aggregator | **DONE** — keep `changes` + job named `ci`; extend filters |
| CI-OPT-02 | Rust cache on desktop **CI** | **DONE** on `ci.yml` `desktop-test`; **must not** return to signed `desktop-build` (D-04; origin already removed it) |
| CI-OPT-03 | Vitest file parallelism | **DONE** — do not touch `vitest.config.ts` unless adding built-in junit reporter |
| CI-OPT-04 | No signed build on `pull_request` | **DONE** — remainder is remove signed build from `main` push (D-14) |
| CI-OPT-05 | `v*` tags trigger FaynoSync publish | **DONE** — remainder is GH Release on tags only (D-16) |
| CI-OPT-06 | Explicit `pnpm` Tauri CLI, no marketplace Tauri wrappers | **DONE** — keep |
| BRIEF-1.1 | Path filters on `main` (drop `github.ref == 'refs/heads/main' \|\|`) | D-28 + pending-check trap (workflow-level `paths` forbidden on `ci`) |
| BRIEF-1.2 | Draft-PR gating + `ready_for_review` | D-29; default `pull_request` types omit `ready_for_review` |
| BRIEF-1.3 | Concurrency unify | D-31; never cancel tags/dispatch/deploy |
| BRIEF-1.4 | `run-name` | D-31; no PR titles in `run:` |
| BRIEF-2.1 | `desktop-web` / `desktop-rust` split | D-30; do not use `apps/desktop/**` as web filter |
| BRIEF-2.2 | `backend-test` consolidate + 25% walltime guard | D-30; keep `squawk` as separate job |
| BRIEF-2.3 | Filtered `pnpm install --frozen-lockfile` | Same syntax as origin Dockerfile |
| BRIEF-2.4 | Local `setup-pnpm-ci` if ≥2 identical setups | Discretion; signed path must not use it |
| BRIEF-3.1 | Gitleaks: `push` main-only, drafts, dispatch, concurrency, SARIF | **Modify** origin `gitleaks.yml`; do not recreate |
| BRIEF-3.2–3.3 | Zizmor path filter + Actionlint in same workflow | **Modify** origin `zizmor.yml` |
| BRIEF-3.4 | CodeQL path + language-split + draft skip | **Modify** origin `codeql.yml`; Rust `build-mode: none` supported |
| BRIEF-4.* | Signed desktop only `v*` + dispatch; GH Release; pin runners | D-14–D-19 |
| BRIEF-5 | Dependabot caps, monthly, Nest major group | D-32; `day` is weekly-only |
| BRIEF-6 | Backend Bake-or-keep, Trivy, remove skip-migrate, one deploy job, drop `latest` | Origin already has Trivy×2, health/ready, PREV_TAG; remainder is cleanup |
| BRIEF-7 | `persist-credentials: false`, service digests, permissions | D-36 digest pin caveat: Dependabot does not bump GHA `services.*.image` |
| BRIEF-8 | Step summaries + native test reports, no SaaS | D-35; Jest/Vitest built-in reporters only |
</phase_requirements>

## Summary

Phase 04.4 implements the remainder of `docs/claredtool-github-actions-optimierungsauftrag.md` after CI-OPT-01–06. **Ist-Stand for planning is `origin/main` at `1419b31c029ef68b2c1f64402264b3a77d2ad1b1`, not the local `gsd/phase-04.4-…` working tree.** That branch is missing commits `#92`–`#98` (gitleaks/zizmor/codeql, Harden-Runner, Trivy, `package-manager-cache: false`, skip-migrate job, `/health/ready` rollback, squawk). Wave 0 must merge or rebase `origin/main` before any YAML edit.

On `origin/main`, several brief items are **already shipped**: Harden-Runner `egress-policy: audit` on CI/signing/deploy; signed desktop has `package-manager-cache: false` and no rust-cache; Trivy HIGH/CRITICAL on both images; SBOM + keyless cosign (keep — do not strip); deploy already GETs previous Coolify tag and polls `https://clared-api.puzzlessdev.online/health/ready`; gitleaks/zizmor/codeql workflow files exist. Remainder is fan-out (main exception, drafts, job split), tightening those three security workflows, removing signed `main` desktop, Dependabot caps, dropping GHCR `latest`, deleting the skip-only GHA migrate job (API `CMD` stays the migrator), collapsing the deploy matrix, checkout/digest/summaries.

**Primary recommendation:** Merge `origin/main` first. Then seven commits (D-33) against that tree. Required check stays `ci`. Do not add `pull_request_target` or `workflow_run`. Pin every `uses:` to a 40-char SHA. Do not push, dispatch production, or tag `v*` during implementation.

## Architectural Responsibility Map

| Capability | Primary Tier | Secondary Tier | Rationale |
|------------|-------------|----------------|-----------|
| Path-filtered CI aggregator named `ci` | GitHub Actions (CI) | — | Required status check lives in GHA; app code does not own merge gates |
| Draft / `ready_for_review` gating | GitHub Actions (CI) | — | `pull_request` event types + `github.event.pull_request.draft` |
| Desktop-web typecheck/build/tests | GitHub Actions (CI) | Browser/Client (sources only) | Vitest/tsc run on ubuntu; no signed artifacts |
| Desktop-rust fmt/clippy/test | GitHub Actions (CI) | — | Cargo on ubuntu-24.04 with Tauri Linux deps |
| Signed Win/macOS Tauri + FaynoSync | GitHub Actions (release) | FaynoSync (external) | Secrets + Apple/Win signing; triggers `v*` + dispatch only |
| GitHub Release (MSI/DMG) | GitHub Actions (release) | GitHub Releases API | Tags only; `contents: write` on that job |
| Secret scan (Gitleaks) | GitHub Actions (security) | GitHub code scanning | Job name `gitleaks`; SARIF upload |
| Workflow lint (Actionlint + Zizmor) | GitHub Actions (security) | — | Path-filtered; not required checks |
| CodeQL JS/TS + Rust | GitHub Actions (security) | GitHub code scanning | Path + language split; weekly full |
| GHCR image build + Trivy | GitHub Actions (CD) | GHCR | `main` + dispatch; no PR images |
| Coolify deploy + health/rollback | GitHub Actions (CD) | Coolify API + API `/health/ready` | Keep `coolify-nest-deploy` concurrency |
| Dependabot version PRs | GitHub Dependabot | npm/Cargo/GHCR ecosystems | YAML only; security updates stay on |
| Branch Protection / Secret Scanning | GitHub Settings (human) | — | Manual checklist (D-06/D-09/D-12) |
| Prisma migrate in production | API container CMD | — | Do not run from GHA (D-22) |

## Standard Stack

### Core (already in `origin/main` — reuse these SHAs)

| Library / Action | Version / SHA | Purpose | Why Standard |
|------------------|---------------|---------|--------------|
| `actions/checkout` | `3d3c42e5aac5ba805825da76410c181273ba90b1` `# v7.0.1` | Checkout | Already pinned |
| `pnpm/action-setup` | `0977fd99725f1db4007ccb2928dbb4e90d06cc86` `# v6.0.10` | pnpm bootstrap | Origin SHA (local branch is stale `ff378ebe…`) |
| `actions/setup-node` | `820762786026740c76f36085b0efc47a31fe5020` `# v7.0.0` | Node 24 | `package-manager-cache: false` on signed jobs `[VERIFIED: /actions/setup-node]` |
| `dorny/paths-filter` | `ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d` `# v4.0.3` | `changes` job | Existing |
| `dtolnay/rust-toolchain` | `6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772` | Rust 1.97.1 | Existing; omit `components` on signed jobs |
| `swatinem/rust-cache` | `6323deb102c322ba6fcbdcafc7e3dddab59af2b6` `# v2.9.2` | CI rust cache only | Origin SHA; **never** on signed desktop |
| `step-security/harden-runner` | `e14015d583714f6e62063499dc959a02595150a1` `# v2.21.1` | Egress audit | Already on origin signing/deploy/CI `[VERIFIED: gh api this session]` |
| `gitleaks/gitleaks-action` | `e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e` `# v3.0.0` | Secret scan | Already on origin; keep SHA |
| `zizmorcore/zizmor-action` | `6fc4b006235f201fdab3722e17240ab420d580e5` `# v0.6.1` | GHA static analysis | Already on origin `[VERIFIED: Context7 /zizmorcore/zizmor]` |
| `github/codeql-action/{init,autobuild,analyze,upload-sarif}` | `cdf488f595d80d6e07e03d4674febd5ab45fa938` `# v4.37.9` | CodeQL + SARIF | Already on origin codeql.yml |
| `aquasecurity/trivy-action` | `ed142fd0673e97e23eac54620cfb913e5ce36c25` `# v0.36.0` | Image scan | Already on origin `[VERIFIED: gh peel this session]` |
| `docker/build-push-action` | `53b7df96c91f9c12dcc8a07bcb9ccacbed38856a` `# v7.3.0` | GHCR push | Existing |
| `docker/metadata-action` | `dc802804100637a589fabce1cb79ff13a1411302` `# v6.2.0` | Tags | Existing |
| `actions/upload-artifact` | `043fb46d1a93c77aae656e7c1c64a875d1fc6a0a` `# v7.0.1` | Bundles | `compression-level` / `retention-days` / `if-no-files-found` `[CITED: github.com/actions/upload-artifact]` |
| pnpm | `11.15.1` (root `packageManager`) | Filtered installs | `[VERIFIED: package.json:6]` quote: `"packageManager": "pnpm@11.15.1"` |
| Node | `24` | CI + images | Origin WR-05 |
| Rust | `1.97.1` | Desktop | `[VERIFIED: apps/desktop/src-tauri/rust-toolchain.toml:1-3]` quote: `channel = "1.97.1"` / `components = ["rustfmt", "clippy"]` |

### Supporting (add in this phase)

| Library / Action | Version / SHA | Purpose | When to Use |
|------------------|---------------|---------|-------------|
| `softprops/action-gh-release` | `efb35369e0ad2afab669f228072c1b0d510eae64` `# v3.0.3` | GH Release on `v*` | Tag-only job; `fail_on_unmatched_files: true` `[VERIFIED: gh peel this session]` |
| `docker/bake-action` | `d3418bd7d0e9324001bca92fa8ba175ea7e6dc9b` `# v7.3.0` | Optional Bake spike | Only if Bake attempt keeps tags/digests/cosign clear `[VERIFIED: gh api this session]` |
| Actionlint image | `rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667` | Workflow lint | Pin digest, **not** `:latest`; no `curl \| bash` `[VERIFIED: docker buildx this session]` `[CITED: github.com/rhysd/actionlint docs/usage.md]` |
| CI Postgres | `postgres:16-alpine@sha256:cf78e76683b9ca8c5733cbbdce6c9262b45b6767934dd0a95e671f9a0fc20685` | `backend-test` / `squawk` services | Keep **16** (CI today); local compose is 18 — do not silently bump `[VERIFIED: docker buildx this session]` |
| CI Redis | `redis:7-alpine@sha256:ff02b58f971e7d7d156a1267e283fcbbeee91773b6aa36c49dac28ecfe28eadf` | `backend-test` services | Keep **7** (CI today); local compose is 8 `[VERIFIED: docker buildx this session]` |

Re-verify image digests at execute time — they rotate. Comment the readable tag beside the digest.

### Alternatives Considered

| Instead of | Could Use | Tradeoff |
|------------|-----------|----------|
| `gitleaks/gitleaks-action` | Official `gitleaks/gitleaks` container CLI | CLI needs no org license and emits SARIF cleanly; action is already SHA-pinned and running. Keep action for fail-the-job; add `upload-sarif` for D-10. `[CITED: github.com/gitleaks/gitleaks-action]` `GITLEAKS_LICENSE` required for **organizations**, not user accounts |
| Bake | Keep two `build-push` | Origin already has two digest outputs for cosign + two Trivy scans. Bake is optional spike (D-20); default fallback = keep two-step |
| RootFS compare + one Trivy | Keep both Trivy scans | Both scans already exist; compare is extra complexity. Keep both (D-21) |
| `jest-junit` npm | Jest `--reporters=github-actions` + Vitest built-in `junit` | No new reporting SaaS or lockfile dep (D-35) |
| `windows-2025` / `macos-14` | `windows-2022` + `macos-15` | See D-17 recommendation below |

**Installation:** no new npm/Cargo packages. Actions are `uses:` SHA pins only.

**Version verification:** `gh api repos/<owner>/<repo>/git/ref/tags/<tag>` (peel annotated tags) this session. `npm view` N/A.

## Package Legitimacy Audit

No new registry packages (npm/PyPI/crates) in this phase.

| Package | Registry | Age | Downloads | Source Repo | Verdict | Disposition |
|---------|----------|-----|-----------|-------------|---------|-------------|
| — | — | — | — | — | — | No installs |

**Packages removed due to [SLOP] verdict:** none
**Packages flagged as suspicious [SUS]:** none

`squawk-cli@2.64.0` is already `npm install -g` on origin `ci.yml` squawk job — do not add it to the lockfile; do not expand.

## Architecture Patterns

### System Architecture Diagram

```text
[push main / pull_request / ready_for_review]
        |
        v
   ci.yml (ALWAYS runs; NO workflow-level paths)
        |
        +-- changes (dorny/paths-filter) --> outputs
        |
        +-- [if not draft AND path] desktop-web
        +-- [if not draft AND path] desktop-rust
        +-- [if not draft AND path] backend-test (postgres+redis)
        +-- [if not draft AND path] squawk (keep; origin G5)
        |
        v
   aggregator job name exactly "ci"
        if always(); fail on failure|cancelled; skipped children OK

[push main only + pull_request incl drafts]
        gitleaks.yml --> job name "gitleaks" --> fail on leak + SARIF

[.github/workflows|actions|dependabot.yml]
        zizmor.yml --> actionlint (digest image) + zizmor-action

[js/ts/rs/manifests OR weekly cron]
        codeql.yml --> detect --> analyze-js-typescript | analyze-rust
        draft PR: skip analyze jobs (workflow still runs detect or is path-skipped)

[v* tag | workflow_dispatch]
        desktop-build.yml --> tauri (pinned OS) --> FaynoSync
        v* only --> GH Release (MSI+DMG)

[push main paths | workflow_dispatch]
        backend-image.yml --> build (sha- tags, no latest) --> Trivy
                         --> deploy (API then worker, health/ready, rollback)
                         NO GHA migrate job
```

### Recommended Project Structure

```
.github/
├── actions/
│   └── setup-pnpm-ci/action.yml   # only if ≥2 identical Node/pnpm CI setups remain
├── workflows/
│   ├── ci.yml                     # aggregator name: ci
│   ├── gitleaks.yml               # modify origin file
│   ├── zizmor.yml                 # + actionlint
│   ├── codeql.yml                 # language-split
│   ├── desktop-build.yml          # no main push
│   ├── backend-image.yml          # no latest, no migrate job
│   ├── labeler.yml                # UNCHANGED
│   └── sync-labels.yml            # UNCHANGED
├── dependabot.yml
.gitleaks.toml                     # empty / extend defaults only
```

### Pattern 1: Job-level path filter (required check)

**What:** Workflow always starts; expensive jobs `if:` on `changes` outputs.
**When to use:** Any required check (`ci`, later `gitleaks` if always-on).
**Do not:** `on.push.paths` / `on.pull_request.paths` on the `ci` workflow.

DATA_k7m2q9xp_START
```yaml
# Source: https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax
# If a workflow is skipped due to path filtering, checks remain Pending
# and block merge of required checks.
```
DATA_k7m2q9xp_END

`[VERIFIED: Context7 /websites/github_en_actions]`

### Pattern 2: Draft skip without skipping aggregator

DATA_b4n8w1ce_START
```yaml
on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

# heavy job:
if: |
  (github.event_name != 'pull_request' || github.event.pull_request.draft == false) &&
  (needs.changes.outputs.desktop-web == 'true' || needs.changes.outputs.shared == 'true')
```
DATA_b4n8w1ce_END

Default `pull_request` types are `opened`, `synchronize`, `reopened` only — **`ready_for_review` must be listed** `[VERIFIED: Context7 /websites/github_en_actions]`.

Skipped jobs report success for required checks; the aggregator `if: always()` plus `failure`/`cancelled` checks is what fails `ci` `[VERIFIED: Context7 /websites/github_en_actions]` ("A skipped job reports its status as Success").

### Pattern 3: Concurrency

DATA_p9r3t6ak_START
```yaml
# PR/analysis workflows:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# tags / workflow_dispatch / coolify-nest-deploy:
cancel-in-progress: false
```
DATA_p9r3t6ak_END

`[VERIFIED: Context7 /websites/github_en_actions]` Official pattern is `group: ${{ github.workflow }}-${{ github.ref }}`. Origin `ci.yml` currently uses `ci-${{ github.workflow }}-${{ github.ref }}` — drop the extra `ci-` prefix (D-31).

### Pattern 4: Least-privilege permissions

Omitting a scope after any `permissions:` key sets that scope to `none` `[VERIFIED: Context7 /websites/github_en_actions]`.

| Workflow / job | Write scopes |
|----------------|--------------|
| `ci.yml` | none (`contents: read` only) |
| `gitleaks` scan | `contents: read`; upload-sarif step/job: `security-events: write` |
| `zizmor` | `security-events: write` + `contents: read` + `actions: read` (already) |
| CodeQL analyze | `security-events: write` + `actions: read` + `contents: read` |
| backend build | `packages: write`; keep `id-token: write` for existing cosign |
| GH Release job | `contents: write` on that job only |
| FaynoSync publish | `contents: read` (external API) |
| Coolify deploy | `contents: read` |
| labeler | leave as-is (`pull-requests: write`) |

Audit origin `backend-image` `actions: write` on build (needed for GHA cache?). If unused, drop it in commit 7.

### Anti-Patterns to Avoid

- **Workflow-level `paths` on `ci`:** pending required check. Use job `if:`.
- **`pull_request_target` / `workflow_run` for deploy:** D-02.
- **`uses: …@v4` / `@latest` / `@master` as the ref:** SHA only (comment may show tag).
- **`cache: pnpm` or rust-cache on signed `tauri` job:** D-04. Origin already correct.
- **`apps/desktop/**` as the web filter:** Rust changes would start web jobs (D-30).
- **Required CodeQL/Zizmor** while path-skipped (D-13).
- **Untrusted `${{ github.event.pull_request.title }}` in `run:`:** expression injection `[CITED: gha-security-review skill]`. Put attacker-controlled strings in `env:` then quote in shell, or omit.
- **Pushing `:latest`:** D-24. Origin still has `type=raw,value=latest,enable=main` — remove that line.
- **New reporting SaaS / `jest-junit` package** without extra justification (D-35).

## Don't Hand-Roll

| Problem | Don't Build | Use Instead | Why |
|---------|-------------|-------------|-----|
| Path detection | Custom `git diff` scripts | `dorny/paths-filter` (already pinned) | PR base/head semantics |
| Secret scanning | Custom regex job | `gitleaks/gitleaks-action` + `.gitleaks.toml` | History-aware; already running |
| Workflow lint | Ad-hoc yamllint only | Actionlint digest image + zizmor-action | Expressions, shell, matrix |
| Code scanning | Homegrown SAST | `github/codeql-action` | SARIF + Security tab |
| Image vulns | Skip or one unproven scan | Existing Trivy×2 | D-21 coverage |
| GH Release | Manual `curl` to releases API | `softprops/action-gh-release` SHA | Asset upload + unmatched-file fail |
| Node/pnpm setup | Copy-paste 4 times | Local composite **if** ≥2 CI twins | Signed jobs excluded |
| JUnit SaaS | Codecov/Datadog | Native Jest/Vitest reporters | D-35 |
| Coolify from GHA | MCP/CLI inside Actions | Keep existing `curl` PATCH+POST | GHA cannot use Cursor MCP; existing pattern |

**Key insight:** Cost savings come from **not starting jobs**, not from rewriting tools. Origin already has the security baseline; this phase is trigger/fan-out/release-frequency.

## Runtime State Inventory

Omitted — not a rename/refactor/migration of product identifiers. (Workflow job *names* change: `lint`/`desktop-test` → `desktop-web`/`desktop-rust`; `backend-unit`/`backend-e2e` → `backend-test`. Branch Protection required check stays `ci`. New required check `gitleaks` is **manual after merge**.)

| Category | Items Found | Action Required |
|----------|-------------|------------------|
| Stored data | None for this phase | — |
| Live service config | Branch Protection required check `ci`; CodeQL/Gitleaks/Zizmor not required | Manual: add required `gitleaks` after merge (D-09). Do not API-edit |
| OS-registered state | None | — |
| Secrets/env vars | `TAURI_SIGNING_PRIVATE_KEY`, `FAYNOSYNC_CI_TOKEN`, `COOLIFY_TOKEN`, `vars.COOLIFY_*`, optional `GITLEAKS_LICENSE` if `clezcoding` is an org | Do not rotate. Checklist: confirm whether org license is needed |
| Build artifacts | GHCR `:latest` still pushed from origin | Stop new `:latest` pushes (D-24); do not delete old tags in this phase |

## Common Pitfalls

### Pitfall 1: Pending required check
**What goes wrong:** Docs-only PR cannot merge; `ci` stays Pending.
**Why:** Workflow-level `paths` skips the whole workflow.
**How to avoid:** Never put `paths` on `ci.yml` `on:`. Job-level `if:` only.
**Warning signs:** GitHub "Expected — Waiting for status to be reported".

### Pitfall 2: `ready_for_review` never starts heavy jobs
**What goes wrong:** Draft converted to Ready does nothing.
**Why:** Default `pull_request` types omit `ready_for_review` `[VERIFIED: Context7 /websites/github_en_actions]`.
**How to avoid:** Explicit `types: [opened, synchronize, reopened, ready_for_review]`.
**Warning signs:** Ready PR shows only `changes` + `ci` with skipped children forever.

### Pitfall 3: Aggregator `needs` list stale after rename
**What goes wrong:** `ci` succeeds while new jobs fail, or YAML error on missing needs.
**Why:** Origin aggregator lists `lint`, `desktop-test`, `backend-unit`, `backend-e2e`, `squawk`.
**How to avoid:** Update `needs` + `check()` names in the same commit as the split. Keep `squawk`.

### Pitfall 4: rust-toolchain.toml reinstalls clippy on release
**What goes wrong:** D-19 violated even if workflow omits `components:`.
**Why:** `[VERIFIED: apps/desktop/src-tauri/rust-toolchain.toml:1-3]` includes `components = ["rustfmt", "clippy"]`.
**How to avoid:** On signed jobs set `dtolnay/rust-toolchain` `components: ""` (empty) or do not use the toml components — verify with a dry log after first staging run (D-34: after merge).

### Pitfall 5: Dependabot `day` on monthly
**What goes wrong:** Invalid or ignored config.
**Why:** `day` is for **weekly** only; `monthly` runs on the first of the month `[VERIFIED: Context7 /websites/github_en]` `[CITED: docs.github.com dependabot-options-reference]`.
**How to avoid:** `interval: monthly` + `time` + `timezone` only (no `day`). Stagger `time`.

### Pitfall 6: GHA service-image digests vs Dependabot
**What goes wrong:** Digests rot; Dependabot never bumps `jobs.*.services.*.image`.
**Why:** Dependabot `docker` / `docker-compose` / `github-actions` do not edit workflow `services:` blocks.
**How to avoid:** Pin digest + comment tag; add manual checklist item (quarterly). Do not pretend Dependabot will bump `ci.yml` services. Optional later: document in UPDATER-OPS. Honest conflict with D-36 wording — treat as documented limitation + human bump.

### Pitfall 7: Gitleaks SARIF "resolved" false sense
**What goes wrong:** Secret removed in a later commit looks fixed in Security tab but remains in history.
**Why:** gitleaks-action FAQ `[CITED: github.com/gitleaks/gitleaks-action]` does not recommend code scanning for this reason.
**How to avoid:** Still implement D-10 (locked). Job **fail** is the merge gate; SARIF is extra. Rotate leaked secrets. Note in manual checklist.

### Pitfall 8: Planning against local workflows
**What goes wrong:** Recreate gitleaks/codeql/zizmor; fight SHA drift; drop squawk/cosign/Trivy.
**Why:** Local `gsd/phase-04.4-*` branch does not contain origin `#92`–`#98`.
**How to avoid:** Wave 0 merge `origin/main` (`1419b31`).

### Pitfall 9: Expression injection in `run-name` vs `run:`
**What goes wrong:** PR title in a `run:` script is shell injection.
**Why:** `run-name` is display-only (safe-ish); `run:` interpolates into bash.
**How to avoid:** `run-name: CI · PR #${{ github.event.pull_request.number }} · ${{ github.head_ref }}` (ref, not title). Never `github.event.pull_request.title` in `run:`.

## Code Examples

### Filtered pnpm install (already in origin Dockerfile)

`[VERIFIED: origin/main apps/backend/Dockerfile]` verbatim:

```
RUN pnpm install --frozen-lockfile --filter @clared/tax-engine... --filter ./apps/backend...
```

Use in CI:

```yaml
# backend-test
- run: pnpm install --frozen-lockfile --filter @clared/tax-engine... --filter ./apps/backend...

# desktop-web
- run: pnpm install --frozen-lockfile --filter ./apps/desktop...
```

`--filter ./apps/desktop...` pulls `@clared/ui` via workspace. `--frozen-lockfile` remains mandatory (brief 2.3).

### desktop-web vs desktop-rust filters (do not use `apps/desktop/**` for web)

```yaml
desktop-web:
  - 'apps/desktop/src/**'
  - 'apps/desktop/index.html'
  - 'apps/desktop/package.json'
  - 'apps/desktop/vite.config.ts'
  - 'apps/desktop/vitest.config.ts'
  - 'apps/desktop/tsconfig.json'
  - 'apps/desktop/tsconfig.node.json'
  - 'apps/desktop/scripts/**'
  - 'packages/ui/**'
desktop-rust:
  - 'apps/desktop/src-tauri/**'
  - 'apps/desktop/src-tauri/rust-toolchain.toml'
shared:  # both + backend as today
  - 'package.json'
  - 'pnpm-lock.yaml'
  - 'pnpm-workspace.yaml'
  - '.npmrc'
  - '.github/workflows/ci.yml'
```

`build.rs` reads `tauri.conf.json` via `serde_json` only — **no Node** `[VERIFIED: apps/desktop/src-tauri/build.rs:1-41]`. Skip pnpm/Node on `desktop-rust` unless a local `cargo test` proves otherwise (brief 2.1).

### setup-node signed vs CI

```yaml
# signed desktop (origin already):
- uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
  with:
    node-version: 24
    package-manager-cache: false

# normal CI:
- uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
  with:
    node-version: 24
    cache: pnpm
```

`[VERIFIED: /actions/setup-node]` `package-manager-cache: false` disables default npm caching.

### Gitleaks remainder (modify origin file)

Origin today (verbatim from `1419b31` `.github/workflows/gitleaks.yml`):

```
on:
  push:
  pull_request:
```

Change to:

```yaml
name: gitleaks
on:
  push:
    branches: [main]
  pull_request:  # includes drafts by default
  workflow_dispatch:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
permissions: {}
jobs:
  gitleaks:
    name: gitleaks
    # keep fetch-depth: 0, persist-credentials: false, existing action SHA
    permissions:
      contents: read
      security-events: write  # only because this job uploads SARIF (D-10)
```

Add `github/codeql-action/upload-sarif@cdf488f595d80d6e07e03d4674febd5ab45fa938` with `category: gitleaks` after producing `gitleaks.sarif`. If the action does not write SARIF to the workspace, add a digest-pinned `gitleaks/gitleaks` CLI step `--report-format sarif --report-path gitleaks.sarif` **in addition** (still fail the job on leaks). Do not `continue-on-error`.

### CodeQL language split

Origin matrix (verbatim): `language: ["javascript-typescript", "rust"]` with `autobuild`.

Replace with two jobs `analyze-js-typescript` and `analyze-rust`, `fail-fast` independence preserved. Detect job sets outputs; schedule/dispatch forces both true; draft skips analyze.

Rust: official table lists **None** as default-setup for Rust `[CITED: docs.github.com codeql-for-compiled-languages]` "Used by default setup … Yes (C/C++, C#, Java and Rust)". Discretion: set `build-mode: none` on `analyze-rust` to skip cargo autobuild cost; if the first scheduled run is empty/weak, revert that job to autobuild (already in origin). JS/TS: `build-mode: none`.

Workflow-level `paths` **allowed** on CodeQL (not a required check) (D-13).

### Dependabot remainder

```yaml
# npm: open-pull-requests-limit: 5 (was 10); keep weekly monday 06:00 Europe/Berlin
# cargo: limit 3 (was 5)
# github-actions / docker / docker-compose:
schedule:
  interval: monthly
  time: "06:30"   # stagger 06:30 / 06:45 / 07:00
  timezone: Europe/Berlin
open-pull-requests-limit: 3
# NO day: on monthly

groups:
  nestjs-major:
    applies-to: version-updates
    patterns: ["@nestjs/*"]
    update-types: ["major"]
  docker-patch-minor:
    update-types: [patch, minor]
    patterns: ["*"]
```

Keep glib ignore and `node` `>=25` `[VERIFIED: .github/dependabot.yml:40-45 and 91-94]` (local file matches origin). Origin npm still `limit: 10`.

Nest packages on backend `[VERIFIED: apps/backend/package.json:16-21,42-43]`: `@nestjs/bullmq` `12.0.0`; `@nestjs/common|core|platform-express` `11.2.3`; `@nestjs/swagger` `11.4.7`; `@nestjs/terminus` `11.1.1`; `@nestjs/cli` `11.0.24`; `@nestjs/testing` `11.2.3`. Glob `@nestjs/*` is correct; bullmq already on 12.

### Desktop-release remainder

Remove `on.push.branches: [main]` (keep `tags: ['v*']` + `workflow_dispatch`). Drop publish `if` branch `github.ref == 'refs/heads/main' && github.event_name == 'push'`.

```yaml
strategy:
  fail-fast: ${{ github.event_name == 'workflow_dispatch' && !startsWith(github.ref, 'refs/tags/v') }}
  matrix:
    os: [windows-2022, macos-15]  # after D-17 staging test; comment in YAML
```

Public-repo labels `[VERIFIED: Context7 + wigolo fetch docs.github.com/en/actions/reference/runners/github-hosted-runners]`: `windows-latest, windows-2025, windows-2022` and `macos-latest, macos-14, macos-15, macos-26`. **Recommend `windows-2022` + `macos-15`** (freeze current macos-latest ARM generation; Windows LTS-like 2022). Staging test after merge (D-34/D-17) — if Tauri/signing fails, swap to `windows-2025` / `macos-14`.

Artifacts:

```yaml
compression-level: 0
retention-days: ${{ startsWith(github.ref, 'refs/tags/v') && 14 || 3 }}
if-no-files-found: error
```

`[CITED: github.com/actions/upload-artifact]` compression 0 = no compression; default 6.

GH Release job (`v*` only): `softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64` with MSI+DMG globs; `fail_on_unmatched_files: true`. `permissions: contents: write` on that job. Production dispatch: FaynoSync only.

### Backend remainder

1. Delete redundant paths `apps/backend/Dockerfile` and explicit tax-schema (covered by `apps/backend/**` and `packages/tax-engine/**`).
2. Remove `type=raw,value=latest,enable=…` from metadata-action; keep `flavor: latest=false`.
3. **Delete `migrate` job**; `deploy.needs: [build]` only. API CMD stays `[VERIFIED: origin/main apps/backend/Dockerfile]` `CMD ["sh", "-c", "pnpm exec prisma migrate deploy && node dist/main.js"]`. Comment: multi-replica needs one external migrator later (brief 6.6) — do not activate.
4. Collapse matrix into one job: API then worker; reuse origin GET `docker_registry_image_tag` + `/health/ready` poll + rollback. Worker: Coolify app status `running` (origin already; no public URL).
5. Keep both Trivy scans (already). Keep SBOM+cosign. Keep `provenance: false` / `sbom: false` on buildx.
6. Bake: optional spike; **default keep two `build-push`** because origin already threads `DIGEST_API` / `DIGEST_WORKER` into cosign. If Bake attempt loses digest outputs, abort Bake (D-20).

Health gate: **`/health/ready`** `[VERIFIED: apps/backend/src/health/health.controller.ts:16-48]` — `GET /health` returns `{ status: "ok" }`; `GET ready` pings postgres+redis (+ optional Gotenberg). Origin deploy already polls `https://clared-api.puzzlessdev.online/health/ready`.

### persist-credentials

Default is `true` `[VERIFIED: Context7 /actions/checkout]`. Origin only sets `persist-credentials: false` on squawk + gitleaks/zizmor/codeql. Add to every checkout that does not `git push` (none do).

### Step summary / tests

```bash
echo "desktop-web: ${{ needs.changes.outputs.desktop-web }}" >> "$GITHUB_STEP_SUMMARY"
```

`[VERIFIED: Context7 /websites/github_en_actions]` `$GITHUB_STEP_SUMMARY`.

Jest: `pnpm --filter ./apps/backend test -- --reporters=default --reporters=github-actions` (built-in). Vitest: `--reporter=default --reporter=junit --outputFile=junit.xml` (built-in, no new dep). Upload XML only `if: failure()`; `retention-days: 5`.

## State of the Art

| Old Approach (local gsd branch / CONTEXT) | Current Approach (`origin/main` 1419b31) | When Changed | Impact |
|-------------------------------------------|------------------------------------------|--------------|--------|
| No gitleaks/zizmor/codeql in tree | Those three workflows exist | #92 | Phase 3 = **edit**, not create |
| Signed desktop `cache: pnpm` + rust-cache | `package-manager-cache: false`; no rust-cache | #95–#96 | D-04 already satisfied on origin |
| No Trivy / Harden-Runner | Both present | #92–#93 | Do not re-add; do not strip |
| No GHA migrate job | Skip-only migrate job exists | #76 | **Delete it** (D-22) |
| Deploy matrix without health | PREV_TAG + `/health/ready` poll | origin | Collapse to one job; keep logic |
| CONTEXT: "no `.github/actions/`" | Still none | — | Composite still optional (D-30) |
| CodeQL Rust = autobuild only | Docs: `none` valid for Rust default setup | current GitHub docs | Prefer `build-mode: none` |

**Deprecated/outdated:**
- CONTEXT.md "Existing Code Insights" claiming no gitleaks/codeql/zizmor and no Harden-Runner — **stale vs origin/main**.
- `windows-latest` / `macos-latest` as long-term pins (D-17).
- GHA `migrate` job as "scale-out path" while CMD still migrates — dual migrators (brief 6.4 forbids).

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|-------|---------|---------------|
| A1 | `clezcoding` may be a GitHub **organization** requiring `GITLEAKS_LICENSE` | Gitleaks | Action starts failing after trigger tightening; founder adds free starter license (manual) or switch scan step to CLI image |
| A2 | Actionlint `1.7.12` image digest stays valid through execute | Actionlint | Re-inspect digest at execute; pin whatever current 1.7.x is |
| A3 | Postgres/Redis **index** digests fetched 2026-09-04 stay valid | Services | Re-run `docker buildx imagetools inspect` at execute |
| A4 | Empty `components: ""` on dtolnay overrides rust-toolchain.toml components | Desktop-release | Staging log still installs clippy — then use a release-only toolchain override |
| A5 | Bake attempt may be abandoned | D-20 | Two-step remains; document in commit 6 |

## Open Questions (RESOLVED)

1. **Is `clezcoding` an organization?** RESOLVED
   - What we know: gitleaks-action requires license for orgs `[CITED: github.com/gitleaks/gitleaks-action]`. Origin workflow does not set `GITLEAKS_LICENSE` and still ran (brief metrics).
   - What's unclear: account type.
   - Recommendation: manual checklist item; do not invent a secret in YAML.
   - Resolution: Plan 07 writes this as a founder item in `04.4-MANUAL-GITHUB.md`. No `GITLEAKS_LICENSE` invented in YAML.

2. **`actions: write` on backend build** RESOLVED
   - What we know: origin sets it with `id-token: write` (cosign) and `packages: write`.
   - Recommendation: commit 7 audit; drop if unused.
   - Resolution: Plan 07 task 1 audits backend-image build permissions and drops `actions: write` if unused.

3. **Bake** RESOLVED
   - Locked to try; recommend keep two-step if cosign digests get harder.
   - Planner: one spike task with explicit abort criteria.
   - Resolution: Plan 06 task 3 is the Bake spike with abort criteria (default keep two-step; DIGEST_API and DIGEST_WORKER must still feed cosign).

## Environment Availability

| Dependency | Required By | Available | Version | Fallback |
|------------|------------|-----------|---------|----------|
| Node | pnpm tests | ✓ | v26.8.1 (local); CI uses 24 | CI image |
| pnpm | filtered install | ✓ | 11.15.1 | — |
| Docker | digest inspect, Actionlint image | ✓ | 29.7.2 | — |
| rustc | local cargo fmt/clippy/test | ✓ | 1.97.1 | — |
| `gh` | SHA resolve | ✓ | 2.98.0 | — |
| actionlint | local YAML lint (D-34) | ✗ | — | `docker run --rm -v "$PWD:/repo" rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667 -color` |
| zizmor | local workflow audit | ✗ | — | `uvx zizmor@1.30.0 .` or origin action version; Wave 0 install |
| shellcheck | brief local validation | ✗ | — | `brew install shellcheck` Wave 0; Actionlint image includes shellcheck |
| Coolify from GHA | deploy | N/A (remote) | — | Do not fire during implementation |

**Missing dependencies with no fallback:** none blocking (Docker fallback covers actionlint).

**Missing dependencies with fallback:** actionlint, zizmor, shellcheck — install in Wave 0 or use container.

Step 2.6: NOT skipped (external CLIs for validation).

## Validation Architecture

Nyquist enabled (`workflow.nyquist_validation: true`).

### Test Framework

| Property | Value |
|----------|-------|
| Framework | Actionlint 1.7.12 + zizmor + Python/PyYAML parse + existing Jest/Vitest/Cargo |
| Config file | none for GHA; `apps/backend/jest.config.cjs`; `apps/desktop/vitest.config.ts` |
| Quick run command | `docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667` |
| Full suite command | actionlint + `uvx zizmor .` + `pnpm --filter ./apps/desktop test` + `pnpm --filter ./apps/backend test` + `cargo test --locked --manifest-path apps/desktop/src-tauri/Cargo.toml` |

### Phase Requirements → Test Map

| Req ID | Behavior | Test Type | Automated Command | File Exists? |
|--------|----------|-----------|-------------------|-------------|
| BRIEF-1.1 | No `github.ref == 'refs/heads/main' \|\|` on heavy jobs | unit (grep) | `! grep -n "github.ref == 'refs/heads/main' ||" .github/workflows/ci.yml` | ❌ Wave 0 grep check |
| BRIEF-1.2 | `ready_for_review` in types | unit (grep) | `grep -F 'ready_for_review' .github/workflows/ci.yml` | ❌ |
| D-01 | Job named `ci` | unit (grep) | `grep -E '^  ci:' .github/workflows/ci.yml` | ✅ origin |
| D-02 | No `pull_request_target` / no deploy `workflow_run` | unit (grep) | `! grep -R pull_request_target .github/workflows` | ✅ |
| D-03 | SHA-pinned uses | unit (grep) | `! grep -rh 'uses:' .github/workflows \| grep -vE '@[0-9a-f]{40}'` | ✅ origin (keep) |
| D-04 | `package-manager-cache: false` on signed job | unit (grep) | `grep package-manager-cache .github/workflows/desktop-build.yml` | ✅ origin |
| BRIEF-2.1 | No `apps/desktop/**` in desktop-web filter | unit (grep) | filters in `ci.yml` | ❌ |
| BRIEF-3.3 | Actionlint in zizmor workflow | unit (grep) | `grep actionlint .github/workflows/zizmor.yml` | ❌ |
| BRIEF-4 | No `branches: [main]` signed trigger | unit (grep) | desktop-build `on.push` | ❌ |
| BRIEF-5 | Dependabot limits | unit (grep) | `open-pull-requests-limit: 5` npm | ❌ |
| BRIEF-6 | No `value=latest` | unit (grep) | backend-image metadata | ❌ |
| BRIEF-6 | No `migrate` job | unit (grep) | `! grep -E '^  migrate:' .github/workflows/backend-image.yml` | ❌ (job exists on origin — must delete) |
| D-34 GitHub scenarios | docs/draft/JS/Rust/backend/workflow | manual-only | After explicit user push OK | — |

### Sampling Rate

- **Per task commit:** actionlint on changed YAML + grep gates above
- **Per wave merge:** actionlint + zizmor + relevant `pnpm`/`cargo` tests
- **Phase gate:** Full lint + "no unpinned uses" + aggregator still named `ci`

### Wave 0 Gaps

- [ ] Merge/rebase `origin/main` (`1419b31`) onto the planning branch so gitleaks/zizmor/codeql/squawk/Trivy/Harden-Runner exist
- [ ] Install or containerize actionlint, zizmor, shellcheck
- [ ] Re-inspect Postgres/Redis/Actionlint digests immediately before pin
- [ ] Do **not** add `tests/test_gha.py` unless a tiny grep self-check is cheaper — prefer `scripts/` grep in the plan verify steps, not a new test framework

## Security Domain

`security_enforcement` enabled (ASVS L1).

### Applicable ASVS Categories

| ASVS Category | Applies | Standard Control |
|---------------|---------|-----------------|
| V2 Authentication | no (no app auth change) | — |
| V3 Session Management | no | — |
| V4 Access Control | yes (GHA token) | `permissions:` least privilege; no `pull_request_target` |
| V5 Input Validation | yes (workflow `run:`) | No untrusted PR titles in shell; `env:` wrapping |
| V6 Cryptography | yes (signing, cosign) | Existing Tauri key + keyless cosign; no new crypto |
| V10 Malicious Code | yes (supply chain) | SHA-pin actions; no `curl \| bash`; Harden-Runner audit |
| V14 Configuration | yes | Secret Scanning + Push Protection **manual**; `.gitleaks.toml` |

### Known Threat Patterns for GitHub Actions

| Pattern | STRIDE | Standard Mitigation |
|---------|--------|---------------------|
| Pwn request (`pull_request_target` + fork checkout) | Elevation | Forbidden (D-02) |
| Expression injection in `run:` | Tampering | No attacker-controlled `${{ }}` in `run:`; gha-security-review skill |
| Cache poisoning on signed artifacts | Tampering | `package-manager-cache: false`; no rust-cache on signed path (origin) |
| Unpinned action tag move | Tampering | Full SHA (D-03) |
| Secret in step summary | Information disclosure | D-05; never echo tokens/Coolify JSON |
| Workflow_run deploy on untrusted artifact | Elevation | Forbidden (D-02) |
| GHCR `:latest` surprise pull | Tampering | Stop pushing latest (D-24) |
| Dual migrators (GHA + CMD) | Tampering / DoS | Delete GHA migrate job (D-22) |

## Project Constraints (from `.cursor/rules/`)

| Rule | Directive for this phase |
|------|--------------------------|
| `coolify-mcp-cli.mdc` | Product Coolify ops via MCP/CLI. **GHA deploy stays existing `curl` to Coolify API** (runners have no Cursor MCP). Do not open Coolify UI to "test" during implementation (D-06). |
| `kaneo-mcp.mdc` | Orchestrator owns Kaneo — **this researcher created no Kaneo tasks**. |
| `ponytail.mdc` / `honey.mdc` | Minimum YAML; reuse origin workflows; no new SaaS; deletion of skip-migrate and `:latest` over new layers. |
| `karpathy-guidelines.mdc` | Surgical diffs; do not "clean up" labeler (D-36). |
| `authentik-mcp.mdc` / `dbhub-coolify-postgres.mdc` | Out of scope (no Authentik/DB schema work). |
| `gha-security-review` skill | Ban `pull_request_target`; SHA-pin third-party; map `${{ }}` in `run:`. |
| `dependabot` skill | One `.github/dependabot.yml`; `day` weekly-only; groups first-match. |

## Sources

### Primary (HIGH confidence — files opened / API this session)

- `origin/main` `1419b31` workflows via GitHub MCP `get_file_contents`: `ci.yml`, `desktop-build.yml`, `backend-image.yml`, `gitleaks.yml`, `zizmor.yml`, `codeql.yml`, `dependabot.yml`, `apps/backend/Dockerfile`
- Local reads: `04.4-CONTEXT.md`, brief, `ci.yml` (stale branch), `apps/backend/src/health/health.controller.ts`, `package.json`, `apps/backend/package.json`, `rust-toolchain.toml`, `build.rs`, `vitest.config.ts`, `pnpm-workspace.yaml`
- Context7: `/websites/github_en_actions`, `/websites/github_en`, `/actions/checkout`, `/actions/setup-node`, `/gitleaks/gitleaks`, `/zizmorcore/zizmor`, `/rhysd/actionlint`, `/websites/zizmor_sh`, `/github/codeql`, `/docker/docs`, `/aquasecurity/trivy`, `/softprops/action-gh-release`, `/pnpm/action-setup`
- `gh api` tag peel: trivy-action, bake-action, action-gh-release, harden-runner
- `docker buildx imagetools inspect`: postgres:16-alpine, redis:7-alpine, rhysd/actionlint:1.7.12

### Secondary (MEDIUM confidence)

- GitHub-hosted runner label table `[CITED: https://docs.github.com/en/actions/reference/runners/github-hosted-runners]`
- Dependabot `schedule.day` weekly-only `[CITED: https://docs.github.com/en/code-security/reference/supply-chain-security/dependabot-options-reference]`
- CodeQL Rust `build-mode: none` in default-setup table `[CITED: https://docs.github.com/en/code-security/how-tos/find-and-fix-code-vulnerabilities/manage-your-configuration/codeql-for-compiled-languages]`
- upload-artifact `compression-level` `[CITED: https://github.com/actions/upload-artifact]`
- gitleaks-action license + SARIF FAQ `[CITED: https://github.com/gitleaks/gitleaks-action]`
- classify-confidence seam: Context7 `--verified` returned **MEDIUM** (use file reads as HIGH for Ist-Stand)

### Tertiary (LOW confidence)

- Whether Bake will preserve cosign digest outputs without extra scripting `[ASSUMED]` until spike
- Whether `components: ""` fully suppresses toml components `[ASSUMED]`

## Metadata

**Confidence breakdown:**
- Standard stack: HIGH — SHAs and versions from origin + `gh`/`docker` this session
- Architecture: HIGH — locked D-01–D-36 + origin remainder map
- Pitfalls: HIGH — pending-check and draft types from official docs; branch-skew from git log

**Research date:** 2026-09-04
**Valid until:** 7 days (Actions/runner labels and image digests move fast)

**Planner Wave 0 (mandatory):** merge `origin/main` @ `1419b31` before editing workflows. Do not commit from this research agent. Do not create Kaneo tasks. Do not change Branch Protection via API.
