---
phase: 04.4-github-actions-optimization-ci-fan-out-security-workflows-de
plan: 03
subsystem: infra
tags: [github-actions, gitleaks, zizmor, actionlint, codeql, sarif]

requires:
  - phase: 04.4-02
    provides: "ci.yml desktop-web/desktop-rust/backend-test split; aggregator still named ci"
provides:
  - "Gitleaks fail-closed with code-scanning SARIF category gitleaks (D-08, D-10)"
  - "Defaults-only .gitleaks.toml with no fixture-path allowlist (D-11)"
  - "zizmor.yml workflow-level paths plus digest-pinned Actionlint in the same job (D-13)"
  - "CodeQL detect plus analyze-js-typescript / analyze-rust with draft skip and weekly/dispatch both-true (BRIEF-3.4)"
affects: [04.4-04, 04.4-07, branch-protection-gitleaks, github-secret-scanning]

actuals:
  tokens: 2131
  tasks: 3
  commits: 4

tech-stack:
  added:
    - "zricethezav/gitleaks:v8.30.1 digest-pinned CLI for SARIF"
    - "rhysd/actionlint:1.7.12 digest-pinned in zizmor.yml"
  patterns:
    - "security-events: write only on jobs that upload SARIF"
    - "Actionlint lives in the path-filtered zizmor workflow, not a third PR-visible workflow"
    - "CodeQL detect outputs javascript-typescript and rust; schedule/dispatch force both true"

key-files:
  created:
    - .gitleaks.toml
  modified:
    - .github/workflows/gitleaks.yml
    - .github/workflows/zizmor.yml
    - .github/workflows/codeql.yml
    - .github/workflows/desktop-build.yml

key-decisions:
  - "gitleaks-action does not write a workspace SARIF; add digest-pinned zricethezav/gitleaks CLI plus upload-sarif if: always()"
  - "A2 Actionlint 1.7.12 index digest unchanged from RESEARCH"
  - "CodeQL Rust and JS/TS use build-mode none (GitHub compiled-languages default-setup lists None for Rust)"
  - "Fold desktop-build GITHUB_OUTPUT redirects (SC2129) so in-workflow Actionlint can fail closed"
  - "D-09 required check gitleaks and D-12 Secret Scanning stay founder-after-merge; no GitHub settings API"

patterns-established:
  - "Pattern: fail-closed secret scan + SARIF extra (job fail is the merge gate)"
  - "Pattern: path-skipped security workflows must not be required checks"
  - "Pattern: CodeQL detect job + language-split analyze with draft skip and ready_for_review"

requirements-completed:
  - BRIEF-3.1
  - BRIEF-3.2
  - BRIEF-3.3
  - BRIEF-3.4
  - D-08
  - D-10
  - D-11
  - D-13
  - D-33

coverage:
  - id: D1
    description: Gitleaks job still named gitleaks; findings fail the job; SARIF uploads via upload-sarif SHA cdf488f5 category gitleaks
    requirement: D-10
    verification:
      - kind: other
        ref: "grep upload-sarif@cdf488f595d80d6e07e03d4674febd5ab45fa938 and category: gitleaks; continue-on-error count 0"
        status: pass
    human_judgment: false
  - id: D2
    description: .gitleaks.toml exists defaults-only with no fixture-path allowlist
    requirement: D-11
    verification:
      - kind: other
        ref: "test -f .gitleaks.toml && ! grep -E 'fixtures|__tests__|test/' .gitleaks.toml"
        status: pass
    human_judgment: false
  - id: D3
    description: zizmor.yml has workflow-level paths; Actionlint runs via rhysd/actionlint digest image; no actionlint.yml
    requirement: D-13
    verification:
      - kind: other
        ref: "grep .github/workflows/** and rhysd/actionlint:1.7.12@sha256:; test ! -f .github/workflows/actionlint.yml; docker actionlint exit 0"
        status: pass
    human_judgment: false
  - id: D4
    description: CodeQL on.push/on.pull_request paths per brief 3.4; detect plus analyze-js-typescript and analyze-rust; draft skip; schedule/dispatch both true; build-mode none
    requirement: BRIEF-3.4
    verification:
      - kind: other
        ref: "python3 yaml DETECT_OK; grep ready_for_review and build-mode: none; docker actionlint exit 0"
        status: pass
    human_judgment: false
  - id: D5
    description: D-09 and D-12 remain founder-after-merge; this plan did not call the GitHub settings API
    requirement: D-09
    verification: []
    human_judgment: true
    rationale: "Founder must add required check gitleaks and enable Secret Scanning after merge; executor must not call Branch Protection or Secret Scanning APIs"

duration: 5min
completed: 2026-09-04
status: complete
---

# Phase 04.4 Plan 03: Security workflows + Actionlint Summary

**Fail-closed Gitleaks with Security-tab SARIF, Actionlint inside path-filtered zizmor.yml, and CodeQL detect plus language-split analyze jobs**

## Performance

- **Duration:** 5 min
- **Started:** 2026-09-04T04:05:51Z
- **Completed:** 2026-09-04T04:11:40Z
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments

- Gitleaks stays job id `gitleaks`, fails on findings, uploads `gitleaks.sarif` with `github/codeql-action/upload-sarif@cdf488f595d80d6e07e03d4674febd5ab45fa938` category `gitleaks`
- Defaults-only `.gitleaks.toml` with no `[[allowlists]]` fixture paths
- `zizmor.yml` path-filtered on workflow/action YAML; Actionlint runs in the same job via `rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667`; no third PR-visible workflow
- `codeql.yml` workflow-level paths on push/PR (not on schedule/dispatch); `detect` fans out `javascript-typescript` vs `rust`; drafts skip analyze; weekly and manual force both languages; `build-mode: none`; CodeQL not a required check

## Task Commits

Each task was committed atomically:

1. **Task 1: Gitleaks SARIF plus defaults toml** - `6a2c0ec` (feat)
2. **Task 2: Zizmor paths plus Actionlint in same workflow** - `c2ecaff` (feat)
3. **Task 3: CodeQL detect, language split, draft skip** - `efdb1a8` (feat)

**Plan metadata:** `docs(04.4-03): complete security workflows and Actionlint plan`

_Note: TDD tasks may have multiple commits (test → feat → refactor)_

## Files Created/Modified

- `.github/workflows/gitleaks.yml` - `security-events: write`, digest-pinned CLI SARIF, upload-sarif category gitleaks
- `.gitleaks.toml` - comment-only defaults header (D-11)
- `.github/workflows/zizmor.yml` - brief 3.2 paths, Wednesday 04:42 UTC schedule, Actionlint before zizmor-action
- `.github/workflows/codeql.yml` - detect + analyze-js-typescript + analyze-rust, draft skip, ready_for_review
- `.github/workflows/desktop-build.yml` - SC2129 GITHUB_OUTPUT fold so Actionlint exits 0

## Decisions Made

- gitleaks-action uploads a SARIF *artifact*, not a workspace file. Added `zricethezav/gitleaks:v8.30.1@sha256:c00b6bd0aeb3071cbcb79009cb16a60dd9e0a7c60e2be9ab65d25e6bc8abbb7f` (`gitleaks/gitleaks` is not a public Docker Hub image) plus `upload-sarif` with `if: always()` so findings still reach the Security tab when the job fails.
- Actionlint 1.7.12 index digest re-inspected at execute; unchanged from RESEARCH (A2).
- CodeQL Rust `build-mode: none` — GitHub compiled-languages docs list None as default-setup for Rust. Revert analyze-rust to autobuild only if a scheduled run is empty (not this phase).
- D-09 (`gitleaks` required check) and D-12 (Secret Scanning + Push Protection) stay founder checklist in Plan 07. No Branch Protection / Environments / Secret Scanning API calls.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Actionlint SC2129 in desktop-build.yml**
- **Found during:** Task 2 (Zizmor paths plus Actionlint)
- **Issue:** Full-tree Actionlint failed on origin `desktop-build.yml` three separate `>> "$GITHUB_OUTPUT"` redirects (SC2129). New Actionlint step would fail every zizmor run.
- **Fix:** Folded the three echoes into one `{ ... } >> "$GITHUB_OUTPUT"` block.
- **Files modified:** `.github/workflows/desktop-build.yml`
- **Verification:** `docker run rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667` exit 0
- **Committed in:** `c2ecaff` (Task 2)

**2. [Rule 2 - Missing Critical] SARIF upload after a failing scan**
- **Found during:** Task 1 (Gitleaks SARIF)
- **Issue:** Plan listed upload-sarif after gitleaks-action. A failing scan would skip upload without `if: always()`. Job fail is the merge gate; SARIF is extra (Pitfall 7) but D-10 still wants the Security tab.
- **Fix:** CLI SARIF step and upload-sarif both `if: always()` (upload also requires `hashFiles('gitleaks.sarif') != ''`).
- **Files modified:** `.github/workflows/gitleaks.yml`
- **Verification:** grep upload-sarif SHA + category gitleaks; no continue-on-error
- **Committed in:** `6a2c0ec` (Task 1)

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 missing critical)
**Impact on plan:** Both required for Actionlint and D-10 to work. No scope creep. Image name `zricethezav/gitleaks` is the published CLI (plan said `gitleaks/gitleaks`).

## Issues Encountered

None beyond the documented deviations. Actionlint digest still matches RESEARCH. No `GITLEAKS_LICENSE` invented in YAML (A1 stays Plan 07 checklist if `clezcoding` is an org).

## Authentication Gates

None.

## User Setup Required

None - no external service configuration required. D-09 and D-12 remain founder-after-merge (Plan 07), not this plan.

## Next Phase Readiness

Ready for 04.4-04 (desktop-release remainder). Do not mark CodeQL or Zizmor as required checks. Do not push or tag. D-33 logical commit 3 of 7 is the three feat commits above plus this summary commit — keep them linear, no squash.

## Self-Check: PASSED

- FOUND: `.github/workflows/gitleaks.yml`
- FOUND: `.gitleaks.toml`
- FOUND: `.github/workflows/zizmor.yml`
- FOUND: `.github/workflows/codeql.yml`
- FOUND: `6a2c0ec`, `c2ecaff`, `efdb1a8`

---
*Phase: 04.4-github-actions-optimization-ci-fan-out-security-workflows-de*
*Completed: 2026-09-04*
