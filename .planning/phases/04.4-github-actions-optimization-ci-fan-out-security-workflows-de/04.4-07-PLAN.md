---
phase: 04.4-github-actions-optimization-ci-fan-out-security-workflows-de
plan: 07
type: execute
wave: 7
depends_on:
  - 04.4-06
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/desktop-build.yml
  - .github/workflows/backend-image.yml
  - .github/workflows/gitleaks.yml
  - .github/workflows/zizmor.yml
  - .github/workflows/codeql.yml
  - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-MANUAL-GITHUB.md
  - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-VALIDATION.md
autonomous: true
requirements:
  - BRIEF-7
  - BRIEF-8
  - D-02
  - D-03
  - D-05
  - D-06
  - D-33
  - D-34
  - D-35
  - D-36
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every actions/checkout in .github/workflows except none that git push (there are none) sets persist-credentials false (D-36, BRIEF-7)."
    - "ci.yml postgres and redis service images are tag@sha256:… with the readable tag in a comment; versions stay 16 and 7 (D-36). Dependabot cannot bump jobs.*.services.*.image; 04.4-MANUAL-GITHUB.md names quarterly human re-pin as the D-36 substitute (Pitfall 6)."
    - "uses: lines under .github/workflows are pinned to 40-char SHAs except local ./.github/actions/ paths (D-03)."
    - "Aggregator and deploy/build jobs append GITHUB_STEP_SUMMARY with changed areas, skipped jobs, image tags, deploy/rollback — no secrets, no Coolify JSON, no PR titles (D-05, D-35, BRIEF-8)."
    - "desktop-web uses built-in Vitest junit reporter and uploads junit.xml only if: failure() on that job; backend-test uses built-in Jest github-actions reporter only (no junit.xml upload); retention-days 5; no new npm reporter package (D-35)."
    - "labeler.yml and sync-labels.yml are byte-unchanged vs origin besides unrelated merges (D-36)."
    - "04.4-MANUAL-GITHUB.md lists founder steps: required check gitleaks, Secret Scanning plus Push Protection, GITLEAKS_LICENSE if org, quarterly service-digest bump (Dependabot cannot bump services.*.image; human quarterly re-pin is the D-36 substitute), D-34 scenario table after explicit push OK, D-17 staging dispatch after merge — no production dispatch, no v* tag in this phase (D-06, D-09, D-12, D-34). Same file contains the brief Vorher/Nachher fan-out table (deliverable 3)."
    - "uvx zizmor@1.30.0 . exits 0; Wave 0 origin findings are fixed in this plan, not carried as documented exceptions (D-34)."
    - statement: "GitHub scenario table (docs-only, draft, ready-for-review JS/TS, Rust-only, backend-only, workflow-YAML, docs-only main) is not executed until the user says push OK (D-34)."
      verification: backstop
  artifacts:
    - .github/workflows/ci.yml
    - .github/workflows/backend-image.yml
    - .github/workflows/desktop-build.yml
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-MANUAL-GITHUB.md
  key_links:
    - "echo ... >> \"$GITHUB_STEP_SUMMARY\" with needs.changes.outputs only"
    - "desktop-build.yml writes GITHUB_STEP_SUMMARY (target + artifact names, no signing material)"
    - "desktop-web upload-artifact of junit.xml if: failure(); backend-test has no junit.xml artifact"
    - "Harden-Runner egress-policy stays audit not block (D-36)"
  prohibitions:
    - statement: "Do not add the privileged PR-from-base trigger for PR code (D-02)."
      status: unverified
    - statement: "Do not add workflow_run for desktop or backend deploy (D-02)."
      status: unverified
    - statement: "Do not change Branch Protection, Environments, or Secret Scanning via API (D-06)."
      status: unverified
    - statement: "Do not trigger production Coolify deploy or create a v* tag during this phase (D-06, D-34)."
      status: unverified
    - statement: "Do not add a new reporting SaaS or jest-junit lockfile dependency (D-35)."
      status: unverified
    - statement: "Do not set Harden-Runner egress-policy to block (D-36 deferred)."
      status: unverified
    - statement: "Do not edit labeler.yml or sync-labels.yml (D-36)."
      status: unverified
    - statement: "Do not put secrets.COOLIFY_TOKEN, FAYNOSYNC_CI_TOKEN, or TAURI_SIGNING_PRIVATE_KEY into GITHUB_STEP_SUMMARY (D-05)."
      status: unverified
---

<objective>
D-33 logical commit 7: checkout/digest/permissions hardening, step summaries, native test reports, founder checklist (BRIEF-7, BRIEF-8, D-02, D-03, D-05, D-06, D-34–D-36).

Purpose: Least-privilege leftovers, readable CI summaries without secret leakage, JUnit only on failure, human GitHub settings documented not automated.

Output: persist-credentials and service digests; summaries; reporters; 04.4-MANUAL-GITHUB.md. No live GitHub scenario runs.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-RESEARCH.md
@.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-PATTERNS.md
@.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-VALIDATION.md
@docs/claredtool-github-actions-optimierungsauftrag.md
</context>

<tasks>

<task type="auto">
  <name>persist-credentials, service digests, permissions audit</name>
  <files>.github/workflows/ci.yml, .github/workflows/desktop-build.yml, .github/workflows/backend-image.yml, .github/workflows/gitleaks.yml, .github/workflows/zizmor.yml, .github/workflows/codeql.yml</files>
  <read_first>
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-PATTERNS.md (persist-credentials analog, Least-privilege, Services analog, SHA-pinned uses)
    - .github/workflows/ci.yml
    - .github/workflows/backend-image.yml
    - .github/workflows/labeler.yml
  </read_first>
  <action>
    Add persist-credentials: false to every actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 that lacks it (BRIEF-7, D-36). None of these workflows git push.

    Pin ci.yml service images at execute time via docker buildx imagetools inspect postgres:16-alpine and redis:7-alpine. Write image: postgres:16-alpine@sha256:… and redis:7-alpine@sha256:… with the floating tag in a comment. Do not bump to 18/8. RESEARCH digests are stale-able (A3). Record the live digests in VALIDATION.md.

    Audit backend-image.yml build job permissions: keep packages write and id-token write for cosign. Drop actions: write if no step needs it (GHA cache may; if cache-from type=gha requires it, keep and note in SUMMARY). ci.yml stays contents: read only. GH Release job already has contents write from Plan 04.

    Confirm Harden-Runner on signing/deploy/CI still uses egress-policy: audit (D-36). Do not edit labeler.yml or sync-labels.yml.

    Grep all uses: under .github/workflows for a 40-char SHA after @, allowing ./.github/actions/ local paths (D-03).

    After those edits, run uvx zizmor@1.30.0 . and mitigate every finding on the six workflow files this task owns (artipacked via persist-credentials; unpinned uses via SHA grep already this task). Wave 0 VALIDATION.md origin findings are in-scope here, not a documented exception (Plan 01 only deferred them to this plan). Leave the tree so Plan 07 task 3 uvx zizmor@1.30.0 . exits 0.
  </action>
  <acceptance_criteria>
    - Every checkout with: block in the six workflow files includes persist-credentials: false
    - ci.yml services postgres and redis image values contain @sha256:
    - Image tags remain 16-alpine and 7-alpine
    - labeler.yml is unmodified in this commit
    - No uses: line has @v4 or @latest as the ref
  </acceptance_criteria>
  <verify>
    <automated>python3 -c '
import pathlib, re, yaml
skip = {"labeler.yml", "sync-labels.yml"}
missing = []
for p in pathlib.Path(".github/workflows").glob("*.yml"):
    if p.name in skip:
        continue
    t = p.read_text()
    d = yaml.safe_load(t)
    for jid, job in (d.get("jobs") or {}).items():
        for i, step in enumerate(job.get("steps") or []):
            uses = str(step.get("uses") or "")
            if "actions/checkout@" not in uses:
                continue
            with_ = step.get("with") or {}
            if with_.get("persist-credentials") is not False:
                missing.append("%s:%s:%s" % (p.name, jid, i))
    for m in re.finditer(r"uses:\s*(\S+)", t):
        u = m.group(1)
        if u.startswith("./"):
            continue
        if not re.search(r"@[0-9a-f]{40}", u):
            raise SystemExit("UNPINNED %s: %s" % (p, u))
print("missing_persist", missing)
assert not missing
' && grep -F '@sha256:' .github/workflows/ci.yml | grep -q postgres && grep -F '@sha256:' .github/workflows/ci.yml | grep -q redis && git diff --name-only -- .github/workflows/labeler.yml .github/workflows/sync-labels.yml | wc -l | grep -q '^0$'</automated>
  </verify>
  <done>Checkouts persist-credentials false; CI services digest-pinned; uses SHA-pinned; labeler untouched (BRIEF-7, D-03, D-36).</done>
</task>

<task type="auto">
  <name>Step summaries and native test reports</name>
  <files>.github/workflows/ci.yml, .github/workflows/backend-image.yml, .github/workflows/desktop-build.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - .github/workflows/backend-image.yml
    - apps/desktop/package.json
    - apps/backend/package.json
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-RESEARCH.md (Step summary / tests)
  </read_first>
  <action>
    Aggregator job ci: append changed-area booleans from needs.changes.outputs (desktop-web, desktop-rust, backend, shared, tax-engine) and each needed job result to $GITHUB_STEP_SUMMARY (D-35, BRIEF-8). Use env: for any dynamic values then echo quoted in the shell. Do not interpolate github.event.pull_request.title. Do not print secrets.

    backend-image build and deploy jobs: append image sha tags and deploy/rollback outcome. Never dump Coolify API JSON, COOLIFY_TOKEN, or registry passwords (D-05).

    desktop-build: append target (tag vs dispatch vs staging) and artifact names, not signing key material.

    Test reports: desktop-web vitest add --reporter=default --reporter=junit --outputFile=junit.xml (built-in). Upload that junit.xml with actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a only on job desktop-web, if: failure(), retention-days: 5. backend-test jest add --reporters=default --reporters=github-actions (built-in) only — no junit.xml, no upload-artifact of junit on backend-test (Jest github-actions reporter does not write that file). Do not add jest-junit or Codecov. Do not edit vitest.config.ts or jest.config.cjs unless CLI flags are proven ignored — prefer CLI (PATTERNS).
  </action>
  <acceptance_criteria>
    - ci.yml contains GITHUB_STEP_SUMMARY
    - backend-image.yml contains GITHUB_STEP_SUMMARY
    - desktop-build.yml contains GITHUB_STEP_SUMMARY
    - ci.yml contains reporter=junit or --reporter=junit and github-actions
    - desktop-web upload-artifact of junit.xml is gated with if: failure()
    - backend-test has github-actions reporter and no junit.xml upload
    - package.json files gained no jest-junit dependency
  </acceptance_criteria>
  <verify>
    <automated>grep -F 'GITHUB_STEP_SUMMARY' .github/workflows/ci.yml && grep -F 'GITHUB_STEP_SUMMARY' .github/workflows/backend-image.yml && grep -F 'GITHUB_STEP_SUMMARY' .github/workflows/desktop-build.yml && grep -E 'reporter=junit|--reporter=junit' .github/workflows/ci.yml && grep -F 'github-actions' .github/workflows/ci.yml && grep -B6 'junit.xml' .github/workflows/ci.yml | grep -q 'failure()' && python3 -c "
import yaml
d=yaml.safe_load(open('.github/workflows/ci.yml'))
jobs=d['jobs']
dw=jobs['desktop-web']
found=False
for step in dw.get('steps') or []:
    uses=str(step.get('uses') or '')
    with_=step.get('with') or {}
    blob=str(with_.get('path') or '')+' '+str(with_.get('name') or '')
    if 'upload-artifact' in uses and 'junit' in blob:
        assert 'failure()' in str(step.get('if') or '')
        found=True
assert found, 'desktop-web missing junit upload'
bt=jobs['backend-test']
blob=yaml.dump(bt)
assert 'github-actions' in blob
assert 'junit.xml' not in blob
print('JUNIT_DESKTOP_WEB_ONLY_OK')
" && ! grep -F 'jest-junit' apps/backend/package.json && docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667</automated>
  </verify>
  <done>Summaries without secrets; native JUnit on failure only (D-05, D-35, BRIEF-8).</done>
</task>

<task type="auto">
  <name>Founder checklist and D-34 local-only close</name>
  <files>.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-MANUAL-GITHUB.md, .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-VALIDATION.md, .github/workflows/ci.yml, .github/workflows/desktop-build.yml, .github/workflows/backend-image.yml, .github/workflows/gitleaks.yml, .github/workflows/zizmor.yml, .github/workflows/codeql.yml</files>
  <read_first>
    - docs/claredtool-github-actions-optimierungsauftrag.md
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-VALIDATION.md
    - .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-RESEARCH.md (Open Questions, Pitfall 6–7)
  </read_first>
  <action>
    Write 04.4-MANUAL-GITHUB.md as the D-06 end checklist. Cursor must not call GitHub settings APIs. Include: after merge, founder adds required status check named exactly gitleaks with no admin bypass (D-09); enable Secret Scanning and Push Protection on the public repo (D-12); confirm whether clezcoding is an org needing GITLEAKS_LICENSE (A1); Dependabot cannot bump jobs.*.services.*.image (Pitfall 6); human quarterly re-pin of ci.yml postgres:16-alpine and redis:7-alpine digests is the D-36 substitute; Dependabot still bumps the docker / github-actions ecosystems in dependabot.yml and that does not cover GHA service images; Gitleaks SARIF history caveat (Pitfall 7); D-17 staging desktop dispatch on both windows-2022 and macos-15 after merge — not in this PR; D-34 GitHub scenario table (docs-only PR, draft, ready_for_review JS/TS, Rust-only, backend-only, workflow-YAML, docs-only main) only after explicit user push OK; never production dispatch and never v* during this phase.

    In the same file, write a Vorher/Nachher fan-out table (brief Cursor deliverable 3) mapping Ist-Stand (shared-dependency main push up to 17 jobs; docs-only main 10 jobs; ci + desktop-build + codeql ~90 percent runner time) to post-04.4 expected jobs per scenario: docs-only, draft, ready_for_review JS/TS (no Rust CodeQL), Rust-only (no JS CodeQL), backend-only, workflow-YAML, weekly CodeQL both languages. Include the brief savings note (~40 percent from desktop frequency + main path filters + CodeQL language split). Copy that table into 04.4-07-SUMMARY.md when writing the SUMMARY.

    Repo-wide grep: no privileged PR-from-base trigger token; no workflow_run in desktop-build.yml or backend-image.yml (D-02).

    Update VALIDATION.md D-34 row as manual-after-push-OK. Wave 0 allowed documenting origin zizmor findings; this task must clear them. Read VALIDATION.md Wave 0 zizmor output. For each remaining finding on a workflow this phase owns, apply the mitigation on that file in this commit (checkout persist-credentials, env-wrap untrusted interpolations, SHA pins, package-manager-cache false already on signed path). uvx zizmor@1.30.0 . must exit 0. Do not treat Wave 0 notes as an allowlist at this gate. Run Actionlint plus uvx zizmor@1.30.0 . (D-34 close gate; must exit 0) plus a SHA-pin grep across workflows. Record ShellCheck as covered by the Actionlint image.

    Do not git push. Do not gh workflow run. Do not create Kaneo tasks.
  </action>
  <acceptance_criteria>
    - 04.4-MANUAL-GITHUB.md exists and contains the strings gitleaks, Secret Scanning, Push Protection, and ready_for_review
    - 04.4-MANUAL-GITHUB.md contains a Vorher/Nachher fan-out table
    - 04.4-MANUAL-GITHUB.md states Dependabot cannot bump services images and names quarterly human re-pin as the D-36 substitute
    - grep -R the privileged PR-from-base trigger across .github/workflows is empty
    - backend-image.yml and desktop-build.yml have no workflow_run:
    - VALIDATION.md mentions 04.4-07
    - uvx zizmor@1.30.0 . exits 0
    - Wave 0 origin zizmor findings are gone from a fresh run (Plan 01 documented-origin-exception does not apply at this gate)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-MANUAL-GITHUB.md && grep -q 'gitleaks' .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-MANUAL-GITHUB.md && grep -q 'Secret Scanning' .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-MANUAL-GITHUB.md && grep -Eiq 'fan-out|vorher' .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-MANUAL-GITHUB.md && grep -F 'D-36' .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-MANUAL-GITHUB.md && grep -Ei 'cannot bump|does not bump' .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-MANUAL-GITHUB.md && grep -F 'services' .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-MANUAL-GITHUB.md && ! grep -R 'pull_request_target' .github/workflows && ! grep -E '^[[:space:]]*workflow_run:' .github/workflows/desktop-build.yml .github/workflows/backend-image.yml && grep -q '04.4-07' .planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-VALIDATION.md && uvx zizmor@1.30.0 . && docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:1.7.12@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667</automated>
    <human-check>Do not run the brief GitHub scenario table or staging desktop dispatch until the user explicitly allows a push. After merge: apply 04.4-MANUAL-GITHUB.md (required check gitleaks, Secret Scanning, Push Protection).</human-check>
  </verify>
  <done>Checklist written; D-02 greps clean; D-34 stays local until push OK (D-06, D-09, D-12, D-34).</done>
</task>

</tasks>

<!-- planner-discipline-allow: pull_request_target -->
<!-- planner-discipline-allow: workflow_run: -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Step summary markdown | Visible on GitHub; must not contain secrets |
| JUnit artifacts | Failure-only; 5-day retention |
| Service image digests | Supply chain for CI Postgres/Redis |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.4-01 | Elevation of privilege | all workflows | high | mitigate | Final grep: no privileged PR-from-base trigger (D-02). Block-on high. |
| T-04.4-02 | Tampering | GITHUB_STEP_SUMMARY / run: | high | mitigate | env-wrap; no PR title; no untrusted HTML injection via title (D-31, D-05). |
| T-04.4-03 | Tampering | junit artifacts | medium | mitigate | Upload only on failure; unsigned CI path. |
| T-04.4-04 | Tampering | uses: | high | mitigate | 40-char SHA grep (D-03). Block-on high. |
| T-04.4-05 | Information disclosure | step summaries | high | mitigate | Forbid token names in SUMMARY file; no Coolify JSON (D-05). Block-on high. |
| T-04.4-06 | Elevation of privilege | workflow_run | high | mitigate | Final grep on deploy/desktop workflows (D-02). |
| T-04.4-07 | Tampering | GHCR latest | low | accept | Already removed in Plan 06. |
| T-04.4-08 | Tampering | dual migrators | low | accept | Already removed in Plan 06. |
| T-04.4-SC | Tampering | installs | high | accept | No new reporter packages. |
</threat_model>

<verification>
Python persist-credentials per checkout with-block. Service @sha256. Summaries on ci, backend-image, and desktop-build. JUnit failure-only on desktop-web (not backend-test). Actionlint plus uvx zizmor exit 0 (Wave 0 origin findings fixed). D-02 greps. Checklist file with fan-out table and D-36 substitute. No push.
</verification>

<success_criteria>
Hardening and summaries landed. Founder checklist exists. Required check still named ci. D-33 commit 7 of 7. Linear seven-commit history; do not squash those seven.
</success_criteria>

## Artifacts this phase produces

- Workflow checkouts `persist-credentials: false`; CI Postgres/Redis digest pins
- `$GITHUB_STEP_SUMMARY` on ci / backend-image / desktop-build
- Native JUnit/github-actions reporters; failure-only artifact
- `04.4-MANUAL-GITHUB.md` — D-06/D-09/D-12/D-17/D-34 founder steps plus Vorher/Nachher fan-out table

<output>
Create `.planning/phases/04.4-github-actions-optimization-ci-fan-out-security-workflows-de/04.4-07-SUMMARY.md` when done (include the fan-out table).
D-33 commit 7 of 7: hardening/summaries/test reports. Single commit. Do not squash with commits 1–6. Do not push.
</output>
