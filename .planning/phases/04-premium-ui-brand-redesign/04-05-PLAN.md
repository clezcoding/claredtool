---
phase: 04-premium-ui-brand-redesign
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified:
  - apps/desktop/src/main.tsx
  - apps/desktop/src/lib/theme.ts
  - apps/desktop/src/lib/theme.test.ts
  - apps/desktop/src/components/invoice-empty-state.tsx
  - apps/desktop/src/__tests__/phase03-autosave.test.tsx
  - apps/desktop/src/auth/login-gate.tsx
  - apps/desktop/src/__tests__/auth-gate.test.tsx
autonomous: true
gap_closure: true
gap_ids: [G-04-4, G-04-5, G-04-6]
requirements: [UI-01, BRAND-01]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Empty states render documented empty copy and illustration. Return CTA is Beispielrechnung anzeigen. Tax rail and Vorschau hide while empty state is showing. (G-04-4, UI-SPEC empty, D-15)"
    - "Splash screen (wordmark + spinner) on launch. In-app loading uses existing skeleton.tsx matching layout, not full-screen spinners. (D-16, D-18)"
    - "Error states render the documented error copy with retry options."
    - "Motion durations under 300ms, custom ease-out, prefers-reduced-motion respected. Button press scale(0.97). (D-17)"
    - "LoginGate renders a first-party img with src /login-gate-hero.png; wordmark h1 Clared uses the system sans stack (D-04, D-14, D-15, G-04-5)."
    - "While currentPref() is system, a prefers-color-scheme change calls applyTheme('system') so html+body D-02 paint and .dark stay aligned (G-04-6, D-06, D-02)."
    - "Hell/Dunkel persisted prefs ignore OS change; chip stays identity + Abmelden only (D-07)."
  artifacts:
    - apps/desktop/src/main.tsx
    - apps/desktop/src/lib/theme.ts
    - apps/desktop/src/lib/theme.test.ts
    - apps/desktop/src/components/invoice-empty-state.tsx
    - apps/desktop/src/auth/login-gate.tsx
    - apps/desktop/src/__tests__/phase03-autosave.test.tsx
    - apps/desktop/src/__tests__/auth-gate.test.tsx
  key_links:
    - "main.tsx matchMedia change listener → syncSystemAppearance() → applyTheme('system') → paintDocument html+body (same hex as 04-04)."
    - "InvoiceEmptyState onStart still startNewDraft; rechnung showRail = !showHero unchanged."
    - "LoginGate img src /login-gate-hero.png reads apps/desktop/public/login-gate-hero.png (already on disk; no Higgsfield job)."
    - "Anmelden still session.login() / invoke open_login_window (Phase 2 AUTH-01 unchanged)."
  prohibitions:
    - "must NOT ship two CSS bundles or duplicate Light/Dark component trees (D-08)."
    - "must NOT add a theme control to the session chip (D-07 / Phase 2 D-36)."
    - "must NOT add nav items or routes; information architecture is frozen (D-11)."
    - "must NOT package a webfont; system fonts only (D-03)."
    - "must NOT let the Darstellung override win over the OS for reduced-motion (D-17)."
    - "must NOT implement out-of-scope mockups (10-onboarding, 11-settings, 12-profile, 13-dashboard, 14-catalog, 15-export) nor resurrect rejected explorations (Nordic / glass / warm-editorial / corporate-teal) (D-13)."
    - "must NOT rebuild mockup product IA (Übersicht / Banking / Senden) to chase 1:1 craft vs approved 02–09 — that remains human UAT (D-11 look, D-13)."
    - "must NOT build a custom titlebar (D-19)."
    - "must NOT invert PdfPaper colors in dark theme (D-09)."
    - "must NOT generate in-app illustrations at runtime or spend Higgsfield credits (D-14, D-25)."
    - "must NOT rewrite splash architecture or SPLASH_HOLD_MS; splash observability stays human UAT / PBU (G-04-3 closed in code)."
  assumptions:
    - "[FLAGGED EDGE — adjacency / UI-01 probe] Where two surfaces meet they stay distinct whisper-separated regions (border-border/70), not a merged slab. No new collision algorithm."
    - "[RESOLVED EDGE — empty] Empty/loading/error follow 04-UI-SPEC UI Considerations; G-04-4 restores the documented return CTA. Tax/PDF hide via existing showRail."
    - "[FLAGGED EDGE — ordering / UI-01 probe] No new sort. Draft picker, entity list, and country options keep Phase 3 order."
    - "[FLAGGED — BRAND-01 unclassified probe] BRAND-01 remains tokens + approved-mockup SSOT + motion; this gap-closure is UI/theme/auth surfaces only — no second identity model, no new external API."
    - "[ASSUMED] public/login-gate-hero.png (2688×1520) from 04-03 remains the Crafted hero; this plan wires it, it does not regenerate it."
    - "[ASSUMED] applyTheme('system') on OS change is the correct paint path; classList.toggle('dark') alone cannot beat inline html/body background from 04-04."
---

<objective>
Close remaining verification blockers G-04-4, G-04-5, and G-04-6 after 04-04: empty-state return CTA copy, LoginGate Crafted hero + system wordmark, OS color-scheme listener that re-runs applyTheme paint.

Purpose: UI-01/BRAND-01 shipping surfaces match 04-UI-SPEC empty CTA, 04-03 login hero wiring (D-04/D-14/D-15), and D-06 live OS-follow after inline boot paint (D-02).
Output: syncSystemAppearance + tests; InvoiceEmptyState CTA copy + autosave RTL; LoginGate img + font-sans + token canvas + auth-gate assertion.
Locked: D-01 Crafted Minimal. D-02 oatmeal/charcoal paint. D-03 system fonts. D-04 Clared wordmark sans. D-06 OS-follow. D-07 Darstellung not chip. D-08 one tree. D-09 PdfPaper light. D-11/D-13 5-item shell, no mockup IA. D-14 no new Higgsfield. D-15 DE/EN gate copy. D-16/D-18 splash+skeletons already shipped. D-17 reduced-motion.
</objective>

<assumption_delta_decision>
noun: Crafted Minimal appearance pref (`system` | `light` | `dark`)
decision: no-change
rationale: Gap-closure UI/theme only. OS listener reuses applyTheme; no new identity or API.
</assumption_delta_decision>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-premium-ui-brand-redesign/04-CONTEXT.md
@.planning/phases/04-premium-ui-brand-redesign/04-VERIFICATION.md
@.planning/phases/04-premium-ui-brand-redesign/04-UAT.md
@.planning/phases/04-premium-ui-brand-redesign/04-UI-SPEC.md
@.planning/phases/04-premium-ui-brand-redesign/04-04-SUMMARY.md
@.planning/phases/04-premium-ui-brand-redesign/04-03-PLAN.md
@.planning/phases/04-premium-ui-brand-redesign/04-SECURITY.md
@apps/desktop/src/main.tsx
@apps/desktop/src/lib/theme.ts
@apps/desktop/src/lib/theme.test.ts
@apps/desktop/src/components/invoice-empty-state.tsx
@apps/desktop/src/auth/login-gate.tsx
@apps/desktop/src/routes/rechnung.tsx
@apps/desktop/src/__tests__/phase03-autosave.test.tsx
@apps/desktop/src/__tests__/auth-gate.test.tsx
</context>

<interfaces>
From apps/desktop/src/lib/theme.ts (04-04):
THEME_KEY, ThemePref, resolveDark(pref), applyTheme(pref), currentPref(), paintDocument via applyTheme
PAINT_LIGHT #F7F7F5, PAINT_DARK #111110

From apps/desktop/src/main.tsx today:
applyTheme(currentPref()) before createRoot
matchMedia (prefers-color-scheme: dark) change: if currentPref()==="system" then classList.toggle("dark", resolveDark("system")) — missing paint

From apps/desktop/src/components/invoice-empty-state.tsx:
InvoiceEmptyState({ onStart? }); img /empty-state-hero.png; heading Noch keine Rechnung erstellt
rechnung.tsx: showHero → InvoiceEmptyState onStart={startNewDraft}; showRail = !showHero

From apps/desktop/src/auth/login-gate.tsx:
LoginGate; CTA_CLASS; handleLogin session.login / invoke open_login_window
public asset: apps/desktop/public/login-gate-hero.png
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end OS appearance change re-paints html+body via applyTheme</name>
  <precondition>apps/desktop/src/lib/theme.ts exports applyTheme, currentPref, resolveDark and paints html+body with PAINT_LIGHT / PAINT_DARK from plan 04-04</precondition>
  <reversibility rating="reversible">Listener only re-invokes existing applyTheme; pref enum unchanged (D-06).</reversibility>
  <files>apps/desktop/src/lib/theme.ts, apps/desktop/src/lib/theme.test.ts, apps/desktop/src/main.tsx</files>
  <read_first>
    - apps/desktop/src/main.tsx (change listener lines 10–16)
    - apps/desktop/src/lib/theme.ts (applyTheme / paintDocument / currentPref)
    - apps/desktop/src/lib/theme.test.ts (stubMatchMedia, paintedBackground helpers)
    - .planning/phases/04-premium-ui-brand-redesign/04-VERIFICATION.md (truth 24, G-04-6)
  </read_first>
  <behavior>
    - After applyTheme("system") with matchMedia dark, calling syncSystemAppearance after stubbing matchMedia light paints html and body to Pale Oatmeal and removes class dark.
    - After applyTheme("dark"), syncSystemAppearance with matchMedia light leaves Deep Charcoal paint and class dark (pref is not system).
    - After applyTheme("light"), syncSystemAppearance with matchMedia dark leaves oatmeal paint and no class dark.
  </behavior>
  <action>
    G-04-6 per D-06 and D-02. RED: add the three syncSystemAppearance behaviors above to theme.test.ts (reuse paintedBackground; accept hex or jsdom rgb). GREEN: export syncSystemAppearance from theme.ts — when currentPref() is system, call applyTheme("system"); otherwise return without writing class or inline styles. In main.tsx, the prefers-color-scheme change listener must call syncSystemAppearance only (keep the existing matchMedia query string). Do not add IPC, menu items, or SessionChip theme entries (D-07). Do not add a second CSS bundle (D-08). Keep PREFS unknown→system (T-04-01).
  </action>
  <verify>
    <automated>pnpm --filter desktop exec vitest run src/lib/theme.test.ts && python3 -c "
from pathlib import Path
m=Path('apps/desktop/src/main.tsx').read_text()
assert 'syncSystemAppearance' in m
assert 'classList.toggle' not in m.split('addEventListener')[1] if 'addEventListener' in m else False
t=Path('apps/desktop/src/lib/theme.ts').read_text()
assert 'export function syncSystemAppearance' in t
print('G-04-6 gates ok')
"</automated>
    <human-check>Darstellung=System; flip OS Light/Dark: canvas hex and tokens follow together (04-UAT test 3 after this fix).</human-check>
  </verify>
  <acceptance_criteria>
    - theme.test.ts covers syncSystemAppearance for system-follow and light/dark lock.
    - main.tsx change handler calls syncSystemAppearance.
    - applyTheme still paints html+body #F7F7F5 / #111110.
  </acceptance_criteria>
  <done>OS color-scheme change while pref is system re-paints D-02 hex on html and body; locked Hell/Dunkel ignore OS.</done>
</task>

<task type="auto" tdd="true">
  <name>Empty-state return CTA Beispielrechnung anzeigen</name>
  <files>apps/desktop/src/components/invoice-empty-state.tsx, apps/desktop/src/__tests__/phase03-autosave.test.tsx</files>
  <read_first>
    - apps/desktop/src/components/invoice-empty-state.tsx
    - apps/desktop/src/routes/rechnung.tsx (showHero, InvoiceEmptyState onStart={startNewDraft}, showRail)
    - apps/desktop/src/__tests__/phase03-autosave.test.tsx (empty invoice list then clicks the empty CTA)
    - apps/desktop/src/__tests__/invoice.test.tsx (header CTA path after a draft exists — do not retarget that header)
    - .planning/phases/04-premium-ui-brand-redesign/04-UI-SPEC.md (UI Considerations empty row)
  </read_first>
  <behavior>
    - InvoiceEmptyState primary button accessible name is Beispielrechnung anzeigen.
    - phase03-autosave empty-list flow clicks that name, then a line-item textbox appears (startNewDraft still runs).
  </behavior>
  <action>
    G-04-4 per UI-SPEC empty coverage and D-15. RED: in phase03-autosave.test.tsx waitFor/getByRole/click the empty-canvas primary using name Beispielrechnung anzeigen (empty GET /api/invoices still []). GREEN: set InvoiceEmptyState button children to that same string; keep img src /empty-state-hero.png, heading Noch keine Rechnung erstellt, EMPTY_BODY, onStart then startNewDraft / entity-picker focus. Do not edit showHero/showRail in rechnung.tsx (rail and Vorschau already hide). Do not restore SAMPLE_INVOICE from this CTA (04-04/STATE: focus form). Do not add Übersicht/Banking/Senden (D-13). Header picker label on numbered drafts stays invoiceLabel (G-04-1 already closed).
  </action>
  <verify>
    <automated>pnpm --filter desktop exec vitest run src/__tests__/phase03-autosave.test.tsx && python3 -c "
from pathlib import Path
p=Path('apps/desktop/src/components/invoice-empty-state.tsx').read_text()
assert 'Beispielrechnung anzeigen' in p
assert '/empty-state-hero.png' in p
r=Path('apps/desktop/src/routes/rechnung.tsx').read_text()
assert 'showRail = !showHero' in r
print('G-04-4 gates ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - invoice-empty-state.tsx button label is Beispielrechnung anzeigen.
    - phase03-autosave.test.tsx uses that accessible name for the empty-list start click.
    - rechnung.tsx still sets showRail = !showHero.
  </acceptance_criteria>
  <done>Empty invoice canvas shows documented illustration and return CTA; tax rail and Vorschau stay hidden while showHero is true.</done>
</task>

<task type="auto" tdd="true">
  <name>Wire LoginGate Crafted hero and system-font wordmark</name>
  <files>apps/desktop/src/auth/login-gate.tsx, apps/desktop/src/__tests__/auth-gate.test.tsx</files>
  <read_first>
    - apps/desktop/src/auth/login-gate.tsx
    - apps/desktop/src/__tests__/auth-gate.test.tsx
    - .planning/phases/04-premium-ui-brand-redesign/04-03-PLAN.md (hero img src, Clared h1)
    - apps/desktop/public/login-gate-hero.png (exists; do not regenerate)
  </read_first>
  <behavior>
    - LoginGate unsigned render includes an img whose src is /login-gate-hero.png and a heading named Clared.
    - Anmelden still invokes open_login_window when SessionContext is absent.
  </behavior>
  <action>
    G-04-5 per D-04, D-14, D-15, and 04-03 (Using existing public PNG per user decision; no Higgsfield create). RED: unsigned gate test asserts getByRole img or container query for src /login-gate-hero.png plus heading Clared. GREEN: add img src="/login-gate-hero.png" alt="" (decorative; D-25 first-party path only). Wordmark h1 text Clared with font-sans (system stack, D-03/D-04). Root canvas uses semantic bg-background (tokens swap Light/Dark, D-08). Keep DE/EN locale toggle and bilingual copy already in the file (D-15). Keep handleLogin session.login / invoke open_login_window. Keep CTA_CLASS press scale and motion-reduce (D-17). Do not add Darstellung to SessionChip. Do not fetch remote image URLs.
  </action>
  <verify>
    <automated>pnpm --filter desktop exec vitest run src/__tests__/auth-gate.test.tsx && python3 -c "
from pathlib import Path
t=Path('apps/desktop/src/auth/login-gate.tsx').read_text()
assert 'src=\"/login-gate-hero.png\"' in t or \"src='/login-gate-hero.png'\" in t
assert 'font-sans' in t
assert 'Clared' in t
assert 'open_login_window' in t
print('G-04-5 gates ok')
"</automated>
    <!-- planner-discipline-allow: font-serif -->
    <!-- planner-discipline-allow: bg-[#f7f7f5] -->
  </verify>
  <acceptance_criteria>
    - login-gate.tsx contains img src /login-gate-hero.png and h1 Clared with font-sans.
    - auth-gate.test.tsx asserts the hero img src.
    - Login path still invoke open_login_window / session.login.
  </acceptance_criteria>
  <done>Unsigned user sees Crafted login hero plus system-font Clared and can Anmelden; PNG is no longer orphaned.</done>
</task>

</tasks>

<threat_model>
ASVS level: 1. Block on: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → theme.ts / main.tsx | Untrusted clared-theme string still gated by PREFS |
| matchMedia → applyTheme | OS appearance event drives paint only when pref is system |
| static PNG → LoginGate / empty state | First-party public/ paths only |
| LoginGate CTA → Tauri invoke | Existing open_login_window; no new privileged commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | localStorage clared-theme | low | mitigate | Keep PREFS.has; unknown → system; syncSystemAppearance reads currentPref only |
| T-04-06 | Tampering | public PNG paths | low | mitigate | img src stays /login-gate-hero.png and /empty-state-hero.png; no https:// illustration URLs (D-25) |
| T-04-07 | Spoofing | login-gate hero | low | accept | Cosmetic first-party PNG; OIDC still session.login / open_login_window (Phase 2) |
| T-04-08 | Tampering | applyTheme inline styles | low | mitigate | OS listener calls applyTheme/paintDocument only; no innerHTML/eval of stored pref |
| T-04-12 | Tampering | matchMedia change | low | mitigate | syncSystemAppearance no-ops unless currentPref()==="system" so a spoofed OS event cannot override Hell/Dunkel |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package install in this gap-closure plan |

</threat_model>

<verification>
- pnpm --filter desktop exec vitest run src/lib/theme.test.ts
- pnpm --filter desktop exec vitest run src/__tests__/phase03-autosave.test.tsx
- pnpm --filter desktop exec vitest run src/__tests__/auth-gate.test.tsx
- python gates: syncSystemAppearance in main+theme; empty CTA string; login img src
- Human: 04-UAT 1–3 (1:1 craft vs mockups 02–09 is visual only; G-04-6 paint on OS flip; splash hold still PBU)
</verification>

<success_criteria>
G-04-4, G-04-5, G-04-6 closed in code: empty return CTA matches UI-SPEC, LoginGate shows /login-gate-hero.png with sans Clared, OS change while system re-paints D-02 hex. D-13 IA unchanged. G-04-1/2/3 not re-opened. Splash min-hold remains human UAT.
</success_criteria>

## Artifacts this phase produces (Plan 04-05)

- **OS paint sync:** `syncSystemAppearance` in `theme.ts`; `main.tsx` change listener calls it; tests in `theme.test.ts`.
- **Empty CTA:** `invoice-empty-state.tsx` button copy Beispielrechnung anzeigen; `phase03-autosave.test.tsx` clicks that name.
- **Login hero:** `login-gate.tsx` wires `/login-gate-hero.png` + `font-sans` wordmark; `auth-gate.test.tsx` asserts img.

<output>
Create `.planning/phases/04-premium-ui-brand-redesign/04-05-SUMMARY.md` when done
</output>
