---
phase: 04-premium-ui-brand-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/styles/globals.css
  - packages/ui/src/styles/globals.css
  - apps/desktop/src/lib/theme.ts
  - apps/desktop/src/lib/theme.test.ts
  - apps/desktop/src/lib/theme-menu.ts
  - apps/desktop/src/main.tsx
  - apps/desktop/src/App.tsx
  - apps/desktop/src/components/splash.tsx
  - apps/desktop/vite.config.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [UI-01, BRAND-01]
estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 4
  confidence: med
must_haves:
  truths:
    - "Crafted Minimal tokens are the single visual SSOT; both Light and Dark render from one component tree (D-08) — no duplicate Light/Dark components, no second CSS bundle."
    - "On launch appearance follows the OS (D-06): with no stored pref the app resolves via matchMedia('(prefers-color-scheme: dark)')."
    - "The Darstellung native app menu offers Hell / Dunkel / System (D-07); selecting one persists the pref and re-resolves the .dark class live; the session chip gains no theme item."
    - "A short launch splash shows the wordmark 'Clared' in the system font plus a spinner, then the shell (D-16, D-04)."
    - "Motion: transitions run under 300ms with a custom ease-out and honor prefers-reduced-motion; primary-button press applies scale(0.97) (D-17)."
    - "Money numerals use font-variant-numeric: tabular-nums via a utility, not a second font family (D-03)."
    - "BRAND-01 exists in .planning/REQUIREMENTS.md under Desktop & UI and in the traceability table mapped to Phase 4 (D-05)."
    - "The ROADMAP Phase 4 section names only 'Crafted Minimal' — the superseded 'Nordic Calm Fintech' string is gone from the heading AND Success Criteria (D-01)."
    - "Typography is the system font stack only; no webfont family remains in the token file (D-03)."
    - "The visual SSOT is mockups/approved/ and ONE 04-UI-SPEC.md covers all in-scope surfaces before code (D-10) — not page-by-page image gates."
  artifacts:
    - apps/desktop/src/lib/theme.ts
    - apps/desktop/src/lib/theme.test.ts
    - apps/desktop/src/lib/theme-menu.ts
    - apps/desktop/src/components/splash.tsx
    - apps/desktop/src/styles/globals.css
    - packages/ui/src/styles/globals.css
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  key_links:
    - "main.tsx calls applyTheme(currentPref()) BEFORE ReactDOM render — pre-paint ordering prevents FOUC (Pitfall 1)."
    - "theme-menu action → applyTheme(id) → document.documentElement.classList.toggle('dark') → Tailwind :root/.dark cascade → every screen."
    - "matchMedia('(prefers-color-scheme: dark)') 'change' listener re-resolves ONLY while pref === 'system'."
    - "Reduced-motion is OS-driven and independent of the Darstellung color override (D-17) — never gate motion on the color pref."
  prohibitions:
    - "must NOT ship two CSS bundles or duplicate Light/Dark component trees (D-08)."
    - "must NOT add a theme control to the session chip (D-07 / Phase 2 D-36) — chip is identity + logout only."
    - "must NOT add nav items or routes; information architecture is frozen (D-11)."
    - "must NOT package a webfont; system fonts only (D-03)."
    - "must NOT let the Darstellung override win over the OS for reduced-motion (D-17)."
    - "must NOT implement out-of-scope mockups (10-onboarding, 11-settings, 12-profile, 13-dashboard, 14-catalog, 15-export) nor resurrect rejected explorations (Nordic / glass / warm-editorial / corporate-teal) (D-13)."
    - "must NOT build a custom titlebar; keep the native OS titlebar with decorations on even if a mockup shows one (D-19)."
  assumptions:
    - "[ASSUMED A1] Exact Amber --destructive hex sampled from 03-rechnung-dark.png — sparse emphasis only (D-02 discretion)."
    - "[ASSUMED A2] Dark-theme raised-surface / hairline HSL sampled from 03-rechnung-dark.png & 05-entities-dark.png (D-02 discretion)."
    - "[ASSUMED A3] Motion --dur 180ms + cubic-bezier(0.22,1,0.36,1); any <300ms ease-out satisfies D-17."
    - "[ASSUMED A4] core:default capability permits the JS menu build; if a core:menu ACL error appears at runtime add core:menu:default to src-tauri/capabilities/default.json."
    - "[FLAGGED EDGE — ordering] List ordering is inapplicable to a restyle: no new sort is introduced; Phase 3 list order is preserved unchanged. Not a UI acceptance for this phase."
---

<objective>
Establish the Crafted Minimal visual system and the OS-follow theme engine that every other Phase 4 plan builds on. This is the phase's Wave-1 blocker: the token split in globals.css plus the theme resolve/apply/persist logic, wired end-to-end so one component tree renders both Light and Dark, with a native Darstellung menu and a launch splash.

Purpose: BRAND-01 — turn "Crafted Minimal" (color tokens, system type, motion, tabular numerals) into the single source of truth, and replace Phase 1's forced-dark boot with OS-follow + persisted override (D-06/D-07/D-08). Until this ships, per-route restyle has no tokens to consume.
Output: rewritten dual-theme globals.css (both copies), lib/theme.ts (+ RED→GREEN test), lib/theme-menu.ts, splash component, OS-follow main.tsx boot, restyled App shell, and BRAND-01 added to REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-premium-ui-brand-redesign/04-CONTEXT.md
@.planning/phases/04-premium-ui-brand-redesign/04-RESEARCH.md
@.planning/phases/04-premium-ui-brand-redesign/04-PATTERNS.md
@.planning/phases/04-premium-ui-brand-redesign/04-UI-SPEC.md
@.planning/phases/04-premium-ui-brand-redesign/mockups/approved/01-brandkit.png
@.planning/phases/04-premium-ui-brand-redesign/mockups/approved/03-rechnung-dark.png
@docs/macos-ui-design-mit-tauri.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end theme engine + dual-theme token split (one path, both themes)</name>
  <files>apps/desktop/src/lib/theme.ts, apps/desktop/src/lib/theme.test.ts, apps/desktop/src/styles/globals.css, packages/ui/src/styles/globals.css, apps/desktop/src/main.tsx, apps/desktop/vite.config.ts</files>
  <read_first>
    - apps/desktop/src/lib/theme.ts (new — no analog; RESEARCH Pattern 2)
    - apps/desktop/src/styles/globals.css (the file main.tsx imports — active token SSOT; currently `:root, .dark` forced navy, webfont sans, body font hard-coded)
    - packages/ui/src/styles/globals.css (mirror copy — keep in sync)
    - apps/desktop/src/main.tsx (boot; line 6 forces dark unconditionally)
    - apps/desktop/src/components/spinner.tsx (matchMedia reduced-motion idiom — reuse SSOT)
    - .planning/phases/04-premium-ui-brand-redesign/04-RESEARCH.md (Pattern 1, Pattern 2, Pattern 4, Wave 0 Gaps, Assumptions A1–A3)
    - .planning/phases/04-premium-ui-brand-redesign/mockups/approved/03-rechnung-dark.png (sample Amber + dark HSL, A1/A2)
    - .planning/phases/04-premium-ui-brand-redesign/mockups/approved/02-rechnung-light.png (Light palette reference)
  </read_first>
  <behavior>
    - resolveDark("dark") === true; resolveDark("light") === false.
    - resolveDark("system") follows a mocked matchMedia('(prefers-color-scheme: dark)').matches.
    - applyTheme("dark") adds the dark class to documentElement; applyTheme("light") removes it; applyTheme persists the pref to localStorage under THEME_KEY.
    - currentPref() returns the persisted pref, defaulting to "system" when localStorage is empty or holds an unknown value (guarded cast).
  </behavior>
  <action>
    RED first: create apps/desktop/src/lib/theme.test.ts (Vitest + jsdom) asserting the four behaviors above with mocked matchMedia and localStorage; confirm a jsdom test environment exists in apps/desktop/vite.config.ts (add a test block with environment "jsdom" if the config lacks one — this is the Wave 0 vitest gap). Run it, watch it fail. GREEN: implement apps/desktop/src/lib/theme.ts exporting THEME_KEY, the ThemePref union (light|dark|system), resolveDark(pref), applyTheme(pref), currentPref() per RESEARCH Pattern 2 (~15 lines; guard the localStorage read with a "system" fallback — non-secret enum, no privilege). Then split the token file: in apps/desktop/src/styles/globals.css keep the @theme inline alias block and @custom-variant dark verbatim; break the shared forced-dark rule into a Light :root block (Pure White #FFFFFF background, Pale Oatmeal #F7F7F5 card/sidebar, Deep Charcoal #111110 foreground, Muted Stone #8A8A8A muted-foreground, Soft Sage Green #A8BFA3 primary+accent, sampled Amber destructive [A1], oatmeal-darker border, radius 8px) and a Dark .dark block (Deep Charcoal background, sampled raised-surface card [A2], Oatmeal foreground, Sage accent, sampled Amber destructive, charcoal hairline border — "whisper separator", radius 8px); replace the webfont sans declaration with the system stack (-apple-system, BlinkMacSystemFont, "Segoe UI", system-ui, sans-serif); set body font-family to var(--font-sans); add a .tabular-nums utility (font-variant-numeric: tabular-nums), motion tokens (--ease-out cubic-bezier(0.22,1,0.36,1), --dur 180ms) and a prefers-reduced-motion media block clamping transition/animation durations. Mirror the identical rewrite into packages/ui/src/styles/globals.css so the two copies stay consistent. In apps/desktop/src/main.tsx remove the boot-time forced-dark line (line 6) and instead import from ./lib/theme and call applyTheme(currentPref()) before ReactDOM render, then register a matchMedia('(prefers-color-scheme: dark)') "change" listener that re-toggles the dark class only when currentPref() === "system". Do not touch pdf-paper inline colors.
  </action>
  <verify>
    <automated>pnpm --filter desktop test theme && pnpm --filter desktop build && grep -q '\.dark {' apps/desktop/src/styles/globals.css && grep -q ':root {' apps/desktop/src/styles/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter desktop test theme` exits 0 (all four theme behaviors pass).
    - `pnpm --filter desktop build` exits 0.
    - apps/desktop/src/styles/globals.css contains `:root {` and `.dark {` as two separate rule blocks (no combined `:root,` `.dark` selector).
    - `grep -c 'Inter' apps/desktop/src/styles/globals.css` returns 0 (webfont removed, D-03).
    - `grep -q 'classList.add("dark")' apps/desktop/src/main.tsx` fails — the unconditional forced-dark mutation is gone.
    - apps/desktop/src/lib/theme.ts exports THEME_KEY, ThemePref, resolveDark, applyTheme, currentPref.
    - packages/ui/src/styles/globals.css has the same split :root/.dark blocks (mirror in sync).
  </acceptance_criteria>
  <done>Theme logic is tested and green; the token file renders both themes from one tree; boot follows the OS with no forced dark and no FOUC.</done>
  <reversibility rating="costly">D-06 OS-follow + D-01 palette: every token and Higgsfield crop must work in both themes; reversing means re-deriving the whole palette.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Darstellung native menu + App shell restyle + launch splash</name>
  <files>apps/desktop/src/lib/theme-menu.ts, apps/desktop/src/App.tsx, apps/desktop/src/components/splash.tsx, apps/desktop/src/main.tsx</files>
  <read_first>
    - apps/desktop/src/App.tsx (AppShell, NAV_ITEMS, hash router, sidebar active/idle token classes at :39-51)
    - apps/desktop/src/main.tsx (call site after applyTheme)
    - apps/desktop/src/components/spinner.tsx (spinner + reduced-motion gate for the splash)
    - .planning/phases/04-premium-ui-brand-redesign/04-RESEARCH.md (Pattern 3 native menu, Pitfall 3 ACL)
    - .planning/phases/04-premium-ui-brand-redesign/04-PATTERNS.md ("Token consumption" App.tsx:39-51, Darstellung menu assignment)
    - .planning/phases/04-premium-ui-brand-redesign/mockups/approved/04-entities-light.png (floating/whisper-separator sidebar treatment)
  </read_first>
  <action>
    Create apps/desktop/src/lib/theme-menu.ts using @tauri-apps/api/menu (RESEARCH Pattern 3): a Submenu titled "Darstellung" holding three CheckMenuItem entries with ids matching ThemePref — text "Hell", "Dunkel", "System"; each item's action calls applyTheme(id) then re-syncs all three checkmarks against currentPref(); build the Menu and call setAsAppMenu(). Include the platform default app submenu items so nothing is lost when replacing the menu on macOS. Invoke this builder once from main.tsx after applyTheme(currentPref()) (main window only; guard against non-Tauri/test env). Restyle apps/desktop/src/App.tsx chrome 1:1 to the approved entities/rechnung mockups: sidebar uses the new Sage accent for the active NavLink (bg-accent text-accent-foreground) and muted-foreground for idle, a light "whisper separator" border via the new border token (not a heavy painted rail); keep the existing NAV_ITEMS set and hash router untouched — add no nav entries. Create apps/desktop/src/components/splash.tsx: the wordmark "Clared" in the system font (D-04) plus the existing Spinner, honoring prefers-reduced-motion; mount it in App while /me is warming, then reveal the shell (D-16 — typography-only splash; splash.png stays optional atmosphere for a later plan). If a core:menu ACL error surfaces at first run, add core:menu:default to src-tauri/capabilities/default.json (record in SUMMARY).
  </action>
  <verify>
    <automated>pnpm --filter desktop build && grep -q 'Darstellung' apps/desktop/src/lib/theme-menu.ts && grep -q 'setAsAppMenu' apps/desktop/src/lib/theme-menu.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/desktop/src/lib/theme-menu.ts references @tauri-apps/api/menu, defines a "Darstellung" submenu with three CheckMenuItem ("Hell", "Dunkel", "System"), and calls setAsAppMenu.
    - apps/desktop/src/components/splash.tsx renders the text "Clared" and the Spinner component.
    - `grep -c 'NAV_ITEMS' apps/desktop/src/App.tsx` shows the array is referenced but the entry count is unchanged from the pre-restyle file (no nav items added, D-11).
    - `pnpm --filter desktop build` exits 0.
    - The theme menu is wired in the menu file, NOT the chip: `grep -q 'Darstellung' apps/desktop/src/lib/theme-menu.ts` succeeds.
    - The session chip gains no theme menu item (D-07): `grep -c 'Darstellung' apps/desktop/src/components/session-chip.tsx` returns 0 (chip stays identity + logout only; real path is components/, not auth/).
  </acceptance_criteria>
  <!-- planner-discipline-allow: Darstellung -->

  <done>The OS menu shows Darstellung with a live-checked Hell/Dunkel/System; the shell is restyled to the mockups with a Sage-accent sidebar; launch shows a wordmark+spinner splash.</done>
  <reversibility rating="reversible">Menu and shell chrome are pure webview/TS; revertable without data or config migration.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Add BRAND-01 requirement (D-05)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (Desktop & UI section, Traceability table)
    - .planning/phases/04-premium-ui-brand-redesign/04-CONTEXT.md (D-05 wording)
  </read_first>
  <action>
    In .planning/REQUIREMENTS.md add BRAND-01 under the "Desktop & UI" list: "BRAND-01: Crafted Minimal is the visual system (design tokens + approved-mockup SSOT + motion). UI-01 still requires mockup/UI-SPEC before code." Add a Traceability row mapping BRAND-01 → Phase 4 → Pending. Update the coverage counts to reflect the new requirement. Do not renumber or alter existing requirement IDs.
  </action>
  <verify>
    <automated>grep -q 'BRAND-01' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md contains a BRAND-01 entry in the Desktop & UI section.
    - .planning/REQUIREMENTS.md Traceability table has a BRAND-01 → Phase 4 row.
    - No existing requirement ID text is modified.
  </acceptance_criteria>
  <done>BRAND-01 is a tracked v1 requirement mapped to Phase 4 (D-05 satisfied).</done>
</task>

<task type="auto">
  <name>Task 4: Retitle ROADMAP Phase 4 to Crafted Minimal (D-01)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 4 section — heading, Goal, Success Criteria; the superseded "Nordic Calm Fintech" string survives in Success Criteria #1)
    - .planning/phases/04-premium-ui-brand-redesign/04-CONTEXT.md (D-01 name SSOT)
  </read_first>
  <action>
    In .planning/ROADMAP.md, within the "### Phase 4: Premium UI & Brand Redesign" section only, replace the superseded brand name "Nordic Calm Fintech" with "Crafted Minimal" per D-01 — specifically Success Criteria #1 (currently 'A "Nordic Calm Fintech" design system … exists as SSOT and is approved'). Preserve the rest of that criterion's wording; change only the brand-name token. Do not edit any other phase section, requirement IDs, or unrelated ROADMAP content. Use scoped Edit, not a whole-file rewrite.
  </action>
  <verify>
    <automated>awk '/^### Phase 4:/{f=1;next} /^### Phase 5:/{f=0} f' .planning/ROADMAP.md | grep -c 'Nordic Calm Fintech' | grep -qx 0 && grep -q 'Crafted Minimal' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - The Phase 4 section of .planning/ROADMAP.md contains zero occurrences of "Nordic Calm Fintech" (heading + Goal + all Success Criteria) — proven by the awk-scoped `grep -c … | grep -qx 0` above.
    - Success Criteria #1 in the Phase 4 section names "Crafted Minimal".
    - No other ROADMAP phase section or requirement ID is modified.
  </acceptance_criteria>
  <!-- planner-discipline-allow: Nordic Calm Fintech -->
  <done>ROADMAP Phase 4 is fully retitled to "Crafted Minimal"; no superseded brand name remains in that section (D-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → webview | Persisted theme pref read at boot (untrusted local storage) |
| native menu → webview | Tauri menu action dispatches into the React app |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | localStorage["clared-theme"] | low | mitigate | Value is a non-secret enum; currentPref() guards the read and defaults to "system" on any unknown value — no privilege attached. |
| T-04-02 | Elevation | Darstellung menu action | low | accept | Menu actions call local pure functions (applyTheme) only; no eval, no new IPC to privileged commands; keychain/session IPC unchanged. |
| T-04-03 | Tampering | globals.css token asset | low | accept | First-party CSS built into the bundle; no remote token source. |
| T-04-SC | Tampering | npm/cargo installs | low | accept | No package install this phase (RESEARCH: all deps pre-existing); Package Legitimacy Gate not applicable. |

No high/critical threats — nothing to block on.
</threat_model>

<verification>
- `pnpm --filter desktop test theme` green (BRAND-01 theme unit).
- `pnpm --filter desktop build` exit 0.
- globals.css (both copies) has separate `:root {` / `.dark {` blocks, system font, no webfont, tabular-nums + motion tokens.
- main.tsx boots via applyTheme(currentPref()); no unconditional dark class.
- theme-menu.ts builds the Darstellung app menu; splash renders wordmark + spinner. Chip has no theme control (D-07).
- BRAND-01 present in REQUIREMENTS.md + traceability.
- ROADMAP Phase 4 section retitled to "Crafted Minimal"; no "Nordic Calm Fintech" remains in that section (D-01).
- Manual UAT (phase gate): menu appears; Hell/Dunkel/System switch live; no FOUC at launch; reduced-motion honored.
</verification>

<success_criteria>
The Crafted Minimal token system and OS-follow theme engine are live and tested; the shell renders both themes from one tree; Darstellung menu + splash exist; BRAND-01 is a tracked requirement. Wave 2 can restyle per-route against real tokens.
</success_criteria>

## Artifacts this phase produces (Plan 04-01)

- **New files:** `apps/desktop/src/lib/theme.ts`, `apps/desktop/src/lib/theme.test.ts`, `apps/desktop/src/lib/theme-menu.ts`, `apps/desktop/src/components/splash.tsx`.
- **Functions/exports (theme.ts):** `THEME_KEY`, `ThemePref` (type: light|dark|system), `resolveDark(pref)`, `applyTheme(pref)`, `currentPref()`.
- **CSS token names added/changed in `globals.css` (both `apps/desktop/src/styles/` and `packages/ui/src/styles/`):** light `:root` + dark `.dark` blocks for `--background`, `--card`, `--foreground`, `--muted-foreground`, `--primary`, `--accent`, `--accent-foreground`, `--destructive`, `--border`, `--radius`; `--font-sans` (system stack); utility `.tabular-nums`; motion tokens `--ease-out`, `--dur`; `prefers-reduced-motion` media block.
- **Boot wiring:** `main.tsx` OS-follow initializer + matchMedia change listener; `theme-menu.ts` `Darstellung` app menu wired from `main.tsx`.
- **Requirement:** `BRAND-01` added to `.planning/REQUIREMENTS.md`.
- **ROADMAP:** Phase 4 section retitled "Nordic Calm Fintech" → "Crafted Minimal" — executor Task 4 (heading, Goal, and Success Criteria), grep-verified.

> **Validation note (W3):** `04-VALIDATION.md` is intentionally the empty template (`nyquist_compliant: false`) at plan time. It is filled by `/gsd-validate-phase 4` after execution — do NOT fabricate validation content here.

<output>
Create `.planning/phases/04-premium-ui-brand-redesign/04-01-SUMMARY.md` when done.
</output>
