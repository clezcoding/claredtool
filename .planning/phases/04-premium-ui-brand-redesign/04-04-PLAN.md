---
phase: 04-premium-ui-brand-redesign
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - apps/desktop/index.html
  - apps/desktop/src/lib/theme.ts
  - apps/desktop/src/lib/theme.test.ts
  - apps/desktop/src/components/splash.tsx
  - apps/desktop/src/App.tsx
  - packages/ui/src/components/combobox.tsx
  - apps/desktop/src/routes/rechnung.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
gap_ids: [G-04-1, G-04-2, G-04-3]
requirements: [UI-01, BRAND-01]
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Empty states render the documented empty copy and illustration. Return CTA is Beispielrechnung anzeigen. Tax rail and Vorschau hide while empty state is showing."
    - "Splash screen (wordmark + spinner) on launch. In-app loading uses existing skeleton.tsx matching layout, not full-screen spinners."
    - "Error states render the documented error copy with retry options."
    - "Motion durations under 300ms, custom ease-out, prefers-reduced-motion respected. Button press scale(0.97) is disabled when that media query matches."
    - "Cold launch first paint of html and body is Pale Oatmeal #F7F7F5 (light) or Deep Charcoal #111110 (dark) with matching color-scheme, never the UA default unstyled canvas (G-04-2, D-02, D-06)."
    - "Splash wordmark Clared plus an observable spinner paints with inline styles before Tailwind/CSS arrives and remains until CSS is applied and the min hold elapses (G-04-3, D-16, D-04)."
    - "Invoice header picker visible text is the invoiceLabel string (number or Neue Rechnung); the control value is the invoice id string, not an InvoiceRow object (G-04-1)."
    - "ComboboxInput chevron/clear is a single interactive control — no button nested inside another button (G-04-1)."
    - "In-scope surfaces keep Crafted Minimal density: whisper separators (border-border/70), sage accent pills, existing 5-item shell (D-11, D-13)."
  artifacts:
    - apps/desktop/index.html
    - apps/desktop/src/lib/theme.ts
    - apps/desktop/src/lib/theme.test.ts
    - apps/desktop/src/components/splash.tsx
    - apps/desktop/src/App.tsx
    - packages/ui/src/components/combobox.tsx
    - apps/desktop/src/routes/rechnung.tsx
    - .planning/REQUIREMENTS.md
  key_links:
    - "index.html blocking script and applyTheme(currentPref()) share resolveDark() semantics and D-02 hex so html/body inline background stays aligned after React mounts."
    - "AuthenticatedApp holds Splash while state==='boot' OR minSplashDone is false (SPLASH_HOLD_MS 700 in non-test)."
    - "invoiceLabel(invoice, isUnnumbered) feeds picker display; SelectItem/Combobox itemToStringValue uses invoice.id as value."
    - "ComboboxClear/Trigger composition in packages/ui combobox.tsx is the shared fix for entities.tsx and kunden.tsx callers."
  prohibitions:
    - "must NOT ship two CSS bundles or duplicate Light/Dark component trees (D-08)."
    - "must NOT add a theme control to the session chip (D-07 / Phase 2 D-36) — chip is identity + logout only."
    - "must NOT add nav items or routes; information architecture is frozen (D-11)."
    - "must NOT package a webfont; system fonts only (D-03)."
    - "must NOT let the Darstellung override win over the OS for reduced-motion (D-17)."
    - "must NOT implement out-of-scope mockups (10-onboarding, 11-settings, 12-profile, 13-dashboard, 14-catalog, 15-export) nor resurrect rejected explorations (Nordic / glass / warm-editorial / corporate-teal) (D-13)."
    - "must NOT build a custom titlebar; keep the native OS titlebar with decorations on even if a mockup shows one (D-19)."
    - "must NOT invert PdfPaper colors in dark theme (D-09)."
    - "must NOT generate in-app illustrations at runtime (D-25)."
    - "must NOT invent a second dark palette to match cooler mockup-03 rgb; token hex stays D-02 Crafted Minimal."
  assumptions:
    - "[FLAGGED EDGE — adjacency / UI-01 probe] Where two surfaces meet they stay distinct whisper-separated regions (border-border/70), not a merged slab. No new collision algorithm."
    - "[RESOLVED EDGE — empty] Empty/loading/error follow 04-UI-SPEC UI Considerations; do not re-derive empty from the UI-01 specless probe."
    - "[FLAGGED EDGE — ordering / UI-01 probe] No new sort. Draft picker, entity list, and country options keep Phase 3 order."
    - "[FLAGGED — BRAND-01 unclassified probe] BRAND-01 remains tokens + approved-mockup SSOT + motion; gap-closure does not add a second identity or appearance model."
    - "[ASSUMED] Vite CSS may arrive after first HTML parse; inline html/body paint is the FOUC guard, not a second stylesheet."
---

<objective>
Close UAT gaps G-04-1, G-04-2, and G-04-3 so a user can cold-launch without a white flash, see the Clared splash, then pick an invoice without object text in the picker — restyling the existing 5-item shell only (D-13).

Purpose: Make VERIFICATION.md human 1:1 truths re-testable by fixing diagnosed boot and picker bugs, not by re-asking visual 1:1 with no code change.
Output: Boot inline paint + splash hold, shared Combobox unnest, invoice picker string display, REQUIREMENTS traceability rows for PDF-01/OFFL-01/AUDT-01 pointed at Phase 5.
Locked decisions (do not regress; D-05: BRAND-01 already in REQUIREMENTS): D-01: Crafted Minimal name SSOT. D-02: D-02 palette (oatmeal/charcoal boot paint). D-03: system fonts. D-04: wordmark Clared. D-06: OS-follow. D-07: Darstellung menu not chip. D-08: one component tree. D-09: PdfPaper stays light. D-10: one UI-SPEC. D-11: IA frozen 5-item shell. D-12: in-scope mockups 01–09. D-13: no mockups 10–15. D-14: no new Higgsfield spend. D-15: DE primary copy. D-16: splash wordmark+spinner. D-17: reduced-motion. D-18: in-app skeletons. D-19: native titlebar.
</objective>

<assumption_delta_decision>
noun: Crafted Minimal appearance pref (`system` | `light` | `dark`)
decision: no-change
rationale: ROADMAP phrase custom ease-out is a CSS motion token, not a modeling change. Gap-closure does not introduce a second identity model.
</assumption_delta_decision>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-premium-ui-brand-redesign/04-CONTEXT.md
@.planning/phases/04-premium-ui-brand-redesign/04-UAT.md
@.planning/phases/04-premium-ui-brand-redesign/04-UI-SPEC.md
@.planning/phases/04-premium-ui-brand-redesign/04-PATTERNS.md
@.planning/phases/04-premium-ui-brand-redesign/04-SECURITY.md
@apps/desktop/index.html
@apps/desktop/src/lib/theme.ts
@apps/desktop/src/components/splash.tsx
@apps/desktop/src/App.tsx
@packages/ui/src/components/combobox.tsx
@apps/desktop/src/routes/rechnung.tsx
</context>

<interfaces>
From apps/desktop/src/lib/theme.ts:
THEME_KEY, ThemePref = "light" | "dark" | "system", resolveDark(pref), applyTheme(pref), currentPref()

D-02 paint hex (boot inline + applyTheme):
light document background #F7F7F5 (Pale Oatmeal), dark #111110 (Deep Charcoal)

From apps/desktop/src/routes/rechnung.tsx:
invoiceLabel(invoice: InvoiceRow | null, isCurrentUnnumbered: boolean): string
currentPickerInvoice / pickerLabel / draftId

From apps/desktop/src/App.tsx:
SPLASH_HOLD_MS (0 in test MODE, 700 otherwise)
AuthenticatedApp: Splash while state === "boot" || !minSplashDone

From packages/ui/src/components/combobox.tsx:
Combobox, ComboboxInput, ComboboxTrigger, ComboboxClear
ComboboxClear currently render-composes the input-group icon Button primitive (G-04-1)
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end cold launch — oatmeal/charcoal first paint then observable Clared splash</name>
  <precondition>apps/desktop/src/lib/theme.ts exports resolveDark, applyTheme, currentPref from plan 04-01</precondition>
  <reversibility rating="reversible">Inline boot colors and splash hold are local paint; appearance pref model stays system|light|dark (D-06).</reversibility>
  <files>apps/desktop/index.html, apps/desktop/src/lib/theme.ts, apps/desktop/src/lib/theme.test.ts, apps/desktop/src/components/splash.tsx, apps/desktop/src/App.tsx</files>
  <read_first>
    - apps/desktop/index.html (blocking theme IIFE — today paints html only; light uses Pure White which matches UA flash)
    - apps/desktop/src/lib/theme.ts (class toggle only; no inline background/color-scheme)
    - apps/desktop/src/lib/theme.test.ts (extend applyTheme assertions)
    - apps/desktop/src/components/splash.tsx (wordmark exists; spinner still class-based)
    - apps/desktop/src/App.tsx (SPLASH_HOLD_MS / minSplashDone)
    - apps/desktop/src/main.tsx (confirm applyTheme(currentPref()) still runs before ReactDOM createRoot)
    - apps/desktop/src/components/spinner.tsx (reduced-motion gate idiom — reuse, do not add theme items)
    - .planning/phases/04-premium-ui-brand-redesign/04-UAT.md (G-04-2, G-04-3)
  </read_first>
  <behavior>
    - applyTheme("dark") sets documentElement and body style.background to #111110 and colorScheme to dark, and adds class dark.
    - applyTheme("light") sets documentElement and body style.background to #F7F7F5 and colorScheme to light, and removes class dark.
    - applyTheme("system") with mocked matchMedia dark follows the dark paint; with light follows the light paint.
  </behavior>
  <action>
    G-04-2 / G-04-3. Per D-02 use Pale Oatmeal and Deep Charcoal for first paint (not the UA canvas color). RED: extend theme.test.ts with the three applyTheme paint behaviors above (jsdom body + documentElement). GREEN: applyTheme calls resolveDark then writes the same background and color-scheme onto html and body that the boot script uses. In index.html, the blocking IIFE must toggle class dark from the same pref rules as currentPref/resolveDark (THEME_KEY clared-theme, unknown → system), then set background and color-scheme on both html and body. Catch path uses OS matchMedia the same way. Splash: inline layout/color/font on the root div (system stack per D-03); wordmark text Clared; spinner is a small ring with inline width/height/border so it paints without Tailwind; honor prefers-reduced-motion by omitting spin animation when that query matches (D-17) — do not gate motion on Darstellung. Do not add splash.png img unless D-16 typography splash is abandoned; keep type-only. App.tsx: keep Splash mounted while session state is boot or min hold has not elapsed; SPLASH_HOLD_MS stays 0 when import.meta.env.MODE === "test" else 700. Do not add Darstellung entries to SessionChip (D-07). Do not add NAV_ITEMS (D-11, D-13).
  </action>
  <verify>
    <automated>pnpm --filter desktop exec vitest run src/lib/theme.test.ts && python3 -c "import pathlib,re,sys; html=pathlib.Path('apps/desktop/index.html').read_text();
assert 'document.body' in html or 'body.style' in html;
assert '#F7F7F5' in html or '#f7f7f5' in html;
assert '#111110' in html;
t=pathlib.Path('apps/desktop/src/lib/theme.ts').read_text();
assert '#F7F7F5' in t or '#f7f7f5' in t;
assert '#111110' in t;
s=pathlib.Path('apps/desktop/src/components/splash.tsx').read_text();
assert 'Clared' in s;
assert 'style=' in s;
a=pathlib.Path('apps/desktop/src/App.tsx').read_text();
assert 'SPLASH_HOLD_MS' in a and 'minSplashDone' in a"</automated>
    <human-check>Cold launch with clared-theme cleared, OS Dark then OS Light: no full-window unstyled flash; splash shows Clared + spinner before LoginGate/shell.</human-check>
  </verify>
  <acceptance_criteria>
    - theme.test.ts covers applyTheme light/dark/system painting html+body background and color-scheme.
    - index.html IIFE sets background and color-scheme on html and body using D-02 oatmeal/charcoal.
    - Splash paints Clared + spinner via inline styles; App holds Splash for boot or 700ms (0 in test).
    - SessionChip unchanged: no theme menu item (D-07).
  </acceptance_criteria>
  <done>User can cold-launch and first paint is oatmeal or charcoal; splash wordmark+spinner is observable; Hell/Dunkel/System path unchanged after CSS loads.</done>
</task>

<task type="auto" tdd="false">
  <name>Invoice picker string display and shared Combobox single-control trigger</name>
  <files>packages/ui/src/components/combobox.tsx, apps/desktop/src/routes/rechnung.tsx</files>
  <read_first>
    - packages/ui/src/components/combobox.tsx (ComboboxInput addon: Trigger plus Clear render composition)
    - packages/ui/src/components/input-group.tsx (icon Button primitive used as Clear render)
    - apps/desktop/src/routes/rechnung.tsx (invoiceLabel, picker, draftId)
    - apps/desktop/src/routes/entities.tsx and kunden.tsx (Combobox callers — fix shared primitive once)
    - .planning/phases/04-premium-ui-brand-redesign/04-UAT.md (G-04-1)
  </read_first>
  <action>
    G-04-1. Locked D-13: restyle existing shell; do not add Übersicht/Banking/Senden or mockups 10–15. Invoice picker: visible label is invoiceLabel(...); control value is the invoice id string (Select value={draftId} + SelectItem value={row.id} with children invoiceLabel, or Combobox itemToStringValue that returns that string). Do not bind an InvoiceRow object as the visible input value. Grep desktop src for Combobox usages; keep entities/kunden itemToStringValue (labelDe / name). In combobox.tsx, Clear must not compose the input-group icon Button primitive as its render element — one native control for chevron/clear so React does not hydrate nested buttons. Trigger stays ComboboxPrimitive.Trigger without wrapping that same Button primitive. Keep ComboboxInput + InputGroup + InputGroupInput. Whisper/sage density on Rechnung header may use existing border-border/70 and bg-primary/30; do not retokenize PdfPaper (D-09).
    <!-- planner-discipline-allow: InputGroupButton -->
  </action>
  <verify>
    <automated>python3 -c "
from pathlib import Path
r=Path('apps/desktop/src/routes/rechnung.tsx').read_text()
assert 'function invoiceLabel' in r
assert 'SelectItem' in r and 'value={row.id}' in r
assert 'invoiceLabel(row' in r
c=Path('packages/ui/src/components/combobox.tsx').read_text()
assert 'render={<InputGroupButton' not in c
print('picker+combobox gates ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - rechnung picker selected value is row.id; displayed text uses invoiceLabel.
    - combobox.tsx has no render={<InputGroupButton on Clear.
    - entities.tsx and kunden.tsx inherit the Combobox fix; no new routes.
  </acceptance_criteria>
  <done>Invoice picker shows a human invoice label; Combobox no longer hydrates nested buttons; IA unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Point stale PDF-01 OFFL-01 AUDT-01 traceability at Phase 5</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (Traceability table rows PDF-01 / OFFL-01 / AUDT-01)
    - .planning/ROADMAP.md (Phase 5 owns those requirement IDs)
    - .planning/phases/04-premium-ui-brand-redesign/04-VERIFICATION.md (deferred section)
  </read_first>
  <action>
    Documentation only (D-05 BRAND-01 already present). In the Traceability table, set PDF-01, OFFL-01, and AUDT-01 Phase column to Phase 5 (not Phase 4). Leave status Pending. Do not implement PDF generation, offline sync, or audit_logs.
  </action>
  <verify>
    <automated>python3 -c "
from pathlib import Path
t=Path('.planning/REQUIREMENTS.md').read_text().split('## Traceability',1)[1]
for rid in ('PDF-01','OFFL-01','AUDT-01'):
    line=next(x for x in t.splitlines() if x.startswith('| '+rid))
    assert 'Phase 5' in line, line
    assert 'Phase 4' not in line, line
print('traceability ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - Traceability rows PDF-01, OFFL-01, AUDT-01 list Phase 5.
    - No product code for those requirements in this task.
  </acceptance_criteria>
  <done>REQUIREMENTS.md no longer maps Phase 5 work to Phase 4.</done>
</task>

</tasks>

<threat_model>
ASVS level: 1. Block on: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → boot IIFE / theme.ts | Untrusted clared-theme string crosses into class and inline paint |
| Combobox user text → invoice/entity ids | Picker must submit ids, not free-form objects as values |
| static first-party CSS/JS → webview | No new remote script |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | localStorage clared-theme | low | mitigate | Keep PREFS enum guard; unknown → system (already closed in 04-SECURITY.md; boot IIFE must use the same three-value check) |
| T-04-08 | Tampering | index.html boot IIFE inline styles | low | mitigate | Only set background and color-scheme from resolveDark-equivalent boolean; do not assign localStorage into innerHTML or eval |
| T-04-09 | Elevation of privilege | Splash hold | low | accept | Min hold is cosmetic; does not skip LoginGate or grant session |
| T-04-10 | Tampering | Combobox / invoice picker value | medium | mitigate | Control value is invoice/entity id string; display via invoiceLabel / itemToStringValue |
| T-04-11 | Tampering | Nested interactive controls | low | mitigate | Single button/trigger in ComboboxInput addon — prevents hydration mismatch hiding a broken picker |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package install in this gap-closure plan |
</threat_model>

<verification>
- `pnpm --filter desktop exec vitest run src/lib/theme.test.ts` green.
- index.html + theme.ts share D-02 oatmeal/charcoal inline paint on html and body.
- Splash Clared + inline spinner; App min hold.
- rechnung picker id + invoiceLabel; combobox Clear not composed from the input-group icon Button primitive.
- REQUIREMENTS traceability Phase 5 for PDF-01 OFFL-01 AUDT-01.
- Human re-test of 04-UAT tests 1–3 after execute (1:1 = restyle of 5-item shell, not mockup IA).
</verification>

<success_criteria>
G-04-1, G-04-2, G-04-3 are closed in code: no white FOUC, observable splash, picker shows invoiceLabel, no nested Combobox buttons, D-13 IA unchanged. Phase 4 human 1:1 can be re-run against Crafted tokens/density, not a second product IA.
</success_criteria>

## Artifacts this phase produces (Plan 04-04)

- **Boot paint:** index.html IIFE writes `background` + `color-scheme` on `html` and `body`; `applyTheme` mirrors those inline styles (hex `#F7F7F5` / `#111110`).
- **Splash hold:** `SPLASH_HOLD_MS` + `minSplashDone` in `App.tsx`; `Splash` inline wordmark + ring (no Tailwind dependency for first paint).
- **Picker contract:** `invoiceLabel` + id-valued Select/Combobox on `rechnung.tsx`.
- **Shared Combobox:** `packages/ui/src/components/combobox.tsx` Clear/Trigger unnested (callers `entities.tsx` / `kunden.tsx` inherit).
- **Docs:** REQUIREMENTS.md traceability Phase 5 for PDF-01, OFFL-01, AUDT-01.

<output>
Create `.planning/phases/04-premium-ui-brand-redesign/04-04-SUMMARY.md` when done
</output>
