---
phase: 04-premium-ui-brand-redesign
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/desktop/src/routes/entities.tsx
  - apps/desktop/src/routes/kunden.tsx
  - apps/desktop/src/auth/login-gate.tsx
  - apps/desktop/src/components/session-chip.tsx
  - apps/desktop/src/components/session-banner.tsx
  - apps/desktop/src/components/error-state.tsx
  - apps/desktop/src/components/skeleton.tsx
  - apps/desktop/src/components/spinner.tsx
  - apps/desktop/public/login-gate-hero.png
  - apps/desktop/public/empty-state-hero.png
  - apps/desktop/public/empty-entities.png
  - apps/desktop/public/splash.png
autonomous: true
requirements: [UI-01, BRAND-01]
user_setup:
  - service: higgsfield-cli
    why: "Generate the one missing Login-Gate hero (D-14); reuse existing Crafted PNGs for the rest."
    env_vars: []
    dashboard_config:
      - task: "Ensure `higgsfield` CLI is authenticated with the Starter plan (~10 credits). If credits/CLI are unavailable, generate the login-gate hero in ChatGPT and drop the PNG at apps/desktop/public/login-gate-hero.png (D-14 fallback)."
        location: "/opt/homebrew/bin/higgsfield"
estimate:
  tokens: 55000
  raw_tokens: 27000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Entities and Kunden are restyled 1:1 to 04-entities-light.png / 05-entities-dark.png; Kunden reuses the Entities list+panel structure (D-12); IA unchanged (D-11)."
    - "The login gate is restyled to 08-login.png with the new Crafted Login-Gate hero and the system-font wordmark 'Clared' (D-04); bilingual DE/EN copy where the mockup shows it (D-15)."
    - "The session chip stays identity + logout only — no theme item added (D-07 / Phase 2 D-36)."
    - "Error and skeleton states render the documented copy (UI-SPEC) and match layout (D-18); the spinner keeps its prefers-reduced-motion gate (D-17)."
    - "public/ carries the Crafted assets: login-gate-hero.png (new hero), empty-state-hero.png and empty-entities.png (from mockups/higgsfield), splash.png — old navy heroes replaced (D-14)."
  artifacts:
    - apps/desktop/src/routes/entities.tsx
    - apps/desktop/src/routes/kunden.tsx
    - apps/desktop/src/auth/login-gate.tsx
    - apps/desktop/src/components/session-chip.tsx
    - apps/desktop/src/components/session-banner.tsx
    - apps/desktop/src/components/error-state.tsx
    - apps/desktop/public/login-gate-hero.png
    - apps/desktop/public/empty-state-hero.png
    - apps/desktop/public/empty-entities.png
    - apps/desktop/public/splash.png
  key_links:
    - "login-gate.tsx <img src='/login-gate-hero.png'> resolves to the swapped Crafted PNG in public/ — same src path, file replaced underneath."
    - "entities.tsx list+panel is the SSOT; kunden.tsx copies its restyle 1:1 (shared mockup, D-12)."
    - "Only first-party PNGs shipped in public/; no remote or runtime image loading (D-25)."
  prohibitions:
    - "must NOT add a theme / Darstellung control to the session chip (D-07 / Phase 2 D-36)."
    - "must NOT add nav items or routes; IA is frozen (D-11)."
    - "must NOT regress Phase 3 entity/customer list+panel create, country→legal-form, or EU-VAT behavior."
    - "must NOT generate in-app illustrations at runtime or via Cursor GenerateImage; static first-party PNGs only (D-25)."
    - "must NOT overspend Higgsfield credits — cost-check before create; login-gate hero only; report leftover (D-14, Pitfall 5)."
  assumptions:
    - "[ASSUMED A5] ~3 credits remain after GPT Image 2 login-gate hero (7 of 10); cost-check before create and report leftover."
    - "[RESOLVED EDGE — adjacency] A list row and its detail panel render as distinct bordered surfaces (whisper separator); they never visually merge."
    - "[RESOLVED EDGE — empty] An empty entities/customers list renders the empty-state (copy + illustration), not a blank pane or an error."
---

<objective>
Restyle the entities/customers surfaces and the auth/session UI to the approved Crafted Minimal mockups, restyle the shared error/skeleton/spinner states, and swap the static illustration assets in public/ — including generating the one missing Login-Gate hero within the 10-credit Higgsfield budget.

Purpose: UI-01 / BRAND-01 for the management + login surfaces and the shared empty/error/loading language, plus D-14 asset wiring. Runs in parallel with 04-02 (zero file overlap).
Output: entities/kunden/login-gate/session-chip/session-banner/error-state/skeleton/spinner restyled, and four Crafted PNGs in public/ replacing the old navy art.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-premium-ui-brand-redesign/04-CONTEXT.md
@.planning/phases/04-premium-ui-brand-redesign/04-PATTERNS.md
@.planning/phases/04-premium-ui-brand-redesign/04-UI-SPEC.md
@.planning/phases/04-premium-ui-brand-redesign/04-01-PLAN.md
@.planning/phases/04-premium-ui-brand-redesign/mockups/approved/04-entities-light.png
@.planning/phases/04-premium-ui-brand-redesign/mockups/approved/05-entities-dark.png
@.planning/phases/04-premium-ui-brand-redesign/mockups/approved/08-login.png
@.planning/phases/04-premium-ui-brand-redesign/mockups/higgsfield/empty-invoices.png
@.planning/phases/04-premium-ui-brand-redesign/mockups/higgsfield/empty-entities.png
@.planning/phases/04-premium-ui-brand-redesign/mockups/higgsfield/splash.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entities + Kunden list+panel restyle</name>
  <files>apps/desktop/src/routes/entities.tsx, apps/desktop/src/routes/kunden.tsx</files>
  <read_first>
    - apps/desktop/src/routes/entities.tsx (list+panel SSOT: imports :1-13, comboboxTriggerClass :49-50, list rows :180-196, loading/empty/error :167-197, detail/create panel :200-340)
    - apps/desktop/src/routes/kunden.tsx (shares Entities mockup + list+panel)
    - .planning/phases/04-premium-ui-brand-redesign/04-PATTERNS.md (entities/kunden assignments, Card container, focus ring, whisper separator)
    - .planning/phases/04-premium-ui-brand-redesign/mockups/approved/04-entities-light.png
    - .planning/phases/04-premium-ui-brand-redesign/mockups/approved/05-entities-dark.png
  </read_first>
  <action>
    Restyle entities.tsx to 04/05-entities mockups keeping the list+panel structure, the three-way loading→empty→error branch, the comboboxTriggerClass token, the Card detail/create panel (max-w-xl), field groups, submit min-h-11 self-start font-semibold with inline Spinner, and field-error text-destructive. Give list rows the "whisper separator" feel via the new border token (border border-border, selected → bg-muted, aria-current) — not a heavy painted rail. Preserve country→legal-form and EU-VAT behavior. Then restyle kunden.tsx by copying the entities restyle 1:1 (same list+panel, Combobox, Card) — do not diverge structurally. Add no nav items or routes.
  </action>
  <verify>
    <automated>pnpm --filter desktop build</automated>
  </verify>
  <acceptance_criteria>
    - entities.tsx keeps the loading→empty→error three-way branch (grep for Skeleton, empty copy, ErrorState references retained) and the detail/create Card.
    - kunden.tsx structurally mirrors entities.tsx (list+panel + Combobox + Card) with no added routes.
    - List rows use border-border with selected bg-muted and aria-current.
    - `pnpm --filter desktop build` exits 0.
  </acceptance_criteria>
  <done>Entities and Kunden match the Crafted mockups in both themes with a whisper-separator list; Phase 3 create/validation behavior intact.</done>
</task>

<task type="auto">
  <name>Task 2: Login gate + session chip + session banner restyle</name>
  <files>apps/desktop/src/auth/login-gate.tsx, apps/desktop/src/components/session-chip.tsx, apps/desktop/src/components/session-banner.tsx</files>
  <read_first>
    - apps/desktop/src/auth/login-gate.tsx (CTA_CLASS :7-8, hero img :31-35, wordmark h1 :36)
    - apps/desktop/src/components/session-chip.tsx (CHIP_CLASS :28-29, Badge, DropdownMenu identity+logout)
    - apps/desktop/src/components/session-banner.tsx (Card + text-destructive / text-muted-foreground)
    - .planning/phases/04-premium-ui-brand-redesign/04-PATTERNS.md (login-gate hero swap, chip no-theme-item)
    - .planning/phases/04-premium-ui-brand-redesign/mockups/approved/08-login.png
  </read_first>
  <action>
    Restyle login-gate.tsx to 08-login.png: keep the hero <img src="/login-gate-hero.png"> markup and empty alt (the file is swapped in Task 3), keep the wordmark <h1>Clared</h1> in the system font (D-04), reuse CTA_CLASS with the Sage primary token, and keep the bilingual DE/EN copy the login mockup shows (D-15). Restyle session-chip.tsx (CHIP_CLASS bg-card hover:bg-muted + focus ring, Badge variant secondary, DropdownMenu identity + logout ONLY — add no theme/Darstellung item, D-07). Restyle session-banner.tsx (Card, text-destructive for unauthorized, text-muted-foreground for cancel).
  </action>
  <verify>
    <automated>pnpm --filter desktop build</automated>
  </verify>
  <acceptance_criteria>
    - login-gate.tsx keeps `<img src="/login-gate-hero.png"` and a wordmark heading with text "Clared".
    - session-chip.tsx DropdownMenu contains only identity + logout entries — no theme control (grep for a Darstellung/theme menu item is absent).
    - session-banner.tsx uses text-destructive for the unauthorized branch.
    - `pnpm --filter desktop build` exits 0.
  </acceptance_criteria>
  <done>Login gate, chip, and banner match the Crafted mockups; the chip stays identity+logout only.</done>
</task>

<task type="auto">
  <name>Task 3: Shared states polish + Crafted asset swap + Login-Gate hero generation</name>
  <files>apps/desktop/src/components/error-state.tsx, apps/desktop/src/components/skeleton.tsx, apps/desktop/src/components/spinner.tsx, apps/desktop/public/login-gate-hero.png, apps/desktop/public/empty-state-hero.png, apps/desktop/public/empty-entities.png, apps/desktop/public/splash.png</files>
  <read_first>
    - apps/desktop/src/components/error-state.tsx (Card + retry)
    - apps/desktop/src/components/skeleton.tsx (animate-pulse bg-muted primitive)
    - apps/desktop/src/components/spinner.tsx (reduced-motion gate :2-10 — keep)
    - .planning/phases/04-premium-ui-brand-redesign/04-UI-SPEC.md (Copywriting Contract error/empty, UI Considerations)
    - .planning/phases/04-premium-ui-brand-redesign/mockups/higgsfield/empty-invoices.png (→ empty-state-hero.png)
    - .planning/phases/04-premium-ui-brand-redesign/mockups/higgsfield/empty-entities.png
    - .planning/phases/04-premium-ui-brand-redesign/mockups/higgsfield/splash.png
    - $HOME/.claude/skills/higgsfield-generate/SKILL.md (GPT Image 2, cost before create, D-14)
  </read_first>
  <action>
    Confirm error-state.tsx renders the UI-SPEC error copy with a retry, skeleton.tsx keeps its animate-pulse bg-muted primitive matching layout (D-18), and spinner.tsx keeps its prefers-reduced-motion gate (D-17) — token classes already swap their colors, so touch visuals only where the mockups require. Copy mockups/higgsfield/empty-invoices.png → apps/desktop/public/empty-state-hero.png (replacing the old navy hero), and copy empty-entities.png and splash.png into apps/desktop/public/. Generate the missing Login-Gate hero with the higgsfield CLI: run `higgsfield generate cost gpt_image_2 …` FIRST, then `create --wait` only if within the 10-credit budget, prompt in the Crafted Minimal language (oatmeal/charcoal/white/sage, calm fintech still-life), and save it as apps/desktop/public/login-gate-hero.png replacing the old navy hero. After the job, record remaining credits in the SUMMARY. If the CLI/credits are unavailable, leave a clear public/login-gate-hero.png drop-in instruction for a ChatGPT-generated PNG (D-14 fallback) and note it in the SUMMARY — do not block the plan.
  </action>
  <verify>
    <automated>pnpm --filter desktop build && test -f apps/desktop/public/empty-entities.png && test -f apps/desktop/public/splash.png</automated>
  </verify>
  <acceptance_criteria>
    - apps/desktop/public/ contains login-gate-hero.png, empty-state-hero.png, empty-entities.png, and splash.png.
    - empty-state-hero.png is the Crafted empty-invoices illustration (old navy replaced).
    - spinner.tsx still gates animate-spin on prefers-reduced-motion (grep for the reduced-motion matchMedia check present).
    - Leftover Higgsfield credits (or the ChatGPT fallback) are reported in the SUMMARY.
    - `pnpm --filter desktop build` exits 0.
  </acceptance_criteria>
  <done>Shared states match the Crafted language; public/ holds the four first-party Crafted PNGs; the login-gate hero is generated within budget with leftover credits reported (or a fallback documented).</done>
  <reversibility rating="costly">Higgsfield GPT Image 2 spends ~7 of 10 Starter credits (A5) — cost-check before create; irreversible credit spend, so verify budget first.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static asset → webview | first-party PNGs in public/ loaded by <img> |
| CLI → filesystem | higgsfield CLI writes a generated PNG into public/ |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Tampering | public/*.png illustration assets | low | mitigate | Only first-party PNGs (copied from mockups/ or generated via the authenticated CLI); no remote/runtime image loading (D-25). |
| T-04-07 | Spoofing | login-gate hero asset | low | accept | Cosmetic hero on the local login gate; no auth logic reads the image; Authentik OIDC path (Phase 2) unchanged. |
| T-04-SC | Tampering | npm/cargo installs | low | accept | No package install this phase; higgsfield is a pre-installed build-time CLI, not a runtime dependency. |

No high/critical threats.
</threat_model>

<verification>
- `pnpm --filter desktop build` exit 0.
- entities/kunden/login-gate/session-chip/session-banner/error-state restyled to mockups.
- session chip has no theme item (D-07); no nav items added (D-11).
- public/ holds the four Crafted PNGs; old navy heroes replaced.
- spinner keeps reduced-motion gate; leftover credits reported.
- Manual UAT: Entities, Kunden, Login match 04/05/08 mockups in both themes.
</verification>

<success_criteria>
The management and auth surfaces render 1:1 to the approved Crafted mockups in both themes, shared states speak the Crafted language, and public/ carries the first-party Crafted illustrations with the login-gate hero generated inside the 10-credit budget — no Phase 3 behavior regression, chip and IA untouched.
</success_criteria>

## Artifacts this phase produces (Plan 04-03)

- **Restyled files (no new symbols):** `routes/entities.tsx`, `routes/kunden.tsx`, `auth/login-gate.tsx`, `components/session-chip.tsx`, `components/session-banner.tsx`, `components/error-state.tsx`, `components/skeleton.tsx`, `components/spinner.tsx`.
- **New / replaced public assets:** `apps/desktop/public/login-gate-hero.png` (new Crafted GPT Image 2 hero), `apps/desktop/public/empty-state-hero.png` (replaced with Crafted empty-invoices), `apps/desktop/public/empty-entities.png` (new), `apps/desktop/public/splash.png` (new).
- **No new functions, no new routes, no new nav items.**

<output>
Create `.planning/phases/04-premium-ui-brand-redesign/04-03-SUMMARY.md` when done.
</output>
