---
phase: 04-premium-ui-brand-redesign
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/desktop/src/routes/rechnung.tsx
  - apps/desktop/src/routes/tax.tsx
  - apps/desktop/src/routes/pdf.tsx
  - apps/desktop/src/components/tax-rail.tsx
  - apps/desktop/src/components/line-item-card.tsx
  - apps/desktop/src/components/pdf-paper.tsx
  - apps/desktop/src/components/invoice-empty-state.tsx
autonomous: true
requirements: [UI-01, BRAND-01]
estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "The split invoice canvas (form + tax rail) is restyled 1:1 to 02-rechnung-light.png / 03-rechnung-dark.png, IA unchanged (D-11)."
    - "Money values (einzelpreis, netto, totals, tax rate) use the .tabular-nums utility, not a second font family (D-03)."
    - "The '+ Position' / primary button uses the Sage primary token and applies scale(0.97) on press (D-17)."
    - "The invoice empty-state renders the empty-invoices illustration + documented empty copy; tax rail and Vorschau hide while empty (UI-SPEC empty; Phase 1 E6)."
    - "In-app loading uses the layout-matching skeleton, not a full-screen spinner (D-18); error states render the documented error copy with retry (UI-SPEC error)."
    - "The PDF stage follows the app theme while the PdfPaper content stays light (D-09)."
  artifacts:
    - apps/desktop/src/routes/rechnung.tsx
    - apps/desktop/src/routes/tax.tsx
    - apps/desktop/src/routes/pdf.tsx
    - apps/desktop/src/components/tax-rail.tsx
    - apps/desktop/src/components/line-item-card.tsx
    - apps/desktop/src/components/pdf-paper.tsx
    - apps/desktop/src/components/invoice-empty-state.tsx
  key_links:
    - "All surfaces consume semantic token classes (bg-background, text-muted-foreground, bg-accent, text-destructive) so the 04-01 :root/.dark split auto-swaps them."
    - "Autosave (600ms), live tax rail subscription, and showHero/showRail gating from Phase 3 must keep working after restyle."
  prohibitions:
    - "must NOT invert PdfPaper colors in dark theme — pdf-paper.tsx keeps inline light colors; do not tokenize its content (D-09, Pitfall 4)."
    - "must NOT force a dark stage in Light mode (D-09)."
    - "must NOT regress Phase 3 autosave, live-tax-rail, or list/canvas behavior."
    - "must NOT hard-code hex colors in components; read semantic token classes only."
  assumptions:
    - "[FLAGGED EDGE — adjacency] Where the field grid card and the tax rail sit side by side they render as distinct bordered surfaces (whisper separator), never visually merging."
    - "[RESOLVED EDGE — empty] An empty invoice renders the empty-state (illustration + copy + CTA 'Beispielrechnung anzeigen'), not a blank canvas or an error."
---

<objective>
Restyle the invoice surface — Rechnung split canvas, Tax rules screen, live Tax rail, line-item cards, PDF viewer stage, and the invoice empty-state — 1:1 to the approved Crafted Minimal mockups using the Wave-1 tokens, while preserving all Phase 3 behavior.

Purpose: UI-01 / BRAND-01 for the invoice → live-tax → PDF loop. This is the product's core surface; it must match 02/03-rechnung, 06-tax-engine, 07-pdf-viewer, and 09-empty-state exactly, in both themes.
Output: 7 restyled files consuming the new tokens, with tabular money numerals, motion press feel, and the D-09 PdfPaper guard intact.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-premium-ui-brand-redesign/04-CONTEXT.md
@.planning/phases/04-premium-ui-brand-redesign/04-PATTERNS.md
@.planning/phases/04-premium-ui-brand-redesign/04-UI-SPEC.md
@.planning/phases/04-premium-ui-brand-redesign/04-01-PLAN.md
@.planning/phases/04-premium-ui-brand-redesign/mockups/approved/02-rechnung-light.png
@.planning/phases/04-premium-ui-brand-redesign/mockups/approved/03-rechnung-dark.png
@.planning/phases/04-premium-ui-brand-redesign/mockups/approved/06-tax-engine.png
@.planning/phases/04-premium-ui-brand-redesign/mockups/approved/07-pdf-viewer.png
@.planning/phases/04-premium-ui-brand-redesign/mockups/approved/09-empty-state.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rechnung split-canvas + line-item cards restyle</name>
  <files>apps/desktop/src/routes/rechnung.tsx, apps/desktop/src/components/line-item-card.tsx</files>
  <read_first>
    - apps/desktop/src/routes/rechnung.tsx (split canvas SSOT: field grid :563-676, Select triggers :610-613, "+ Position" :701-710, autosave chip :519-535, showHero :561, showRail :481/:714-730, canvas skeleton :456-471)
    - apps/desktop/src/components/line-item-card.tsx (einzelpreis/netto toFixed(2) at :81/:104; numeric coercion :65/:89 — keep validation)
    - .planning/phases/04-premium-ui-brand-redesign/04-PATTERNS.md (rechnung assignment, comboboxTriggerClass, CTA/focus-ring)
    - .planning/phases/04-premium-ui-brand-redesign/mockups/approved/02-rechnung-light.png
    - .planning/phases/04-premium-ui-brand-redesign/mockups/approved/03-rechnung-dark.png
  </read_first>
  <action>
    Restyle rechnung.tsx to 02/03-rechnung mockups keeping the split form/rail structure, field grid Card, Select trigger token classes (min-h-11 w-full bg-card font-normal text-foreground hover:bg-muted), autosave chip copy (Speichert…/Gespeichert/…fehlgeschlagen), and the showHero/showRail gating exactly. Change only token-driven visuals; do not alter the autosave debounce or the tax-rail subscription. Give the "+ Position" primary button the Sage primary token (bg-primary text-primary-foreground) and add the :active scale(0.97) press feel via the motion utility from 04-01. In line-item-card.tsx apply the .tabular-nums utility to the einzelpreis and netto money cells; keep the Number(...)||0 numeric coercion and field validation untouched.
  </action>
  <verify>
    <automated>pnpm --filter desktop build && grep -q 'tabular-nums' apps/desktop/src/components/line-item-card.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/desktop/src/components/line-item-card.tsx applies the tabular-nums utility to the money cells.
    - rechnung.tsx primary "+ Position" button uses bg-primary (Sage) and has an :active scale press.
    - Autosave chip strings and the showHero/showRail gating logic are unchanged (grep for "Speichert" still present; showRail/showHero references retained).
    - `pnpm --filter desktop build` exits 0.
    - No raw hex color literal is introduced in either file (`grep -Ec '#[0-9a-fA-F]{3,6}' apps/desktop/src/routes/rechnung.tsx` unchanged from baseline).
  </acceptance_criteria>
  <done>The invoice canvas matches the Crafted mockups in both themes; money is tabular; press feel is present; Phase 3 autosave/rail behavior intact.</done>
</task>

<task type="auto">
  <name>Task 2: Tax screen + Tax rail + invoice empty-state restyle</name>
  <files>apps/desktop/src/routes/tax.tsx, apps/desktop/src/components/tax-rail.tsx, apps/desktop/src/components/invoice-empty-state.tsx</files>
  <read_first>
    - apps/desktop/src/routes/tax.tsx (:28-45 dl/dt/dd field list mirroring tax-rail)
    - apps/desktop/src/components/tax-rail.tsx (dl field list, text-muted-foreground keys, mini-preview :65-68 hard-codes #fff on purpose — leave it)
    - apps/desktop/src/components/invoice-empty-state.tsx (hero img + empty copy + return CTA)
    - .planning/phases/04-premium-ui-brand-redesign/04-UI-SPEC.md (Copywriting Contract, UI Considerations empty/error)
    - .planning/phases/04-premium-ui-brand-redesign/mockups/approved/06-tax-engine.png
    - .planning/phases/04-premium-ui-brand-redesign/mockups/approved/09-empty-state.png
  </read_first>
  <action>
    Restyle tax.tsx and tax-rail.tsx together to 06-tax-engine.png: keep the dl/dt/dd field list, muted-foreground keys, and the full StagedTaxDecision field set; apply .tabular-nums to the tax-rate/money-ish values. Leave the tax-rail mini-preview inline #fff (D-09 — do not tokenize). Restyle invoice-empty-state.tsx to 09-empty-state.png: keep the existing empty heading/body copy and the return CTA "Beispielrechnung anzeigen"; the hero <img> continues to reference /empty-state-hero.png (the Crafted illustration is swapped underneath in Plan 04-03 — keep the same src path and empty alt). Preserve the behavior that the tax rail and Vorschau hide while the empty state shows.
  </action>
  <verify>
    <automated>pnpm --filter desktop build && grep -q 'tabular-nums' apps/desktop/src/components/tax-rail.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tax-rail.tsx applies tabular-nums to numeric/money values and retains the inline #fff mini-preview (grep for the inline light color still present).
    - invoice-empty-state.tsx keeps the CTA text "Beispielrechnung anzeigen" and an <img> whose src is "/empty-state-hero.png".
    - tax.tsx still renders the dl field list (grep for `<dl` present).
    - `pnpm --filter desktop build` exits 0.
  </acceptance_criteria>
  <done>Tax screen, rail, and empty-state match the mockups; money is tabular; empty behavior and D-09 mini-preview preserved.</done>
</task>

<task type="auto">
  <name>Task 3: PDF viewer stage restyle with PdfPaper guard (D-09)</name>
  <files>apps/desktop/src/routes/pdf.tsx, apps/desktop/src/components/pdf-paper.tsx</files>
  <read_first>
    - apps/desktop/src/routes/pdf.tsx (:5 stage uses bg-background — follows theme)
    - apps/desktop/src/components/pdf-paper.tsx (:10-16 inline background:#fff; color:#111 — DO NOT tokenize)
    - .planning/phases/04-premium-ui-brand-redesign/04-PATTERNS.md (pdf D-09 guard)
    - .planning/phases/04-premium-ui-brand-redesign/mockups/approved/07-pdf-viewer.png
  </read_first>
  <action>
    Restyle the pdf.tsx stage/chrome to 07-pdf-viewer.png using token classes (bg-background surrounding stage follows the app theme). Do NOT change pdf-paper.tsx content colors: the inline light background and dark text stay inline so the dark theme cannot invert invoice numbers (D-09). Optionally apply .tabular-nums to paper numerals without changing their inline colors. Do not force a dark stage in Light mode.
  </action>
  <verify>
    <automated>pnpm --filter desktop build && grep -Eq '#fff|#ffffff' apps/desktop/src/components/pdf-paper.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/desktop/src/components/pdf-paper.tsx still contains the inline light background color (grep for `#fff` / `#ffffff` succeeds) and the inline dark text color — content NOT tokenized (D-09).
    - pdf.tsx stage uses bg-background (theme-following), not a hard-coded dark background.
    - `pnpm --filter desktop build` exits 0.
  </acceptance_criteria>
  <done>The PDF stage follows the theme; the paper stays light in both themes — invoice numbers never invert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → invoice form | numeric/text fields in line-item-card / rechnung (existing V5 validation) |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering | line-item-card numeric coercion | low | mitigate | Restyle must not remove Number(...)||0 coercion / field validation (V5); acceptance keeps it. |
| T-04-05 | Information Disclosure | PdfPaper dark-theme invert | low | mitigate | D-09 guard — inline colors kept so dark theme cannot render invoice content illegibly. |
| T-04-SC | Tampering | npm/cargo installs | low | accept | No package install this phase. |

No high/critical threats.
</threat_model>

<verification>
- `pnpm --filter desktop build` exit 0.
- Money cells (line-item-card, tax-rail) carry tabular-nums.
- pdf-paper.tsx keeps inline light colors (D-09).
- Autosave / live-tax-rail / empty-state gating behavior unchanged.
- Manual UAT: Rechnung, Tax, PDF match 02/03/06/07 mockups in both themes.
</verification>

<success_criteria>
The invoice → live-tax → PDF surfaces render 1:1 to the approved Crafted mockups in Light and Dark, with tabular money, Sage primary press feel, and the PdfPaper content protected from inversion — with no Phase 3 behavior regression.
</success_criteria>

## Artifacts this phase produces (Plan 04-02)

- **Restyled files (no new symbols):** `routes/rechnung.tsx`, `routes/tax.tsx`, `routes/pdf.tsx`, `components/tax-rail.tsx`, `components/line-item-card.tsx`, `components/pdf-paper.tsx`, `components/invoice-empty-state.tsx`.
- **Applied tokens/utilities:** `.tabular-nums` on money cells; Sage `bg-primary` + `:active scale(0.97)` on the primary button; semantic token classes throughout. No new files, no new public/ assets (asset swap is Plan 04-03).

<output>
Create `.planning/phases/04-premium-ui-brand-redesign/04-02-SUMMARY.md` when done.
</output>
