---
phase: 04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze
plan: 06
type: execute
wave: 4
depends_on:
  - 04.3-01
  - 04.3-05
files_modified:
  - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-MONITOR-OPS.md
autonomous: true
requirements:
  - AUDT-01
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "gitignored repo-root .env already holds TELEGRAM_BOT_TOKEN TELEGRAM_CHAT_ID HEALTHCHECKS_PING_URL (H1/H2 done); executor copies those names into Coolify — do not re-ask (D-03, D-08)."
    - "Coolify services clared-kuma FQDN https://kuma.puzzlessdev.online and clared-beszel hub https://beszel.puzzlessdev.online with built-in login, no Authentik in front, volumes /app/data and /beszel_data, pinned images (D-01, D-05, D-09, D-10)."
    - "clared-api scheduled task */2 * * * * curls ping URL, command length ≤255 (D-08)."
    - "No staging Kuma (D-06). No Kuma status page created by agent (D-07)."
  artifacts:
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-MONITOR-OPS.md
  key_links:
    - "Kuma monitor matrix is H3 (Plan 08); this plan only deploys empty Kuma + Beszel hub/agent compose."
    - "FaynoSync Kuma URL is public https://updates.puzzlessdev.online (Pitfall 8) — documented for H3."
  prohibitions:
    - statement: "Do not put Authentik, Cloudflare Access, or IP allowlist in front of Kuma/Beszel (D-05)."
      status: forbidden
      verification: "MONITOR-OPS.md states built-in login only."
    - statement: "Do not host Kuma/Beszel on Vercel (D-08)."
      status: forbidden
      verification: "Services created on Coolify server 5enlklyiijhf4x61t4pwdi8y."
    - statement: "Do not store Telegram token or ping URL in git."
      status: forbidden
      verification: "MONITOR-OPS.md has env NAMES only."
---

<objective>
Copy H1/H2 secrets from gitignored .env into Coolify, then deploy Kuma/Beszel volumes and Healthchecks cron (AUDT-01 monitoring wave).

Purpose: Monitors stay up when Authentik is down (D-05); VPS-down uses Healthchecks not same-box Kuma (D-08). Human already filled TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID, HEALTHCHECKS_PING_URL in repo-root .env — do not re-ask BotFather or Healthchecks UI.
Output: clared-kuma, clared-beszel, cron on clared-api, 04.3-MONITOR-OPS.md.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-RESEARCH.md
@.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-CONTEXT.md
.cursor/rules/coolify-mcp-cli.mdc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assert H1 Telegram keys in gitignored .env</name>
  <files>.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-MONITOR-OPS.md</files>
  <read_first>
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-RESEARCH.md (Human-needed H1)
  </read_first>
  <action>
    Human already completed H1 (BotFather + group Clared ops). Values live in gitignored repo-root .env as TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID. Do not re-ask. Assert both keys exist and are non-empty without printing values. Start 04.3-MONITOR-OPS.md listing those env NAMES only (no values). clared-kuma does not exist yet — Task 3 copies the two names onto the Kuma service via Coolify env_vars / coolify service env after create. Never commit .env.
  </action>
  <acceptance_criteria>
    - python key-presence check on .env for TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID exits 0
    - MONITOR-OPS.md lists the two env NAMES and contains no token assignment
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "from pathlib import Path; t=Path('.env').read_text();
def present(k):
    return any(line.startswith(k+'=') and len(line.split('=',1)[1].strip())>0 for line in t.splitlines() if not line.strip().startswith('#'))
assert present('TELEGRAM_BOT_TOKEN') and present('TELEGRAM_CHAT_ID'); print('H1_ENV_KEYS_OK')" && test -f .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-MONITOR-OPS.md && grep -F 'TELEGRAM_BOT_TOKEN' .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-MONITOR-OPS.md</automated>
  </verify>
  <done>Telegram env NAMES ready for Task 3 Coolify copy (D-03).</done>
</task>

<task type="auto">
  <name>Task 2: Copy HEALTHCHECKS_PING_URL from .env onto clared-api</name>
  <files>.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-MONITOR-OPS.md</files>
  <read_first>
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-RESEARCH.md (Human-needed H2)
  </read_first>
  <action>
    Human already completed H2 (Healthchecks.io check period 5 min grace 5 min + Telegram integration). HEALTHCHECKS_PING_URL lives in gitignored repo-root .env. Do not re-ask. Copy that name onto clared-api uuid yzmje7zsrp1qwtvsd7izjhaf via user-coolify env_vars (resource application, action create or update, production scope not is_preview) or /usr/local/bin/coolify app env create/update. Never print the URL. Never git the value. Append the env NAME to MONITOR-OPS.md.
  </action>
  <acceptance_criteria>
    - clared-api env list JSON includes key HEALTHCHECKS_PING_URL (value masked)
    - MONITOR-OPS.md contains HEALTHCHECKS_PING_URL as a NAME
  </acceptance_criteria>
  <verify>
    <automated>/usr/local/bin/coolify app env list yzmje7zsrp1qwtvsd7izjhaf --format json | python3 -c "import json,sys; d=json.load(sys.stdin); rows=d if isinstance(d,list) else d.get('data') or []; assert any((x.get('key') or '')=='HEALTHCHECKS_PING_URL' for x in rows), [x.get('key') for x in rows]; print('H2_COOLIFY_KEY_OK')" && grep -F 'HEALTHCHECKS_PING_URL' .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-MONITOR-OPS.md</automated>
  </verify>
  <done>Dead-man ping URL exists in Coolify (D-08).</done>
</task>

<task type="auto">
  <name>Task 3: Deploy Kuma + Beszel + Healthchecks cron</name>
  <precondition>gitignored repo-root .env holds TELEGRAM_BOT_TOKEN TELEGRAM_CHAT_ID HEALTHCHECKS_PING_URL; HEALTHCHECKS_PING_URL already on clared-api from Task 2. If kuma/beszel/grafana certs fail, pause for H5 DNS A/AAAA 185.245.96.241 and 2a13:fd40:1:2::535 — only if wildcard *.puzzlessdev.online does not already cover slugs (A5).</precondition>
  <reversibility rating="costly">D-01 DNS+proxy bookmarks; D-09 volumes.</reversibility>
  <files>.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-MONITOR-OPS.md</files>
  <read_first>
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-RESEARCH.md (inventory, H5, Kuma/Beszel create, Healthchecks cron, Pitfall 4)
    - .cursor/rules/coolify-mcp-cli.mdc
  </read_first>
  <action>
    user-coolify MCP service create docker_compose_raw: clared-kuma image louislam/uptime-kuma:2.5.3 (or Plan 01 twin), volume /app/data, FQDN https://kuma.puzzlessdev.online, no Authentik middleware (D-01, D-05, D-09, D-10). After Kuma exists, copy TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID from gitignored .env onto clared-kuma via env_vars / coolify service env (never print values). clared-beszel hub henrygd/beszel:0.18.8 volume /beszel_data APP_URL=https://beszel.puzzlessdev.online FQDN that host; agent henrygd/beszel-agent:0.18.8 docker.sock; KEY/TOKEN wait for H4 — agent env placeholders allowed until Plan 08. Prod only (D-06). Attach scheduled_tasks on clared-api yzmje7zsrp1qwtvsd7izjhaf frequency */2 * * * * command curl -fsS -m 10 of the ping URL baked at create time if Coolify does not expand env, else curl -fsS -m 10 "$HEALTHCHECKS_PING_URL"; command string ≤255 chars (D-08, A4). Write MONITOR-OPS.md with FQDNs, volume paths, image pins, cron, Kuma target matrix copied from RESEARCH for H3 (public vs internal URLs), D-07 no status page. GF Grafana slug listed as future Kuma target. Do not create Kuma monitors (no Kuma MCP — H3). Do not open Kuma UI.
  </action>
  <acceptance_criteria>
    - coolify service list includes clared-kuma and clared-beszel
    - clared-kuma env list includes TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID (masked)
    - MONITOR-OPS.md contains kuma.puzzlessdev.online, beszel.puzzlessdev.online, */2 * * * *, and the RESEARCH URL matrix table text for clared-api /health
    - scheduled task exists on clared-api (MCP scheduled_tasks or coolify equivalent)
  </acceptance_criteria>
  <verify>
    <automated>/usr/local/bin/coolify service list --format json | python3 -c "import json,sys; d=json.load(sys.stdin); rows=d if isinstance(d,list) else d.get('data') or []; n=str(rows).lower(); assert 'kuma' in n and 'beszel' in n, n[:500]" && KID=$(/usr/local/bin/coolify service list --format json | python3 -c "import json,sys; d=json.load(sys.stdin); rows=d if isinstance(d,list) else d.get('data') or []; k=next(x for x in rows if 'kuma' in (x.get('name') or '').lower()); print(k.get('uuid') or '')"); /usr/local/bin/coolify service env list "$KID" --format json | python3 -c "import json,sys; d=json.load(sys.stdin); rows=d if isinstance(d,list) else d.get('data') or []; keys={x.get('key') for x in rows}; assert 'TELEGRAM_BOT_TOKEN' in keys and 'TELEGRAM_CHAT_ID' in keys, keys; print('KUMA_TELEGRAM_KEYS_OK')" && grep -F 'kuma.puzzlessdev.online' .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-MONITOR-OPS.md && grep -F '*/2 * * * *' .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-MONITOR-OPS.md</automated>
  </verify>
  <done>Kuma/Beszel deployed; Healthchecks cron attached; matrix documented for H3 (D-01, D-02, D-08, D-09, D-10).</done>
</task>

</tasks>

## Artifacts this phase produces

clared-kuma, clared-beszel, HEALTHCHECKS cron, 04.3-MONITOR-OPS.md. Monitor rows inside Kuma = Plan 08 H3.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet → Kuma/Beszel | Built-in login only; must work when Authentik is down |
| Coolify cron → Healthchecks.io | Ping URL is a secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.3-13 | Denial of Service | Authentik in front of monitors | high | mitigate | D-05 no Authentik middleware |
| T-04.3-14 | Information Disclosure | Telegram token / ping URL in git | high | mitigate | Coolify env only |
| T-04.3-15 | Denial of Service | Cron interval ≥ HC period | medium | mitigate | */2 vs period 5 min (Pitfall 4) |
</threat_model>

<verification>
.env keys present; HEALTHCHECKS_PING_URL on clared-api; services listed; MONITOR-OPS.md has FQDNs and cron; no secrets in git.
</verification>

<success_criteria>
Founder can later log into Kuma/Beszel on public HTTPS; Healthchecks expects pings every 5 minutes from Coolify cron.
</success_criteria>

<output>
Create `.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-06-SUMMARY.md` when done
</output>
