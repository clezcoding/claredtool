---
phase: 04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze
verified: 2026-08-29T23:10:00Z
status: passed
score: 6/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
decision_coverage:
  honored: 35
  total: 35
  not_honored: []
---

# Phase 04.3: Infra & Prep for PDF, Offline & Audit Verification Report

**Phase Goal:** Deploy supporting Coolify infra and wire desktop/backend scaffolding so Phase 5 focuses on product flows (generate PDF, audit trail, offline sync) — not plumbing
**Verified:** 2026-08-29T23:10:00Z
**Status:** passed
**Re-verification:** No — initial verification

Do not require PDF download UI, `audit_logs` rows, or offline sync logic (Phase 5).

Existing `04.3-UAT.md` (`status: complete`, 25/25, 2026-08-29) was not modified. H3/H4/H6 human checks are **verified-by-UAT**, not re-opened. `04.3-REVIEW.md` is advisory (`issues_found`, CR-01 + CR-02): recorded as warnings; must-haves exist in code so they are not FAILED.

## Goal Achievement

### Observable Truths

Roadmap Success Criteria are the contract. PLAN frontmatter truths that restate an SC keep roadmap wording. PLAN-only operational checks sit in artifacts / key links / warnings.

| # | Truth | Status | Evidence |
| --- | ------- | ---------- | -------------- |
| 1 | Wave 1 — Monitoring: Uptime Kuma on Coolify monitors `clared-api`, Authentik, Postgres; Beszel tracks VPS CPU/RAM/docker; alert webhooks documented | ✓ VERIFIED | Coolify `clared-kuma` `xeeo9cjvfpozmhvaq6vcgqhm` running:healthy, image `louislam/uptime-kuma:2.5.3`, FQDN `https://kuma.puzzlessdev.online`. Kuma MCP: 7 monitors all status=1 (UP): ClaredApi Health `https://clared-api.puzzlessdev.online/health`, ClaredAuthentik, FaynoSync `/health`, Grafana Login, Postgres TCP `:5432`, Redis TCP `:6379`, Gotenberg `/health` internal Basic Auth. `listStatusPages` = `[]`. Telegram channel id 2 attached (`notificationIDList`). `clared-beszel` hub+agent images `henrygd/beszel:0.18.8` / `beszel-agent:0.18.8`, hub FQDN `https://beszel.puzzlessdev.online`. Alert NAMES in `04.3-MONITOR-OPS.md`. UAT tests 1–2 pass (H3/H4). |
| 2 | Wave 2 — PDF render infra: Gotenberg on Coolify; NestJS PDF module skeleton (HTML template + invoice facts → PDF bytes contract); optional OTEL on Gotenberg | ✓ VERIFIED | Coolify `clared-gotenberg` image `gotenberg/gotenberg:8.36.0`, `fqdn` null, Kuma `/health` 200. Nest: `pdf.contract.ts` `PDF_QUEUE=pdf-generation`, A4 8.27×11.7, `HtmlInvoiceFacts`/`PdfBytes`; `gotenberg.client.ts` POST `/forms/chromium/convert/html`; `PdfModule` on HTTP app; no `PdfController`. Gotenberg has no OTEL env (D-16; optional). Jest `pdf.contract.spec` + `gotenberg.client.spec`  — 9 tests in the named trio pass (see spot-checks). |
| 3 | Wave 3 — Backend observability prep: Pino in NestJS; OpenTelemetry SDK bootstrap; OTel Collector + Grafana deployed or documented; no `audit_logs` product UI | ✓ VERIFIED | `LoggerModule.forRoot` + `bufferLogs` in `main.ts`; `initOtel()` before `NestFactory`; worker `initOtel({ serviceName: "clared-worker" })` + pino line `Worker gestartet`. Coolify `clared-observability` running:healthy: Grafana `13.2.0` FQDN `https://grafana.puzzlessdev.online`, Loki `3.7.7`, Tempo `3.0.3`, Prometheus `v3.14.0`, collector `0.159.0`. OTEL env **names** on `clared-api-ghcr` + `clared-worker-ghcr` (values masked). No Prisma `audit_logs`. `04.3-OTEL-OPS.md` documents collector OTLP 4318 + Loki `otlphttp`. UAT 3 + 23–25. |
| 4 | Wave 4 — Desktop PDF/offline prep: `tauri-plugin-dialog`, `tauri-plugin-fs`, `tauri-plugin-sharekit`; `tauri-plugin-sql` scaffold (migrations dir, empty schema — no sync logic) | ✓ VERIFIED | `Cargo.toml` sql/dialog/fs/sharekit. `lib.rs` SQLCipher keychain account `offline-sqlcipher`, DB `clared-offline.db` under `app_data_dir()`, `PRAGMA key` in Rust, no JS secret handler. `migrations/0001_init.sql` only `_init`. `default.json` fs `$APPDATA`+`$TEMP` only; `login.json` `core:default` only. `debug-panel.tsx` `debug-section-offline`, `DEBUG_PANEL_ENABLED = import.meta.env.DEV`, no generate-PDF control. Vitest capabilities-043 + debug-panel-offline pass. |
| 5 | Wave 5 — Async jobs prep: BullMQ skeleton on existing Redis (`pdf-generation` queue + worker stub); no invoice PDF templates | ✓ VERIFIED | `@nestjs/bullmq` + `bullmq` in backend `package.json`. `PdfService.add` → queue `pdf-generation` prefix `clared-bull`. `PdfProcessor` `@Processor(PDF_QUEUE)`. Live `clared-worker-ghcr` `start_command=node dist/worker.js`, `fqdn` null, alias `clared-worker`, logs contain `"msg":"Worker gestartet"` and `WorkerModule`/`BullModule`. Dockerfile `test -f dist/worker.js`; worker stage `CMD node dist/worker.js`. No invoice template files / `PdfController`. |
| 6 | Phase 5 owns end-user PDF download, `audit_logs` rows, and offline sync — this phase only prepares pipes | ✓ VERIFIED | No `PdfController`, no generate-PDF UI, no `audit_logs` model/UI, migration SQL only `_init`. Phase 5 ROADMAP SCs explicitly consume 4.3 pipes. |

**Score:** 6/6 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | ----------- | ------ | ------- |
| `apps/backend/src/pdf/pdf.contract.ts` | Queue + A4 + types | ✓ VERIFIED | `PDF_QUEUE`, `PDF_PAPER_A4`, `DEFAULT_GOTENBERG_URL` host-local comment |
| `apps/backend/src/pdf/gotenberg.client.ts` | Chromium HTML convert | ✓ VERIFIED | EmptyHtmlError before fetch; Basic Auth; FormData `index.html` |
| `apps/backend/src/pdf/pdf.module.ts` / `pdf.service.ts` / `pdf.processor.ts` | Enqueue + worker processor | ✓ VERIFIED | HTTP app registers queue; processor only on `WorkerModule` |
| `apps/backend/src/worker.ts` / `worker.module.ts` | ApplicationContext, no HTTP | ✓ VERIFIED | `controllers: []`; no `listen` |
| `apps/backend/src/telemetry/otel.ts` | SDK bootstrap, export-off local | ✓ VERIFIED | `spanProcessors: []` when exporter none; OTLP when `otlp` |
| `apps/backend/src/telemetry/pino-options.ts` | Redact + dual-write | ✓ VERIFIED | stdout `pino/file` dest 1; OTLP transport when `OTEL_LOGS_EXPORTER=otlp` |
| `apps/backend/Dockerfile` | `dist/worker.js` assert; API migrate+main | ✓ VERIFIED | Lines 32–33, 43–47 |
| `compose.clared.yml` | Local Gotenberg 8.36.0 + worker | ✓ VERIFIED | `127.0.0.1:3000:3000`; no grafana stack; no public hostname |
| `apps/desktop/src-tauri/src/lib.rs` | SQLCipher + plugins | ✓ VERIFIED | Keychain + auto_extension PRAGMA |
| `apps/desktop/src-tauri/capabilities/default.json` | dialog/sql/fs/sharekit | ✓ VERIFIED | fs APPDATA/TEMP only |
| `apps/desktop/src-tauri/migrations/0001_init.sql` | Empty schema | ✓ VERIFIED | `_init` only |
| `apps/desktop/src/components/debug-panel.tsx` | DEV smokes | ✓ VERIFIED | `debug-section-offline` |
| `04.3-PDF-OPS.md` / `04.3-MONITOR-OPS.md` / `04.3-OTEL-OPS.md` | Ops SSOT NAMES/UUIDs | ✓ VERIFIED | No password assignments |
| `04.3-UAT.md` | H3/H4/H6 evidence | ✓ VERIFIED | 25/25; secrets policy in frontmatter |

**Artifacts:** 15/15 verified (gsd-tools `verify.artifacts` returned empty arrays because PLAN `artifacts:` are path lists, not `{path, provides}` objects — verified by hand)

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `PdfService.add` | BullMQ `pdf-generation` | `@InjectQueue(PDF_QUEUE)` | ✓ WIRED | Same name as `@Processor` |
| `PdfProcessor` | `convertHtmlToPdf` | `job.data.html` | ✓ WIRED | Empty HTML never fetched (spec) |
| `gotenberg.client.ts` | Gotenberg | `GOTENBERG_URL` + Basic Auth env | ✓ WIRED | Coolify env **names** on API-ghcr + worker-ghcr |
| `main.ts` | `initOtel` + Pino | before `NestFactory`; `bufferLogs` | ✓ WIRED | Worker also `initOtel` |
| Health e2e | `GET /health` | `{ status: ok }` no PII keys | ✓ WIRED | `health.e2e-spec.ts` |
| SQL plugin | `app_data_dir()/clared-offline.db` | absolute `sqlite:` URL | ✓ WIRED | `lib.rs` `register_offline_sql` |
| Debug panel | dialog/fs/sql/sharekit | DEV section | ✓ WIRED | testid `debug-section-offline` |
| Kuma | RESEARCH matrix | Plan 08 / UAT | ✓ WIRED | 7 monitors; no status page |
| Collector alias | API/worker OTEL | `OTEL_EXPORTER_OTLP_ENDPOINT` **name** present | ✓ WIRED | Values not revealed |
| Worker start | `node dist/worker.js` | Coolify `start_command` + GHCR worker tag | ✓ WIRED | Live app `mwdbgkc8grcth77wx0cseom8` |

**Wiring:** 10/10 connections verified

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| Gotenberg convert | PDF bytes | `fetch` Chromium HTML | Yes when worker has a job | ✓ FLOWING (client real; no product enqueue yet — Phase 5) |
| Kuma monitors | heartbeat status | live Kuma MCP | Yes, all UP | ✓ FLOWING |
| Grafana | login page | Coolify FQDN | Kuma 200; UAT datasources | ✓ FLOWING |
| Debug offline SQL | `clared-offline.db` | SQLCipher file + `_init` | Smoke path in DEV | ✓ FLOWING |
| `PdfService.add` | queue jobs | no HTTP caller | Scaffold only | ⚠️ HOLLOW_PROP until Phase 5 — **intentional** (no PdfController) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| PDF contract + Gotenberg client + initOtel | `pnpm exec jest --testPathPatterns='pdf.contract.spec' --testPathPatterns='gotenberg.client.spec' --testPathPatterns='otel.spec'` in `apps/backend` | 3 suites, 9 tests passed | ✓ PASS |
| Desktop fs scope + offline debug section | `vitest run src/__tests__/capabilities-043.test.ts src/__tests__/debug-panel-offline.test.ts` | 2 files, 3 tests passed | ✓ PASS |
| Live worker bootstrap | Coolify logs `clared-worker-ghcr` | `"msg":"Worker gestartet"`, `WorkerModule` | ✓ PASS |
| Kuma matrix live | `getMonitorSummary` | 7/7 UP | ✓ PASS |
| Local Nest+Gotenberg on :3000 together | not started (no servers) | CR-01 collision in compose vs `main.ts` PORT default | ⚠️ WARNING (not a missing artifact) |

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| — | — | No `scripts/*/tests/probe-*.sh`; PLAN/SUMMARY do not declare probe scripts | SKIPPED |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| PDF-01 (prep) | 01, 02, 05, 08 | Backend-rendered PDF later; this phase is Gotenberg + queue + contract | ✓ SATISFIED (prep) | Full download/view is Phase 5 SC 2 |
| OFFL-01 (prep) | 01, 04, 08 | Offline SQLite scaffold | ✓ SATISFIED (prep) | SQLCipher empty DB + plugins; sync is Phase 5 SC 4. REQUIREMENTS.md still maps OFFL-01 to Phase 5 (and marks it Complete there) — not a 04.3 product claim |
| AUDT-01 (prep) | 01, 03, 06, 07, 08 | App logs + Coolify Grafana/Prometheus; not `audit_logs` rows | ✓ SATISFIED (prep) | Pino + OTel + Grafana stack; no `audit_logs` table |

**Orphaned requirements:** None. REQUIREMENTS.md maps PDF-01 / OFFL-01 / AUDT-01 to Phase 5. Plans 01–08 claim the same IDs as **prep**. Full product IDs stay Phase 5.

**Coverage:** 3/3 prep IDs accounted

### Decision Coverage

All trackable CONTEXT.md decisions are honored by shipped artifacts. (`honored` 35 / `total` 35, `not_honored`: [])

### Test Quality Audit

| Test File | Linked Req | Active | Skipped | Circular | Assertion Level | Verdict |
|-----------|-----------|--------|---------|----------|-----------------|---------|
| `pdf.contract.spec.ts` | PDF-01 prep | 2 | 0 | no | Value (queue name, A4) | OK |
| `gotenberg.client.spec.ts` | PDF-01 prep | 2 | 0 | no | Behavioral (no fetch on empty; FormData) | OK |
| `otel.spec.ts` | AUDT-01 prep | 3+ predicate tests | 0 | no | Value on predicate; **does not assert SDK installs allowlist** | ⚠️ INSUFFICIENT for D-23 spans (CR-02) |
| `capabilities-043.test.ts` | OFFL-01 prep | yes | 0 | no | Value (`$HOME` omitted) | OK |
| `debug-panel-offline.test.ts` | OFFL-01 prep | 1 | 0 | no | Existence of section | OK |
| `health.e2e-spec.ts` | AUDT-01 prep | 2 | 0 | no | Value `{ status: ok }` + PII key absence | OK |

**Disabled tests on requirements:** 0
**Circular patterns detected:** 0
**Insufficient assertions:** 1 (`isAllowedSpanAttribute` unit-only — CR-02)

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `compose.clared.yml` | 27–28 | Host `:3000` vs Nest `PORT` default 3000 | ⚠️ Warning (REVIEW CR-01) | Local Nest + compose Gotenberg cannot bind together. Coolify path unaffected. PLAN required `127.0.0.1:3000:3000`. |
| `apps/backend/src/telemetry/otel.ts` | 20–68 | `isAllowedSpanAttribute` never passed to NodeSDK | ⚠️ Warning (REVIEW CR-02) | Prod `OTEL_TRACES_EXPORTER=otlp` can leak URL/query and Redis statements. Pino redact does not cover spans. Bootstrap still exists (truth 3). |
| Coolify `clared-observability` | Loki/Tempo/collector | `running:unhealthy` (excluded from stack status) | ⚠️ Warning | Grafana healthy; UAT datasource Save & test OK. Coolify healthchecks noisy. |
| Coolify `healthchecks-ping` | task `xqg0ryd5cbrewduqoiam9ebk` | Cron on **exited** `clared-api` `yzmje7zsrp1qwtvsd7izjhaf`; live `clared-api-ghcr` has **no** scheduled tasks | ⚠️ Warning | Dead-man switch may not ping after GHCR cutover. Kuma still monitors API. Move cron to live app. |
| `gotenberg.client.ts` | 21–23 | `GOTENBERG_URL` fail-open to loopback | ℹ️ Info (REVIEW WR-01) | Missing env hits container localhost, not Gotenberg |
| `pdf.service.ts` | `add` | No HTTP caller | ℹ️ Info | Intentional stub until Phase 5 |

No `TBD` / `FIXME` / `XXX` in phase-touched source. No stub `PdfController`.

### Human Verification Required

N/A — Infrastructure/foundation phase (`UI hint: no`). No new pending human items.

H3/H4/H6 (Kuma admin/matrix, Beszel enroll, Grafana first login) already recorded in `04.3-UAT.md` tests 1–3 as **pass** (2026-08-29). Treated as **verified-by-UAT**.

UAT note: Grafana admin password may have been changed in UI after first login; Coolify `GF_SECURITY_ADMIN_PASSWORD` may be stale.

### Gaps Summary

No blocking gaps. Phase goal holds: Coolify Gotenberg, Kuma/Beszel, Grafana/LGTM, BullMQ worker, Pino/OTel bootstrap, and desktop SQL/dialog/fs/sharekit scaffold are present, substantive, and wired. Phase 5 can focus on generate-PDF, `audit_logs`, and sync.

Non-blocking follow-ups (do not fail 04.3): remap Gotenberg host port (CR-01), install span allowlist on the SDK (CR-02), move Healthchecks cron to `clared-api-ghcr`, treat Coolify `unhealthy` on Loki/Tempo/collector as an ops check.

### Prohibitions (judgment / grep)

| Statement | Evidence | Verdict |
| --------- | -------- | -------- |
| No `latest` image tags on 04.3 stack | compose + Coolify images pinned | honored |
| No public Gotenberg/worker FQDN | `fqdn` null | honored |
| No `PdfController` / generate control | grep empty | honored |
| No Gotenberg OTEL | no OTEL keys on Gotenberg service | honored |
| No Grafana/Loki in `compose.clared.yml` | services postgres/redis/gotenberg/worker | honored |
| No `audit_logs` Prisma | no model | honored |
| No Kuma status page | `listStatusPages` [] | honored |
| No secrets in UAT/OPS git | NAMES/UUIDs only in those files | honored |
| Telegram/ping not in git | `.gitignore` has `.env`; MONITOR-OPS NAMES only | honored |

---

_Verified: 2026-08-29T23:10:00Z_
_Verifier: Claude (gsd-verifier)_
