# Phase 04.3 — PDF Operations (SSOT)

**Status:** Active (Plan 05)  
**Scope:** Coolify production Gotenberg + BullMQ worker (PDF-01 prep, D-11, D-14)  
**Secrets:** Names + UUIDs + aliases only. Values live in Coolify env. Never commit passwords.

## Coolify inventory (do not guess)

| Resource | Name | UUID | Notes |
|----------|------|------|-------|
| Project | Clared | `eseyqnptwe3y4wp4myirf8do` | |
| Environment | production | `d1innp9cxvmlsrlj6ksrpxnz` | |
| Server | localhost | `5enlklyiijhf4x61t4pwdi8y` | |
| Destination | coolify | `y7xsaczoksa1ga6pakatj3iw` | Docker network `coolify` |
| GitHub App | Public GitHub | `jwuqhppefgpd2wrp8edf23xs` | `create_github` 500 on this app; worker created via `coolify app clone` |
| API | `clared-api` | `yzmje7zsrp1qwtvsd7izjhaf` | Enqueues PDF jobs |
| Gotenberg service | `clared-gotenberg` | `iokfnlnzidksuvrabx7ixxiv` | Compose service, no FQDN |
| Gotenberg sub-app | `gotenberg` | `dpkuhajg8okkejbghcr6abps` | Image `gotenberg/gotenberg:8.36.0`; `fqdn` null; `required_fqdn` false |
| Worker | `clared-worker` | `vwhnsihwpezppgwepvwxrk2h` | Same git/Dockerfile as API |

Git repo: `clezcoding/claredtool.git`

## DNS aliases (Pitfall 1)

Internal HTTP uses **aliases**, not application UUIDs.

| Alias | Resource | URL |
|-------|----------|-----|
| `clared-gotenberg` | Gotenberg compose on `coolify` network | `http://clared-gotenberg:3000` |
| `clared-worker` | Worker `custom_network_aliases` + `custom_internal_name` | no HTTP (BullMQ consumer) |

`GOTENBERG_URL` value (Coolify only, not git): `http://clared-gotenberg:3000`

## Git / image (worker)

| Field | Value |
|-------|--------|
| `git_branch` | `gsd/phase-04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze` |
| `dockerfile_location` | `/apps/backend/Dockerfile` |
| `start_command` | `node dist/worker.js` (acceptance field; Coolify 4.3.14 dockerfile pack does **not** inject this as container CMD — Nixpacks-only) |
| `dockerfile_target_build` | `worker` (actual PID 1: `node dist/worker.js`) |
| Last Dockerfile stage | `api` CMD still runs `prisma migrate deploy && node dist/main.js` (Coolify Postgres is Docker-internal DNS; GitHub-hosted runners cannot reach it). GHA migrate job skips when `DATABASE_URL` is unset. |

## GHCR attestations (TECH-DEBT)

`backend-image.yml` keeps `provenance: false` and `sbom: false`. Coolify cannot pull attestation indexes. Re-enable after a Coolify upgrade that can.
| `connect_to_docker_network` | `true` (worker) |
| `fqdn` | empty / null — **never assign a public hostname** |
| Health check | disabled (worker has no HTTP) |
| Prisma migrate | **not** run on worker |

Prove runtime: Coolify app logs must contain `Worker gestartet` and `name":"clared-worker"`. Must **not** contain `RouterExplorer` or `prisma migrate`.

## Env NAMES (values never git)

Gotenberg service (`iokfnlnzidksuvrabx7ixxiv`) interpolates Basic Auth from Coolify:

| Name | Env UUID (Gotenberg service) |
|------|------------------------------|
| `GOTENBERG_API_BASIC_AUTH_USERNAME` | `jfe9pk7j9rgraeev9hqhudiq` |
| `GOTENBERG_API_BASIC_AUTH_PASSWORD` | `7lqui31pkjx2uvtsnoedxalv` |

Compose (not Coolify env keys): `API_ENABLE_BASIC_AUTH=true`, `API_TIMEOUT=30s`, `API_DISABLE_DOWNLOAD_FROM=true`, `CHROMIUM_DENY_PRIVATE_IPS=true`, `CHROMIUM_DENY_PUBLIC_IPS=true`. Omit `CHROMIUM_ALLOW_LIST`. No OTEL on Gotenberg (D-16).

Native Coolify health: compose `healthcheck` `CMD curl -fsS http://127.0.0.1:3000/health` (Gotenberg image ships curl; 8.36+ `/health` is exempt from basic auth). No public FQDN — Traefik never sees this check. Kuma still uses Basic Auth on the same path as belt-and-suspenders.

Worker and API (production) also have these **names**:

- `GOTENBERG_URL`
- `GOTENBERG_API_BASIC_AUTH_USERNAME`
- `GOTENBERG_API_BASIC_AUTH_PASSWORD`
- `REDIS_URL` (copied from `clared-api`; reveal only that key when rotating)

Worker-only until Plan 07: `OTEL_TRACES_EXPORTER`, `OTEL_LOGS_EXPORTER`, `OTEL_SERVICE_NAME` (`clared-worker`).

Copy Basic Auth: Coolify `env_vars` list `reveal=true` **and** `key=` one name at a time. Never dump all secrets.

## Redis / BullMQ

API enqueue and worker consume the same Redis. Queue prefix: `clared-bull` (D-14). Do not point Uptime Kuma at the worker.

## Uptime Kuma (Plan 06 / H3)

Gotenberg is internal. Monitor:

1. **Preferred:** HTTP `http://clared-gotenberg:3000/health` with Basic Auth credentials stored **in Kuma**, matching Coolify names above.
2. **A3 fallback:** if `/health` is covered by Basic Auth and Kuma HTTP auth is awkward, TCP probe **port 3000** on alias `clared-gotenberg`.

No public FQDN for this check.

## Gotenberg network flag

MCP/CLI cannot set `connect_to_docker_network` on the **service** object (stays false). Compose joins the external `coolify` network with alias `clared-gotenberg`. Equivalent for DNS.

## Operator commands (MCP then CLI)

```bash
coolify service list --format json
coolify service get iokfnlnzidksuvrabx7ixxiv --format json
coolify app list --format json
coolify app get vwhnsihwpezppgwepvwxrk2h --format json
coolify app logs vwhnsihwpezppgwepvwxrk2h
```

Expect worker `fqdn` null and `start_command` `node dist/worker.js`. Confirm `dockerfile_target_build` via Coolify MCP `get_application` (CLI `app get` may omit the field).

---

## 04.6 cutover addendum (D-10 / D-24) — Gotenberg + worker deleted

**Recorded:** 2026-09-05  
**Phase:** 04.6-takumi-pdfcn-pdf-engine-cutover plan 05  
**Purpose:** One-way Chromium renderer + BullMQ worker removal. Per D-10 — do not rewrite 04.3 PLAN/SUMMARY bodies; this addendum is the live SSOT after cutover.

| Resource | Historical name | Deleted UUID | Notes |
|----------|-----------------|--------------|-------|
| Gotenberg service | `clared-gotenberg` | `iokfnlnzidksuvrabx7ixxiv` | Deleted via user-coolify `service` action=delete (04.6-05) |
| Gotenberg sub-app | `gotenberg` | `dpkuhajg8okkejbghcr6abps` | Goes with the service |
| Worker application | `clared-worker-ghcr` | `mwdbgkc8grcth77wx0cseom8` | Live UUID at delete time. Plan/table above still lists stale `vwhnsihwpezppgwepvwxrk2h` — do not use that id |
| API env purge | `clared-api-ghcr` | `ucmhsb0srwr7ewt9wxejc4e9` | Delete keys only: `GOTENBERG_URL`, `GOTENBERG_API_BASIC_AUTH_USERNAME`, `GOTENBERG_API_BASIC_AUTH_PASSWORD`. Keep `REDIS_URL`. Historical table UUID `yzmje7zsrp1qwtvsd7izjhaf` is stale |

**Git:** `compose.clared.yml` keeps postgres + redis only (no gotenberg/worker). `apps/backend/.env.example` has no `GOTENBERG_*`.

**Rollback:** Pin previous API image tag. Do **not** re-enable Chromium/Gotenberg or recreate worker without a new engine phase (D-01, D-02).
