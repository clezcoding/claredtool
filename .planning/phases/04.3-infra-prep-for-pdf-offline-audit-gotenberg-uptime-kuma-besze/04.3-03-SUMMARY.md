---
phase: 04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze
plan: 03
subsystem: infra
tags: [pino, nestjs-pino, opentelemetry, otel, redact, health]

requires:
  - phase: 04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze
    provides: worker.ts ApplicationContext + .env.example OTEL exporters none (D-24)
provides:
  - initOtel NodeSDK with local exporters none (no :4318)
  - nestjs-pino JSON stdout + PII redact (D-23)
  - Dual-write pino-opentelemetry-transport only when OTEL_LOGS_EXPORTER=otlp (D-30)
  - Health e2e asserts status ok and no PII keys
affects: [04.3-07, 05]

actuals:
  tokens: 34649
  tasks: 3
  commits: 3

tech-stack:
  added:
    - "nestjs-pino@4.6.1"
    - "pino@10.3.1"
    - "pino-http@11.0.0"
    - "pino-opentelemetry-transport@4.0.2"
    - "@opentelemetry/sdk-node@0.221.0"
    - "@opentelemetry/auto-instrumentations-node@0.79.0"
    - "@opentelemetry/exporter-trace-otlp-http@0.221.0"
  patterns:
    - "initOtel before NestFactory; empty spanProcessors when exporters none (env default is otlp)"
    - "Pino owns logs; NodeSDK logRecordProcessors [] (no duplicate OTLP logs)"
    - "Always pino/file destination 1; add pino-opentelemetry-transport only when OTEL_LOGS_EXPORTER=otlp"

key-files:
  created:
    - apps/backend/src/telemetry/otel.ts
    - apps/backend/src/telemetry/pino-options.ts
  modified:
    - apps/backend/src/main.ts
    - apps/backend/src/worker.ts
    - apps/backend/src/app.module.ts
    - apps/backend/src/telemetry/otel.spec.ts
    - apps/backend/test/health.e2e-spec.ts
    - apps/backend/test/jest-e2e.json
    - apps/backend/package.json
    - pnpm-workspace.yaml
    - pnpm-lock.yaml

key-decisions:
  - "NodeSDK never exports logs; Pino dual-write owns OTLP logs when OTEL_LOGS_EXPORTER=otlp (D-30)"
  - "When traces exporter is none, pass spanProcessors: [] so getSpanProcessorsFromEnv cannot default to otlp and hang on :4318 (D-24)"
  - "Worker uses raw pino with the same redact, not LoggerModule/pino-http"

patterns-established:
  - "Pattern: initOtel({ serviceName }) analog initDesktopSentry skip-when-unset / idempotent flag"
  - "Pattern: PINO_REDACT_PATHS shared TOKEN/NAME/IBAN/invoice-line keys with sentry.ts (D-23)"

requirements-completed: []

coverage:
  - id: D1
    description: initOtel does not throw when exporters are none; idempotent; span allowlist denies customer names, IBAN, tokens
    requirement: AUDT-01
    verification:
      - kind: unit
        ref: "apps/backend/src/telemetry/otel.spec.ts"
        status: pass
    human_judgment: false
  - id: D2
    description: nestjs-pino LoggerModule with bufferLogs, useLogger, JSON stdout, redact; worker lightweight pino
    requirement: AUDT-01
    verification:
      - kind: other
        ref: "grep bufferLogs main.ts && grep LoggerModule app.module.ts && initOtel before NestFactory"
        status: pass
    human_judgment: false
  - id: D3
    description: GET /health returns { status: ok } JSON with no customerName/iban/authorization/access_token keys
    requirement: AUDT-01
    verification:
      - kind: e2e
        ref: "apps/backend/test/health.e2e-spec.ts#GET /health returns 200 with status ok and no PII keys"
        status: pass
    human_judgment: false

duration: 6min
completed: 2026-08-29
status: complete
---

# Phase 04.3 Plan 03: Pino + OTel local export off Summary

**nestjs-pino JSON stdout with TOKEN/NAME/IBAN/invoice-line redact; `initOtel` starts NodeSDK without OTLP when exporters are `none` (no `:4318`); health e2e asserts `{ status: ok }` and forbids PII keys.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-08-29T02:30:59Z
- **Completed:** 2026-08-29T02:37:27Z
- **Tasks:** 3
- **Files modified:** 11

## Accomplishments

- `initOtel` in `main.ts` and `worker.ts` (`service.name` `clared-worker`) before NestFactory. Local `OTEL_TRACES_EXPORTER=none` / `OTEL_LOGS_EXPORTER=none` constructs NodeSDK with empty span processors — no OTLP exporter, no collector.
- `LoggerModule.forRoot` + `bufferLogs: true` + `app.useLogger(app.get(Logger))`. Dual-write: always `pino/file` dest 1; `pino-opentelemetry-transport` only when `OTEL_LOGS_EXPORTER=otlp`.
- Worker: lightweight pino, same redact, German boot line `Worker gestartet`. No HTTP logger.
- Health e2e: `body.status === "ok"`; `JSON.stringify` has no `customerName` / `iban` / `authorization` / `access_token`. Ready probe unchanged (200 or 503).

## Task Commits

Each task was committed atomically:

1. **Task 1: initOtel local exporters none** - `f713a99` (feat)
2. **Task 2: nestjs-pino JSON stdout + redact** - `31e7649` (feat)
3. **Task 3: Health e2e Pino JSON / no-PII** - `3608b69` (test)

**Plan metadata:** (this commit)

## Files Created/Modified

- `apps/backend/src/telemetry/otel.ts` — NodeSDK bootstrap, span allowlist, German span name constants
- `apps/backend/src/telemetry/otel.spec.ts` — none/idempotent/allowlist
- `apps/backend/src/telemetry/pino-options.ts` — shared redact paths + transport targets
- `apps/backend/src/main.ts` — `initOtel`, `bufferLogs`, `useLogger`
- `apps/backend/src/worker.ts` — `initOtel({ serviceName: "clared-worker" })` + lightweight pino
- `apps/backend/src/app.module.ts` — `LoggerModule.forRoot` (helmet/CORS/Swagger/APP_GUARD unchanged)
- `apps/backend/test/health.e2e-spec.ts` — status ok + PII key forbid
- `apps/backend/test/jest-e2e.json` — transform `@nestjs/bullmq` ESM
- `apps/backend/package.json` / `pnpm-lock.yaml` / `pnpm-workspace.yaml` — deps; `protobufjs` allowBuilds false

## Decisions Made

- NodeSDK log pipeline stays off (`logRecordProcessors: []`). Prod logs ride Pino → `pino-opentelemetry-transport` (D-30), not a second NodeSDK OTLP logger.
- Traces `none`: explicit empty `spanProcessors` because OTel env default is `otlp` (would contact `:4318`).
- OTLP endpoint `http://clared-otel-collector:4318` is set only when traces exporter is `otlp`.
- AUDT-01 in this plan is **prep** (application JSON logs). Full ID stays Phase 5 (`audit_logs` + Grafana). Not marked complete in REQUIREMENTS.md.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] protobufjs ignored build scripts**
- **Found during:** Task 1
- **Issue:** `pnpm add` exited 1 with `ERR_PNPM_IGNORED_BUILDS` for `protobufjs@7.6.6` (OTel transitive). Packages still installed. Same class as Plan 02 `msgpackr-extract`.
- **Fix:** `allowBuilds.protobufjs: false` in `pnpm-workspace.yaml` (JS codec; not a slopsquat).
- **Files modified:** `pnpm-workspace.yaml`
- **Verification:** `pnpm --filter backend install` then `otel.spec.ts` 3 passed
- **Committed in:** `f713a99`

**2. [Rule 3 - Blocking] @nestjs/bullmq ESM broke health e2e**
- **Found during:** Task 3
- **Issue:** `@nestjs/bullmq` is `"type": "module"`. Jest CJS failed to parse `export` when AppModule imported PdfModule (Plan 02).
- **Fix:** `transformIgnorePatterns` + `allowJs` in `test/jest-e2e.json`.
- **Files modified:** `apps/backend/test/jest-e2e.json`
- **Verification:** suite compiles and runs
- **Committed in:** `3608b69`

**3. [Rule 3 - Blocking] RuleSeed blocked AppModule boot on host Postgres**
- **Found during:** Task 3
- **Issue:** Host `:5432` is not compose Postgres (`role "clared_app" does not exist`). `RuleSeedService.onModuleInit` upsert failed before `/health`. Stale `.env` container IP `192.168.107.2` also timed out (live IP `192.168.107.4`).
- **Fix:** Stub `RuleSeedService` in health e2e so `/health` boots without seed (matches “no Postgres I/O” on the public probe). Ready still 200 or 503 with postgres/redis words.
- **Files modified:** `apps/backend/test/health.e2e-spec.ts`
- **Verification:** `pnpm --filter backend exec jest --config ./test/jest-e2e.json test/health.e2e-spec.ts` 2 passed
- **Committed in:** `3608b69`

---

**Total deviations:** 3 auto-fixed (all Rule 3 blocking)
**Impact on plan:** Required for install, e2e parse, and AppModule boot. No Grafana in compose. No Promtail. No `audit_logs` Prisma model. No Coolify this plan.

## Issues Encountered

- Host `127.0.0.1:5432` is a different Postgres than OrbStack `claredtool-postgres-1` (container IP). Health e2e does not require that mapping; Plan 05/product e2e should keep using `docker inspect` IP or published port that actually maps to `clared_app`.

## Authentication Gates

None.

## User Setup Required

None - no external service configuration required. Local OTel exporters stay `none` in `.env.example`. If health **ready** should be 200 against compose Postgres: start **OrbStack** (not Docker Desktop), `docker context use orbstack`, then point `DATABASE_URL` at `docker inspect claredtool-postgres-1 --format '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}'` — never the Authentik DB.

## Next Phase Readiness

Ready for `04.3-05` (Coolify Gotenberg + worker) in parallel wave. Do **not** add Grafana to `compose.clared.yml` (Plan 07). Do not scrape Coolify logs / Promtail. Product `audit_logs` stays Phase 5.

## Self-Check: PASSED

- FOUND: `apps/backend/src/telemetry/otel.ts`
- FOUND: `apps/backend/src/telemetry/pino-options.ts`
- FOUND: `apps/backend/src/main.ts` (`initOtel`, `bufferLogs`, `useLogger`)
- FOUND: `apps/backend/src/worker.ts` (`initOtel`)
- FOUND: `apps/backend/src/app.module.ts` (`LoggerModule`)
- FOUND: `f713a99` `31e7649` `3608b69`
- VERIFY: `otel.spec.ts` 3 passed
- VERIFY: `health.e2e-spec.ts` 2 passed
- VERIFY: `compose.clared.yml` has no grafana/loki/tempo/collector
