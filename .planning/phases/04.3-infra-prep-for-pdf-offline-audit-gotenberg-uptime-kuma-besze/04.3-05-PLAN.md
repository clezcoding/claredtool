---
phase: 04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze
plan: 05
type: execute
wave: 3
depends_on:
  - 04.3-02
files_modified:
  - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PDF-OPS.md
autonomous: true
requirements:
  - PDF-01
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Coolify service clared-gotenberg has no FQDN, alias clared-gotenberg on coolify network, image 8.36.0, Basic Auth env names, API_TIMEOUT=30s, both Chromium deny-IP flags, API_DISABLE_DOWNLOAD_FROM=true (D-11, D-12, D-13, D-19, D-20)."
    - "Coolify application clared-worker uses same GitHub app/Dockerfile as clared-api, start_command node dist/worker.js, no FQDN, alias clared-worker, no prisma migrate (D-14)."
    - "GOTENBERG_URL http://clared-gotenberg:3000 plus Basic Auth names exist on worker (and API if enqueue from API in prod) via Coolify env_vars — values never git (D-11, D-12)."
    - "04.3-PDF-OPS.md records UUIDs, aliases, env NAMES, git_branch used."
  artifacts:
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PDF-OPS.md
  key_links:
    - "Worker start_command replaces Dockerfile CMD so migrate does not run (Pitfall 2)."
    - "Internal DNS is alias not app UUID (Pitfall 1)."
  prohibitions:
    - statement: "Do not assign a public hostname to Gotenberg or the worker (D-11, D-14)."
      status: forbidden
      verification: "coolify app/service get JSON fqdn empty or unset for both."
    - statement: "Do not SSH the VPS or open Coolify UI or raw-curl Coolify API."
      status: forbidden
      verification: "Executor used user-coolify MCP then coolify CLI only."
---

<objective>
Deploy internal Gotenberg and same-image worker on Coolify production so Phase 5 PDF jobs have a prod pipe (PDF-01 prep).

Purpose: Local tracer is proven; prod must match D-11/D-14 network and start_command.
Output: clared-gotenberg service, clared-worker application, 04.3-PDF-OPS.md.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-RESEARCH.md
@.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PATTERNS.md
@.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-02-SUMMARY.md
.cursor/rules/coolify-mcp-cli.mdc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Coolify clared-gotenberg internal service</name>
  <reversibility rating="costly">D-11 internal Gotenberg + network policy; public FQDN later would be a security incident not a rename.</reversibility>
  <precondition>user-coolify MCP authenticated; Coolify project Clared uuid eseyqnptwe3y4wp4myirf8do environment production d1innp9cxvmlsrlj6ksrpxnz server 5enlklyiijhf4x61t4pwdi8y</precondition>
  <files>.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PDF-OPS.md</files>
  <read_first>
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-RESEARCH.md (Coolify inventory, Gotenberg yaml, Pitfall 1)
    - .cursor/rules/coolify-mcp-cli.mdc
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-05-SUMMARY.md
  </read_first>
  <action>
    GetDynamicTools namespace user-coolify then CallDynamicTool. If clared-gotenberg already exists (list_services), do not duplicate — update image/env. Else service create docker_compose_raw name clared-gotenberg project_uuid eseyqnptwe3y4wp4myirf8do server_uuid 5enlklyiijhf4x61t4pwdi8y environment_name production. Compose: image gotenberg/gotenberg:8.36.0 (or Plan 01 Hub twin), expose 3000, no ports, no Coolify FQDN. Env API_ENABLE_BASIC_AUTH=true API_TIMEOUT=30s API_DISABLE_DOWNLOAD_FROM=true CHROMIUM_DENY_PRIVATE_IPS=true CHROMIUM_DENY_PUBLIC_IPS=true; omit CHROMIUM_ALLOW_LIST. Generate username/password with openssl rand; set Coolify env_vars GOTENBERG_API_BASIC_AUTH_USERNAME GOTENBERG_API_BASIC_AUTH_PASSWORD — never write values into git or PDF-OPS.md. custom_network_aliases clared-gotenberg; connect_to_docker_network true. No OTEL env (D-16). Fallback CLI /usr/local/bin/coolify --format json if MCP lacks a field.
  </action>
  <acceptance_criteria>
    - list_services or coolify service list JSON includes name clared-gotenberg
    - get_service shows no public FQDN
    - env_vars names include GOTENBERG_API_BASIC_AUTH_USERNAME and GOTENBERG_API_BASIC_AUTH_PASSWORD (values masked)
  </acceptance_criteria>
  <verify>
    <automated>/usr/local/bin/coolify service list --format json | python3 -c "import json,sys; d=json.load(sys.stdin); names=[x.get('name') or x.get('Name') for x in (d if isinstance(d,list) else d.get('data') or d.get('services') or [])]; assert any('gotenberg' in (n or '').lower() for n in names), names"</automated>
  </verify>
  <done>Internal Gotenberg on Coolify with Basic Auth env (D-11, D-12, D-19, D-20).</done>
</task>

<task type="auto">
  <name>Task 2: Coolify clared-worker same image no migrate</name>
  <reversibility rating="costly">D-14 second Coolify app + deploy matrix.</reversibility>
  <files>.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PDF-OPS.md</files>
  <read_first>
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-RESEARCH.md (clared-worker create_github, Pitfall 2)
    - apps/backend/Dockerfile
  </read_first>
  <action>
    application create_github: github_app_uuid jwuqhppefgpd2wrp8edf23xs, git_repository same as clared-api (clezcoding/claredtool.git), dockerfile_location /apps/backend/Dockerfile, start_command node dist/worker.js, omit fqdn, name clared-worker, project/environment/server same as API. git_branch = git branch --show-current at execute (must contain worker.ts). Then update custom_network_aliases=clared-worker, connect_to_docker_network true. Copy REDIS_URL from clared-api (yzmje7zsrp1qwtvsd7izjhaf) via env_vars; set GOTENBERG_URL=http://clared-gotenberg:3000 and the two Basic Auth names (same values as Gotenberg service, still not git). OTEL_TRACES_EXPORTER=none OTEL_LOGS_EXPORTER=none until Plan 07. Deploy worker. Do not run prisma migrate on worker. Do not point Kuma at worker.
  </action>
  <acceptance_criteria>
    - coolify app list JSON includes clared-worker
    - get_application start_command is node dist/worker.js
    - fqdn omitted/empty
  </acceptance_criteria>
  <verify>
    <automated>/usr/local/bin/coolify app list --format json | python3 -c "import json,sys; d=json.load(sys.stdin); rows=d if isinstance(d,list) else d.get('data') or []; assert any((x.get('name') or '')=='clared-worker' for x in rows), rows"</automated>
  </verify>
  <done>Worker app exists, no public host, start_command skips migrate (D-14).</done>
</task>

<task type="auto">
  <name>Task 3: Write 04.3-PDF-OPS.md (NAMES + UUIDs)</name>
  <files>.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PDF-OPS.md</files>
  <read_first>
    - .planning/phases/04.2-desktop-platform-hardening-tauri-plugins-updater-log-prevent/04.2-UPDATER-OPS.md
  </read_first>
  <action>
    Write 04.3-PDF-OPS.md analog UPDATER-OPS: Coolify UUIDs, aliases clared-gotenberg and clared-worker, env NAMES only, git_branch, Kuma internal URL http://clared-gotenberg:3000/health with A3 fallback TCP 3000, note API enqueue uses same Redis prefix clared-bull. Never paste secrets. Also set GOTENBERG_* on clared-api if API will enqueue in prod (same NAMES).
  </action>
  <acceptance_criteria>
    - 04.3-PDF-OPS.md exists and contains clared-gotenberg, clared-worker, GOTENBERG_URL, start_command
    - file does not contain a password assignment
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PDF-OPS.md && grep -F 'clared-gotenberg' .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PDF-OPS.md && grep -F 'node dist/worker.js' .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PDF-OPS.md && ! grep -Ei 'GOTENBERG_API_BASIC_AUTH_PASSWORD=.+' .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PDF-OPS.md</automated>
  </verify>
  <done>Ops note is SSOT for Phase 5 PDF pipe (D-11, D-14).</done>
</task>

</tasks>

## Artifacts this phase produces

Coolify clared-gotenberg, clared-worker, 04.3-PDF-OPS.md.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Gotenberg | Must be unreachable (no FQDN) |
| Worker → Gotenberg | Docker network + Basic Auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.3-04 | Information Disclosure | Gotenberg WAN | high | mitigate | No FQDN; Basic Auth; alias DNS (D-11) |
| T-04.3-05 | Elevation of Privilege | Worker migrate | medium | mitigate | start_command node dist/worker.js (D-14) |
| T-04.3-12 | Information Disclosure | Secrets in git | high | mitigate | PDF-OPS names only; Coolify env_vars |
</threat_model>

<verification>
coolify lists gotenberg service and worker app; PDF-OPS.md has aliases and start_command; no password values in git.
</verification>

<success_criteria>
Prod worker can resolve http://clared-gotenberg:3000 on coolify network with Basic Auth env set.
</success_criteria>

<output>
Create `.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-05-SUMMARY.md` when done
</output>
