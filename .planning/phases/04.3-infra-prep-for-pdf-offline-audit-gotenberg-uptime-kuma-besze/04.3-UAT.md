---
status: in-progress
phase: 04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze
source: [04.3-08-PLAN.md, 04.3-RESEARCH.md, 04.3-MONITOR-OPS.md, 04.3-OTEL-OPS.md, 04.3-PDF-OPS.md]
started: 2026-08-29T05:18:00Z
updated: 2026-08-29T22:07:00Z
tester: human H3/H4 done; H6 pending
secrets_policy: NAMES and pass/fail only. No bot tokens, ping URLs, KEY/TOKEN values, or admin passwords.
---

## Current Test

[awaiting H6 — Grafana first login]

## H3 — Kuma first admin + monitor matrix

**Status:** pass (2026-08-29)  
**URL:** https://kuma.puzzlessdev.online  
**Resume:** human typed `done` with seven monitor names. Verified via Uptime Kuma MCP (`getMonitorSummary` + `listMonitors`). All seven UP. First-admin password stays in a password manager, not git.

FaynoSync live URL is `/health` (root returns 401 `missing authorization header`; RESEARCH root URL was too coarse). Gotenberg HTTP `/health` + Basic Auth in Kuma succeeded (no A3 TCP fallback). MCP patched Gotenberg `maxretries` 0 → 3 to match D-04.

### Monitors (RESEARCH matrix D-02, D-11, D-28)

| Target | Check | Address | Created | Kuma monitor name |
|--------|-------|---------|---------|-------------------|
| clared-api | HTTP | `https://clared-api.puzzlessdev.online/health` | yes (id 6, UP 200) | ClaredApi Health |
| Authentik | HTTP | `https://clared-auth.puzzlessdev.online` | yes (id 7, UP 200) | ClaredAuthentik |
| FaynoSync | HTTP | `https://updates.puzzlessdev.online/health` (public; not Docker-internal) | yes (id 8, UP 200) | FaynoSync updates |
| Grafana | HTTP | `https://grafana.puzzlessdev.online/login` (200, not authenticated crawl) | yes (id 9, UP 200) | Grafana Login |
| Postgres | TCP | `ypmashcpqnmb3dpxs6nabz4l:5432` on `coolify` | yes (id 10, UP) | Postgres Clared |
| Redis | TCP | `vvqmbjedd979kkjuwrenghn8:6379` | yes (id 11, UP) | Redis Clared |
| Gotenberg | HTTP | `http://clared-gotenberg:3000/health` + Basic Auth in Kuma | yes (id 12, UP 200) | Gotenberg /health |

Gotenberg Basic Auth credentials live in Kuma DB volume, not this file.

### Telegram notify (D-04)

| Item | Status |
|------|--------|
| Notify after retries, then down | pass — all monitors `maxretries` 3; channel `Clared Ops Gruppe` (id 2, Telegram, `isDefault`) attached |
| Recovery notify | pass — same channel on all monitors (Kuma sends recovery when group is attached) |
| TLS expiry notify | pass — `expiryNotification` true on the four public HTTPS monitors |
| Test notification fired | pass (human `done` resume; channel active) |

Token NAMES only: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID` (already in Coolify on `clared-kuma`). Values never written here.

### Status page (D-07)

Public status page enabled: **no**. MCP `listStatusPages` returned empty. Human confirms: yes.

### Not monitored (D-02, D-06)

Garage, Kaneo, worker FQDN, staging FaynoSync — do not add.

## H4 — Beszel admin + agent KEY/TOKEN

**Status:** pass (2026-08-29)  
**URL:** https://beszel.puzzlessdev.online  
**Resume:** human typed `done` after hub showed the VPS agent online.

Env NAMES only (values in Coolify, never git): `BESZEL_KEY`, `BESZEL_TOKEN`, hub `APP_URL`.

| Check | Status |
|-------|--------|
| Hub first admin created | pass |
| This VPS added; KEY/TOKEN minted in hub UI | pass — system `clared-vps`, Host `clared-beszel-agent`, port `45876` |
| Coolify env names set (masked); agent restarted | pass — `BESZEL_KEY` uuid `kjppffqkemyngleh5dpjbgxo`; compose interpolates `${BESZEL_KEY}` / `${BESZEL_TOKEN}`; `HUB_URL=http://clared-beszel:8090`; agent log `WebSocket connected` |
| Agent online (CPU/RAM visible) | pass (human `done`) |

## H6 — Grafana first login

**Status:** pending  
**URL:** https://grafana.puzzlessdev.online  
**Resume:** human types `done` when Grafana login succeeded (or describes cert/DNS failure for H5).

Env NAMES only: `GF_SECURITY_ADMIN_USER`, `GF_SECURITY_ADMIN_PASSWORD`.

| Check | Status |
|-------|--------|
| Login with Coolify `GF_SECURITY_*` works | pending |
| No Authentik interstitial (D-22) | pending |
| Loki / Tempo / Prometheus datasources (note leftover if not pre-provisioned) | pending |
| Optional H5 DNS — skipped 2026-08-29 (wildcard already covers `kuma.` / `beszel.` / `grafana.`) | skipped |

## Preconditions (executor, 2026-08-29)

| Fact | Result |
|------|--------|
| `https://kuma.puzzlessdev.online` responds | HTTP 302 (HEAD) |
| Grafana FQDN exists | `https://grafana.puzzlessdev.online/login` HTTP 200 |
| Gotenberg alias exists | `clared-gotenberg` in 04.3-PDF-OPS.md (`iokfnlnzidksuvrabx7ixxiv`) |

## Evidence notes

H3 names from human chat 2026-08-29 (no credentials): ClaredApi Health, ClaredAuthentik, FaynoSync updates, Grafana Login, Postgres Clared, Redis Clared, Gotenberg /health.
