---
phase: 04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze
plan: 03
type: execute
wave: 3
depends_on:
  - 04.3-02
files_modified:
  - apps/backend/package.json
  - apps/backend/src/main.ts
  - apps/backend/src/app.module.ts
  - apps/backend/src/telemetry/otel.ts
  - apps/backend/src/telemetry/otel.spec.ts
  - apps/backend/test/health.e2e-spec.ts
  - apps/backend/.env.example
autonomous: true
requirements:
  - AUDT-01
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Nest HTTP uses nestjs-pino Logger bufferLogs; GET /health still returns { status: ok } as JSON with no customer/IBAN/token keys (D-23)."
    - "Pino redact covers invoice lines, names, IBAN, tokens (same key classes as 04.2 D-40); German internal log names (D-23)."
    - "initOtel starts NodeSDK HTTP traces; local OTEL_TRACES_EXPORTER=none OTEL_LOGS_EXPORTER=none does not throw and does not contact :4318 (D-24)."
    - "Prod dual-write: stdout pino/file destination 1 and pino-opentelemetry-transport (D-30); worker bootstrap also calls initOtel with service.name clared-worker (D-29)."
    - "Empty Pino log line is still valid JSON object (AUDT empty edge); API vs worker logs are separate streams not merged (AUDT adjacency)."
  artifacts:
    - apps/backend/src/telemetry/otel.ts
    - apps/backend/src/main.ts
    - apps/backend/src/app.module.ts
  key_links:
    - "main.ts calls initOtel before NestFactory.create; AppModule LoggerModule.forRoot; worker.ts also initOtel."
    - "Health e2e still uses GET /health not /health/ready for the public probe body."
  prohibitions:
    - statement: "Do not add Grafana stack to compose.clared.yml (D-24)."
      status: forbidden
      verification: "compose.clared.yml has no grafana/loki/tempo/otel-collector service."
    - statement: "Do not scrape Coolify log files or add Promtail/Alloy (D-30)."
      status: forbidden
      verification: "No promtail/alloy files under apps/backend."
    - statement: "Do not persist audit_logs product rows or UI (Phase 5)."
      status: forbidden
      verification: "No prisma model audit_logs added."
---

<objective>
Backend observability prep: Pino JSON stdout, OTel SDK with local export off, health e2e no-PII (AUDT-01 prep).

Purpose: Application logs exist before Grafana; local Nest must not hang on missing collector (D-23, D-24, D-29, D-30).
Output: otel.ts, LoggerModule, health e2e Pino assertions, otel.spec GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-RESEARCH.md
@.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PATTERNS.md
@.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-02-SUMMARY.md
apps/backend/src/main.ts
apps/backend/src/app.module.ts
apps/backend/src/worker.ts
apps/desktop/src/lib/sentry.ts
apps/backend/test/health.e2e-spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: initOtel local exporters none (GREEN otel.spec)</name>
  <files>apps/backend/package.json, apps/backend/src/telemetry/otel.ts, apps/backend/src/telemetry/otel.spec.ts, apps/backend/src/worker.ts, apps/backend/.env.example</files>
  <read_first>
    - apps/desktop/src/lib/sentry.ts
    - apps/backend/src/telemetry/otel.spec.ts
    - apps/backend/src/worker.ts
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PATTERNS.md (otel.ts, OTel local export off)
  </read_first>
  <behavior>
    - initOtel with both exporters none returns without throw.
    - initOtel is idempotent (second call no-op).
    - Span attribute allowlist excludes customer names, IBAN, tokens (D-23).
  </behavior>
  <action>
    pnpm --filter backend add nestjs-pino pino pino-http pino-opentelemetry-transport @opentelemetry/sdk-node @opentelemetry/auto-instrumentations-node @opentelemetry/exporter-trace-otlp-http (all OK in legitimacy table). Implement telemetry/otel.ts analog sentry.ts skip-when-unset: if exporters are none, still construct NodeSDK without OTLP exporter (D-24). Call initOtel at top of main.ts and worker.ts before NestFactory. German span names (04.2 D-54). Prod endpoint env OTEL_EXPORTER_OTLP_ENDPOINT=http://clared-otel-collector:4318 used only when exporters are otlp (D-29). Do not import loki exporters. Confirm .env.example already has none exporters from Plan 02; add OTEL_SERVICE_NAME if missing.
  </action>
  <acceptance_criteria>
    - pnpm --filter backend test -- otel.spec.ts exits 0
    - otel.ts reads OTEL_TRACES_EXPORTER and OTEL_LOGS_EXPORTER
    - worker.ts and main.ts both call initOtel
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter backend test -- otel.spec.ts</automated>
  </verify>
  <done>Local OTel boot does not throw (D-24, D-29).</done>
</task>

<task type="auto">
  <name>Task 2: nestjs-pino JSON stdout + redact</name>
  <files>apps/backend/src/main.ts, apps/backend/src/app.module.ts</files>
  <read_first>
    - apps/backend/src/main.ts
    - apps/backend/src/app.module.ts
    - apps/desktop/src/lib/sentry.ts
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PATTERNS.md (main.ts, app.module.ts, Pattern 3)
  </read_first>
  <action>
    LoggerModule.forRoot in AppModule with pinoHttp autoLogging and redact paths matching sentry.ts TOKEN_KEY_RE / NAME_KEY_RE plus iban and invoice line fields (D-23). NestFactory.create(AppModule, { bufferLogs: true }); app.useLogger(app.get(Logger)) from nestjs-pino. Dual-write targets: always pino/file destination 1; add pino-opentelemetry-transport only when OTEL_LOGS_EXPORTER is otlp (D-30). Keep helmet CORS Swagger. Keep APP_GUARD and ValidationPipe whitelist. GET /health stays @Public. WorkerModule may use a lightweight pino instance with the same redact — no HTTP logger.
  </action>
  <acceptance_criteria>
    - main.ts contains bufferLogs: true and useLogger
    - app.module.ts imports LoggerModule
    - redact config includes iban and token-like keys
  </acceptance_criteria>
  <verify>
    <automated>grep -F 'bufferLogs' apps/backend/src/main.ts && grep -F 'LoggerModule' apps/backend/src/app.module.ts</automated>
  </verify>
  <done>Pino JSON stdout wired; PII keys redacted (D-23, D-30).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Health e2e Pino JSON / no-PII</name>
  <files>apps/backend/test/health.e2e-spec.ts</files>
  <read_first>
    - apps/backend/test/health.e2e-spec.ts
    - apps/backend/src/health/health.controller.ts
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PATTERNS.md (health.e2e-spec.ts)
  </read_first>
  <behavior>
    - GET /health still 200 with status ok.
    - Response JSON string has no keys customerName, iban, authorization, access_token.
    - Ready probe path unchanged (200 or 503 with postgres and redis words).
  </behavior>
  <action>
    Extend health.e2e-spec.ts: assert body.status === ok; JSON.stringify(body) must not contain customerName, iban, authorization, access_token (D-23). Keep existing ready test. Do not switch Kuma target to ready. Set AUTH_TEST_MODE if required for AppModule boot like other e2e.
  </action>
  <acceptance_criteria>
    - health.e2e-spec.ts asserts status ok and forbidden PII key names absent from body JSON
    - pnpm --filter backend exec jest --config ./test/jest-e2e.json test/health.e2e-spec.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter backend exec jest --config ./test/jest-e2e.json test/health.e2e-spec.ts</automated>
  </verify>
  <done>/health remains Kuma-safe JSON without PII keys (AUDT-01 prep, D-23).</done>
</task>

</tasks>

## Artifacts this phase produces

initOtel, Pino LoggerModule, health e2e no-PII assertions. Collector/Grafana deploy is Plan 07.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nest → stdout / OTLP | Log and span attributes must not carry PII |
| Local process → collector | Must not require collector when exporters none |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.3-07 | Information Disclosure | Pino/OTel PII | high | mitigate | redact + span allowlist (D-23); e2e forbids PII keys |
| T-04.3-08 | Denial of Service | Local OTLP hang | medium | mitigate | exporters none locally (D-24, Pitfall 5) |
| T-04.3-SC | Tampering | npm pino/otel | high | mitigate | legitimacy OK table |
</threat_model>

<verification>
otel.spec.ts green; health e2e green; LoggerModule + bufferLogs present.
</verification>

<success_criteria>
Backend emits JSON logs without invoice/customer/IBAN/token fields; local boot does not need Grafana.
</success_criteria>

<output>
Create `.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-03-SUMMARY.md` when done
</output>
