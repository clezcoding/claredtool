---
phase: 04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze
fixed_at: 2026-08-30T00:10:00Z
review_path: .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-REVIEW.md
iteration: 1
findings_in_scope: 10
fixed: 10
skipped: 0
status: all_fixed
---

# Phase 04.3: Code Review Fix Report

**Fixed at:** 2026-08-30T00:10:00Z
**Source review:** `.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-REVIEW.md`
**Iteration:** 1

**Summary:**
- Findings in scope: 10
- Fixed: 10
- Skipped: 0

**Verification:** Gates ran in isolated worktree (`rf-04.3-47883-1788048442`) after `pnpm install`. `pnpm --filter ./apps/backend test -- --testPathPattern='pdf|gotenberg|otel'` — 7 passed. `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml sentry_scrub` — 2 passed.

## Fixed Issues

### WR-01: Poison-queue jobs may retry on validation errors

**Files modified:** `apps/backend/src/pdf/pdf.processor.ts`
**Commit:** `0bc454b`
**Applied fix:** Throw `UnrecoverableError` from BullMQ for empty/invalid HTML job payloads instead of `EmptyHtmlError`.

### WR-02: Gotenberg success responses are not validated as PDF

**Files modified:** `apps/backend/src/pdf/gotenberg.client.ts`, `apps/backend/src/pdf/gotenberg.client.spec.ts`
**Commit:** `2955eab`, `7a850e3`, `e9590a7`
**Applied fix:** Reject responses shorter than 5 bytes or missing `%PDF` magic; added non-PDF rejection test and fixed existing mock to five-byte `%PDF-` header.

### WR-03: PDF queue jobs have no retention policy

**Files modified:** `apps/backend/src/pdf/pdf.service.ts`
**Commit:** `7d99a5d`
**Applied fix:** `removeOnComplete: { age: 86400 }` and `removeOnFail: { age: 604800 }` on enqueue.

### WR-04: Worker SIGTERM handler can leave an unhandled rejection

**Files modified:** `apps/backend/src/worker.ts`
**Commit:** `a3ee5a9`
**Applied fix:** Wrap `app.close()` in try/catch; log and `process.exit(1)` on teardown failure.

### WR-05: Node.js runtime version drift between CI and Docker

**Files modified:** `apps/backend/Dockerfile`, `.github/workflows/ci.yml`, `.github/workflows/desktop-build.yml`, `.nvmrc`
**Commit:** `4908624`
**Applied fix:** Aligned all runtime pins to Node **24** (`node:24-bookworm-slim`, `node-version: 24`, `.nvmrc`).

### WR-06: Rust unit tests for Sentry scrubbing are not in CI

**Files modified:** `.github/workflows/ci.yml`
**Commit:** `0155141`
**Applied fix:** Added `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml sentry_scrub` to `desktop-test` job.

### IN-01: Compose worker build omits Dockerfile `worker` target

**Files modified:** `compose.clared.yml`
**Commit:** `9f1b42d`
**Applied fix:** Added `target: worker` under worker service `build`.

### IN-02: API readiness does not cover Gotenberg or PDF worker path

**Files modified:** `apps/backend/src/health/health.controller.ts`
**Commit:** `2afa3b7`
**Applied fix:** Optional Gotenberg `/health` ping when `GOTENBERG_URL` is set; skips with message when unset. Worker queue depth deferred (comment).

### IN-03: Release capabilities grant fs/dialog/sharekit to main window

**Files modified:** `apps/desktop/src-tauri/capabilities/default.json`
**Commit:** `25a1a68`
**Applied fix:** Documented intentional Phase 4.3 broad caps in `description`; no permission tightening.

### IN-04: Prisma migrate runs on every API container start

**Files modified:** `apps/backend/Dockerfile`
**Commit:** `5a17151`
**Applied fix:** Added `ponytail:` comment documenting single-replica migrate expectation and multi-replica upgrade path.

---

_Fixed: 2026-08-30T00:10:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
