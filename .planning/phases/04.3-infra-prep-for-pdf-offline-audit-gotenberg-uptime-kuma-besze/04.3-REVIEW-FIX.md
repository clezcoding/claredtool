---
phase: 04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze
fixed_at: 2026-08-29T23:26:00Z
review_path: .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-REVIEW.md
iteration: 1
findings_in_scope: 11
fixed: 11
skipped: 0
status: all_fixed
---

# Phase 04.3: Code Review Fix Report

**Fixed at:** 2026-08-29T23:26:00Z
**Source review:** `.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-REVIEW.md`
**Iteration:** 1

**Summary:**
- Findings in scope: 11
- Fixed: 11
- Skipped: 0

**Verification:** Gates ran in the **main checkout** after worktree fast-forward (not in the isolated worktree — no `node_modules` there).

| Gate | Result |
|------|--------|
| `pnpm --filter ./apps/backend test` | 6 suites, 31 tests passed |
| `pnpm --filter ./apps/desktop test` | 34 suites, 179 tests passed |

## Fixed Issues

### CR-01: Host port 3000 collision — Nest and Gotenberg cannot run together

**Files modified:** `compose.clared.yml`, `apps/backend/.env.example`, `apps/backend/src/pdf/pdf.contract.ts`, `apps/backend/src/pdf/pdf.contract.spec.ts`
**Commit:** `9a88f4d`
**Applied fix:** Gotenberg host publish `127.0.0.1:3001:3000`; `GOTENBERG_URL` and `DEFAULT_GOTENBERG_URL` updated to port 3001.

### CR-02: D-23 span allowlist never applied; auto-instrumentation can export PII

**Files modified:** `apps/backend/src/telemetry/otel.ts`, `apps/backend/src/telemetry/otel.spec.ts`
**Commit:** `cdf1459`, `3a00e1e` (follow-up: import types via `@opentelemetry/sdk-node` re-exports)
**Applied fix:** `AllowlistSpanProcessor` wraps `BatchSpanProcessor`; Redis/PG statement capture disabled; `initialized` set after `start()`; `resetOtelForTests()` added.

### WR-01: `GOTENBERG_URL` fail-open default is loopback

**Files modified:** `apps/backend/src/pdf/gotenberg.client.ts`
**Commit:** `a4d5b76`
**Applied fix:** `gotenbergOrigin()` throws when `GOTENBERG_URL` missing outside `NODE_ENV=test`; test env keeps `DEFAULT_GOTENBERG_URL` fallback.

### WR-02: Convert fetch has no timeout; BullMQ lock matches Gotenberg 30s with no margin

**Files modified:** `apps/backend/src/pdf/gotenberg.client.ts`, `apps/backend/src/pdf/pdf.processor.ts`
**Commit:** `a4d5b76` (fetch timeout), `1a7f94d` (processor `lockDuration: 45_000`)
**Applied fix:** `AbortSignal.timeout(30_000)` on convert fetch; `@Processor` lock duration 45s.

### WR-03: Pino redact misses nested `html` (job payload)

**Files modified:** `apps/backend/src/telemetry/pino-options.ts`
**Commit:** `35f55f0`
**Applied fix:** Added `*.html`, `data.html`, `job.data.html` to redact paths.

### WR-04: Queue payload not validated; `html` undefined throws TypeError

**Files modified:** `apps/backend/src/pdf/pdf.service.ts`, `apps/backend/src/pdf/pdf.processor.ts`
**Commit:** `a4b3caf` (enqueue), `1a7f94d` (processor guard, same commit as WR-02 lockDuration)
**Applied fix:** `EmptyHtmlError` thrown when `html` missing or blank at enqueue and at process.

### WR-05: Release capabilities grant unrestricted SQL execute/load

**Files modified:** `apps/desktop/src-tauri/capabilities/default.json`, `apps/desktop/src-tauri/capabilities/debug.json`, `apps/desktop/src-tauri/build.rs`
**Commit:** `9f0cdd1`
**Applied fix:** SQL permissions removed from release `default.json`; `debug.json` holds `sql:allow-*`; `build.rs` includes debug capability only when `TAURI_ENV_DEBUG` is set.

### WR-06: German span names exported but never used

**Files modified:** `apps/backend/src/telemetry/otel.ts`
**Commit:** `cdf1459`
**Applied fix:** Removed unused `SPAN_HTTP_ANFRAGE` and `SPAN_PDF_WARTESCHLANGE` constants.

### WR-07: CI never compiles `worker.ts` / `nest build`

**Files modified:** `.github/workflows/ci.yml`
**Commit:** `02b4165`
**Applied fix:** Added `pnpm --filter ./apps/backend exec nest build` before backend unit tests.

### WR-08: Image deploy is not main-only; phase branch stays in `on.push`

**Files modified:** `.github/workflows/backend-image.yml`
**Commit:** `c72b326`
**Applied fix:** Removed phase branch from `on.push`; deploy job requires `github.ref == 'refs/heads/main'`.

### WR-09: API entrypoint does not load `.env`; worker does

**Files modified:** `apps/backend/src/main.ts`
**Commit:** `9349a12`
**Applied fix:** `import "dotenv/config"` as first import (mirrors `worker.ts`).

---

_Fixed: 2026-08-29T23:26:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
