---
phase: 04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze
plan: 04
type: execute
wave: 2
depends_on:
  - 04.3-01
files_modified:
  - apps/desktop/package.json
  - apps/desktop/src-tauri/Cargo.toml
  - apps/desktop/src-tauri/src/lib.rs
  - apps/desktop/src-tauri/capabilities/default.json
  - apps/desktop/src-tauri/migrations/0001_init.sql
  - apps/desktop/src/components/debug-panel.tsx
  - apps/desktop/src/__tests__/debug-panel-offline.test.ts
  - apps/desktop/src/__tests__/debug-panel-gate.test.ts
autonomous: true
requirements:
  - OFFL-01
estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "tauri-plugin-sql opens sqlite file clared-offline.db under app_data_dir with SQLCipher PRAGMA key from keychain account offline-sqlcipher service com.clared.app; first launch generates 32 random bytes once, no rotation (D-31, D-32)."
    - "Migration _init dummy table exists (D-31)."
    - "fs scope allow is APPDATA and TEMP only; user-chosen paths go through dialog (D-34)."
    - "dialog, fs, sql, sharekit smokes live in debug panel DEV only; DEBUG_PANEL_ENABLED stays import.meta.env.DEV; no product generate control (D-18, D-33, D-35)."
    - "login.json permissions remain core:default only."
    - "Interrupted first-launch key write: second launch reuses existing keychain item (OFFL concurrency — single key)."
  artifacts:
    - apps/desktop/src-tauri/src/lib.rs
    - apps/desktop/src-tauri/capabilities/default.json
    - apps/desktop/src-tauri/migrations/0001_init.sql
    - apps/desktop/src/components/debug-panel.tsx
  key_links:
    - "SQL plugin path is absolute sqlite: URL under app.path().app_data_dir() not AppConfig relative (Pitfall 6)."
    - "Key never passed to JS; no generate_handler for the SQLCipher secret."
  prohibitions:
    - statement: "Do not grant unbounded fs:default or Downloads/home directory scopes (D-34)."
      status: forbidden
      verification: "capabilities-043.test.ts still passes after this plan."
    - statement: "Do not store SQLCipher key in plugin-store or .env (D-32)."
      status: forbidden
      verification: "offline-sqlcipher used only with keyring Entry in lib.rs."
    - statement: "Do not add product PDF save/share UI or SQLite sync tables (D-18, Phase 5)."
      status: forbidden
      verification: "debug-panel has no generate-pdf control; migration SQL only _init."
---

<objective>
Desktop OFFL-01 prep: official sql/dialog/fs + sharekit, SQLCipher empty DB, debug-panel smokes only (D-31–D-35).

Purpose: Phase 5 can add sync/save without inventing plugins or key story.
Output: plugins registered, capabilities, _init migration, DEV smokes green in vitest.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-CONTEXT.md
@.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-RESEARCH.md
@.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PATTERNS.md
@.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-01-SUMMARY.md
apps/desktop/src-tauri/src/lib.rs
apps/desktop/src-tauri/capabilities/default.json
apps/desktop/src-tauri/capabilities/login.json
apps/desktop/src/components/debug-panel.tsx
apps/desktop/src/lib/clipboard.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sql/dialog/fs + sharekit + SQLCipher unify</name>
  <reversibility rating="costly">D-31 SQLCipher on empty DB; plaintext later is a migration.</reversibility>
  <files>apps/desktop/package.json, apps/desktop/src-tauri/Cargo.toml, apps/desktop/src-tauri/src/lib.rs, apps/desktop/src-tauri/migrations/0001_init.sql</files>
  <read_first>
    - apps/desktop/src-tauri/src/lib.rs
    - apps/desktop/src-tauri/Cargo.toml
    - apps/desktop/src-tauri/capabilities/login.json
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PATTERNS.md (lib.rs, Cargo.toml, SQL migration)
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-RESEARCH.md (SQLCipher, Package Legitimacy)
  </read_first>
  <action>
    From apps/desktop: pnpm tauri add sql, pnpm tauri add dialog, pnpm tauri add fs (official 2.4.0 / 2.7.2 / 2.5.1 line). Add crate tauri-plugin-sharekit = 0.4.0-rc.5 and npm @choochmeque/tauri-plugin-sharekit-api@0.4.0-rc.5 (D-35, no SUS gate). cargo tree -p libsqlite3-sys after add; unify feature bundled-sqlcipher-vendored-openssl (A2). Register plugins in lib.rs chain analog existing clipboard/log. SQL: add_migrations version 1 description _init sql CREATE TABLE IF NOT EXISTS _init (ok INTEGER NOT NULL DEFAULT 1); also write migrations/0001_init.sql same SQL as SSOT if plugin expects files. DB path: app.path().app_data_dir() join clared-offline.db as sqlite: URL (Pitfall 6). Keychain: KEYCHAIN_SERVICE com.clared.app account offline-sqlcipher; if get_password missing, 32 random bytes set_password; PRAGMA key in Rust after connect — never to JS (D-32). Session account stays session. Do not edit login.json.
  </action>
  <acceptance_criteria>
    - Cargo.toml contains tauri-plugin-sql, tauri-plugin-dialog, tauri-plugin-fs, tauri-plugin-sharekit
    - lib.rs contains offline-sqlcipher and clared-offline.db
    - 0001_init.sql contains _init
    - cargo tree -p libsqlite3-sys in apps/desktop/src-tauri prints bundled-sqlcipher or sqlcipher
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/desktop/src-tauri/migrations/0001_init.sql && grep -F 'offline-sqlcipher' apps/desktop/src-tauri/src/lib.rs && grep -F 'clared-offline.db' apps/desktop/src-tauri/src/lib.rs && grep -F '_init' apps/desktop/src-tauri/migrations/0001_init.sql</automated>
  </verify>
  <done>SQLCipher empty DB + plugins registered (D-31, D-32, D-35).</done>
</task>

<task type="auto">
  <name>Task 2: Capabilities APPDATA+TEMP + dialog/sql</name>
  <files>apps/desktop/src-tauri/capabilities/default.json</files>
  <read_first>
    - apps/desktop/src-tauri/capabilities/default.json
    - apps/desktop/src-tauri/capabilities/login.json
    - apps/desktop/src/__tests__/capabilities-043.test.ts
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PATTERNS.md (default.json)
  </read_first>
  <action>
    Add to default.json windows main only: dialog:allow-open, dialog:allow-save, sql:allow-load, sql:allow-execute, sql:allow-select, sql:allow-close, sharekit default permission for main, and fs:scope allow paths $APPDATA, $APPDATA/**, $TEMP, $TEMP/** (D-34). Do not add fs:default. Do not copy these onto login.json. Re-run capabilities-043.test.ts — it must still pass (home-directory token absent).
  </action>
  <acceptance_criteria>
    - default.json includes dialog:allow-open and sql:allow-load
    - default.json fs:scope allow lists $APPDATA and $TEMP
    - login.json permissions array is still only core:default
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec vitest run src/__tests__/capabilities-043.test.ts && python3 -c "import json; d=json.load(open('apps/desktop/src-tauri/capabilities/default.json')); p=d['permissions']; assert 'dialog:allow-open' in p; assert 'sql:allow-load' in p; login=json.load(open('apps/desktop/src-tauri/capabilities/login.json')); assert login['permissions']==['core:default']"</automated>
  </verify>
  <done>Main window scoped fs/dialog/sql; login unchanged (D-34).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Debug-panel offline smokes (GREEN RED tests)</name>
  <files>apps/desktop/src/components/debug-panel.tsx, apps/desktop/src/__tests__/debug-panel-offline.test.ts, apps/desktop/src/__tests__/debug-panel-gate.test.ts</files>
  <read_first>
    - apps/desktop/src/components/debug-panel.tsx
    - apps/desktop/src/lib/clipboard.ts
    - apps/desktop/src/__tests__/debug-panel-gate.test.ts
    - apps/desktop/src/__tests__/debug-panel-offline.test.ts
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PATTERNS.md (debug-panel.tsx)
  </read_first>
  <behavior>
    - DebugPanel DEV renders data-testid debug-section-offline.
    - Smokes for dialog, fs (app-data/temp), sql select _init, sharekit; failure sets German Fehlgeschlagen and desktopLog.error.
    - Release/non-DEV still returns null (DEBUG_PANEL_ENABLED).
    - No generate-PDF control.
  </behavior>
  <action>
    Add Section id debug-section-offline with data-testid, buttons analog handleClipboardTest using mocked plugins (D-33, D-35). Soft-fail try/catch. Extend pluginChecks. Append section id to debug-panel-gate.test.ts list. Make debug-panel-offline.test.ts pass. Keep existing secret grep. Do not dump keychain or session token. Internals German (D-54).
  </action>
  <acceptance_criteria>
    - debug-panel.tsx contains debug-section-offline
    - debug-panel.tsx does not contain a generate PDF product control
    - pnpm --filter ./apps/desktop exec vitest run src/__tests__/debug-panel-offline.test.ts src/__tests__/debug-panel-gate.test.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec vitest run src/__tests__/debug-panel-offline.test.ts src/__tests__/debug-panel-gate.test.ts src/__tests__/capabilities-043.test.ts</automated>
  </verify>
  <done>DEV smokes for dialog/fs/sql/sharekit; product PDF UI absent (D-18, D-33, D-35).</done>
</task>

</tasks>

## Artifacts this phase produces

offline-sqlcipher keychain account, clared-offline.db, debug-section-offline, plugin permissions on main.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView JS → fs/sql | Capabilities must not allow home directory |
| Rust → keychain | SQLCipher key never crosses to JS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.3-09 | Elevation of Privilege | fs scope | high | mitigate | APPDATA+TEMP only; dialog for user paths (D-34); capabilities-043 test |
| T-04.3-10 | Information Disclosure | SQLCipher key | high | mitigate | keychain offline-sqlcipher; PRAGMA in Rust (D-32) |
| T-04.3-11 | Information Disclosure | debug panel release | medium | mitigate | DEBUG_PANEL_ENABLED DEV only (D-33) |
| T-04.3-SC | Tampering | cargo/npm plugins | high | mitigate | official plugins OK; sharekit D-35 skip SUS |
</threat_model>

<verification>
capabilities-043 green; debug-panel-offline green; lib.rs keychain account + db filename present.
</verification>

<success_criteria>
Debug panel (DEV) can smoke sql _init, scoped fs, dialog, sharekit; encrypted empty DB lives in app-data.
</success_criteria>

<output>
Create `.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-04-SUMMARY.md` when done
</output>
