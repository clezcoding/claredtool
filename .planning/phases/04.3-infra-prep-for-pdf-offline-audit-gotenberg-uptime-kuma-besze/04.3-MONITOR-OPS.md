# Phase 04.3 — Monitor Operations (SSOT)

**Status:** Active (Plan 06 Task 2)  
**Scope:** Uptime Kuma + Beszel + Healthchecks.io (AUDT-01, D-01, D-03, D-05, D-08)  
**Secrets:** Env **NAMES** only. Values live in gitignored repo-root `.env` and Coolify. Never commit tokens or ping URLs.

## Env NAMES (values never git)

Copied later onto Coolify `clared-kuma` (Task 3). Source: gitignored `.env` (H1 done).

| Name | Destination |
|------|-------------|
| `TELEGRAM_BOT_TOKEN` | Coolify env on `clared-kuma` (Kuma Telegram notify; H3 wires the bot in Kuma UI) |
| `TELEGRAM_CHAT_ID` | Coolify env on `clared-kuma` (group “Clared ops”, negative id) |

`clared-kuma` does not exist yet. Task 3 creates the service, then copies these two names via Coolify `env_vars`.

Copied onto Coolify `clared-api` (`yzmje7zsrp1qwtvsd7izjhaf`, production, not preview). Source: gitignored `.env` (H2 done).

| Name | Destination |
|------|-------------|
| `HEALTHCHECKS_PING_URL` | Coolify env on `clared-api` — cron (Task 3) curls this; never git the value |

Auth: **built-in login only** on Kuma and Beszel. No Authentik, Cloudflare Access, or IP allowlist in front (D-05).
