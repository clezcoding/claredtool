# Phase 04.3 — Monitor Operations (SSOT)

**Status:** Active (Plan 06)  
**Scope:** Uptime Kuma + Beszel + Healthchecks.io (AUDT-01, D-01, D-03, D-05, D-08, D-09, D-10)  
**Secrets:** Env **NAMES** + Coolify UUIDs only. Values live in gitignored `.env` and Coolify. Never commit tokens or ping URLs.

Auth: **built-in login only** on Kuma and Beszel. No Authentik, Cloudflare Access, or IP allowlist in front (D-05). Hosted on Coolify server `5enlklyiijhf4x61t4pwdi8y`, not Vercel (D-08).

## Coolify inventory (do not guess)

| Resource | Name | UUID | Notes |
|----------|------|------|-------|
| Project | Clared | `eseyqnptwe3y4wp4myirf8do` | |
| Environment | production | `d1innp9cxvmlsrlj6ksrpxnz` | Prod only (D-06). No staging Kuma. |
| Server | localhost | `5enlklyiijhf4x61t4pwdi8y` | |
| Destination | coolify | `y7xsaczoksa1ga6pakatj3iw` | Docker network `coolify` |
| API | `clared-api` | `yzmje7zsrp1qwtvsd7izjhaf` | Healthchecks cron attached |
| Kuma service | `clared-kuma` | `xeeo9cjvfpozmhvaq6vcgqhm` | Compose; Traefik gzip + HTTPS redirect only |
| Kuma sub-app | `kuma` | `kit7ikrlkfygit9vmsko9pcg` | FQDN `https://kuma.puzzlessdev.online` |
| Beszel service | `clared-beszel` | `vhjanbog4nnmhyahiy703gkm` | Hub + agent compose |
| Beszel hub | `beszel` | `6pp5coykyalxkee8qq1p5trk` | FQDN `https://beszel.puzzlessdev.online` |
| Beszel agent | `beszel-agent` | `ofwormlswmrampmgiexzoqkf` | No FQDN. `exclude_from_status` until H4 |
| Healthchecks task | `healthchecks-ping` | `xqg0ryd5cbrewduqoiam9ebk` | On `clared-api` |

## Image pins (D-10)

| Service | Image |
|---------|--------|
| Kuma | `louislam/uptime-kuma:2.5.3` |
| Beszel hub | `henrygd/beszel:0.18.8` |
| Beszel agent | `henrygd/beszel-agent:0.18.8` |

## Volumes (D-09)

Coolify persistent volumes. Not Clared Postgres.

| Mount | Storage UUID | Volume name |
|-------|--------------|-------------|
| `/app/data` (Kuma SQLite) | `ukjsxmp173fkvygirkuxe2wg` | `xeeo9cjvfpozmhvaq6vcgqhm_kuma-data` |
| `/beszel_data` (hub) | `chy6yocikqh031whgaems7cf` | `vhjanbog4nnmhyahiy703gkm_beszel-data` |
| `/var/lib/beszel-agent` | `qyeq0mnuyjfuuoco89ffa08x` | `vhjanbog4nnmhyahiy703gkm_beszel-agent-data` |

Agent also bind-mounts `/var/run/docker.sock` read-only.

## Env NAMES (values never git)

| Name | Destination |
|------|-------------|
| `TELEGRAM_BOT_TOKEN` | Coolify env on `clared-kuma` (H3 wires bot in Kuma UI) |
| `TELEGRAM_CHAT_ID` | Coolify env on `clared-kuma` (group “Clared ops”) |
| `HEALTHCHECKS_PING_URL` | Coolify env on `clared-api` — cron bakes this at create |
| `BESZEL_KEY` | Coolify env on `clared-beszel` (H4 2026-08-29; value never git) |
| `BESZEL_TOKEN` | Coolify env on `clared-beszel` (H4 2026-08-29; value never git) |
| Hub `APP_URL` | `https://beszel.puzzlessdev.online` (compose, not a secret) |

## Healthchecks cron (D-08)

| Field | Value |
|-------|--------|
| App | `clared-api` `yzmje7zsrp1qwtvsd7izjhaf` |
| Name | `healthchecks-ping` |
| Frequency | `*/2 * * * *` |
| Command | `curl -fsS -m 10` plus ping URL **baked from** Coolify `HEALTHCHECKS_PING_URL` at create (A4). Length 72 (≤255). Value never git. |
| Timeout | 30s |
| Enabled | true |

Healthchecks.io check: period **5 min**, grace **5 min**. Cron shorter than period (Pitfall 4). Silence → Telegram group (configured in Healthchecks UI, H2).

Do **not** host Kuma/Beszel on Vercel.

## Kuma target matrix (H3 / Plan 08 — do not create monitors here)

Copied from RESEARCH Internal vs public URL matrix (D-02, D-11, D-28). FaynoSync path corrected 2026-08-29 to `/health`. Kuma MCP `project-0-claredtool-uptime-kuma` can read/update monitors; first-admin and Beszel/Grafana UIs stay human.

| Target | Check type | URL / address | Network | Auth |
|--------|------------|---------------|---------|------|
| clared-api | HTTP | `https://clared-api.puzzlessdev.online/health` | public | none (`GET /health` returns `{ status: "ok" }`) |
| Authentik | HTTP | `https://clared-auth.puzzlessdev.online` | public | none (login page 200; D-05) |
| FaynoSync | HTTP | `https://updates.puzzlessdev.online/health` | public | none (`GET /health` 200 `{ status: "healthy" }`; root `/` is 401 without API key) |
| Grafana | HTTP | `https://grafana.puzzlessdev.online/login` (or `/`) | public | none (login page 200; D-28). **Future** Kuma target — Grafana slug not deployed in this plan. |
| Postgres | TCP | `ypmashcpqnmb3dpxs6nabz4l:5432` | `coolify` | none (port open) |
| Redis | TCP | `vvqmbjedd979kkjuwrenghn8:6379` | `coolify` | none |
| Gotenberg | HTTP | `http://clared-gotenberg:3000/health` | `coolify` | Basic Auth stored **in Kuma**, not git. A3 fallback: TCP `:3000` |
| Kuma / Beszel | — | public HTTPS | public | **built-in login only** — not Kuma targets of themselves |
| Worker | — | no public host | — | **not** a D-02 Kuma target |
| Garage / Kaneo / Vaultwarden | — | — | — | **not** monitored |

FaynoSync is public `https://updates.puzzlessdev.online` (Pitfall 8) — do not probe it on the Coolify internal network.

Do **not** use `/health/ready` for the API (Postgres+Redis). Do **not** create a Kuma public status page (D-07). Logged-in dashboard only (H3).

## Remaining human steps (not this plan)

| # | What |
|---|------|
| H3 | Open `https://kuma.puzzlessdev.online`, first admin, monitors from the matrix, Telegram notify (retries + recovery + TLS). **No status page.** |
| H4 | Open `https://beszel.puzzlessdev.online`, first admin, enroll VPS, paste `BESZEL_KEY` / `BESZEL_TOKEN` into Coolify env, restart agent. |

H5 skipped 2026-08-29 — wildcard `*.puzzlessdev.online` already covers `kuma.` / `beszel.`.

## Operator commands (MCP then CLI)

```bash
coolify service list --format json
coolify service get xeeo9cjvfpozmhvaq6vcgqhm --format json
coolify service get vhjanbog4nnmhyahiy703gkm --format json
coolify service env list xeeo9cjvfpozmhvaq6vcgqhm --format json
coolify app task list yzmje7zsrp1qwtvsd7izjhaf --format json
```

Do not pass `--show-sensitive`. Do not dump ping URL or Telegram token into git or chat.
