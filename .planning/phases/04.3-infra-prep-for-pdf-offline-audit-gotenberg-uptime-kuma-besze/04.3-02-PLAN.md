---
phase: 04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze
plan: 02
type: execute
wave: 2
depends_on:
  - 04.3-01
files_modified:
  - apps/backend/package.json
  - apps/backend/src/app.module.ts
  - apps/backend/src/worker.ts
  - apps/backend/src/worker.module.ts
  - apps/backend/src/pdf/pdf.module.ts
  - apps/backend/src/pdf/pdf.service.ts
  - apps/backend/src/pdf/pdf.processor.ts
  - apps/backend/src/pdf/gotenberg.client.ts
  - apps/backend/src/pdf/empty-html.error.ts
  - apps/backend/src/pdf/gotenberg.client.spec.ts
  - apps/backend/Dockerfile
  - compose.clared.yml
  - apps/backend/.env.example
autonomous: true
requirements:
  - PDF-01
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Local compose runs Gotenberg 8.36.0 on 127.0.0.1:3000 with Basic Auth and both Chromium deny-IP flags; worker service command is node dist/worker.js with no prisma migrate (D-12, D-14, D-15, D-19, D-20)."
    - "HTTP Nest enqueues on BullMQ queue pdf-generation prefix clared-bull; worker ApplicationContext processes jobs and does not listen on HTTP (D-14)."
    - "Worker converts non-empty stub HTML via POST /forms/chromium/convert/html with A4 fields and Basic Auth; empty html throws EmptyHtmlError without calling Gotenberg (D-13, D-17, D-18, D-20)."
    - "Gotenberg has no public hostname in compose; API/worker use GOTENBERG_URL (D-11)."
    - "Dockerfile nest build asserts dist/worker.js exists; CMD still migrates then main (D-14)."
    - "Assumption-delta: Gotenberg remains stdout-only — no OTEL flags (D-16)."
  artifacts:
    - apps/backend/src/worker.ts
    - apps/backend/src/worker.module.ts
    - apps/backend/src/pdf/gotenberg.client.ts
    - compose.clared.yml
    - apps/backend/Dockerfile
  key_links:
    - "PdfService.add uses PDF_QUEUE; PdfProcessor @Processor same name; BullMQ connection from REDIS_URL not RedisService KeyValueStore."
    - "gotenberg.client.ts fetch FormData files=@index.html paperWidth paperHeight; Authorization Basic from GOTENBERG_API_BASIC_AUTH_USERNAME / GOTENBERG_API_BASIC_AUTH_PASSWORD."
  prohibitions:
    - statement: "Do not add a public Coolify FQDN or host bind other than 127.0.0.1:3000 for local Gotenberg."
      status: forbidden
      verification: "compose.clared.yml gotenberg ports line is 127.0.0.1:3000:3000 only."
    - statement: "Do not add Grafana/Loki/Tempo/collector to compose.clared.yml (D-24)."
      status: forbidden
      verification: "compose.clared.yml service keys are postgres, redis, gotenberg, worker only."
    - statement: "Do not add a Nest PdfController or desktop generate control (D-18)."
      status: forbidden
      verification: "No PdfController file; worker.module.ts controllers array empty."
    - statement: "Do not enable Gotenberg OTEL (D-16)."
      status: forbidden
      verification: "gotenberg environment has no OTEL_ or API_TRACE keys."
---

<objective>
Tracer: prove Nest enqueue → Redis pdf-generation → worker ApplicationContext → Gotenberg Chromium HTML convert locally before Coolify expansion (PDF-01 prep).

Purpose: Catch queue, migrate-on-worker, SSRF, and auth architecture failures on one production-quality path (D-11–D-20). 13 files is one vertical slice (Nest worker+client tracer plus Dockerfile/compose expansion) — not split.
Output: BullMQ + worker + Gotenberg client + local compose gotenberg/worker + Dockerfile worker.js assert.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-CONTEXT.md
@.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-RESEARCH.md
@.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PATTERNS.md
@.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-01-SUMMARY.md
apps/backend/src/main.ts
apps/backend/src/app.module.ts
apps/backend/src/redis/redis.service.ts
apps/backend/Dockerfile
compose.clared.yml
</context>

<assumption_delta_decision>
noun: Gotenberg observability
decision: no-change
rationale: ROADMAP Wave 2 said optional OTEL on Gotenberg; CONTEXT D-16 locks stdout-only. Do not add a second telemetry path. Promote is not needed — logs stay container stdout until a later phase revisits D-16.
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>End-to-end enqueue → worker → Chromium HTML PDF — one path</name>
  <reversibility rating="costly">D-13/D-14 Gotenberg + same-image worker contract; swapping renderer later rewrites the client and Coolify service.</reversibility>
  <files>apps/backend/package.json, apps/backend/src/pdf/empty-html.error.ts, apps/backend/src/pdf/gotenberg.client.ts, apps/backend/src/pdf/gotenberg.client.spec.ts, apps/backend/src/pdf/pdf.service.ts, apps/backend/src/pdf/pdf.processor.ts, apps/backend/src/pdf/pdf.module.ts, apps/backend/src/worker.ts, apps/backend/src/worker.module.ts, apps/backend/src/app.module.ts</files>
  <read_first>
    - apps/backend/src/main.ts
    - apps/backend/src/app.module.ts
    - apps/backend/src/redis/redis.service.ts
    - apps/backend/src/invoices/invoices.module.ts
    - apps/backend/src/pdf/pdf.contract.ts
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PATTERNS.md (worker.ts, worker.module.ts, pdf.service.ts, Pattern 1–2)
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/COVERAGE.md (Gotenberg INTEGRATE html only)
  </read_first>
  <action>
    pnpm --filter backend add @nestjs/bullmq bullmq (Package Legitimacy Approved; no human gate). Wire BullModule.forRoot from REDIS_URL with prefix clared-bull and registerQueue name PDF_QUEUE in AppModule or PdfModule once only (D-14). PdfService injects the queue and add() with HtmlInvoiceFacts; do not log html (D-23). Worker.ts NestFactory.createApplicationContext(WorkerModule) — no listen, no helmet (Pattern 1). WorkerModule controllers empty; PdfProcessor extends WorkerHost @Processor(PDF_QUEUE). gotenberg.client.ts uses stdlib fetch + FormData to GOTENBERG_URL default DEFAULT_GOTENBERG_URL path /forms/chromium/convert/html with fields paperWidth 8.27 paperHeight 11.7 and Basic Auth env GOTENBERG_API_BASIC_AUTH_USERNAME GOTENBERG_API_BASIC_AUTH_PASSWORD (D-12, D-17, D-20). EmptyHtmlError if html trim empty — return before fetch (edge empty). Processor calls convert then returns { contentType application/pdf, byteLength }. Unique BullMQ jobId per enqueue (crypto.randomUUID) so parallel enqueue does not collide (edge concurrency). Do not import AppModule into WorkerModule. Do not wrap BullMQ in RedisService. Do not add axios. Do not add PdfController.
  </action>
  <acceptance_criteria>
    - worker.ts calls createApplicationContext(WorkerModule) and has no listen(.
    - gotenberg.client.ts includes the string /forms/chromium/convert/html and EmptyHtmlError on empty html.
    - gotenberg.client.spec.ts covers empty html (no fetch) and UTF-8 html string passed as multipart file body (encoding = UTF-8 bytes).
    - app.module.ts imports PdfModule; REDIS_URL missing still throws from Bull forRoot factory analog redis.service.ts.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter backend test -- gotenberg.client.spec.ts pdf.contract.spec.ts</automated>
  </verify>
  <done>One enqueue→processor→HTML convert client path exists and empty input never hits Gotenberg (D-11–D-18, D-20).</done>
</task>

<task type="auto">
  <name>Task 2: Dockerfile worker.js assert</name>
  <files>apps/backend/Dockerfile</files>
  <read_first>
    - apps/backend/Dockerfile
    - apps/backend/tsconfig.build.json
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PATTERNS.md (Dockerfile)
  </read_first>
  <action>
    After nest build, add test -f dist/worker.js beside existing test -f dist/main.js. Do not change CMD (API still prisma migrate deploy && node dist/main.js) (D-14). Confirm tsconfig.build.json include src so worker.ts emits dist/worker.js.
  </action>
  <acceptance_criteria>
    - Dockerfile contains test -f dist/worker.js
    - Dockerfile CMD still includes prisma migrate deploy and dist/main.js
    - pnpm --filter backend build && test -f apps/backend/dist/worker.js
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter backend build && test -f apps/backend/dist/worker.js && grep -F 'test -f dist/worker.js' apps/backend/Dockerfile && grep -F 'dist/main.js' apps/backend/Dockerfile</automated>
  </verify>
  <done>Image build proves worker.js; API CMD still migrates (D-14).</done>
</task>

<task type="auto">
  <name>Task 3: Local compose Gotenberg + worker + E2E curl</name>
  <precondition>Docker daemon running; compose.clared.yml redis healthy after up</precondition>
  <files>compose.clared.yml, apps/backend/.env.example</files>
  <read_first>
    - compose.clared.yml
    - apps/backend/.env.example
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-RESEARCH.md (Gotenberg yaml, Pattern 2 curl)
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PATTERNS.md (compose.clared.yml)
  </read_first>
  <action>
    Add gotenberg service image gotenberg/gotenberg:8.36.0 ports 127.0.0.1:3000:3000 environment API_ENABLE_BASIC_AUTH=true GOTENBERG_API_BASIC_AUTH_USERNAME GOTENBERG_API_BASIC_AUTH_PASSWORD (compose default local-only values matching .env.example, same class as POSTGRES_PASSWORD clared_app_dev) API_TIMEOUT=30s API_DISABLE_DOWNLOAD_FROM=true CHROMIUM_DENY_PRIVATE_IPS=true CHROMIUM_DENY_PUBLIC_IPS=true — omit CHROMIUM_ALLOW_LIST (D-15, D-19, D-20). Add worker service build dockerfile apps/backend/Dockerfile context repo root, command node dist/worker.js (overrides image CMD; D-14), depends_on redis and gotenberg, env REDIS_URL=redis://redis:6379 GOTENBERG_URL=http://gotenberg:3000 plus the two Basic Auth names, OTEL_TRACES_EXPORTER=none OTEL_LOGS_EXPORTER=none (D-14, D-24). No Grafana services. Extend .env.example with GOTENBERG_URL=http://127.0.0.1:3000 GOTENBERG_API_BASIC_AUTH_USERNAME GOTENBERG_API_BASIC_AUTH_PASSWORD OTEL_TRACES_EXPORTER=none OTEL_LOGS_EXPORTER=none OTEL_SERVICE_NAME=clared-api (names only; local dummy values OK). docker compose -f compose.clared.yml up -d redis gotenberg then curl Pattern 2 against 127.0.0.1:3000 with a one-line index.html; output must start with %PDF. Host worker: REDIS_URL and GOTENBERG_* from .env, node dist/worker.js must stay running without listen logs.
  </action>
  <acceptance_criteria>
    - compose.clared.yml gotenberg image is gotenberg/gotenberg:8.36.0
    - worker command is node dist/worker.js and does not contain prisma
    - curl convert html to 127.0.0.1:3000 with Basic Auth writes a file whose first four bytes are %PDF
  </acceptance_criteria>
  <verify>
    <automated>docker compose -f compose.clared.yml up -d redis gotenberg && python3 -c "from pathlib import Path; import re; t=Path('compose.clared.yml').read_text(); m=re.search(r'(?m)^  worker:\\n(?:    .*\\n)+', t); assert m, 'worker service missing'; b=m.group(0); assert 'node dist/worker.js' in b, b; assert 'prisma' not in b.lower(), b" && python3 -c "open('/tmp/clared-index.html','w').write(chr(60)+'html'+chr(62)+chr(60)+'body'+chr(62)+'ok'+chr(60)+'/body'+chr(62)+chr(60)+'/html'+chr(62))" && curl -fsS --request POST http://127.0.0.1:3000/forms/chromium/convert/html --user "${GOTENBERG_API_BASIC_AUTH_USERNAME:-dev}:${GOTENBERG_API_BASIC_AUTH_PASSWORD:-dev}" --form files=@/tmp/clared-index.html --form paperWidth=8.27 --form paperHeight=11.7 -o /tmp/clared-tracer.pdf && python3 -c "p=open('/tmp/clared-tracer.pdf','rb').read(4); assert p==b'%PDF', p"</automated>
  </verify>
  <done>Local Gotenberg HTML convert returns PDF bytes; worker compose command has no migrate (D-15, D-17, D-20).</done>
</task>

</tasks>

## Artifacts this phase produces

See Plan 01 table. This plan owns worker.ts, WorkerModule, PdfService, gotenberg.client.ts, EmptyHtmlError, compose gotenberg+worker.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → Gotenberg | Internal HTTP; Basic Auth; untrusted HTML must not fetch WAN |
| Nest API → Redis | Job payload must not include tokens; html may be stub only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.3-03 | Information Disclosure | Chromium URL fetch | high | mitigate | HTML route only; deny public+private IPs; empty html never fetched (D-17, D-20) |
| T-04.3-04 | Information Disclosure | Gotenberg WAN | high | mitigate | 127.0.0.1 bind local; Coolify no FQDN in Plan 05 (D-11) |
| T-04.3-05 | Elevation of Privilege | Worker CMD migrate | medium | mitigate | compose/Coolify start_command node dist/worker.js (D-14) |
| T-04.3-06 | Information Disclosure | PII in job logs | high | mitigate | Do not log html body (D-23) |
| T-04.3-SC | Tampering | npm bullmq | high | mitigate | Legitimacy table Approved; pin from pnpm add |
</threat_model>

<verification>
gotenberg.client.spec.ts green; nest build emits worker.js; local curl HTML convert yields %PDF.
</verification>

<success_criteria>
Founder (or agent) can enqueue a stub HTML job and obtain PDF bytes from local Gotenberg without a public Gotenberg host or invoice templates.
</success_criteria>

<output>
Create `.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-02-SUMMARY.md` when done
</output>
