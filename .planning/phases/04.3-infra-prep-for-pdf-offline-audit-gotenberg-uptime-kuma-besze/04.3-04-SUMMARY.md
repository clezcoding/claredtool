---
phase: 04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze
plan: 04
subsystem: desktop
tags: [tauri, sqlcipher, plugin-sql, plugin-dialog, plugin-fs, sharekit, keychain, debug-panel]

requires:
  - phase: 04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze
    provides: sql/dialog/fs/sharekit vitest mocks + capabilities-043 $HOME guard + debug-panel-offline RED
provides:
  - tauri-plugin-sql sqlite + SQLCipher empty clared-offline.db under app_data_dir
  - keychain account offline-sqlcipher (32 bytes once, PRAGMA key in Rust)
  - dialog/fs/sql/sharekit capabilities on main; login.json core:default
  - DEV debug-section-offline smokes (no product PDF UI)
affects: [05]

actuals:
  tokens: 15764
  tasks: 3
  commits: 4

tech-stack:
  added:
    - "@tauri-apps/plugin-sql@~2.4.0"
    - "@tauri-apps/plugin-dialog@~2.7.2"
    - "@tauri-apps/plugin-fs@~2.5.1"
    - "@choochmeque/tauri-plugin-sharekit-api@0.4.0-rc.5"
    - "tauri-plugin-sharekit@0.4.0-rc.5"
    - "libsqlite3-sys@0.30.1 bundled-sqlcipher-vendored-openssl"
  patterns:
    - "SQL plugin registered in setup with absolute sqlite: URL under app_data_dir (Pitfall 6)"
    - "SQLCipher key in OS keychain; sqlite3_auto_extension applies PRAGMA key per connection; never JS"
    - "fs:scope $APPDATA+$TEMP only; dialog for user paths; login.json stays core:default"

key-files:
  created:
    - apps/desktop/src-tauri/migrations/0001_init.sql
  modified:
    - apps/desktop/src-tauri/src/lib.rs
    - apps/desktop/src-tauri/Cargo.toml
    - apps/desktop/src-tauri/capabilities/default.json
    - apps/desktop/src/components/debug-panel.tsx
    - apps/desktop/src/__tests__/debug-panel-offline.test.ts
    - apps/desktop/src/__tests__/debug-panel-gate.test.ts
    - apps/desktop/src/__tests__/setup.ts
    - apps/desktop/package.json
    - pnpm-lock.yaml

key-decisions:
  - "PRAGMA key via sqlite3_auto_extension because tauri-plugin-sql 2.4.0 Pool::connect has no after_connect hook (D-32)"
  - "sql plugin loaded in setup() with absolute sqlite: URL so path_mapper PathBuf::push replaces AppConfig (Pitfall 6)"
  - "fs command perms allow-exists/read-text-file/write-text-file plus fs:scope; never fs:default"
  - "sharekit:default on main only; guest API shareText (D-35)"

patterns-established:
  - "Pattern 1: KEYCHAIN_SERVICE com.clared.app + account offline-sqlcipher; session account unchanged"
  - "Pattern 2: DEV debug-section-offline smokes; DEBUG_PANEL_ENABLED stays import.meta.env.DEV"

requirements-completed: [OFFL-01]

coverage:
  - id: D1
    description: SQLCipher empty DB clared-offline.db under app_data_dir; keychain offline-sqlcipher; _init migration
    requirement: OFFL-01
    verification:
      - kind: other
        ref: "grep offline-sqlcipher + clared-offline.db in lib.rs; 0001_init.sql contains _init; cargo tree -p libsqlite3-sys shows bundled-sqlcipher"
        status: pass
    human_judgment: false
  - id: D2
    description: Main window dialog/sql/sharekit + fs APPDATA/TEMP; login.json core:default only
    requirement: OFFL-01
    verification:
      - kind: unit
        ref: "apps/desktop/src/__tests__/capabilities-043.test.ts"
        status: pass
    human_judgment: false
  - id: D3
    description: DEV debug-section-offline smokes; no generate-PDF control; DEBUG_PANEL_ENABLED DEV-only
    requirement: OFFL-01
    verification:
      - kind: unit
        ref: "apps/desktop/src/__tests__/debug-panel-offline.test.ts"
        status: pass
      - kind: unit
        ref: "apps/desktop/src/__tests__/debug-panel-gate.test.ts"
        status: pass
    human_judgment: false

duration: 9min
completed: 2026-08-29
status: complete
---

# Phase 04.3 Plan 04: Desktop SQLCipher + dialog/fs/sharekit debug smokes Summary

**Official sql/dialog/fs + sharekit registered; SQLCipher empty `clared-offline.db` under app_data_dir with keychain `offline-sqlcipher`; DEV debug-section-offline smokes green; login.json still `core:default`.**

## Performance

- **Duration:** 9 min
- **Started:** 2026-08-29T02:19:00Z
- **Completed:** 2026-08-29T02:28:29Z
- **Tasks:** 3
- **Files modified:** 11

## Accomplishments

- `pnpm tauri add sql/dialog/fs` (2.4.0 / 2.7.2 / 2.5.1) plus `tauri-plugin-sharekit` 0.4.0-rc.5.
- `libsqlite3-sys` 0.30.1 unified with `bundled-sqlcipher-vendored-openssl` (A2).
- Keychain account `offline-sqlcipher` (32 random bytes once); `PRAGMA key` in Rust via `sqlite3_auto_extension`; not in `generate_handler`.
- Dummy `_init` migration; DB file `clared-offline.db` under `app_data_dir()`.
- Main capabilities: dialog open/save, sql load/execute/select/close, sharekit:default, fs APPDATA+TEMP only.
- DEV `debug-section-offline` smokes; `DEBUG_PANEL_ENABLED` unchanged; no product PDF control.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add sql/dialog/fs + sharekit + SQLCipher unify** - `0e5e10b` (feat)
2. **Task 2: Capabilities APPDATA+TEMP + dialog/sql** - `acf3d80` (feat)
3. **Task 3: Debug-panel offline smokes (GREEN)** - `65cc289` (feat)

**Plan metadata:** (this commit)

## TDD Gate Compliance

- RED: `047eb00` from 04.3-01 `debug-panel-offline.test.ts` (section missing)
- GREEN: `65cc289` `feat(04.3-04): add DEV debug-panel offline plugin smokes`
- REFACTOR: skipped

## Files Created/Modified

- `apps/desktop/src-tauri/src/lib.rs` — sharekit + SQL plugin in setup; SQLCipher hook; `offline-sqlcipher`
- `apps/desktop/src-tauri/Cargo.toml` — sql (sqlite), dialog, fs, sharekit, libsqlite3-sys sqlcipher, getrandom
- `apps/desktop/src-tauri/migrations/0001_init.sql` — `_init` dummy table
- `apps/desktop/src-tauri/capabilities/default.json` — dialog/sql/sharekit + fs scope
- `apps/desktop/src/components/debug-panel.tsx` — `debug-section-offline`
- `apps/desktop/src/__tests__/debug-panel-offline.test.ts` — GREEN
- `apps/desktop/src/__tests__/debug-panel-gate.test.ts` — section id appended
- `apps/desktop/src/__tests__/setup.ts` — Database default export + shareText + BaseDirectory
- `apps/desktop/package.json` / `pnpm-lock.yaml` — guest bindings

## Decisions Made

- SQLCipher applied with `sqlite3_auto_extension` + `PRAGMA key` because plugin 2.4.0 has no connect hook (D-32).
- Absolute `sqlite:` URL built from `app.path().app_data_dir()` so the plugin's AppConfig `path_mapper` is replaced (Pitfall 6).
- Extra fs command permissions for exists/read/write text; still no `fs:default` / `$HOME`.
- Sharekit smoke uses `shareText`, not a PDF file share (D-18 / D-35).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] FS command permissions besides fs:scope**
- **Found during:** Task 2
- **Issue:** `fs:scope` alone does not enable exists/read/write commands the DEV smokes call.
- **Fix:** Added `fs:allow-exists`, `fs:allow-read-text-file`, `fs:allow-write-text-file`. No `fs:default`.
- **Files modified:** `apps/desktop/src-tauri/capabilities/default.json`
- **Verification:** `capabilities-043.test.ts` passed (`$HOME` absent)
- **Committed in:** `acf3d80`

**2. [Rule 2 - Missing Critical] PRAGMA key on every sqlx connection**
- **Found during:** Task 1
- **Issue:** `tauri-plugin-sql` 2.4.0 `Pool::connect` has no after_connect; JS must never see the key.
- **Fix:** Key in `OnceLock` from keychain; `sqlite3_auto_extension` runs `PRAGMA key` in Rust.
- **Files modified:** `apps/desktop/src-tauri/src/lib.rs`
- **Verification:** `offline-sqlcipher` and `PRAGMA key` present; not in `generate_handler`
- **Committed in:** `0e5e10b`

**3. [Rule 3 - Blocking] Vitest mocks for default Database + shareText**
- **Found during:** Task 3
- **Issue:** Guest SQL API is `Database.load` default export; sharekit is `shareText`. Wave 0 mock was named `load` / `share`.
- **Fix:** setup.ts default + named `load`; `shareText`; `BaseDirectory` stub.
- **Files modified:** `apps/desktop/src/__tests__/setup.ts`
- **Verification:** three vitest files exit 0
- **Committed in:** `65cc289`

---

**Total deviations:** 3 auto-fixed (2 Rule 2, 1 Rule 3)
**Impact on plan:** Required for encrypted DB and DEV smokes. No product PDF/sync scope.

## Issues Encountered

None beyond the auto-fixes above. `pnpm tauri add` did not touch `login.json`.

## Authentication Gates

None.

## Known Stubs

None — `_init` is the intentional D-31 dummy table, not a UI stub.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Phase 5 can add sync tables and product save/share without inventing plugins or the SQLCipher key story. Do not grant `fs:default` / `$HOME`. Do not pass the SQLCipher key to JS.

## Self-Check: PASSED

- FOUND: `apps/desktop/src-tauri/src/lib.rs`
- FOUND: `apps/desktop/src-tauri/capabilities/default.json`
- FOUND: `apps/desktop/src-tauri/migrations/0001_init.sql`
- FOUND: `apps/desktop/src/components/debug-panel.tsx`
- FOUND: `0e5e10b` `acf3d80` `65cc289`

---
*Phase: 04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze*
*Completed: 2026-08-29*
