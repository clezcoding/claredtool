---
phase: 04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze
plan: 05
subsystem: infra
tags: [coolify, gotenberg, bullmq, worker, docker]

requires:
  - phase: 04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze
    provides: local Gotenberg tracer + worker.ts + BullMQ pdf-generation (04.3-02)
provides:
  - Coolify internal clared-gotenberg 8.36.0 no FQDN alias clared-gotenberg
  - Coolify clared-worker Dockerfile target worker PID 1 node dist/worker.js
  - 04.3-PDF-OPS.md UUIDs aliases env NAMES
affects: [04.3-06, 05]

actuals:
  tokens: 3365
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Coolify 4.3.14 dockerfile pack: start_command is Nixpacks-only; worker uses dockerfile_target_build"
    - "Last Dockerfile stage stays API migrate+main; worker stage CMD node dist/worker.js"
    - "create_github 500 on Public GitHub app: clone clared-api then strip FQDN"

key-files:
  created:
    - .planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PDF-OPS.md
  modified:
    - apps/backend/Dockerfile

key-decisions:
  - "Gotenberg: create docker_compose_raw (did not exist); no public FQDN; Basic Auth via Coolify env NAMES"
  - "Worker: clone API then dockerfile_target_build=worker because start_command does not override Dockerfile CMD"
  - "GOTENBERG_* names on clared-api and clared-worker for enqueue + convert"

patterns-established:
  - "Pattern: PDF-OPS analog UPDATER-OPS — UUIDs aliases env NAMES never password assignment"
  - "Pattern: internal DNS is custom_network_aliases not app UUID (Pitfall 1)"

requirements-completed: []  # PDF-01 is Phase 5 user download; this plan is Coolify pipe prep only

coverage:
  - id: D1
    description: Coolify clared-gotenberg running 8.36.0, no FQDN, Basic Auth env names, Chromium deny IP flags
    requirement: PDF-01
    verification:
      - kind: other
        ref: "coolify service list --format json assert name clared-gotenberg; get_service fqdn null"
        status: pass
    human_judgment: false
  - id: D2
    description: clared-worker start_command node dist/worker.js, fqdn empty, PID 1 Worker gestartet
    requirement: PDF-01
    verification:
      - kind: other
        ref: "coolify app get assert start_command; logs Worker gestartet not RouterExplorer"
        status: pass
    human_judgment: false
  - id: D3
    description: 04.3-PDF-OPS.md aliases UUIDs env NAMES git_branch Kuma health URL no password assignment
    requirement: PDF-01
    verification:
      - kind: other
        ref: "grep clared-gotenberg and node dist/worker.js; ! grep PASSWORD="
        status: pass
    human_judgment: false

duration: 90min
completed: 2026-08-29
status: complete
---

# Phase 04.3 Plan 05: Coolify Gotenberg + clared-worker Summary

**Internal Gotenberg 8.36.0 on Coolify with no public FQDN and Basic Auth env names; same-repo worker runs `node dist/worker.js` via Dockerfile `--target worker` (Coolify 4.3.14 ignores `start_command` on dockerfile packs); ops SSOT in 04.3-PDF-OPS.md.**

## Performance

- **Duration:** 90 min
- **Started:** 2026-08-29T02:39:00Z
- **Completed:** 2026-08-29T03:42:00Z
- **Tasks:** 3
- **Files modified:** 2 git (Dockerfile, PDF-OPS) plus Coolify resources

## Accomplishments

- `clared-gotenberg` `iokfnlnzidksuvrabx7ixxiv` image `gotenberg/gotenberg:8.36.0`, expose 3000, no host ports, sub-app `fqdn` null. Alias `clared-gotenberg` on network `coolify`. Compose env: `API_ENABLE_BASIC_AUTH`, `API_TIMEOUT=30s`, `API_DISABLE_DOWNLOAD_FROM`, both Chromium deny-IP flags. No `CHROMIUM_ALLOW_LIST`. No OTEL.
- `clared-worker` `vwhnsihwpezppgwepvwxrk2h`: clone of `clared-api` (MCP `create_github` 500). FQDN stripped. `start_command` `node dist/worker.js`. `dockerfile_target_build=worker`. Deploy `8tu2puvixorhqhmrthphhu9d` commit `3cef719` used `docker build --target worker`. Logs: `Worker gestartet`, `name":"clared-worker"`. No `RouterExplorer`.
- `GOTENBERG_URL` / Basic Auth **names** on worker and API. `REDIS_URL` copied from API. Worker OTEL exporters none until Plan 07.
- `04.3-PDF-OPS.md`: UUIDs, aliases, env NAMES, `git_branch`, Kuma `http://clared-gotenberg:3000/health` + A3 TCP 3000. Redis prefix `clared-bull`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Coolify clared-gotenberg internal service** - n/a (Coolify-only; no git files)
2. **Task 2: Coolify clared-worker same image no migrate** - `3cef719` (feat)
3. **Task 3: Write 04.3-PDF-OPS.md** - `00dc2cc` (docs)

**Plan metadata:** (this commit)

## Files Created/Modified

- `apps/backend/Dockerfile` — named `build` stage; `worker` CMD `node dist/worker.js`; last stage `api` keeps migrate+main
- `.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PDF-OPS.md` — ops SSOT

## Decisions Made

- Worker create path: `coolify app clone` then strip FQDN — GitHub App has no `private_key`, `create_github` 500.
- Container CMD: `dockerfile_target_build=worker`, not `start_command` (Coolify 4.3.14 `ApplicationDeploymentJob` Nixpacks-only for start_command).
- Gotenberg `connect_to_docker_network` stays false on the service object; compose `networks.coolify.external` + alias is the DNS equivalent.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Dockerfile pack ignores start_command**
- **Found during:** Task 2
- **Issue:** Coolify 4.3.14 injects `start_command` only for Nixpacks. Dockerfile CMD stayed `prisma migrate deploy && node dist/main.js`. Worker logs showed Nest API (`RouterExplorer`).
- **Fix:** Multi-stage Dockerfile; Coolify `dockerfile_target_build=worker`. `start_command` still `node dist/worker.js` for plan verify.
- **Files modified:** `apps/backend/Dockerfile`
- **Verification:** deploy log `--target worker`; runtime `Worker gestartet`
- **Committed in:** `3cef719`

**2. [Rule 3 - Blocking] create_github 500**
- **Found during:** Task 2
- **Issue:** MCP/CLI `create_github` `Attempt to read property "private_key" on null` (Public GitHub app).
- **Fix:** `coolify app clone yzmje7zsrp1qwtvsd7izjhaf --name clared-worker` then update fields; immediately cleared auto-assigned public FQDN.
- **Files modified:** none (Coolify)
- **Verification:** `fqdn` null; `WORKER_START_FQDN_OK`
- **Committed in:** n/a

**3. [Rule 2 - Correctness] Gotenberg network flag**
- **Found during:** Task 1
- **Issue:** MCP/CLI cannot set `connect_to_docker_network` on the service object.
- **Fix:** Compose joins external `coolify` network with alias `clared-gotenberg`.
- **Files modified:** none (Coolify compose)
- **Verification:** `get_service` sub-app `fqdn` null; list name `clared-gotenberg`
- **Committed in:** n/a

**Total deviations:** 3 auto-fixed (Rule 1 ×1, Rule 2 ×1, Rule 3 ×1)
**Impact on plan:** Required for PID 1 = worker and no public Gotenberg/worker host. No scope creep.

## Issues Encountered

- Phase branch was missing on GitHub; first clone failed exit 128. Pushed `gsd/phase-04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze` (not force, not main).
- `custom_docker_run_options --entrypoint` crash-looped (`exited:unhealthy`). Cleared before target-build deploy.
- MCP `dockerfile` inline field 422 — not used.
- CLI `app get` omits `dockerfile_target_build`; MCP `get_application` shows `worker`.

## User Setup Required

None for this plan. Plan 06 owns Kuma monitor create (H3). Founder does not assign FQDNs to Gotenberg or worker.

## Next Phase Readiness

- Plan 06 can HTTP-check `http://clared-gotenberg:3000/health` (Basic Auth in Kuma) or TCP 3000.
- Phase 5 PDF jobs: worker already on `clared-bull` + Gotenberg alias.
- Do not deploy clared-api against this Dockerfile until next API deploy (last stage remains `api`).

## Self-Check: PASSED

- FOUND: `apps/backend/Dockerfile`
- FOUND: `.planning/phases/04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze/04.3-PDF-OPS.md`
- FOUND: `3cef719`
- FOUND: `00dc2cc`
---
*Phase: 04.3-infra-prep-for-pdf-offline-audit-gotenberg-uptime-kuma-besze*
*Completed: 2026-08-29*
