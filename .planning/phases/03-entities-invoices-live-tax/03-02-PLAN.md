---
phase: 03-entities-invoices-live-tax
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/backend/prisma/schema.prisma
  - apps/backend/prisma/migrations
  - apps/backend/src/prisma/prisma.service.ts
  - apps/backend/src/app.module.ts
  - apps/backend/src/auth/permissions.decorator.ts
  - apps/backend/src/auth/permissions.guard.ts
  - apps/backend/src/auth/permissions.guard.spec.ts
  - apps/backend/src/entities
  - apps/backend/src/customers
  - apps/backend/src/invoices
  - apps/backend/src/tax
  - apps/backend/package.json
  - apps/backend/Dockerfile
  - apps/backend/test/entities.e2e-spec.ts
  - apps/backend/test/invoices.e2e-spec.ts
  - apps/backend/test/tax.e2e-spec.ts
  - packages/tax-engine
  - packages/ui/src/components/input.tsx
  - packages/ui/src/components/label.tsx
  - packages/ui/src/components/select.tsx
  - packages/ui/src/components/combobox.tsx
  - packages/ui/src/index.ts
  - apps/desktop/src/auth/api.ts
  - apps/desktop/src/auth/session-provider.tsx
  - apps/desktop/src/routes/entities.tsx
  - apps/desktop/src/routes/rechnung.tsx
  - apps/desktop/src/components/tax-rail.tsx
autonomous: true
requirements: [ENT-01, INV-01, TAX-01, TAX-02]
user_setup:
  - service: orbstack-postgres
    why: "Local Prisma migrate for product tables; Nest stays native pnpm"
    dashboard_config:
      - task: "OrbStack running (docker compose -f compose.clared.yml up -d postgres)"
        location: "local machine — OrbStack 2.x"

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Owner can POST one entity, POST one customer bound to that entity, POST one draft invoice with line items, GET it by id, and POST /api/tax/evaluate with the same invoice-shaped body to receive a TaxDecision (ENT-01, INV-01, TAX-01, D-02, D-10, D-18)"
    - "evaluate(facts) lives in packages/tax-engine and Nest imports it; desktop never imports the package (TAX-02, D-10, D-15)"
    - "Product controllers are registered before CatchAllController; unauthenticated unknown /api still 401 (Phase 2 D-10)"
    - "POST /api/entities requires entity.create; viewer 403; Anlegen stays visible disabled for non-owners (ENT-01, D-04)"
    - "Customer row always has entityId FK; orphan insert is impossible (D-02)"
    - "Draft invoice number is assigned per entity plus calendar year as RE-{year} zero-padded to 3 digits (D-20)"
    - "Currency defaults from the seller entity and remains changeable on the invoice (D-20)"
    - "supplyType persists default service and is not shown on line cards; tax is document-level (D-12)"
    - "Exactly one TaxRule match applies; 0 or many matches become HTTP 422; priority is unused for selection (D-13, A10)"
    - "Rules JSON files are SSOT; OnModuleInit upserts tax_rules and deletes DB rows not in files (D-14)"
    - "apiFetch sends Authorization Bearer from setSessionToken called in applySession (D-10 desktop gap)"
    - "Prisma pool max is 10 (Pitfall 4)"
    - "Dockerfile copies and builds packages/tax-engine CJS before nest build (Pattern 6)"
    - "E1 nav: local routes, no remote load chrome; missing invoice.read uses Phase 1 ErrorState in main pane with nav still shown; five locked labels; no overflow (UI-SPEC E1)"
    - "E8 session: Phase 2 silent-boot Spinner; success lands last draft or first-run empty not the Phase 1 sample (D-17); auth error chrome unchanged; chip footer overflow and truncation unchanged (UI-SPEC E8)"
    - "[EDGE explicit · INV-01 adjacency] Consecutive invoices of the same entity and year get distinct RE-{year}-{n} values; unique (entityId, number); numbers never merge"
    - "[EDGE explicit · INV-01 empty] POST with items [] persists a draft; customerId may be null until identity is filled"
    - "[EDGE explicit · INV-01 concurrency] Parallel POSTs of the first invoice of a year both succeed with distinct counters via ON CONFLICT DO UPDATE RETURNING — not Prisma upsert"
    - "[EDGE explicit · TAX-01 concurrency] evaluate is stateless; overlapping POSTs are independent; HTTP 422 does not pick a winner"
    - "[EDGE backstop · ENT-01 idempotency] Two identical POST /api/entities create two rows (UUID ids, no unique on name)"
    - "[EDGE backstop · ENT-01 concurrency] Parallel entity creates both persist; no name lock"
  artifacts:
    - "packages/tax-engine/src/index.ts (evaluate)"
    - "packages/tax-engine/rules/EU_INTRACOMM_B2B_SERVICE.json"
    - "apps/backend/prisma/schema.prisma models Entity Customer Invoice InvoiceItem TaxRule InvoiceCounter"
    - "apps/backend/src/auth/permissions.guard.ts"
    - "apps/backend/src/entities/entities.controller.ts"
    - "apps/backend/src/invoices/invoices.controller.ts"
    - "apps/backend/src/tax/tax.controller.ts"
    - "apps/backend/src/tax/facts-mapper.ts"
  key_links:
    - "RequirePermission on product routes; PermissionsGuard APP_GUARD after AuthGuard"
    - "facts-mapper.ts maps invoice-shaped DTO to TransactionFacts; TaxController never accepts a client TaxDecision"
    - "rule-seed OnModuleInit files win over tax_rules (D-14)"
    - "setSessionToken in api.ts wired from session-provider applySession"
  prohibitions:
    - statement: "MUST NOT present Clared as open source, free, or customer-self-hosted"
      status: flagged-unverified
    - statement: "MUST NOT invent a collision or priority-tie winner when more than one TaxRule matches"
      status: flagged-unverified
    - statement: "MUST NOT embed the tax engine in the Tauri desktop or evaluate without a server round-trip"
      status: flagged-unverified
---

<objective>
End-to-end tracer: one owner creates an entity and a customer, persists a draft invoice with line items, and sees a live TaxDecision from POST /api/tax/evaluate backed by packages/tax-engine. Then push the Prisma schema.

Purpose: Prove the architecture (CJS library, file RuleStore, PermissionsGuard, controllers before catch-all, Bearer product fetch, document-level tax) before expanding catalogs, 23 rules, and autosave UX.
Output: Product tables + one matrix JSON rule + working evaluate path + schema applied locally.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-entities-invoices-live-tax/03-CONTEXT.md
@.planning/phases/03-entities-invoices-live-tax/03-RESEARCH.md
@.planning/phases/03-entities-invoices-live-tax/03-PATTERNS.md
@.planning/phases/03-entities-invoices-live-tax/03-UI-SPEC.md
@.planning/phases/03-entities-invoices-live-tax/03-01-PLAN.md
@apps/backend/src/app.module.ts
@apps/backend/prisma/schema.prisma
@apps/backend/src/http/catch-all.controller.ts
@apps/backend/Dockerfile
@apps/desktop/src/auth/api.ts
@docs/clared-tax-rule-dsl-schema.json
@docs/clared-tax-engine-architecture.md
</context>

<interfaces>
Prisma @@map table names (PROJECT.md list plus RESEARCH A3 counters):
- entities, customers, invoices, invoice_items, tax_rules, invoice_counters

Permissions catalog strings (reuse rbac.ts, do not duplicate the array):
- POST /api/entities → entity.create
- GET /api/entities → entity.read
- POST /api/customers → kunde.write
- GET /api/customers → kunde.read
- POST /api/invoices → invoice.write
- GET /api/invoices/:id → invoice.read
- POST /api/tax/evaluate → tax.evaluate

evaluate public API:
- evaluate(facts: TransactionFacts): TaxDecision
- throws EvaluateError with code no_unique_match when matched.length !== 1

Invoice number formatter:
- RE-{calendar year}-{last} with last zero-padded to width 3 (D-20, sample RE-2026-001)

TransactionFacts mapper (D-18, Pitfall 7):
- supplier_is_business true when entity has a legal form
- supplier_vat_registered / customer_vat_registered / customer_is_business true iff the corresponding vatId is non-empty
- supply_type from invoice.supplyType default service (D-12)
- channel default direct (A7)
</interfaces>

<tasks>

<task type="tracer">
  <name>End-to-end "create entity, draft invoice, live evaluate" — one path only</name>
  <reversibility rating="costly">D-15 package boundary, D-14 file SSOT, D-18 invoice-shaped evaluate body, and product Prisma models are the extract/migrate seam later phases inherit.</reversibility>
  <files>apps/backend/prisma/schema.prisma, apps/backend/src/prisma/prisma.service.ts, apps/backend/src/app.module.ts, apps/backend/src/auth/permissions.decorator.ts, apps/backend/src/auth/permissions.guard.ts, apps/backend/src/entities/entities.module.ts, apps/backend/src/entities/entities.controller.ts, apps/backend/src/entities/entities.service.ts, apps/backend/src/customers/customers.module.ts, apps/backend/src/customers/customers.controller.ts, apps/backend/src/customers/customers.service.ts, apps/backend/src/invoices/invoices.module.ts, apps/backend/src/invoices/invoices.controller.ts, apps/backend/src/invoices/invoices.service.ts, apps/backend/src/tax/tax.module.ts, apps/backend/src/tax/tax.controller.ts, apps/backend/src/tax/facts-mapper.ts, apps/backend/src/tax/rule-seed.ts, apps/backend/package.json, packages/tax-engine/package.json, packages/tax-engine/src/index.ts, packages/tax-engine/src/schema.ts, packages/tax-engine/src/match.ts, packages/tax-engine/src/store.ts, packages/tax-engine/rules/EU_INTRACOMM_B2B_SERVICE.json, packages/ui/src/index.ts, apps/desktop/src/auth/api.ts, apps/desktop/src/auth/session-provider.tsx, apps/desktop/src/routes/entities.tsx, apps/desktop/src/routes/rechnung.tsx, apps/desktop/src/components/tax-rail.tsx</files>
  <read_first>
    - .planning/phases/03-entities-invoices-live-tax/03-RESEARCH.md (Patterns 1–7, Pitfalls 1–8, Code Examples, Discretion lock list)
    - .planning/phases/03-entities-invoices-live-tax/03-PATTERNS.md (PermissionsGuard analog, catch-all last, CJS package, apiFetch Bearer)
    - .planning/phases/03-entities-invoices-live-tax/03-CONTEXT.md (D-01 through D-20)
    - docs/clared-tax-rule-dsl-schema.json
    - docs/clared-tax-engine-architecture.md
    - apps/backend/src/app.module.ts
    - apps/backend/src/auth/public.decorator.ts
    - apps/backend/src/auth/auth.guard.ts
    - apps/backend/prisma/schema.prisma
    - apps/backend/src/prisma/prisma.service.ts
    - apps/desktop/src/auth/api.ts
    - apps/desktop/src/auth/session-provider.tsx
    - packages/ui/components.json
  </read_first>
  <action>
    Discretion locked for this phase (cite in code comments only as D-ids, not as v1 hedges): debounce 600ms both (UI-SPEC); evaluate invoice-shaped body only; invoice numbers per entity plus calendar year format RE-{year}-{padded 3}; no email or notes on customers; reuse empty-state-hero.png; match count not 1 → 422; supplyType default service hidden; Dockerfile COPY+build tax-engine CJS; product routes before CatchAllController; Bearer on desktop apiFetch; PATCH and list come in 03-05 except GET-by-id now; Prisma pool max 10; exactly one tax match or throw; honor D-12 document-level tax.

    Tax-engine (TAX-02, D-15): implement evaluate, match, store, schema types matching docs/clared-tax-rule-dsl-schema.json (required rule_id, version, conditions, effect; additionalProperties false on root). Install ajv@8.20.0 and ajv-formats@3.0.1 only (RESEARCH Package Legitimacy Audit OK). Ajv2020 from ajv/dist/2020 plus ajv-formats. Ship CJS dist (main ./dist/index.js). One rule file EU_INTRACOMM_B2B_SERVICE.json: concrete exemplar AT supplier / DE customer, both VAT-registered businesses, supply_type service, effect invoice_tax_rate 0, reverse_charge_flag true, tax_liability_party customer, legal_reference EU VAT Directive Art. 44, 196, applied_rule_id EU_INTRACOMM_B2B_SERVICE. Matcher: exactly one match or throw EvaluateError no_unique_match. Do not read priority for selection (D-13). Include source_citation and audit_trace as empty arrays on TaxDecision. Do not persist audit_logs (Phase 4).

    Prisma schema: keep existing generator. Add models Entity, Customer, Invoice, InvoiceItem, TaxRule, InvoiceCounter with @@map names entities, customers, invoices, invoice_items, tax_rules, invoice_counters. String @id @default(uuid()). Money Decimal(12,2) never Float (A5). Customer.entityId required FK (D-02). Invoice.customerId optional. Invoice.supplyType default service. InvoiceItem.position Int, bezeichnung, menge, einzelpreis, netto (server computes menge * einzelpreis). InvoiceCounter unique (entityId, year). Invoice @@unique([entityId, number]). Do not rewrite the empty init migration; add a new dated migration in Task 2. Raise PrismaPg max from 1 to 10 in prisma.service.ts (Pitfall 4). Add workspace dep @clared/tax-engine on the backend package.

    Nest: Permissions decorator + guard modeled on public.decorator.ts / auth.guard.ts. Guard skips when metadata missing. Return false when permission absent (Nest 403). Register APP_GUARD PermissionsGuard after AuthGuard. Controllers at api/entities, api/customers, api/invoices, api/tax — no global prefix. Insert those controllers before CatchAllController (Pitfall 2). DTOs class-validator only (global whitelist already on). Entity create: name, country, legalForm, address required; vatId optional this slice (EU required is 03-03). Customer create: entityId required. Invoice POST: nested createMany items with position from array index. Number via tagged $queryRaw INSERT ON CONFLICT DO UPDATE RETURNING last inside $transaction (Pattern 3) — never Prisma upsert, never string-concat SQL. Currency default from entity.currencyDefault if omitted, then overridable (D-20). TaxController POST evaluate: invoice-shaped DTO → facts-mapper → evaluate. UnprocessableEntityException 422 on EvaluateError. Viewer has tax.evaluate. Do not add tax.override or invoice.issue routes. rule-seed OnModuleInit: glob packages/tax-engine/rules/*.json, upsert by ruleId+version, delete DB rows not in files (D-14).

    Desktop: setSessionToken module-level in api.ts (same pattern as setOnUnauthorized). apiFetch attaches Bearer when token present. applySession and logout in session-provider call set and clear. Do not put token on SessionContextValue. npx shadcn add input label select combobox -c packages/ui (radix-nova already). Export from packages/ui/src/index.ts. Do not add Dialog, Sheet, toast, or Button CVA variants.

    Tracer UI one path only (D-01): Entities screen — enabled Anlegen when entity.create opens the writable create form in the existing detail Card slot (not a dialog, not a new route). Persist via POST /api/entities. Rechnung: after login if this is the first invoice, empty form is OK (D-17); owner with invoice.write can POST a draft that includes the new entityId and a customerId from POST /api/customers (Kunden may stay mostly mock this slice; creating the customer via API from the entity panel flow or a minimal Kunden entity combobox is enough for the path). TaxRail reads POST /api/tax/evaluate result for the four canonical dt keys (D-12). On 422 keep last good or em dash (D-11). Do not embed evaluate in the desktop bundle. Do not add the invoice header picker yet (03-05, D-08). Do not remove PDF peek. Sample invoice remains a test fixture, not the signed-in landing if any draft exists — if zero drafts, empty form (D-17).
  </action>
  <verify>
    <automated>pnpm --filter @clared/tax-engine test || true; pnpm --filter ./apps/backend exec tsc --noEmit --pretty false; test -f packages/tax-engine/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/tax-engine/src/index.ts exports evaluate
    - packages/tax-engine/rules/EU_INTRACOMM_B2B_SERVICE.json exists
    - grep -v '^#' apps/backend/src/app.module.ts | grep -c CatchAllController is greater than 0
    - EntitiesController InvoicesController TaxController identifiers appear in app.module.ts before CatchAllController
    - apps/backend/src/auth/permissions.guard.ts exists
    - apps/desktop/src/auth/api.ts contains setSessionToken
    - schema.prisma contains @@map("entities") and @@map("invoice_counters")
    - prisma.service.ts adapter max is 10
  </acceptance_criteria>
  <done>One architectural path exists in source: entity + customer + invoice + evaluate library + Bearer fetch + tax rail. Schema files are written; migrate is the next task.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Prisma named migration after schema edits</name>
  <precondition>OrbStack Docker is up; compose.clared.yml postgres is reachable at DATABASE_URL used by apps/backend (same local DB as Phase 2 SCHEMA_PUSH).</precondition>
  <files>apps/backend/prisma/schema.prisma, apps/backend/prisma/migrations</files>
  <read_first>
    - apps/backend/prisma/schema.prisma
    - apps/backend/prisma.config.ts
    - compose.clared.yml
    - apps/backend/prisma/migrations/20260822014200_init/migration.sql
  </read_first>
  <action>
    After Task 1 schema.prisma is complete, create a new named migration without rewriting the empty init. From apps/backend, with DATABASE_URL pointing at compose.clared.yml postgres, run pnpm --filter backend exec prisma migrate dev in non-interactive mode with name product_entities_invoices_tax (use --name product_entities_invoices_tax). Do not use prisma db push --accept-data-loss. Do not put migrate dev in the Dockerfile; image CMD stays prisma migrate deploy then node dist/main.js. SCHEMA_PUSH_REQUIRED: no verification of this plan may pass until migrate succeeds. Coolify later uses prisma migrate deploy on start.
  </action>
  <verify>
    <automated>cd apps/backend && npx prisma migrate deploy</automated>
  </verify>
  <acceptance_criteria>
    - A new directory under apps/backend/prisma/migrations besides 20260822014200_init exists
    - The new migration SQL creates tables entities, customers, invoices, invoice_items, tax_rules, invoice_counters
    - npx prisma migrate deploy in apps/backend exits 0
    - The init migration SQL is still the empty comment-only file
  </acceptance_criteria>
  <done>Product schema is applied to local Postgres via a named migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unsip tracer e2e, Dockerfile COPY tax-engine, prove the path</name>
  <files>apps/backend/test/entities.e2e-spec.ts, apps/backend/test/invoices.e2e-spec.ts, apps/backend/test/tax.e2e-spec.ts, apps/backend/src/auth/permissions.guard.spec.ts, apps/backend/Dockerfile, .github/workflows/ci.yml</files>
  <read_first>
    - apps/backend/Dockerfile
    - apps/backend/test/entities.e2e-spec.ts
    - .github/workflows/ci.yml
    - packages/tax-engine/package.json
  </read_first>
  <behavior>
    - Owner POST entity 201; viewer 403
    - Owner POST invoice with items; GET by id returns items and a RE- year number
    - Owner POST /api/tax/evaluate invoice-shaped AT/DE B2B service body returns applied_rule_id EU_INTRACOMM_B2B_SERVICE
    - Evaluate with identity that matches no rule returns 422
  </behavior>
  <action>
    Remove describe.skip('phase03-product') from entities, invoices, tax e2e and permissions.guard.spec.ts. Adjust fixtures so the tax happy path is the tracer AT/DE intra-EU B2B service (VAT ids present). Keep unauthenticated 401 cases. PermissionsGuard unit spec runs against the real guard.

    Dockerfile (Pitfall 1, discretion 8): COPY packages/tax-engine/package.json before pnpm install --filter ./apps/backend...; COPY packages/tax-engine sources and rules/*.json before build; RUN pnpm --filter @clared/tax-engine build before nest build. Keep CMD prisma migrate deploy && node dist/main.js. Copy docs/clared-tax-rule-dsl-schema.json into the image if store.ts reads it by relative path, or copy the schema into the package at build time — files must be present at runtime (D-14).

    Optional CI: add pnpm --filter @clared/tax-engine test only if the one-rule spec is green; otherwise leave CI addition to 03-04. Do not break desktop tsc.
  </action>
  <verify>
    <automated>pnpm --filter ./apps/backend test -- permissions.guard.spec.ts && pnpm --filter ./apps/backend test:e2e -- entities && pnpm --filter ./apps/backend test:e2e -- invoices && pnpm --filter ./apps/backend test:e2e -- tax</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --filter ./apps/backend test:e2e -- entities exits 0
    - pnpm --filter ./apps/backend test:e2e -- invoices exits 0
    - pnpm --filter ./apps/backend test:e2e -- tax exits 0
    - grep -v '^#' apps/backend/Dockerfile | grep -c 'tax-engine' is greater than 0
    - describe.skip('phase03-product') is absent from the three product e2e files
  </acceptance_criteria>
  <done>Authenticated owner can create an entity, persist a draft invoice, and evaluate live tax; viewer cannot create entities; image can resolve @clared/tax-engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Desktop WebView → Nest product routes | Bearer session; untrusted JSON bodies |
| Nest → Postgres | Prisma client + tagged $queryRaw counters |
| Nest → packages/tax-engine | In-process library; rule JSON from repo files |
| npm registry → ajv / ajv-formats | First install of those two packages |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of privilege | POST /api/entities | high | mitigate | RequirePermission entity.create; PermissionsGuard returns false → 403; e2e viewer 403 |
| T-03-02 | Tampering | POST /api/invoices | high | mitigate | ValidationPipe whitelist; no issued/status fields; nested items server-computed netto |
| T-03-03 | Tampering | POST /api/tax/evaluate | high | mitigate | Invoice-shaped DTO only; server maps facts and calls evaluate; reject client TaxDecision fields via whitelist |
| T-03-04 | Tampering | tax-engine RuleStore | high | mitigate | Ajv2020 vs docs/clared-tax-rule-dsl-schema.json; additionalProperties false; no rules-write HTTP |
| T-03-06 | Tampering | invoice_counters SQL | high | mitigate | Tagged Prisma $queryRaw only; never string concatenation |
| T-03-07 | Tampering | tax-rail legal_reference | medium | mitigate | React text nodes String(tax[field]); no dangerouslySetInnerHTML |
| T-03-05 | Information disclosure | instance-global rows | medium | accept | RESEARCH A4: no tenant_id this phase; SAAS-01 deferred; Authentik still sole identity SoT |
| T-03-SC | Tampering | npm installs ajv, ajv-formats | high | mitigate | RESEARCH Package Legitimacy Audit both OK; pin 8.20.0 and 3.0.1; no postinstall scripts |
</threat_model>

<assumption_delta_decision>
noun: shared instance / second identity or tenant source of truth
decision: no-change
rationale: RESEARCH A4 — product tables are instance-global with no tenant_id. RBAC remains capability checks on the Phase 2 Authentik session. This is not a second identity provider and not a new tenant SoT; SAAS-01 row isolation stays deferred.
</assumption_delta_decision>

<verification>
pnpm --filter ./apps/backend test:e2e -- entities
pnpm --filter ./apps/backend test:e2e -- invoices
pnpm --filter ./apps/backend test:e2e -- tax
pnpm --filter ./apps/backend test -- permissions.guard.spec.ts
npx prisma migrate deploy (apps/backend)
</verification>

<success_criteria>
Owner tracer path green against migrated Postgres. Viewer 403 on entity create. Evaluate returns the intra-EU B2B service decision from the file SSOT rule. Dockerfile copies tax-engine. Desktop apiFetch sends Bearer.
</success_criteria>

## Artifacts this phase produces

This plan:
- `evaluate`, `EvaluateError`, `loadRules`, `matches` in @clared/tax-engine
- `RequirePermission`, `PermissionsGuard`
- `EntitiesController`, `CustomersController`, `InvoicesController`, `TaxController`
- `mapDraftToFacts` in facts-mapper.ts
- `RuleSeed` OnModuleInit
- Prisma models Entity, Customer, Invoice, InvoiceItem, TaxRule, InvoiceCounter
- `setSessionToken` / Bearer `apiFetch`
- shadcn Input, Label, Select, Combobox on @clared/ui
- named product migration

<output>
Create `.planning/phases/03-entities-invoices-live-tax/03-02-SUMMARY.md` when done
</output>
