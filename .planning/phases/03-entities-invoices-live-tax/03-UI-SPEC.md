---
phase: 3
slug: entities-invoices-live-tax
status: draft
shadcn_initialized: true
preset: radix-nova
created: 2026-08-22
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> Inherits Phase 1 signed-in shell tokens and Phase 2 auth chrome. This phase turns mock Entities / Kunden / Rechnung / Tax into live create + list + draft autosave + server `evaluate`. Do not regress the dark-first dense shell, login gate, session chip, or PDF peek chrome. PDF generation stays Phase 4 (peek remains staged).

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | radix-nova (`packages/ui/components.json`; `baseColor` neutral, CSS variables; create URL preset `b0`) |
| Component library | Radix via shadcn/ui (`base: radix`, Tailwind v4) |
| Icon library | Lucide React |
| Font | Inter (`--font-sans` in `apps/desktop/src/styles/globals.css`; stack `Inter, ui-sans-serif, system-ui, sans-serif`. No webfont `@font-face`.) |

**Source:** Live `npx shadcn info -c packages/ui` on 2026-08-22 + Phase 1 / Phase 2 UI-SPEC. Installed today: `badge`, `button`, `card`, `dropdown-menu`. Theme stays forced dark. Do not re-init. Do not switch preset. Do not add a light theme.

**Reuse:** `@clared/ui` `Button`, `Card`, `Badge`, `DropdownMenu*`; desktop `Spinner` (16×16, `sr-only` „Wird geladen“), `ErrorState`, `Skeleton`, `TaxRail`, `InvoiceEmptyState`, `LineItemCard`, `CreateDisabledButton` (rewired — see Surfaces). **Add from shadcn official (`@shadcn`) only:** `input`, `label`, `select`, `combobox`. No Dialog, Sheet, AlertDialog, Sonner/toast, Avatar.

**Live `Button` constraint (carry Phase 2):** `packages/ui/src/components/button.tsx` has no `variant` prop and always applies `bg-primary font-medium`. Do not add a CVA variant API this phase. Override with `className` at the call site:

| Surface | className extras (on `@clared/ui` Button) |
|---------|-------------------------------------------|
| „Neue Rechnung“, owner/write „Anlegen“, empty-form primary | keep `bg-primary`; add `min-h-11 font-semibold` (600) |
| Disabled „Anlegen“ / disabled „Neue Rechnung“ | keep `bg-primary`; `disabled` + `min-h-11 font-semibold`; hint sits under the button (Label 12px / 400 / `text-muted-foreground`) |
| Invoice picker trigger, seller/customer/currency/country/legal-form triggers | `min-h-11 bg-card text-foreground font-normal hover:bg-muted` — never `bg-primary` |
| „+ Position“ | `bg-primary font-semibold` (600), compact `px-3 py-1.5` (not 44px — Phase 1 inherit, list-bottom control) |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, field-error gap |
| sm | 8px | Compact element spacing, list row gap, chip/banner inner gap, sidebar `p-2` |
| md | 16px | Default element spacing, page `p-6`, create-panel field stack, header gap |
| lg | 24px | Section padding, card spacing |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing, empty-state column |

Exceptions: Interactive header/picker/Anlegen/Neue-Rechnung controls use 44px (`min-h-11`) minimum hit target (not strictly 4-multiple). Do not enlarge Phase 1 nav rows or „+ Position“ to 44px. Line-item hover-X stays 14px icon, visible on hover/focus (Phase 1 D-21).

**Source:** Phase 1 UI-SPEC 8-point scale + Phase 2 44px exception. No new scale. Radius: `--radius` / `--radius-md` = 8px. Lists, panels, inputs, pickers, cards use `rounded-md` (8px). Do not introduce a new radius.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Label | 12px | 400 (regular) | 1.4 |
| Heading | 20px | 600 (semibold) | 1.3 |
| Display | 28px | 600 (semibold) | 1.2 |

Weights limited to 400 and 600. Body is already set on `body` in `globals.css`. Do not ship `font-medium` (500) on Phase 3 CTAs — override the live Button default as tabulated above. Display is **not** used on signed-in product screens this phase (gate title remains Phase 2).

**Source:** Phase 1 UI-SPEC. Screen titles (`Entities`, `Kunden`, `Rechnung …`, `Tax`) use Heading. Field labels, autosave status, RBAC hints, tax-rail `dt`, combobox empty copy use Label. Values, list rows, form inputs, legal text use Body.

---

## Visual Hierarchy

**Signed-in invoice (this phase focal point):** Invoice heading (20px semibold) at top of the left panel, with the invoice picker in the same header row. First visual anchor after login (last edited draft, D-17). Tax rail is secondary (right, no accent fill). Autosave status is tertiary muted Label in the header — it must not compete with „Neue Rechnung“.

**Entities / Kunden:** Screen heading (20px) + „Anlegen“ in the header. List is the primary scan. Create/detail panel opens **under** the list in the same `max-w-xl` column (D-01) — not a dialog, not a split inspector, not a new route. When the create panel is open, it is the focal form; the list stays visible above.

**Tax screen:** Heading + full TaxDecision definition list. Same density as the rail, more fields. No second CTA.

**Signed-out / login / chip / banners:** Unchanged Phase 2. Silent-boot success no longer lands on the Phase 1 sample invoice — it lands on the last edited draft or the first-run empty (D-17 replaces Phase 1 D-22 and Phase 2 silent-boot „filled sample“).

**Source:** Phase 3 CONTEXT D-01, D-07, D-08, D-17. Phase 1 Visual Hierarchy for the split canvas (D-14). Phase 1 D-08 dense-but-calm.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | hsl(222 47% 11%) | App background, main surfaces |
| Secondary (30%) | hsl(217 33% 17%) | Cards, sidebar, list rows, create panel, pickers, `--card` / `--popover` |
| Accent (10%) | hsl(217 91% 60%) | Reserved list below |
| Destructive | hsl(0 84% 60%) | Line-item delete X, field errors, tax-rail evaluate error, autosave error, Phase 1/2 error text. Not used on „Anlegen“. |
| Foreground | hsl(210 40% 98%) | Headings, input values, list names |
| Muted foreground | hsl(215 20% 65%) | Labels, hints, autosave „Gespeichert“, combobox empty, placeholders |
| Ring | hsl(217 91% 60%) | `focus-visible` ring only (`--ring`) |

Accent reserved for:
- Primary CTA „Neue Rechnung“ (when `invoice.write`)
- „Anlegen“ when the user may create (Entities: `entity.create`; Kunden: `kunde.write`)
- „+ Position“
- Active sidebar navigation item (Phase 1 inherit)
- Empty-state text link (if any remains — this phase has none on Rechnung)
- Phase 2 gate / 401 / cancel „Anmelden“ (unchanged)

Do **not** put accent on: invoice picker trigger, seller/customer/currency/country/legal-form triggers, selected list row, tax rail, autosave status, disabled Anlegen, PDF peek, session chip, role badge.

**List row hover/selected (Phase 3 correction):** `hover:bg-muted` and selected `bg-muted` + `border-border`. Do not use `hover:bg-accent` on entity/customer/invoice-picker rows (live Phase 1 mock painted primary on hover; that violates the 10% accent list once these lists are real). Nav active state stays `bg-accent` (already reserved).

**Dark-only:** `:root` and `.dark` share the same HSL. No theme toggle.

**Focus:** Pickers, Anlegen, Neue Rechnung, inputs, combobox, line-item X use `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background`. Keyboard path required for create panel and pickers. Color is not the only selected-row signal (selected uses `bg-muted` plus `aria-current="true"` or equivalent).

**Source:** Live CSS in `apps/desktop/src/styles/globals.css` (same HSL as Phase 1/2). CONTEXT.md D-04, D-11.

---

## Surfaces (this phase)

### Shell (unchanged)

- Left sidebar `w-48` (192px), five nav items: Rechnung · Entities · Kunden · Tax · PDF. Session chip footer unchanged (Phase 2).
- Main pane `p-6` (24px) on Entities / Kunden / Tax. Rechnung stays split: left form `p-6` + right tax rail `w-80` / `p-4`.
- Remove the Phase 1 **Demo: Bereit / Laden / Fehler** buttons. Loading and error are real network states.

### Landing after login (D-17)

| Condition | Screen |
|-----------|--------|
| At least one draft exists | Rechnung, last edited invoice loaded, tax rail showing last good TaxDecision or em-dash placeholders |
| Zero invoices | Rechnung first-run empty (illustration + empty form). Not the Phase 1 filled sample. `SAMPLE_INVOICE` stays a test fixture only. |
| `invoice.read` missing | Phase 1 `ErrorState` in the main pane („Ein Fehler ist aufgetreten…“) — do not fake an empty marketing state |

### Entities (ENT-01)

- Header: Heading „Entities“ + „Anlegen“.
- „Anlegen“ **always visible**. Enabled iff `entity.create`. Otherwise disabled with hint „Nur Inhaber können Entities anlegen.“ (D-04). Server still 403s if called; that 403 is not the primary UX.
- List: one row per entity, Body 14px, name as primary text. Click selects the row and opens the **read-only detail panel** under the list (Phase 1 D-29 slot).
- „Anlegen“ (enabled) opens the **writable create form in the same panel slot**, replacing detail. Clicking a row while create is open switches back to that row’s detail. No dialog, no `/entities/new` route (D-01).
- Create fields, stacked `gap-2` (8px) inside `Card` `max-w-xl` (D-19):
  1. Name — required, `Input`
  2. Land — required, Combobox (searchable). Placeholder „Land wählen“.
  3. Rechtsform — required, Combobox. Disabled until Land is set. Placeholder „Zuerst Land wählen“. Options = that country’s catalog only (D-03). Never a worldwide flat dump, never free-text.
  4. Adresse — required, `Input`
  5. USt-IdNr. — visible **only when Land is an EU country**. Required in that case. Hidden otherwise (not an empty optional).
- Submit control: none named „Speichern“. Creating persists on explicit primary inside the panel: the panel’s accent button label is **„Entity anlegen“** (one-shot create, not draft autosave). After success, the new row is selected and the panel switches to read-only detail.
- Do not add email/notes. Do not add entity delete UI (`entity.delete` stays unused). Do not add tax-override chrome.

### Kunden (ENT-01)

- Same list + panel pattern as Entities. Heading „Kunden“.
- „Anlegen“ always visible. Enabled iff `kunde.write`. Otherwise disabled with hint „Keine Berechtigung zum Anlegen von Kunden.“
- Customer **always belongs to one entity** (D-02). Create form fields:
  1. Entity — required Combobox of entities the user can read. Placeholder „Entity wählen“.
  2. Name — required
  3. Land — required Combobox
  4. Adresse — required
  5. USt-IdNr. — EU-only, same rule as entity
- No legal-form field on customers (D-19). No email/notes (mock does not show them).
- Panel submit: **„Kunden anlegen“**. After success, select the new row + read-only detail (name, address, country, USt-IdNr. if present, parent entity name).
- Do not add customer delete UI.

### Rechnung canvas (INV-01)

**Header row** (`flex items-start justify-between gap-4`):
- Left: Heading „Rechnung“ + Combobox invoice picker (D-08) showing the current number (or „Neue Rechnung“ while unnumbered). Searchable. Not a sidebar item.
- Right cluster (`flex items-center gap-2`): autosave status (Label 12px) then „Neue Rechnung“.

**„Neue Rechnung“** (D-07): enabled iff `invoice.write`. Opens an **empty form** (no copy of last draft, no Phase 1 sample). Previous drafts remain in the picker. Disabled hint when missing write: „Keine Berechtigung zum Anlegen von Rechnungen.“

**Autosave (D-05):** no Save button. After **600ms** pause in typing (discretion default; same pause as tax evaluate, D-09). Status Label 12px:

| State | Copy | Color |
|-------|------|-------|
| In flight | Speichert… | `text-muted-foreground`; optional 16×16 Spinner beside the text. `prefers-reduced-motion: reduce`: no spin |
| Success | Gespeichert | `text-muted-foreground` |
| Failure | Speichern fehlgeschlagen. Eingaben bleiben sichtbar. | `text-destructive` |

Failure does not blank the form and does not block typing. Retry on the next debounce. No toast.

**Without `invoice.write`:** picker and fields are read-only; autosave is off; status hidden; „Neue Rechnung“ disabled with the hint above. Tax rail still evaluates if `tax.evaluate` (viewer/auditor).

**Identity card** (new, under header; `Card` + `grid gap-3` like the existing number/date/due card):

| Field | Control | Rules |
|-------|---------|-------|
| Rechnungsnummer | read-only `Input` | Assigned by backend (year + counter). Placeholder until first successful autosave: „Wird vergeben…“. User cannot edit. |
| Datum | `Input` | Existing |
| Fällig | `Input` | Existing |
| Entity (seller) | Combobox | Required. Placeholder „Entity wählen“. Changing seller filters Kunden and, if currency still equals the previous entity default, updates currency to the new entity default (D-20). |
| Kunde | Combobox | Required. Disabled until Entity is set. Placeholder „Zuerst Entity wählen“. Options = customers of that entity only (D-02). |
| Währung | Select | Defaults from seller entity; changeable per invoice (D-20). Options: EUR, USD, AED, plus the entity default if missing from that list. Trigger is secondary (not accent). |

**Line items:** compact cards (Phase 1 D-17–D-21). Four fields stay visible. This phase they are **editable `Input`s** (same chrome as header inputs). Netto is computed read-only. Document-level tax only — no per-line tax on the card. „+ Position“ at list bottom. Hover/focus X deletes the line immediately (no confirm dialog; copy is the accessible name „Position löschen“).

**Empty form vs first-run illustration:**
- **Zero invoices (first-run):** reuse `apps/desktop/public/empty-state-hero.png` above the empty form in the left pane (`max-w-xl`, `rounded-md`, `alt=""`). Heading + body from Copywriting empty rows. **Remove** „Beispielrechnung anzeigen“. Tax rail and PDF peek stay hidden while the illustration is showing (Phase 1 E6). The empty identity card + empty line list + „+ Position“ sit under the copy so the user can start without a second CTA.
- **Neue Rechnung while drafts exist:** no illustration. Empty identity card + empty line list. Tax rail visible with em-dash placeholders until the first successful evaluate.
- After the first persistable keystroke (or first successful autosave), hide the illustration if it was showing and reveal the tax rail.

**PDF peek:** unchanged Phase 1 staged paper under the rail. Do not wire real PDF. Do not remove. Click still opens `/pdf` staged view.

### Live tax (TAX-01)

- Re-evaluate automatically **600ms** after relevant fields change (D-09). No „Steuer berechnen“ button. Desktop never embeds the engine (D-10).
- Invoice **rail shows exactly four fields** as `dt` keys (canonical, not translated): `invoice_tax_rate`, `reverse_charge_flag`, `legal_reference`, `applied_rule_id` (D-12). Heading remains „Live Steuerberechnung“.
- `/tax` shows all nine TaxDecision fields (Phase 1 list) from the **same** evaluate result, live.
- Before the first successful evaluate: labels visible, values em dash `—`. Never show `SAMPLE_INVOICE` tax numbers on the signed-in path.
- On evaluate failure (D-11): **keep the last good TaxDecision** (or em dashes if none). Show a small error **in the rail** (and the same line on `/tax`): Body 14px / 400 / `text-destructive`, wraps. Do not blank the rail. Do not block typing. No modal. Next debounce retries. Optional rail text button „Erneut versuchen“ (accent link style `text-primary`, not a filled CTA) re-fires evaluate immediately.
- No tax override UI (D-16). `tax.override` unused in UI.

### Country → legal form (D-03)

Two sequential Comboboxes, never one 200-row dump. Country Combobox is searchable (German display names, ISO stored). Legal-form Combobox is searchable **within the selected country**. Changing country clears legal form. Combobox empty copy: „Kein Land gefunden.“ / „Keine Rechtsform gefunden.“

### Higgsfield

Reuse Phase 1 `empty-state-hero.png` (2688×1520) for first-run invoice empty (discretion). **Do not** generate a new illustration this phase. Do not reuse `login-gate-hero.png` here. Missing file: keep heading, body, and empty form; do not swap in the login-gate art.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | Neue Rechnung |
| Empty state heading | Noch keine Rechnung erstellt |
| Empty state body | Erstellen Sie Ihre erste Rechnung und sehen Sie sofort die Steuerberechnung. Der komplette Ablauf – vom Entwurf bis zum fertigen PDF – dauert unter 2 Minuten. |
| Error state | Ein Fehler ist aufgetreten. Bitte versuchen Sie es erneut oder kontaktieren Sie den Support. |
| Destructive confirmation | Position löschen: hover/focus X, no dialog — accessible name „Position löschen“. No entity/customer/invoice delete UI this phase. |

Additional copy elements:

| Element | Copy |
|---------|------|
| Entities Anlegen | Anlegen |
| Entities create submit | Entity anlegen |
| Entities RBAC hint | Nur Inhaber können Entities anlegen. |
| Entities empty heading | Noch keine Entity angelegt |
| Entities empty body | Legen Sie Ihre erste Firma an, um Rechnungen zu stellen. |
| Kunden Anlegen | Anlegen |
| Kunden create submit | Kunden anlegen |
| Kunden RBAC hint | Keine Berechtigung zum Anlegen von Kunden. |
| Kunden empty heading | Noch keine Kunden angelegt |
| Kunden empty body | Legen Sie einen Kunden für die gewählte Entity an. |
| Invoice write hint | Keine Berechtigung zum Anlegen von Rechnungen. |
| Invoice picker empty | Keine Rechnung gefunden. |
| Invoice number placeholder | Wird vergeben… |
| Seller placeholder | Entity wählen |
| Customer placeholder | Zuerst Entity wählen |
| Customer placeholder (entity set) | Kunde wählen |
| Country placeholder | Land wählen |
| Legal form placeholder (no country) | Zuerst Land wählen |
| Legal form placeholder (country set) | Rechtsform wählen |
| Country combobox empty | Kein Land gefunden. |
| Legal form combobox empty | Keine Rechtsform gefunden. |
| EU VAT required | USt-IdNr. ist für EU-Länder Pflicht. |
| Autosave in flight | Speichert… |
| Autosave success | Gespeichert |
| Autosave error | Speichern fehlgeschlagen. Eingaben bleiben sichtbar. |
| Tax rail heading | Live Steuerberechnung |
| Tax evaluate error | Steuerberechnung fehlgeschlagen. Letzte gültige Werte bleiben sichtbar. |
| Tax evaluate retry | Erneut versuchen |
| Tax empty value | — |
| Add line item | + Position |
| Invoice form labels | Rechnungsnummer · Datum · Fällig · Entity · Kunde · Währung · Bezeichnung · Menge · Einzelpreis · Netto |
| Entity/Kunde field labels | Name · Land · Rechtsform · Adresse · USt-IdNr. · Entity |
| PDF peek | Vorschau |
| Network retry | Erneut versuchen |
| Remove | Beispielrechnung anzeigen (delete this control on the signed-in empty path) |
| Remove | Wird in Phase 3 aktiviert (replace with the RBAC hints above) |
| Remove | Demo: Bereit / Demo: Laden / Demo: Fehler |

Do not write: Stellen, issued, override, Steuer berechnen, Speichern (invoice), open source, kostenlos, English nav labels beyond the existing Entities / Tax / PDF. German domain terms stay Entity, Kunde, Rechnung (root CONTEXT.md). `index.html` `lang="de"`.

**Source:** Phase 1 copy rows reused for invoice empty + generic ErrorState. CONTEXT.md D-04, D-05, D-07, D-11, D-16, D-19. Defaults (not re-asked): RBAC hints, tax-rail error, autosave labels, entity/customer empty.

---

## UI Considerations

> Populated by the ui-phase UI-consideration probe (Step 9.5). Empty-state and error-state COPY live in `## Copywriting Contract` — this section covers state coverage and references those rows.

Applicable state considerations resolved: 48 covered, 0 backstop, 0 unresolved

Phase 1 E1–E10 and Phase 2 E11–E17 remain in force except where this table **replaces** a Phase 1 empty/landing rule (E1/E4/E6/E7/E8).

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | E4 first-run invoice | ✅ covered | Zero invoices: Phase 1 hero + Copywriting empty heading/body; no „Beispielrechnung anzeigen“; empty form under the copy (D-07, D-17). |
| empty | E1 Neue Rechnung | ✅ covered | With existing drafts: empty identity card + empty line list; picker keeps prior drafts; no illustration (D-07). |
| empty | E6 PDF peek / tax rail | ✅ covered | Hidden while first-run illustration is showing; revealed after first persistable keystroke/autosave. Peek stays staged. |
| empty | E18 entity list | ✅ covered | Copywriting Entities empty heading/body; Anlegen still visible (enabled or disabled per D-04). |
| empty | E20 kunden list | ✅ covered | Copywriting Kunden empty heading/body; Anlegen still visible. |
| empty | E23 invoice picker | ✅ covered | Combobox empty: „Keine Rechnung gefunden.“ First-run uses E4, not a blank picker-only pane. |
| empty | E22 country/legal-form | ✅ covered | Combobox empty: „Kein Land gefunden.“ / „Keine Rechtsform gefunden.“ Legal form disabled until country set. |
| empty | E25 tax rail | ✅ covered | Before first success: four labels + em dash `—`. Never sample tax numbers on the signed-in path. |
| empty | E27 seller/customer | ✅ covered | Placeholders „Entity wählen“ / „Zuerst Entity wählen“ / „Kunde wählen“. Customer disabled until seller set. |
| loading | E18, E20 lists | ✅ covered | `Skeleton` rows in the `max-w-xl` list (Phase 1 skeleton language). Header + Anlegen stay visible. |
| loading | E1 invoice load | ✅ covered | `Skeleton` for identity card + three line-card placeholders; Spinner may sit in the header. No Demo buttons. |
| loading | E24 autosave | ✅ covered | „Speichert…“ + optional 16×16 Spinner. `prefers-reduced-motion: reduce`: static ring. |
| loading | E25, E26 evaluate | ✅ covered | Last good values (or em dashes) stay; do not swap the rail for a skeleton. Optional Spinner in the rail heading row while in flight. |
| loading | E19, E21 create submit | ✅ covered | Panel submit button label stays; Spinner inside the button; button disabled while in flight. |
| error | E18, E20, E1 load | ✅ covered | Phase 1 `ErrorState` + „Erneut versuchen“ (Copywriting error row). |
| error | E24 autosave | ✅ covered | Copywriting autosave error; form stays; next debounce retries. |
| error | E25, E26 evaluate | ✅ covered | Copywriting tax error in the rail/`/tax`; last good TaxDecision kept; typing unrestricted (D-11). |
| error | E19, E21 validation | ✅ covered | Field error under the control, Label 12px `text-destructive`. EU VAT: „USt-IdNr. ist für EU-Länder Pflicht.“ |
| error | E29 Anlegen RBAC | ✅ covered | Disabled button + hint; 403 is not the primary UX (D-04). |
| populated | E18, E20 | ✅ covered | Name rows; selected `bg-muted`; click opens read-only detail under the list. |
| populated | E2 line items | ✅ covered | Compact 4-field editable cards + „+ Position“ (D-17–D-21). |
| populated | E23 picker | ✅ covered | Current number (or „Neue Rechnung“ if unnumbered) on the trigger; list of drafts. |
| populated | E25 rail | ✅ covered | Four canonical fields from live TaxDecision (D-12). |
| populated | E26 /tax | ✅ covered | Nine canonical fields from the same decision. |
| partial | E1 identity | ✅ covered | Incomplete draft stays on-canvas; number placeholder until first autosave; missing seller/customer keep placeholders. |
| partial | E2 line items | ✅ covered | Blank new card (Bezeichnung empty, Menge/Einzelpreis 0) is visible, not hidden. |
| partial | E19, E21 | ✅ covered | USt-IdNr. hidden until EU country; legal form empty until country set. |
| overflow | E18, E20 lists | ✅ covered | Column scrolls; header + Anlegen stay put. Names wrap (no ellipsis). |
| overflow | E2 line items | ✅ covered | Left panel scrolls when cards exceed the pane. |
| overflow | E23, E22, E27 combobox | ✅ covered | List viewport scrolls inside ComboboxContent; trigger truncates with `title` = full string. |
| overflow | E25 rail | ✅ covered | Rail scrolls if legal text exceeds the pane. |
| overflow | E5 nav | ✅ covered | Fixed 5 items; no overflow (Phase 1 D-06). |
| zero-one-many | E18, E20 | ✅ covered | Zero = empty copy; one or many = stacked rows; panel still one at a time. |
| zero-one-many | E2 line items | ✅ covered | Zero on first-run uses E4 illustration+form; zero after Neue Rechnung with drafts = empty list + „+ Position“; one or many = cards. |
| zero-one-many | E23 picker | ✅ covered | Zero invoices = first-run E4; one or many = picker lists them. |
| long-text | E18, E20 names | ✅ covered | Wrap in the row. |
| long-text | E2 Bezeichnung | ✅ covered | Wrap inside the card. |
| long-text | E25 legal_reference | ✅ covered | Wrap, then rail scrolls. |
| long-text | E10 / E24 / tax error | ✅ covered | Wrap inside the rail/header. No ellipsis on errors. |
| long-text | E23 trigger | ✅ covered | Truncate + native `title`. |
| long-text | E3 nav, E9 CTA | ✅ covered | Short German/locked labels; no truncation. |

**Kinds (author-confirmed):** E18/E20 list-collection + interactive-control · E19/E21 form · E22 interactive-control + form · E23 list-collection + interactive-control · E24 static-content · E25/E26 static-content · E27 interactive-control + form · E29 interactive-control. E1/E2/E4/E6 as Phase 1, updated by D-07/D-17.

---

## Out of scope (do not draw)

- Invoice issue / „Stellen“, real PDF, multilingual TaxDecision blocks on paper (Phase 4)
- `audit_logs` UI (Phase 4)
- Offline / embedded engine (Phase 4)
- Tax override UI (`tax.override`)
- Entity/customer/invoice delete chrome
- Create dialogs, extra routes, worldwide legal-form dump, free-text legal form
- CH/UK rule modules not in `docs/clared-tax-rule-matrix.md`
- Stripe/seats, profile, billing, toasts, light theme, Button CVA variants
- New Higgsfield art this phase

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official (`@shadcn`, `https://ui.shadcn.com/r/styles/{style}/{name}.json`) | existing: badge, button, card, dropdown-menu; this phase: input, label, select, combobox | not required — 2026-08-22 (live `npx shadcn info -c packages/ui`: only `@shadcn`; no third-party registries) |
| third-party | none | n/a |

**Note:** CONTEXT.md declares no third-party registries. Use radix docs (`ui.shadcn.com/docs/components/radix/...`) to match `base: radix`. Combobox is the official radix Combobox (`Combobox`, `ComboboxInput`, `ComboboxContent`, `ComboboxEmpty`, `ComboboxList`, `ComboboxItem`) — not a third-party “command palette” kit. Registry vetting gate not applicable (no third-party blocks).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
