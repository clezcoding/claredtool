---
phase: 03-entities-invoices-live-tax
plan: 05
type: execute
wave: 4
depends_on: ["03-02", "03-03"]
files_modified:
  - apps/backend/src/invoices
  - apps/desktop/src/routes/rechnung.tsx
  - apps/desktop/src/routes/tax.tsx
  - apps/desktop/src/components/tax-rail.tsx
  - apps/desktop/src/components/invoice-empty-state.tsx
  - apps/desktop/src/components/line-item-card.tsx
  - apps/desktop/src/__tests__/invoice.test.tsx
  - apps/desktop/src/__tests__/demo-states.test.tsx
  - apps/desktop/src/__tests__/screens.test.tsx
  - apps/desktop/src/__tests__/phase03-tax-rail.test.tsx
autonomous: true
requirements: [INV-01, TAX-01]
user_setup: []

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Drafts autosave after 600ms pause; no required Save button; no per-field-blur save (INV-01, D-05)"
    - "This phase is draft only; no Stellen / issued status (D-06)"
    - "Neue Rechnung opens an empty form; previous drafts stay in the header picker; it does not copy the last invoice or restore the Phase 1 sample (D-07)"
    - "Past invoices are a picker in the invoice header, not a new sidebar item (D-08)"
    - "Tax re-evaluates automatically after the same 600ms pause; no calculate-tax button; desktop does not embed the engine (D-09, D-10)"
    - "On evaluate failure keep the last good TaxDecision and show the Copywriting tax error in the rail; typing stays enabled (D-11)"
    - "Invoice rail shows four canonical keys invoice_tax_rate, reverse_charge_flag, legal_reference, applied_rule_id; /tax shows all nine from the same result (D-12)"
    - "No tax override UI; tax.override unused (D-16)"
    - "After login open the last updatedAt invoice; if none, first-run empty form (D-17 replaces Phase 1 sample home)"
    - "PATCH /api/invoices/:id last-write-wins; GET /api/invoices lists drafts for the picker (INV-01, A9)"
    - "Line items are four editable fields; netto server-computed; no per-line tax (D-12, Phase 1 D-18)"
    - "E4 Rechnung empty (drafts exist): empty identity card + empty line list; picker keeps drafts; no illustration (UI-SPEC E4 empty, D-07)"
    - "E4 loading: Skeleton identity + three line-card placeholders; autosave Speichert with optional 16px Spinner; prefers-reduced-motion reduce means no spin (UI-SPEC E4 loading)"
    - "E4 error: load ErrorState + retry; autosave error copy; form stays; write missing disables Neue Rechnung with hint (UI-SPEC E4 error)"
    - "E4 populated: last edited draft; identity filled; compact 4-field cards + + Position (UI-SPEC E4 populated, D-17)"
    - "E4 partial: incomplete draft stays; number Wird vergeben until first autosave; blank new line card visible (UI-SPEC E4 partial)"
    - "E4 overflow: left pane scrolls; identity grid gap-4 (UI-SPEC E4 overflow)"
    - "E4 zero-one-many: zero lines after Neue Rechnung with drafts = empty list + + Position; one or many = cards (UI-SPEC E4 zero-one-many)"
    - "E4 long-text: Bezeichnung wraps; autosave/error labels wrap, no ellipsis (UI-SPEC E4 long-text)"
    - "E5 tax rail loading: last good or em dashes stay; optional Spinner in rail heading; no skeleton swap (UI-SPEC E5 loading)"
    - "E5 tax rail error: Copywriting tax error in rail and /tax; last good kept; Erneut versuchen text-link (UI-SPEC E5 error, D-11)"
    - "E5 overflow: rail scrolls if legal_reference exceeds the pane (UI-SPEC E5 overflow)"
    - "E5 long-text: legal text and error wrap then scroll; no ellipsis on errors (UI-SPEC E5 long-text)"
    - "E6 picker empty: Combobox empty Keine Rechnung passt zur Suche; zero invoices uses E7 (UI-SPEC E6 empty)"
    - "E6 picker loading: trigger stays; list may skeleton inside ComboboxContent (UI-SPEC E6 loading)"
    - "E6 picker error: draft list fail ErrorState in main pane; picker trigger remains (UI-SPEC E6 error)"
    - "E6 picker populated: trigger shows current number or Neue Rechnung if unnumbered; list of drafts (UI-SPEC E6 populated)"
    - "E6 picker partial: unnumbered new draft still appears as Neue Rechnung on the trigger (UI-SPEC E6 partial)"
    - "E6 picker overflow: Combobox list viewport scrolls; trigger truncates with native title = full string (UI-SPEC E6 overflow)"
    - "E6 picker zero-one-many: zero invoices = E7; one or many = picker lists them (UI-SPEC E6 zero-one-many)"
    - "E6 picker long-text: trigger truncates + title; list rows wrap or scroll (UI-SPEC E6 long-text)"
    - "E7 first-run empty: hero empty-state-hero.png + Copywriting empty heading/body; empty form under copy; no sample-restore control (UI-SPEC E7 empty, D-07, D-17)"
    - "E7 first-run loading: illustration is static; after first persistable keystroke hide hero and reveal rail (UI-SPEC E7 loading)"
    - "E7 first-run error: missing invoice.read uses ErrorState not marketing empty; image 404 keeps copy + form (UI-SPEC E7 error)"
    - "E7 first-run populated: not a populated list; leaving first-run is E4 after persist; hero alt empty (UI-SPEC E7 populated)"
    - "E7 first-run partial: empty identity + empty line list + + Position under copy (UI-SPEC E7 partial)"
    - "E7 first-run overflow: left pane max-w-xl; hero rounded-md; column scrolls (UI-SPEC E7 overflow)"
    - "E7 first-run zero-one-many: this surface is zero-invoice layout; one or more invoices never show the hero (UI-SPEC E7 zero-one-many)"
    - "E7 first-run long-text: empty body wraps in the column (UI-SPEC E7 long-text)"
    - "[EDGE explicit · INV-01 idempotency] POST always inserts a new invoice; PATCH is last-write-wins with no If-Match"
    - "[EDGE backstop · INV-01 ordering] InvoiceItem.position equals request array index; GET returns ORDER BY position ASC, id ASC"
    - "Without invoice.write: picker and fields read-only; autosave off; Neue Rechnung disabled with Keine Berechtigung zum Anlegen von Rechnungen.; tax rail still evaluates if tax.evaluate (UI-SPEC)"
    - "Demo Bereit/Laden/Fehler switcher is removed from Rechnung (UI-SPEC Shell)"
  artifacts:
    - "apps/backend/src/invoices/invoices.controller.ts PATCH and GET list"
    - "apps/desktop/src/routes/rechnung.tsx"
    - "apps/desktop/src/components/tax-rail.tsx"
    - "apps/desktop/src/routes/tax.tsx"
  key_links:
    - "One 600ms timeout in Rechnung useEffect cleanup fires both saveDraft and postEvaluate (D-05, D-09)"
    - "Evaluate skipped until seller and customer present; autosave still runs on partial drafts"
    - "Tax screen and tax rail share the last successful TaxDecision object"
  prohibitions:
    - statement: "MUST NOT present Clared as open source, free, or customer-self-hosted"
      status: flagged-unverified
    - statement: "MUST NOT embed the tax engine in the Tauri desktop or evaluate without a server round-trip"
      status: flagged-unverified
    - statement: "MUST NOT persist tax evaluations to audit_logs or issue final invoices this phase"
      status: flagged-unverified
---

<objective>
Invoice list/PATCH/numbering plus the live canvas: 600ms autosave, header picker, D-17 landing, first-run empty, document-level live tax on rail and /tax.

Purpose: Finish INV-01 and TAX-01 as the two-minute loop without PDF.
Output: PATCH+list APIs, wired Rechnung and Tax screens, rewritten desktop tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-entities-invoices-live-tax/03-CONTEXT.md
@.planning/phases/03-entities-invoices-live-tax/03-UI-SPEC.md
@.planning/phases/03-entities-invoices-live-tax/03-RESEARCH.md
@.planning/phases/03-entities-invoices-live-tax/03-PATTERNS.md
@apps/desktop/src/routes/rechnung.tsx
@apps/desktop/src/components/tax-rail.tsx
@apps/desktop/src/routes/tax.tsx
@apps/desktop/src/components/invoice-empty-state.tsx
@apps/desktop/src/components/line-item-card.tsx
@apps/backend/src/invoices
</context>

<interfaces>
Autosave status copy:
- Speichert…
- Gespeichert
- Speichern fehlgeschlagen. Eingaben bleiben sichtbar.

Tax error copy:
- Steuerberechnung fehlgeschlagen. Letzte gültige Werte bleiben sichtbar.
- Erneut versuchen (text-primary underline, not a filled CTA)

Invoice number placeholder: Wird vergeben…
GET /api/invoices sort: updatedAt descending (D-17 last edited)
PATCH whitelist: date, dueDate, entityId, customerId, currency, supplyType, items[] — never status, never number overwrite by client
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PATCH invoice, GET list, stable item order</name>
  <files>apps/backend/src/invoices/invoices.controller.ts, apps/backend/src/invoices/invoices.service.ts, apps/backend/test/invoices.e2e-spec.ts</files>
  <read_first>
    - apps/backend/src/invoices
    - apps/backend/test/invoices.e2e-spec.ts
    - .planning/phases/03-entities-invoices-live-tax/03-RESEARCH.md (Pattern 2 PATCH, A9, Pitfall 3)
  </read_first>
  <behavior>
    - GET /api/invoices with invoice.read returns an array sorted by updatedAt descending
    - PATCH /api/invoices/:id with invoice.write replaces items in one transaction (deleteMany + createMany) and updates updatedAt
    - PATCH cannot change number; extra whitelist keys are stripped
    - GET by id returns items ordered by position then id
    - Viewer with invoice.read can GET; viewer without invoice.write cannot PATCH (403)
  </behavior>
  <action>
    Add GET /api/invoices (invoice.read) and PATCH /api/invoices/:id (invoice.write). PATCH uses $transaction: invoiceItem.deleteMany by invoiceId, createMany from body.items with position = index, invoice.update of draft fields. Last-write-wins; no If-Match (INV-01 idempotency). Number remains server-assigned; strip number from PATCH DTO via whitelist (D-20). Do not add status or issued. GET list orders by updatedAt desc for D-17. GET by id include items orderBy position asc, id asc (INV-01 ordering backstop). 404 when id missing. Extend invoices.e2e-spec.ts. Currency changeable (D-20). supplyType default service if omitted (D-12).
  </action>
  <verify>
    <automated>pnpm --filter ./apps/backend test:e2e -- invoices</automated>
  </verify>
  <acceptance_criteria>
    - InvoicesController has GET collection and PATCH :id
    - pnpm --filter ./apps/backend test:e2e -- invoices exits 0
    - PATCH DTO class does not include a client-writable number field
  </acceptance_criteria>
  <done>Autosave and picker have list + update endpoints; item order is stable.</done>
</task>

<task type="auto">
  <name>Task 2: Rechnung autosave, header picker, D-17 landing, empty and line cards</name>
  <files>apps/desktop/src/routes/rechnung.tsx, apps/desktop/src/components/invoice-empty-state.tsx, apps/desktop/src/components/line-item-card.tsx</files>
  <read_first>
    - .planning/phases/03-entities-invoices-live-tax/03-UI-SPEC.md (Rechnung canvas, Copywriting, first-run vs Neue Rechnung)
    - apps/desktop/src/routes/rechnung.tsx
    - apps/desktop/src/components/invoice-empty-state.tsx
    - apps/desktop/src/components/line-item-card.tsx
    - apps/desktop/src/data/sample-invoice.ts
  </read_first>
  <action>
    Remove the three local demo-state switcher buttons and the demo state machine from Rechnung. Landing: GET /api/invoices, open highest updatedAt (D-17); zero invoices show first-run empty (hero at apps/desktop/public/empty-state-hero.png, alt empty, Copywriting empty heading/body). Reuse the Phase 1 empty-state PNG (discretion 5); do not call an image generator. If that PNG is missing, keep heading, body, and empty form — do not substitute the login-gate illustration.

    Header: Heading Rechnung + Combobox picker (D-08) showing current number or Neue Rechnung while unnumbered. Searchable; empty copy Keine Rechnung passt zur Suche. Not a nav item. Right cluster: autosave Label then Neue Rechnung. Neue Rechnung enabled iff invoice.write; opens empty identity + empty lines without copying the last draft and without restoring sample data (D-07). Disabled hint Keine Berechtigung zum Anlegen von Rechnungen.

    Autosave (D-05): 600ms setTimeout in useEffect cleanup — no lodash. POST when the draft has no id yet; PATCH thereafter. Status Speichert / Gespeichert / Speichern fehlgeschlagen. Eingaben bleiben sichtbar. Failure does not blank the form. prefers-reduced-motion reduce: Spinner does not spin. Without invoice.write: fields read-only, autosave off, status hidden.

    Identity card grid gap-4: Rechnungsnummer read-only Wird vergeben until server assigns; Datum; Fällig; Entity Combobox; Kunde Combobox disabled until Entity set (D-02); Währung Select defaulting from entity, options EUR USD AED plus entity default (D-20). Changing seller updates currency only if it still equals the previous entity default.

    LineItemCard: editable Input fields; Netto read-only computed; p-4; hover/focus X accessible name Position löschen, no confirm dialog. + Position px-4 py-2 at list bottom. No per-line tax. supplyType stays default service hidden (D-12).

    First-run: tax rail and PDF peek hidden while the illustration shows (Phase 1 E6). After first persistable keystroke hide hero and reveal rail. Neue Rechnung while drafts exist: no illustration; rail visible with em dashes until first successful evaluate.

    Keep PDF peek staged Link to /pdf. Do not add a sidebar history item.
  </action>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec tsc --noEmit --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - rechnung.tsx contains a 600ms timeout for persist
    - invoice-empty-state no longer offers a control that restores sample invoice data
    - line-item-card.tsx uses Input or input for bezeichnung, menge, einzelpreis
    - rechnung.tsx has no Demo Bereit label
    - pnpm --filter ./apps/desktop exec tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Operator can create a new draft, autosave it, and pick past drafts from the header.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Live tax debounce, last-good rail, /tax nine fields, rewrite desktop tests</name>
  <files>apps/desktop/src/components/tax-rail.tsx, apps/desktop/src/routes/tax.tsx, apps/desktop/src/routes/rechnung.tsx, apps/desktop/src/__tests__/invoice.test.tsx, apps/desktop/src/__tests__/demo-states.test.tsx, apps/desktop/src/__tests__/screens.test.tsx, apps/desktop/src/__tests__/phase03-tax-rail.test.tsx</files>
  <read_first>
    - apps/desktop/src/components/tax-rail.tsx
    - apps/desktop/src/routes/tax.tsx
    - apps/desktop/src/__tests__/invoice.test.tsx
    - apps/desktop/src/__tests__/demo-states.test.tsx
    - .planning/phases/03-entities-invoices-live-tax/03-UI-SPEC.md (Live tax, Copywriting)
  </read_first>
  <behavior>
    - Shared 600ms pause calls evaluate with the invoice-shaped draft, not an invoice id
    - First successful TaxDecision fills rail four keys and /tax nine keys
    - 422/5xx keeps previous four values and shows the tax-error copy plus Erneut versuchen
    - Signed-in Rechnung does not display SAMPLE_INVOICE tax numbers on boot
  </behavior>
  <action>
    Share the 600ms timeout with autosave (D-09). POST /api/tax/evaluate with invoice-shaped body (D-18). Skip evaluate until seller and customer are present; still autosave. Before first success, values are em dash. On failure keep last good; small error in the rail and the same line on /tax; do not use full-page ErrorState for evaluate failure (that would block typing). Optional Erneut versuchen text-primary underline refires immediately. Canonical dt keys stay English (D-12). /tax TAX_FIELDS nine keys from the same state. Render String(tax[field]) text nodes. No override controls (D-16). Viewer with tax.evaluate still evaluates when invoice.write is missing.

    Rewrite invoice.test.tsx, screens.test.tsx tax cases, and demo-states.test.tsx: landing is last draft or empty, not sample tax on boot; delete Demo switcher cases; remove sample-restore assertions. Unsip phase03-tax-rail.test.tsx. Keep data-testid hooks. SAMPLE_INVOICE remains importable as a fixture only.
  </action>
  <verify>
    <automated>pnpm --filter ./apps/desktop test -- src/__tests__/phase03-tax-rail.test.tsx && pnpm --filter ./apps/desktop test -- src/__tests__/invoice.test.tsx && pnpm --filter ./apps/desktop test -- src/__tests__/demo-states.test.tsx && pnpm --filter ./apps/desktop test -- src/__tests__/screens.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --filter ./apps/desktop test -- src/__tests__/phase03-tax-rail.test.tsx exits 0
    - pnpm --filter ./apps/desktop test -- src/__tests__/invoice.test.tsx exits 0
    - pnpm --filter ./apps/desktop test -- src/__tests__/demo-states.test.tsx exits 0
    - tax-rail.tsx still uses dt keys invoice_tax_rate, reverse_charge_flag, legal_reference, applied_rule_id
    - tax.tsx reads the same evaluate result object as the rail
    - describe.skip('phase03-product') is absent from phase03-tax-rail.test.tsx
  </acceptance_criteria>
  <done>Live tax rail and /tax update from server evaluate; last good survives errors; sample tax is gone from the signed-in path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Desktop draft JSON → PATCH /api/invoices/:id | Untrusted; whitelist draft fields only |
| Desktop draft JSON → POST /api/tax/evaluate | Must not accept a TaxDecision from the client |
| Invoice picker / last-edited | invoice.read required; missing permission is ErrorState |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Tampering | PATCH /api/invoices/:id | high | mitigate | class-validator whitelist; forbid number and issued/status; PermissionsGuard invoice.write |
| T-03-03 | Tampering | live evaluate from canvas | high | mitigate | Invoice-shaped body; server evaluate; no override UI (D-16) |
| T-03-07 | Tampering | legal_reference / invoice_text_block_id | medium | mitigate | React text nodes only |
| T-03-11 | Denial of service | 600ms autosave+evaluate loop | low | accept | Debounce 600ms; Prisma pool 10 from 03-02; no extra rate limit this phase |
| T-03-05 | Information disclosure | GET /api/invoices list | medium | accept | A4 instance-global drafts; SAAS-01 deferred |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages this plan |
</threat_model>

<assumption_delta_decision>
noun: shared instance / second identity source
decision: no-change
rationale: Invoice rows remain instance-global (A4). Last-edited landing is updatedAt across the instance, not a per-tenant cursor. Authentik still sole identity SoT.
</assumption_delta_decision>

<verification>
pnpm --filter ./apps/backend test:e2e -- invoices
pnpm --filter ./apps/desktop test -- src/__tests__/invoice.test.tsx
pnpm --filter ./apps/desktop test -- src/__tests__/phase03-tax-rail.test.tsx
pnpm --filter ./apps/desktop test -- src/__tests__/demo-states.test.tsx
</verification>

<success_criteria>
User creates a draft that autosaves, picks it from the header, and sees live tax from the server. Evaluate failure keeps last good values. Signed-in home is last edited or empty, not the Phase 1 sample.
</success_criteria>

## Artifacts this phase produces

This plan:
- `InvoicesController` GET list + PATCH
- Rechnung autosave + header picker + D-17 landing
- Shared 600ms evaluate
- TaxRail last-good + `/tax` live nine fields
- Rewritten invoice/demo/screens Vitest files

<output>
Create `.planning/phases/03-entities-invoices-live-tax/03-05-SUMMARY.md` when done
</output>
