---
phase: 03-entities-invoices-live-tax
plan: 06
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - packages/ui/src
  - apps/desktop/src/auth/api.ts
  - apps/desktop/src/auth/session-provider.tsx
  - apps/desktop/src/routes/entities.tsx
  - apps/desktop/src/routes/rechnung.tsx
  - apps/desktop/src/components/tax-rail.tsx
autonomous: true
requirements: [ENT-01, INV-01, TAX-01]
user_setup: []

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "apiFetch sends Authorization Bearer from setSessionToken called in applySession (D-10 desktop gap)"
    - "Owner with entity.create can POST one entity from the Entities list+panel (D-01, D-04); Rechnung can POST one draft and TaxRail shows four canonical keys from POST /api/tax/evaluate (INV-01, TAX-01, D-12)"
    - "Desktop never imports @clared/tax-engine; evaluate is server-only (D-10, D-15)"
    - "On evaluate 422 the rail keeps last good values or em dash (D-11)"
    - "E1 nav: local routes, no remote load chrome; missing invoice.read uses Phase 1 ErrorState in main pane with nav still shown; five locked labels; no overflow (UI-SPEC E1)"
    - "E8 session: Phase 2 silent-boot Spinner; first-run empty form is OK this slice (full last-edited landing is 03-05, D-17); auth error chrome unchanged (UI-SPEC E8)"
  artifacts:
    - "apps/desktop/src/auth/api.ts (setSessionToken)"
    - "packages/ui/src/components/combobox.tsx"
    - "apps/desktop/src/routes/entities.tsx"
    - "apps/desktop/src/routes/rechnung.tsx"
    - "apps/desktop/src/components/tax-rail.tsx"
  key_links:
    - "setSessionToken in api.ts wired from session-provider applySession and cleared on logout"
    - "Entities panel POST /api/entities; Rechnung POST /api/invoices; TaxRail POST /api/tax/evaluate invoice-shaped body (D-18)"
    - "Header picker, PATCH, 600ms autosave expansion are 03-05; country catalog is 03-03"
  prohibitions:
    - statement: "MUST NOT present Clared as open source, free, or customer-self-hosted"
      status: flagged-unverified
    - statement: "MUST NOT embed the tax engine in the Tauri desktop or evaluate without a server round-trip"
      status: flagged-unverified
    - statement: "MUST NOT add Dialog, Sheet, toast, or Button CVA variants"
      status: flagged-unverified
---

<objective>
Desktop tracer: attach Bearer on product apiFetch, add shadcn Input/Label/Select/Combobox, and wire one owner path — create entity in the list panel, persist one draft invoice, show live tax rail from POST /api/tax/evaluate.

Purpose: Prove the desktop architecture (session token, no embedded engine, list+panel create) after 03-02 HTTP exists. Catalogs, 23 rules, autosave picker stay later plans.
Output: Bearer product fetch + one-path Entities/Rechnung/TaxRail UI.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-entities-invoices-live-tax/03-CONTEXT.md
@.planning/phases/03-entities-invoices-live-tax/03-RESEARCH.md
@.planning/phases/03-entities-invoices-live-tax/03-PATTERNS.md
@.planning/phases/03-entities-invoices-live-tax/03-UI-SPEC.md
@.planning/phases/03-entities-invoices-live-tax/03-02-PLAN.md
@apps/desktop/src/auth/api.ts
@apps/desktop/src/auth/session-provider.tsx
@apps/desktop/src/routes/entities.tsx
@apps/desktop/src/routes/rechnung.tsx
@apps/desktop/src/components/tax-rail.tsx
@packages/ui/components.json
</context>

<interfaces>
Bearer (same pattern as setOnUnauthorized):
- setSessionToken(token: string | null) module-level in api.ts
- apiFetch attaches Authorization Bearer when token is present
- applySession and logout call set and clear
- token is not a field on SessionContextValue

shadcn (radix-nova, -c packages/ui):
- Input, Label, Select, Combobox (Combobox, ComboboxInput, ComboboxContent, ComboboxEmpty, ComboboxList, ComboboxItem)
- Export from packages/ui/src/index.ts

TaxRail dt keys (D-12):
- invoice_tax_rate, reverse_charge_flag, legal_reference, applied_rule_id
</interfaces>

<tasks>

<task type="tracer">
  <name>End-to-end "Bearer entity create, draft invoice, live tax rail" — one desktop path only</name>
  <reversibility rating="costly">D-01 list+panel create and D-10 server-only evaluate lock the desktop seams 03-03/03-05 expand.</reversibility>
  <files>apps/desktop/src/auth/api.ts, apps/desktop/src/auth/session-provider.tsx, packages/ui/src/index.ts, apps/desktop/src/routes/entities.tsx, apps/desktop/src/routes/rechnung.tsx, apps/desktop/src/components/tax-rail.tsx</files>
  <read_first>
    - .planning/phases/03-entities-invoices-live-tax/03-RESEARCH.md (Pattern 5 Bearer gap, Pattern 7 Combobox)
    - .planning/phases/03-entities-invoices-live-tax/03-UI-SPEC.md (Entities list+panel, tax rail)
    - apps/desktop/src/auth/api.ts
    - apps/desktop/src/auth/session-provider.tsx
    - apps/desktop/src/routes/entities.tsx
    - apps/desktop/src/routes/rechnung.tsx
    - apps/desktop/src/components/tax-rail.tsx
    - packages/ui/components.json
  </read_first>
  <action>
    Discretion locked: debounce 600ms both (UI-SPEC) once autosave exists in 03-05; this slice may POST on Anlegen / first persist without the shared timer. Evaluate invoice-shaped body only (D-18). Reuse empty-state-hero.png. No header picker yet (03-05, D-08). Country→legal-form catalog is 03-03; tracer entity fields may be plain Input/Select with AT + GmbH typed for the one path.

    Bearer: setSessionToken module-level in api.ts (same pattern as setOnUnauthorized). apiFetch attaches Bearer when token present. applySession and logout in session-provider call set and clear. Do not put token on SessionContextValue.

    npx shadcn add input label select combobox -c packages/ui (radix-nova already). Export from packages/ui/src/index.ts. Do not add Dialog, Sheet, toast, or Button CVA variants.

    Tracer UI one path only (D-01): Entities screen — enabled Anlegen when entity.create opens the writable create form in the existing detail Card slot (not a dialog, not a new route). Persist via POST /api/entities. Rechnung: first-run empty form is OK (D-17 stub); owner with invoice.write can POST a draft that includes the new entityId and a customerId from POST /api/customers (Kunden may stay mostly mock this slice; a minimal Entity combobox or API create from the entity panel is enough). TaxRail reads POST /api/tax/evaluate result for the four canonical dt keys (D-12). On 422 keep last good or em dash (D-11). Do not embed evaluate in the desktop bundle. Do not remove PDF peek. Sample invoice remains a test fixture, not the signed-in landing if the owner just created a draft.

    Keep Phase 1 nav labels and ErrorState-in-main-pane when invoice.read is missing (UI-SPEC E1). Do not change silent-boot Spinner (E8).
  </action>
  <verify>
    <automated>pnpm --filter ./apps/desktop exec tsc --noEmit --pretty false && grep -v '^#' apps/desktop/src/auth/api.ts | grep -c setSessionToken</automated>
  </verify>
  <acceptance_criteria>
    - apps/desktop/src/auth/api.ts contains setSessionToken
    - apiFetch attaches Authorization when a token is set
    - entities.tsx Anlegen remains in the header and posts to /api/entities (no /entities/new route)
    - tax-rail.tsx still uses dt keys invoice_tax_rate, reverse_charge_flag, legal_reference, applied_rule_id
    - pnpm --filter ./apps/desktop exec tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Owner can create one entity from the panel, persist one draft, and see rail fields from server evaluate. Bearer is attached.</done>
</task>

<task type="auto">
  <name>Task 2: Prove desktop does not import the tax-engine package</name>
  <files>apps/desktop/package.json, apps/desktop/src</files>
  <read_first>
    - apps/desktop/package.json
    - packages/tax-engine/package.json
  </read_first>
  <action>
    Confirm apps/desktop/package.json has no @clared/tax-engine dependency and no desktop source file imports that package (D-10, D-15). Product tax calls go only through apiFetch POST /api/tax/evaluate. Leave phase03-* Vitest files skipped; 03-03 and 03-05 unsip them.
  </action>
  <verify>
    <automated>grep -R '@clared/tax-engine' apps/desktop --include='*.ts' --include='*.tsx' --include='package.json' | wc -l | awk '{exit ($1==0)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - grep -R '@clared/tax-engine' under apps/desktop returns no matches
    - Desktop still depends on @clared/ui not @clared/tax-engine
  </acceptance_criteria>
  <done>Desktop evaluate path is HTTP-only; engine stays in packages/tax-engine consumed by Nest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Desktop WebView → Nest product routes | Bearer session; untrusted JSON bodies |
| Desktop bundle → tax engine | Must not exist; evaluate is HTTP only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering | live evaluate from canvas | high | mitigate | Invoice-shaped body; server evaluate; no TaxDecision submitted from desktop (D-18) |
| T-03-07 | Tampering | tax-rail legal_reference | medium | mitigate | React text nodes String(tax[field]); no dangerouslySetInnerHTML |
| T-03-01 | Elevation of privilege | Anlegen button | high | mitigate | UI disable is not sufficient; server PermissionsGuard from 03-02 remains source of truth (D-04) |
| T-03-05 | Information disclosure | instance-global rows | medium | accept | RESEARCH A4: no tenant_id this phase; SAAS-01 deferred |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new registry packages this plan; shadcn copies files into packages/ui |
</threat_model>

<assumption_delta_decision>
noun: shared instance / second identity source
decision: no-change
rationale: Desktop uses the Phase 2 Authentik session token. No second identity SoT. Product rows remain instance-global (A4).
</assumption_delta_decision>

<verification>
pnpm --filter ./apps/desktop exec tsc --noEmit --pretty false
grep setSessionToken apps/desktop/src/auth/api.ts
grep -R @clared/tax-engine apps/desktop (expect none)
</verification>

<success_criteria>
apiFetch sends Bearer. Owner one-path UI can create an entity, persist a draft, and show rail fields from server evaluate. Desktop does not import @clared/tax-engine.
</success_criteria>

## Artifacts this phase produces

This plan:
- `setSessionToken` / Bearer `apiFetch`
- shadcn Input, Label, Select, Combobox on @clared/ui
- One-path Entities panel + Rechnung POST + TaxRail evaluate

<output>
Create `.planning/phases/03-entities-invoices-live-tax/03-06-SUMMARY.md` when done
</output>
