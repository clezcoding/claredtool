---
phase: 03-entities-invoices-live-tax
plan: 04
type: tdd
wave: 3
depends_on: ["03-02"]
files_modified:
  - packages/tax-engine/rules
  - packages/tax-engine/src/evaluate.spec.ts
  - packages/tax-engine/src/match.ts
  - packages/tax-engine/scripts
  - .github/workflows/ci.yml
autonomous: true
requirements: [TAX-02]
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every rule class in docs/clared-tax-rule-matrix.md has one TaxRule JSON file and one evaluate test that expects that applied_rule_id (TAX-02, D-13)"
    - "There are exactly 23 rule JSON files; none for CH or UK classes (D-13, TAX-04 deferred)"
    - "evaluate throws EvaluateError no_unique_match when 0 rules match and when 2 rules match; it does not sort by priority (D-13, A10)"
    - "Each JSON validates against docs/clared-tax-rule-dsl-schema.json via Ajv2020 (D-14)"
    - "Ambiguous matrix cells use one concrete exemplar (AT 20, DE 19, UAE 5, reverse-charge 0) never a runtime union (RESEARCH Pattern 4)"
    - "[EDGE explicit · TAX-02 unclassified] Match count not 1 is an error, not a silent pick"
  artifacts:
    - "packages/tax-engine/rules/*.json (23 files)"
    - "packages/tax-engine/src/evaluate.spec.ts"
  key_links:
    - "RuleStore loadRules validates every file; Nest rule-seed from 03-02 already globs rules/*.json so 23 files load on next boot (D-14)"
    - "Conditions are disjoint enough that each of the 23 fixtures matches exactly one file"
  prohibitions:
    - statement: "MUST NOT invent a collision or priority-tie winner when more than one TaxRule matches"
      status: flagged-unverified
    - statement: "MUST NOT add CH or UK rule modules that are not rows in docs/clared-tax-rule-matrix.md"
      status: flagged-unverified
    - statement: "MUST NOT present Clared as open source, free, or customer-self-hosted"
      status: flagged-unverified
---

<objective>
Fill the tax-engine RuleStore with all 23 matrix classes as JSON + expected TaxDecision tests, generated from one table so files stay disjoint.

Purpose: TAX-02 content completeness (D-13) without inventing a priority-tie algorithm.
Output: 23 Ajv-valid JSON rules, green evaluate.spec.ts, CI runs the package tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-entities-invoices-live-tax/03-CONTEXT.md
@.planning/phases/03-entities-invoices-live-tax/03-RESEARCH.md
@docs/clared-tax-rule-matrix.md
@docs/clared-tax-rule-dsl-schema.json
@packages/tax-engine/src/evaluate.spec.ts
@packages/tax-engine/rules/EU_INTRACOMM_B2B_SERVICE.json
</context>

<interfaces>
Concrete exemplars (one row per matrix id). Facts use ISO countries; rates are numbers; reverse_charge_flag boolean.

id | supplier | customer | supply_type | customer_is_business | rate | reverse | liability
EU_DOMESTIC_B2B_SERVICE | AT | AT | service | true | 20 | false | supplier
EU_DOMESTIC_B2C_SERVICE | AT | AT | service | false | 20 | false | supplier
EU_INTRACOMM_B2B_SERVICE | AT | DE | service | true | 0 | true | customer
EU_INTRACOMM_B2C_DIGITAL | AT | DE | digital_service | false | 19 | false | supplier
EU_EXPORT_SERVICE_TO_THIRD | AT | JP | service | true | 0 | false | supplier
EU_EXPORT_GOODS_TO_THIRD | AT | US | goods | true | 0 | false | supplier
EU_TO_EU_B2B_GOODS_INTRACOMM | AT | DE | goods | true | 0 | true | customer
EU_TO_EU_B2C_GOODS_INTRACOMM | AT | DE | goods | false | 19 | false | supplier
THIRD_TO_EU_B2B_SERVICE | US | AT | service | true | 0 | true | customer
THIRD_TO_EU_B2C_DIGITAL | US | AT | digital_service | false | 20 | false | supplier
US_TO_EU_B2B_SERVICE | US | DE | service | true | 0 | true | customer
US_TO_EU_B2C_DIGITAL | US | DE | digital_service | false | 19 | false | supplier
EU_TO_US_B2B_SERVICE | AT | US | service | true | 0 | false | supplier
EU_TO_US_B2C_DIGITAL | AT | US | digital_service | false | 0 | false | supplier
UAE_TO_EU_B2B_SERVICE | AE | DE | service | true | 0 | true | customer
UAE_TO_EU_B2B_DIGITAL | AE | DE | digital_service | true | 0 | true | customer
UAE_TO_EU_B2C_DIGITAL | AE | DE | digital_service | false | 19 | false | supplier
EU_TO_UAE_B2B_SERVICE | AT | AE | service | true | 5 | true | customer
EU_TO_UAE_B2B_DIGITAL | AT | AE | digital_service | true | 5 | true | customer
EU_TO_UAE_B2C_DIGITAL | AT | AE | digital_service | false | 5 | false | supplier
DOMESTIC_SPECIAL_PROPERTY | AT | AT | property | true | 20 | false | supplier
DOMESTIC_SPECIAL_EVENT | AT | AT | special | true | 20 | false | supplier
THIRD_TO_THIRD_B2B_SERVICE | US | AE | service | true | 0 | false | supplier

VAT-registered flags follow the business flags except B2C customer_vat_registered false. supplier_is_business always true in these fixtures.

Disjointness (exactly-one-match-or-throw): every row’s (supplier_country, customer_country, supply_type, customer_is_business) tuple is unique. EU_EXPORT_SERVICE_TO_THIRD uses JP (not US, not AE) so it does not share AT/US/service/B2B with EU_TO_US_B2B_SERVICE. THIRD_TO_EU_* uses customer AT; US_TO_EU_* uses customer DE. Generator must fail if any two rows collide on that tuple.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate 23 TaxRule JSON files from the exemplar table</name>
  <files>packages/tax-engine/scripts/generate-rules.mjs, packages/tax-engine/rules</files>
  <read_first>
    - docs/clared-tax-rule-dsl-schema.json
    - docs/clared-tax-rule-matrix.md
    - packages/tax-engine/rules/EU_INTRACOMM_B2B_SERVICE.json
    - packages/tax-engine/src/store.ts
    - packages/tax-engine/src/match.ts
  </read_first>
  <behavior>
    - loadRules on the rules directory returns 23 Ajv-valid objects
    - Each file rule_id equals its filename stem
    - No file named with CH or GB as a rule class
  </behavior>
  <action>
    Do not hand-type 23 near-duplicate JSON files. Write packages/tax-engine/scripts/generate-rules.mjs that emits one JSON per row of the exemplar table above into packages/tax-engine/rules/{id}.json. Each file must satisfy docs/clared-tax-rule-dsl-schema.json required keys rule_id, version, conditions, effect. Use additionalProperties false — do not emit unknown keys. Conditions must be disjoint so each fixture matches exactly one rule (equality on supplier_country, customer_country, supply_type, customer_is_business / vat flags). The generator asserts that no two table rows share the same (supplier, customer, supply_type, customer_is_business) tuple — EU_EXPORT_SERVICE_TO_THIRD is AT/JP/service/B2B, never AT/US. Effect uses the concrete rate, reverse_charge_flag, tax_liability_party, legal_reference string from the matrix legal_reference column (copy from docs/clared-tax-rule-matrix.md), invoice_text_block_id from that table, invoice_tax_shown complementary to reverse where the matrix is unambiguous. version 1.0.0. priority may be stored but match.ts must not use it (D-13). Overwrite the tracer EU_INTRACOMM_B2B_SERVICE.json with the generated file so it stays equivalent. Run the generator. If match.ts cannot express a condition, extend matches() with equality on the TransactionFacts fields already in the architecture doc — do not add a scoring function.
  </action>
  <verify>
    <automated>node packages/tax-engine/scripts/generate-rules.mjs && test $(ls packages/tax-engine/rules/*.json | wc -l | tr -d ' ') -eq 23</automated>
  </verify>
  <acceptance_criteria>
    - packages/tax-engine/rules contains exactly 23 json files
    - Every stem matches a matrix id from docs/clared-tax-rule-matrix.md
    - generate-rules.mjs exists and is the author of those files
  </acceptance_criteria>
  <done>23 disjoint TaxRule JSON files exist and are schema-shaped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Green evaluate.spec 23 classes plus 0-match and 2-match throws</name>
  <files>packages/tax-engine/src/evaluate.spec.ts, packages/tax-engine/src/match.ts, .github/workflows/ci.yml</files>
  <read_first>
    - packages/tax-engine/src/evaluate.spec.ts
    - packages/tax-engine/src/index.ts
    - .github/workflows/ci.yml
  </read_first>
  <behavior>
    - For each of 23 ids, evaluate(fixtureFacts) returns applied_rule_id equal to that id and invoice_tax_rate equal to the table rate
    - Facts that match nothing throw EvaluateError no_unique_match
    - Two overlapping rules in a temp dir throw EvaluateError no_unique_match (do not pick first)
  </behavior>
  <action>
    Expand evaluate.spec.ts: one it per matrix id using the exemplar facts. Expected TaxDecision fields are literals from the table (not recomputed by calling matches). Add 0-match using a country pair no rule covers (for example IS to IS consumer goods) and 2-match by loading a temp directory that contains two copies of the same conditions. Keep evaluate a pure function. Add pnpm --filter @clared/tax-engine test to .github/workflows/ci.yml after backend unit tests. Do not add CH/UK tests.
  </action>
  <verify>
    <automated>pnpm --filter @clared/tax-engine test</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --filter @clared/tax-engine test exits 0
    - evaluate.spec.ts contains all 23 matrix ids
    - evaluate.spec.ts asserts throw on 0 matches and on 2 matches
    - .github/workflows/ci.yml runs pnpm --filter @clared/tax-engine test
  </acceptance_criteria>
  <done>TAX-02 matrix content is tested; CI runs the engine package.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo JSON files → evaluate | Trusted authors in git; Ajv still validates |
| evaluate callers | Pure function; no HTTP in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | rules/*.json | high | mitigate | Ajv2020 vs SSOT schema; generator emits only schema keys; files remain git-reviewed SSOT (D-14) |
| T-03-10 | Elevation of privilege | match.ts priority | high | mitigate | Tests prove 2-match throws; grep/read match.ts to confirm priority is not consulted |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages this plan |
</threat_model>

<assumption_delta_decision>
noun: shared instance / second identity source
decision: no-change
rationale: Engine is process-local and stateless. No tenant dimension on rules. Authentik unchanged.
</assumption_delta_decision>

<verification>
pnpm --filter @clared/tax-engine test
test $(ls packages/tax-engine/rules/*.json | wc -l | tr -d ' ') -eq 23
</verification>

<success_criteria>
23 matrix classes each have JSON + a passing TaxDecision fixture. 0-match and 2-match throw. CI runs the package.
</success_criteria>

## Artifacts this phase produces

This plan:
- `packages/tax-engine/scripts/generate-rules.mjs`
- 23 `packages/tax-engine/rules/{MATRIX_ID}.json`
- Green `evaluate.spec.ts` (23 + 0 + 2)

<output>
Create `.planning/phases/03-entities-invoices-live-tax/03-04-SUMMARY.md` when done
</output>
