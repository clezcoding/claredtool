---
phase: 03-entities-invoices-live-tax
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/tax-engine/package.json
  - packages/tax-engine/tsconfig.json
  - packages/tax-engine/jest.config.cjs
  - packages/tax-engine/src/evaluate.spec.ts
  - apps/backend/test/entities.e2e-spec.ts
  - apps/backend/test/invoices.e2e-spec.ts
  - apps/backend/test/tax.e2e-spec.ts
  - apps/backend/src/auth/permissions.guard.spec.ts
  - apps/desktop/src/__tests__
autonomous: true
requirements: [ENT-01, INV-01, TAX-01, TAX-02]
user_setup: []

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Nyquist Wave 0 files exist for ENT-01, INV-01, TAX-01, TAX-02 per 03-VALIDATION.md (D-04, D-10, D-13, D-15)"
    - "Backend product e2e specs are written with real assertions and wrapped in describe.skip('phase03-product') so existing CI e2e stays green until 03-02 unsips them"
    - "Owner POST /api/entities is specified to return 201; viewer session is specified to return 403 (ENT-01, D-04)"
    - "POST /api/invoices with line items and GET /api/invoices/:id returning those items is specified (INV-01)"
    - "POST /api/tax/evaluate with an invoice-shaped body is specified to return canonical TaxDecision fields (TAX-01, D-12, D-18)"
    - "@clared/tax-engine Jest spec exists for evaluate(facts) and is RED when run (TAX-02, D-15)"
    - "Desktop Vitest files exist for disabled Anlegen without entity.create, tax-rail keep-last-good, and autosave Speichert/Gespeichert status; they use describe.skip('phase03-product') (ENT-01, TAX-01, INV-01, D-04, D-05, D-11)"
    - "signedInOwner.permissions includes entity.create, kunde.write, invoice.write, invoice.read, tax.evaluate so later owner UI tests can enable Anlegen (D-04)"
  artifacts:
    - "apps/backend/test/entities.e2e-spec.ts"
    - "apps/backend/test/invoices.e2e-spec.ts"
    - "apps/backend/test/tax.e2e-spec.ts"
    - "apps/backend/src/auth/permissions.guard.spec.ts"
    - "packages/tax-engine/package.json"
    - "packages/tax-engine/src/evaluate.spec.ts"
    - "apps/desktop/src/__tests__/phase03-entities.test.tsx"
    - "apps/desktop/src/__tests__/phase03-tax-rail.test.tsx"
    - "apps/desktop/src/__tests__/phase03-autosave.test.tsx"
  key_links:
    - "describe.skip('phase03-product') is the unsip signal for 03-02 (backend e2e + guard spec), 03-03 (entities desktop), and 03-05 (tax-rail + autosave desktop)"
    - "tax-engine package is CJS (no type:module); 03-01 does not add ajv and does not export evaluate yet — spec is RED"
    - "Existing auth.e2e-spec.ts unauthenticated GET /api/invoices 401 stays collected and green (Phase 2 D-10)"
  prohibitions:
    - statement: "MUST NOT present Clared as open source, free, or customer-self-hosted"
      status: flagged-unverified
    - statement: "MUST NOT implement production entity, invoice, or tax HTTP handlers in this plan"
      status: flagged-unverified
    - statement: "MUST NOT invent a collision or priority-tie winner in the evaluate spec — 0 or many matches must throw"
      status: flagged-unverified
  flagged_assumptions: []
---

<objective>
Nyquist Wave 0: write the RED (or skip-wrapped) public-seam specs for entities, invoices, tax evaluate, tax-engine evaluate, PermissionsGuard, and desktop Anlegen/tax-rail before production handlers. Fill owner fixture permissions.

Purpose: 03-02 tracer and later expansion plans turn a written specification green instead of inventing assertions after the fact.
Output: tax-engine Jest package (RED evaluate spec) + skipped Nest e2e/guard specs + skipped desktop specs + owner permissions on signedInOwner.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-entities-invoices-live-tax/03-CONTEXT.md
@.planning/phases/03-entities-invoices-live-tax/03-RESEARCH.md
@.planning/phases/03-entities-invoices-live-tax/03-PATTERNS.md
@.planning/phases/03-entities-invoices-live-tax/03-VALIDATION.md
@.planning/phases/03-entities-invoices-live-tax/03-UI-SPEC.md
@apps/backend/test/auth.e2e-spec.ts
@apps/backend/src/auth/rbac.spec.ts
@apps/desktop/src/__tests__/auth-signed-in.ts
@packages/ui/package.json
</context>

<interfaces>
From apps/backend/test/auth.e2e-spec.ts (copy bootstrap):
- AUTH_TEST_MODE=1
- DATABASE_URL fallback postgresql://prisma-test:unused@127.0.0.1:5432/clared
- Seed owner ticket with groups ["clared-owner"]; viewer with ["clared-viewer"]
- POST /auth/session then Authorization Bearer on product routes
- Unauthenticated GET /api/invoices already expects 401 — do not remove

From apps/desktop/src/data/sample-invoice.ts StagedTaxDecision keys (canonical):
- place_of_supply_country, tax_liability_party, invoice_tax_rate, invoice_tax_shown, reverse_charge_flag, legal_reference, invoice_text_block_id, applied_rule_id, applied_rule_version

From 03-RESEARCH Pattern 4:
- export function evaluate(facts: TransactionFacts): TaxDecision
- EvaluateError("no_unique_match") when match count is not 1
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Skip-wrapped Nest e2e + PermissionsGuard RED specs</name>
  <files>apps/backend/test/entities.e2e-spec.ts, apps/backend/test/invoices.e2e-spec.ts, apps/backend/test/tax.e2e-spec.ts, apps/backend/src/auth/permissions.guard.spec.ts</files>
  <read_first>
    - .planning/phases/03-entities-invoices-live-tax/03-VALIDATION.md (Wave 0 rows 03-W0-01 through 03-W0-03)
    - .planning/phases/03-entities-invoices-live-tax/03-RESEARCH.md (Validation Architecture, Pattern 1 permissions)
    - apps/backend/test/auth.e2e-spec.ts
    - apps/backend/src/auth/rbac.spec.ts
    - apps/backend/src/auth/rbac.ts
  </read_first>
  <behavior>
    - Owner Bearer POST /api/entities with name, country AT, legalForm GmbH, address returns 201 and a body id
    - Viewer Bearer POST /api/entities returns 403
    - Unauthenticated GET /api/entities returns 401
    - Owner POST /api/invoices with entityId, currency EUR, items [{bezeichnung, menge, einzelpreis}] returns 201 including items; GET /api/invoices/:id returns the same items
    - Owner POST /api/tax/evaluate with invoice-shaped JSON (entity + customer identity + items, not TransactionFacts keys) returns 200 with invoice_tax_rate, reverse_charge_flag, legal_reference, applied_rule_id
    - PermissionsGuard returns false when request.user.permissions lacks the required catalog string; skips when metadata is missing
  </behavior>
  <action>
    Copy the AppModule bootstrap, AUTH_TEST_MODE, ticket seed, and 15s afterAll close from auth.e2e-spec.ts into three new e2e files. Wrap every describe in this plan's new backend files with describe.skip('phase03-product') so pnpm --filter ./apps/backend test:e2e stays green on GitHub CI until 03-02 removes the skip. Do not edit auth.e2e-spec.ts.

    entities.e2e-spec.ts (ENT-01, D-04): owner group clared-owner POST /api/entities 201; viewer clared-viewer 403; missing Authorization 401. Body uses class-validator fields name, country, legalForm, address (D-19). Two identical owner POSTs are two 201s with different ids (ENT-01 idempotency backstop — UUID primary keys, no unique on name).

    invoices.e2e-spec.ts (INV-01): owner POST nested items then GET by id returns those items. Unauthenticated GET /api/invoices/:id 401 (catch-all or AuthGuard). Empty items array still 201 (INV-01 empty). Do not assert issued or Stellen.

    tax.e2e-spec.ts (TAX-01, D-18): POST /api/tax/evaluate invoice-shaped body (seller/customer countries and VAT flags as invoice fields, not engine internals). Expect 200 canonical D-12 keys. A second it() with a body that matches no rule expects 422 (TAX-02 unclassified / A10 — not a priority pick).

    permissions.guard.spec.ts next to rbac.spec.ts: unit-test the intended RequirePermission metadata + guard false → Nest 403 contract using a mock ExecutionContext. Wrap in the same describe.skip('phase03-product') so pnpm --filter ./apps/backend test stays green until 03-02 adds PermissionsGuard. Do not register APP_GUARD in this plan.
  </action>
  <verify>
    <automated>pnpm --filter ./apps/backend test:e2e && pnpm --filter ./apps/backend test -- rbac.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/backend/test/entities.e2e-spec.ts exists and contains describe.skip('phase03-product')
    - apps/backend/test/invoices.e2e-spec.ts exists and contains describe.skip('phase03-product')
    - apps/backend/test/tax.e2e-spec.ts exists and contains describe.skip('phase03-product')
    - apps/backend/src/auth/permissions.guard.spec.ts exists and contains describe.skip('phase03-product')
    - grep -v '^#' apps/backend/test/entities.e2e-spec.ts | grep -c 'entity.create' is greater than 0 or the file documents owner vs viewer 403
    - pnpm --filter ./apps/backend test:e2e exits 0
    - pnpm --filter ./apps/backend test -- rbac.spec.ts exits 0
  </acceptance_criteria>
  <done>Wave 0 Nest specs exist; CI e2e and unit suites still pass because new describes are skipped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold @clared/tax-engine RED spec + desktop skip-wrapped UI specs</name>
  <reversibility rating="costly">D-15 package name and CJS main lock the Nest import seam for later TAX-03 extract.</reversibility>
  <files>packages/tax-engine/package.json, packages/tax-engine/tsconfig.json, packages/tax-engine/jest.config.cjs, packages/tax-engine/src/evaluate.spec.ts, apps/desktop/src/__tests__/phase03-entities.test.tsx, apps/desktop/src/__tests__/phase03-tax-rail.test.tsx, apps/desktop/src/__tests__/phase03-autosave.test.tsx, apps/desktop/src/__tests__/auth-signed-in.ts</files>
  <read_first>
    - .planning/phases/03-entities-invoices-live-tax/03-RESEARCH.md (Pattern 4, Pattern 6 CJS, Package Legitimacy Audit — ajv not installed this plan)
    - .planning/phases/03-entities-invoices-live-tax/03-PATTERNS.md (tax-engine package.json analog vs @clared/ui)
    - packages/ui/package.json
    - apps/backend/tsconfig.json
    - apps/desktop/src/__tests__/invoice.test.tsx
    - apps/desktop/src/__tests__/auth-signed-in.ts
    - docs/clared-tax-rule-matrix.md (23 ids — list them as it.each names, do not invent CH/UK)
  </read_first>
  <behavior>
    - evaluate is not yet exported; pnpm --filter @clared/tax-engine test fails (RED)
    - Spec names all 23 matrix rule ids from docs/clared-tax-rule-matrix.md plus a 0-match throw and a 2-match throw
    - Desktop phase03-entities: without entity.create, Anlegen is disabled and hint is Nur Inhaber können Entities anlegen.
    - Desktop phase03-tax-rail: on evaluate failure the previous invoice_tax_rate remains visible
    - Desktop phase03-autosave: after the 600ms pause the canvas shows Speichert then Gespeichert (INV-01, D-05)
  </behavior>
  <action>
    Create packages/tax-engine as a private workspace package name @clared/tax-engine. Omit type module. Set main to ./dist/index.js and types to ./dist/index.d.ts. tsconfig module commonjs, declaration true, outDir dist — copy compiler shape from apps/backend/tsconfig.json, not from packages/ui. Add a jest.config.cjs (ts-jest, testRegex .spec.ts$) and a test script. Do not add ajv this plan (install is 03-02 after RESEARCH OK audit). Do not add a src/index.ts export yet so the spec is RED.

    evaluate.spec.ts (TAX-02, D-13, D-15): it.each over the 23 matrix ids exactly as in docs/clared-tax-rule-matrix.md (EU_DOMESTIC_B2B_SERVICE through THIRD_TO_THIRD_B2B_SERVICE). Each case will call evaluate(facts) and expect applied_rule_id once 03-04 fills fixtures; this plan may assert evaluate is a function so the import fails now. Add two extra tests: zero matching rules throws; two matching rules throws — never pick first or priority. Do not add CH or UK rule ids.

    Desktop: add phase03-entities.test.tsx, phase03-tax-rail.test.tsx, and phase03-autosave.test.tsx wrapped in describe.skip('phase03-product'). Entities spec (ENT-01, D-04, UI-SPEC E2): Anlegen visible; when permissions omit entity.create, button disabled and hint copy from 03-UI-SPEC Copywriting (Nur Inhaber können Entities anlegen.). Tax-rail spec (TAX-01, D-11, D-12): keep last good values and show the tax-error copy from Copywriting when evaluate fails. Autosave spec (INV-01, D-05): after typing pause the status copy Speichert then Gespeichert appears; no required Save button. Do not rewrite invoice.test.tsx or demo-states.test.tsx this plan (03-05 unsips autosave).

    Update signedInOwner.permissions to the catalog strings entity.create, kunde.write, invoice.write, invoice.read, tax.evaluate (D-04). Do not put a raw access token on SessionContext.
  </action>
  <verify>
    <automated>pnpm --filter ./apps/desktop test && (pnpm --filter @clared/tax-engine test; test $? -ne 0)</automated>
  </verify>
  <acceptance_criteria>
    - packages/tax-engine/package.json name is @clared/tax-engine and has no type module field
    - packages/tax-engine/src/evaluate.spec.ts lists all 23 matrix ids from docs/clared-tax-rule-matrix.md
    - pnpm --filter @clared/tax-engine test exits non-zero (RED)
    - apps/desktop/src/__tests__/phase03-entities.test.tsx contains describe.skip('phase03-product')
    - apps/desktop/src/__tests__/phase03-tax-rail.test.tsx contains describe.skip('phase03-product')
    - apps/desktop/src/__tests__/phase03-autosave.test.tsx contains describe.skip('phase03-product')
    - grep -v '^#' apps/desktop/src/__tests__/auth-signed-in.ts | grep -c 'entity.create' is greater than 0
    - pnpm --filter ./apps/desktop test exits 0
  </acceptance_criteria>
  <done>tax-engine spec is RED; desktop Wave 0 files exist skipped; owner fixture carries create/write/evaluate permissions; desktop CI still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spec files → future HTTP API | Assertions describe authz; no new listener this plan |
| npm workspace → packages/tax-engine | New package.json only; no registry install this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of privilege | POST /api/entities spec | high | mitigate | Viewer 403 and owner 201 cases written now; PermissionsGuard implementation is 03-02 |
| T-03-03 | Tampering | POST /api/tax/evaluate spec | high | mitigate | Spec requires invoice-shaped body and server-returned TaxDecision; desktop must not submit a decision object |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install this plan. ajv/ajv-formats wait for 03-02 and RESEARCH Package Legitimacy Audit OK |
| T-03-05 | Information disclosure | shared instance rows | medium | accept | RESEARCH A4: no tenant_id this phase; RBAC is capability not row-level until SAAS-01 |
</threat_model>

<assumption_delta_decision>
noun: shared instance / second identity source
decision: no-change
rationale: Wave 0 adds tests only. Authentik remains the sole identity source from Phase 2. RESEARCH A4 — product tables will be instance-global with no tenant_id; that is a no-change versus Phase 2, not a second tenant SoT.
</assumption_delta_decision>

<verification>
pnpm --filter ./apps/backend test:e2e
pnpm --filter ./apps/backend test -- rbac.spec.ts
pnpm --filter ./apps/desktop test
pnpm --filter @clared/tax-engine test  (expect non-zero)
</verification>

<success_criteria>
Wave 0 files listed in 03-VALIDATION.md exist. Backend and desktop CI commands still exit 0. tax-engine test is RED. signedInOwner carries product permissions.
</success_criteria>

## Artifacts this phase produces

This plan:
- `entities.e2e-spec.ts`, `invoices.e2e-spec.ts`, `tax.e2e-spec.ts` (skipped)
- `permissions.guard.spec.ts` (skipped)
- `@clared/tax-engine` package scaffold + RED `evaluate.spec.ts`
- `phase03-entities.test.tsx`, `phase03-tax-rail.test.tsx`, `phase03-autosave.test.tsx` (skipped)
- `signedInOwner.permissions` product catalog strings

<output>
Create `.planning/phases/03-entities-invoices-live-tax/03-01-SUMMARY.md` when done
</output>
